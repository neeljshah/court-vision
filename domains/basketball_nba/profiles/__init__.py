"""domains.basketball_nba.profiles -- 2K-style player/team/lineup attribute
engine. Every attribute is a documented composition of ingredients already
on disk (see attribute_registry.py). NETWORK: zero. edge_claimed: False
everywhere -- descriptive/DESCRIPTIVE presentation layer only, never a gate.
"""
