"""domains.soccer.profiles -- soccer TEAM ATTRIBUTE ENGINE (style profiles).

Registry-driven build over the statsbomb 400-match event corpus (possession +
formation attributes) and the footballdata ~25.8k-match season corpus
(finishing/home-strength attributes). See attribute_registry.py for the
per-attribute spec and build_profiles.py for the builder.
"""
