---
name: Reproduction attestation
about: Report a pass or fail running REPRODUCE.md on your own machine
labels: reproduction
---

Ran [REPRODUCE.md](../../REPRODUCE.md)'s three commands on a fresh clone.
Pass and fail reports are both welcome -- a fail gets fixed and thanked
publicly.

## Environment

- OS:
- Python version:
- Commit SHA (or clone date):

## Result

- [ ] PASS -- `check_all.py` exited 0
- [ ] FAIL -- see anomalies below

Paste the `check_all.py` summary line (the `total N pass N fail N no_check N
runtime <N>s` line) here:

```
```

## Pass / fail counts

- Pass:
- Fail:
- No-check (expected, not a failure):

## Anomalies / notes (free text)

<!-- Anything unexpected: an error you had to work around, a slower/faster
runtime than the recorded one, an OS-specific quirk. -->
