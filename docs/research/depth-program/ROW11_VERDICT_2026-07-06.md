# ROW 11 VERDICT -- soccer_intl in-game m36 ADVERSE segment tightening

Row 11 claim: soccer_intl in-game m36 ADVERSE (worse_segments UNK/H1/H2) needs
a segment x channel trust gate so ADVERSE segments tighten the floor
(suppress-only).

## VERDICT: ROW-11-RESIDUAL-EXISTS

The check code exists and is fail-closed, but on the LIVE ledger it never
reaches soccer_intl's adverse segment -- live-unreachable for this row's
target sport. Corrected from a prior (wrong) SATISFIED verdict after review.

## Root cause (reproduced this run)

**Channels are keyed by channel, not channel+sport.**
`edge_greenlight.py` `edge_greenlight()` groups settled rows only by
`_channel_of(r)` (L106-108) -- every sport in a channel lands in one bucket.
`greenlight_trust_honesty.py` `channel_trust_status()` L138-139 then
collapses that mixed-sport bucket to a SINGLE plurality sport via
`Counter(sports).most_common(1)[0][0]`. `_e_segment_trust(sport, ...)` (L104)
reads `ingame_segment_trust_multi.json` only `if sport != "mlb"`; when the
plurality is `mlb` it reads the MLB-only `ingame_segment_trust.json` instead
and never opens the multi-sport file.

**Reproduced from the live ledger this run** (`load_ledger()` ->
`_dedup_settled()` -> group by `_channel_of`):

| channel | sport counts | plurality |
|---|---|---|
| moneyline | mlb=91, wnba=4 (total 95) | mlb |
| paper_pm | mlb=175 (total 175) | mlb |
| paper | mlb=351 (total 351) | mlb |
| paper_ingame | mlb=136, soccer_intl=23 (total 159) | mlb |

Every channel resolves `sport=mlb`. Direct call:
`_e_segment_trust("mlb", root, table, {})` -> `("green", None)`,
`adverse_segments == []` -- multi-sport file never opened. A hypothetical
`_e_segment_trust("soccer_intl", ...)` DOES return
`("red", "segment_adverse")`, `adverse_segments == ["UNK"]` -- the check
logic itself is correct, but that call never happens live: `soccer_intl` is
never the plurality sport in any channel.

**Live artifact confirms it.** Fresh in-process `edge_greenlight()` and the
on-disk `data/frontend/ops/edge_greenlight.json` (identical, checked this
run) show `paper_ingame` criterion-e: `channel_trust_status: RED`,
`channel_trust_reasons: ["settled_n_or_coverage_below_60",
"same_venue_clv_ci_straddles_zero", "reconcile_not_applicable:paper_ingame"]`
-- RED is real but for reasons unrelated to soccer_intl.
`"segment_adverse"` never appears. Row-11's mechanism does not fire on live
data; this RED is coincidental cover from other floors, not this row's gate.

## What's still missing (the residual)

`channel_trust_status()` needs to check segment trust PER SPORT PRESENT in
the channel's rows, not just the plurality sport -- iterate `set(sports)`,
call `_e_segment_trust(s, ...)` per sport, RED if any sport's adverse list
is non-empty (suppress-only, as today). Until that lands, soccer_intl's
adverse segment cannot suppress any channel it shares with a larger mlb
population. File:line: `greenlight_trust_honesty.py:138-139` (plurality
collapse), `:104` (mlb-only branch skipping the multi file).

No code changed -- read-only verification, PREMISE-CHECK ONLY lane.
edge_claimed: false.
