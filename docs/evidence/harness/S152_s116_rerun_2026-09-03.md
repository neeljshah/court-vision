# S152 -- S116 re-run end to end to a NEW archive

Contract: `docs/evidence/tracking/VERIFIER_CONTRACT.md`, sections B and Q. Self-checked.
Calibration language only (Q6). SCREEN, uncharged: no prereg seal (Q1 n/a), K never read,
`_charge_ledger` never imported (Q2 n/a). SINGLE-WINDOW (Q5).

## Premise (Q8) -- CONFIRMED

The header of the landed `data/cache/eval_gate/s116_pooled_ingame_2026-09-03.csv` had 23 columns
ending `d_partial_vs_persport`; neither `model_raw` nor `market_raw` was present, so
`tests/platformkit/ingame/test_s116_pooled_ingame_a2.py` hit its
`pytest.skip("archive predates raw source columns")` and asserted nothing.

## Run

    cd /c/Users/neelj/nba-ai-system
    PYTHONPATH=. nohup python <scratchpad>/s152_rerun.py > <scratchpad>/s152_run.log 2>&1 &

The 4-line scratchpad runner
(`C:/Users/neelj/AppData/Local/Temp/claude/C--Users-neelj-nba-ai-system/94ad507e-cbc9-47ac-9667-91146366346b/scratchpad/s152_rerun.py`)
imports `scripts.platformkit.eval_gate.s116_pooled_ingame`, rebinds `run.__defaults__` to
`(OUT_DIR, "s116_pooled_ingame_2026-09-03_rerun", None)` -- `run(out_dir, stem, rows)` already
takes both as parameters, and the default binds at def time so setting `m.STEM` alone would not
move it -- then calls the module's own `main()` unmodified. THE MODULE WAS NOT EDITED.

WALL TIME: start `2026-09-03T03:58:43Z`; both artifacts written `2026-09-02 22:59:36 -0500`
= `2026-09-03T03:59:36Z`. **53 s** end to end (corpus load + 5-fold walk-forward on both sports
+ archive write). No pod contact, no network.

NEW artifacts (both under gitignored `data/`, never committed):

| path | md5 | shape |
|---|---|---|
| `data/cache/eval_gate/s116_pooled_ingame_2026-09-03_rerun.csv` | `23b3eda8abd45c46816c62aa7bc6594f` | 202,304 rows x 25 cols |
| `data/cache/eval_gate/s116_pooled_ingame_2026-09-03_rerun.json` | `b2e4f7057f4f777fbcc8f574d64a5bde` | 20,992 bytes |

LANDED ARCHIVE NEVER OVERWRITTEN: `s116_pooled_ingame_2026-09-03.csv` still has mtime
`2026-09-02 20:14:07 -0500`, 73,814,835 bytes, md5 `926fd3e3ca80296e60b4f7d548a4f90d`.

## (1) Number-by-number comparison, landed JSON vs re-run JSON

Every leaf scalar of both summaries was flattened and compared (`generated_at` and
`per_tick_csv` excluded as run-stamps): **500 identical, 60 differ**. All 60 are on the MLB
side and every one is a function of ONE input change. NBA is identical leaf for leaf.

HEADLINE POINT ESTIMATES -- BOTH REPRODUCE EXACTLY:

| number | landed | re-run |
|---|---|---|
| MLB `partial_vs_line` | `+0.012837467659961226` | `+0.012837467659961226` |
| NBA `partial_vs_line` | `-0.0003426691316624856` | `-0.0003426691316624856` |

HEADLINE MLB INTERVAL -- DOES NOT REPRODUCE; the cause is named below:

| number | landed | re-run |
|---|---|---|
| MLB `partial_vs_line` ci95 | `[-0.002273, +0.027948]` | `[-0.002376, +0.028051]` |
| MLB `n_clusters` (scored) | 63 | **54** |
| MLB `corpus.n_clusters` | 145 | **126** |

The NBA interval `[-0.0011237158544390446, +0.0004383775911140735]` is identical in both.

CAUSE (RE-LABEL). `s116_corpus.load_mlb` reads
`scripts.platformkit.eval_gate.real_game_split` (`assign_real_game_seq`, `cluster_ids`) to build
the MLB cluster id. That module was changed by S131 in commit `0c9b9d8d7`, dated
`2026-09-02 21:11:34 -0500` -- AFTER the landed archive was written at `20:14:07`. S131 requires
corroboration (a gap of at least 20 minutes, or a score reset) before an inning decrease opens a
new real game, collapsing near-instant scoreboard-feed regressions; store-wide 392 -> 360 real
games. On this SCREEN partition that is 145 -> 126 MLB clusters in the census and 63 -> 54 scored
clusters. The re-run MLB interval is therefore the CORRECTED one on the S131 cluster unit, and the
landed `[-0.002273, +0.027948]` should be re-labelled **pre-S131 cluster unit**. No bar moved
(Q3): `IMPROVEMENT_BAR` is `0.004` in both artifacts.

The 60 differing leaves, all traceable to 63 -> 54 clusters:

- every MLB `dm/*` block -- `stat`, `p_value`, both `ci95` endpoints, `n_clusters` (8 pairs x 5 = 40)
- `n_clusters` 63 -> 54; `icc_by_cluster` 0.608742 -> 0.565468; `design_effect` 93.8186 -> 101.6847;
  `n_eff` 103.0606 -> 95.0881
- `n_informative` 2,622 -> 2,613, with `n_held_model` 8,819 -> 8,828, `n_held_market`
  7,303 -> 7,312, `n_held_both` 7,047 -> 7,056 (held-value runs are detected WITHIN a cluster, so
  merging re-used tickers into fewer real games moves nine ticks across a run boundary), plus the
  four derived `tick_informative` statistics and `n_games` / `n_games_informative` 63 -> 54
- `corpus/mlb/n_clusters` 145 -> 126
- three fold counts: `folds[2].n_test_clusters` 52 -> 43, `folds[2].n_train_clusters` 850 -> 844,
  `folds[3].n_train_clusters` 861 -> 853

UNCHANGED across the two runs, to the printed digits: every Brier for both sports (MLB line
0.215528 / null 0.210827 / pooled 0.211336 / persport 0.203722 / partial 0.202690; NBA line
0.078611 / null 0.078931 / persport = pooled = partial 0.078953), all 8 MLB and all 8 NBA
`improvement` values, all NBA `dm` blocks, every fitted coefficient, every per-fold `lambda`
(MLB `[0.0, 100.0]`, NBA `[0.0, 1000.0, 0.0, 1.0, 0.0]`), `coefficient_drift` for both sports,
`n_scored_ticks` 202,304, NBA `n` 192,635 / `n_clusters` 673 / `n_informative` 78,761 / `n_eff`
2370.0394, MLB `n` 9,669, `nba_not_hurt` True and `prereg_draft_warranted` False. The verdict is
unchanged: the MLB partial arm does not clear the +0.004 bar with a lower interval bound above
zero, and NBA is structurally unhurt.

## (2) n_informative(NBA) from the NEW archive's own columns

`flag_ticks(r, game_col="cluster", ts_col="ts_utc", market_col="market_raw", model_col="model_raw")`
run on the NBA rows of the re-run CSV alone:

    nba n 192635 n_informative 78761 n_games 673
    mlb n   9669 n_informative   2613 n_games  54

**n_informative(NBA) = 78,761**, matching both the re-run summary and the landed summary -- now
computed from writer-produced raw columns in a full end-to-end run, rather than from the S149
no-refit reconstruction. The Q9 completeness gap S149 opened is closed for NBA.

n = 2 sports (CONSTRUCT, exhaustive -- the corpus has exactly two) (Q7).

## (3) Test re-pointed and PASSING

`tests/platformkit/ingame/test_s116_pooled_ingame_a2.py`: MLB keeps reading the landed archive
(its quoted numbers are the pre-S131 cluster unit and still hold there); NBA now reads
`s116_pooled_ingame_2026-09-03_rerun.csv`, whose NBA block is identical leaf for leaf and which
carries `model_raw` / `market_raw`. Both existing `pytest.skip` guards are kept and a third added
for a missing re-run archive; the raw-column skip now guards the re-run frame. No asserted value
was changed (B10/Q3): all ten Brier constants, both improvements, both intervals and both
`(n, n_informative)` pairs are byte-identical to the S142 version.

    python -m pytest tests/platformkit/ingame/test_s116_pooled_ingame_a2.py -q  ->  1 passed in 1.97s
    python -m pytest tests/platformkit/ingame/test_s116_pooled_ingame.py -q     ->  8 passed in 3.16s

## Rails

A5: the other readers of the S116 archive stem
(`scripts/platformkit/eval_gate/s148_live_requote.py`, `archive_read.py`, `s137_rebaseline.py`)
point at the LANDED path, which is untouched -- the re-run is a new file, so no reader changes
behaviour. B2 non-additive: none; the re-run CSV is the landed schema plus two columns. B5: no
pod contact. `data/registry/` untouched; the FWER ledger untouched (18 rows, md5
`a4ae7c13995672e478d59770591b83ba`, never opened); no flag flipped ON; nothing under `src/`,
`kernel/`, `api/`, `intel/`, `scripts/team_system/`; `s116_pooled_ingame.py` and `s116_corpus.py`
not edited; no push.

## NOT VERIFIED

- SINGLE-WINDOW (Q5): one MLB SCREEN corpus and one NBA SCREEN corpus, verdict sides never read.
  No second corpus, so nothing here is an AHEAD.
- The MLB headline interval `[-0.002273, +0.027948]` published by S116 is NOT reproducible from
  today's code: it is the pre-S131 cluster unit. The corrected value on the S131 unit is
  `[-0.002376, +0.028051]`. The verdict does not move, but every downstream quote of the old
  interval needs the pre-S131 label.
- MLB `n_informative` moved 2,622 -> 2,613 for the same reason. The MLB half of the a2 test still
  asserts 2,622 against the landed archive and would fail against the re-run; only NBA was
  re-pointed, as the row specified.
- The re-run archives live under `data/` and are gitignored, so this evidence is reproducible on
  this box only, by re-running the command above (53 s).
- Both sports still measure a NULL against the +0.004 bar. This is a calibration screen, not an
  edge claim, and a null is a success.
