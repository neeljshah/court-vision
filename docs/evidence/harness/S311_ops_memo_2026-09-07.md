# OPS S311 launcher hardening memo

## ATTEMPT 2 - corrections applied

- C1 -> `patched2/codex_pipeline.sh:79-93`: require the current build EXIT, extract home from that DISPATCHED row, obtain SHA from `git log master..HEAD`, and surface NO COMMIT.
- C2 -> `patched2/codex_pipeline.sh:79`: the explicit EXIT:0-after-last-DISPATCHED gate precedes every Git or lane-commit lookup.
- C3 -> `patched2/codex_pipeline.sh:114-120`: ACCEPT/CLOSED writes READY TO LAND and creates the empty land-log sentinel; it never calls codex-land.
- C4 -> `OPS_MEMO.md:34`: the deliberate-row replay procedure removes `cx_pipeline_state` (and the G state when applicable).
- C5 -> `patched2/codex_pipeline.sh:85-96`: NO COMMIT calls lane_commit.py; a validated `LANE_COMMIT <sha>` proceeds directly to verification.
- C6 -> `patched2/codex-land:13` and `OPS_MEMO.md:36`: queue-free step 9 is held until dispatcher has a caller and FIFO migration is ready.
- C7 -> `OPS_MEMO.md:20`: live codex-land-g was re-checked read-only and has no sweep_queue reference; no mirror edit was made.
- C8 -> `patched2/dispatcher.sh:23-28`: orphan blocking is scoped by `receipts/<SID>.HOME.<home>` to the requested home.
- C9 -> `patched2/dispatcher.sh:31-32`: the oldest ready row is selected by mtime, preserving FIFO behavior.
- C10 -> `patched2/dispatcher.sh:44`: an existing claimed SID is rejected before mv can overwrite it.
- C11 -> `patched2/dispatcher.sh:50-59`: crash hooks are marked test-only; never set `DISPATCHER_CRASH` in production.
- C12 -> `patched2/pod_run:14-16`: `POD_DATA_RO` is optional and defaults to the deployed `/workspace/nba-ai-system/data` symlink target.
- C13 -> `patched2/pod_run:115-122`: restore the 47 GB stop/3 GB warning and prune per-job roots older than two days.

The same corrected structure is in `patched2/codex_pipeline_g.sh:77-116`: G IDs start at G181, verification uses codex-verify-g, EXIT is scoped to the last DISPATCHED, lane_commit is supported, and NO COMMIT uses one consistent per-run marker.
Read-only C7 check on 2026-09-07: `C:/Users/neelj/bin/codex-land-g` contains no `sweep_queue` reference.

## Test

- `python tests/run_failure_matrix.py`
- `SUMMARY: 12 passed, 0 failed (portable fallback; Git Bash unavailable)`
- `python tests/validate_artifacts.py` -> `VALIDATION: 0 errors`
- The 12 cases include lane_commit-after-EXIT and G-loop NO COMMIT dedupe; no build fixture contains a manufactured hex `SHA:` line.

## Apply order copied from OPUS_REVIEW.md

Prerequisite: require a zero-failure matrix before any stage. Use whole-file copies, not DIFF patches.

1. Stage 1 - pod_run: back up live pod_run, install `patched2/pod_run`, then smoke-test `pod_run a14 -- python -c "print('pod ok')"`; roll back on remote setup failure.
2. Stage 2 - S loop: touch `codex_pipeline.stop`; wait for `pipeline stop`; back up and install `patched2/codex_pipeline.sh`; then deliberately clear replay markers with `rm -rf /c/Users/neelj/AppData/Local/Temp/cx_pipeline_state`.
3. Clear the S stop file, relaunch with the exact hidden_launch.py command recorded in OPUS_REVIEW.md, and observe a full build-to-verify plus READY TO LAND cycle; roll back if neither appears within two cycles.
4. Stage 3 - dispatcher.sh plus codex-land is HOLD: first choose a dispatcher caller and migrate the six flat rows to `sweep_queue/ready` in FIFO order; install both files only after those prerequisites exist, and retain sweep_queue.txt until ready drains once.
5. Stage 4 - G loop: touch `codex_pipeline_g.stop`; wait for `pipeline stop`; back up and install `patched2/codex_pipeline_g.sh`; clear `cx_pipeline_g_state`; clear the stop file; relaunch with the review's pipeline_g hidden_launch.py command; observe one full cycle.

## NOT VERIFIED

- No live launcher, worktree, queue, repository, commit, network, SSH, SCP, or pod was modified or exercised.
- Git Bash could not start in this managed session (CreateFileMapping error 5), so sh syntax/runtime and the real-shell matrix branch remain unverified here; the explicit portable matrix passed.
- Pod smoke test, real tar/scp, quota/headroom, cleanup against real job roots, data-root existence/read-only enforcement, and real NTFS junction/hardlink rejection remain unverified.
- Dispatcher still has no confirmed live caller; no existing flat row was migrated or drained, and codex-land performed no landing.
- Live S/G loop bounce, lane_commit against an actual sandbox-failed build, and the resulting verifier/READY transition remain unverified.
