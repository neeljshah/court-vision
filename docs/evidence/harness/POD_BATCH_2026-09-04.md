# Pod Batch 2026-09-04

Verdict: **BLOCKED AT DEPLOY PARITY (11/12).** The `/workspace` volume
reported 64 percent used, but the extraction account hit a separate quota.
The full deploy and the required single-file repair both exited 2. No process
was killed, restarted, or launched. Calibration language only; no flags changed.

## Scope and code identity

- Main-repo HEAD: `fa7954a7238dc9eea429dac444aba50092c68fe9`.
- Pod: `root@213.192.2.123:40034`, repo `/workspace/nba-ai-system`.
- Protected paths were read only. No write was made under `data/` or `vault/`
  by this lane, and no registry or FWER-ledger write was attempted.
- The only local file written by this lane is this memo.

## Step 0 - capacity gate

Command:

```bash
ssh -F ~/.ssh/config.pod pod 'df -h /workspace; du -sh /workspace/nba-ai-system/data 2>/dev/null; ls /workspace/*.log | head'
```

Exit code: 0. Evidence: `/workspace` was 929T total, 593T used, 337T
available, 64 percent used. The repo data tree was 45G. The first logs were
`bootstrap.log`, `bootstrap2.log`, `foundry_runner_s154.log`,
`g172_cv2_environment_gap_20260903_a5.log`, `keep_track_daemon.log`,
`keepalive.log`, `mlb_book_capture.log`, `paper.log`, and `track_daemon.log`.
The greater-than-90-percent stop condition was not met.

## Step 1 - deploy and parity

Full deploy command:

```bash
set -o pipefail
git -c core.autocrlf=false archive HEAD -- scripts/platformkit domains kernel api src tests docs/evidence/harness/S217_mlb_depth_capture_pod_2026-09-04.md | ssh -F ~/.ssh/config.pod pod 'cd /workspace/nba-ai-system && tar -x --no-same-owner'
```

Result: `tar_rc=2`. Tar reported `Disk quota exceeded` while extracting
multiple files and ended with `Exiting with failure status due to previous
errors`. No stderr was suppressed. The required 12-file comparison immediately
after that failed extraction was:

| File | HEAD md5 | Pod md5 | HEAD/pod bytes | Result |
|---|---|---|---:|---|
| `scripts/platformkit/foundry_runner.py` | `18dcb3e7fddc067bdfb5ec1f748787c8` | `18dcb3e7fddc067bdfb5ec1f748787c8` | 16500/16500 | MATCH |
| `scripts/platformkit/signal_foundry.py` | `ac8161e3b65078afaca035ddac401b77` | `ac8161e3b65078afaca035ddac401b77` | 13281/13281 | MATCH |
| `scripts/platformkit/ops/pod_bootstrap_check.py` | `22b694d9f00666b3d3e3df576253c4e5` | `22b694d9f00666b3d3e3df576253c4e5` | 12935/12935 | MATCH |
| `scripts/platformkit/ingame/inplay_capture_loop.py` | `b5d9ddbe316adeed3c534e31d9ee3053` | `b5d9ddbe316adeed3c534e31d9ee3053` | 71567/71567 | MATCH |
| `scripts/platformkit/ingame/cycle_history.py` | `12df710614f9636e20fc40acc9b78be6` | `d41d8cd98f00b204e9800998ecf8427e` | 2636/0 | EMPTY, MISMATCH |
| `scripts/platformkit/eval_gate/calibration_report.py` | `a1febfdc52e2206c5f3b277e4f390c47` | `a1febfdc52e2206c5f3b277e4f390c47` | 13638/13638 | MATCH |
| `scripts/platformkit/tracking_harness.py` | `856db0b3c0da4e89ba2418467d30bb6e` | `856db0b3c0da4e89ba2418467d30bb6e` | 21426/21426 | MATCH |
| `scripts/platformkit/track_daemon.py` | `6e884d8c16194c3e7809df97fe5274e2` | `6e884d8c16194c3e7809df97fe5274e2` | 21118/21118 | MATCH |
| `scripts/platformkit/ingame/mlb_book_capture.py` | `b5be8ee4996f0c1f9df77ad8b08d0c47` | `b5be8ee4996f0c1f9df77ad8b08d0c47` | 14605/14605 | MATCH |
| `scripts/platformkit/ingame/arm_registry.py` | `18919637df026e26d17dfe0b6967a182` | `18919637df026e26d17dfe0b6967a182` | 3005/3005 | MATCH |
| `scripts/platformkit/ingame/blend_apply.py` | `34269b8926171a7003f5771a553609c8` | `34269b8926171a7003f5771a553609c8` | 7481/7481 | MATCH |
| `scripts/platformkit/ingame/boxscore_read.py` | `10b4fe34e87227c3a65d08475c9a24c8` | `10b4fe34e87227c3a65d08475c9a24c8` | 7562/7562 | MATCH |

The S217-related ninth file is `mlb_book_capture.py`, named by the S217 memo.
The three additional in-game files were `arm_registry.py`, `blend_apply.py`,
and `boxscore_read.py`.

Required repair command:

```bash
set -o pipefail
git -c core.autocrlf=false archive HEAD -- scripts/platformkit/ingame/cycle_history.py | ssh -F ~/.ssh/config.pod pod 'cd /workspace/nba-ai-system && tar -x --no-same-owner'
```

Repair result: `tar_rc=2`, again `Disk quota exceeded`. Final recheck remained
0 bytes with md5 `d41d8cd98f00b204e9800998ecf8427e`. A bounded search found no other
`cycle_history.py` under `/workspace`; no replacement, deletion, or relaunch
was attempted. Final parity is **11/12**.

## Step 2 - bootstrap check

Command: `PYTHONDONTWRITEBYTECODE=1 python scripts/platformkit/ops/pod_bootstrap_check.py`.
Exit code: 1. Reported probes: **0/7; NOT VERIFIED**, because import failed
before probe execution:

```text
ImportError: cannot import name 'MLB_EXISTENCE_FLOOR_PROBES' from 'scripts.platformkit.ops.preflight_floors'
```

The dependency was then measured directly: local HEAD
`scripts/platformkit/ops/preflight_floors.py` is 1,197 bytes with md5
`936a5eef26e74967f5c5ee813133d6b3`; the pod file is 0 bytes with md5
`d41d8cd98f00b204e9800998ecf8427e`. This is additional deploy damage outside
the required 12-file table.

## Step 3 - foundry runner

The required `/proc` scan skipped its own shell and required argv0
`/usr/local/bin/python` plus `foundry_runner`. It found no matching process,
so pid before was `none` and its prior arguments could not be read. The newest
matching log was `/workspace/foundry_runner_s154.log` (6,835,769 bytes). Its
last three lines were:

```text
screen_failed tier=T0 family=nba:asof_team_adv feature=home_ast_pct_asof reason=ScreenRefused: unavailable: home_ast_pct_asof not found one-row-per-event in no frozen family source
screen_failed tier=T0 family=nba:asof_team_adv feature=away_pace_asof reason=ScreenRefused: unavailable: away_pace_asof not found one-row-per-event in no frozen family source
promotions_held family=nba:boxdetail_asof count=1 reason=allow_charge_off
```

No signal was sent. A relaunch was prohibited by 11/12 parity and unavailable
same-argument evidence. Pid after is `none`; the requested new-log first five
lines are NOT VERIFIED.

Read-only SQLite URI query of
`data/cache/eval_gate/hypotheses.sqlite` (23,883,776 bytes) reported:

- queued = 43,536 rows (`queue.claimed_at IS NULL`)
- claimed = 2,184 rows (`queue.claimed_at IS NOT NULL`)
- done = 1,387 `result` rows (1,387 distinct hash/tier pairs; T0 758, T1 629)
- excluding same-tier completed rows: 43,286 queued and 1,676 claimed; 758
  queue rows have a same-tier result
- claimed owners: pid labels 55054 with 1,068 rows and 55269 with 1,116 rows;
  neither pid existed in the measured `/proc` census

## Step 4 - S217 capture

Skipped; pid = `skipped`. HEAD memo
`docs/evidence/harness/S217_mlb_depth_capture_pod_2026-09-04.md` records a Q8
premise-falsified closure, names no launch or dry-run command, and says files
to deploy are none. The live capture heartbeat did enumerate September 4 MLB
games, but the absent command is decisive. No substitute command was invented.

## Step 5 - bounded measurements

### S19/S55 paper stack

No process matching `python -u -m supervisor --profile paper` existed. Direct
supervisor child count from `/proc` was therefore 0. No supervisor action was
taken. The last three lines of `/workspace/paper.log` were:

```text
2026-09-03 14:17:41,298 INFO supervisor.supervisor: supervisor: launched m33_http_wedge_reaper pid=37086
2026-09-03 14:17:42,310 INFO supervisor.supervisor: supervisor: launched m43_settle_sweep pid=37176
2026-09-03 14:17:43,328 INFO supervisor.supervisor: supervisor: launched m44_exec_evidence pid=37268
```

### S32 in-play capture

`/proc` showed pid 36783 running
`/usr/local/bin/python -u -m scripts.platformkit.ingame.inplay_capture_runner`.
Stdout was `/workspace/nba-ai-system/logs/m2_inplay_capture.out` (175 bytes)
and held one startup line; its restricted non-calibration token is not copied
here. Stderr was `logs/m2_inplay_capture.err` (3,100 bytes); its final logical
lines reported HTTP 429 responses for WNBA and MLB, followed by NUL padding.

Today's exact cycle store was named by the deployed code contract as
`data/cache/ingame_cycle_history/2026-09-04.jsonl`. It was missing, so today's
stored cycle-tick count was **0**. Separately, the current heartbeat
`data/cache/ingame_grade/_capture_heartbeat.json` was 18,216 bytes and had
`as_of=2026-09-04T00:59:56Z`; daemon heartbeat
`data/cache/daemon_heartbeats/m2_inplay_capture.txt` carried the same time.

### S33 MLB depth capture

No process cmdline contained `mlb_book_capture`. `/workspace/mlb_book_capture.log`
was 0 bytes, so it had no last-three-line payload. No action was taken.

### Utilization

```text
GPU utilization: 0 percent; memory used: 676 MiB
uptime: 147 days 15:13; load average: 9.97, 13.09, 18.56
RAM GiB: total 1007, used 81, free 164, buff/cache 774, available 926
swap GiB: total 0, used 0, free 0
```

### Partial files

Read-only command:
`find /workspace/nba-ai-system/data -type f -name '*.part' -printf '%s %p\n'`.
Count: **5**. Nothing was deleted.

| Bytes | Path |
|---:|---|
| 1,983,643,648 | `/workspace/nba-ai-system/data/footage_bridge/baseball__npb_04.mp4.part` |
| 1,440,022,528 | `/workspace/nba-ai-system/data/footage_bridge/football__football_m8UWuQoflJo.mp4.part` |
| 3,705,238,507 | `/workspace/nba-ai-system/data/footage_bridge/football__football_wHZt1eY3A9s.mp4.part` |
| 1,478,557,696 | `/workspace/nba-ai-system/data/footage_bridge/tennis__tennis_03.mp4.part` |
| 1,344,339,968 | `/workspace/nba-ai-system/data/footage_bridge/wnba__wnba_02.mp4.part` |

Final capacity recheck remained 64 percent at `/workspace`; the repo data tree
remained 45G. This confirms that volume use alone did not expose the separate
write quota.

## NOT VERIFIED

- Full deploy integrity is not verified; tar exited 2 and parity is 11/12.
- The exact set of all files affected by the failed full extraction was not
  enumerated. Two confirmed empty files are `cycle_history.py` and
  `preflight_floors.py`.
- Bootstrap probe outcomes are not verified; execution stopped before probe 1.
- Foundry same-argument restart, new pid, and new-log first five lines are not
  verified because no matching pid existed and parity did not clear.
- S217 execution and resumability are not verified because its memo contains no
  command and closes at the premise.
- Supervisor liveness is not verified; only absence of the supervisor cmdline,
  a zero direct-child count, and the existing paper-log tail were measured.
- Today's S32 cycle-history rows are not verified beyond the measured missing
  file; the live heartbeat confirms the runner was still cycling.
