# S311 OPS applied -- 2026-09-07

Applied after two Opus reviews (attempt 1: C1-C17; attempt 2: R1-R4 on top). Installed files are
`patched2_fixed/*` (patched2 + R1-R4), not the as-submitted `patched2/*`.
## What was applied
- `pod_run`: argv sent JSON+base64, decoded remotely into `subprocess.run(argv)` (no remote-shell
  re-split); `safe_path` rejects `..`/absolute/drive-letter/backslash and checks every ancestor for
  symlink/NTFS-reparse/hardlink; tar+ssh statuses split via PIPESTATUS (tar rc<=1 = ok, "file
  changed while reading"); fetch only after sentinel parses and `remote_rc -eq 0`; 47 GB stop / 3 GB
  warning guard restored; job roots under `jobs/<job-id>` pruned past 2 days; `POD_DATA_RO` opt-in,
  defaults to the deployed `/workspace/nba-ai-system/data` symlink (R2: tar rc<=1 not ==0; R3:
  ship-manifest CRLF fix so `tar -T` can stat paths on Windows).
- Both loops (`codex_pipeline.sh`, `codex_pipeline_g.sh`): EXIT read only after the last DISPATCHED
  row; verifier bound to that row's SID/home/SHA; `lane_commit.py` called in the NO COMMIT branch, a
  valid `LANE_COMMIT <sha>` proceeds to verify same-tick; ACCEPT/CLOSED writes READY TO LAND + empty
  land-log sentinel, never calls codex-land; G-loop dedupe bug fixed (broken string match replaced
  by `note_once`); R1 moved the verify-log sha read above the git fallback (fixes a NO-COMMIT loop
  on landed rows); R4 gates the sha read and commit to the newest build log actually naming that
  home (fixes committing another lane's in-flight work under a stale SID).
## Backups (in `C:\Users\neelj\bin`)
`pod_run.bak_2026-09-07_preops`, `codex_pipeline.sh.bak_2026-09-07_preops`,
`codex_pipeline_g.sh.bak_2026-09-07_preops`.
## Test matrix
`run_failure_matrix.py`: 9 passed, 3 failed (shell fixtures -- harness defects, not `patched2`).
`run_loop_matrix2.py patched2_fixed`: 6 passed, 0 failed (real Git Bash).
Smoke: `pod_run a22 -- python --version` -> `POD_RUN_DONE ... rc=0`.
## Bounce
Stop files 17:47:36 -> `pipeline stop` 17:47:45(S)/17:47:38(G) -> install + `sh -n` 17:47 -> stop
files removed 17:48:04 -> `pipeline start` 17:48:12(S), 17:48:55(G).
## HELD
`dispatcher.sh.proposed` (staged, no caller); `codex-land` step 9 (would drop the only
`sweep_queue.txt` drain) -- both blocked on an orchestrator decision, not installed.
## NOT VERIFIED
Pod `/workspace/data-ro` existence; `dispatcher.sh` end-to-end queue drain; a post-bounce
NO-COMMIT->lane_commit->verify cycle in production (fixture-proven only); real NTFS
junction/hardlink rejection; an unexplained extra `pipeline start` at 17:54:20 (no surviving
process, harmless).
