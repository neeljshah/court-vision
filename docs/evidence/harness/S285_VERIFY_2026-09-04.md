VERDICT: ACCEPT
Candidate: `fe43ebd9e5c67191cd0ff8b969bfbc39692f7c55`; full stat/diff and all 128,339 CSV lines inspected; five additive files.
REFS: `memo` = `S285_event_conformal_width_2026-09-04_retry3.md`; `result` = adjacent JSON; `pair` = adjacent paired-loss CSV; `preregistration` = `S285_preregistration_event_conformal_width_2026-09-04_v2.md`; `script` = `scripts/platformkit/eval_gate/s285_event_conformal_width.py`; `test` = `tests/platformkit/ingame/test_s285_event_conformal_width.py`; `spec` = `docs/evidence/tracking/specs/S285_spec.md`.
PASS ACCEPTANCE: 217 near/121 settled clusters; both coverage gaps are -0.066666667 with lower CI below 0, hence NULL under the frozen rule; S265/S276 artifacts, S123, and 600 MiB limit are unchanged (`memo:11-20`; `spec:22-36`; `script:28`).
PASS PREMISE: claimed = reproduced 79,919 ticks/269 games, p50=13, p90=136, near=40,051, settled=7,870, middle=31,998; planted-future-row prior values unchanged (`result:92-98`; `script:176-187`).
PASS NUMBERS 0.90: claimed = reproduced coverage near/settled/pooled 1/0.933333333/1; half-width 0.168602183/0.120114296/0.149771680; coverage gap -0.066666667 CI [-0.142857143,0.060000000]; width gap 0.048487887 CI [0.032294004,0.063200542] (`result:168-229`).
PASS NUMBERS 0.80: claimed = reproduced coverage near/settled/pooled 1/0.933333333/1; half-width 0.124319017/0.035307979/0.084881664; coverage gap -0.066666667 CI [-0.180000000,0.093437500]; width gap 0.089011037 CI [0.080324731,0.094650302] (`result:105-166`).
PASS REPRODUCTION: all 128,338 archive rows parsed; 64,169 unique states/nominal, zero malformed or source-distance mismatches; SHA matched; maximum numeric delta 5.55e-17 (`pair:1-128339`; `result:88-90`).
PASS B1: whole-sample p50/p90 are fixed before scoring; all held-out ticks are near, settled, or named middle exclusions (`script:179-211`; `spec:34-36`).
PASS B2: all five paths are additions; no field/status is renamed or removed and importer census found no existing reader (`test:10`).
PASS B3: this audit adds no quarantine or absent-evidence gate (`script:108-115,176-235`).
PASS B4: no claim or retry lifecycle is introduced (`script:176-235`).
PASS B5: reported compute route is pod scratch under the B5 note; scorer contains no deployment operation (`result:87`; `script:176-235`).
PASS B6: no file moved or retired; no orphan import or module reference exists (`test:10`).
PASS B7: the complete scored evaluator archive was reproduced, not a leading slice (`pair:1-128339`).
PASS B8: static widths fit train folds and score held-out folds only (`script:84-105`; `result:16-84`).
PASS B9: evaluator units are 64,169 unique tick states/nominal across 217 games, with nonconstant event distances (`script:194-200`; `pair:1-128339`).
PASS B10: 0.05/0 and 600 MiB bars match master/spec; S101 400/50 grouping values remain unchanged (`spec:26-33`; `script:28,210,225-226`).
PASS Q1: seal `b5accd1d3e1f80877e1d44a699838501363726958d6a68cb090d89f14d5a76c7` reproduces; prereg commit `1cb0ebd41` at 15:18:28 CDT predates first preserved metric at 15:26:11 CDT (`preregistration:1-80`; `result:86,100-102`).
PASS Q2: prereg declares this uncharged with no K or ledger access (`preregistration:72-78`).
PASS Q3: decision rule, 2,000 resamples/seed 2850904, grouping, and memory bar match prereg/spec (`preregistration:45-76`; `result:10-15`).
PASS Q4: shared walk-forward uses game purge and symmetric one-day embargo for every held-out fold; no meta-learner (`script:84-105`; `result:16-84`).
PASS Q5: result is NULL, not a superiority claim, so a second corpus is not invoked (`result:166,229`).
PASS Q6: all candidate files decode as ASCII and the restricted-claim scan is clean (`memo:1-30`; `script:1-242`; `test:1-32`).
PASS Q7: near/settled bins contain 217/121 game clusters at each nominal, both >=30 (`result:119,151,182,214`).
PASS Q8: whole sealed-sample premise was independently remeasured before adjudication (`result:92-98`; `script:179-187`).
PASS ADDITIVITY/READERS: name-status is five additions; only the spec test imports the new module; no reader behavior changed (`test:10`).
PASS LOC: touched Python files are 242 and 32 lines, both <=300; repository LOC rail passed (`script:1-242`; `test:1-32`).
PASS EVIDENCE: memo, JSON, CSV, preregistration, script, and test all exist and decode as ASCII (`memo:25-30`; `result:88-102`).
TEST: `python -m pytest tests/platformkit/ingame/test_s285_event_conformal_width.py -q -p no:cacheprovider` -> 1 passed in 4.63s (local).
TEST: `python -m pytest tests/platformkit/test_loc_rail_scope.py -q -p no:cacheprovider` -> 1 passed in 1.87s (local).
PASS RSS: verifier archive replay peak 223,981,568 bytes; claimed pod scorer peak 311,398,400 bytes; both below 629,145,600 (`result:233-237`).
CORRECTION (minimal memo diff, outside adjudication): append `## NOT VERIFIED` and `- Historical pod scratch isolation and the reported scorer peak RSS were not independently observed.`
CORRECTION (minimal helper diff, outside adjudication): at `script:63,73`, retain the pre-sort index and reindex the result to `frame.index`; add one unsorted-input fixture.
NOT VERIFIED: historical pod scratch isolation; candidate scorer peak RSS (archive-only verifier RSS was measured); full raw-to-band regeneration was disallowed by read-only verification.
2026-09-04 | NBA in-game calibration | S285 | static-band coverage gap -0.066666667 at both nominal levels; 95 pct CIs [-0.142857143, 0.060000000] and [-0.180000000, 0.093437500] | NULL (verified: codex-sol, contract A/B/Q)
NEW GAP: S285-MEMO-NOT-VERIFIED - candidate memo has no explicit NOT VERIFIED list (`memo:1-30`); apply the minimal memo correction above.
NEW GAP: S285-UNSORTED-DERIVATION - public helper output is misaligned for unsorted input although the scorer sorts first (`script:58-73`); apply the minimal helper correction above.
