# S19 pod 30-pass readout -- INSUFFICIENT

GAP S19 | mlb (execution) | the verifier's post-deploy pod step. Contract
`VERIFIER_CONTRACT.md` (A2, B7, Q3); spec `specs/S19_spec.md`; module memo
`docs/evidence/harness/S19_request_governor_2026-09-03.md`.

## Relaunch and metric mapping
S19 was deployed by S21 (`a0018f9fb`) and `run_pod_capture` relaunched at
**pid 21622** (cwd `/workspace/nba-ai-system`, `CV_CAPTURE_POD=1` and
`CV_MLB_BOOK_ARCHIVE_LIVE=1` from `/proc/21622/environ`), first cadence row
**2026-09-02T14:15:22.631837Z**. The pod clock reads 2026-09-02 UTC, so the
shard is `data/cache/ingame_books/mlb/2026-09-02.jsonl` (the register labels the
relaunch 2026-09-03; this readout uses the pod clock).
The spec metric "capture pass wall seconds" is the cadence row's
**`tick_latency_sec`** = `round(clock() - started, 3)` bracketing exactly one
`capture_once` call (`mlb_book_capture.py:281`); `achieved_cadence_sec` is
start-to-start spacing, not the wall. There is no `n_tickers` field -- the
per-pass fetch count is **`n_snapshot_rows`** (18 on a 9-game slate).

## Rows found since the relaunch
n = **41** cadence rows, 14:15:22.631837Z to 14:35:22.632837Z (>= the 30 asked
for). `tick_latency_sec`: **median 0.401 s, p90 0.426 s** (min 0.393, max
0.623). `n_live_games` = 0 and `n_snapshot_rows` = 0 in all 41;
`target_cadence_sec` = `achieved_cadence_sec` = 30.0 (the idle path). Across the
whole 2026-09-02 shard (844 rows) `n_live_games` is 0 in every row.

## Verdict: INSUFFICIENT
The spec denominator is one full pass at **9 games x 2 sides = 18 orderbook
fetches**. Every measured pass has 0 live games and issues 0 governed fetches:
`capture_once` returns after discovery. The 0.401 s wall is an **empty pass**,
so the `<= 15 s median AND p90` bar was never exercised -- neither PASS nor
FAIL. The bar is unchanged (Q3).

## Pre-deploy live window (BEFORE, not AFTER) and re-run window
`2026-09-01.jsonl` holds the only live passes on record: 6 cadence rows,
23:51:07.921477Z to 2026-09-02T00:00:19.793998Z, each `n_live_games = 9` and
`n_snapshot_rows = 18` -- the exact spec denominator. `tick_latency_sec` =
111.508, 110.706, 109.801, 119.978, 106.759, 104.612; **median 110.254 s, p90
119.978 s** (n = 6). Serial path; reproduces the spec's `before = ~105 s`.
Baseline only, and n = 6 < 30 regardless. That slate went live at 23:51 UTC, so
re-run over **2026-09-02T23:50Z - 2026-09-03T00:50Z** (US evening): near 110 s
30 passes need ~55 min, at the 5 s target under 15 s ~8 min.

## Recompute command (any window; only live passes score -- the honesty rail)

```
python3 -c "import json,statistics,sys;p,s,e=sys.argv[1:4];r=[json.loads(l) for l in open(p)];w=sorted(x['tick_latency_sec'] for x in r if x.get('record_type')=='cadence' and s<=x['capture_ts']<e and x['n_live_games']);print('n',len(w),('median %.3f p90 %.3f'%(statistics.median(w),w[-(-9*len(w)//10)-1])) if w else 'NO LIVE PASSES -- INSUFFICIENT')" /workspace/nba-ai-system/data/cache/ingame_books/mlb/2026-09-02.jsonl 2026-09-02T23:50 2026-09-03T00:50
```

## NOT VERIFIED
- The `<= 15 s median AND p90` bar: 0 of 41 post-deploy passes carry the
  9-games-x-2-sides denominator; 30 pod passes at it remain unrun.
- No claim the concurrency change helps or does not help on the pod -- the
  empty-pass wall measures discovery only, not the 18-fetch path.
- The 09-01 rows are pre-deploy, n = 6, BEFORE only; not verified that that
  process ran identical discovery code to pid 21622.
- Read-only on the pod: nothing killed, started, deployed or copied; pid facts
  from `/proc` only. Q6 clean -- INSUFFICIENT is an honest measurement.
