# Astra round 13 (2026-09-21 evening): next twelve rows, baseline output checks, prompt changes, deadline assessment

Source: codex design lane (read-only), archived verbatim by the orchestrator. Design text, not a measurement.

1. Next twelve build rows

Paths: I = scripts/platformkit/ingame; E = scripts/platformkit/execution; O = scripts/platformkit/ops. Each row owns only its listed files, matching test files, and a uniquely named acceptance memo. Pending prerequisite lanes must land before dependent integration. Tests run one file at a time.

1) Budgeted capture scheduler; Problem: breadth prevents usable cadence. Before: docs/evidence/tracking/specs/S341_spec.md R10 fixes are accepted and deployed. CHANGE: deterministic pregame admissions, separate provider budgets, pagination priority, bounded retries, admission reduction on missed deadlines. Files: I/capture_scheduler.py, I/local_capture_runner.py. Acceptance: 48-hour counts show selected-book cadence within frozen limits, submission book/state ages within 5/15 seconds, and no budget overruns; unchanged snapshots count. Size M; unblocks forward collection. Parallel: 2-4 after capture ownership release.

2) Write trades as pages drain; Problem: fetched prints remain vulnerable in memory. Before: inspect I/local_capture_runner_trades.py and I/local_capture_client.py after fix 1e for retained multi-tick batches. CHANGE: durably append each page before persisting its cursor; advance completeness watermark only after closure; preserve original receipts and explicit gaps. Files: those two modules only. Acceptance: kill between append, sync and checkpoint; restart loses no acknowledged prints, conserves distinct IDs, and never fabricates completeness. Size M; unblocks reliable tape. Parallel: 1, 3-4.

3) Laptop resilience and independent capture; Problem: one host can interrupt every series. Before: docs/evidence/harness/ASTRA_ROUND12_2026-09-21.md section 5 identifies laptop interruption. CHANGE: OS-managed hidden restart, reboot recovery, sleep prevention while collecting, resume-gap reporting, free-space refusal, bounded shard rotation, archive-growth watchdog, independent-host deployment recipe. Files: O/capture_service.ps1, O/capture_health.py, O/capture_shards.py. Acceptance: reboot/network/disk-full drills preserve checkpoints and expose gaps; overlapping captures retain host identity. No second credential-free host is verified. Size M; unblocks continuity. Parallel: 1-2, 4.

4) NBA corpus readiness; Problem: converter output is not yet a usable second corpus. Before: I/nba_checkpoints_to_joined.py emits nba_checkpoints_r1, absent from I/baseline_four_arm_features.py's mapping. CHANGE: publish under a supported name; materialize, hash, census and audit model/label availability, including last-tick close_ts semantics. Files: converter, I/nba_corpus_readiness.py. Acceptance: production loader accepts output; reconcile 465,249 ticks and 232,951 model matches; disclose fourth-quarter concentration; count scored games after embargo. Size M; unblocks 9. Parallel: capture work.

5) Enforce certified fees; Problem: cent-boundary undercharging compromises fee-netted markout. Before: python -m scripts.platformkit.execution.fee_time_certification --self-check reports discrepancies. CHANGE: refuse scored execution while certification fails; prepare the exact Decimal correction for owner disposition. Files: E/fee_certification_gate.py; docs/research/organization-sprint/PROPOSED_R13_fee_correction.md. Acceptance: boundary and cumulative partial-fill fixtures agree after authorized correction; invalid inputs refuse; every fill carries schedule identity. Size S; unblocks 6-7. Parallel: 1-4. Existing fee module remains owner-controlled.

6) Rebuild forward replay; Problem: rejected integration bypassed canonical interfaces. Before: E/quote_reservation.py and E/capture_book_adapter.py exist; S369-S371 acceptance memos confirm fixes. CHANGE: availability-time policy loop, venue-time fill eligibility, canonical position ledger, reservations, adapter-only books, independent expiry/cancellation timers and late-print reconciliation. Files: E/forward_replay.py, E/forward_replay_io.py. Acceptance: real captured fixture traverses the complete chain; fractional conservation, restart equivalence, future-record isolation, unchanged marks and same-game identity pass. Qualification computes no losses. Size L; unblocks 7. Parallel: 4, 8, 10.

7) Forward preregistration and charged wrapper; Problem: collection needs binding adjudication rules. Before: row 6 qualification output passes; row 5 certification passes; I/s359_four_arm_trial.py's canonical-ledger guards are accepted. CHANGE: seal populations, dates, families, timing, horizons, exclusions, concentration and sensitivities; charge before scoring with atomic attempt reservation and launch K. Files: E/forward_trial.py; docs/evidence/ingame/R13_FORWARD_PREREG.md. Acceptance: wrong ledger, changed seal, concurrent launch and reused attempt refuse; crash remains charged; July stays NOT RUN. Size M; unblocks qualifying adjudication. Parallel: 9-10.

8) Four-arm output auditor; Problem: a plausible summary can conceal invalid evidence. Before: docs/evidence/ingame/S347_PREREG_SEALED_2026-09-21.md and accepted S359 wrapper exist. CHANGE: implement the three output checks below, without invoking fitting. Files: I/four_arm_output_audit.py. Acceptance: seeded denominator, chronology, sign and charge defects refuse verdict release; absent audit evidence is explicit. Size M; unblocks trustworthy MLB interpretation and row 9. Parallel: all collection work; finish before verdict viewing.

9) NBA preregistration and charged trial; Problem: many source games do not establish powered independent calibration evidence. Before: row 4 manifest and eligibility census pass the actual scorer loader. CHANGE: freeze NBA coverage, phases, provenance, embargo and comparisons before scoring; document prior S86 use and correction requirements at launch K. Files: I/nba_four_arm_trial.py; docs/evidence/ingame/R13_NBA_PREREG.md. Acceptance: canonical charge precedes one run; identical-arm populations, adequate scored-game counts, concentration and cross-corpus requirements survive row 8. Size M; unblocks second-corpus assessment. Parallel: 6-7, 10.

10) Live NFL situation verification; Problem: adapter situation fields remain fixture-only. Before: domains/nfl/ingest_nfl_states.py marks yardline_100 semantics_unverified. CHANGE: capture raw live payloads and verify possession, down, distance, yard-line orientation, timeouts, clock and two-team linkage against observed transitions. Files: domains/nfl/verify_live_situation.py, domains/nfl/situation_normalize.py. Acceptance: possession reversal and period transition validate mappings; missing fields remain unknown; receipts bound availability. Size M; unblocks reliable NFL cohorts and state-dependent replay. Parallel: 1-9.

11) Commission family/week evidence; Problem: component counts do not establish consecutive qualifying weeks. Before: E/family_week_ledger.py passes S365 verification and row 7 fixes family definitions. CHANGE: ingest hashed daily qualification artifacts, expose scheduled denominators and gaps, forecast both families separately. Files: E/family_week_commission.py. Acceptance: missing weeks break streaks; shared games cannot establish independence; zero-fill games remain visible; forecasts use observed qualification rates. Size M; unblocks an auditable eight-week trajectory. Parallel: 9-10.

12) Point-level tennis source probe; Problem: neither historical data nor current ESPN capture supplies qualifying point state. Before: RESULTS_LEDGER_SYSTEM.md's dated S361 finding records zero usable historical matches. CHANGE: bounded live probe of public sources, starting with the [official ITF service](https://www.itftennis.com/en/world-tennis-tour-live/). Files: domains/tennis/live_point_probe.py. Acceptance: demonstrate credential-free points, server, games, sets, identities, receipts and reconnect behavior, or publish BLOCKED. No qualifying programmatic source is currently verified. Size S; conditionally unblocks forward tennis. Parallel: everything.

2. Distrust and three output checks

Distrust either direction if exclusions change, training availability is unproven, folds have inadequate training, optimization fails, a few games dominate, or phase/tick weighting reverses interpretation. Captured model_prob does not prove its underlying inputs were timely. The state-bearing selection excludes much of the original population. D versus A alone cannot establish incremental model contribution beyond C.

Before reading the verdict:

1) Reconcile provenance and accounting: committed seal, corpus/code hashes, canonical charge, launch K and attempt identity; 31,433 raw ticks, 29,887 eligible ticks, 176 eligible games, 12,339 transitions and four warmup folds. Eligible counts are not scored-cell counts.
2) Audit archived keys and chronology: identical observations across arms; output outcomes match evaluator records; held-out games never train themselves; every training close precedes the embargo boundary. Require training-only preprocessing evidence.
3) Reconstruct losses, weights, intervals and leave-one-game-out summaries from saved predictions, without refitting. Check candidate-minus-A signs, concentration and cell counts. A favorable interval remains SINGLE-WINDOW. An interval spanning zero does not establish MATCH.

3. Five prompt changes

- Supply verbatim real-row fixtures and actual consumer schemas before coding.
- Require the failing reproduction first, then preservation of existing contracts.
- Freeze one authoritative spec revision; amend it before changing instructions.
- Specify clocks, units, identities, durability ordering and refusal behavior at every boundary.
- Require an end-to-end real-row handoff, per-file tests and checked process exits before submission; construct-test totals alone are insufficient.

4. Deadline assessment

The path remains possible, but its schedule is unproven: no qualifying forward weeks exist, cadence and continuity remain unresolved, and two independent families need demonstrated coverage. A 56-day window beginning November 5 ends December 31; eight complete ISO weeks require an earlier November 2 start. NBA is the strongest second-calibration-corpus route, but it does not supply forward weeks. The owner's most useful single decision is to provision a dedicated always-on second capture host now, retaining the laptop as an independently identified overlapping collector.
