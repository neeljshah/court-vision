# S20 PREMISE -- paper stack measured from /proc (2026-09-02 04:25-04:40 UTC)
Lane S20 (paper-live week keystone) + the new S30. READ-ONLY on the pod: nothing killed, started,
scp'd or committed there. Calibration / CLV-series language only. Plan under test: docs/research/
organization-sprint/PLAN_EXECUTION_ANSWER_LAYER_2026-09-03.md sections 0-1. Pod reached via the
Host alias: ssh -F ~/.ssh/config.pod pod (60 s timeout, no hardcoded port).
## RECOMMENDATION (first)
DO NOT LAUNCH `python -m supervisor --profile paper` tonight. Two measured reasons.
1. Boot is kill-then-respawn, not adoption. supervisor.py:171 calls `_reconcile_survivors()` then
   `_launch(st)` unconditionally; _restart.py:53 is explicit -- "Kill any ALREADY-RUNNING child of
   each spec BEFORE the launch loop"; :62 resolves `find_by_match`, present on posix (proc.py
   :54-64 -> proc_posix.py:220); :75-79 calls `sv._proc.kill(handle)` = SIGTERM/3s/SIGKILL
   (proc_posix.py:182-205). All 11 live paper children match the bare module string and die.
2. The respawn leaves the placer chain DOWN. m1_producer readiness is HEARTBEAT on data/frontend/
   predict_service/_heartbeat.json, fresh_sec=1500 (stack_specs.py:32,267); that file's pod mtime
   is 2026-08-31 19:53 -- 32.7 h stale at 04:38 UTC. `_await_ready` polls 15 times
   (supervisor.py:178), so m1_producer never reads READY and `_deps_ready` (supervisor.py:126)
   leaves m1_api_paper PENDING and m1_paper PENDING behind it. :8099 answers 200 today; a boot
   now takes it down with no measured path back inside the boot window.
Nothing is gained tonight: the three dead paper members (m20, m27, m33) all act on ledger rows and
there are zero rows; the real S20 blocker is upstream of execution (section 2). Read-only
pre-flight before any future launch -- launch only once that mtime is under 1500 s old:
`ssh -F ~/.ssh/config.pod pod 'cd /workspace/nba-ai-system && stat -c "%y %n" data/frontend/predict_service/_heartbeat.json; date -u'`
## 1. Every python process on the pod, from /proc (never pgrep)
22 /proc entries matched "python" at 04:31 UTC: 1 is the enumerating shell (3182016), 3 are `bash
-c` wrappers (3066729, 3691619, 3713440), leaving 18 live interpreters. Read per pid: PPid from
status, `stat -c %y /proc/<pid>`, cwd, `readlink /proc/<pid>/fd/1`; paper profile = the 14 names in
config/boot/paper.json. ALIVE (11 of 14), every one ppid 1, cwd /workspace/nba-ai-system,
fd/1 logs/<name>.out: 3186458 m1_producer (predict_service.scheduler --interval 600) start 08-31
19:53:47; 8394 m1_api_paper (predict_service.app) 16:02:42; at 16:02:45 -- 8605 m1_paper
(pm_trading.auto_loop --forever), 8606 m1_bankroll (paper.bankroll_daemon), 8607 m2_inplay
(odds_provider.inplay_runner), 8608 m2_inplay_capture (ingame.inplay_capture_runner), 8609
m12_pm_paper_tick, 8610 m17_kalshi_scan, 8611 m18_pm_close_capture; at 16:02:46 -- 8994
m43_settle_sweep, 8995 m44_exec_evidence.
DEAD (3 of 14): m20_ingame_clv_verdict, m27_ingame_paper_settle, m33_http_wedge_reaper -- no /proc
entry. m33's heartbeat froze at 08-31 20:12 alongside m9_supervisor.txt and both left a 0-byte
.tmp at 20:13: the supervisor died mid atomic write and took those three with it. No `python -m
supervisor` process exists now. Heartbeats at 04:33 UTC: m2_inplay/m12/m18 04:33,
m2_inplay_capture 04:31, m1_paper 04:27, m1_bankroll 04:24, m17/m43/m44 04:02. No m1_producer file
in daemon_heartbeats/ -- its readiness file is the predict_service one above.
NEVER IN THE PAPER PROFILE (7 live, untouched): 577 jupyter-lab; 3040635 mlb_book_capture.
run_pod_capture (fd/1 /workspace/mlb_book_capture.log); 3066730 tracking.basketball_floor_gate;
3127047 track_daemon --workers 10 --forever; 3691621 retrain_loop; 3713444 foundry_runner; 3749549
queue_runner (data/footage_queue_mlb.json).
## 2. Why 0 bets all-time -- the pairing path, not the placement path
data/frontend/paper_today.json: n_placed_alltime 0, n_settled_alltime 0. data/cache/ingame_grade/
_capture_heartbeat.json at 2026-09-02T04:36:33Z: n_live 0, n_pairs 0, n_bets 0, n_settled 0,
cycle_duration_sec 33.006, n_requests_total 19, n_429_total 1. Its 88-game list by (sport,
reason), denominator 88 games in that cycle: tennis no_live_state 60, mlb no_live_state 16, kbo
no_live_state 5, npb no_live_state 3, mlb no_model_prob 2, npb no_model_prob 2; paired=true 0 of
88. That cycle is off-hours and proves nothing alone (no MLB game is live at 04:36 UTC); the
load-bearing evidence spans BOTH live windows:
* data/cache/ingame_grade/{mlb,soccer_intl,tennis}/*.jsonl = 25 + 1 + 354 files, 380 rows, one row
  per file, all 380 carrying the identical 7-key schema {sport, game_id, ts, settled, home_win,
  state_summary, edge_claimed}, every row settled=true / FINAL. `grep -l model_prob data/cache/
  ingame_grade/*/*.jsonl` returns 0 files -- capture_pair_once has NEVER written a paired row.
* Those settle rows were written DURING the 09-01 evening window (mlb 401816771.jsonl mtime Sep 2
  04:28, 401816764 03:21, 401816769 03:34, 401816768 03:02, 401816766 02:32), so the settle writer
  ran while games finalized -- only the pairing side is missing.
* data/frontend/exec_evidence_series.jsonl: 38 hourly snapshots, 2026-08-31T15 to 2026-09-02T04.
  Denominator 38/38: every one reports n_gated 0, n_ungated 0, clv_by_market {}, realized_ingame
  {}. The placement funnel is empty for the whole run.
* data/frontend/clv_ledger.jsonl (DEFAULT_LEDGER, clv_ledger.py:49) does NOT exist on the pod;
  record_ingame_bet writes that path when path is None (paper_ingame.py:175) -- one fill creates it.
The rows carry no per-tick decision field of their own -- the only reason field lives in the
overwritten heartbeat -- so the refusal is derived from the code path: inplay_capture_loop.py
:794-816 returns reason="no_live_state" when neither ls_fn(sport, gid) nor _scan_live_by_legs can
bridge a Kalshi ticker (e.g. KXMLBGAME-26SEP022210STLLAD) to an ESPN live state, and that return
precedes _build_tick (:851) and _dt.on_tick (:869) entirely.
S19 pass wall confirmed: /workspace/mlb_book_capture.log is 0 bytes, mtime 2026-09-01 23:49, for
pid 3040635 -- nothing written in 4.8 h; cadence rows carrying tick_latency_sec could NOT be
checked (empty log, no archive located read-only). logs/m2_inplay_capture.err (1628 B) holds Kalshi
HTTP 429 lines for mlb/KXMLBSPREAD, soccer_intl/KXWCGAME + KXWCTEAMTOTAL, wnba/KXWNBASPREAD +
KXWNBATOTAL; logs/m2_inplay_capture.out (350 B) holds two startup lines only.
## 3. Code parity, pod vs local HEAD (`tr -d '\r' | md5sum`)
IDENTICAL (3): ingame/inplay_capture_loop.py 629cd0f0f814edca331f9b7c60777a7b; ingame/
inplay_capture_runner.py 67d9977b33e3ba2d2c7a734f768ecf15; execution/thresholds.py
b5331840cca78c1ca07b4639d0d0e32f. DIFFERS (3, pod vs local): execution/paper_maker.py
99f451961276786b3eedec6530ebafd3 vs 62f36ddf34147041d98dff0b753860db; grade_paper_one.py
b25591488f755d291ecb5da484ded470 vs 8597ea1e3ea8e4a4bc3adaf7140a150b; ingame/inplay_daytrader.py
23927f09a22789ac49890d34ac062f70 vs c7cf4ea8fa1239398f84784e2d447102. ABSENT ON POD (1): ingame/
inplay_daytrader_maker.py (local b79f88119550b3fafac86b89047fc6f7). thresholds.ORDER_MODE read
(not imported) on the pod: line 33 `ORDER_MODE: str = "maker_only"`.
PLAN SECTION 0 IS FALSIFIED TWICE. (i) Its "NOT verified" note says the pod shows no
inplay_capture_runner; pid 8608 IS that runner, alive 36.6 h. (ii) Its claim that all three S20
blockers are already on the pod is false for all three: the pod's paper_maker.py is 99 lines with
no _market_suspended and no suspended_at_entry (local HEAD has both, paper_maker.py:22-36,76);
grade_paper_one.py differs; inplay_daytrader_maker.py is absent. The pod runs the PRE-SPLIT
429-line inplay_daytrader.py, which still carries the entry chain (stale_state :360-365,
ORDER_MODE :366, maker.quote :369) -- functional-but-older, not broken by the split.
## 4. Adoption behaviour if the paper profile booted now
Answer (a) no: it neither adopts nor duplicates -- it kills, then respawns; deciding lines quoted
in RECOMMENDATION 1. Duplicate writers are therefore NOT the launch risk; a DOWN placer chain is.
_singleton.py:78-112 would GRANT the lock (no live supervisor holds it), so the boot would not be
refused. Settlement: m43_settle_sweep = paper.settle_sweep_daemon -> settle_sweep.sweep()
(settle_sweep_daemon.py:26), pid 8994, ppid 1, beating 04:02, INDEPENDENT of the supervisor, which
has been dead since 08-31 20:12 while m43 kept beating. The in-game settler
m27_ingame_paper_settle is the dead one, with nothing to settle while the ledger is absent.
## 5. Writer env on 8608 / 8609 (`tr '\0' '\n' < /proc/<pid>/environ`)
Both carry exactly CV_PROP_MAX=0, CV_WC_PROP_MAX=0, NBA_AI_SUPERVISED=1 -- the three global_env
entries of config/boot/paper.json, so they WERE launched by a supervisor running --profile paper.
CV_LEDGER_WRITER, CV_CAPTURE_POD and CV_MLB_BOOK_ARCHIVE_LIVE are all ABSENT; per
writer_identity.py:36-45 an unset CV_LEDGER_WRITER on a posix host returns True, so that absence
does NOT block ledger writes. The other two absences are unmeasured in effect.
## 6. Local per-file tests (master, not a worktree)
tests/platformkit/ingame/test_inplay_daytrader.py -> 41 passed, 0 failed; test_maker_only_wiring.py -> 12 passed, 0 failed.
## Stop-rule risks against the plan's four integrity stops
(a) Two writers: not created by a supervisor boot (section 4 kills first); created only by
    hand-relaunching a daemon whose twin is alive, or by a second posix host. The local Windows box
    is refused by writer_identity (os.name == "nt").
(b) Gross grading, gross_legacy_rows() > 0: unevaluable -- the ledger file is absent, so the count is 0 by construction, not by verification.
(c) Capture dead > 6 h: would NOT fire today -- the daemon is ALIVE (heartbeat 04:31, 5 min old)
    yet has produced 0 paired rows in 36.6 h. The rule keys on daemon liveness, so it is blind to
    the failure actually present; S30 should add "zero paired rows across a completed live window"
    as its own alarm. (d) unit_result without outcome: n/a at 0 rows.
## What clv_daily_readout.py must read (it exists neither locally nor on the pod)
DEFAULT_LEDGER is absent on the pod, so the module must (1) treat the missing path as an honest
zero -- n_filled 0, n_settled 0, verdict NO_DATA -- never create it, never synthesize a row, never
print a verdict word off an empty series; (2) read the three artifacts that DO exist and name their
denominators: paper_today.json (n_placed_alltime 0), exec_evidence_series.jsonl (38 hourly
snapshots, 38/38 with n_gated 0 and n_ungated 0), and data/cache/ingame_grade/*/*.jsonl (380 rows,
380 settle-label, 0 paired); (3) write data/frontend/analytics/execution_status.json, ALSO absent
on the pod today, so the execution_status MCP tool has no live artifact to serve.
## THE ONE most likely reason the maker pool has never filled
The capture path has never produced a PAIRED tick, so the placement primitive was never reached.
Three independent artifacts agree: 380/380 ingame_grade rows carry only the settle-label schema and
0 files contain "model_prob"; 38/38 hourly exec snapshots report n_gated 0 / n_ungated 0 across
both live windows; and clv_ledger.jsonl -- the file record_ingame_bet creates on its first write
(paper_ingame.py:175) -- does not exist. The upstream refusal is live-state resolution at
inplay_capture_loop.py:794-816, which returns before _build_tick (:851) and on_tick (:869). With no
tick, enter_new_position never runs, maker.quote never rests an order, and the pool has nothing to
cross. The 5 s MLB state-age ceiling (quote_freshness.py:44) and the 30 s maker TTL floor
(paper_maker.py:71) against a 33.0 s cycle are plausible SECOND-order refusals, unreachable today.
## NOT VERIFIED
* Per-tick reason distribution DURING the 08-31 and 09-01 live MLB windows: the heartbeat is
  overwritten and no per-cycle history exists, so only the 04:36 off-hours snapshot could be
  quoted; the "never paired" claim rests on the corpus and the absent ledger, not on that cycle.
* Whether cadence rows carry tick_latency_sec (mlb_book_capture.log is 0 bytes, no archive found).
* Effect of the absent CV_CAPTURE_POD / CV_MLB_BOOK_ARCHIVE_LIVE on capture behaviour.
* Behavioural content of the three DIFFERS files beyond the greps quoted in section 3.
* Whether m1_producer (pid 3186458) is hung or merely not writing that heartbeat file.
