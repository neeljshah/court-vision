# S267 attempt 1b preregistration

Date: 2026-09-04
Machine: local worktree `C:/Users/neelj/nba-track-a18`, because this is a source-only schema-consumer repair; no pod operation is authorized.

Intent: add explicit, immutable consumer summaries to `build_report` without moving its existing default top-level values. Update the three named consumers to read the positional summary.

Inputs, opened one at a time: `docs/evidence/calibration/nba_reliability_2026-09-03.json` (7774 bytes), `docs/evidence/calibration/mlb_reliability_2026-09-03.json` (7700 bytes), `docs/evidence/calibration/soccer_reliability_2026-09-03.json` (8772 bytes), and `docs/evidence/calibration/tennis_reliability_2026-09-03.json` (7856 bytes). These JSON inputs have no raster resolution.

Binding construct: for each sport, reproduce `build_report` twice, first with no ordering arguments and second with `order_by="event_date", unit_col="corpus_unit"`. Record the nine-key `positional` and `per_unit` summaries emitted by each call. Compare each basis's `ece_after` to the fixed S50 targets: nba positional 0.024842541854003943 and per_unit 0.02658254607099417; mlb positional 0.008076735465020577 and per_unit 0.012666184733512256; soccer positional 0.009302164221488884 and per_unit 0.028722315180213532; tennis positional 0.008402944939872484 and per_unit 0.015403176684314482.

Acceptance bar: all three consumers explicitly read `positional`; the eight fixed-basis ECE values match the targets within 1e-9; every flip-invariance maximum absolute difference is exactly 0.0; default top-level values are unchanged. This is an exhaustive 4-sport construct, not an ahead claim. No threshold may change.

Method: `build_report` already uses strict expanding out-of-fold calibration. The construct has no model-arm comparison; its callback-free output is rechecked from the shared evaluator surface only through the existing `walk_forward` purge (48 hours) and symmetric `cpcv_evaluate` embargo interface, which remain untouched. Every stored quantity is emitted by the report callback under test; no pod deployment occurs.

Artifacts to create after sealing: `docs/evidence/harness/S267_key_explicit_consumers_2026-09-04.md`, `docs/evidence/harness/S267_key_explicit_consumers_<sport>_2026-09-04.json` for all four sports, and `scripts/platformkit/eval_gate/test_s267_key_explicit_consumers.py`.

Seal-SHA256: 2334148630D369F34734A3567C137233F17FC197221A0B5080DDEAA820EF909A
