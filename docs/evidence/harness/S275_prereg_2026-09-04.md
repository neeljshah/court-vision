# S275 preregistration: explicit calibration-basis consumers

Status: SEALED BEFORE SCORING.

Machine: local worktree `C:/Users/neelj/nba-track-a16` (the corpus cache and
shared evaluator are local; no pod operation is authorized or required).

## Scope

This preregistration binds S275 only. The binding premise is a fresh census of
the three named implicit consumer reads, confirmation that `build_report()`
does not publish `positional` or `per_unit`, and fresh reports at both bases for
every sport. If that premise is false, S275 closes FALSIFIED and no change or
scored acceptance comparison is made.

If the premise holds, the additive change is limited to explicit `positional`
and `per_unit` report dictionaries, backward-compatible explicit consumer
reads, and one focused construct test. Top-level default-path keys and values,
the S50 references, `cpcv_engine.py`, and `walkforward.py` remain unchanged.

## Inputs and fixed measurements

The eight immutable targets are the existing small JSON artifacts, opened one
at a time: `docs/evidence/calibration/{nba,mlb,soccer,tennis}_reliability_2026-09-03.json`
for positional, and the matching `_reliability_per_unit_2026-09-03.json` files
for per-unit. Each input path, byte size, and JSON `ece_after` value will be
recorded in the evidence memo.

For each sport, `build_report(load_gate_corpus(sport), sport)` is positional;
`build_report(load_gate_corpus(sport), sport, order_by="event_date",
unit_col="corpus_unit")` is per-unit. The existing report route retains its
walk-forward expanding-fit evaluation contract; this change does not fit a new
model or alter its evaluator.

The fixed acceptance comparisons are all four sports by both bases: each
`ece_after` must differ from its S50 target by at most `1e-6`. A second fresh
run must differ from the newly committed S275 reference JSON by at most `1e-9`.
No sport, basis, row, or corpus unit may be excluded. The constructed
flip-invariance comparison is the three named consumer readings across all four
sports for positional and per-unit calls, and every absolute difference must be
exactly `0.0`.

## Evidence and verdict

The committed evidence will be
`docs/evidence/harness/S275_key_explicit_consumers_v2_2026-09-04.md`, one new
dated reference JSON with eight scalars, one second-run reproduction JSON, and
a flip-invariance diff table. It will state the preregistration path and this
seal, all sixteen comparison values, the three read-site confirmations, input
identity, and focused-test result. Language is limited to calibration outcomes.

Seal SHA-256 of every LF byte above this line: C05180271ABE4519D8C4E4BB529901BD42103E95C7C5BA7CAF0B7710400C3627
