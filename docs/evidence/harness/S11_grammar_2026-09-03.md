# S11 -- hypothesis grammar + semantic hash (verifier reproduction)

Verdict: **ACCEPT WITH CORRECTIONS**. Codex died before self-reporting; its work was
recovered from worktree a12 as `8fe7631cc`. Every number below was reproduced by the
verifier in MASTER, not quoted from the lane.

## ACCEPTANCE RULE outcome

| item | spec | reproduced in master |
|---|---|---|
| metric | deduped distinct hypotheses enumerated | **116,370 distinct `semantic_hash`** |
| denominator | enumeration over the on-disk catalogues of step 1 | 264,330 enumerated rows over 69 present parquets / 979 columns |
| before | 0 (`scripts/platformkit/foundry/` absent, no `semantic_hash` anywhere) | premise re-verified 2026-09-03: package absent, `grep -rn semantic_hash scripts/ tests/ kernel/ src/ api/` = 0 hits |
| bar | >= 1,000 deduped rows AND 0 collisions | 116,370 >= 1,000 PASS; **0 collisions** PASS |
| n | 1,000 (scored) | exceeded by 116x |
| reproduction | verifier re-runs `enumerate_family` in master and recounts | done, independent script, not the lane test |

Arithmetic: 979 columns x 9 transform variants (`raw`, `ew@{3,5,10,20}`,
`rank_in_league`, `z_vs_league`, `delta_vs_prior`, `ratio_to_opponent`) x 2 conditionings
x 3 horizons x 5 markets = 264,330 predicted = 264,330 enumerated (exact match).
147,960 rows fold into an existing hash because a column name repeats across parquets of
the same sport -- that is the dedup the row asks for, and it happens by hashing, not by
filtering after the fact.

## Catalogue existence (step 1) -- ABSENT paths NAMED, never assumed

32 named catalogue entries checked plus two globs
(`data/cache/pit/opp_allowed_asof_*`, `data/cache/ingame/*states*`).

PRESENT: 69 files. ABSENT / SKIPPED: **5**

- `data/domains/soccer/asof_discipline_features.parquet`
- `data/domains/tennis/asof_features_wta.parquet`
- `data/domains/tennis/asof_return_wta.parquet`
- `data/domains/tennis/asof_meta_wta.parquet`
- `data/domains/tennis/schedule_density_wta.parquet`

Premise correction (Q8): the REDTEAM_SIGNAL_FACTORY memo said 7 absent (discipline + 6
tennis `_wta` mirrors). Measured today it is 5 -- `asof_hold_wta.parquet` (20 cols) and
`asof_setdetail_wta.parquet` (40 cols) DO exist and were enumerated.

## REDTEAM prerequisites

- SF-5/SF-6 closed alphabet: `grammar.py` does not import `regime_calibration`.
  Conditioning arrives only as explicit frozensets on the family spec and is validated
  against a hardcoded closed alphabet (`phase`, `rest in {B2B,RESTED,NORMAL}`,
  `month=YYYY-MM`, `confidence in {T1,T2,T3}`). PASS.
- SF-8/SF-9 runtime_available: `enumerate_family` requires one explicit bool per column
  and raises otherwise; `runtime_contract.classify_feature` is never called. PASS.
  `ValueError(runtime_available requires one explicit declaration per column)` reproduced.
- SF-12 off-grid: `ValueError(ew halflife must be one of {3, 5, 10, 20})` reproduced from
  both `semantic_hash` and `enumerate_family`. An absent parquet raises
  `ValueError(catalogue parquet is absent: ...)`. PASS.
- Permutation: permuted conditioning + a differently-named unused parameter hash
  IDENTICALLY (reproduced True). One grid step apart (`halflife` 5 vs 10) hashes
  DIFFERENTLY (reproduced True).

## Contract checks

- LOC: `grammar.py` 181, `test_grammar.py` 54, both `__init__.py` 1 -- all <= 300.
- Dependencies: stdlib + pandas only; no new dependency.
- Writes: `grep -rniE "to_parquet|to_csv|open\(|write|_charge_ledger|backtest_fwer"` over
  the package returns nothing. `data/registry/signal_registry.parquet` is opened read-only
  through `pd.read_parquet` and only when it exists. No trial is charged -- enumeration is
  not a trial.
- must-not-move: `regime_calibration.py`, `signal_foundry.py`, `foundry_runner.py`,
  `scripts/platformkit/eval_gate/` thresholds and `data/registry/` are all clean in
  `git status`. `python -c "import scripts.platformkit.foundry_runner"` still succeeds.
  `data/cache/eval_gate/backtest_fwer.jsonl` is 14 rows -- the spec cites 13, the 13 -> 14
  charge was S06's single charge on 2026-09-03, untouched by this row.
- A5 name clash: no pre-existing `scripts/platformkit/foundry/` package; the flat
  `foundry_runner.py`, `signals/foundry_run.py` and `analytics_showcase/mechanism_foundry.py`
  are distinct module names and are untouched.
- Q6: calibration language only; no dollar, ROI, profit or edge word, no retracted figure.

## Tests

`python -m pytest tests/platformkit/foundry/test_grammar.py -q -p no:cacheprovider`
-> **3 passed** in MASTER (the worktree also reported 3 passed).

## Corrections applied at landing

1. `scripts/platformkit/foundry/__init__.py` (named by the spec CHANGE step) is NOT landed
   and must not be: `.gitignore:342` `scripts/**/_*` ignores every `__init__.py` under
   `scripts/`, so `scripts/platformkit/__init__.py`, `eval_gate/__init__.py` and
   `signals/__init__.py` are all untracked today and every one of those packages resolves
   as a PEP 420 namespace package. `foundry` matches its siblings exactly and imports
   cleanly (3 tests pass in master). Filed as a gap, not forced past the ignore rule.
2. This memo did not exist in the commit; the verifier wrote it from its own reproduction.

## NOT VERIFIED

- The 116,370 count is a CONSTRUCT enumeration, not a scored or sampled metric. No
  hypothesis here has been evaluated, priced, promoted or charged against the FWER ledger.
- The catalogue spec used for the reproduction is the verifier's own reading of spec step 1
  plus two globs; a different sport-labelling of the `data/cache/ingame/*states*` files
  changes the distinct count (the earlier all-NBA labelling gave 116,370 -> 111,240). The
  ENUMERATED total (264,330) and the collision count (0) are invariant to that choice.
- `Hypothesis` carries no `family` or `runtime_available` field, so the teacher-lane vs
  runtime-lane split is not recoverable from an enumerated row -- S13/S14 must carry it.
- `_REGISTRY_PATH` is relative, so `signal_id` resolution silently degrades to raw column
  names when the process cwd is not the repo root. Not exercised beyond repo-root runs.
- No claim is made that any of these hypotheses is predictive. Calibration language only.
