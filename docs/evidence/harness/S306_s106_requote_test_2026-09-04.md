# S306 S106 requote direct-importer test

Date: 2026-09-07
Spec: `docs/evidence/tracking/specs/S306_spec.md`
Contract self-check: `docs/evidence/tracking/VERIFIER_CONTRACT.md`, sections B
and Q. This is an enumerated CONSTRUCT result (n = 4), not a scored
calibration comparison.

## Premise first

The exact binding before-condition was re-run before the change:

```text
0 direct importer tests for scripts/platformkit/eval_gate/s106_requote.py
```

The four landed rows naming this route are S106 (landed `34c90bc99`), S121
(landed `d71f411fb`), S137 (landed `9bd7e0552`), and S143 (landed
`bc778706b`). The premise therefore held and the additive test was required.

## Preregistration

Preregistration: `docs/evidence/harness/S306_s106_requote_test_prereg_2026-09-04.md`

Seal SHA-256 (LF bytes above the seal line):
`4986edbfa8b20c917e87f41adf0fbb77899801b72a362b2af5e27a6bd4db8f10`

The preregistration was written and sealed before the construct comparison.
Sandbox policy denied index access, so the seal was computed from LF-normalized
bytes above the seal line rather than staged bytes. The test reads that file,
normalizes CRLF to LF, and verifies the seal without `git show HEAD`. The
orchestrator commits these files by explicit pathspec (`lane_commit`).

## Inputs and route identity

| Input | Bytes opened | Resolution |
|---|---:|---|
| `scripts/platformkit/eval_gate/s106_requote.py` | 8,777 | n/a (Python source) |
| `docs/evidence/harness/S306_s106_requote_test_prereg_2026-09-04.md` | 1,447 | n/a (Markdown) |

`s106_requote.py` SHA-256 before and after:
`6556ed197cbf6e2f18cd60d4c5237c35379f09821b31542a6803cec8fe5b2c7a`.
The module is byte-identical; only the additive test and evidence files were
created. No store path was opened and no data file was written.

## Construct result

The fixture has three rows; `seq_by` maps only `(A, 2026-07-05T20:00:00Z)` to
sequence 2. It includes the literal corrected cluster `A#2` and an unmatched
`B` tick.

| Behaviour | Exact assertion | Result |
|---|---|---|
| Matched sequence | matched row has `real_game_seq == 2` and `cluster == A#2` | PASS |
| Unmatched sequence | unmatched B row has `real_game_seq == 1` and `cluster == B#1` | PASS |
| Row identity | attached frame has 3 rows; coverage is `n_rows=3`, `n_unmatched_in_joined_store=2` | PASS |
| Before correction reproduction | `_pair(..., game, loss)` CI equals direct `_quote(..., game, loss)` CI `[0.09999999999999967, 0.10000000000000037]` | PASS |

Test command: `python -m pytest tests/platformkit/eval_gate/test_s106_requote.py -q -p no:cacheprovider`

Result: `4 passed in 0.42s`.

Sign convention for any delta in the exercised route: improvement = baseline
loss minus candidate loss; positive = candidate better. No delta is reported
by this construct test.

## Verifier self-check

- B1-B10: no metric filtering, schema change, gate change, deployment, moved
  file, or threshold change; route module is unchanged.
- Q1: preregistration path and verified seal are recorded above before the
  construct comparison.
- Q2, Q4, Q5, Q9: not applicable; no charged or out-of-sample comparison,
  evaluator execution, corpus claim, or paired-loss archive was created.
- Q3: the specification's 4/4 bar is unchanged.
- Q6: calibration language only.
- Q7: n = 4 is the exhaustive enumerated construct set; reproduction is the
  focused test.
- Q8: the exact direct-importer before-condition is quoted above.

## NOT VERIFIED

- No production archive was read, so archive-level re-quote outputs were not
  reproduced here.
- No out-of-sample calibration comparison or shared evaluator run was needed
  for this local construct test.
- No commit SHA can be verified in this sandbox; files are ready for the
  orchestrator's explicit-path `lane_commit`.
