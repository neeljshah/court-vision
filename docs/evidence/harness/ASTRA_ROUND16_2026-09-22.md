# ASTRA ROUND 16 (2026-09-22, evening) -- gpt-6-astra, read-only design lane (cx_astra_r16)

Archived verbatim by the orchestrator. Candidate ids are allocated by the orchestrator only (NEXT_GAP_ID S406 at archive time).
Context given to the lane: landed today S387, S394, S401, S403, S404, S402, S400, S399; in flight S390, S393, S395, S396, S397, S398, S405.

1. **candidate 1: Attest schedule identities independently.** S404 trusts mappings without book-team evidence. Before/quote: S404_spec.md A1, "missing_directional_team_evidence 19". Own new `scripts/platformkit/ops/schedule_identity_attestation.py`. Accept only mappings supported by independent event metadata, including outcome orientation; swapped keys must refuse. Probe all six scheduled games, especially TORBAL. Waits on S403/S404. False PASS risk: treating the schedule itself as corroboration.

2. **candidate 2: Make qualification reproducible after snapshot deletion.** Live growth previously prevented qualification. Before/quote: S390_spec.md A3, "input_changed_during_run" on the "186 MB" shard. Own new `scripts/platformkit/ops/qualification_prefix_reproduce.py`. Accept identical counts from reconstructed, hash-verified prefixes; unavailable bytes mean NOT VERIFIED. Probe growing real shards and a torn final line. Waits on S390. False PASS risk: replaying newer bytes under an older digest.

3. **candidate 3: Close the remaining NBA reconstruction gap.** S405 does not itself resolve every unauditable check. Before/quote: S395_spec.md A3, "C.means NOT_AUDITABLE". Own new `scripts/platformkit/ingame/nba_means_reconstruction.py` and modify `nba_four_arm_output_audit.py` there. Accept independent reconstruction of every applicable mean; absent required fields block release. Probe real census populations through the producer/auditor seam without scoring. Waits on S395/S405. False PASS risk: importing producer arithmetic as independent verification.

4. **candidate 4: Complete the single NBA release attestation.** Assembled text is insufficient closure evidence. Before/quote: S402_nba_seal_package_2026-09-22.md, "do-not-seal parses to eight." Own new `scripts/platformkit/ingame/nba_release_attestation.py` and `docs/evidence/harness/nba_release_attestation.md`. Accept eight substantive closures, verified corpus hashes, sealed pins, guarded K=19-to-20 charge before metrics, and audited output. Probe real package, then ONE authorized trial before verification. Waits on S402/S395/S405/candidate 3. False PASS risk: an existing evidence file contains no closure.

5. **candidate 5: Locate Kalshi's decimal contamination.** The other venue also loses usable observations. Before/quote: S398_spec.md A3, "'expected a decimal string': 45". Own new `scripts/platformkit/execution/kalshi_decimal_census.py`. Accept complete refusal accounting by field, producer version and record type, preserving original bytes. Probe the entire named shard and fresh receipts. Waits on S398. False PASS risk: converting archived floats to strings and calling them original precision.

6. **candidate 6: Resolve venue units before certification.** Fee and quantity semantics remain unresolved. Before/quote: S399_polymarket_certification_2026-09-22.md, "55 PASS, 37 MISMATCH, 0 ERROR and 3 UNVERIFIED." Own new `scripts/platformkit/execution/venue_unit_evidence.py` and `docs/evidence/harness/venue_unit_evidence.json`. Accept dated, market-specific evidence for both venues; unknowns retain refusal. Probe fresh metadata against independently derived expectations. Waits on S399/S398. False PASS risk: selecting whichever interpretation matches the calculator.

7. **candidate 7: Audit cross-venue temporal causality.** Pair discovery has not demonstrated usable synchronized observations. Before/quote: S398_spec.md A3, "paired_instants 0 still". Own new `scripts/platformkit/execution/cross_venue_causal_audit.py`. Accept causal joins, explicit future-receipt exclusions and closed denominators; descriptive symmetric matching stays separately labelled. Probe only post-fix-1e archives, reporting receipt offsets and episodes. Waits on S397/S398. False PASS risk: future observations appear contemporaneous.

8. **candidate 8: Establish real signal-feature support.** Static declarations overstate available inputs. Before/quote: S396_spec.md A2, "all 60 seal as EVALUABLE", despite "27 such classes" requiring unavailable inputs. Own new `scripts/platformkit/eval_gate/signal_feature_census.py`. Accept all 146 inventory identities accounted for, finite-feature support and truncation invariance, without fitting. Probe whole-window support distributions before sealing families. Waits on S396; charging follows NBA. False PASS risk: aliases inflate independent evidence.

9. **candidate 9: Freeze per-sport residual populations.** Existing model provenance cannot support new residual interpretations. Before/quote: S402_nba_seal_package_2026-09-22.md, "Every constant is UNKNOWN". Own new `scripts/platformkit/ingame/state_residual_prereg.py` and `docs/evidence/ingame/state_residual_programs.json`. Accept sport-specific C-minus-B contracts with receipt-causal inputs, phases, exclusions and independent-game counts; D stays descriptive. Probe real state availability for each sport without scoring. Waits on S393 and NBA disposition. False PASS risk: another venue duplicates the same games.

10. **candidate 10: Bind sizing declarations to evidence.** Well-formed digests do not establish a licence. Before/quote: S400_spec.md A1, "licensed() turns on any two distinct well-formed digests." Own new `scripts/platformkit/execution/paper_sizing_evidence_resolver.py`. Accept verified seals, audited results, corpus independence, launch-K requirements, certification and completed-week history; otherwise zero, with no runtime activation. Probe actual evidence artifacts. Waits on S400/S395/S390. False PASS risk: copied results masquerade as separate corpora.

PRE-MORTEM

1. Prefix copying tears a line. Visible: parser refusals increase. Before 22:35Z, confirm byte counts, digest checks and unchanged reader refusal accounting.
2. Snapshots span inconsistent cutoffs. Visible: unmatched receipts or missing tape closures. Check common as-of filtering and every input's prefix boundary.
3. Wrong state root is read. Visible: state_key_absent, zero coverage. Check resolved production paths, including the v2 state location.
4. A keyed mapping is wrong. Visible: potentially clean counts despite misattribution. Independently check all six identities before start.
5. Capacity selection replaces the denominator. Visible: three games instead of six. Check six retained entries and exactly three served flags.
6. Qualification runs prematurely. Visible: low decisions and coverage over 3,600,000 ms. Check full-window completion and next-day shard discovery.
7. Replay uses the two-second TTL. Visible: coverage capped near 40 percent. Check declared profile hash, five-second tick and five-second TTL.
8. Tape silence masks interruption. Visible: suspiciously zero gaps despite reconnects. Check supervisor intervals and backfill-closure evidence.
9. Revisions manufacture a week. Visible: duplicated games or inflated streak. Check deterministic revision selection, seven daily artifacts and completed-week-only appends.

NOT VERIFIED:
- All requested documents were read.
- Live archives, process state, canonical launch K, in-flight diffs and current venue documentation were not inspected.
- No execution checks, network requests, edits or commits were performed.