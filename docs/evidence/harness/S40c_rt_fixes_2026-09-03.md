# S40c -- harness-core red-team remainder (RT-2, RT-10, RT-12..17)

Lane E1, main repo, 2026-09-03. Closes the REMAINING open list on register row S40
after S40 (5a2c9618e) and S40b (7ef4d6c6c). Source of every finding:
`docs/evidence/harness/REDTEAM_HARNESS_CORE_2026-09-03.md` (memo d4e29f46c).

Every finding below was REPRODUCED on the code as it stood before the fix, then
re-measured after it, and carries one regression test that fails on the old code.
Reproduction scripts live only under the session scratchpad; no repo file outside
the table was touched. Calibration language only -- nothing here is a market claim,
and every counter added is a denominator, never a result.

| id | before (reproduced) | after (measured) | commit |
|----|---------------------|------------------|--------|
| RT-2 | `hedge_trial_arms.py:177` keyed the Hedge cache on `id(train)`. Inside the real `cpcv_evaluate` on 64 synthetic states: **28 splits, 448 predictor calls, only 3 distinct `id(train)` addresses, 400 calls served a stale split's state**, and the cached predictor disagreed with an uncached refit on **400 of 448** rows. Weights fit on another split's games -- which can include the current test block -- priced the test rows. | Key is `tuple(sorted(game_id for train))`. Same run: cached and uncached predictors agree on **448/448** rows. The regression test fails on the old key (asserted by swapping it back). | 870a01ac1 |
| RT-13 | `hedge_trial_arms.py:160-163` synthesizes `home`/`away` from the game_id suffix. MEASURED: `mlb-2026-06-28-NYY-BOS` -> `home=''`, `away='BOS'` and `mlb-2026-06-29-LAD-SFG` -> `home=''`, `away='SFG'`, so `_same_team` is **True for two games with disjoint real teams**; a Kalshi ticker `KXMLBGAME-26JUL011310AAABBB0` -> `home='BB0'`, `away='AAB'`, the real codes shifted by the trailing sequence digit. | Every state carries `team_key_source = TEAM_KEY_SOURCE` naming the synthesis, so `n_train` and any "purged by team" wording is not read as a team dimension. **No number changes.** The old per-file assertion `len(s["home"]) == 3` pinned the synthesis as correct; it is replaced by the measurement above. **PROPOSED, not applied:** carrying the real `home`/`away` through `load_corpus` -- it changes `n_train` on both corpora. | 3b3045444 |
| RT-10 | `hedge_trial_runner.py:229,236` passed `charge if sport == "mlb" else None` and hardcoded `"mlb"` as the ledger sport. MEASURED on the real artifact `data/cache/eval_gate/hedge_trial_2026-09-01.json`: `mlb` and `soccer_intl` both carry `k_cumulative=12` with `ledger_row=None` -- two corpora scored, the second inheriting the first block's K. | `sport_charge(spec, sport)` charges once per (predictor, corpus) under the corpus's own sport name. Test (no real ledger touched, `_charge_ledger` monkeypatched): two corpora produce two rows with sports `["mlb", "soccer_intl"]` and `k_cumulative` `[1, 2]`. Charging every corpus can only RAISE K, which tightens the per-test bar; it never loosens it. | 3b3045444 |
| RT-12 | `combo/nested_cv.py:133` scored an EMPTY sealed holdout. MEASURED: 3 game_ids over `n_folds=5` leaves outer folds 3 and 4 empty; `holdout_fold=3` returned `n_holdout_games=0`, `outer_score=0.0`, raised nothing and set no status -- a caller reading `outer_score` could not tell a 0-game holdout from a real one. | `select_then_score` raises `ValueError("sealed outer holdout is empty: fold 3 of 5 holds 0 of 3 game_ids; ...")`. A 40-game corpus still scores (`n_holdout_games=10`, `outer_score=0.25`) and the selector still never sees a holdout id. Adds `combo/test_nested_cv.py`, the per-file test the module docstring already promised (none existed). | 6754f1226 |
| RT-14 | `eval_gate/retro_correction.py:12,31` hardcoded `RETRO_SWEEP_TRIALS = 85` and the prose "60 current catalog classes" with nothing tying either to the catalog. MEASURED 2026-09-03: `len(catalog_signals()) = 60`, `RETRO_SWEEP_TRIALS = 85`, and the module contains no assertion at all. | The pre-registered width is UNCHANGED (it must not shrink to the surviving catalog), but `render_report` raises `ValueError` when `len(pairs) > RETRO_SWEEP_TRIALS` -- a `raise`, not an `assert`, so `python -O` keeps the guard (RT-11's lesson). The prose count is rendered from `len(pairs)`. At today's catalog of 60 the report bytes are unchanged. | 2347cc948 |
| RT-15 | `eval_gate/pbo.py:66` used `np.array_split`, giving UNEQUAL blocks when `n_obs % s_blocks != 0`. MEASURED: `contiguous_blocks(70, 16)` -> `[5,5,5,5,5,5,4,4,4,4,4,4,4,4,4,4]` and, across all 12870 combos, an IS half holding **32..38 rows** -- the same non-uniform-rank break `_check_s_blocks` already rejects for odd `s_blocks`. | Rows are trimmed to `n_obs - (n_obs % s_blocks)`. Same call: sizes all 4, every IS half exactly **32** rows. `cscv_pbo` reports `detail["n_obs_scored"]=64` and `detail["dropped_tail_rows"]=6` beside the unchanged input `n_obs=70`. The prior test pinned the unequal split by asserting full coverage of a non-multiple `n_obs`; it now uses an exact multiple. | 1ea75edb4 |
| RT-16 | `eval_gate/backtest_runner.py:86-87` dropped unpriceable / unsettled joined rows with NO counter, so `n_games` was a survivor count. MEASURED on a synthetic corpus: 10 joined rows, 5 carrying a corrupt price -> 5 states returned, 5 dropped, nothing in the report naming the 5. | `load_states` fills an optional `counts` dict and `run_backtest` emits it as `corpus_counts` beside `n_games`. MEASURED on a 66-row corpus (5 corrupt prices, 1 unsettled): `{"n_joined": 66, "dropped_unpriceable": 5, "dropped_unsettled": 1, "n_states": 60}`, the three buckets summing to `n_joined`, with `n_games = 60 < 66`. Additive: return values and all existing callers unchanged. Shared-module token held as `account1 / S40c` for this file and released. | bf1871c2a |
| RT-17 | `eval_gate/close_join.py:115` did `joined, _ = _joined(...)`, DISCARDING the close-drop counts, then applied its own four-way `notna` + `_spine_join == "both"` filter with no count of what that removed. MEASURED on a 10-row synthetic join: 6 states out, 4 rows dropped, none of them named anywhere in the return. | `gate_corpus_states(..., counts=<dict>)` fills the dict in place with the close counts plus `n_joined=10`, `spine_unmatched=1`, `null_close=1`, `null_target=2`, `n_states=6` -- the four buckets summing to `n_joined`. Additive: the return value stays a plain `list` and all six existing callers are unchanged. | 17174e672 |

## Tests

Regression tests, one per finding, each verified to fail on the pre-fix code for
RT-2 (swapped the key back: 1 failed) and by construction for the rest (the
reproduction IS the assertion). Per-file runs, all green:

| suite | result |
|-------|--------|
| `scripts/platformkit/test_hedge_trial_arms.py` | 9 passed (was 6) |
| `scripts/platformkit/combo/test_nested_cv.py` | 1 passed (new file) |
| `scripts/platformkit/eval_gate/test_retro_correction.py` | 2 passed (was 1) |
| `scripts/platformkit/eval_gate/test_pbo.py` | 16 passed (was 15) |
| `scripts/platformkit/eval_gate/test_close_join_tennis.py` | 9 passed (was 8) |
| `scripts/platformkit/eval_gate/test_backtest_runner.py` | 4 passed (was 3) |
| consumers unchanged: `test_cpcv_engine` 5, `test_stacker` 4, `test_close_join_mlb` 6, `test_close_join_soccer` 4, `test_catalog_rescreen` 8, `test_combo_search` 2, `test_leak_contract` 6, `test_redteam2` 7, `tests/platform/test_stack_gate_pregame` 14, `tests/platform/test_combo_factory` 13 | all passed |

110 tests across 16 files, 0 failed.

## PROPOSED (not applied)

- **RT-13 real team keys.** Carrying the corpus's real `home`/`away` through
  `load_corpus` into `game_states` is the only way to make the purge a team
  purge. It changes `n_train` on both corpora, so it is a separate row.
- **RT-15 trim direction.** The trim drops the TRAILING `n_obs % s_blocks` rows,
  which are the most recent. Dropping the oldest instead would keep recent
  evidence; nothing measured here distinguishes the two.

## No bar moved

No threshold, prereg width, alpha or verdict rule was changed. `RETRO_SWEEP_TRIALS`
stays 85; `R.BAR` and `R.LOCK` are untouched; `eps_eff` and `DEFAULT_EPS` are
untouched. RT-10 and RT-14 can only tighten (more K, a refused over-wide catalog);
RT-12, RT-16 and RT-17 add stops and denominators; RT-2, RT-13 and RT-15 correct
measurements that were wrong in the code's own terms. No `data/registry/` write,
no flag flipped ON, no `--force`, no process started or killed, pod untouched.

## NOT VERIFIED

- **RT-15 changes published PBO numbers.** No existing PBO artifact was
  recomputed, so any on-disk `pbo` value produced on a non-multiple `n_obs` was
  computed with unequal blocks and is not reproduced by today's code. The
  direction of the change is unmeasured.
- **RT-10 has never actually charged a second corpus.** The fix is proven at the
  `sport_charge` boundary with `_charge_ledger` monkeypatched; no real ledger row
  was written and `hedge_trial_runner.main` was not run end to end.
- **RT-16 / RT-17 counters are proven on synthetic corpora only** (66 rows and 10
  rows). No real `data/domains/**` join was re-run, so the real drop counts for
  any sport remain unmeasured.
- **RT-12's raise is a new failure path for five callers**
  (`combo/batch_gate.py`, `combo/combo_gate.py`, `combo/stack_gate_pregame.py`,
  `combo/run_nba_teamadv_stack_v1.py`, `survivor_corners_combo_gate.py`). Their
  test files pass, but none was run against a corpus small enough to produce an
  empty outer fold, so the new stop is unexercised in production.
- **RT-13 is a labelling fix, not a correctness fix.** The purge still runs on a
  synthesized key; the artifact now says so, and nothing else changed.
- **`scripts/platformkit/eval_gate/cpcv_engine.py` was dirty in the working tree
  at lane start** -- reverted to the pre-RT-18 deny-list redaction (no
  `strict_redaction` / `allow_keys` parameters), i.e. behind its own landed
  commit 112c995d7. This lane did NOT touch or repair that file. Whoever owns
  S40b's RT-18 should check it.
