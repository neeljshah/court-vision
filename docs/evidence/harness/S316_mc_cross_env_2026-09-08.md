VERDICT: PARTIAL -- the alternate-local version arm is n=0: Torch 2.2.0 there cannot bridge to NumPy 2.2.6 (`RuntimeError: Numpy is not available`), so cross-host/build divergence is localized to the without-replacement categorical call (torch.multinomial without replacement at src/sim/fast_sim.py:91); version versus hardware/build remains unisolated; the version decision row was not reached.

Spec: `docs/evidence/tracking/specs/S316_spec.md`; contract: `docs/evidence/tracking/VERIFIER_CONTRACT.md` sections B and Q1-Q9.
Preregistration: `docs/evidence/harness/S316_mc_cross_env_2026-09-08_preregistration.md`, LF-normalised pre-seal SHA-256 `d03ef82c52efc9d9b5b23e2f227c8e178deaffe9b5a9e3a522770f84ffc56064`, sealed as its own commit `c834e10b7` at 2026-09-08T16:12:49Z.
prereg commit postdates scored artifacts (attempt 1); attempt 1b re-ran every arm after the sealed commit c834e10b7 -- the pod block opened 2026-09-08T16:42:52Z and ran 29 m 2 s, the local arms stamped 16:47:10Z, 16:48:37Z, 16:50:49Z and 16:53:47Z, every one after 16:12:49Z (pod arm clock in `pod_arms_clock.csv`, local clocks in each arm's `environment.json`); all attempt-1 artifacts were overwritten or deleted.

Machines: LOCAL `DESKTOP-VUIITL8`, Python 3.10.20 / NumPy 1.26.4 / Torch 2.1.2+cu121, 12 CPUs; ALTERNATE LOCAL, Python 3.10.0 / NumPy 2.2.6 / Torch 2.2.0+cu121; POD `771c926940a2`, Python 3.12.3 / NumPy 2.1.2 / Torch 2.8.0+cu128, 256 CPUs. CPU route throughout; pod work ran `nice -n 19` under `/workspace/wt/a2`, no deployed-tree path was written and no daemon or guard was touched.
Inputs, all tabular (resolution N/A): `data/cache/eval_gate/s92_nba_lineup_dynamic_2026-09-03_all.csv` (38630145 bytes); `docs/evidence/harness/S255_asof_rate_snapshot_producer_2026-09-04/player_rate_snapshots.parquet` (565095); `team_rate_snapshots.parquet` (22677); `cluster_qualification.csv` (36282); comparison target `docs/evidence/harness/S266_nba_sim_third_arm_construct_2026-09-04/S266_selected_tick_series.csv` (53203). All four scored inputs were verified SHA-256 identical on both machines, as were the 26 shipped Python modules.
Sidecars: one `environment.json` per arm directory under `docs/evidence/harness/S316_mc_cross_env_2026-09-08/`.

PREMISE TRUE: the exact 30-cluster local replay emitted 180 unique state keys and reproduced the S266 archive with a max `p_simulator` difference of 0.0; all eight frozen tick columns, simulator Brier and simulator ECE differed by 0.0. Local simulator Brier 0.25590413411458335, ECE 0.13515624999999992; pod simulator Brier 0.2496853298611111, ECE 0.09340277777777779.
Every scored arm used the shared `cpcv_evaluate` route with 8 groups, one test group, strict redaction and the existing symmetric 3-day embargo, one evaluator record per state key. Improvement means baseline loss minus candidate loss; positive means the candidate has lower loss.

| Arm / control | n | max abs p_simulator delta | ticks > 1e-9 | sim Brier delta | sim ECE delta | result |
|---|---:|---:|---:|---:|---:|---|
| LOCAL premise / S266 archive | 180 | 0.0 | 0 | 0.0 | 0.0 | premise holds |
| LOCAL OMP=MKL=Torch threads 1 / LOCAL premise | 180 | 0.0 | 0 | 0.0 | 0.0 | threads do not move it |
| LOCAL OMP=MKL=Torch threads 128 / LOCAL premise | 180 | 0.0 | 0 | 0.0 | 0.0 | thread count does not move it |
| ALTERNATE LOCAL threads 1 / LOCAL threads 1 | 0 | N/A | N/A | N/A | N/A | did not score; see NOT VERIFIED |
| POD default 128 threads / prior POD-1 | 180 | 0.0 | 0 | 0.0 | 0.0 | POD-1 reproduced exactly |
| POD OMP=MKL=Torch threads 1 / POD default | 180 | 0.0 | 0 | 0.0 | 0.0 | threads do not move it |
| POD threads 1 / LOCAL threads 1 | 180 | 0.359375 | 171 | 0.006218804253472238 | 0.04175347222222213 | threads fixed; cross-host/build divergence localized to the without-replacement categorical call; version versus hardware/build unisolated |
| POD default / LOCAL premise | 180 | 0.359375 | 171 | 0.006218804253472238 | 0.04175347222222213 | prior cross-environment difference reproduced |

Host acceptance note: this box reports 12 CPUs but `torch.set_num_threads(128)` is accepted and `torch.get_num_threads()` returns 128, so the preregistered 128-thread local arm ran and is reported above.

RNG consumption audit of the executed path, every `file:line` read from the code in this commit:

| Draw site | Call | Generator and seeding | Can draws per tick depend on a floating-point comparison? |
|---|---|---|---|
| `src/sim/fast_sim.py:86` via `_samp_lineup`, 4 calls per possession pair at `:186-187` | `torch.multinomial(team.lineup_p, N, replacement=True, generator=gen)` | `torch.Generator(device="cpu")` seeded at `fast_sim.py:178` by `gen.manual_seed(seed)`; the tick seed is built at `scripts/platformkit/ingame/s256_nba_sim_engine_v3.py:167` as `int(game) % 2147483647 + int(grid_target_elapsed)` | No. N and the call count are fixed. |
| `src/sim/fast_sim.py:91` via `_pick`, 8 call sites per pass at `:103,112,119,122,151,158,161,164` | `torch.multinomial(weights + 1e-9, 1, generator=gen)` | same generator | No. Every `_pick` runs on all N lanes unconditionally. |
| `src/sim/fast_sim.py:104,111,116,117,136,144,157,160` | `torch.rand(N, generator=gen, device=dev)` | same generator | No. All eight execute in each of the four `range(4)` OREB passes at `:101`. |
| `src/sim/fast_sim.py:181` feeding the loop at `:185` | `n_poss = int(round((home.pace * hp + away.pace * ap) / 2))`; not a draw, it sets how many possessions draw | pace is built at `s256_nba_sim_engine_v3.py:166` | YES, and it is the only one: a float round fixes the loop count, so a one-possession change shifts the whole stream. |
| `scripts/platformkit/ingame/s256_nba_sim_engine_v3.py:96` | `np.random.default_rng(SEED).choice(...)` | NumPy Generator, SEED 2561001 | Not executed: the 30 game ids are supplied. |
| `src/sim/basketball_sim.py:179,185,186,200,214,216,220,226,253,264,272,278,280` (reference possession engine), `:328` (`_apply_dispersion`), `:560` (`simulate_game`), `:647,665,667` (count calibration) | NumPy `choice`, `integers`, `random`, `standard_normal`, `negative_binomial`, `poisson` | `np.random.default_rng` on `seed`, `seed + 7`, `seed + 777` | Not executed: `price()` calls `fast_sim` only, and `_finalize` receives anchor and dispersion False so the `basketball_sim.py:585` block never opens. |

The one draw-count branch is measured, not assumed: `--audit-only` emits `state_key`, `seed`, the exact `pace` bits and `n_poss` for all 180 ticks. `local_audit/`, `local_alt_audit/` and `pod_audit/S316_draw_count_audit.csv` agree on all 180 rows in all four columns across the three environments (identical LF-normalised SHA-256 `9988d329efa8cae7ef5db11280ed89f5c038dac5894d9325c23dd005ad5db7b9`), so seed and draw count are not the cause.
Bisect (`--rng-probe`, `local_premise/S316_rng_probe.json` and `pod_probe/S316_rng_probe.json`): from one `manual_seed(401809918)` CPU generator the two Torch builds return an identical `torch.rand(8)` stream and identical generator advancement after `torch.multinomial(..., replacement=True)`, but the without-replacement categorical `torch.multinomial(weights + 1e-9, 1)` returns different rows and leaves the generator at a different position. That result is unchanged by `torch.set_num_threads(1)` and by `torch.use_deterministic_algorithms(True)` on both hosts.

Decision table row reached: **none -- cross-host/build divergence is localized to the without-replacement categorical call (torch.multinomial without replacement at src/sim/fast_sim.py:91); version versus hardware/build remains unisolated; the version decision row was not reached.** The thread row is excluded by measurement on both machines, and the row-3 audit it points to was executed and clears the only fp draw-count branch. Localized site: the CPU `torch.multinomial` without-replacement call at `src/sim/fast_sim.py:91` runs eight times per OREB pass, so the two environments diverge from the first possession; which of library version, build or hardware is responsible is not isolated.
Additive fix (PROPOSED, not implemented): replace that categorical with a version-stable inverse-CDF draw, one `torch.rand` row against the normalised cumulative weights. It is NOT implemented here because the call site is `src/`, a human-gated path, and no monkeypatch of a gated module was made. Per the preregistration, with no fix implemented the proposed bar is within-environment 1e-9 plus the reported cross-environment difference: max `p_simulator` 0.359375 on 171 of 180 ticks, largest metric difference simulator ECE 0.04175347222222213.
No flag exists or was flipped; no schema, bar, source, default, register, data or ledger path moved; every S287 value is untouched, since this row re-measures the same 180-tick restriction and rescores nothing of S287's. No comparison is labelled AHEAD. Eye check: NONE.
Limitations: 180 ticks are the restriction, not the 2,130-tick construct; a future fix that moves `p_simulator` at the 1e-4 level would change S287's reported numbers and must be reported there, never rescored here.
Wall time: local premise 1 m 43 s, local threads 1 1 m 18 s, local threads 128 2 m 5 s, alternate local abort 20 s, each local audit and probe under 1 m; pod block 29 m 2 s total (default arm 24 m 47 s, threads-1 arm 4 m 9 s, audit 6 s).

LF-normalised SHA-256: preregistration `eeee640a7144b849572b47c6a18bccc64464919ff03c2d27ddcdf862abcdf46d`; `S316_arm_comparisons.json` `80babbf5738be5cefe48507ca40a8c635cf551b3a3c6726e080272b49a05cd41`; draw-count audit, all three environments `9988d329efa8cae7ef5db11280ed89f5c038dac5894d9325c23dd005ad5db7b9`; LOCAL tick series, premise and both thread arms equal `fd9d08793509aaed9df5c1c11a48e19703a3c8cc5fabcb10077c185dccceb879`; POD tick series, default and threads 1 equal `690d1017e8dd63cc4003e986d7ea0bfdf5f367e84807e626aedce8c4026e4a2b`; runner `scripts/platformkit/ingame/s316_mc_cross_env.py` `c50a7726c05c2d55ace4862fe697309be901b2dfe4475c209959763e99824279`.
Self-check B1-B10 / Q1-Q9: all 180 fixed states are retained and none is excluded after scoring; the commit only adds files and fields; no absent-evidence quarantine, claim lifecycle, module move or deployed-tree write; the complete 180-state construct is compared; Q1 holds because the sealed preregistration commit precedes every scored artifact; Q2 is N/A, no charged trial, no ledger read or written, K is N/A; Q5 is N/A; Q9 records are archived.
Proposed ledger line (verified): `2026-09-08 | in-game calibration | S316 | n=180: local and pod thread arms each show max p_simulator delta 0.0; cross-host/build differs on 171 ticks (max 0.359375), while the same-host version arm is n=0, so version versus hardware/build remains unisolated | PARTIAL (verified: codex-sol, contract A/B/Q)`

NOT VERIFIED:
- The alternate-local version arm did not score. Exact failure: `RuntimeError: Numpy is not available` raised at `src/sim/fast_sim.py:191` (`npbox = {k: box[k].cpu().numpy() for k in _STATS}`); that interpreter's Torch 2.2.0+cu121 was built against NumPy 1.x and cannot bridge to its installed NumPy 2.2.6. Preserved in `local_alt_threads_1_stderr.txt`. Its NumPy-only draw-count audit did run and agrees with the other two environments.
- The proposed inverse-CDF fix is not implemented, so it has no flag-ON cross-environment reproduction and no flag-OFF byte-neutrality result.
- Which Torch release between 2.1.2 and 2.8.0 changed the kernel is not bisected; only the two installed builds were probed.
- The 128-thread setting was compared against a 12-CPU host locally and a 256-CPU host on the pod; the pod's physical thread capacity is not reproducible locally.

Q6 exemption disclosure: raw machine CSV cells in `S316_draw_count_audit.csv` and the tick series carry digit runs that a naive scan flags; they are unedited measurements, and the prose and code of this row scan clean.
Corrections (fix 1c, 2026-09-08): applied the verifier's three minimal-diff corrections -- `summary.pop("fills", [])` -> `summary.get("fills", [])` in `s316_mc_cross_env.py` so the established `fills` field is retained and `fills_csv` stays purely additive (all five arm summaries regenerated from their already-committed CSVs via `_compact`, no simulator run, growing 66 -> 1508 lines with every other field byte-identical to the rejected commit); `test_s316_mc_cross_env.py:37` now requires both `fills` and `fills_csv` present, not removal; the deleted `comparison_local_premise_vs_threads_1.json` is restored byte-for-byte from `0a6fded99`; VERDICT and the proposed ledger line are downgraded DONE -> PARTIAL for the n=0 alternate-local arm. New gaps the verifier named, unresolved here: same-hardware Torch 2.1.2 vs 2.2.0 probes match at one thread in deterministic mode, so version versus hardware/build remains unisolated; pod routing was unavailable to the verifier's sandbox; the verifier's own REJECT memo could not be committed from that sandbox.

Vocabulary follows contract Q6; automated scan required.
