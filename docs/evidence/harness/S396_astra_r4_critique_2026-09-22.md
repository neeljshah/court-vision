# S396_astra_r4_critique_2026-09-22.md -- gpt-6-astra second-tier critique, archived verbatim by the orchestrator (2026-09-22)

- **BLOCKING - builder time leakage.** [signal_audit_runner.py:108](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_runner.py:108) consumes training rows in `walk_forward`'s string timestamp order ([walkforward.py:135](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/walkforward.py:135)). `10:00+00:00` precedes `11:00+02:00` lexically but follows it chronologically. A stateful builder therefore sees future state while building the earlier training row. The census's parsed sort does not fix scoring. **Test:** use mixed offsets, 20 training rows, and a builder asserting monotonic UTC time. Also move `MemoryStub.seen` to class storage: fresh instances cannot isolate the full-corpus census from subsequent folds.

- **BLOCKING - reference/settlement timing is unproved.** [signal_audit_runner.py:98](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_runner.py:98) injects closes without availability checks; :110-111 consumes training closes/outcomes using decision-time eligibility alone. [backtest_runner.py:130](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/backtest_runner.py:130) manufactures noon timestamps. A later actual close can enter an earlier decision; another game's outcome can enter training before settlement. **Test:** overlapping unrelated games after 20 warm-up games; require refusal/exclusion when close availability or settlement exceeds the relevant decision. A pregame close legitimately precedes settlement; its availability, and training-label settlement, need separate checks.

- **BLOCKING - repeat charge.** [signal_audit_runner.py:173](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_runner.py:173) protects only an occupied output directory; :190 unconditionally charges. Crash, then identical manifest plus new `--out`, produces another charge with the same hypothesis hash. K correctly increases from the locked ledger; it is not reused. **Test:** fail the loader, retry unchanged into another directory, require refusal and one ledger row. Also inject append-then-raise into `_charge_ledger`: it sits outside the receipt/failure `try`.

- **BLOCKING - seal-to-charge race.** [signal_audit_runner.py:176](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_runner.py:176) hashes one read; :178 validates another. Replacing the manifest between them with an internally consistent NE-to-EVALUABLE revision changes the allowance while charging the old digest. **Test:** swap valid manifest bytes after the committed check; assert no charge. Ordinary edits before `check_seal` are refused, and disk-only catalog changes do not flip sealed NE marks.

- **BLOCKING - wrong signal identity.** [signal_audit_family.py:205](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_family.py:205) requires only a nonempty registry builder; rederivation ignores that binding. Runner :250 executes it. A `noise` hypothesis can score `features:planted`. **Test:** change only that builder name and require manifest rejection.

- **BLOCKING - swallowed leak exception.** [signal_audit_catalog.py:226](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_catalog.py:226) converts `LeakError` raised inside `signal.build` into `BuildFailed`; :274 counts it, allowing an UNDERPOWERED verdict. **Test:** a stub raising `LeakError` must terminate the family. Other catches: runner :256 emits REFUSED for KeyError/TypeError/ValueError; catalog :208 temporarily suppresses sorting failures; family :67 and runner :218 rethrow.

- **CORRECTION - unequal-coverage Romano-Wolf.** [signal_audit_runner.py:160](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_runner.py:160) passes differing covered game sets into [romano_wolf.py:55](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/romano_wolf.py:55), which treats them as independent corpora despite overlap. **Test:** two strongly correlated arms with one missing game; require shared resampling on overlapping games or explicit refusal. This affects the secondary column. Defaults are 2,000 draws, seed 2718.

- **CORRECTION - unsupported tail claim.** [signal_audit_verdicts.py:231](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_verdicts.py:231) interpolates; it does not pin unresolved tails to extrema as :275-277 and memo :191-194 claim. **Test:** `interval(arange(2000), .00015625)` gives approximately `[.156171875, 1998.843828125]`. Both primary intervals do use the same draws; nominal tiny-tail coverage is unproved.

- **NOTE - scoped passes.** Runner :232 rejects duplicate game IDs, so :274's clustering claim applies only to this single-state window, not pooled horizons/windows. Head/constant-feature fallback reconstructs as `p_reference`; [signal_audit_verdicts.py:114](/C:/Users/neelj/nba-harness-h57/scripts/platformkit/eval_gate/signal_audit_verdicts.py:114) quotes comma-containing IDs. Add a comma-ID round-trip test. No apparent Q6 prose violation; historical divisor figures are explicitly superseded.

VERDICT: REJECT

NOT VERIFIED:
- Tests, runtime reproduction, memo metrics, real corpus behavior, or real-ledger behavior. No edits, commits, pytest, or network access performed.