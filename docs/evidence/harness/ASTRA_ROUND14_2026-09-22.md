# Astra round 14 (2026-09-22, after the S347 result): interpretation, next twelve rows, NBA self-deceptions, binding constraint

Source: codex design lane (read-only), archived verbatim by the orchestrator. Design text, not a measurement.

The released MLB trial is UNDERPOWERED in all 96 comparisons across 135 scored games.
C is near zero overall, while D has higher loss than the market in every overall cell, weakening the observed support for the in-game conditioning thesis.
B's lower overall point estimates suggest recalibration deserves continued measurement, but establish no AHEAD result.
The intervals establish neither MATCH nor BEHIND, and D-minus-A alone cannot isolate the model's incremental contribution beyond C.
A second corpus could change the scientific assessment, but cannot change this frozen MLB result or establish cross-corpus support without comparable estimands, adequate precision and resolved exposure.

NEXT TWELVE ROWS

Paths: I = scripts/platformkit/ingame; E = scripts/platformkit/execution; O = scripts/platformkit/ops; P = docs/evidence/ingame. Each row additionally owns matching per-file tests and its uniquely numbered acceptance memo under docs/evidence/harness/R14/. Ownership is disjoint. New modules remain below 300 lines.

1. Prospective schedule writer
Problem: no qualifying game has a prospective selection.
Before: E/forward_replay_qualification.py:108 permits selected_at == scheduled_start.
CHANGE: deterministically select one canonical ticker from archived discovery; persist game_id, ticker, sport, family, scheduled_start, actual selected_at and source hashes before start. Refuse equality, lateness, backdating and replacement; retain missing games.
Files: E/forward_schedule.py, E/forward_replay_qualification.py.
Acceptance: permutation-stable selection; exact-boundary refusals; durable immutable schedule accepted by replay.
Size M. Unblocks prospective collection.

2. OWNER ten-minute redeploy checklist
Problem: landed capture improvements remain absent from running processes.
Before: docs/operations/CAPTURE_RESILIENCE_RUNBOOK.md still targets nba-harness-h33 and says state capture is absent.
CHANGE: executable lines using the actual deployment root, interpreter and archive. Minutes 0-2 record commit/process identities; 2-4 OWNER creates STOP/STOP_STATE and confirms exits; 4-6 removes those sentinels and starts both S376 supervisors through Start-Process -WindowStyle Hidden; 6-10 verifies receipts, page writes and scheduler telemetry.
Files: that runbook; O/capture_deploy_preflight.ps1.
Acceptance: one writer per archive, pinned code, preserved checkpoints/gaps; no automatic lock stealing. Ten minutes establishes deployment only.
Size S. Unblocks current-code capture.

3. Native capture-to-qualification bridge
Problem: S362 does not consume the native evidence needed for qualification.
Before: E/forward_replay_io.py accepts tape_watermark, while S374 emits trade_backfill_closed; CLI lacks supervisor journals.
CHANGE: exact state game_key linkage; both supervisors' host gaps; proved pagination closure including quiet intervals. Never treat newest trade time as a completeness endpoint.
Files: E/forward_capture_bridge.py, E/forward_replay_io.py, E/forward_replay.py; I/local_capture_runner_trades.py.
Acceptance: native fixtures reach qualification; missing closure, ambiguous linkage and overlapping gaps prevent qualification.
Size M. Unblocks trustworthy daily replay.

4. Bind capture capacity to the prospective schedule
Problem: dynamic focus and default timing cannot deliver the declared observation plan.
Before: I/capture_scheduler.py has no schedule-consumption interface; replay defaults to two-second reservations.
CHANGE: consume row 1's frozen selections; declare capacity, cadence and reservation lifetime before future games. Two-second intervals at five-second polling cover at most 40 percent; select a feasible prospective profile without changing qualification bars.
Files: I/capture_scheduler.py, I/local_capture_runner.py, I/forward_capture_profile.json.
Acceptance: synthetic full-hour coverage meets frozen limits; bounded live smoke confirms cadence; dropped games remain scheduled.
Size M. Unblocks qualifying collection.

5. Complete the NFL live-status correction
Problem: situation verification ignores actual normalized live rows.
Before: domains/nfl/verify_live_situation.py (the two status checks near lines fifty-one and fifty-four) requires status "in".
CHANGE: finish the existing correction using archived status "live" fixtures; retain unknown-status handling and transition requirements.
Files: domains/nfl/verify_live_situation.py.
Acceptance: python -m pytest tests/domains/nfl/test_verify_live_situation.py -q; actual live rows enter transition checks, while insufficient support leaves semantics unresolved.
Size S. Unblocks reliable NFL situation evidence; does not independently establish qualification.

6. Daily score-blind qualification and week report
Problem: component completion does not establish a qualified week.
Before: rows 1, 3 and 4 acceptance memos exist; E/family_week_ledger.py remains authoritative.
CHANGE: hash schedules, archives, supervisor journals and policy configuration; report every scheduled game, integer reasons and covered milliseconds. Preserve 90 percent/480 decisions, 5/15-second freshness, frozen gap limits and the full 3600-second denominator.
Files: O/forward_qualification_daily.py; E/family_week_commission.py.
Acceptance: no outcome or scoring function is accessed; absent games remain; four distinct qualified games per completed sport/family ISO week; missing weeks break streaks.
Size M. Unblocks first verified forward week.

7. NBA eligibility and features amendment
Problem: the shared scorer cannot implement S382's populations.
Before: I/baseline_four_arm_features.py lacks nba_checkpoints_r1; select_rows requires model data for every arm; S347 release is documented.
CHANGE: add the exact corpus name; route ts/close_ts through parse_venue_time, preserving strict boundaries; require finite integral quarter/home_score/away_score, refusing booleans and fractions; exclude terminal rows. Invalid model data excludes D only. A/B/C retain full eligible populations; D comparisons refit comparators on identical paired training/test subsets.
Files: I/baseline_four_arm_features.py, I/baseline_four_arm_eligibility.py, I/baseline_four_arm.py.
Acceptance: boundary, population and chronology regressions.
Size L. Unblocks NBA census. MLB remains frozen at its run commit; this amendment requires neither rerun nor automatic re-audit.

8. Reconcile S383 with the declared primary
Problem: implementation and draft estimate different quantities.
Before: I/baseline_four_arm_period.py averages available periods within each game; S382 instead fixes one-quarter weights across period means.
CHANGE: implement S382's formula: average within game-period, across games within period, then equally across Q1-Q4. Bootstrap whole games and recompute period means; freeze handling of empty-period draws before scoring. Support A/B/C independently of D and paired incremental contrasts.
Files: I/baseline_four_arm_period.py.
Acceptance: missing-period fixture distinguishes formulas; contributions reconstruct the primary; no silent renormalization.
Size M. Unblocks a defensible primary.

9. NBA exposure and provenance disposition
Problem: ?outside S86? is being confused with untouched evidence.
Before: S385 records S58 exposure of all 1,593 games and unresolved model constants.
CHANGE: freeze exposure identities and document which decisions used S58/S86 outputs; inspect available authoring records without new scores. Default to descriptive interpretation while exposure remains unresolved; keep D descriptive.
Files: P/R14_NBA_EXPOSURE_PROVENANCE.md.
Acceptance: explicit population permissions and unresolved evidence; no invented untouched subset.
Size S. Unblocks honest prereg scope.

10. Reconcile census and finish the NBA seal package
Problem: the draft still contains unknown denominators and hashes.
Before: rows 7-9 accepted; P/S382_NBA_PREREG_DRAFT_r1_2026-09-21.md remains unsealed.
CHANGE: reproduce S377's original census, separately reconcile amended populations, and pin complete manifests, versions, exposure sets, folds, period support and D subsets. Prepare the final seal for orchestrator review.
Files: I/nba_corpus_readiness.py; P/R14_NBA_PREREG.md.
Acceptance: repeated counts-only census is deterministic; discrepancies stop sealing; no unresolved placeholders; row 12's code version pinned before seal.
Size M. Unblocks sealed NBA prereg.

11. NBA output auditor
Problem: MLB auditing assumptions do not cover different arm populations or the new primary.
Before: rows 7-9 contracts accepted; I/four_arm_output_audit.py assumes the original four-arm output.
CHANGE: independently reconstruct population-specific chronology, losses, primary weights, intervals, concentration and D-minus-C. Separate incremental C-minus-B evidence from C-minus-A. Keep near-zero concentration ratios explicitly uninterpretable.
Files: I/nba_four_arm_output_audit.py.
Acceptance: seeded subset, period, sign and exposure defects prevent release; no refitting; no NBA metrics before charge.
Size M. Unblocks trustworthy result release.

12. NBA charged-trial runner
Problem: S359 is MLB-specific.
Before: inspect I/s359_four_arm_trial.py launch guards; rows 7-9 and 11 accepted.
CHANGE: build I/nba_four_arm_trial.py as its sibling, reading corrected S383 C-minus-A Brier as primary. Preserve committed seal, manifest-before-corpus, canonical ledger, atomic attempt reservation, launch K and charge-before-metrics.
Files: I/nba_four_arm_trial.py.
Acceptance: synthetic concurrent launches, changed hashes and reused attempts refuse; crashes remain charged; secondary cells cannot replace primary; real launch requires row 10's committed seal.
Size M. Unblocks one controlled NBA trial.

Parallel groups: first {1,2,5,7,8,9}; then {3,4,11} after their prerequisites; then {6,12}; finish 10 with runner pins. Redeployment can proceed once its accepted capture dependencies are ready.

THREE NBA SELF-DECEPTIONS

Exposure: calling the 796 non-S86 games untouched despite S58. Guard: identity-level exposure accounting and descriptive interpretation unless decision independence is demonstrated.

Period weighting: removing terminal ticks but retaining accidental fourth-quarter dominance, or treating S383's current formula as the draft's formula. Guard: reconcile formulas before seal, freeze live period denominators and prohibit primary substitution.

Model provenance: treating chronological replay as proof that constants were selected independently. Guard: recover parameter authoring evidence; otherwise keep D descriptive and compare only identical paired populations.

The immediate binding constraint is capture deployment, followed by qualified data accumulation. Eleven landed rows and repeated verification demonstrate substantial implementation throughput; they cannot recover an unscheduled game or repair fourteen snapshots per hour retrospectively. NBA scoring supplies no forward weeks, and one qualified week supplies only the beginning of the eight-week requirement. The single owner decision this week is to execute the supervised capture redeployment with a narrow, prospectively scheduled collection profile and verify its live cadence.
