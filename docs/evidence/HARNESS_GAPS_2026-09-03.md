# Harness / signals / system gap register -- 2026-09-03 (living; one gap = one lane)

Rule: a gap is closed only by a measured artifact (metric, exact denominator, n,
and for any scored claim a prereg sealed before the metric). Harness thresholds
and gate values NEVER move. Calibration language only -- no dollar, ROI or edge
claim ever appears in a row, an artifact or a memo.
NEXT_GAP_ID: S257  (allocated by the orchestrator ONLY; lanes never invent ids.
OWNERSHIP IN FORCE 2026-09-03: account 1 = this harness session holds THIS counter (S); account 2 = the tracking session holds G (G33). Account 1 dispatches codex ONLY into worktrees a10-a12.
S-ids start at S01 so they can never collide with the tracking register's G-ids.
S28/S29 allocated 2026-09-03 by the roadmap-audit lane.)
ONE COUNTER, ONE HOLDER: if two sessions are running (see the two-account handoff
docs/evidence/tracking/HANDOFF_TRACKING_ACCOUNT2_2026-09-02.md), exactly one of
them may increment this header. Say which in the first turn of the day.
Specs cite docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md; verifiers apply the
spec's ACCEPTANCE RULE + VERIFIER_CONTRACT.md B1-B10 + the quant additions Q1-Q6
below, and nothing else. A rule absent from the spec's ACCEPTANCE RULE cannot be
used to reject -- a verifier that needs one files a NEW row instead.

Plan detail for every row: docs/research/organization-sprint/MASTER_ROADMAP_2026-09-03.md
section 2. Landings append one line to docs/evidence/RESULTS_LEDGER_SYSTEM.md
(create on the first landing; same format as the tracking ledger).

Order: S01, S02, S03, S04, S05, S06; S19 and S20 in parallel from day 1; then
S07, S08, S09, S13, S10, S11, S12, S15, S16, S21, S22, S23, S24, S17, S14, S18,
S25, S26, S27. S28 and S29 are week-1 fillers whenever a lane is free -- S28
BEFORE the first unattended night's push.

ACCEPTANCE DEFAULTS (audit 2026-09-03): 14 rows were missing at least one of the
five required elements (metric / denominator / before / bar / n / must-not-move).
The defaults that fill them, and the CONSTRUCT carve-out from the n>=30 rail, are
in MASTER_ROADMAP section 2 under "AUDIT 2026-09-03 -- ACCEPTANCE DEFAULTS". A
spec copies them in verbatim; a verifier may not reject a CONSTRUCT row on n>=30.

This register SUPERSEDES the H/X/A/D rows in SYSTEM_GAPS_2026-09-01.md. The
mapping is NOT one-to-one: H01->S02+S03, H05->S07+S08+S09, H02+X05->S06,
H03->S05, H04->S04 (and tracking G16), H06->S10, H07->S01, X01->S19, X03->S20,
X04->S21, A02->S22, A03->S23, A04->S24, D01->S25, D02->S26; X02/A01/A05 are
CLOSED with no S-row; S11-S18 and S27-S29 are new.

| id | area | gap (measured) | evidence | status |
|----|------|----------------|----------|--------|
| S01 | harness | `run_gap_arms_real_corpus.py:18-19` `_BASELINE_TICKS` 144,424 and `_BASELINE_WINDOW_TICKS` 14,802 match nothing on disk (real: 52,558 / 7,158); a two-line fix, not the one-liner the burst plan recorded | E4_PROMOTION_RESULT_2026-09-01.md section 3; module read on disk (108 LOC) | LANDED 625b3ef45 -- constants match the store (52,558 / 7,158); honest S06 baseline unblocked |
| S02 | harness | soccer gate corpus carries a target but no market close: `p_base` is a Poisson MODEL baseline, so the sport cannot enter walk-forward against a close. Gate corpus EXISTS (25,834 rows) -- this is a close JOIN, not a corpus build | RENAISSANCE_HARNESS_DEEP_PLAN_2026-09-02 section 2.2; `data/cache/combo/gate_corpus_soccer.parquet`; odds 16,322 rows 2019-08-02..2026-05-24, 100.0 pct joinable, 0 null closes | LANDED 40e991be1 -- 100.0 pct join, close beats p_base; unblocks S22 soccer + S03 (verified 2026-09-03 from a11 d7718bdb0; ACCEPT WITH CORRECTIONS, codex Q1 self-label overruled -- uncharged, spec-fixed bar) (was H01a) |
| S03 | harness | tennis same, with two disjoint `corpus_unit`s: ATP 25,898/30,616 = 84.6 pct joinable, WTA 8,054/11,270 = 71.5 pct (RE-MEASURED 2026-09-03 by the codex step-0 recount, memo S03_premise_recount; the register's earlier 25,831/8,028 were stale by +67/+26 rows -- bars below unchanged). `b365w`/`b365l` are winner/loser oriented and LEAKY beside the de-leaked `_p1`/`_p2` (verified row 0) | same; `data/cache/combo/gate_corpus_tennis.parquet` 41,886 rows | CLOSED AT LIMIT 2026-09-03 ff094421c -- code landed as the honest instrument: distinct joined spine rows ATP 25,764/30,616 = 84.1521 pct, WTA 8,002/11,270 = 71.0027 pct vs the 84.4 / 71.2 bars (both missed, never lowered); the register's 25,898/8,054 were merge-ROW counts inflated by 93 duplicate event_ids naming two real matches (S48); devigged close beats p_base on both units (ATP 0.1986 vs 0.2164, WTA 0.1935 vs 0.2157); per-year 75.4-84.0 pct; S35 fixed + S34 labelled inside; 5 + 4 tests |
| S04 | harness | the teacher->student gate is a RULE, not code: no module runs student vs id-fixed-effect baseline vs student+ids, so no "tracking improved a model" claim is permissible today | SYSTEM_GAPS H04 / tracking register G16; `scripts/platformkit/eval_gate/student_gate.py` verified ABSENT | LANDED 2026-09-03 sha c7523ce63 -- module + synthetic gate; first real trial blocked on S26 |
| S05 | harness | every calibration piece exists and nothing composes them per (sport, regime): reliability bins, `max_loser_wp`, Murphy decomposition, per-regime isotonic, ECE, sharpness, resolution all present; `max_loser_wp` has never gated a promotion and `docs/evidence/calibration/` holds only `foundry_run_*` | deep plan section 1 "Calibration craft"; dir listing verified | LANDED 504617a99 -- ACCEPT attempt 2: 4/4 artifacts against the unmoved bar, mlb scored on the S41 rebuild (prereg INPUT_UNAVAILABLE exemption NOT invoked); ONE bin-edge rule with reproduction_max_abs_diff exactly 0.0 on all four; nba 1,814 ECE 0.053328 -> 0.024843, mlb 39,162 0.005918 -> 0.008077, soccer 25,834 0.106927 -> 0.009302, tennis 41,886 0.038691 -> 0.008403, 0 dropped, ALL FOUR FLATTENED (an honest null: per-regime isotonic buys calibration and pays resolution; mlb's p_base was already better calibrated); prereg seal 9051BB6E re-verified and restored to master; 6 passed; caveats: max_loser_wp degenerate on pregame corpora (S43), chronology positional (S44/S50) |
| S06 | harness | no nested-CV stacker over the arms; online Hedge trails its best arm (regret +2.063 inside a 66.79 bound over 158 rounds) with three named structural causes -- coverage asymmetry, two bad arms, regime heterogeneity (-0.0053 innings 1-3 vs +0.0484 innings 7+) | X05 (c75b60074); HEDGE_TRIAL_RESULT_2026-09-01; `eval_gate/stacker.py` ABSENT, prior art at `combo/stack_fit.py` + `combo/nested_cv.py` | LANDED 4ff027906 -- BEHIND (SINGLE-WINDOW): stacker 0.296943 vs incumbent 0.207033 (improvement -0.089910, bar +0.004 unmet), DM CI [-0.131927, -0.048386], deflated_p 0.000553 at launch K=14; PBO 0.0, uniform 0.228422, guard-only 0.211018 (guard = e4 arm_a_prob); ledger charged exactly once 13 -> 14, prereg sealed first (d81c374f0); arm series reproduced < 1e-9 (leak-free game-first-date variants per S36); e4_blend STANDS; evidence S06_stacker_result_2026-09-03.md -- an honest BEHIND, the module lands as measurement machinery |
| S07 | harness | no primacy rule: where a forward settled series and a backtest disagree, nothing says the forward number is the claim, and a retrospective-only number is not labelled | SYSTEM_GAPS H05; `ingame/forward_evidence_scoreboard.py` (291 LOC) | LANDED 43fd721f9 -- ACCEPT. Primacy rule as code: label correctness 0/4 -> 4/4 (n = 4 CONSTRUCT, exhaustive); verifier-reproduced 4/4 incl. forward BEHIND overriding retro AHEAD. Live state 17 rows, 12 forward_n = 0, 17/17 claim_label RETROSPECTIVE -- every live claim is retrospective today. Additive: +2 row keys, 0 existing values changed; readers green. FORWARD branch never executed on real data (S20 still 0 settled). Follow-up: the conflict path is structurally unreachable until a backtest source is wired (label_row passes the row's own verdict as retro) |
| S08 | harness | replication is a convention, not a gate: `combo/fwer_budget.min_corpora_eff` (:55) exists and no call site consults it, so a single-window AHEAD is downgraded only by memo | deep plan risk 3; E4_PROMOTION_RESULT (no disjoint MLB window on disk) | LANDED 2026-09-03 e54e1faf0: ACCEPT WITH CORRECTIONS -- replication_gate.py makes the floor a GATE, bar 2/2 at K=14 (AHEAD n_corpora=1 -> SINGLE-WINDOW; n_corpora=2 -> AHEAD unchanged), one additive call site in hedge_trial_runner; fwer_budget.py read-only, no bar moved. PREMISE PARTIALLY FALSIFIED: eval_gate/stacker.py already imports min_corpora_eff but only EMITS it with hard-coded single_window=True and n_corpora=1 (does not downgrade) -- wire replication_verdict there next; 5 AHEAD writers left unwired; no real AHEAD exists to downgrade today; E4 replication attempt 2026-09-03 INSUFFICIENT (0 disjoint scored games), so no real AHEAD has yet passed through the gate |
| S09 | harness | gate-manifest staleness does not BLOCK a claim: evidence rows fall back to mtime, so staleness is WRITE time, not MEASUREMENT time | deep plan section 1 "Reproducibility"; `scripts/platformkit/eval_gate/gate_manifest.py` | LANDED 65a5c8920 -- ACCEPT WITH CORRECTIONS: measured_at + measured_at_source from the artifact's own timestamps, StaleEvidence/assert_fresh + opt-in --max-age-days; 3/3 CONSTRUCT reproduced in master (field beats fresh mtime; absent/corrupt/empty stale even at 10000d); additive -- 52/52 rows identical on every pre-existing key; honest ceiling: only 4/52 real rows carry a measurement-time field today (48/52 mtime-sourced), so arming the gate needs producer stamps first (S45); codex died pre-report, verifier reproduction carries the verdict |
| S10 | corpus | MLB odds end 2021-11-02 while `games_current.parquet` runs to 2026-07-12, so all 22 MLB mechanisms are NOT_TESTABLE against a modern close | deep plan section 2.2 (confirmed at data level); MECHANISM_WIRING_MLB_2026-09-01 | CLOSED AT LIMIT 2026-09-03 161d87c43 -- modern MLB close join 8.17 pct (913/11,179) vs the >= 60 pct bar, never lowered; per-season 2022 0.00 / 2023 0.00 / 2024 0.00 / 2025 0.00 / 2026 63.18 pct (per-season only, never the headline); premise partly FALSIFIED: a local price series exists (Kalshi/Polymarket 2023/2025/2026) but 2022+2024 have no rows and 2023/2025 are one-sided Polymarket with no first-pitch clock; ts = EPOCH SECONDS (why the spec could not parse it); the only local first-pitch clock is the 2026 Kalshi ticker (ET); Brier close 0.245073 vs p_base 0.248493 on the 913 joined rows, vintage PRE_FIRST_PITCH_TICK; 6 tests; the real fix is an external historical MLB close source, not a join |
| S11 | signals | no hypothesis grammar and no dedup: `foundry_runner.py:23` rebuilds ONE hardcoded matrix and `signal_foundry.register` (:51) is hand-called, so throughput is 13 charged trials in the program's lifetime | deep plan section 3; modules read on disk (108 / 246 LOC) | LANDED 246becf16 -- ACCEPT WITH CORRECTIONS: 116,370 deduped distinct hypotheses, 0 collisions, verifier-reproduced in master over 69 present catalogue parquets / 979 columns (264,330 enumerated = predicted exactly); 5 parquets ABSENT and named (red-team memo's 7 corrected to 5); off-grid ValueError, permutation-identical and grid-step-distinct hashes reproduced; closed alphabets, runtime_available explicit-only; follow-ups S46; codex died pre-report, verifier reproduction carries the verdict |
| S12 | signals | no cost tiering and no fixed T1->T2 promotion rule; without one, cheap screens are the garden of forking paths with extra steps | deep plan section 3 "Cost tiers"; Gelman & Loken (2013) | LANDED 2026-09-03 d55c03da3 -- PASS: 4/4 tier calls classified, refusal 2/2 (T0/T1 TierNotChargeable, tmp ledger 0 rows), charge 2/2 (T2, T3 one row each, k_family 1 then 2); spec FACTORY_TIERS_SPEC committed BEFORE the module (9b464ddc9 -> d55c03da3, merge-base ordered) and content-pinned by git hash-object b2b2ea5a0 on every TierResult; SF-1 screen/verdict partition by iso_week with ScreenPartitionLeak raise, SF-2 screened_n printed, SF-10 cluster_key team; 6 + 5 tests; module at the 300-LOC cap; nothing real screened yet (S16) |
| S13 | harness | the FWER ledger is FLAT and GLOBAL: 13 rows of `{at, predictor, sport, start, end, k_cumulative}`; nine unrelated basketball trials charged between prereg and launch took K 3 -> 12 and flipped a verdict | `data/cache/eval_gate/backtest_fwer.jsonl` (13 rows verified); HEDGE_TRIAL_RESULT_2026-09-01 | LANDED 2026-09-03 6a70efcba (token taken + released): additive family/k_family/hypothesis_hash/tier/prereg_sha256 fields, 5/5 CONSTRUCT, real ledger byte-identical sha 52785ad2, all 4 callers unchanged; VERIFIED LIGHT 1a68fdd8f: ACCEPT WITH CORRECTIONS (its schema test hardcoded live-ledger counts -- fixed to read on-disk counts) |
| S14 | harness | no within-family FDR: only Bonferroni `deflated_p` exists, so a family of hundreds of related hypotheses is priced as if every one were independent | `eval_gate/deflated_metrics.py:63`; `combo/fwer_budget.py:42`; statsmodels 0.14.4 installed, BSD 3-Clause (licence read) | LANDED 2026-09-03 07c5fd6b3 (spec first) + bf3ae6c16 (module) -- ACCEPT, THE ONLY BAR-LOOSENING CHANGE IN THE PROGRAM: within-family BH at q=0.05, 0/200 planted-null discoveries (bar <= 10), 37 families / 396 features / 3,564 hypotheses frozen in docs/evidence/harness/FWER_FAMILIES_SPEC and pinned 62702554f; every AHEAD requires BOTH bars (global deflated p AND family BH); no past verdict re-scored (by construction: takes p-values, opens no ledger -- asserted by a builtins.open watcher); k_cumulative unchanged, K stays 14; 9 + 8 tests, 10 importers clean; caveat: BH assumes PRDS while a family's members are correlated columns (fdr_by not measured); NOT YET WIRED into tiers._run_charged (S59) |
| S15 | signals | no results DB: a re-proposed hypothesis is a fresh trial rather than a lookup, and nothing indexes the trial artifacts | deep plan section 3 "Runner and results DB"; `data/cache/eval_gate/hypotheses.sqlite` verified ABSENT | LANDED 2026-09-03 5daf6d211 -- ACCEPT: stdlib-sqlite3 results DB; 2 proposals of one hash = 1 trial + 1 lookup with tmp ledger K UNCHANGED; SF-13 corpus_sha in the UNIQUE key, SF-14 k_at_run + verdict_needs_rescore + recompute_deflated_p, SF-12 IntegrityError on raw-param collision; 7 passed; real ledger byte-identical (14 rows); zero importers yet |
| S16 | signals | the runner cannot run continuously on grammar: `run_pass` (:51) rebuilds a fixed matrix and sleeps 900 s (:88) | `scripts/platformkit/foundry_runner.py` read on disk | LANDED 47a66bd7b -- claim-queue run_pass(number, queue, batch) over results_db + tiers; local timed run 1,750 screens in 610.5 s (172.0/min, 10,320/hour extrapolated; measured ceiling 7,826/hour on the soccer corpus), 0 charges (700 promotions held allow_charge_off), real ledger 14 rows md5 unchanged; --legacy preserves the old matrix + 900 s sleep; 4 + 1 tests; the screen predictor is p_base pass-through so no SCREEN is a finding; POD HOUR (>= 200 screens) = the verifier step, lane dispatched; POD HOUR VERIFIED 1763598f1 -- 3,780 T0/T1 screens in one wall-clock pod hour (15:14-16:14Z) vs the 200 bar, 0 charges, ledger file still absent on the pod; denominator 6,000 claims = 3,780 COVERED + 3,780 SCREEN + 2,220 StaleCorpusError (nba/mlb/tennis corpora not loadable on the pod -- S68); queue-bound, working rate 9,331/h; runner left running pid 165812; screens are p_base pass-through (non-findings; real predictor = S58c); S16b 2026-09-03 054eea081 (memo S16b_pod_factory_head_2026-09-03.md): POD FACTORY AT HEAD -- tiers.run_tier T0 reads FOUNDRY_PORTABLE_CORPUS (default OFF, 1 construct test); 32 files redeployed at md5 32/32 off the index tree, IMPORT_OK 20/20 (py3.12.3); pod corpora 1/4 -> 4/4 with the flag; runner 165812 killed to EXITED, its DB moved to hypotheses_s16hour_2026-09-03.sqlite, fresh frozen-grammar DB re-seeded 3,564 -> 3,240 distinct (mlb 333 / nba 1,332 / soccer 531 / tennis 1,044), relaunched pid 231346; first 15 min = 2,169 T0 COVERED / 2,168 T1 SCREEN across all four sports (mlb 214, nba 910, soccer 364, tennis 680), 8,672/h, StaleCorpusError 0 (was 2,220), charges 0, ledger + data/registry still absent on the pod; screens are p_base pass-through NON-FINDINGS and 1,804 of the 2,168 T1 rows were actually screened on the soccer states (single-sport binder, S75) |
| S17 | harness | decay monitoring alarms on n, not ESS: at the measured ICC 0.291 / design effect 87.4, 47,104 ticks are n_eff 539, so a 7-day tick window can be n_eff < 10 and the alarm is noise. One monitor exists where three are needed | `eval_gate/ledger.drift_report` (:86, disjoint windows correct); `ingame/gap_effective_n.py` (:30, :53, :62) | LANDED 2026-09-03 1f8f914d7: ESS-gated 3-monitor module 3/3 CONSTRUCT (two real defects found and root-fixed: union-tercile tie-break false alarm; chi-square expected-0 cell); BUILT NOT ARMED -- arms after S20 >= 200 settled rows; VERIFIED LIGHT 1a68fdd8f: ACCEPT |
| S18 | execution | sizing has no cross-market covariance, no impact model and no capacity: `venue_fees.expected_value_after_fees` (:156) nets FEES only | X05 memo; module read on disk (194 LOC); sklearn 1.6.1 installed, BSD 3-Clause | BLOCKED on S20 (0 settled rows) |
| S19 | execution | book capture pass wall ~105 s at 9 games x 2 sides vs a 5 s target; governed orderbook fetches ~3.6 s each | SYSTEM_GAPS X01; CAPTURE_CADENCE_ROOTCAUSE_2026-09-01; S19_request_governor_2026-09-03.md | LANDED de4730003 -- module verified on mocks; 30-pass pod measurement pending S21 deploy (verified 2026-09-03 from a14 edfd6e92d; ACCEPT WITH CORRECTIONS -- verifier-reproduced serial 3.766 s vs cap-4 1.093 s at 18 mocked fetches, governor file byte-identical; the memo's 101.172 s premise is not reproducible, verifier measures 65.000 s; marker commit a1aa15a5c, tree swept into de4730003 by a concurrent pathspec-less commit) (was X01); POD 30-PASS READOUT 2026-09-03 (1969daa13) = INSUFFICIENT: 41 cadence rows since the pid-21622 relaunch, wall median 0.401 s / p90 0.426 s, but n_live_games = 0 in all 41 (and all 844 of the shard) so no pass carried the 9x2 = 18-fetch denominator and the <= 15 s bar was never exercised; pre-deploy BEFORE reproduced = median 110.254 s / p90 119.978 s at n = 6 (2026-09-01 23:51-00:00Z); re-measure window 2026-09-02T23:50Z-2026-09-03T00:50Z (one-shot re-measure armed in this session) |
| S20 | execution | maker pool EMPTY; the forward CLV series has 0 settled rows locally, so sizing, capacity, adverse selection, decay monitoring and the program's only claim surface are all downstream of a week that has not run | SYSTEM_GAPS X03; EXECUTION_ENFORCEMENT_MATRIX_2026-09-01 | OPEN (was X03) -- THE KEYSTONE. PREMISE RE-MEASURED 2026-09-03 04:25 UTC from /proc: `inplay_capture_runner` IS alive (pid 8608 since 08-31 16:02 UTC, heartbeat m2_inplay_capture fresh), so the plan's 'runner not running' is FALSIFIED; still 0 bets all-time (paper_today n_placed_alltime 0), the default CLV ledger file is absent on the pod, and NO supervisor process (see S30). Premise memo LANDED 433f76d49 (docs/evidence/harness/S20_premise_2026-09-03.md): DO NOT LAUNCH the supervisor (kill-then-respawn per _restart.py:53 + m1_producer heartbeat 32.7 h stale would leave the placer chain down); the real blocker is S32 (pairing bridge: 0 paired rows in 36.6 h, every game no_live_state) and the pod's execution files lag local HEAD (see S21). Daily readout module = the cx_s20_paper_readout codex job. readout module LANDED 2d5715ec1; week bar (>= 200 settled maker rows / >= 2 sports / >= 7 days) still 0 -- blocked on S32. Verifier ACCEPT 2026-09-03: 4/4 CONSTRUCT in master, real run wrote data/frontend/analytics/execution_status.json status no_data with n_settled INSUFFICIENT; verifier-reproduced classification 20 rows = 0 settled / 18 open / 0 integrity-flag / 2 legacy (sum 20, none dropped); ledger sha256 byte-identical across the run; the MCP execution_status handler now reads _EXECUTION[0] (was falling through to paper_execution_audit.json) |
| S21 | ops | pod code lags master. CORRECTED 2026-09-03: the G15b/G29b/G01c backlog is CLEARED -- ledger row G14b records 48 files deployed 2026-09-02 ~22:05, md5-identical, imports ok, keeper 3127042 / daemon 3127047 restarted (commit c7816aecd). What is outstanding is G08 (f07c71cd7) and every landing from 2026-09-03 forward. Two steps the row was missing: resolve the pod ssh port from the `config.pod` alias (it drifts, 40045 -> 40048) and fail loud rather than hardcoding it; record the new daemon pid from `/proc` only, never `pgrep` | SYSTEM_GAPS X04; RESULTS_LEDGER.md row G14b (2026-09-02); memory pod_tracking_ops_2026_09_01 | LANDED 2026-09-03 a0018f9fb -- 132 files (20 harness modules + 112 __init__.py) md5 100 pct on the pod; imports 15/20 under /usr/local/bin/python (5 fails = never-deployed 09-01 eval_gate deps / stale clv_beatrate_rollup / no xgboost -- S47 scope, none imported by a running process); predict_service.app bounced 19239 -> 24212 under the keeper; quant_exec.build_validation now imports; mlb book capture relaunched pid 21622 with the env flags, cadence row 14:15:22 UTC (achieved 30.0 s, latency 0.422 s) -- S19 30-pass can start; /ready still false-negative (S49b); S21b 2026-09-03 7dc5a6645 -- FULL-TREE PARITY: 3,427 tracked-at-HEAD files (3,501 minus 74 tracking-owned) deployed, md5 100 pct identical (322 absent + 80 stale = 402 changed); imports 2,069/2,118 -- of 49 fails 11 reproduce locally (S61), 38 pod-only (20 statsmodels -- installed 2026-09-03 + pinned in the bootstrap; 7 src.prediction.bet_grades human-gated; 10 .gitignore:342 scripts/**/_* helpers UNDEPLOYABLE by any archive -- S60; 1 paper_track_record IndexError); preflight 14/14; one child bounced m1_paper 19449 -> 152399; the pod is pinned at 4ff779286 and drifts with every landing; S21c delta parity deploy PASS beddbd20d -- 50/50 md5 100 pct at 277bfa90b, imports 31/31 (the three _-helpers and statsmodels now import on the pod), preflight 14/14 + 6/6 EXIT 0 twice, one child bounced (m1_api_paper 24212 -> 203573, /ready 200), 8 deferred (5 foundry live-measured + 3 S57), 1 stale-on-pod (retired probe never deleted) |
| S22 | analytics | soccer 0/15 and tennis 0/23 mechanisms wired, blocked only by the close join | SYSTEM_GAPS A02; MECHANISM_WIRING_RESULT_2026-09-01 | LANDED c35de372a -- soccer 15/15, tennis 23/23 wired (0 not_wired); verdicts 0 CONFIRMED_LOCAL / 3 NULL_LOCAL / 35 NOT_TESTABLE, each naming its absent column (the soccer gate corpus has no event-grain ingredients; two tennis columns are non-null on 0 of 11,270 WTA rows -> single_corpus_unit flagged); ATP/WTA never pooled, walked per unit by event_date (S50 audit: soccer event_date globally monotonic, tennis one backward step at row 30,616 confirmed); whole-corpus median splits, uncharged -- AHEAD/BEHIND needs a charged S12 T2 trial; 15 + 16 + 18 tests green |
| S23 | analytics | the `harness_health` MCP artifact has no generator | SYSTEM_GAPS A03; MCP_ADVANCE_2026-09-01 | LANDED 2026-09-03 bbf49a597: harness_health_report.py composes 5 existing artifacts (golden run, null-ship, retro-correction, 13-row FWER ledger, gate manifest); handler no_data -> ok with source_artifact + as_of(as_of_source labelled); 4+5 tests pass; artifact itself gitignored-local; VERIFIED LIGHT 1a68fdd8f: ACCEPT |
| S24 | ops | artifact refresh has no scheduler (`fleet_on` false) | SYSTEM_GAPS A04; MCP_ARTIFACT_FRESHNESS_2026-09-01 | ACCEPT 2026-09-03 863a62d72 -- artifact_refresh.py --once: 3/3 consecutive real passes monotone (n=3 CONSTRUCT), 4 producers wired (atlas, mechanism_exposure, harness_health, execution_status) + tracking_program_status NO_PRODUCER named every pass; all 5 MCP handlers ok after refresh; NO OS task armed; 7 tests |
| S25 | corpus | the ingest content gate LANDED and has never run on the real queue: no queue JSON re-gated, and the pod daemon plus the local downloader still run the old code | RESULTS_LEDGER.md G01c row (4212afa1e, 14 tests pass, "NOT verified" list) | BLOCKED on S21 |
| S26 | corpus | the called-pitch CSV is cached at 66,665 rows but `command_target` columns need video only baseball METRIC_LOCAL produces, so the framing gate stays NOT_TESTABLE | SYSTEM_GAPS D02; FRAMING_PREREG_RESULT_2026-09-01 | BLOCKED on the tracking baseball lane; it is what blocks S04's first REAL trial |
| S27 | analytics | the answer contract is documented but not mechanical: nothing enforces `source_artifact` + `as_of` + verdict on every answer, or a refusal in their absence | `docs/AI_CONSUMER_CONTRACT.md` (129 lines, verified) | LANDED 2026-09-03 29eccf7c9: 50-probe envelope test over 13 tools, HONEST RED 15 green / 35 red (dominant systemic: no tool emits staleness_days -- 28 probes; P2 win_probability as_of=now 4/4 sports; P3 system_health confirmed); two predicted reds FALSIFIED green mid-session (harness_health via S23, execution_status via S20 readout); follow-ups S37 S38 |
| S28 | ops | secrets-scan and the data//vault/ staging guard exist only as prose in three runbooks; push to public `origin master` is allowed by the 2026-09-07 override, so the one step protecting the public repo is the one a long unattended night skips. No hook enforces it (the SessionStart `loop_status.sh` hook proves the mechanism works here) | `.claude/settings.local.json` (SessionStart hook present, no pre-push guard); `.claude/rules/data-vault-nocommit.md`; TRACKING_PROGRAM_STATE section 4 step 4 | LANDED 2026-09-03 2c68c78f1: pre-push guard live at .git/hooks/pre-push (private trees + secret patterns + non-fast-forward refused, 5/5 CONSTRUCT); first real push scanned 4 commits clean; caveats: local hook (fresh clone unprotected until install_prepush.sh), --no-verify bypasses |
| S29 | ops | the FWER audit trail has no backup: `data/cache/eval_gate/backtest_fwer.jsonl` (13 rows verified) is what every `deflated_p` in the program is computed against, `hypotheses.sqlite` (S15) will join it, both are gitignored and pod-authoritative, and a volume loss or bad write makes every past verdict unreproducible | `data/cache/eval_gate/` listing (backtest_fwer.jsonl, .lock, two trial JSONs, gate_manifest.json -- no backup); S15 | LANDED 2026-09-03 a9a3a74a0 (main-repo lane after the worktree FALSIFIED correctly): ledger_backup.py 5/5 CONSTRUCT + real run -> data/backups/eval_gate/2026-09-02/ (4 files, manifest sha256s verify, source byte-identical 52785ad2); NIGHTLY NOT ARMED -- the schtasks one-liner is in the memo for Neel; VERIFIED LIGHT 1a68fdd8f: ACCEPT |

| S30 | ops | the pod paper stack runs ORPHANED: bankroll_daemon 8606, inplay_capture_runner 8608, pm_paper_tick_runner 8609, settle_sweep_daemon 8994 all have ppid 1 (started 2026-08-31 16:02 UTC by a supervisor whose m9_supervisor heartbeat stopped 08-31 20:12 UTC); no `python -m supervisor` process exists, so a dead child is never relaunched and the S20 week has no keeper. Starting a second supervisor over live children risks two writers (the S20 integrity stop rule) -- adoption behaviour must be read from supervisor/supervisor.py before any launch | /proc scan 2026-09-02 04:16 UTC via the config.pod alias; daemon_heartbeats listing; supervisor/_singleton.py | CLOSED 2026-09-04 by measurement on the NEW pod (213.192.2.123): pod_bootstrap.sh booted `-m supervisor --profile paper` pid 36169 and every paper-stack process is its child (14 of 14 ProcSpecs: scheduler, app, auto_loop, bankroll_daemon, inplay_runner, inplay_capture_runner, pm_paper_tick_runner, kalshi_scan_runner, pm_close_capture_runner, ingame_clv_verdict_daemon, ingame_paper_settle, http_wedge_reaper_runner, settle_sweep_daemon, exec_evidence_daemon; ps ppid = 36169, 0 orphans with ppid 1 except mlb_book_capture 36193 which is not a supervisor child by design); pod_bootstrap_check --functional 7/7 incl. supervisor_lock_env. The orphaned 2026-08-31 stack died with the old pod |

| S31 | harness | S04 landed with two verifier-filed defects: the CONSTRUCT tests give every row a unique game_id so ICC=0 and the INSUFFICIENT branch (n_eff<30, clusters<20) is never exercised; and _student_plus_ids hardcodes prior_strength=50.0 while run_student_gate never threads the parameter, so the two id-using arms silently diverge for any other value | S04 verifier report 2026-09-03; scripts/platformkit/eval_gate/student_gate.py (landed c7523ce63) | DONE 2026-09-04 d48731fe3 -- ACCEPT (codex a12 2bb4b0a69; verified Sonnet, contract A/B/Q, Opus 529; memo S31_student_gate_defects_2026-09-04.md). Premise reproduced on master (test rows unique game_id -> ICC 0 short-circuit at gap_effective_n.py:37; prior_strength 50.0 hardcoded at student_gate.py:102, not threaded). After: prior_strength threaded as a keyword defaulting to 50.0 (0 other callers), new 19-cluster CONSTRUCT case reaches INSUFFICIENT with a real icc 0.0652, student_plus_ids at prior_strength 3.0 matches an independent recomputation to 1e-12 and differs from 50.0; the four thresholds byte-identical; test_student_gate 4/4 (was 2); 217 LOC |
| S32 | execution | THE keystone blocker: the pairing bridge has produced 0 paired rows in 36.6 h across two live MLB windows -- inplay_capture_loop.py:794-816 returns no_live_state for EVERY game (88/88 in the sampled cycle; 380 settle rows exist but `grep -l model_prob data/cache/ingame_grade/*/*.jsonl` = 0 files, so capture_pair_once has NEVER written a paired row); neither ls_fn nor _scan_live_by_legs bridges a Kalshi ticker (e.g. KXMLBGAME-26SEP022210STLLAD) to an ESPN live state; also the stop rule keys on daemon liveness so this failure is invisible to it -- 'zero paired rows across a completed live window' must alarm | S20 premise memo section 2 (433f76d49); pod /proc + data reads 2026-09-02 04:36 UTC | VALIDATED 2026-09-03 14:05 UTC: after the 05:08Z corpus deploy the pod ledger went from ABSENT to 22 MLB moneyline rows (11 open / 11 settled, first 05:28Z, last 06:09Z) -- the maker pool filled within 20 minutes of the fix; then the container restarted (~06:47Z) and the orphaned stack died (S30). CLOSED as root-caused and fixed; remaining sub-item = the series-twin start-time guard (in LANE I / S39) |
| S33 | execution | /workspace/mlb_book_capture.log is 0 bytes with mtime 2026-09-01 23:49 while pid 3040635 runs -- the book-capture writer has written nothing in ~5 h, so S19's 30-pass acceptance has no cadence rows to score and the depth archive may be silently empty; needs a read-only check of where run_pod_capture actually writes before any restart | S20 premise memo section 2; /proc/3040635 fd/1 | OPEN (new 2026-09-03) |

| S34 | harness | `close_join.gate_corpus_states` stamps a SYNTHETIC vintage (state_ts fixed 12:00:00, feature_avail 00:00:00), so walk_forward's leak guard passes BY CONSTRUCTION on these states -- any sport scored through them (S03, S22) inherits a leak check that can never fail; needs a real odds/feature timestamp or an explicit SYNTHETIC-VINTAGE label carried into every artifact | S02 verifier report 2026-09-03; scripts/platformkit/eval_gate/close_join.py (landed 40e991be1) | LABELLED 2026-09-03 ff094421c -- coverage_report and every state carry vintage: SYNTHETIC until a real odds timestamp exists |
| S35 | harness | `close_join.coverage_report.by_corpus_unit` groups MATCHED rows only, so per-unit join_rate is 1.0 by construction and hides per-unit coverage holes wherever unjoined > 0 -- exactly the S03 case (ATP 84.4 / WTA 71.2) | same report | FIXED 2026-09-03 ff094421c -- by_corpus_unit uses the full spine per unit; a 1.0 per-unit rate with unjoined > 0 now raises; soccer unaffected -- 2026-09-04 spec-writer premise re-measure: FALSIFIED, fix stands (close_join.py:290-301 groups the full frame and raises on a 1.0-rate unit with unjoined > 0; test_close_join_tennis.py:63-95 9/9; memo S35_coverage_by_unit_2026-09-04.md) |

| S36 | harness | e4_blend and e2_regime fit windows self-leak across UTC midnight: gap_blend_arm.py:84 asserts DATE order only, and 124/178 corpus games span midnight, so 52.86 pct of e4's scored ticks (25,000/47,292) and 43.49 pct of e2's (2,867/6,593) had their OWN game's outcome in the fit window. Measured effect: e4 leak-free 0.206786 vs shipped 0.207033 (leak did NOT flatter; the E4 AHEAD direction stands, conservatively), e2 leak-free 0.254351 vs shipped 0.252261 (leak FLATTERED e2 by +0.002090). Fix = game-first-date fit windows + per-fold game-disjoint assert in both arm modules; the S06 stacker already consumes only the leak-free variants | S06 OOF pre-flight memo c5d49ce58 (Fable lane, Briers reproduced to 1e-9) | OPEN (new 2026-09-03) -- arm-module fix pass after S06; 2026-09-03 E4 replication on a disjoint MLB corpus attempted (Lane R, 6330190cf): INSUFFICIENT -- 0 disjoint scored games vs the 30 floor, nothing charged, K stays 14, e4_blend remains SINGLE-WINDOW (Q5); window 2 = S55 -- DONE 2026-09-04 9c68ecda8 -- ACCEPT (Sonnet build in a13 da6990329 + correction 3998d5a46 because codex was 404; verified Opus twice, contract A/B/Q; memo S36_midnight_fit_windows_2026-09-04.md). fit_window='game_first_date' (opt-in) + per-fold game-disjoint assert: e4 0.206785778212713 (n 47,104 / 158 games, 0 leak ticks), e2 0.254350980569173 (n 6,579 / 157 games, 0), reproduced to 1e-15; legacy tick_date mode now COUNTS self-leak (25,000/47,292 = 52.86 pct; 2,867/6,593 = 43.49 pct) with one warning and never raises, default-mode output byte-identical to master; 9 readers classified in the memo table; tests 6+5+4+1. Verifier NEW GAPs (not blocking): the register's 'shipped' Briers 0.207033 / 0.252261 are not what master's default mode returns today (0.206629 / 0.251859) -- the row's before-numbers came from the S06 preflight slice, re-label on next touch; S36_repro_2026-09-04.py still asserts the pre-correction 'tick_date raises' claim -- housekeeping |

| S37 | analytics | HONESTY-CRITICAL refusal miss: the ask resolver classified probe E04 'how much money can we make beating the closing line' as player_stat instead of edge_language, so it answered instead of refusing; the edge-language classifier needs money-phrase coverage ('how much money', 'beating the closing line', 'make money', 'winnings') with the envelope test's E-probes as the regression | S27 memo 29eccf7c9 probe E04 | LANDED 2026-09-03 f8af99a63 -- E04 refused with the rule citation; money-outcome family 0/8 -> 8/8, E-probes 3/4 -> 4/4, calibration questions 3/3 unaffected (109 tests green across the resolver suites); phrase list is a floor, adversarial probing still open; VERIFIED LIGHT 1a68fdd8f: ACCEPT |
| S38 | analytics | envelope systemic gaps behind 30 of the 35 S27 reds: (a) NO tool emits staleness_days (28 probes; the only implementation is the unwired P1 gate_manifest_tool -- human applies P1 + restarts the MCP server); (b) no_data responses missing a note in rating_attribute + compose_scout; (c) mechanism_effect returns a fifth status 'ambiguous' documented in AI_CONSUMER_CONTRACT.md but not admitted by the envelope -- either admit it or map it; (d) comparables stamps wall-clock as_of over a 2026-07-26 corpus (P2 class) | S27 memo 29eccf7c9 | LANDED 2026-09-03 aaf021002: ACCEPT -- (b)(c)(d) fixed, envelope 16 green / 34 red -> 21 green / 29 red (+A02 A07 A08 S04 S05); ambiguous admitted as the documented fifth status AND required to carry source_artifact + note (stricter); comparables as_of = corpus mtime + corpus_staleness_days + computed_at; (a) staleness_days NOT emitted -- P1/human, 28 of the 29 remaining reds are exactly that; the 29th is H01 (P3); no assert weakened |
| S39 | harness | pre-existing per-file test defects recorded by the baseline (commit e0eb96e12): tests/platformkit/ingame/test_inplay_capture_loop.py 10 failures on one pair-capture field (likely the S32 bridge -- confirm after S32 lands); tests/platformkit/test_scoreboard.py 2 failures (settled_no_close rows counted into CLV); tests/platformkit/mcp_server/test_server.py 1 subprocess 120 s timeout (flaky?) | TEST_BASELINE_2026-09-03.md de4730003 | ACCEPT (landed) 81c2fbdc3 d9e91049b f2075326c -- three pre-existing test defects root-caused and FIXED: capture-loop 10 failures were a stale taker-era decision contract (a1fb821c6 maker lifecycle), NOT the S32 twin mis-bind, 51/10 -> 61/0; scoreboard 2 failures were a stale duplicate suite plus real docstring drift (no constant moved), 24/2 -> 26/0; MCP 120 s timeout was flaky-slow (2 of 3) from 18 stale claims-index sidecars forcing a 2.87 GB residual scan, fixed by rebuilding the sidecars with the timeout unchanged, 7/1 in 138 s -> 8/0 in 3-4 s; follow-ups: nothing in scripts/ rebuilds the sidecars; two suites cover pm_trading.scoreboard |
| S40 | harness | harness-core red-team remediation program: 21 reproduced findings (memo d4e29f46c), worst = RT-1 romano_wolf NaN-poisoning manufactures rejections; RT-2 hedge_trial_arms key=id(train) stale-split cache (28 splits -> 3 addresses); RT-18 deny-list vintage redaction lets a new settled column through (oracle Brier 0.0000, no LeakError); RT-3 a charged trial in a WORKTREE reads a fresh ledger K=1 (why S06 runs main-repo only); RT-19 contended ledger lock fails open; RT-11 shin assert-only guards die under -O; RT-8 planted-null crashes read as fdr_hat=0; plus 4 open prior-red-team items (run_gate BEATS_CLOSE on raw p, DM_MIN_N counts states not clusters, n_trials=2, golden fixture unhashed) | REDTEAM_HARNESS_CORE_2026-09-03.md d4e29f46c | PARTIAL 2026-09-03 5a2c9618e: RT-1 (romano_wolf degenerate-arm mask -- manufactured rejection reproduced then killed), RT-8 (all-crashed nulls now fdr_hat=None frozen=True), RT-11 (shin domain guard 0<pi<=1 raises under -O too; lane corrected the memo's bracketing diagnosis by measurement), RT-19 (contended ledger lock now raises LedgerLockError, all 8 sites, never runs unlocked) all LANDED with 20 per-file tests green; RT-2/18 handled inside S06's arm rules; REMAINING open: RT-3/20 K-source discipline, RT-5/6/7/9 silent caps, RT-12..17/21, 4 prior-red-team items -- week-1 queue; VERIFIED LIGHT 1a68fdd8f: ACCEPT (RT-1 reproduced byte-for-byte; S40b RT-18 strict-mode LeakError reproduced; 76 tests green across 12 files); S40b 2026-09-03 (112c995d7 b2c04dce5 e30d32ca9 6012ec47c e13db0cbe 7ef4d6c6c): RT-18 deny-list -> allow-list redaction (oracle Brier 0.0000 leak killed, run_gate strict), RT-3/RT-20 K-source discipline (non-canonical ledger refused unless explicit; prior_k = max(ckpt, ledger)), RT-5/6/9 three silent caps named, RT-4 DM interval on the same t as its p-value (every DM CI in the tree is now slightly wider; no published artifact recomputed -- S06's CI still excludes 0 by a wide margin), RT-21 three tautological tests derived; prior-red-team 1.1/1.2/1.3 FIXED, n_trials=len(CORPORA) PROPOSED (S51); 12 new tests, 23 suites green; VERIFIED LIGHT 1a68fdd8f: ACCEPT (RT-1 reproduced byte-for-byte; S40b RT-18 strict-mode LeakError reproduced; 76 tests green across 12 files). REMAINING open: RT-2/10/12..17; S40c 2026-09-03 (870a01ac1 3b3045444 6754f1226 2347cc948 1ea75edb4 17174e672 bf1871c2a 7e065e672): REMAINDER CLOSED -- RT-2 stale-split cache killed (400/448 stale calls -> 0), RT-10 one charge per (predictor, corpus) under its own sport, RT-12 empty sealed holdout raises, RT-13 synthesized team key labelled (real home/away PROPOSED), RT-14 sweep width tied to the catalog (raise not assert), RT-15 equal CSCV blocks + reported trim, RT-16 corpus_counts beside n_games, RT-17 gate_corpus_states drop accounting; 110 tests green across 16 files; no bar moved; nothing of RT-1..RT-21 remains open |

| S41 | corpus | the mlb gate corpus is STALE and blocks every read-only 4-sport row: data/cache/combo/gate_corpus_mlb.parquet built 2026-07-05, source parquet changed 2026-07-16, so load_gate_corpus('mlb') raises StaleCorpusError; needs a build_gate_corpus('mlb') rebuild row with a freshness check (specs pin the loader read-only, so the rebuild is its own lane) | S05 verifier e72347e48, reproduced in master | LANDED 9cb019ca4 -- ACCEPT: premise confirmed (2/4 mlb sources moved), rebuilt with the builder untouched 38,809 -> 39,162 rows, column set identical, y/p_base identical on all 38,800 overlapping ids (362 new / 9 gone upstream); loadable sports 3/4 -> 4/4; additive freshness_report(sport); 10 passed |
| S42 | harness | two bin-edge rules disagree: wp_diagnostics.reliability bins by min(9,int(p*10)) while scoring.ece / calib_decomp.decompose use numpy edges, so any artifact publishing both a bin table and a summary ECE/Murphy figure is self-inconsistent whenever predictions land exactly on the 0.1 grid (10/1,814 nba isotonic rows moved) | S05 verifier e72347e48 | LANDED 51045f640 -- ACCEPT: calib_decomp.bin_edges/bin_index is the one rule; nba isotonic bin-table-vs-summary ECE 9.27e-04 -> 1.28e-16 (n = 1,814, 0 dropped; raw reproduces on both at 4.86e-17); legacy_bins=True keeps the old assignment; labels and bars unchanged; CORRECTION -- 13 rows move / 142 grid landings, not 10/94 (those came from the untracked a10 module); memo S42_bin_edges_2026-09-03.md |
| S43 | harness | max_loser_wp is degenerate on the pregame gate corpora (one row per event_id -- every 'game path' is a single tick), so it can never gate a promotion from that input; it needs the per-tick in-game stream (data/cache/ingame_grade_joined) as its input when it becomes a gate | S05 verifier e72347e48 | LANDED d84576202 -- DESCRIPTIVE (no bar armed): max_loser_wp fed the per-tick in-game stream (47,104 ticks / 158 games, 81 multi-tick loser paths); raw_model / e4 leak-free / market: Brier 0.236683 / 0.206786 / 0.195387, ECE 0.086901 / 0.074150 / 0.066715, Murphy REL 0.013128 / 0.009860 / 0.006109, max-loser-WP p50 0.6582 / 0.6700 / 0.6200, p90 0.9677 / 0.9059 / 0.8900, share > 0.8 0.4198 / 0.1605 / 0.1481 -- the guard's gain is in the tail, not the median; Briers reproduce the S06/E4 anchors; ESS n_eff 420-483; gating needs its own sealed bar (Q3); 4 tests |
| S44 | corpus | the gate corpora carry NO date/timestamp column, so walk_forward_recalibrate (and any walk-forward over them) is chronological only by row position; every leak-free claim built on these corpora is positional -- add a real date column at the builder or label every such artifact POSITIONAL-ORDER | S05 verifier e72347e48 | LANDED d1520be04 -- ACCEPT: additive event_date on all 4 gate corpora at the builder (row counts unchanged, every pre-existing column DataFrame.equals True), order_basis 0/4 -> 4/4; MEASURED tennis is NOT globally chronological (ATP 30,616 rows ahead of WTA 11,270; row order jumps 2025-12-17 -> 2015-01-19) -> S50; order_basis stays POSITIONAL-ORDER because walk_forward_recalibrate reads row order; 10 passed |

| S45 | harness | gate-manifest staleness follow-ups from the S09 landing: (a) --max-age-days OVERWRITES status UNREADABLE/EMPTY with STALE, hiding unreadable artifacts from gate_manifest_tool and harness_health_report; (b) render_table's footer omits the STALE count a human reads; (c) 48/52 eval-gate artifacts carry no as_of-style field, so arming assert_fresh today blocks 92 pct of rows -- producers must stamp generated_at before S27 arms the gate | S09 verifier 18098d582 | LANDED 4f10eb59f -- (a)+(b) ACCEPT and S40 RT-7 CLOSED: stale is its own flag (UNREADABLE filter 0 -> 1 rows), footer STALE=<n>, a future measured_at beyond 1.0 d tolerance is measured_at_invalid=future and never fresh (RT-7's -1581.0 case blocked); additive 53 rows +2 keys; (c) RE-FRAMED -- the 48 unstamped rows are all category=evidence, all 5 category=ledger rows are stamped and 0/5 stale, so the gate can arm today scoped to ledger (not armed); memo S45_manifest_followups_2026-09-03.md |
| S46 | harness | grammar follow-ups from the S11 landing: (a) `.gitignore:342` `scripts/**/_*` silently untracks EVERY __init__.py under scripts/ -- all platformkit packages are namespace packages on a fresh clone (repo-wide finding, worth its own fix); (b) Hypothesis carries no family or runtime_available field, so the FWER family and teacher/runtime split are unrecoverable from an enumerated row; (c) _REGISTRY_PATH is relative -- off-root cwd silently changes every hash; (d) `phase=` conditioning alphabet left open to typos; (e) the catalogue-to-sport mapping lives in the test, not a shared module S12/S15 can reuse | S11 verifier b5c60e36e | LANDED 4ca819047 + c8bdc8a68 -- ACCEPT: (a) .gitignore:342 negation, tracked __init__.py 20 -> 113/114, fresh-clone import verified, 1 held by :208 models/ (S49); (b) family + runtime_available additive, hash-excluded; (c) _REGISTRY_PATH anchored, same hash from 3 cwds; (d) phase alphabet closed {1..9,period,quarter,inning}, measured union {1,2,3} over 5 files; (e) catalogue.py shared mapping, 69 present / 5 absent named; S11 reproduced exactly 116,370 distinct / 0 collisions; the test-local mapping had mislabelled gate_corpus_nba (-810); tests 5+13, readers 6+7 |

| S47 | ops | pod packages are NOT persistent: a RunPod container restart keeps /workspace but wipes /usr/local/lib/python3.12/dist-packages (fastapi, uvicorn, scikit-learn were gone on 2026-09-03; only the track daemon survived because someone relaunched it), and no bootstrap script exists in the repo (scripts/pod_bootstrap.sh absent; the memory's sync_to_pod.sh is gone). Needs: a tracked scripts/platformkit/ops/pod_bootstrap.sh that installs requirements-predictor.txt + requirements-live-v2.txt with --break-system-packages, ships every scripts/**/__init__.py (S46a), boots the supervisor paper profile under /usr/local/bin/python, relaunches the mlb book capture, and prints /proc pids + heartbeats -- one command after any restart. SCOPE ADDED by the S21 deploy: the bootstrap deploys the FULL tracked scripts/platformkit + supervisor + predict_service trees (a diff since a guessed baseline missed spa_test/cpcv_engine/deflated_metrics from 09-01), installs xgboost, and persists CV_CAPTURE_POD=1 CV_MLB_BOOK_ARCHIVE_LIVE=1 for run_pod_capture (it dies instantly without them) | this session's pod work; /workspace/paper.log; docs/operations/runpod-runbook.md | LANDED 3c0bc7f4f -- PASS: scripts/platformkit/ops/pod_bootstrap.sh + pod_bootstrap_check.py; pod preflight 14/14 paper modules import (n = 14 CONSTRUCT), EXIT=0, both boot guards SKIP with self-exclusion proven; xgboost 3.4.1 installed, 4 long-landed modules deployed 4/4 md5-identical; S21's 5 import failures now 3 fixed / 2 blocked on stale pod siblings (walkforward.py, mlb_state_features.py -- full-tree parity deploy queued after S40b lands); nothing killed, nothing started; the .sh boot steps have never executed end to end (needs a disposable container) |

| S48 | corpus | tennis odds.parquet has 93 event_ids that name TWO different real matches (33,952 rows over 33,859 distinct ids; e.g. one id = Brisbane R2 2015-01-07 AND Australian Open R3 2015-01-24): the id is built from players + season, collapsing same-pairing rematches -- an upstream id-construction defect that inflates every raw merge and forced S03's ambiguous-drop | S03 lane ff094421c | ACCEPT 2026-09-03 6bad8412f -- PREMISE FALSIFIED: the id already carries tournament + date and BOTH spines are unique (30,616/30,616, 11,270/11,270); the 93-id collision is the ODDS JOIN (join_odds 20-day window lets two tennis-data rows claim one Sackmann match). Additive event_uid landed in the three builders; spines rebuilt with 0 columns changed; odds migrated by derivation; close_join gains an opt-in key (default byte-identical); coverage_report('tennis', key='event_uid') ambiguous_drop 186 -> 0, joined +93, ATP 84.3709 / WTA 71.2334 pct against the untouched 84.4 / 71.2 bars (S03 stays CLOSED AT LIMIT); 130 tests |

| S49 | ops | deploy follow-ups: (a) scripts/team_system/models/ (__init__, domain_model, domain_registry) is untracked via .gitignore:208 `models/` -- invisible on a fresh clone; scripts/team_system/** is HUMAN-GATED so Neel decides (negation line + git add); (b) predict_service /ready is a FALSE NEGATIVE: supervisor/health.py:222-224 caches mount_selfcheck on the app while app.py:579 calls it before the paper routers register, so /ready says not_ready while /api/paper/predictions serves 200 and is in /openapi.json; (c) strength_atlas and mechanism_exposure report an as_of that is not a timestamp, so envelope freshness cannot be judged (S24 lane) | S21 lane a0018f9fb; S46 lane; S24 lane | OPEN (new 2026-09-03); (b) LANDED e7508c5a3 -- mount_selfcheck re-reads app.routes at probe time; before 503 not_ready beside a 200 route, after 200 ready; fail-closed intact; 5 + 7 tests; pod deploy pending (parity lane carries supervisor/health.py; the live port is 8099, not 8098) |

| S50 | corpus | the tennis gate corpus is NOT globally chronological: ATP's 30,616 rows precede WTA's 11,270, so any positional walk-forward over the whole frame jumps from 2025-12-17 back to 2015-01-19 at row 30,616 -- every positional-order tennis number (S05's tennis ECE included) rests on a non-chronological pass; fix = walk-forward per corpus_unit sorted by event_date (now present, S44), never over the concatenated frame; audit soccer's div ordering the same way | S44 lane d1520be04 | LANDED 12db4d4f5 -- ACCEPT WITH CORRECTIONS: per-unit event_date walk added to calibration_report.build_report as OPT-IN (default OFF): a per-unit walk is a no-op NOWHERE (ece_after moves on all four: nba 0.024843 -> 0.026583, mlb 0.008077 -> 0.012666, soccer 0.009302 -> 0.028722 with six interleaved divisions, tennis 0.008403 -> 0.015403), so the S05b artifacts stay untouched and a separate *_per_unit_* artifact family is written; tennis re-scored ATP 0.039461 -> 0.012985 / WTA 0.038484 -> 0.023195 (the whole S05b cost was WTA calibrated on a decade of future ATP history); ece_before reproduces to exactly 0.0 on all four; PREMISE PARTLY FALSIFIED: close_join.gate_corpus_states was already date-sorted with spine date == event_date on 100 pct of soccer/tennis states, so it is unchanged and pinned by a test; 9 + 8 tests; no caller switched yet (batch_gate.py:193 still walks row order) |

| S51 | harness | two S40b leftovers: (a) run_gate.n_trials = len(CORPORA) = 2 prices its deflation as if two trials ever ran; reading the ledger there breaks --golden's offline contract, so it needs an explicit --k-from-ledger mode; (b) gen_golden does not emit the new GOLDEN_SHA256 digest, so a legitimate fixture regeneration fails the gate until the constant is hand-updated | S40b memo 7ef4d6c6c | LANDED 2026-09-03 b291b0fcf -- ACCEPT: (a) --k-source {corpora,ledger}: default corpora byte-identical to master (--golden stdout sha256 28ba7f97 before AND after) and the only source --golden accepts; ledger reads max k_cumulative READ-ONLY, appends nothing, refuses a non-canonical ledger (RT-3 rule); tmp ledger K=7 priced at 7 with bytes unchanged; (b) gen_golden emits <fixture>.sha256 and refreshes GOLDEN_SHA256 in place, expected_seal prefers the sidecar with the constant as fallback, fails closed naming BOTH on mismatch; real regeneration reproduced the fixture at digest fe9298fc; no bar moved; 11 + 12 tests, 6 downstream suites green; --k-source ledger has zero production callers yet |

| S52 | corpus | no local historical MLB moneyline CLOSE exists for 2022-2025 (S10 measured: local price series covers only Kalshi 2026 two-sided with a first-pitch clock; 2023/2025 Polymarket one-sided without one; 2022/2024 absent), so 22 MLB mechanisms and every modern MLB market-relative test stay NOT_TESTABLE until an external historical close source (the same free-odds family the soccer/tennis odds.parquet came from, or a Kalshi history pull for 2023-2025) is acquired, licence recorded, and joined through close_join_mlb | S10 lane 161d87c43 | CLOSED (licence) 2026-09-03 22953f662 -- no free 2022-2025 MLB moneyline close is acquirable under permitted terms: ESPN core API v2 carries a two-sided close but the Disney/ESPN ToU (2024-05-24) bar automated collection into a data set AND use for testing/benchmarking/validating any model or algorithm; sportsbookreviewsonline stops at 2021 (the likely origin of the local 2010-2021 odds.parquet); Kalshi returns empty markets for every 2025 event and 0 events for 2022-2024; the MIT-labelled HF dataset is odds_source=mock_data; others 403 / futures-only / refused on terms. NOTHING fetched, no parquet, K unchanged. Remaining path = a commercial licensed feed -- NEEL'S purchase decision (S62 carries the licence audit) |

| S53 | corpus | the soccer gate corpus carries only 11 pregame shots/SOT as-of columns, so all 15 soccer mechanisms are NOT_TESTABLE against the close (S22): the ingredients exist on disk (data/domains/soccer/asof_xg_proxy.parquet x 12, style_fingerprints, referee_card_foul_profiles, StatsBomb event grain) but are not joined into the gate spine; enrich the spine (additive columns, same 25,834 rows, event_date order) so the mechanisms score, then re-run S22's soccer engine | S22 lane c35de372a | LANDED 9b7ec1096 -- ACCEPT: all 15 soccer mechanisms mapped to on-disk carriers; 1 joinable as-of (diff_xg_supremacy_asof via asof_xg_proxy.parquet on event_id, 25,708/25,834 = 0.9951), 11 need an event/shot/possession grain absent at match grain, 3 name neutral/tournament on soccer_intl/results.parquet which shares 0 of 49,477 rows with the corpus; leaky same-match columns refused BY NAME; spine +17 as-of columns, 25,834 rows unchanged, every pre-existing column DataFrame.equals True, provenance in the sidecar; S22 soccer 15 NOT_TESTABLE -> 14 + 1 NULL_LOCAL (n = 16,284, six units, mixed signs, uncharged); 60 tests; corpus_cache.py now 404 LOC (split queued) |

| S54 | ops | the pod preflight is IMPORT-ONLY: pod_bootstrap_check passed 14/14 while every parquet read on the pod failed (pyarrow wiped by the container restart, found 14:50 UTC when MLBPredictor() raised 'fastparquet is required'); a preflight must run FUNCTIONAL probes -- read one parquet, construct MLBPredictor, one ESPN live-state fetch, one predict_service produce -- and the bootstrap pip step must pin pyarrow (added 2026-09-03) | this session; pod_bootstrap_check.py | LANDED 243ee4773 -- ACCEPT: --functional runs six named probes (parquet read, MLBPredictor init, produce dry-run, ESPN live-state, boot packages with versions, supervisor lock + env flags) in a child of --python, 60 s each, nonzero exit on any FAIL; pod 6/6 OK (rows 27,983; predictions 46; pyarrow 25.0.1, statsmodels 0.15.0, xgboost 3.4.1; supervisor pid 19236 carries both capture flags); wired reported-only into pod_bootstrap.sh step 3; 4 tests; the failing branch was exercised with fake probes only |
| S55 | corpus | MLB in-game corpus WINDOW 2 (the replication corpus every AHEAD needs for Q5) does not exist: the pod tick store holds settle stubs only (27 rows; paired rows need the predictor, which was down from the container restart until 14:50 UTC) and local ingame_grade has 0 usable paired+settled games after 2026-07-12; accrual starts with tonight's live slate at window 1's ~11.9 scored games/day, so the 30-game floor arrives ~3 slates later; then rerun Lane R's E4 replication (prereg first, one charge) | Lane R 6330190cf | OPEN (new 2026-09-03) -- accruing; check `n_pairs` in the capture heartbeat each morning |

| S56 | docs | public numbers that no artifact reproduces (SIGNAL_INVENTORY_REDTEAM b37bc472b): README:28 '0/85 candidate signals' -- the 0 reproduces, the 85 is the n_trials multiplicity constant printed per row while both reports enumerate 60 classes (16/15/15/14); '85 trained signals' vs 86 registry rows disjoint from the 60; '80-artifact intelligence layer' vs 151 on disk with no enumerable 80-subset; 87 vs 130 mechanisms across two artifacts. Correct README, JOB_EVIDENCE_PACKET, INTELLIGENCE.md and CLAUDE.md to the reproducible counts with the artifact each is read from | SIGNAL_INVENTORY_REDTEAM_2026-09-03.md | LANDED ACCEPT 2026-09-03 0094bc3e2 -- README / CLAUDE.md header / PUBLIC_EVIDENCE / INTELLIGENCE / JOB_EVIDENCE_PACKET corrected to 60 catalog classes (0 shipped, spa_catalog_report.txt) / 86 registry rows (signal_registry.parquet, untested) / 151 intelligence files (data/intelligence, mtime 2026-06-02); the 80-subset is not enumerable so it was de-numbered, not renumbered; residual grep 0 outside the new do-not-claim row; Q6 clean |
| S57 | analytics | the 151 intelligence artifacts under data/intelligence are ALL stale (mtime 2026-06-02) versus gate corpora rebuilt 2026-09-02, and 0 of them are registered in gate_manifest.json (19 rows), so nothing dates or blocks a stale intelligence answer; needs the artifact set registered in the manifest with generated_at (S45c pattern) and rebuilt by the S24 refresher where a producer exists | SIGNAL_INVENTORY_REDTEAM b37bc472b | LANDED f51fa46fb -- ACCEPT WITH CORRECTIONS, premise PARTLY FALSIFIED: 0 of 95 producers reads a gate corpus (real inputs are May 2026 db/parquets older than the artifacts), so the layer is wall-clock stale and ungoverned, not input-stale; 151/151 registered category=intelligence (field:generated_at 5, mtime 146), additive 59/59 identical; refresh --once: 22/151 refreshed, 5 failed (3 timeouts, 2 KeyError player_id = schema drift -> S69), 61 inputs-not-rebuilt / 44 inputs-absent / 8 unresolvable / 8 no-producer; 0 gated producers; 7 + 48 tests; assert_fresh not armed |
| S58 | signals | the factory's FIRST charged promotion batch (after S16 lands; serial, one charge each, prereg sealed first, Q1/Q2/Q4/Q5): (1) MLB e2_regime on its own 6,579-tick covered slice vs e4_blend leak-free 0.206786 (resolves a cause S06 named); (2) NBA halftime checkpoint, CI [-0.0098, 0.0015] is the closest-to-resolving row; (3) the 88 already-screened hypotheses promoted to T2 by the frozen rule; (4) the 9 uncharged BEHIND NBA mechanisms as charged trials; (5) tennis ATP + WTA as two corpus_units -- the only place the S08 two-corpora floor is satisfiable today | SIGNAL_INVENTORY_REDTEAM top-5 | BATCH 1 DONE 2026-09-03: (1) BEHIND (SINGLE-WINDOW) d43fdd521 -- e2_regime leak-free 0.254351 vs e4 leak-free 0.206079 on e2's own 6,579-tick / 157-game slice, improvement -0.048272, DM CI [-0.072123, -0.024422], deflated_p 0.001475 at launch K=15, ledger 14 -> 15 exactly once, prereg 112856f47 sealed first, family-of-one labelled (ingame_mlb_arms not in the frozen partition), PBO 0.0, ESS n_eff 467.3; e4_blend STANDS -- S06's regime cause is now a measured negative; (2) NOT RECONSTRUCTIBLE cd7ba7559 -- the NBA halftime artifact's model side has no as-of (Elo at run time, the 2026-07-18 state is gone) and per-game deltas were never archived; nothing charged; a reconstructible version is S63; (3)-(5) pending the S16 pod hour; the S16 runner now passes results_db through (S59 follow-up closed); BATCH 2 (in-game, Fable) 2026-09-03: trial A NULL (SINGLE-WINDOW) 6afb8fed1 -- MLB market-anchor clamp family (9 configs, inner-selected via cpcv) +0.000866 vs e4_gd 0.206786 on 47,104 ticks, DM CI [-0.000364, 0.002096], deflated_p 1.0 at K=16, ledger 15 -> 16, prereg 9c88ea7e8; inner selection was operative on only 5/13 folds (instrument defect -> S72; descriptive per-config table shows every d=0.10 config at +0.0052, never selected on); trial B BEHIND on 0/2 units 41ee4ea1a -- NBA halftime as-of Elo prior vs the Polymarket price on 1,593 games, -0.006583, DM CI [-0.011503, -0.001664], deflated_p 0.1488 at K=17, ledger 16 -> 17, prereg a6f5e614f, per-game deltas archived (Q9); INGAME_SIGNAL_PROGRAM memo ranks 8 hypotheses (soccer/tennis in-game have NO in-play close on disk -- blocked); S58c LANDED 9235e9cb1 + 92154a60a -- REAL T1 SCREENS: walk-forward logistic on [logit(incumbent), z(asof feature)] vs a LABELLED incumbent (devigged close soccer/tennis; p_base nba/mlb), same-game names refused, per-unit differential archived (Q9); 3,240 distinct frozen hypotheses screened locally in 249 s (17,060/hour over 4 procs): 1,212 SCREEN / 673 UNCOVERED / 1,355 refused by name or grain; HONEST DISTRIBUTION: 0/600 soccer+tennis screens beat the close by any margin, NBA 62/564 beat the recalibrated Elo (not a close); 240 candidates (12 families x 20) in S58_promotion_list; charges 0; one end-to-end T2 (soccer top candidate vs the close, verdict partition, dual bar) released as the pipeline proof; T2 #1 (factory pipeline proof) 4fac1f3ff 2026-09-03 -- MATCH (SINGLE-WINDOW): soccer_gate rank 1 diff_shots_for_asof ew10 vs the devigged close on the hash-disjoint verdict partition E1/I1/D1 (8,666), improvement -0.000057, DM CI [-0.000831, +0.000717], deflated_p 1.0 at launch K=18, family n=1 (S74), PBO 0.581, per-unit CIs all contain 0 (n_corpora 0/3, floor 2), ledger 17 -> 18 exactly once through tiers.run_tier, prereg 126dbdb48 sealed first, differential archived (Q9, memo S58_T2_FIRST_CHARGE_2026-09-03.md); the factory ran end to end |

| S59 | signals | the dual bar (S14 dual_bar_verdict) is built but foundry/tiers.py _run_charged still decides T2/T3 on the global deflated_p alone; wire dual_bar_verdict so every charged T2/T3 verdict prints both bars and AHEAD requires both, with the family taken from the frozen FWER_FAMILIES_SPEC by catalogue lookup; also consider fdr_by for correlated within-parquet families (report both q rules, decide by the spec, never after seeing results) | S14 lane bf3ae6c16 NOT VERIFIED | LANDED 2026-09-03 e9960659d -- ACCEPT: charged T2/T3 decide on BOTH bars, AHEAD iff global deflated p AND frozen-family bar; 4/4 construct cases (BH-pass/global-fail -> NOT AHEAD; both -> AHEAD; unfrozen family -> NOT_IN_FROZEN_FAMILIES before charge_tier, 0 ledger rows; 40 recorded family nulls block); q-rule prereg per family (default fdr_bh, fdr_by printed either way); spec pin 62702554f embedded; TierResult +5 additive fields; PromotionRule moved to foundry/promotion.py (re-exported), tiers.py 278 LOC; 49 tests green; real ledger 14 rows; follow-up: the S16 runner must pass its results_db through to run_tier (done in the S58 lane) |

| S60 | ops | ten `_`-prefixed helper modules under scripts/** are imported by tracked code but UNTRACKED via .gitignore:342 `scripts/**/_*` (the same rule that hid every __init__.py, S46a), so no git-archive deploy can ever ship them and the pod fails 10 imports; audit every `scripts/**/_*.py` that a tracked module imports, negate them explicitly in .gitignore (or rename), track them, redeploy | S21b lane 7dc5a6645 | LANDED 2026-09-03 7541ceabe -- ACCEPT: 187 untracked scripts/**/_*.py audited against all 8,246 tracked .py; 6 imported-by-tracked now tracked via six per-path negations at .gitignore:356-361 (181 scratch still ignored by :342; 0 gated-tree offenders); fresh-worktree import 3/3; pod md5 parity 3/3, importers 0/10 -> 10/10, 0 of 14 children needed a bounce; regression test test_private_helpers_tracked.py 1 failed -> 1 passed; static scan only (dynamic imports not walked) |
| S61 | harness | eleven modules in master fail to import on ANY box (stale references: wp_diag_oos.discover_store x2, gate_surface_catalog x2, an intel_query circular import x2, demo_render._draw_frame, market_lag_study.candidate_dirs, drift_report_metrics, nba_gru_dataset, MOUND_TO_PLATE_FEET) -- dead or drifted code that no per-file test covers; fix the reference or retire the module with a zero-reference sweep (B6) | S21b lane 7dc5a6645 import sweep | LANDED 2026-09-03 39bce8516 (memo) + 8835a46d0 -- 11/11 accounted for: 10 FIXED, 1 RETIRED (baseball_funnel_probe, B6 zero-reference sweep), 0 PROPOSED. Three causes: 4 bare sibling imports without a sys.path bootstrap; 3 consumers of a re-export 725a45aab deleted (symbols still in ingame_replay_scoreboard); 4 references to deliberately deleted APIs (2 intel_query import-order cycles, overlay_render vs the rewritten demo_render, the probe vs the retracted 60.5 ft scalar). Import sweep 15/15 at HEAD; 54 passed across 7 per-file tests + test_ask 40/46 (6 unreached). Attribution defect recorded: the shared index swept the code into c2ab121dd / c2409ff22; content verified landed. Pod still carries the 11 at its pinned sha -- next parity deploy |

| S62 | ops | LICENCE AUDIT (human decision): the repo already ingests the ESPN site/core API into soccer, tennis and MLB corpora (docs/DATA.md:158,160; mlb_price_series) and the same Disney/ESPN clause that closed S52 (no automated data-set collection; no model testing/benchmarking/validation) applies to that existing data, with no licence record on file; the soccer licence is one docstring line (football-data.co.uk 'free for personal/research use', domains/soccer/ingest_footballdata.py:6) with no URL/clause/date, and tennis's is restriction-only (ingest_tennisdata.py:7-9 'PRIVATE ... license-restricted', no permission named, no URL/clause/date -- corrected 2026-09-03 by the lane). Needs a licence ledger in the S52 format (source, URL, clause quoted, date, permitted use) for every corpus, and Neel's call on the ESPN-derived corpora and on buying a commercial historical close feed | S52 lane 22953f662; LICENCE_mlb_close_history.md | OPEN (new 2026-09-03) -- HUMAN-GATED decision; do not fetch more ESPN data into corpora until decided; LEDGER BUILT 2026-09-03 0730a4b9e -- docs/evidence/LICENCE_LEDGER.md, 21 sources in the S52 format: 6 DECIDE (ESPN/Disney, NBA Stats API, MLB StatsAPI/GUMBO, Statcast/Savant non-bulk clause, FotMob, YouTube footage), 3 OK (football-data.co.uk, tennis-data.co.uk, StatsBomb conditional on 1.2.2), 12 UNREAD (Kalshi 429, Polymarket client-rendered, Sackmann repos 404 -- the whole tennis spine sits on an unread licence whose upstream is gone -- and 8 never fetched); two docstring corrections recorded, not applied (publishers are MORE permissive than our docstrings); nothing fetched. STAYS OPEN: the DECIDE rows and the commercial-feed purchase are Neel's call; 2026-09-03 S93: DECIDE row 3 (MLB Stats API / GUMBO) is now the BINDING CONSTRAINT on MLB in-game signal quality -- a historical feed/live backfill (~3,780 keyless GETs) is the only route to 1,601 scored games; not fetched pending this decision |

| S63 | signals | the NBA halftime checkpoint claim (n 1,593, CI [-0.009833, 0.001536]) cannot be re-scored: the artifact's model side used the Elo state at run time (no as-of) and per-game deltas were never archived, so its leak-free claim covers checkpoint-row selection only; a prereg'd trial on adapter.baseline_probability(as_of) with ARCHIVED per-game deltas over the 465,249-row checkpoint corpus is the reconstructible version (one charge; ~6,371 dispatches, ~7 h -- pod job) | S58 trial 2 cd7ba7559 | CLOSED 2026-09-03 41ee4ea1a as a MEASURED BEHIND: the reconstructible version (as-of Elo prior, per-game deltas archived) scores 0.171360 vs the market 0.164777 on 1,593 halftime games, -0.006583, DM CI [-0.011503, -0.001664], deflated_p 0.1488 at K=17; the old run-time-Elo -0.0040 is not reproduced; units 2024-25 (CI excludes 0) and 2025-26 (includes 0) |

| S64 | signals | the 60 catalog classes + 86 registry signals re-screened market-relative with archived per-unit differentials (Q9) | SIGNAL_INVENTORY_REDTEAM; spa_catalog_report.txt | LANDED 2026-09-03 893e0afe7 (content) + 9e8634dce (ledger) -- SCREENED, UNCHARGED: 60 testable (2 SCREEN_POSITIVE at p ~0.03-0.05, 57 SCREEN_NULL, 1 SCREEN_NEGATIVE -- 3 of 60 crossing DM 0.05 is what chance produces), 86 registry signals NOT_TESTABLE (no column named in any corpus, both absent names recorded); top deltas all NBA vs p_base at ~+0.0027, n_eff 881; 60/60 differentials archived and reproduced to 1e-12; a NaN->0.0 masking defect that had invented a WTA unit was found and fixed before reporting; 0 of 146 declared runtime-available; NBA/MLB scored vs p_base not a close, MLB/tennis single-unit (Q5 unmet), expanding-window not CPCV (Q4 unmet); nothing charged |
| S65 | corpus | the 11 event-grain soccer mechanisms are CORPUS-limited, not code-limited: StatsBomb open data is the only event-grain soccer source on disk and covers 160 of 16,322 scored rows | S53 lane; S65_soccer_event_asof_2026-09-03.md | CLOSED AT LIMIT 2026-09-03 88a5b2498 -- licence OPEN for research / CLOSED for commercial use (User Agreement 1.1 grants analysis + research, 1.2.2 bars commercial exploitation of the data OR any derived analysis; 2.2 registration outstanding -- Neel; LICENCE_statsbomb_open_data.md agrees with S62's CONDITIONAL row). Overlap measured date-exact: 1,740 on the 25,834 spine (0.0674) -> 160 on the 16,322 SCORED frame = 0.0098 vs MIN_COVERAGE 0.25, 25.5x short, bar NOT moved; join validated (scoreline reproduces the over-2.5 label 1,740/1,740). Cause is the calendar: all four full StatsBomb league seasons are 2015/16 and the close starts 2019-08-02. Nothing built or joined; spine byte-identical; S22 soccer tally unchanged; 5 + 16 tests. Follow-up = a licensed 2019-2026 event feed (S52-shaped purchase decision, folded into S62) |
| S66 | signals | (reserved by the F2 lane) factory robustness: claim leases, producer timeouts, unarmed ProcSpecs, and the results_db family drop below -- in flight | S16/S15/S24 NOT VERIFIED lists | LANDED 2026-09-03 3d76a11e3 -- ACCEPT: 900 s claim lease + reap_expired inside claim + release (dead claimer reclaimable at +901 s, never at +899 s); 120 s per-producer cap -> TIMEOUT row, pass continues; m50_foundry_runner + m51_artifact_refresh registered with --allow-charge absent by construction, NOT in paper.json (arming line = add the two names after m44_exec_evidence; dry-run 14 -> 16 validates); the S67 family defect root-fixed in results_db._hypothesis(); 41 + 28 tests; caveats: pod DB rows from the S16 hour have lease_until NULL (release/re-seed at the next runner restart), ProcSpec has no nice field |
| S67 | signals | results_db._hypothesis() DROPS family (and runtime_available) when reconstructing a claimed Hypothesis, so run_pass falls back to queue.family: all 6,000 pod claims were labelled soccer, SF-2's per-family screened_n is unmet and promotion.top_n would rank a MIXED-SPORT group (nothing charged, K unaffected) -- fix in the F2 lane; the F1 lane recomputes family from the frozen spec until then | S16 pod hour 1763598f1 | FIXED 2026-09-03 3d76a11e3 (in S66): _hypothesis() now reconstructs family + runtime_available (columns were always stored); claim round-trips its family; a mixed queue yields per-sport groups; the pod's 6,000 mislabelled claims are stale rows to re-seed at the next runner restart |
| S68 | corpus | combo/corpus_cache sidecars pin ABSOLUTE host paths in _source_manifest, so a gate corpus copied to another machine always raises StaleCorpusError (nba/mlb/tennis on the pod); a corpus must be rebuilt on every host from its domain sources -- either relativise the manifest paths to the repo root or ship domain sources + rebuild in the bootstrap | S16 pod hour 1763598f1 | LANDED 60e10d3fa -- ACCEPT: sidecar source keys now REPO-RELATIVE + corpus_sha256; load_gate_corpus(sport, portable=True) is an explicit opt-in vouching for the parquet's bytes when a recorded source is missing OR a different file (the pod's own mlb games.parquet, measured); legacy absolute keys still load; backward compat proven (freshness_report identical on all 13 keys 4/4); sidecars regenerated with parquets byte-identical; POD 1/4 -> 4/4 portable (mlb 39,162 / nba 1,814 / soccer 25,834 / tennis 41,886); 22 + 27 tests; the pod runner does not pass portable=True yet -- wired in the post-F1 foundry redeploy |
| S69 | analytics | 4 intelligence producers are BROKEN by schema drift or wall time: build_quarter_momentum and build_tipoff_predictability die on KeyError player_id; build_cv_fatigue_trajectories, build_ingame_momentum, build_lineup_chemistry exceed 300 s -- 4 artifacts frozen at 2026-06-02; plus 129/151 artifacts unchanged in content since June with 61 provably unfixable by re-running (inputs not rebuilt) | S57 lane f51fa46fb | LANDED 3384ddee1 2026-09-03 -- ACCEPT WITH CORRECTIONS, premise PARTLY FALSIFIED: 3 schema faults not 2 (cv_fatigue crashes on KeyError velocity at 301 s, not a timeout) and player_id was never renamed -- 355/357 tracking CSVs still carry it; the cause is a sport-blind MLB tracking run (track_id/cls/x/y, 19,576 rows) inside data/tracking that every NBA builder enumerates. Guard at 3 loaders (skip, never alias), 2 iterrows quadratics removed (89.1 pct of one profile; identical on 5/5 and 6/6 games, 32x and 26x); walls crash -> 132/108/275 s, >300 -> 77 s, 627 -> 179 s; PRODUCER_TIMEOUT_S 300 -> 900 (knob, not a bar); refresh_once over the five = 5 advanced / 5, 8 artifacts regenerated; generated_at added to 2 JSONs (self-stamped 5 -> 7 of 151); 32 tests. Artifact CONTENT not scored -- no signal claim |

| S70 | ops | SHARED-TREE CLOBBER 2026-09-03 10:32 local: a PATHLESS archive-extract of the stale codex branch 7e473807c (codex/synthcal-basketball, not an ancestor of HEAD) overwrote 3,154 tracked files in the main working tree; S37/S38/S45/S57 were absent from disk for ~90 min; the tree was back at HEAD by 12:04 (restorer unidentified); the resident MCP server pid 11348 was started inside the window and needs a restart (human). GUARD landed the same day: pretooluse_guard blocks any local tar extract with no -C or -C into the main repo unless the upstream git archive names an explicit pathspec (the sanctioned verifier landing recipe); 5 probes pass | INTELLIGENCE_GAPFINDER 19ea65d6f section 1; F2 lane report | GUARDED (new 2026-09-03) -- invoking session never identified; MCP restart = human |
| S71 | analytics | answer-layer defects that are NOT human-gated (INTELLIGENCE_GAPFINDER 19ea65d6f): F1 tools.handler_for emits no staleness_days (28 of the 29 remaining envelope reds -- a mcp_server/tools.py change; only the server RESTART is P1); F2 mechanism_exposure reads games/rows but the artifact key is game_sheets (0/1,317 game_ids addressable); F3 execution_status returns ok over an artifact whose own status is no_data with an unparseable as_of; F4 compose_matchup outer as_of = wall clock; F5 the 'why does X hold' phrasing -> not_supported; F6 calibration_number serves a 41-day gitignored vault file while tracked S05 artifacts exist; F7 sentinel overall: VERIFIED at 0/11 verified. Answers graded 16/39 satisfying; intelligence artifacts reproducible 3/30, 15/30 have zero readers | 19ea65d6f | LANDED a5aaa0541 2026-09-03 (memo docs/evidence/harness/S71_answer_layer_fixes_2026-09-03.md) -- envelope 29 red / 22 green -> 5 red / 46 green over 50 probes, no assert weakened, probe file byte-identical; F1 finalize() adds staleness_days(+_source) on every ok envelope (28 reds flipped); F2 game_sheets key (0/1,317 -> addressable); F3 execution_status passes the artifact's no_data through, readout as_of ISO-parseable; F4 compose_matchup as_of = newest block, call time -> computed_at; F5 'why does X hold' resolves; F6 calibration_number serves tracked docs/evidence/calibration/<sport>_reliability_2026-09-03.json (vault file = FALLBACK); F7 sentinel never VERIFIED at 0 verified. Bar <= 1 red NOT MET and NOT lowered: W01-W04 (P2 winprob as_of = wall clock, source is a .py) and H01 (P3 system_health) are the human diffs; resident MCP server pid 11348 restart = human |

| S72 | harness | INSTRUMENT DEFECT in the clamp-family trial (S58 batch 2 trial A): one purged-empty CPCV test state makes a config's inner run fail, so the prereg'd fallback clause handed 8 of 13 outer folds to the incumbent and inner selection was operative on only 27.5 pct of ticks -- the NULL verdict measures the instrument as much as the family; repair the inner predictor to skip/handle an empty test state per state (never per config), then RE-PREREG and RE-CHARGE the family (window 2 first once S55 >= 30 games) | G1 lane 6afb8fed1 | LANDED a44b1f9f0 2026-09-03 (memo docs/evidence/harness/S72_clamp_instrument_2026-09-03.md) -- INSTRUMENT REPAIRED, no verdict, no charge: named state KXMLBGAME-26JUN271910CHCMIL @ 2026-06-28T01:30:36Z in fold 2026-06-30 had 0 purged train states; 8/13 folds at 0/9 configs OK (5 concatenate, 3 infeasible), not scarcity; fix = per-STATE missing-scoring with the empty count recorded, inner score = tick-weighted mean over non-empty inner test states, outer fallback only when NO config scored, every fold carries inner_selection operative|fallback + reason; DRY run 5/13 -> 12/13 operative (2026-06-30 is a correct fallback, 0/140 usable states; 13/13 unreachable without moving MIN_TRAIN or the embargo -- nothing moved, Q3); no Brier/CI/verdict computed; S58 trial-A NULL stays the family's only verdict; main() blocked RECHARGE_BLOCKED until re-prereg + re-charge on window 2 (S55 >= 30 games); 6 tests |

| S73 | signals | two frozen families have no matching corpus and were screened on the wrong market: mlb_inning (period/total) and nba_quarter_shape (period/spread) scored on the ML / over-2.5 gate corpora -- flagged in the promotion list as market-label mismatch, never to be charged from it; either build the period-grain corpora or drop the families from the frozen spec (a spec change, versioned) | F1 lane 92154a60a | OPEN (new 2026-09-03) | -- DECISION 2026-09-04 (Fable, after S171): period-grain corpora are NOT buildable from any local store (no period market exists in 21.87M price rows), so the honest exit is (2): a VERSIONED frozen-spec bump that marks mlb_inning and nba_quarter_shape DROPPED (v1 pin and the 18-row ledger untouched; future trials read v2; the family BH budget is re-priced for v2 only) -> S174 |
| S74 | signals | results_db.family_p_values has no tier filter, so a SCREEN's DM p is archived only (archive.screen_p) and not indexed as raw_p -- the family bar cannot see screens and T2's family n counts only charged rows; add the tier filter + index screen_p as a separate column | F1 lane; F2 owns results_db | DONE 2026-09-04 8bac62c2d -- ACCEPT (codex a10 e214b15ed; verified Sonnet, contract A/B/Q, Opus 529; memo S74_family_tier_filter_2026-09-04.md). Premise reproduced on master (family_p_values 1-arg at results_db.py:155, query filtered on raw_p only, no screen_p column). After: family_p_values(family, tier=None) -- untiered SQL byte-identical, tier='T1' returns the SCREEN's screen_p, tier='T2' the charged raw_p; screen_p is an additive result column with an idempotent ALTER TABLE migration verified on a pre-S74 sqlite holding rows; both existing callers (tiers.py:279, s58_t2_first_trial.py:113) unchanged; n = 4 CONSTRUCT rows; test_results_db 22/22, s150 5/5, tiers 12/12. Pod deploy of results_db.py + results_db_sql.py + runner restart = the next batched ssh. NEW GAP filed as S155 (test_results_db.py 391 LOC, pre-existing) |

| S75 | harness | POD SCREENS MISLABELLED: foundry_runner.screen_queue binds ONE sport's ScreenBinder while results_db.claim has no sport filter, so on the pod 1,804 of 2,168 T1 rows carry corpus = the hypothesis's own sport but were screened on the SOCCER states, and every T0 COVERED was measured on those states (S58c measured 673 UNCOVERED locally with the real binder); --predictor real refuses 3/4 sports at bind time before T0's corpus load, so the pod's StaleCorpusError 0 would be circular under real. Fix = per-sport claim (claim(sport=...)) or a binder per hypothesis sport; bar = pod T1 rows with corpus == the states' sport 100 pct on a 15-min re-read, --predictor real runs 4/4 sports | S16b memo NOT VERIFIED section | LANDED 0fa623edb + verifier 07affe55f/9b1d (2026-09-03, memo S75_per_sport_screen_2026-09-03.md) -- premise HOLDS (claim had no sport filter). Fix: results_db.claim(sport=...) + run_pass takes a {sport: ScreenQueue} map so each claim is screened on ITS OWN sport's states/partition/binder/corpus_sha (--sport a,b,c; single-queue callers unchanged); local proof --predictor real 4 sports: T1 own-sport 127/127, 0/4 sports refused at bind, T0 tennis 94 UNCOVERED (the coverage the single binder hid). VERIFIER POD STEP: load_gate_corpus(portable=None) now reads FOUNDRY_PORTABLE_CORPUS (close_join's own loads refused otherwise); the pod lacked EVERY domain as-of source the real predictor reads (S78) -- 127 top-level parquet (76 MB, espn_* excluded per S62) + the 4 S68 corpora/sidecars shipped; runner 231346 -> 254284 with --sport mlb,nba,soccer,tennis --predictor real; since restart 18:12Z: T1 own-sport 684/684 = 100 pct (mlb 39 / nba 283 / soccer 157 / tennis 205, one distinct corpus_sha per sport), T0 UNCOVERED now honest per sport (mlb 32, nba 119, tennis 137), ScreenRefused = leaky/unavailable refusals on their merits, charges 0, k_global 0, no ledger and no data/registry on the pod. All screens remain non-findings |

| S76 | harness | T2 charge path follow-ups from the first end-to-end run (S58 T2 #1): (a) RealScreenPredictor bucket cache is unsafe under cpcv_evaluate (reset per path by hand in s58_t2_first_trial.py; fix belongs in screen_predictor.py); (b) charge_tier writes the tiers SPEC pin, not a per-trial prereg seal, as the ledger prereg_sha256 (ledger row 18 carries b2b2ea5a0, the tiers pin, while the sealed prereg is 7125552f..cc30); (c) division clustering yields G=3 on the 3-unit verdict side (ICC/n_eff a lower bound; cluster by home team as the trial did); (d) _run_charged has no archive step, so Q9 was met externally. Bar: a charged trial through tiers.run_tier alone satisfies Q9 and records the per-trial seal; no charge needed to fix | S58_T2_FIRST_CHARGE memo section 9 | OPEN (new 2026-09-03) -- instrument-only; ledger stays 18 -- ATTEMPT 1 REJECTED 2026-09-04 (codex a12 867fca662; Opus verifier): acceptance 4/4 reproduced in a DRY run (28/28 distinct CPCV train-sha fits, tier pin b2b2ea5a0 kept beside a new trial_prereg_sha256 7125552f, G(div)=3 with a G(home)=6 companion, 48-row paired archive == n, tmp ledger only, real ledger 18 rows a4ae7c13 unchanged) BUT two green files went red: test_tiers 12 -> 8 passed (home_dm.n_clusters absent on the _fixed_p double) and test_family_combo_screen 3 -> 2 (refit cadence dropped from the cache key). Also: backtest_runner.py edited without the shared-module token (token now held by account1 for S76), LF append into a CRLF ledger, orchestrator files swept into the pathspec. ATTEMPT 2 = the named fix pass (codex, same worktree, log cx_s76_fixpass.log); a failed second = CLOSED AT LIMIT -- DONE 2026-09-04 1b19c491e -- ATTEMPT 2 ACCEPT WITH CORRECTIONS (codex a12 181ddec66 on 867fca662; verified Opus, contract A/B/Q; memo S76_charge_path_followups_2026-09-04.md): 4/4 constructs reproduced independently by the verifier (28/28 distinct CPCV train sets, trial_prereg_sha256 7125552f beside the unchanged tiers pin b2b2ea5a0, div G=3 primary + home G=6 companion, 48/48 paired archive rows, verdict MATCH on a tmp ledger; real ledger 18 rows a4ae7c13 unchanged); attempt-1 regressions restored (test_tiers 12/12, test_family_combo_screen 3/3), CRLF-preserving ledger append with a two-terminator test, _UNSCORED imported; 19 test files green. Corrections at landing: family_combo_screen.py back to <= 300 LOC, memo test count 1 -> 2. Verifier NEW GAPs -> S162 (factory_source_manifest fails in any worktree) and S163 (LOC rail covers 3 modules only); id(train) cache-key aliasing noted, safe under CPCV today. Token released |

| S77 | analytics | TWO PRODUCER TIMEOUT KNOBS WITH NO OWNER: S66 landed artifact_refresh.PRODUCER_TIMEOUT_SEC = 120.0 (no CLI flag; abandons a daemon thread while the subprocess keeps writing) while S69 measured walls of 77-275 s and set the producer-side PRODUCER_TIMEOUT_S to 900; the CLI refresh pass returned 5 TIMEOUT / 0 advanced (601 s) while all five actually completed. Also 6 sibling producers enumerate data/tracking unguarded against sport-blind runs (track_id/cls/x/y), and the write side that places mlb_* runs there is untouched. Bar: one timeout owner >= the measured walls, CLI refresh over the five = 5 advanced, 6 siblings guarded (tests) | S69 memo NEW GAPs | LANDED ab71dccfc 2026-09-03 (memo S77_timeout_owner_2026-09-03.md) -- ACCEPT, premise CONFIRMED (no owner existed): artifact_refresh.py 120.0 vs intelligence_producers.py 900.0, main() never passed timeout_sec so the shorter won while the abandoned daemon thread's child ran on to 900 s. ONE owner: intelligence_producers.PRODUCER_TIMEOUT_S (900.0 unchanged) imported by artifact_refresh, PRODUCER_TIMEOUT_SEC kept as an alias (B2), --timeout-sec added and passed by main(); _runner -> Popen + kill on TimeoutExpired; _run_producer kills the child on join expiry, so a TIMEOUT never leaves a writer running. 6 siblings guarded with the S69 guard (build_clutch_cv, build_possession_type_intel, build_sequential_possession, build_shot_clock_buckets, build_trade_intel with a _has_player_id header helper at both read sites, eval_live_shot_quality; train_shot_quality already guarded). BAR MET: CLI refresh over the five = 5 advanced / 5, 0 failed, 0 TIMEOUT, wall 1165.1 s (424/76/217/242/205 s; four over the old 120 s). 39 tests per file. No bar moved. Follow-ups open: the 4 in-process front-door producers still abandon their thread on timeout; the write side that puts mlb_* into data/tracking/ is untouched |

| S78 | ops | POD BOOTSTRAP DOES NOT PROVISION THE FACTORY'S SOURCE TABLES: the real screen predictor reads data/domains/<sport>/asof_*.parquet, boxdetail/carryover as-of tables, data/cache/pit/*.parquet and the close_join spines (odds/matches/wta_matches); none were on the pod, so --predictor real crashed at bind (nba games.parquet), then tennis odds, then the pre-S68 soccer sidecar, then 363 screen_failed FileNotFound per pass -- each found only by running. Shipped by hand 2026-09-03 (127 files, 76 MB, tar over ssh). Bar: pod_bootstrap_check --functional asserts every source the sidecars + screen_predictor name, and pod_bootstrap.sh (or a manifest) ships them; a runner start with 0 FileNotFound screen_failed in its first 3 passes | S75 verifier step | LANDED 46e65ad22 2026-09-03 (memo S78_factory_source_manifest_2026-09-03.md) -- premise HOLDS (git archive ships only the tracked tree; every factory source is gitignored). scripts/platformkit/ops/factory_source_manifest.py DERIVES the required set from the registries themselves (corpora + sidecar-recorded sources, asof_supply.REGISTRY, catalogue NAMED + GLOBS, family_bars sources, close_join spines, close_join_nba_mlb.SOURCES, the in-game stores' path constants; the ONE hardcode is screen_predictor._teams's three games tables, labelled): 386 paths / 227.1 MB; --check-pod = one read-only ssh sha256sum batch; --ship PRINTS the tar-over-ssh command. Real run: boot-gate subset (61 paths / 96.6 MB) OK 58 / DIFFERS 0 / MISSING 3 (mlb_price_series, nba_checkpoints_full, nba_close_corpus -- named only by the *_close sidecars and close-corpus builders the pod does not read under portable mode; reported, not excluded); full set OK 287 / DIFFERS 56 / MISSING 42 (39 MISSING are in-game stores no pod pass reads; 53 DIFFERS are ingame_grade_joined jsonl the POD ITSELF writes). pod_bootstrap_check --functional gains a factory_sources probe (non-blocking call: a restart boots and reports); runbook restart step 7. LANDMINE fixed: subprocess text=True newline-translated the stdin path list on Windows and the check falsely read 60/61 MISSING -- now bytes; an empty transport RAISES. Tests 5 + 4. Remainder: run the probe ON the pod after the next deploy |

| S79 | signals-pregame | SINGLE-FEATURE CEILING: every factory screen is one feature vs the close (0/600 beat it; T2 #1 MATCH). No family-level COMBINATION has been screened: a walk-forward regularized logistic over a family's top-k screened features (k <= 5) on the SCREEN partition only, compared with the devigged close on that partition, ranked across the 37 families; no charge. Bar: a ranked table of 37 families with combo improvement + game-clustered CI on the screen partition, leak-free (as-of features only, purge + embargo), and ONE prereg draft for the best family if its screen improvement >= +0.004 (else an honest 'no family clears the screen bar' verdict) | S58c, S58 T2 #1 | CLOSED (screen null) 2026-09-03 72fa65c70 (memo S79_family_combo_2026-09-03.md; the commit was swept by a concurrent lane's pathless commit titled G64 -- content byte-identical). Premise held (no combination screen existed); scope corrected: only 12 of 37 families have >= 2 screened singles, the other 25 are still refused (leaky name / unavailable columns, S85). L2 logistic on the top-5 per family, walk-forward on the SCREEN side only (last 800 rows, purge 48 h + embargo 3 d), k=1 control reproduces the stored S58c brier to 0.0: 0/12 clear +0.004, 0/12 CI lower > 0, combo WORSE than its own k=1 in 11/12; best nba_boxdetail +0.000979 vs p_base (Elo, not a close); worst soccer_gate -0.008374 vs the devigged close; mlb_inning CI excludes 0 on the WRONG side. Mechanism: in 6/12 families the top-5 are the same source column at several halflives (redundancy, not information) -- next pick rule = diversify by source column. In-sample selection, so ceilings. 0 charges, ledger 18. No prereg |
| S80 | signals-ingame | PLAYER-GRAIN ARM UNMEASURED: team-grain in-game PPP is saturated (e4 blend incumbent, clamp NULL, e2 BEHIND); the memory-stated signal lane is player grain (who is on the floor / at bat / serving) and no corpus audit says which in-game ticks carry player or lineup state. Bar: per sport, the share of ticks with player/lineup state (data/cache/ingame*, ingame_grade_joined, pit), the as-of availability of that state, and ONE leak-free OOF screen of a single player-grain arm vs the e4 blend on the SCREEN partition (game-first-date variants only, per S36); no charge | live_edge_ingame_grain memory, S36, S58 | CLOSED AS SCREEN_NULL 2026-09-03 8fafd8647 (memo S80_player_grain_2026-09-03.md). Audit: share of in-game ticks carrying player/lineup state -- MLB 8,384/79,566 = 10.53 pct (batter, pitcher, on-deck, bullpen ids; 2026-07-09..07-27); NBA 0.00 pct (nba_checkpoints_full 465,249 ticks / 1,593 games has a line and no lineup; ingame_eval_cache 2,476,544 rows / 1,987 games / 707 players has player_id and NO price -- they never intersect); soccer, tennis, WNBA 0.00 pct. ingame_grade_joined/mlb -- the store every e4/S58 arm reads -- carries 0 of 78,986 player columns (the close-join drops mlb_pitcher_id + 4 siblings); re-joined on (game_id, ts) = 8,309 ticks / 53 games. SCREEN of one player-grain term (pitcher as-of run-prevention residual, shrunk IP/(IP+30)) on the e4 blend, screen side only, game-first-date walk-forward, purged by game, 1-day embargo: n = 2,267 ticks / 13 games, Brier 0.248462 -> 0.244703, improvement +0.003759 BELOW +0.004, DM p 0.7937, CI [-0.0269, +0.0344]; companion purge-only n = 3,717 / 23 games -0.005770. SINGLE-WINDOW; uncharged, ledger still 18. Successors S83 S84 |
| S81 | signals-market | MARKET-MOVE TARGET UNTESTED: every screen targets the OUTCOME vs the close; a different target the close cannot contain by construction is the open-to-close move. Premise first: which corpora carry an opening line beside the close (soccer/tennis odds.parquet, MLB, NBA pit)? If any does, screen the existing as-of features for predicting sign and size of the move on the SCREEN partition (walk-forward, clustered CI); if none does, CLOSED AT LIMIT with the exact columns that exist and what acquisition would unblock it | close_join, corpus_cache sidecars | CLOSED AT LIMIT (soccer) + SINGLE-WINDOW NULL (mlb) 2026-09-03 80fce7b41 (memo S81_market_move_2026-09-03.md). Premise measured over 9 local price corpora: only soccer/odds.parquet (ou_open_* vs ou_close_*, 16,320 events) and mlb_price_series.parquet on Kalshi (935 events; open / close = first / last two-sided TRADED quote strictly before the ticker's first-pitch clock, median gap 71.15 h) carry a pre-game open. The nba clause is FALSIFIED: nba_checkpoints_full has ZERO pre-tip rows (first tick ~21 s after tip); tennis has no open column; other series tickers carry a date and no clock. B9 catch: 87.1 pct of mlb first ticks sit at the 0.500 listing placeholder (a degenerate R^2 0.673 before restricting to traded quotes). Built eval_gate/s81_market_move.py (target logit(close) - logit(open), S108 as-of columns, nested walk-forward elastic net, SCREEN side): soccer n 6,562 -- R^2 +0.00412, sign 0.5393, Brier +0.000024 CI [-0.000058, +0.000105]; mlb n 257 over 33 days -- R^2 +0.85170 but AR(1) c = 0.987-1.021 every fold (the open is discarded; the number recovers a stale 3-day-out quote), Brier +0.008550 CI [-0.007206, +0.024306]. THE CEILING KILLS THE SOCCER ROW: close - open is only +0.001311 Brier, so perfect foresight of the whole move misses +0.004 by 3x -- unreachable by construction, bar NOT lowered. No prereg, no charge, ledger 18. Acquisition to reopen nba: pre-tip prices (nba_close_corpus commence_time into a price series) |

| S82 | signals-ingame | THE FACTORY HAS NO IN-GAME TIER: the grammar's in-game families (mlb_atbat_states, mlb_pitch_states, nba_possession_states, nba_pbp_foul_states, soccer_states, tennis_setdetail) are refused by the PREGAME leak contract as 'same-game columns' (pod log: ScreenRefused leaky), so in-game state features -- the one lane with a measured calibrated improvement (e4 blend) -- are never screened. Bar: an in-game screen tier where the hypothesis = one as-of-within-game state feature added to the e4 blend, scored on TICKS vs the in-play market line at tick time on the SCREEN partition (purge by game, embargo 1 day, game-first-date only per S36), with the leak rule being tick-time as-of not game as-of; ranked table of in-game features by tick-weighted Brier improvement + game-clustered CI; no charge; a prereg draft for the best if it clears +0.004 on the screen side | pod foundry_runner_s75b.log refusals, S36, S43, S58 batch 2 | CLOSED AS SCREEN_NULL 2026-09-03 e391e3f4e (memo S82_ingame_screen_2026-09-03.md). Premise CONFIRMED: 11 live_tick families / 91 features / 46 distinct columns / 819 hypotheses (23 pct of the factory) refused 46/46 as same-game columns. NEW TIER foundry/ingame_screen.py + run_ingame_screen.py: tick-time as-of contract enforced by truncation invariance (assert_tick_asof rebuilds from src[:k+1]; TickTimeLeak), purge on SETTLEMENT (this store quotes up to ~2 days pre-first-pitch), a NULL arm [1, logit(p_e4)] fitted on the identical rows so the bar is applied to improvement vs the recalibrated null (raw e4 comparisons showed a spurious +0.006 that was recalibration, not the feature). MLB is the only scorable tick corpus (soccer supplies only e4's own signal; NBA/tennis/WNBA 0 settled ticks in the joined stores). SCREEN side 15,702 ticks / 41 games (13,184 NO_TRAIN recorded): 0/14 features clear +0.004, 0/14 CIs exclude zero; best tick_index_in_game +0.003332 [-0.001971, +0.008636], leverage_proxy +0.001148 [-0.000106, +0.002403]; score_diff re-added = -0.018 (instrument catches the degenerate hypothesis). Recalibration alone +0.006540 [-0.008751, +0.021831]; the null trails the in-play line by 0.005966. No prereg, no charge. SINGLE-WINDOW. NOT VERIFIED: 41 clusters (CI half-width ~0.005 -- the bar is unresolvable at this n, see S93), only 10/46 grammar columns suppliable, single additive term only |

| S83 | signals-ingame | THE SCORED MLB TICK STORE DROPS PLAYER IDENTITY: the close-join that builds data/cache/ingame_grade_joined/mlb (read by every e4/S58 arm) drops mlb_pitcher_id, mlb_batter_id, on-deck and bullpen ids (0 of 78,986 rows carry them) although the raw tick store has them on 10.53 pct of ticks. Bar: the join carries the five mlb_* player columns through unchanged (row count identical, byte-identical non-player columns), a per-file test, and the S80 screen re-run reads identity from the joined store without the (game_id, ts) re-join | S80 audit | CLOSED 2026-09-03 1d26b7648 (memo S83_mlb_join_player_ids_2026-09-03.md; two files swept into the tracking session's 14e3e78a1). Premise CONFIRMED: ticker_settlement_join.join_ticker_file:187-195 is a fixed eleven-key dict literal discarding every other source key; raw store 11,251 rows carry the five mlb_* columns (8,384 non-null = 10.53 pct), joined store 0 of 78,986. FIX: _CARRY_KEYS + one row.update, additive, 8 lines. Store could NOT be re-joined (espn_boxscores.parquet is down to 2 rows, 1 of 235 tickers resolvable -- S91), so a positional one-off backfill (s83_join_identity_backfill.py, zero callers) added identity to the rows on disk: rows 78,986 -> 78,986, 67,915 lines byte-identical, 0 non-player mismatches, 0 -> 11,071 rows with the columns, 8,287 non-null over 53 games; pre-S83 copy kept. Tests 7 + 5 passed. S80 re-run with additive --no-rejoin: re-join path reproduces exactly (2,267 / +0.003759), joined-store path 2,262 / +0.003623, both SCREEN_NULL, same clusters and betas (the 5-tick gap = 22 duplicate-timestamp collisions the old dict resolved last-wins). Uncharged, ledger 18 |
| S84 | signals-ingame | NBA PLAYER GRAIN IS ONE JOIN AWAY: nba_checkpoints_full.parquet (465,249 ticks / 1,593 games, market line, no lineup) and ingame_eval_cache.parquet (2,476,544 rows / 1,987 games / 707 players, player_id, no price) never intersect. Premise first: do both carry game_id + a game clock or timestamp that can be aligned to +/- one possession? If yes, build lineup-at-tick (five on floor per side, as-of) for the intersecting games and report n_games with both a line and a lineup; then ONE screen of a lineup-strength as-of term on the NBA in-game incumbent (find it: gate_run_nba_possession / ingame_crossval_nba) on the SCREEN side; no charge. If the clocks cannot be aligned, CLOSED AT LIMIT with the exact columns | S80 audit | CLOSED AS SCREEN_NULL 2026-09-03 9739463dd (memo S84_nba_lineup_at_tick_2026-09-03.md). PREMISE FALSIFIED both halves: the two stores key on different namespaces (ESPN event id vs NBA-Stats id; intersections 0) and after the real bridge (market_ticker + games.parquet, 1,280 of 1,593 priced games) only 32 games intersect (the eval cache is 1,938/1,987 games in 2022-23/2023-24; priced corpus starts 2024-10-22) -- and an eval-cache row is one player's one projected stat at one of 11 elapsed buckets, no on-floor state. Clocks ARE alignable within one possession. LINEUP-AT-TICK built from events instead: data/cache/team_system/pbp*/<nba_game_id>.json substitutions (personId + subType in/out), strictly-before guard AsOfViolation tested. Coverage 0.00 pct (S80) -> 577 games / 68,632 priced live-clock ticks at a full 5v5 (0/577 games failed the five-starters check; median 15 distinct home units per game). SCREEN of lineup_strength (five home pie_asof minus five away, centred per game) on the nba_mechanism_ladder BASE incumbent (the only NBA model fitted on this tick corpus), screen side only, game-first-date walk-forward, purged by game, 1-day embargo: n = 33,713 ticks / 284 games, Brier 0.153324 -> 0.153779, improvement -0.000455 BELOW +0.004, DM p 0.7960, CI [-0.003920, +0.003009]; market 0.144101; companion embargo-0 -0.000886. SINGLE-WINDOW; uncharged, ledger 18. Successor S92 |

| S85 | signals-pregame | 25 OF 37 FAMILIES HAVE NEVER PRODUCED A SCREEN: every live_tick family is refused by leaky name and the player / pitcher / referee-grain families are refused as unavailable (the real predictor cannot supply their columns from the gate corpora). The pregame factory surface is 12 families. Bar: per refused family, the exact column it needs and the as-of table that would supply it (or CLOSED AT LIMIT per family); plus the diversify-by-source-column pick rule from S79 as an additive option in tiers/promotion (k features from k distinct source columns) | S79 memo, S58c | CLOSED (screen null + 9 at limit) 2026-09-03 3ce89e092 (memo S85_refused_families_2026-09-03.md). Premise PARTIALLY FALSIFIED: 37 grid families = 12 screened + 11 live_tick + 14 pregame-refused, but tennis_features 15/15 and tennis_return 18/18 ALREADY supply at HEAD (S48/S65 landed after S58c, never re-screened) and nba_quarter_shape passed BOTH guards while serving 0 non-null of 1,814 -- a SILENT all-NaN column (ESPN event_id vs the corpus NBA game_id; on game_id 583/1,814; S111c). CHANGE: foundry/asof_supply.py + 8-line screen_predictor hook -- a declared registry of 11 (family -> table, join, rule) entries with rules event / side / prior (expanding mean STRICTLY BEFORE the event date via merge_asof allow_exact_matches=False); undeclared pairs refuse as before. 13 of 14 families now supply. LOCAL SCREEN (--predictor real, charges off, 404 s): 344 T1 screens, shas byte-equal to S58c / S79 / S108; 5 families screened (was 0): nba_player_value_features +0.005221 CI [+0.000244, +0.010199] vs p_base = Elo (NOT a close; 2 CI-lower > 0 of 344 vs 8.6 expected by chance, both the same column), soccer_style_fingerprints -0.001158 vs the devigged close, others span zero; 151/344 improve (43.9 pct) -- NULL by multiplicity. 8 families CLOSED AT LIMIT on coverage at the frozen 0.8 floor with measured ON-DISK acquisitions (S111): the 5 tennis families at 58 pct because the served window is 469 ATP present + 331 WTA absent (build the _wta sibling parquets from wta_matches.parquet); nba_quarter_shape 35.2 pct (fold linescores_2024_25 through espn_nba_game_bridge); soccer_referee 36.8 pct (table covers E0/E1 only). 1 with NO source: mlb_catcher_framing_index. S79 pick rule shipped as promotion.promote(distinct_source_columns=False), zero callers. Tests 4 + 28. 0 charges, ledger 18 |

| S86 | signals-ingame | THE x10 IN-GAME CORPUS IS ON DISK AND UNSCORED (gapfinder L4/L8, INGAME_GAP_PREMISES): data/cache/inplay_odds/nba_checkpoints_full.parquet = 465,249 ticks / 1,593 games / 2024-10-22..2026-06-13 with period, game_clock_s, score, margin, market_prob and outcome_home_win (0 nulls); S58 trial B scored ONE tick per game because halftime_checkpoints filters elapsed <= 24 and takes tail(1). price_checkpoint(p0, score, period, clock) is a pure state function over the as-of Elo prior, so the same code prices every tick. Bar: a model series at every NBA tick (as-of prior, no same-tick read), tick-weighted Brier vs the market by period x margin bucket on the SCREEN side, n / n_informative / n_eff per bucket, paired-loss series archived (Q9); no charge | INGAME_GAP_PREMISES L4 L8 | LANDED 2026-09-03 c05e8a067 (memo S86_nba_every_tick_2026-09-03.md) -- SCREEN_NEGATIVE, SINGLE-WINDOW. Premise CONFIRMED (465,249 ticks / 1,593 games, 0 nulls; S58 one-tick filter at s58_nba_halftime_asof_trial.py:46; price_checkpoint pure; as-of prior = ratings.replay(games.parquet, until=game_date), strictly before; no-future-read guard re-prices 3,188 ticks with later ticks withheld, max delta 0.0). S58 trial B had NO screen partition (it scored all 1,593 games), so the sibling rule foundry.tiers.partition_corpus(seed=0) on game blocks was used: screen 797 games / 232,951 ticks, verdict 796 untouched. Model 0.081922 vs market 0.077065, improvement -0.004857, DM CI95 [-0.007355, -0.002359], ICC 0.2420, n_eff 3,260, informative 82,248 (35.31 pct). 16 of 27 state cells MATCH (P4 under 6 min; P3 close > 12 min +0.003375 CI crossing 0); behind across P1-P2. MARKET'S OWN RELIABILITY: ECE P1 0.045 / P2 0.036 / P3 0.020 / P4 0.003; worst cells P2|close 0.064718, P1|close 0.055593, one-directional (0.4-0.5 bin realises ~0.55, 0.8-0.9 bin ~0.73 = early overconfidence), max-loser-WP flat across phases. Informative share P1 79 pct, P2 68 pct vs P4 14 pct. No charge, no seal. NEXT ARM = S94 |
| S87 | harness | IN-GAME TICK COUNTS ARE 70 PCT DUPLICATES (gapfinder L5): on ingame_grade_joined/mlb (78,986 rows / 227 games) market_prob is held from the previous tick on 74.97 pct, model_prob on 91.71 pct, BOTH held on 55,022 of 78,759 transitions; informative ticks 23,964; duplicate (game_id, ts) rows 1,659 (2.10 pct). Every published in-game CI is quoted against a tick count that is 70 pct redundant (the known n_eff 420-483 of 47,104 is the same arithmetic). Bar: a dedup + held-flag pass (additive columns is_dup, is_held_market, is_held_model, is_informative) on the joined stores, and every in-game verdict artifact reports n / n_informative / n_eff; the S58 batch-2 and S80 headline CIs re-quoted on informative ticks with the verdicts unchanged or honestly re-labelled | INGAME_GAP_PREMISES L5 | CLOSED 2026-09-03 71a477e41 + c67a5851a (memos S87_tick_informative + S87b_informative_enforced_2026-09-03.md). First half: premise REPRODUCED EXACTLY (dup 1,659 = 2.10 pct; held market 74.97 pct; held model 91.71 pct; both 69.86 pct; informative 23,964 of 78,986); flag_ticks + n_eff via gap_effective_n; three headline CIs re-quoted on informative ticks after reproducing each from its archived series -- S58 trial A 47,104 -> 14,543 (n_eff 566 -> 1,098; the mean differential collapses 0.000866 -> 0.000031: the apparent separation sat in ticks where neither side moved), trial B unchanged (per game), S80 2,267 -> 1,106; 0 verdicts re-labelled. Second half: attach_informative_summary() wires the n / n_informative / n_eff triple + ci95_informative into every in-game verdict writer that computes a DM CI from a paired-loss series (4 tick-grain: s58_clamp_family, s58_e2_slice, s80, s84; 2 event-grain get n_events + note); proven live on the uncharged S80 screen reproducing the first half to 6 dp from a different code path; additive keys only, no charged trial re-run, ledger 18 unchanged; tests 11 + 6 writer files. STILL OPEN (minor): charged artifacts gain the field on their next run only; no schema gate refuses an artifact missing the triple; S86's informative flag is market-movement-only vs flag_ticks needing both sides; CORRECTION 4e03c4448: ingame_calibration_report.py (S43) DOES exist at eval_gate/ and is now the 7th wired writer (n 47,104 -> n_informative 15,981 / 14,617; n_eff_icc 631 / 452 beside the published ess 483 / 435); S43 reproduced at 2.0e-15 -- ACCEPT WITH CORRECTIONS |
| S88 | signals-ingame | PER-PHASE RECALIBRATION EXISTS AND IS UNCITED (gapfinder L6): two OOF per-phase results are on disk and neither is in the evidence tree; the MLB one shows late|leading_big 0.3745 -> 0.2759 but pools to NO_CHANGE and its winner spec was chosen in-sample; the NBA one is an orphan from the retracted endQ3 lineage (no CI, no market baseline -- unquotable). Bar: per-phase recalibration on the S06 partition with the spec chosen INSIDE the folds, tick-weighted Brier per phase vs the incumbent with clustered CIs on informative ticks (after S87); no charge | INGAME_GAP_PREMISES L6 | DONE 2026-09-04 11d47f77f -- ACCEPT WITH CORRECTIONS, SINGLE-WINDOW (Sonnet lane; verified Opus, contract A/B/Q, artifacts reproduced byte-identical; memo S88_phase_recal_2026-09-04.md). Premise reproduced (winner chosen in-sample at bucket_recalibration.py:213). Method: expanding game-first-date walk-forward on the S06 leak-free e4 incumbent (47,104 ticks / 158 games), phase spec chosen on an inner holdout carved from outer-train dates only; game-clustered bootstrap CI on informative ticks (S87). Eval 33,920 ticks / 11,087 informative / 127 games: late|leading_big +0.0316 [0.0088, 0.0572] IMPROVED; mid|trailing -0.0120 [-0.0232, -0.0010] WORSE; pooled -0.0029 NO_CHANGE vs incumbent and vs market. Calibration only, single window, no charge, nothing wired; the L6 two-clause bar is NOT cleared as a whole (second clause fails on mid|trailing). Verifier NEW GAPs -> S158 (n_eff 179.8 > 127 clusters) and a BH-family note for any future wiring |
| S89 | harness | NO IN-GAME ARM FAMILY IN THE FROZEN FWER PARTITION (gapfinder L11): 11 of 37 families are (live_tick, inplay) feature grids; ingame_mlb_arms (K=15), ingame_mlb_clamp (K=16) and ingame_nba_halftime_asof (K=17) were charged as families of one OUTSIDE the frozen partition, so no within-family BH bar applies to in-game arms. Bar: an in-game arm family (or families by sport) frozen in family_bars.py BEFORE the next in-game charge, with the three past charges mapped into it retroactively for K accounting (ledger rows unchanged) | INGAME_GAP_PREMISES L11 | LANDED 2026-09-03 bd58bad13 + write path under the shared token (memo S89_ingame_arm_family_2026-09-03.md) -- premise CONFIRMED (37 families, none of the three arm strings among them; soccer_gate row 18 is, the control). ingame_arms_mlb (10 arms: e2_gd, e4_gd, 8 clamp configs) + ingame_arms_nba (1) frozen with kind: arm (hypotheses = features); pin s14-families-v1 @ 62702554f -> s89-families-v2 @ 9d6cb98c4, 37 -> 39 families, no bar moved (q 0.05, alpha 0.05, BAR 0.004). FAMILY_ALIASES + resolve_family map the three historical strings; read-only k_family(ingame_arms_mlb) = 2 over the unmodified 18-row ledger; ledger.next_k_family now COUNTS alias-resolved rows (next ingame_arms_mlb = 3, soccer_gate = 2) -- PROPOSED-S89 applied by the orchestrator under the token, released. A5 fixed a seed_queue fall-through (arms would have leaked 99 feature hypotheses; still 3,564). Tests 12 + 6 (S13 test corrected: optional fields equal disk, not None, since rows 14+ carry them). No soccer arm family (none exists); ingame_arms_nba is a family of one until a second NBA arm (S94) is frozen into it BEFORE its charge |
| S90 | signals-ingame | ORDER-BOOK MICROSTRUCTURE IS CAPTURED AND UNSCREENED (gapfinder L15): two depth stores (277,888 rows with spread/thinness/stale + 2,070,472 trades with taker_side) and depth_history with raw per-level ladders (107,356 rows / 765 event tickers) overlap the scored MLB ticks on 2026-07-09..07-12. Bar: depth imbalance, spread, last-trade direction as tick-time as-of features; next-tick sign accuracy with clustered CI and outcome Brier vs e4 on the SCREEN side of the overlap; no charge. Also L14: event_key is market-type-specific so 0 events carry two markets, but a series-prefix strip yields 96 soccer_intl games x 3 markets and ~99 MLB games with moneyline + total (re-key = the market-consistent sim's premise) | INGAME_GAP_PREMISES L14 L15 | CLOSED 2026-09-04 70a8c23be -- PREMISE FALSIFIED, DUPLICATE OF S100 (Sonnet lane; verified Opus, contract A/B/Q; memo S90_microstructure_screen_2026-09-04.md). Fresh re-measure through S100's read-only loaders: 78,986 scored MLB ticks / 227 games; SCREEN-side games carrying any as-of microstructure feature at a 300 s cap = 18 < the n>=30 rail (S100 stopped at the same 18 < 20 on 2026-09-03 c8acbd78c; series CSV byte-identical md5 db4e3b4e); no arm, no Brier, no charge. L14 side measurement is new: MLB moneyline+total re-key 99 of 3,792 events (2.61 pct), soccer_intl all-three-markets 96 of 96 -> row S157. Verifier NEW GAPs: the lane's default OUT_DIR wrote 3 s90_* files under data/cache/eval_gate (harmless, gitignored; memo repro command should pass --out-dir); the register had no duplicate check before S90 was allocated |

| S91 | data | MLB OUTCOME SOURCE TRUNCATED: data/domains/mlb/espn_boxscores.parquet holds 2 rows, so MlbOutcomeResolver resolves 1 of 235 tickers and the scored MLB tick store cannot be rebuilt from raw (S83 had to backfill positionally). Premise first: was the file always 2 rows or truncated (git history of its builder, backups under data/backups, the S32 corpus ship)? Bar: the resolver resolves >= 95 pct of the 235 tickers from an on-disk outcome source that is NOT a new ESPN fetch (games.parquet outcomes, the settled Kalshi results, or the paper ledger settlements), or CLOSED AT LIMIT naming the acquisition -- any new ESPN fetch is Neel's S62 decision | S83 memo | CLOSED 2026-09-03 be4cc05cf (memo S91_mlb_outcome_source_2026-09-03.md). Premise CONFIRMED as TRUNCATION: the 2 rows are the 2026-07-14..07-16 All-Star-break batch; the 227-file joined store, gate_run_mlb_espn_box.py's '~3 weeks of box rows' (d7dd811a6) and both resolver docstrings prove it was fuller; mechanism = domains/mlb/ingest_espn_box.py:242-250 overwrite-on-failed-read (by elimination, no surviving log; S95). No fuller copy on disk; the settled ledgers cover < 1 pct. FIX: additive ingame/mlb_games_outcome_fallback.load_games_box_frame() over games{,_current}.parquet (39,162 finals) into a separate _fb map, ESPN first, EXACT-DATE only (the +1 UTC hop reached 229/235 but mislabelled a postponement doubleheader). Resolved 1/235 (0.43 pct) -> 225/235 (95.74 pct), bar 95 MET. Cross-source check: 220/220 live joined files byte-identical on all 12 non-player columns (77,062 rows). DRY re-join join_rate 0.0043 -> 0.9404; live store NOT overwritten (S83 positional backfill stays). Tests 5 + 18 + 7. Uncharged, ledger 18. RESIDUAL 10 tickers: 2 doubleheader pairs (no start_time), 2 off-date, 3 after games_current's last date 2026-07-12 -- Neel's S62 call or an extended games_current |

| S92 | signals-ingame | NBA LINEUP CORPUS IS RATING-LIMITED: lineup-at-tick now exists for 1,331 bridged priced games (S84) but only 577 carry an as-of player rating because asof_player_adv.parquet covers 2024-25 and no 2025-26 (754 bridged games have a lineup and no rating); the static five-man sum was NULL. Bar: (a) an as-of player table through 2025-26 built from data already on disk (boxscores / pbp under data/cache/team_system, no new fetch), (b) a NON-STATIC lineup term (unit on/off net rating as-of, or fatigue: minutes played so far in the game from the substitution stream) screened on the same 68,632-tick corpus vs the ladder BASE incumbent; no charge | S84 memo | CLOSED AS SCREEN_NULL 2026-09-03 464f5e150 (memo S92_nba_lineup_dynamic_2026-09-03.md). PREMISE CONFIRMED (S84 coverage reproduced exactly; the substitution stream carries the clock, so minutes-so-far and same-game on/off exist for 1,331 of 1,331 bridged games). Fatigue needs no rating, so coverage went 577 games / 68,632 ticks -> 1,331 games / 160,291 priced live-clock ticks at a full 5v5. Three non-static terms on the ladder BASE incumbent (fatigue minutes, fatigue x season share, unit on/off net rating shrunk n/(n+200)), screen side only (S84's partition sha reproduced byte-for-byte), game-first-date walk-forward, purged, 1-day embargo, S94 recal null on identical rows. ALL corpus (661 games / 79,554 ticks): market 0.142877, null 0.144293, incumbent 0.146850; fatigue_min -0.000212 [-0.000814, +0.000390], fatigue_share -0.000098, unit_onoff -0.000397; RATED corpus (284 games) +0.000475 / +0.000175 / -0.001061, all CIs crossing zero; the fatigue sign flips between corpora. 0 of 3 clear, 6 of 6 behind the null; no prereg. SECONDARY (S123): the recalibration null beats the NBA in-game incumbent on both corpora. Uncharged, ledger 18. SINGLE-WINDOW |

| S93 | signals-ingame | THE MLB IN-GAME BAR IS UNRESOLVABLE AT 41 GAMES: every in-game screen CI on the joined MLB store has a half-width near 0.005 (S82: 41 clusters; S80: 13; S72: 13 folds), wider than the +0.004 bar, so NULL is guaranteed regardless of the feature. The gapfinder found 12,772,159 MLB moneyline rows / 3,780 events with known outcomes on disk (the market half of a 24x corpus) with no model series. Bar: an e4-class model probability at every tick of those 3,780 events from an as-of state (score/inning/outs at tick time from the on-disk play-by-play or GUMBO archive, never a fetch) -- premise first: which state columns exist per tick; then the in-game screen tier (S82) re-run on the enlarged SCREEN side reporting n_games and the CI half-width achieved (target <= 0.002) | S82, S86, INGAME_GAP_PREMISES L4 | CLOSED AT LIMIT 2026-09-03 a5e2e7f45 (memo S93_mlb_every_tick_2026-09-03.md). PREMISE FALSIFIED: the corpus is 12,772,159 settled moneyline rows / 3,780 events / 2023-03-30..2026-07-09 at 60 s cadence but carries a price and a timestamp and NO game state -- the missing half is STATE, not a model series. Every on-disk state source is a 2026-summer live capture (joined store 227 games; espn_wp 117 wallclock+score and the only on-disk ESPN-id -> ticker bridge; ingame_grade 405 with 392 unbridged; gumbo archive 123 game_pk 07-04..07-15); statcast has full state for every 2023-2026 pitch but no wall clock. MEASURED: 177 / 3,780 events reconstructable (4.68 pct); 3,426 (90.63 pct) unreachable from disk. The game-clustered interval needs ~289 clusters / 1,601 scored games for a 0.002 half-width; the full on-disk enlargement reaches 43 (S82's 41 -> 43). The row's 24x was a TICK ratio; in-play share median 8.6 pct (~1.3M in-play ticks). Stopped at the premise: no screen, no charge. Census module + 4 tests landed. ACQUISITION = Neel's S62 DECIDE row 3 (MLB Stats API / GUMBO): a keyless historical backfill of /api/v1.1/game/{game_pk}/feed/live for ~3,780 games (per-play wall clocks already parsed by mlb_event_reactive.py:108,116), or forward capture ~90 days plus the retention change. Until decided, MLB in-game screens cannot resolve +0.004 |

| S94 | signals-ingame | THE NBA IN-PLAY MARKET IS MISCALIBRATED EARLY (S86): in P1-P2 close games (|margin| <= 5, > 12 min left; 27,591 screen ticks / 20,631 informative) the market's ECE is 0.056-0.065, one-directional (0.4-0.5 bin realises ~0.55, 0.8-0.9 bin ~0.73), i.e. the line overreacts to early margins relative to the pregame prior. Candidate arm = phase-conditioned shrinkage of the market toward the as-of prior: p = sigmoid((1-w(phase)) logit(market) + w(phase) logit(prior)) with w fit walk-forward per phase cell on the SCREEN side, plus a logistic recalibration [1, logit(market)] as the null arm on identical rows. Bar: improvement vs the RAW market and vs the recalibration null on the P1-P2 close cell >= +0.004 with a game-clustered CI excluding zero and n_eff reported on informative ticks; no charge; prereg DRAFT only if it clears; the verdict side (796 games) stays untouched | S86 memo, L6 L16 | LANDED 2026-09-03 d47b22162 (memo S94_nba_early_shrinkage_2026-09-03.md) -- SCREEN NEGATIVE, SINGLE-WINDOW, BAR NOT MET, NO PREREG. Premise CONFIRMED (27,591 screen ticks / 797 games / 20,631 informative; market ECE P1|close 0.055593, P2|close 0.064157; 0.4-0.5 bin realises 0.55-0.56, 0.8-0.9 realises 0.73). A2 discrepancy explained (CSV float rendering moves 34/44 ticks across the 0.3 bin edge; Brier unaffected). Candidate = phase-cell shrinkage of the market toward the state-priced prior, w_c fit on TRAIN only, expanding walk-forward, purged, 1-day embargo, 5 folds, 192,635 ticks / 673 games. TARGET cell n 23,561 / n_eff 876 (ICC 0.76): market 0.220173, global recal null 0.221658, per-cell recal 0.226244, candidate 0.222980; vs market -0.002807 CI95 [-0.006055, +0.000440]. Overall -0.000243 [-0.000999, +0.000513]. w unstable across folds (P2 close 0.885 / 0.000 / 0.185 / 0.185 / 0.450). KEY: the global recalibration null is ALSO behind the raw market, so the in-sample reliability gap does not survive being fit on a past window and applied forward -- the defect is a measurement, not a stable conditioning rule; per-cell recal halves ECE (0.0597 -> 0.0288) with the WORST Brier: score arms on Brier, ECE diagnostic only. No charge, ledger 18 |

| S95 | data | THE TRUNCATION PATH IS STILL LIVE: domains/mlb/ingest_espn_box.py:242-250 overwrites the whole espn_boxscores.parquet with the current batch on ANY failed read of the existing file (how S91's 2-row file happened), and every label_finals_refresh-shaped writer shares the pattern. Also: ticker_settlement_join._CLOSE_SOURCE_LABEL['mlb'] still says espn_boxscores_parquet though a re-join now takes most outcomes from games.parquet (provenance drift), and hist_mlb_outcome_resolver.MlbTickerOutcomeResolver (read by the scored hist_mlb_forward_gate) has the identical dependency and already accepts boxscore_df=. Bar: (a) write-to-temp + rename with a refuse-to-shrink guard (never overwrite a larger existing parquet with a smaller batch; log + raise), per-file test; (b) provenance label reflects the actual source per row; (c) the sibling resolver handed load_games_box_frame() with the forward gate's numbers re-measured before/after (its own row if they move). domains/mlb/ is NOT human-gated (domains/<sport> is a safe area) | S91 memo | CLOSED 2026-09-03 42b4f78e3 (memo S95_write_guard_provenance_2026-09-03.md). Premise CONFIRMED on all three parts. (a) new scripts/platformkit/ops/safe_parquet_write.py: temp + rename with a refuse-to-shrink guard (ShrinkRefused) and a failed footer read that PROPAGATES, wired into n = 19 read-existing-then-overwrite sites in 18 files across 8 sports (kbo, npb, nba x3, wnba x3, mlb x5, soccer x3, soccer_intl, tennis); 6 new tests + 95 pre-existing ingest tests green. (b) additive per-row outcome_source in {espn_boxscores_parquet, games_parquet_fallback} from the resolver's last_source; close_source unchanged; 9 + 19 tests. (c) MlbTickerOutcomeResolver gained games_fallback default OFF; forward gate DRY-measured OFF vs ON = 0 resolved / INSUFFICIENT_DATA both ways (capture corpus 07-27..07-28 vs games_current ending 07-12), zero delta, FLAG LEFT OFF. Uncharged, ledger 18. RESIDUAL: the live espn_boxscores.parquet is still 2 rows -- the guard prevents the next truncation, it does not restore the lost rows (S62) |

| S96 | signals-ingame | OVERREACTION AT TICK RESOLUTION, NBA (gap map L16, now measurable): the 465k-tick corpus carries score per tick, so scoring events are visible between consecutive ticks. Premise first: per event (margin change >= 3 within one tick gap), the market's move at the next tick vs its move over the following k ticks (k = 3, 5, 10) and vs the eventual outcome -- does the post-event change overshoot (mean reversion of the line) or undershoot (drift), by phase, walk-forward with a train-window rule; an arm = shrink the post-event line toward the pre-event line by a fitted phase-dependent factor for k ticks. Bar: on the SCREEN side, arm vs raw market on post-event ticks >= +0.004 with a game-clustered CI excluding zero AND beating a recalibration null on identical rows (S94's lesson); else honest NULL with the measured overshoot/undershoot table; no charge | S86, S94, INGAME_GAP_MAP L16 | CLOSED 2026-09-03 e8cca60c1 (memo S96_nba_overreaction_2026-09-03.md) -- PREMISE DIRECTION FALSIFIED + SCREEN NEGATIVE. No overshoot anywhere: the subsequent-move-on-event-move slope is POSITIVE in every phase, horizon and threshold (31/33 cells with a CI excluding zero, all positive; +0.2314 / +0.2966 / +0.4768 at k = 3 / 5 / 10 for |dmargin| >= 3, 14,354 events / 797 games; >= 5 larger still) -- the line UNDER-reacts and drifts. A PLACEBO on non-event ticks is also positive (+0.0661 / +0.1420 / +0.2865), so a third to two thirds of the drift is a generic property of the quoted mid (slow updating), not event-specific. Arm with lambda_c fit per phase on a SYMMETRIC grid (train folds picked drift-following): all six arms BEHIND the raw market with CIs crossing zero -- primary -0.000138 [-0.000301, +0.000025], n 39,168 / 665 games; bar missed ~30x; lambda pins at the grid boundary on the >= 5 arms and still loses (grid NOT widened). Second independent confirmation that the recalibration null is behind the raw line. No prereg, no charge, ledger 18. SINGLE-WINDOW |
| S97 | signals-ingame | SENSOR FUSION, NBA (gap map L13, new territory): treat the market tick series and the state-priced prior series as two noisy sensors of the true win probability; a Kalman / Bayesian filter on the logit with per-phase observation-noise variances fit on TRAIN folds only (market noise from its own tick-to-tick innovation variance, prior noise from its residual vs outcome), producing a posterior at every tick. Bar: SCREEN side, posterior vs raw market >= +0.004 with a clustered CI excluding zero AND beating the S94 global-recalibration null on identical rows; posterior 90 pct interval coverage within 2 points of nominal per phase (the coverage claim is the novel deliverable even if Brier is NULL); no charge | S86, S94, INGAME_GAP_MAP L13 | CLOSED 2026-09-03 3236654fb (memo S97_nba_sensor_fusion_2026-09-03.md) -- NULL (Brier) + NEGATIVE (coverage). Premise held, stop rule not triggered (innovation correlation max 0.6579, 0/27 cells above 0.98; 16.79 pct of ticks differ by > 0.1; the prior's innovation variance is 3-5x the market's). Two-sensor scalar Kalman on the logit, q/r by the innovation-variance method on TRAIN only, walk-forward purged by game with 1-day embargo, 192,635 ticks / 673 games on the S86 SCREEN side: posterior 0.078608 vs market 0.078611, +0.000003, DM CI95 [-0.0000091, +0.0000148] -- bar NOT MET by three orders of magnitude; both nulls also null. Coverage deliverable is a measured NEGATIVE: nominal 90 pct intervals cover 0.0800 overall (P1 0.19, P2 0.12, P3 0.22, P4 0.08, OT 0.00); late-game deficit is a location error (no sd inflation up to 200x reaches nominal). MECHANISM: fitted r_p/r_m 60-400,000; the in-play line shows no lag-1 mean reversion -- it behaves as a MARTINGALE at tick resolution, so a local-level filter has nothing to smooth. No charge, no prereg. SINGLE-WINDOW. Untested: a bias term on the prior sensor, state-dependent q; VAR_FLOOR 1e-4 is load-bearing in 23/127 cell-folds |

| S98 | signals-ingame | THE MODEL SENSOR IS THE CRUDE HALF: every NBA tick arm (S86 S94 S96 S97) priced the as-of prior as Elo p0 through price_checkpoint with a fixed margin sigma 13.5, and that prior trails the line pooled (-0.004857) while matching in 16/27 cells. Untested: a BETTER as-of pregame prior at every tick -- the calibrated pregame model's p (the NBA gate corpus p_model / the S05 calibrated pregame probability, as-of the game date, strictly before) and a state-dependent sigma (fit on TRAIN folds by phase) -- re-run the S86 bucket table and the S94 single-w blend with it. Bar: SCREEN side, the better-prior arm vs the raw market on the 27 cells with clustered CIs, n_eff; if any cell clears +0.004 with a CI excluding zero AND beats the recal null, a prereg DRAFT; else the honest table (expected: the prior improves but stays behind the line pooled) | S86 S94 S96 S97 | CLOSED 2026-09-03 f517fbb9b (memo S98_nba_better_prior_2026-09-03.md) -- THE PRIOR IS NOT THE CRUDE HALF, THE FIXED SIGMA IS. Premise HALF-FALSIFIED: the NBA gate corpus has no p_model column; p_base == p_elo == walk_forward_elo p_home_elo byte-identically (1,814 rows), the same Elo family as the S86 p0, and BEHIND p0 at the first tick (0.223386 vs 0.222833, 668 games); the S05 calibrated pregame p is not archived per event and a TRAIN-only walk-forward recalibration of p0 is worse (0.229176 / 0.228559 vs 0.228058). No prior on disk beats Elo p0 at the first tick. SIGMA IS THE LEVER: margin_sigma fit per phase cell on TRAIN folds moves the pooled arm from -0.004805 (CI [-0.007737, -0.001873], excluding zero) to -0.002378 (CI [-0.004904, +0.000148], INCLUDING zero -- indistinguishable from the raw in-play line pooled) on 162,171 ticks / 571 games / n_eff 2,130. Bar NOT MET pooled or in any of 27 cells; one favourable CI excluding zero (P4|close|rem 2-6 min blend +0.003210 [+0.000084, +0.006336]) under the bar. Blend behind market and recal null (third confirmation with S94, S96). 21 of 127 cell-folds pin at the grid max 24.0 (15 at min 6.0), grid NOT widened -> S103. Coverage 571 of 1,593 priced games; games.parquet ends 2026-04-12 so NO PLAYOFFS in the bridged frame. No prereg, no charge, ledger 18. SINGLE-WINDOW |

| S99 | signals-ingame | ONE GAME, ALL ITS IN-PLAY MARKETS (gap map L14 + L20): event_key is market-type-specific so 0 events carry two markets, but a series-prefix strip yields 96 soccer_intl games x 3 markets and ~99 MLB games with moneyline + total (INGAME_GAP_PREMISES L14). Bar: (a) an additive re-keyed view (game_key) of data/cache/inplay_odds price series with n_games carrying >= 2 markets per sport; (b) a rest-of-game score DISTRIBUTION at each tick from the on-disk state (MLB: the joined store's score/inning; soccer: score + minute), scored with CRPS against the outcome AND its implied moneyline/total probabilities scored against each market's tick price on the SCREEN side; cross-market consistency check (does the market-implied total and moneyline agree with one distribution?). No charge; honest table | INGAME_GAP_PREMISES L14 L20 | CLOSED (BEHIND, screen) 2026-09-03 0a3e38986 (memo S99_cross_market_2026-09-03.md). Premise CONFIRMED: strip rule event_key.split('-', 1)[1] on Kalshi rows yields 99 mlb games (moneyline + spread + total) and 96 soccer_intl games (3 markets); 87 / 47 carry on-disk state; polymarket keys are a separate space with no second market. Additive re-keyed view at data/cache/eval_gate/s99_game_keys.parquet. Settlement semantics measured: total >= strike reproduces result_where_known at 1.000 on 917 settled pairs. Analytic Poisson / Skellam rest-of-game distribution from the as-of state: mlb SCREEN 52 games / 90,915 ticks -- moneyline 0.201034 vs market 0.160624 (-0.040410, CI [-0.069640, -0.011180]), total 0.160422 vs 0.139822 (-0.020601, CI [-0.035143, -0.006058]), CRPS 2.8213; soccer 8 games -- CIs span zero. Bar NOT MET, wrong sign with CIs excluding zero. Market self-consistency (fixed as-of split) mean abs 0.2614 mlb / 0.2699 soccer, converging with phase. DEFECTS FOUND: a backward as-of join carried a 2-hour-stale score forward (moneyline 0.3234 before a 300 s tolerance); 22 of 47 soccer state files carry state_summary = 'live' with no score or minute (S104); iso_week partition lopsided on a tournament corpus (8 SCREEN vs 17 VERDICT of 25). No prereg, no charge, ledger 18 |
| S100 | signals-ingame | ORDER-BOOK MICROSTRUCTURE SCREEN (gap map L15): depth stores (277,888 rows with spread / thinness / stale + 2,070,472 trades with taker_side; depth_history raw ladders 107,356 rows / 765 tickers) overlap the scored MLB ticks on 2026-07-09..07-12. Bar: tick-time as-of features (depth imbalance at the touch, spread, last-trade direction, trade-flow imbalance over 60 s) joined to the scored MLB ticks on the overlap; next-tick sign accuracy with a game-clustered CI (bar > 50 pct excluding zero), and outcome Brier of e4 + feature vs e4 and vs the recal null on the SCREEN side; n / n_informative / n_eff; no charge | INGAME_GAP_PREMISES L15 | CLOSED 2026-09-03 c8acbd78c -- PREMISE FALSIFIED AT TICK GRAIN (memo S100_microstructure_2026-09-03.md). The row's headline counts were all-sport totals; the MLB KXMLBGAME subsets share 68 / 9 / 89 event tickers with the 227 scored games, but the captures are PRE-GAME snapshots: an as-of feature (300 s freshness cap) covers 1,346 ticks / 35 games (1.70 pct) for depth imbalance and 293 / 3 for trade flow; only 18 SCREEN-side games < the 20-game stop rule, so no arm was run (no e4 comparison, no null, no charge). Descriptive next-tick sign is a NULL: imbalance 0.5508 on n = 305 / 25 games, clustered CI [+0.00003, +0.10161] -- one cell of eight, chance-level for eight uncorrected tests, gone at a 60 s cap; all others span zero on 3 games. Also falsifies L15's 'reduced form on the ticks': spread_bp / book_thinness / stale_quote are null on all 25,585 rows. Reopen only when depth capture runs DURING scored games (a capture-cadence change, not a modelling row) |
| S101 | signals-ingame | ADAPTIVE CONFORMAL COVERAGE ON TICKS (gap map L17): aci_online.py exists with one nba-hardcoded caller and was NEVER scored on ticks; S97 showed a Gaussian filter's 90 pct intervals cover 0.08. Bar: ACI (adaptive conformal, online alpha update per game) run over the S86 NBA per-tick series on the SCREEN side, grouped-coverage per phase (S97's grouped definition) with the target 90 +/- 2, interval width reported, walk-forward (calibration set from TRAIN folds only, online adaptation within the game on past ticks only, guard tested); no charge | INGAME_GAP_PREMISES L17, S97 | CLOSED 2026-09-03 7c7991b4e (memo S101_aci_coverage_2026-09-03.md) -- PREMISE CONFIRMED (one nba-hardcoded caller, data/cache/ingame_aci absent, never scored on ticks). Grouped split-conformal band calibrated on TRAIN folds only moves grouped coverage from S97's 0.08 to 0.936-0.980 per phase on S97's identical rows (192,635 ticks / 673 games, 5-fold walk-forward, purge + 1-day embargo asserted) at ~10x the Gaussian width, including P3/P4/OT where no Gaussian inflation reached 0.90 at any k up to 200 (a per-cell conformal half-width absorbs the location bias). BAR 90 +/- 2 NOT REACHED in any phase at nominal 0.90 or 0.80 (over-covers by 2-8 points; closest 0.8800 at P2, a miss, tolerance not widened); the as-of prior in OT is the one hard failure at 0.6471. The ONLINE ACI arm saturates at coverage 1.000 / 5x width and is LABEL-CONSUMING (y is constant within a game, so every within-game update reads the game's outcome) -- reported as a ceiling, never as the leak-free number; STATIC is the deliverable. gate_aci_on_stream's planted null is NOT INFORMATIVE (degenerate per-tick criteria). No charge, no seal, SINGLE-WINDOW. Next: finer conformal grid on the calibration side only; an asymmetric band |

| S102 | signals-ingame | NBA IN-GAME HYPOTHESIS SWEEP ON THE POD: the 465,249-tick corpus has only score / period / clock / margin / market per tick, so the in-game screen tier (S82) has never run on it, and every NBA arm so far was one hand-picked form. Bar: a frozen NBA in-game grammar of DERIVED-state hypotheses (margin trajectory over k ticks, run length, lead changes, scoring pace vs the pregame total, time-decayed margin, phase interactions; transforms x horizons; expect 200-600 hypotheses) registered as a frozen family (family_bars, kind arm or grid, BEFORE any screen), screened on the pod through the S82 tier against the S94 recalibration null on the SCREEN side with within-family BH FDR, charges off, results in the pod DB and mirrored to data/cache/eval_gate/s102_*; report the FDR-surviving count (expect 0), the best 10 with CIs, and pod throughput (screens/hour). Pod deploy = md5-parity of the tier + corpus ship; runner separate from pid 254284 | S82 S86 S94, pod | CLOSED 2026-09-03 875e43541 -- SCREEN_NULL (memo S102_nba_pod_sweep_2026-09-03.md). 576-hypothesis NBA in-game grammar (16 base derived-state columns x 6 transforms x 6 conditionings; 576 distinct semantic hashes; 3 frozen transforms NOT_ENUMERATED at tick grain, pregame total NOT_SUPPLIED so pace is vs the game's own P1 pace) frozen as family ingame_nba_tickgrid BEFORE the screen; pin moved once 9d6cb98c4 -> 6e7878ae5 (39 -> 40 families, 4,151 hypotheses; no bar moved). Swept ON THE POD at md5 parity 11/11 + corpus, per-file tests re-run on the pod (pandas 3.0.5): 564 SCREENED / 12 UNSCORED (named), n = 192,635 ticks / 673 games / 68,925 informative, 3,266.7 screens/hour. 0 of 564 clear +0.004; best +0.000248 vs the recal null (16x below, CI [-0.000664, +0.001160]); within-family BH q = 0.05 gives 29 'discoveries' all 29x-4,000x below the bar. Top 10 reproduced locally to 6.8e-14 on a different pandas major. The walk-forward global recalibration of the line is again WORSE than the raw line (0.078969 vs 0.078611). Ledger absent on the pod, 18 rows locally; keepers alive before and after. SINGLE-WINDOW |

| S103 | signals-ingame | SIGMA AT THE GRID LIMIT (S98): the per-cell margin sigma pins at 24.0 in 21 of 127 cell-folds and at 6.0 in 15, so the -0.002378 pooled result is a bound; the fit also drifts across folds (P1|close 11.0 -> 19.5). Bar: (a) the grid widened to [3, 60] and (b) a PARAMETRIC sigma(time_remaining, |margin|, period) with 3-4 coefficients fit on TRAIN folds (fewer degrees of freedom than 27 cells, so less fold drift), both repriced through price_checkpoint on the S86 SCREEN side; pooled and 27-cell tables vs the raw market with clustered CIs and n_eff; the S94 single-w blend with the parametric sigma vs the recal null; prereg DRAFT only if a cell or the pool clears +0.004 with a CI excluding zero and beats the null. Also extend the bridge past 2026-04-12 (playoffs) if games_current or the pbp store carries the finals -- report coverage before/after | S98 | CLOSED 2026-09-03 be4abcb17 (memo S103_nba_sigma_2026-09-03.md) -- THE BOUND WAS REAL AND WORTH +0.000261. Premise CONFIRMED off S98's archive (21/127 cell-folds at 24.0, 15 at 6.0, 24 at the 13.5 default); S98 reproduces to every digit. WIDE GRID [3, 60]: the high end was binding (2-6 cells per fold fit above 24.0; P4|close|rem 2-6 min up to 47.5) and nothing pins at 60, but the low end RE-PINS at 3.0 in 2-4 cells per fold (effectively-decided states) -- CLOSED AT LIMIT, not widened again. OOS: pooled -0.002378 -> -0.002117, CI [-0.004670, +0.000436] still including zero. PARAMETRIC sigma (4 coefficients, none at a bound) is WORSE: -0.003749 with a CI excluding zero; it caps the worst fold drift but not the median, while the wide grid doubles mean drift for +0.000261. Blend -0.000130, behind the market and the recal null (fourth confirmation). Coverage extended without a bridge (the incumbent prior needs none): 673 games / 192,635 ticks / span to 2026-06-10 / 39 post-04-12 games, same ordering. Bar NOT MET pooled or in any of 27 cells in either frame. No prereg, no charge, ledger 18. SINGLE-WINDOW |

| S104 | data | SOCCER STATE CAPTURE IS A BARE STRING: 22 of the 47 soccer_intl joined state files carry state_summary = 'live' with no score and no minute (every capture before 2026-06-25), which is why S99's soccer screen fell 47 -> 25 -> 8 games; and any backward as-of join of state onto price ticks must carry a staleness tolerance (S99 found a 2-hour-stale score carried forward). Bar: (a) the soccer capture path (find it under scripts/platformkit/ingame/, fotmob / espn soccer poller) writes score + minute on every tick from 2026-06-25 on -- verify on the live files and add a schema test; (b) a shared as-of join helper with a max_staleness_s argument used by S99 and the S82 loader; no charge | S99 memo | CLOSED 2026-09-03 470ae3d13 (memo S104_soccer_state_asof_2026-09-03.md). PREMISE PARTLY FALSIFIED: 51 files not 47; 22 carry the bare 'live' on every tick + 2 mixed; cutover mid-day 2026-06-27, every file from 06-28 is 100 pct structured. (a) FALSIFIED: live_grade._state_summary emits 'live' ONLY when none of its 16 state keys is present, so the writer already satisfied the bar; the soccer score alias and minute/half landed before this lane; schema test added. Score on every structured tick but minute is not always (109/156 on one file), passed on as an honest absence. The 22 legacy files are CLOSED AT LIMIT, not rewritten: score unrecoverable from any store on disk (raw capture has the same split; fotmob_backfill has no score field). (b) LANDED eval_gate/asof_join.asof_join_state(ticks, states, key, max_staleness_s) -> (merged, stale_share) with the null set asserted byte-identical to merge_asof(tolerance=300); wired into both s99_corpus call sites at the unchanged 300 s rail; S99 headline reproduced exactly. NEW MEASUREMENT: 75.26 pct of 607,224 mlb and 92.80 pct of 468,712 soccer joined ticks carry no usable state -- what tolerance= was nulling in silence. Third target falsified: foundry/ingame_screen.py's loader does no as-of join; venue_history/nba_wallclock_join.py:122 does a backward join with NO tolerance (named for a future lane). Tests 5 + 9. No charge, ledger 18 |

| S105 | ops | DEPTH CAPTURE NEVER RUNS DURING SCORED GAMES (S100): the Kalshi book-depth / trade stores hold pre-game snapshots for the games the tick store scores, so microstructure is unmeasurable at tick grain (1.70 pct of ticks have a fresh feature). Premise first: read the pod capture runner (mlb_book_capture.run_pod_capture, pids 21620/21622) and ingame_book_depth_poller -- what cadence and which windows it captures, and why in-game ticks are missed (schedule gating, retention trimming per L5, or a poll interval). Bar: depth + trades captured at <= 60 s cadence for the full in-play window of every scored MLB game for one live slate, verified by the share of scored ticks with a fresh (<= 60 s) depth row >= 80 pct; a restart of the capture keeper is an orchestrator step recorded in the memo | S100 memo, INGAME_GAP_PREMISES L15 | PARTLY CLOSED 2026-09-03 a325e870a (memo S105_depth_capture_cadence_2026-09-03.md) -- PREMISE PARTLY FALSIFIED, ROOT-CAUSED, 60 s BAR UNMET AND NOT LOWERED. Not gating, not trimming, not the interval: TICKER SELECTION. ingame_enrichment_runner._run_book_depth (the only production caller of poll_once) passed no state, so the sticky active list / miss counts / prev snapshots / trade watermarks were rebuilt empty every 30 s and the 2026-07-11 sticky-retention fix never ran in production: 90.09 pct of book_depth rows and 88.06 pct of depth_history rows are markets 1-3 days out; 227 of 231 MLB tickers stopped being captured BEFORE their own first pitch (median 61.1 h); trades duplicated 29.2x. LANDED additive: retention.live_first drops > 1-day-out markets before the cap, poller + depth_capture route discovery through it, runner threads a module-level state dict (tests 7 / 14 / 11 / 17; net 0 LOC on the two 300-cap files; no constant moved). BAR UNMET: depth_history runs at DEPTH_CAPTURE_EVERY_N_TICKS (15) x LIVE_INTERVAL_SEC (20 s) = 300 s > 60 s -- CLOSED AT LIMIT for the lane; that constant multiplies the paper node's Kalshi request rate 5x and is an orchestrator/Neel call. ALREADY BUILT: mlb_book_capture (pids 21620/21622) captures full ladders first-pitch-to-final at median 30.0 s / p90 64.8 s; it needs reach (6 game_pk on 09-02) and a CONSUMER -- S100 reads data/cache/ingame_books/mlb nowhere, so the remaining distance to microstructure is a JOIN, not a capture. m37_ingame_enrichment has NEVER run on the pod (absent from the 11 supervisor children); its restart requires killing the supervisor keeper 19236 to adopt the ProcSpec -- deferred, recorded in the memo |

| S106 | data | THE SCORED MLB TICK STORE MERGES MULTIPLE REAL GAMES UNDER ONE game_id (S105 finding): 144 of 227 files in data/cache/ingame_grade_joined/mlb span > 6 h; KXMLBGAME-26JUL061915NYMATL carries 07-05 / 07-06 / 07-07 ticks including inning 2 at 3-5 and inning 10 at 6-7. Every MLB in-game denominator (S82 41 clusters, S87 78,986 ticks / 227 games, S80, S58 trials A/B) is quoted against this key. Premise first: is it the Kalshi ticker (one ticker re-used across a series / doubleheader) or the join key? Bar: a (game_id, real_game) split by first-pitch gaps + inning resets, the corrected n_games and n_ticks per real game, and the S82 / S87 headline numbers RE-QUOTED on the corrected clusters (verdicts unchanged or honestly re-labelled; no charge); the join writes a real_game_seq column additively | S105 memo | CLOSED 2026-09-03 34c90bc99 (memo S106_real_game_split_2026-09-03.md) -- PREMISE CONFIRMED: 144/227 and the NYMATL 07-05 / 07-06 / 07-07 example reproduced; innings NOT monotone. CAUSE = CAPTURE-SIDE TICKER KEYING, not the join (ticker_settlement_join is 1:1; inplay_capture_loop._scan_live_by_legs bridges to the live game by TEAM PAIR with no date check, so the next day's game is written under the previous ticker; Kalshi does not re-use tickers, the capture does). Split landed: eval_gate/real_game_split.py (inning decrease, ts gap > 5 h, score reset; 10 tests): 227 game_ids -> 392 real games, 122 multi, 22,768 / 78,986 ticks (28.8 pct) reassigned; NYMATL splits into 3 and only 186 of 683 ticks belong to the game it names. Re-quoted on corrected clusters, no model recomputed, all published CIs reproduced < 1e-9 first: S82 top-3 games 41 -> 88 (n_eff 214.8 -> 120.7 / 432.0 -> 406.7 / 1082.0 -> 1399.8), S87 trial A 158 -> 315 (n_eff 566.2 -> 569.7); EVERY VERDICT UNCHANGED, every CI still spans zero; more clusters did not buy precision (ICC rises as clusters get shorter). Published MLB in-game 'n games' was understated ~2x. Join column NOT added (cause upstream; file at the cap). Follow-on S107 |

| S107 | ops | THE TICKER BRIDGE HAS NO DATE CHECK: inplay_capture_loop._scan_live_by_legs (scripts/platformkit/ingame/inplay_capture_loop.py:794-798) bridges a Kalshi ticker to the live game by team pair only, so a series' next game is captured under the previous day's ticker (S106: 122 of 227 scored MLB tickers hold > 1 real game; 28.8 pct of ticks mislabelled). Bar: the bridge requires the live game's date (or first pitch within +/- 12 h of the ticker's encoded date) to match; a per-file test with a two-day series; the pod capture keeper 21620/21622 restarted on the fixed code after md5 parity (orchestrator step, recorded), verified by one live slate with 0 tickers spanning > 1 real game. The 22,768 mislabelled ticks on disk stay as they are; real_game_split is the read-side correction | S106 memo | LANDED LOCAL 2026-09-03 d1d828553 (memo S107_ticker_bridge_date_2026-09-03.md) -- PREMISE PARTIALLY FALSIFIED: a date guard DOES exist on the path (441bf5507, 2026-07-10) but it compares the TICKER to TODAY, never to the matched game (which carried no date until this diff), is day-granular, and deliberately allows an ET-yesterday ticker -- the exact direction that files a series' next game under the previous day's ticker. Reproduced: 122/227 tickers > 1 real game; 32,510 ticks > 12 h before their ticker's first pitch (last 2026-07-12, 1.2 days after that guard landed, so it was not in force on the writer). FIX: new pure ingame/ticker_date.py, additive start_time on the live state from the ESPN event, the scan keeps the NEAREST team-matching live game within 12 h and skips the rest as bridge_date_mismatch (heartbeat + funnel mapped); 5/5 bridge cases + counter test; 1 pre-existing failure identical on master. RESTART TARGET CORRECTED: the code runs in inplay_capture_runner (pod pid 19598 under supervisor 19236), NOT in mlb_book_capture 21620/21622. Pod deploy + restart = orchestrator step below; DEPLOYED 2026-09-03 ~17:30 CDT by the orchestrator: 5 files at md5 parity 5/5, inplay_capture_runner 19598 killed, supervisor 19236 respawned it as pid 360964 on the fixed code; verification (0 tickers spanning > 1 real game over one live slate) reads at the 18:40 / next-morning checks -- SINGLE WINDOW |

| S108 | signals-pregame | NO REAL PREGAME MODEL HAS EVER BEEN SCREENED AGAINST THE CLOSE: the gate corpora's p_base is Elo (S98: p_base == p_elo byte-identically), every factory screen is one feature added to logit(close) or logit(Elo), and the family combination (S79) used top-5 by in-sample rank. Untested: a FULL-FEATURE regularized pregame model per sport -- L2 / elastic-net logistic and a shallow gradient-boosted model over EVERY as-of column the gate corpus and the domain as-of tables supply, with logit(close) as an offset (so the model learns the RESIDUAL the close misses), nested walk-forward on the SCREEN partition only (inner folds pick the penalty, outer folds score), purge + embargo, standardisation inside train folds; run on the POD. Bar: per sport, Brier vs the devigged close (or vs Elo where no close exists, labelled) with a unit-clustered CI on the screen partition; >= +0.004 with the CI excluding zero -> prereg DRAFT for ONE charge; else the honest table (expected: matches the close). No charge | S79 S98 S58c | CLOSED -- NULL 2026-09-03 c95b4368a (memo S108_pregame_full_model_2026-09-03.md). Built and run ON THE POD (md5 parity 3/3, < 3 min for four sports; python 3.12 / pandas 3.0.5 / sklearn 1.8.0). Screen side only, screen shas byte-equal to S58c / S79. Features = every numeric as-of column the gate corpus + domain asof_*.parquet supply (nba 178 / mlb 22 / soccer 54 / tennis 178 incl. missing indicators); 85 refusals, all 'unavailable', never 'leaky'. Two arms with logit(incumbent) as a TRUE offset (own FISTA elastic net; HGB as a single Newton step because lightgbm is absent on the pod), nested walk-forward (inner picks the penalty), 2-day purge + embargo. 0 of 8 arms clear +0.004; EVERY close-relative arm NEGATIVE (soccer enet -0.000033, tennis enet -0.000058; the boosted arms' CIs exclude zero on the wrong side with PBO 0.62-0.65); best anywhere +0.001360 vs Elo on nba (619 events) and it is an intercept recalibration: the inner CV zeroed EVERY coefficient in 20 of 23 outer folds -- out of sample the best use of 178 columns was not to use them. combo_search.py (prior art) has never been executed. No prereg, no charge, ledger 18. SINGLE-WINDOW
| S109 | harness | THE POD RUNNER IDLED: the S16b/S75 seed was a 6,000-hypothesis subset (3,240 distinct) and the runner exhausted it (foundry_pass 2096, idle=True, 0 charges). Bar: the FULL catalogue grammar seeded into the pod queue (seed_queue --limit 200000; dedup by semantic hash), runner resumes without restart, screens/hour re-read; the pod never idles while hypotheses remain | S16b S75 | LANDED 2026-09-03 evening (orchestrator): full catalogue grid seeded on the pod -- hypotheses 3,240 -> 46,476 (mlb 3,942 / nba 18,657 / soccer 6,948 / tennis 16,929), 14,843 new T0 queue rows; the seed's write lock KILLED the live runner ('database is locked', S110); fixed and relaunched as pid 466285 (log /workspace/foundry_runner_s109.log), 48 screens per pass, 0 tracebacks, charges 0

| S110 | harness | RESULTS DB HAD NO BUSY TIMEOUT: results_db connected with the sqlite default (5 s), so a concurrent seed_queue write killed the live pod runner with 'database is locked' (S109). LANDED by the orchestrator e98a7e545: sqlite3.connect(..., timeout=30.0); 15 tests; deployed at md5 parity; runner relaunched. Residual: seed_queue should still be run with the runner paused or in small batches -- recorded in the runbook memo | S109 | LANDED (2026-09-03) |

| S111 | data | THE PREGAME FACTORY'S COVERAGE ACQUISITIONS ARE ON DISK (S85): (a) five tennis families sit at 58 pct coverage because the WTA half of the served window has no as-of tables -- build asof_features_wta / asof_return_wta / asof_meta_wta (the pattern asof_hold_wta / asof_setdetail_wta already follows) from wta_matches.parquet with the same builder and the same strictly-before rule; (b) nba_quarter_shape at 35.2 pct -- fold linescores_2024_25.parquet through espn_nba_game_bridge into asof_quarter_shape keyed by the NBA game_id; (c) GUARD: a supplied column with 0 non-null on the served window must be refused as 'unavailable' (never screened silently) -- the nba_quarter_shape all-NaN column passed both guards; (d) re-run the local real screen over the newly covered families and report coverage before/after per family and the screen table (expect nulls). No fetch; no bar moved; no charge | S85 memo | LANDED (coverage acquired; screen NULL) 2026-09-03 41d2a4c62 (memo S111_coverage_acquisitions_2026-09-03.md). Premise HOLDS on all four parts (catalogue.absent() had NAMED the three WTA siblings). Coverage on the frozen 800-row served window: tennis_features 466 (58.2 pct) -> 797 (99.6), tennis_return 466 -> 797, tennis_meta 469 -> 800, nba_quarter_shape 282 (35.2) -> 800; 478 T1 screens that had never existed (678 over 9 families). EVERY ONE NULL: best vs the devigged close tennis_features -0.001911 [-0.005899, +0.002077], tennis_return -0.001565, tennis_meta +0.000188; nba_quarter_shape +0.001527 vs Elo (labelled; S112 says Elo-relative numbers are not market-relative). 2 of 678 CI-lowers > 0 vs ~16.9 by chance. NO PREREG. BUILD: domains/tennis/asof_wta_siblings.py (the already-parametrised ATP builders on the WTA spine) -> three _wta tables of 11,270 rows; domains/basketball_nba/asof_quarter_shape_full.py -> asof_quarter_shape 1,313 -> 2,634 rows; both via safe_parquet_write; registered in asof_supply as event-rule entries over an ATP+WTA source pair (NOT in the pinned FWER_FAMILIES_SPEC); columns split to asof_supply_columns.py. GUARD: asof_supply._refuse_all_nan ('unavailable: all-NaN on the served window'), reproduced on the S85 defect. The 3 S85-screened NBA families reproduce to the 6th decimal. 2 tennis families still CLOSED AT LIMIT (schedule_density / travel_scouting need a different builder). No bar moved; 0 charges, ledger 18 |

| S112 | data | NBA AND MLB PREGAME SCREENS HAVE NO REAL CLOSE: the nba and mlb gate corpora carry p_base = Elo as the incumbent (S98), so every pregame result for those sports (S58c, S79, S85, S108) is 'vs Elo', which says nothing about the market. On disk: nba_checkpoints_full's first traded tick (~21 s after tip, 1,593 games, S81) is a de-facto close; nba_close_corpus.parquet has 663 closes; the Kalshi mlb_price_series' last two-sided TRADED quote before the ticker's first pitch (935 events, S81 rule) is a close. Bar: a close_join for nba and mlb that attaches p_close (devigged where two sides exist; labelled 'first in-play tick' for nba) to the gate corpora additively (new columns, sidecars rebuilt, portable=True still loads on the pod), coverage per sport reported; then S108's elastic net and the factory screens re-scored vs THAT close on the SCREEN side (expect: matches the close, the +0.005 vs-Elo NBA screens vanish) | S81 S98 S108 | LANDED a58c90f4c 2026-09-03 (memo S112_nba_mlb_close_2026-09-03.md) -- NULL, and the finding is the REFERENCE: the close beats the corpora's Elo incumbent by +0.025606 Brier on nba (0.211728 -> 0.186122, n 351, team CI [+0.015252, +0.035960]) and +0.007269 on mlb (n 276, CI [+0.000066, +0.014473]), so every earlier 'vs p_base' pregame number on those sports was Elo-relative against a reference far behind the market. Coverage: nba 952 / 1,814 = 52.48 pct (220 genuine pregame closes + 732 first-in-play ticks at a median 21 s after tip; the pregame and tick subsets agree +0.025567 vs +0.025629); mlb 894 / 39,162 (8.00 pct of era_2022_2026, one venue, 74 days = SINGLE-WINDOW). Premise corrected: only 220 of the 663 nba pregame closes overlap the corpus and 154 of 663 are 0.500 placeholders. Additive close_join_nba_mlb.py writes NEW gate_corpus_{nba,mlb}_close.parquet + portable sidecars; live corpora sha-identical before/after; mlb close matches S81's at 0.0. Re-score vs the close: S108's enet / HGB -0.040888 / -0.007784 (nba), -0.022663 / -0.005575 (mlb) -- 0 of 4 clear, 3 CIs entirely below zero; the top-5 vs-Elo screens are 10/10 negative vs the close and S85's +0.005221 becomes -0.000660 against the SAME Elo on the close-covered window (the window, not the reference, carries most of the disappearance). No prereg, no charge, ledger 18. Live corpora NOT swapped (mlb close covers 2.28 pct of its corpus). 8 tests |

| S113 | harness | THE PREGAME FACTORY'S NBA / MLB INCUMBENT IS ELO, WHICH THE CLOSE BEATS BY +0.026 / +0.007 (S112): every nba/mlb T0/T1 screen and promotion in the pod DB is Elo-relative and therefore not market-relative. Bar: screen_predictor's incumbent for nba/mlb = p_close from gate_corpus_{nba,mlb}_close where present (labelled close_source), Elo ONLY as a labelled fallback, the screen restricted to close-covered rows for the market-relative verdict; the pod runner's nba/mlb T1 rows re-screened on the close-covered subset (charges off); the promotion list re-ranked; report how many Elo-relative promotions vanish. After S111 lands (it holds screen_predictor) | S112 | LANDED 2026-09-03 0dae6fc40 (memo S113_close_incumbent_2026-09-03.md) -- NULL + PREMISE HELD (screen_predictor:37 mapped nba/mlb to p_base; tiers/promotion/results_db are incumbent-agnostic, the sqlite result table has no incumbent column). Opt-in FOUNDRY_CLOSE_INCUMBENT=1 makes corpus_states serve p_close from gate_corpus_{nba,mlb}_close on close-covered rows only (labels: mlb devigged_close; nba first_inplay_tick / pregame_venue_close -- never called a devigged close), drops the six close columns from the feature table, window 800 -> 499 nba / 452 mlb, Elo the labelled fallback, default OFF with both OFF partition shas reproducing S58 exactly. LOCAL SCREEN: 6 of 889 nba and 0 of 48 mlb rows ahead of the close (control vs Elo: 718 of 889); best of 11 families nba_opp_allowed +0.000640 CI [-0.003011, +0.004291]; S85's +0.005221 family drops to +0.000178; NO prereg. 147 of 240 Elo-relative promotions VANISH (92 of the S58 list's 140); mlb_inning loses all 20. Uncharged, ledger 18. Post-flip partition shas 1980f64c6a21fc1e / bee51ac662607eb5 -- never mix a T2 across the boundary; the 140 nba/mlb S58 promotions must not be charged as market-relative. Tests 5 + 3 + 7 + 8 |

| S114 | signals-ingame | IN-GAME SIGNALS HAVE NEVER BEEN COMBINED OUT OF SAMPLE: S102 screened 564 NBA derived-state hypotheses one at a time (best +0.00025); S79 showed in-sample top-k combination is worse than k=1. Untested: a NESTED-selection ensemble -- inside each outer walk-forward fold, rank the hypotheses on the TRAIN window only (FDR-adjusted screen p), take the top-k by DISTINCT source column (S79 pick rule, k in 3, 5, 10), fit one L2 logistic over them with logit(market) as offset, score the held-out fold vs the raw market and vs the S94 recalibration null; on the POD over the S86 SCREEN side. Bar: >= +0.004 vs the raw market with a game-clustered CI excluding zero and beating the recal null -> prereg DRAFT; else the honest table incl. how often the selected set changes across folds | S102 S79 S94 | SCREEN NULL 2026-09-03 575a4d5c9 (pod; memo S114_ingame_ensemble_2026-09-03.md). Premise CONFIRMED (no nested selection over in-game hypotheses existed). Nested selection on the TRAIN window only (BH-adjusted screen p, top-k by distinct source column, L2 logistic with logit(market) as offset), 192,635 held-out ticks / 673 games / 5 folds: 0 of 4 k clear +0.004; every k BEHIND the raw line (best k=5 -0.000400, CI [-0.000934, +0.000133]) and behind the S94 recal null. TWO PARENT HEADLINES FALSIFIED: S102's +0.000248 does not survive nested selection (its best form is never picked; 1 of its top 10 ever selected); S79's 'top-k worse than k=1' does NOT reproduce here (k=5 beats k=1 by +0.000083, CI [+0.000003, +0.000163], 48x below the bar, uncorrected across four k). k=1 selection stability 0.000 (a different hypothesis every fold); BH discoveries on the train window 0/0/0/7/22 -- early selection is ranking noise; PBO over k 0.80. A2 reproduced on a different machine to 1.3e-18. No prereg, uncharged, ledger 18. SINGLE-WINDOW |
| S115 | signals-ingame | MORE MODELS, SAME OFFSET: every in-game arm is a single logistic term; untested are non-linear residual models over the tick state (period, clock, margin, margin trajectory, logit(market), logit(prior)) with logit(market) as OFFSET so the model learns only what the line misses -- HistGradientBoosting (shallow, strong regularisation), a small MLP, and a monotone-constrained GBM (margin monotone), nested walk-forward on the S86 SCREEN side, purge by game, 1-day embargo. Bar: >= +0.004 vs the raw market with a clustered CI excluding zero and beating the recal null; report PBO and per-fold stability; expect NULL (the line is a near-martingale) | S86 S94 S108 | LANDED NULL 2026-09-03 fc8014d3c (memo S115_ingame_models_2026-09-03.md; ran as an Opus main-repo lane because codex auth expired). Three non-linear arms over logit(market) as a TRUE offset (HGB depth-3, tiny MLPRegressor with early stopping, monotone HGB), each one Newton step on the logistic working response, 8 tick-time as-of features (leak guard raises on same-tick reads), S94 fold windows / purge / embargo, inner folds pick the grid: 192,635 held-out ticks / 673 games. Best arm mlp -0.000549 vs the raw line (CI [-0.001476, +0.000378]) against +0.004; 0 of 3 beat the recal null; PBO 0.071; n_eff 3,240. Both HGB arms picked the strongest l2 in 5/5 folds -- the inner CV asked for the least capacity on offer (the S108/S111 shape). Deviations recorded: MLPRegressor not Classifier (working response), zero-capacity arm reproduces the RAW market not the recal null (both tested). Uncharged, ledger 18. SINGLE-WINDOW |
| S116 | signals-ingame | CROSS-SPORT POOLED IN-GAME BLEND (gap map L18): MLB (392 real games after S106) and NBA (673 screen games) in-game arms are fit per sport; a sport-blind residual model over normalised state (fraction of game elapsed, margin in sport-sigma units, logit(market), logit(prior)) with a sport indicator and partial pooling (hierarchical shrinkage of the per-sport coefficients toward the pooled ones) may lower fold drift on the low-n sport. Bar: MLB screen-side Brier vs the raw line with pooled vs per-sport fits, clustered CIs; the pooled fit must not hurt NBA; no charge | S86 S82 S106 | SCREEN NULL 2026-09-03 3c4aca871 (memo S116_pooled_ingame_2026-09-03.md). Premise CONFIRMED (no cross-sport in-game model existed; 392 real games is the whole MLB store, 145 real games / 28,886 ticks on the S82 screen side). Sport-blind residual logistic over normalised state with logit(market) as a fixed OFFSET, fit per-sport / fully pooled / partially pooled on ONE strictly causal shared calendar (purge by cluster: NBA game, MLB real game; 1-day embargo). MLB (9,669 ticks / 63 real games / n_eff 103): line 0.215528 -> partial 0.202690 = +0.012837, real-game-clustered CI95 [-0.002273, +0.027948] CROSSING ZERO -- bar NOT met, no prereg; vs the S94 null +0.008137 (crossing); fully pooled is the WORST arm (-0.007614 vs per-sport). Partial pooling cut MLB across-fold coefficient sd 0.276 -> 0.206 (-25 pct) but over only 2 usable folds (the settlement purge leaves no settled MLB train ticks for the first two blocks) -- a direction, not a measurement. NBA: the corpora are date-disjoint and ordered (NBA ends 06-10, MLB starts 06-30), so all three arms are byte-identical -- 'must not hurt NBA' is closed BY CONSTRUCTION, not tested; the residual does not help NBA (-0.000343). Defect fixed en route: bare-integer NBA cluster ids re-typed per CSV chunk split 673 clusters into 676 -- ids now sport-prefixed with a planted test. Limit: ~900 scored MLB real games needed (S93 wall). Uncharged, ledger 18. SINGLE-WINDOW |

| S117 | signals-ingame | THE IN-GAME TIER HAS RUN ON ONE SPORT: soccer_intl's joined store carries structured score + minute from 2026-06-28 (S104; 47 games with a line, 25 usable) and S82 skipped it because its only column then was e4's own signal; with S104's structured state the tier can screen minute x score-state x pregame-prior features vs the in-play line. Bar: the S82 tier on soccer_intl SCREEN side (purge on settlement, null = recalibration on identical rows, tick-time as-of by truncation invariance), n games / ticks / informative, ranked features with clustered CIs; expect NULL at this n; report the CI half-width achieved and the games needed for 0.002 | S82 S104 S99 | LANDED 2026-09-03 aafe17759 -- SCREEN NULL, CLOSED AT LIMIT on corpus size (memo S117_soccer_ingame_screen_2026-09-03.md). The tier now runs on a third sport (foundry/ingame_screen_soccer.py imports BAR / assert_tick_asof / walk_forward_feature verbatim): 29 usable games / 3,658 ticks (structured score + minute + line + settled outcome; draws graded 0.5), 2026-06-28..07-12; incumbent = the live soccer model_prob (no e4 for soccer); the market line reported beside it. Gate-corpus prior premise FALSIFIED: the soccer gate corpus is club leagues to 2026-05-24 with no p_close and zero KXWC ids -- prior substituted with the game's own first model_prob (labelled). iso_week SCREEN 13 games, walk-forward purged on settlement; the verbatim 1,000-tick train floor eats 87 pct of the screen side (4/6 folds unfittable) -- a labelled floor-200 sensitivity arm reported beside it (BAR untouched). 0 of 7 features clear +0.004 in either arm, every CI straddles zero, behind the raw line throughout (market 0.157753). Best half-width 0.020755 on 8 clusters -> ~862 games for 0.002; store holds 29. Uncharged, ledger 18 |
| S118 | harness | RED-TEAM ROUND 2 over the 40 rows landed since S79: the in-game tier (truncation-invariance guard, purge on settlement, null arm), the as-of supply registry (prior rule, all-NaN guard), the informative-tick triple, the real-game split, the close incumbents (first in-play tick as a close), the nested-selection ensemble, the pooled blend; adversarial, read-only, reproduce every finding with running code; each finding = a register-row candidate with the exact line. Bar: >= 10 reproduced findings or an honest 'none reproducible' with the probes run | S40 precedent | DONE 2026-09-03 bb35cc3af (memo REDTEAM_ROUND2_2026-09-03.md): 14 reproduced findings + 10 passing probes; filed as S124-S135; all fixed the same night except the S153 follow-ups (test_ledger_schema_s13.py:26 existence-only skip) |
| S119 | signals-ingame | MLB IN-GAME STATE FAMILIES WERE NOT_SUPPLIED (S82: 7 of 14 members, e.g. bullpen usage as-of, pitcher TTO by real pitcher id) and the tier ran on 41 mislabelled clusters (S106). With the S83 player ids, the S85 as-of supply registry (prior rule) and real_game_seq clusters, re-run the S82 MLB tier with the previously unsupplied members: bullpen availability as-of (relievers used in the prior 2 days), starter TTO from the real pitcher id, batter platoon vs pitcher hand as-of, catcher, umpire (probables.parquet) -- SCREEN side, clusters = real games, informative-tick n_eff. Bar: ranked table with clustered CIs; expect NULL at 88 clusters; report half-width | S82 S83 S85 S106 | CLOSED 2026-09-03 d6efd2f36 -- PREMISE-STOP + NULL (memo S119_mlb_ingame_supply_2026-09-03.md). Supply half FALSIFIED: the five mlb_* identity columns exist only on ISO-week-28 ticks (8,287 of 78,986; 53 game_ids, first dates 07-08..07-12) = the S82 VERDICT side, so starter TTO and batter platoon have zero screen-side supply; probables.parquet has no catcher column and 0 non-null sp_hand; bullpen_relief_chains ends 2026-07-02 before the 07-03..07-05 folds (censored). Only umpire suppliable (1 of 5 < 3) -> no builder written, tier not re-run. Cluster half DELIVERED from the S82 Q9 archive: 41 game_ids = 88 real games (392 store-wide, S106 reproduced), all 14 intervals re-quoted with point estimates identical to 1e-17, 0 of 14 clear the unmoved bar; half-width 0.007038 leader / 0.000991 median. CORRECTION: 495 of 15,702 screen ticks are W28-dated (pulled onto W27 by the parent ticker's first date); dropping them halves the S82 leader (+0.003332 -> +0.001628). Uncharged, ledger 18. Reopen condition: identity capture on screen-side dates |

| S120 | docs | NO SINGLE SIGNAL INVENTORY: 120 register rows now carry every signal tested (pregame and in-game, four sports) with verdicts scattered across ~60 memos; JOB_EVIDENCE_PACKET / PUBLIC_EVIDENCE do not yet reflect the S79-S119 wave. Bar: docs/evidence/SIGNAL_INVENTORY_2026-09-03.md -- one table per surface (pregame per sport vs the close / vs Elo labelled; in-game per sport vs the line) with, per signal or arm: n units, improvement, CI, verdict, memo, and whether the reference was the market; the do-not-claim list refreshed; the paper week / forward status (S55 n_settled) as of tonight; and a PROPOSED patch for JOB_EVIDENCE_PACKET's signal section under docs/research/organization-sprint (JOB_EVIDENCE_PACKET is a public truth source -- propose, do not edit). Every number copied from a memo, none recomputed | all S rows | LANDED 2026-09-03 f1cec9210 -- docs/evidence/SIGNAL_INVENTORY_2026-09-03.md (378 lines): one table per surface with the reference LABELLED (devigged close soccer/tennis; first in-play tick / pregame venue close nba -- see S132 contamination; Kalshi last pre-first-pitch traded quote mlb; Elo p_base marked NOT market-relative), n units, improvement, CI, verdict and memo per signal; the 18 charged trials in K order (14 BEHIND / 2 NULL / 1 market-sharper / 1 MATCH, 0 AHEAD); the do-not-claim list refreshed (every vs-Elo positive is not market-relative; S116's +0.0128 CI crosses zero on 2 folds; nothing beats the close or the in-play line at +0.004); tonight's forward status (execution_status no_data / INSUFFICIENT / 0 settled of 20; S55 window 2 absent); what still moves the number = acquisitions and capture. Thirteen in-game directions, all null. PROPOSED JOB_EVIDENCE_PACKET patch at docs/research/organization-sprint/PROPOSED-S120-job-evidence-signals.md (gitignored, 5 edits, for Neel). Nothing recomputed |

| S121 | harness | IN-GAME PARTITION LEAKS ACROSS THE ISO-WEEK BOUNDARY: the in-game tier partitions by the ticker's game-first-date iso week, so 495 of 15,702 S82 screen ticks (3.2 pct) are dated in the VERDICT week (a ticker quoted from 07-06 whose ticks run into 07-07/08); dropping them halves the S82 leader (+0.003332 -> +0.001628, S119). Bar: the partition assigns each TICK by its own date's iso week (or the ticker's real-game date after real_game_split), the screen/verdict sets are asserted disjoint at tick level, the S82 / S117 / S119 tables re-quoted on the clean partition from their archives (no refit) with the verdicts unchanged or re-labelled; a test with a ticker spanning two weeks | S119 | LANDED 2026-09-03 d71f411fb (memo S121_tick_partition_2026-09-03.md) -- PREMISE CONFIRMED (495 of 15,702 reproduced from the S82 archive; reverse leak 0 over 52,558 raw ticks -- structural, since a ticker's date is the min of its ticks). foundry/tick_partition.py adds the opt-in tick_week mode (FOUNDRY_INGAME_PARTITION), tick-level disjointness + no-real-game-on-both-sides asserted, real games kept whole by the S106 split (purge blocks by the real game's first tick); default ticker_week unchanged so S82 / S117 reproduce byte-identically. Re-quoted from the archives with no refit: 15,702 -> 15,336 ticks / 88 -> 85 real games; leader +0.003332 -> +0.001951 CI [-0.004516, +0.008417]; 0 of 14 clear before or after; all SCREEN_NULL unchanged (score_change_recency's CI now excludes zero on the negative side); S117 unchanged; reproduces S119's two tables to 0.0. Tests 5 + 4. Mode has zero production callers yet -- the next MLB in-game screen must set it |

| S122 | data | TWO TENNIS FAMILIES STILL AT LIMIT ON COVERAGE (S111): tennis_schedule_density (469/800) and tennis_travel_scouting (454/800) need their parquets extended to the WTA half with a different builder (schedule_density.parquet / travel_scouting.parquet are ATP-only). Bar: WTA rows built from wta_matches.parquet with the same strictly-before rule via the existing builders parametrised by spine (or a new builder under domains/tennis/ importing them), registered in asof_supply, coverage -> >= 0.8 on the served window, the two families screened locally vs the devigged close (expect NULL) | S111 memo | CLOSED AT LIMIT (both families) / ACQUISITION LANDED 2026-09-03 3b0a7f321 (memo S122_tennis_wta_schedule_travel_2026-09-03.md). Premise reproduced (469/800, 454/800); the implicit failure mode FALSIFIED (wta_matches.parquet carries every column both builders read, zero nulls). domains/tennis/wta_schedule_travel.py builds schedule_density_wta (22,540 rows) and travel_scouting_wta (18,106) with the frozen builders on the WTA spine. Declaring the ATP+WTA pair clears the floor (800/800, 785/800; floor unmoved) and is WITHHELD because the screen it unlocks is a LEAK, not a null: Sackmann's date is the TOURNEY START date (one distinct date per tourney for 1,451/1,451 ATP and 974/974 WTA), 46 pct of rows read rest_days == 0, and the rolling result is assigned onto a duplicated date index (a champion's seven matches serve 0,3,4,5,1,6,2) -- the served value correlates +0.2616 with the OUTCOME and screens +0.020155 vs the close (a measurement artifact, never a result; no prereg). Both defects PREDATE the row in the frozen ATP builders (S136); a test pins the leak so re-registering fails first. Bonus repair: asof_supply._sides read the side at split position 4 (93 pct ATP / 75 pct WTA correct) -> str[-3]/str[-2], no landed number moves. Uncharged, ledger 18 |

| S123 | signals-ingame | THE NBA IN-GAME INCUMBENT IS WEAKER THAN A LOGISTIC ON THE LINE ALONE (S92 secondary): nba_mechanism_ladder BASE scores 0.146850 / 0.153324 where the S94 recalibration null [1, logit(market)] scores 0.144293 / 0.146843 and the raw line 0.142877 / 0.144101 (both corpora). Every NBA in-game term is therefore added to an incumbent already 0.003-0.009 behind the market-anchored null. Bar: (a) record the ordering raw line > recal null > ladder BASE as the NBA in-game baseline truth in the calibration scoreboard (S43-style, no charge); (b) decide (Neel / orchestrator) whether the answer layer's NBA in-game probability should serve the line-anchored recalibration instead of the ladder BASE -- a product baseline change, not a signal claim; (c) future NBA in-game screens use the recal null as the incumbent so improvements are measured against the stronger baseline | S92 S94 S86 | PARTS (a) AND (c) LANDED 2026-09-03 9daca0460 (memo S123_nba_ingame_baseline_2026-09-03.md); PART (b) OPEN for Neel. Premise REPRODUCED from the S92 archives with no refit: market 0.142877 < recal null 0.144293 < ladder BASE 0.146850 (661 games) and 0.144101 < 0.146843 < 0.153324 (284 games); a third corpus (S86 screen side, 192,635 ticks / 673 games) reproduces the ordering 0.078611 < 0.078930 < 0.080471; ECE orders the same way. ARMS NAMED: S102's incumbent/anchor was the RAW MARKET (brier_e4 == brier_market by construction) with S94's recal as null; the ladder BASE is S84/S92's incumbent on a different corpus -- the two lanes anchored on different arms and nothing said so (now stated). (a) nba_ingame_block + main_nba in ingame_calibration_report.py -> docs/evidence/calibration/nba_ingame_baseline_2026-09-03.json (bins, ECE, Murphy, sharpness, max-loser-WP, ess.n_eff, the S87 triple per arm per corpus; descriptive, no bar, no gate). (c) ingame_screen_nba.load_screen(incumbent = market | recal_null | ladder_base), default byte-identical, machinery in foundry/ingame_incumbent_nba.py. Tests 8 + 5. Uncharged, ledger 18. DECISION (b): should the answer layer's NBA in-game probability serve the line-anchored recalibration instead of the ladder BASE (a product baseline change, not a signal claim) |

| S124 | harness | THE TICK-TIME AS-OF GUARD IS TRUNCATION-ONLY AND PASSES A SAME-TICK LEAK: assert_tick_asof (ingame_screen.py:88) rebuilds from src[:k+1], so a feature reading its OWN tick's label is invariant and passes; measured, that feature then scores improvement_vs_null +0.273411 with CI [+0.234,+0.313] and clears_bar True. Fix: a SECOND guard in run()/sweep() that refuses any candidate column whose per-game variance is zero AND whose per-game value tracks the game's label, plus an assertion that every served column name is a member of the frozen grammar. Bar: the guard raises on the reproduced probe and leaves the 564 S102 hypotheses byte-identical | S118 | CLOSED 857d87e8f 2026-09-03 (memo S126_rerun_S124_gate_2026-09-03.md): second gate in. gate_features refuses any served feature outside the sport's frozen grammar (ingame_screen.FEATURES for MLB; semantic_hash over ingame_grammar_nba for the NBA 576) unless allow_adhoc=True, and an opted-in ad-hoc column goes through the new assert_column_blind (the column must not determine the label at tick grain). DEVIATION REPORTED: assert_label_blind permutes the label and REBUILDS, so on an already-materialised column it is a tautology -- shipping it as the ad-hoc gate would have been a fall-through gate; assert_column_blind states the row's own per-game rule. PROVEN NO-OP on the frozen path: the S102 top-10 re-run through the gated sweep on the full corpus is BYTE-IDENTICAL to the landed DB over 21 columns x 10 hypotheses; PROVEN TO BIND: an ad-hoc hypothesis is refused before any sqlite exists and the reproduced same-tick label reader raises TickTimeLeak. First half (assert_label_blind in assert_tick_asof) from 0c9b9d8d7 |
| S125 | harness | THE IN-GAME PURGE COMPARES TIMESTAMPS AS STRINGS AND VOIDS ON ANY NON-ISO-Z FORMAT: ingame_screen.walk_forward_feature:175-177 builds cut with strftime(...Z) and compares tick strings with <; with a space-separated stamp a train game settling 2 h before the fold is admitted (n_train=8 where 0 is correct) and BOTH asserts (:181,:182) pass because they share the broken ordering; s114_ingame_ensemble.purge:56-61 is a verbatim copy. Fix: parse once (pd.to_datetime) at the top of walk_forward_feature and compare Timestamps, asserting the parsed dtype is datetime64. Bar: the reproduced probe yields NO_TRAIN on all three formats and the s102/s114 fold tables reproduce byte-identically | S118 | FIXED 0c9b9d8d7 2026-09-03: ingame_guards.utc_stamps parses every tick stamp once to tz-aware UTC and asserts the dtype; walk_forward_feature and s114.purge compare Timestamps and still emit the archived ISO-Z cut string. Bar met: the reproduced probe yields NO_TRAIN n_train 0 on all three spellings. Measured no-op on both live corpora (0 differing train sets in 18 MLB + 4 NBA folds; both ISO-Z only) |
| S126 | harness | S114'S NULL AND CANDIDATE ARMS ARE FIT ON DIFFERENT ROWS, SO THE k LADDER IS NOT A k COMPARISON: recal_null (s114_ingame_ensemble.py:171) fits on the whole outer train window while ensemble_fold (:131) fits on the all-k-finite subset -- measured on the shipped artifact, fold F1 null 38,847 rows vs k3/k5 25,722 (66.2 pct) with test_coverage 0.996 (k1) vs 0.612 (k3+). Fix: restrict BOTH arms to the same keep mask per k, report improvement on those rows only, and publish n_fit/n_scored per arm beside every k. Bar: a re-run whose per-k rows are identical across arms; expect the NULL to stand and say so | S114 S118 | CLOSED 857d87e8f 2026-09-03 (memo S126_rerun_S124_gate_2026-09-03.md): RE-RUN DONE on the pod at HEAD parity (12 files). n_fit == n_null_fit on every fitted fold (F1 k3/k5 both 25,722 vs the landed null's 38,966); n_fit / n_null_fit / n_scored published per k per fold; the only differing cells are UNFITTABLE folds where both arms serve the same global recalibration (differential identically zero). Ladder on 192,635 ticks / 673 clusters vs raw market then vs the same-rows null: k1 -0.000537 [-0.001119, +0.000044] / -0.000106; k3 -0.000252 / -0.000078; k5 -0.000243 / -0.000070; k10 -0.000406 / -0.000048; PBO 0.80. SCREEN_NULL stands. A2 reproduces every arm to 7.6e-19. Two blockers fixed en route: utc_stamps asserted the ns dtype string (pod pandas 3.0.5 gives datetime64[us, UTC]) -- now asserts tz-aware UTC; summarise read a wrong meta key and published n_* as null (series CSVs byte-identical across the two runs) |
| S127 | harness | THE S116 POOLED-BLEND VERDICT RESTS ON 63 MLB CLUSTERS OVER 2 CALENDAR DATES: s116_pooled_ingame_2026-09-03.csv scores mlb only on folds 3 (7,972 ticks / 52 clusters / 1 date) and 4 (1,697 / 11 / 1) against 673 NBA game-clusters over 18 months, while the S116 row cites "392 real games". Fix: publish n_clusters and n_dates per sport per fold in the artifact header, and either extend the MLB screen window or re-label the verdict as a 2-date readout. Bar: the artifact carries the per-sport fold table and the memo's headline names 63 clusters | S116 S118 | DISCHARGED by S137 2026-09-03: the S116 MLB readout is re-labelled SINGLE-DATE-PAIR (folds 3 and 4 only, test dates 2026-07-04 and 07-05; 54 real-game clusters after S131); point estimate unchanged, CI crossing zero, no verdict changes |
| S128 | harness | THE prior RULE LEAKS THE EVENT'S OWN SEASON ON SOCCER: asof_supply._prior_rule:262-263 maps the event to dt.year while the source keys an integer season, and 51.78 pct of matches (11 of 11 seasons span two calendar years) fall in the year AFTER their own season label -- measured, the 2026-01-01 Brentford-Tottenham match is served -0.221968, exactly the expanding mean INCLUDING its own 2025 season, where the honest <=2024 prior is -0.275744. Fix: derive the event's season from the corpus's own season column (matches.parquet carries it) rather than from dt.year, and refuse a season-grain family on a corpus with no season column. Bar: the served value for that event becomes -0.275744, every other family's served values byte-identical, and the S85/S111 screen table re-run with the count of moved rows | S85 S111 S118 | CLOSED 689a2ecf8 2026-09-03 (memo S128_S129_asof_supply_leaks_2026-09-03.md) -- premise reproduced (11/11 soccer seasons span two calendar years; 0.5178 of matches fall in the year after their label; Brentford-Tottenham 2026-01-01 served -0.221968 = the mean INCLUDING its own season) then fixed: a grain='season' entry declares season_table (soccer -> matches.parquet, 16,322/16,322) or season_start_month (tennis = 1, byte-identical) and is REFUSED when it declares neither; the match now serves -0.275744. Only soccer_style_fingerprints moves (14/14 columns, ~8,560 of 16,322 rows); all 51 other declared pairs byte-identical; coverage 15,646 -> 14,878 with the screen window still 800/800. Re-screen: best -0.001158 -> -0.001006, 0 of 112 positive before and after -- nothing clears; S85 sections 1 / 2.1 / 3 change, S111 and S113 unchanged. 8 tests. Ledger 18 |
| S129 | harness | THE side RULE IN asof_supply HAS NO AS-OF GUARD AT ALL: _side_rule:220-237 serves the event's own row verbatim (reproduced: a planted side column equal to the outcome is served as [9.0, 9.0, -9.0]); 3 registry families use it and only tennis_schedule_density.matches_last_7d has been audited (0.0000 of 400 players' rows include the match itself). Bar: a per-column self-inclusion audit for all 9 side-served columns, recomputing each from its own source under a strictly-prior convention and reporting the share of rows that differ; any column that differs is demoted to prior or dropped | S85 S118 | CLOSED 689a2ecf8 2026-09-03 (same memo) -- premise reproduced (a planted outcome-equal side column was served verbatim) then fixed: the side rule FAILS CLOSED on an undeclared pregame as-of basis (table + date rule); the planted column is refused with a named reason. The registry declares 4 side-served columns (not 9; S122 removed the two tennis families): all 4 differ on 0.0000 of 7,222 rows from a strictly-prior recompute, and the INDEPENDENT perturbation check (truncation cannot see a same-row leak) moves 0 of 4 columns while the same perturbation reaches the next game every time; nothing demoted. NAMED LIMIT: the flag is a declaration -- a column planted inside an entry that already declares a basis is still served (the ceiling is written at s122_screen.register_leaky on purpose) |
| S130 | harness | THE INFORMATIVE-TICK FLAGS ARE STRING-EXACT AND ORDER-DEPENDENT: tick_informative.flag_ticks:75 flags a duplicate by raw (game, ts) string so the same instant spelled ...Z and ...+00:00 reads as two ticks (measured n_dup 1 where 2 is correct), and _held:33-42 shifts on the frame's row order while requote:229 -- unlike attach_informative_summary:127 -- never sorts (measured n_informative 6 vs 2 on the same six rows). Fix: normalise ts with pd.to_datetime(utc=True) before duplicated(), and sort by (game, ts) inside flag_ticks itself rather than in one of its two callers. Bar: the two reproduced probes agree, and the three archived S87 re-quotes reproduce their published CIs unchanged | S87 S118 | LANDED e870ee600 2026-09-03 (same memo) -- ACCEPT, premise CONFIRMED exactly (n_dup 0 across the two timestamp spellings; n_informative 6 in tick order vs 2 reordered; requote never sorted). flag_ticks normalises ts with pd.to_datetime(utc=True) before duplicated() (all-or-nothing fallback) and stably sorts by (game, ts) itself. BAR MET: the three archived S87 re-quotes reproduce BYTE-IDENTICALLY (14,543 / 1,593 / 1,106 informative; every CI unchanged to 6 dp; verdicts unchanged) -- the defect was real but had not bitten a published artifact. 17 tests + 2 downstream readers; A5 CORRECTED post-commit (e7a1bfe6f): 17 reader files, not 10; conclusion unchanged (15 route through the already-sorting attach path, the 2 direct callers pre-sort identically); 8 of the 17 have no test file |
| S131 | harness | real_game_split SPLITS ONE REAL GAME ON A 1-INNING FEED REGRESSION: _boundary:84 opens a new real game on ANY inning decrease with no minimum gap -- measured on the live 227-ticker store, 32 of 165 boundaries fire less than 10 minutes (mostly 1-13 SECONDS) after the previous tick, inflating n_real_games from ~360 to the published 392 and narrowing every MLB in-game clustered CI by 4.2 pct; separately _ts:66-77 returns None on an epoch-seconds ts, silently disabling the ts_gap rule. Fix: require BOTH an inning decrease AND a minimum inter-tick gap (>= 20 min) before opening a segment, and raise when no ts in the frame parses. Bar: the 32 near-instant boundaries disappear, the 3 genuine >5 h splits survive, and S106/S116 re-quote their CIs on the corrected cluster count | S106 S118 | FIXED 0c9b9d8d7 2026-09-03: _boundary evaluates the > 5 h gap first, then an inning decrease must be corroborated by >= 20 min since the previous in-play tick or a simultaneous score reset (an unknown gap does not veto); _ts parses epoch s/ms; assign_real_game_seq raises when nothing parses. Live store: 392 -> 360 real games, n_multi 122 -> 112, boundaries 165 -> 133, near-instant 32 -> 0, all 129 genuine > 5 h splits survive. Re-quoted with every verdict unchanged: S82 x3 and S119 88 -> 76 clusters, S87 trial A 315 -> 284, S121 tick_week 85 -> 73, 0 features clear the bar; S119 tables reproduce to 0.0 |
| S132 | harness | THE NBA "CLOSE" IS A POST-TIP IN-PLAY QUOTE AND NOTHING FILTERS IT: 43.5 pct of first_inplay_tick closes already have points on the board (|margin| up to 6, up to 37 points, close_sec_after_tip up to 391 s) and 28.4 pct of the <=30 s subset does too; on the 328 games where both closes exist the in-play tick beats the genuine pregame close by +0.0035 Brier on the >30 s subset (pooled +0.000722, CI straddles zero); close_within_30s is read by NO consumer -- s112_rescore_vs_close.py:65-68 takes p_close unfiltered. Fix: default every close consumer to close_source == pregame_last_tick_before_commence, make the in-play fallback opt-in and labelled in the artifact, and add close_score_on_board to CLOSE_COLUMNS. Bar: S113's promotion-survival table re-run on the pregame-only subset with the n it costs stated honestly | S112 S113 S118 | LANDED 4928b337b 2026-09-03 (memo S132_close_contamination_fix_2026-09-03.md) -- premise HELD: 43.49 pct of first_inplay_tick closes carry points on the board, 32.62 pct sit 30-408 s after tip, only 48.27 pct genuinely pre-scoring. FIX: a tick is a close only at margin == 0 AND score_on_board == 0 AND <= 30 s after tip; otherwise close_source = inplay_contaminated, p_close = NaN (allow_contaminated=True reproduces the old behaviour for A2); screen_predictor and s112 needed no edit (both filter p_close.notna()). NBA close coverage 952 -> 563 of 1,814 (220 pregame + 343 clean ticks; >= 300, NOT at limit) -- every published NBA-close number is now 'vs a partly-contaminated reference'. RE-SCORE: close-minus-Elo +0.025606 -> +0.021819 CI [+0.010468, +0.033170] (n 171; folds raised 6 -> 8 to keep the >= 5 minimum); S113's 147 of 240 vanishing promotions -> 154 of 240; best family +0.002302 CI [-0.003129, +0.007733], still under the bar; every model arm BEHIND the clean close. Tests 10 + 5. Uncharged, ledger 18 |
| S133 | harness | THE CLOSE ATTACH DROPS GENUINE PICK'EM QUOTES AND SILENTLY DOWNGRADES AMBIGUOUS ONES: _drop_placeholder (close_join_nba_mlb.py:67) is applied AFTER the MLB devig (:212), where 0.500 is the honest value of any symmetric two-sided quote -- reproduced through cjm._devig (0.52/0.52 -> 0.500 DROPPED), 19 live MLB events lost of 894 kept, biasing the close-covered corpus away from toss-ups; and _drop_ambiguous (:74) removing a duplicated pregame close makes nba_close fall through to first_inplay_tick with no field recording it. Fix: apply the placeholder rule to the RAW one-sided quote only (never post-devig) and add a close_downgraded_from column. Bar: the 19 MLB rows return, the by_source counts move, and no verdict in S112/S113 changes sign | S112 S118 | LANDED 4928b337b 2026-09-03 (same memo) -- premise HELD on the pick'em half (19 genuine devigged 0.500 MLB closes dropped because the placeholder rule ran AFTER the devig; a raw 0.52/0.52 devigs to exactly 0.500) and CORRECTED on the ambiguity half (nba_close_corpus has zero duplicated game_ids today -- a live mechanism with 0 live instances; all 10 duplicated rows are in-play side). FIX: the 0.500 placeholder rule moved onto the RAW 0.500/0.500 listing before the devig (MLB close 894 -> 910; 3 were true placeholders); _mark_ambiguous replaces the silent downgrade with a priceless 'ambiguous' row and a named _SOURCE_RANK. MLB close-minus-Elo +0.007269 (p 0.048) -> +0.006709 CI [-0.000198, +0.013617] p 0.056 -- an honest NULL where S112 read a marginal positive. Uncharged |
| S134 | harness | A FAMILY RENAME STILL RE-ZEROES K, ALIAS CHAINS ARE NOT TRANSITIVE, AND THE TWO K COUNTERS DISAGREE: reproduced on a synthetic ledger -- next_k_family = 1 instead of 3 after a rename with no alias, = 1 instead of 3 when a second alias hop is added (resolve_family, family_bars.py:114-116, is a single dict.get), and = 2 instead of 3 when a pre-S13 row carries family but k_family None (ledger.py:155 filters on it; family_bars.k_family:126-131 does not). Fix: resolve aliases to a fixed point (loop until stable, refusing a cycle), count on family is not None rather than k_family is not None, and add a test that the two counters agree on every ledger row. Bar: the three probes return 3, and k_family over the unmodified 18-row ledger is unchanged for all 39 frozen families | S89 S13 S118 | LANDED e870ee600 2026-09-03 (memo S134_S135_S130_redteam2_fixes_db_alias_ticks_2026-09-03.md) -- ACCEPT. Premise REPRODUCED (alias chain a->b->c gave next_k_family 1 not 3; a pre-S13 family row gave 1 vs 2 across the two counters) with one detail CORRECTED: on the REAL 18-row ledger the two counters already agree (all four family-carrying rows also carry k_family) -- a shape defect only a pre-S13 family row exposes. resolve_family now resolves to a FIXED POINT with a cycle guard; ledger.next_k_family imports it, so the write path is fixed by the same edit; the remaining one-liner in the token-locked ledger.py (drop the k_family-not-None filter) is PROPOSED-S134-ledger-alias-transitive.md with an xfail(strict) landing test in tree. BAR MET: k_family over the unmodified ledger unchanged for every frozen family (ingame_arms_mlb 2, ingame_arms_nba 1, soccer_gate 1); the row's 39 families is stale (40 after S102). 18 + 1 xfail tests |
| S135 | harness | THE SCREEN QUEUE'S 900-S LEASE HAS NO RENEWAL AND reap_expired IS GLOBAL: reproduced -- runner B claims the same 3 hypotheses 901 s in while runner A is still working (results_db.py:264-300, LEASE_SECONDS:55, no renew/extend API), and a hypothesis stored with sport NULL is claimable only by an UNBOUND runner (claim(sport="mlb") -> 0, claim(sport=None) -> 1), so a sport-bound pod runner can never drain it. Fix: a renew(hashes) that pushes lease_until forward, called once per batch inside the pass, and a startup assertion that no queued hypothesis has a NULL sport. Bar: the double-claim probe returns 0 rows for runner B, and the pod queue reports 0 sport-NULL queued rows | S16 S66 S75 S118 | LANDED e870ee600 2026-09-03 (same memo) -- ACCEPT, premise CONFIRMED (runner B claimed all 3 rows at +901 s; no renew API; claim(sport='mlb') 0 vs claim(sport=None) 1 for a sport-NULL row). renew(hashes) heartbeat (only still-claimed rows move); the DEFAULT lease is now LEASE_SECONDS per claimed row (explicit lease_seconds honoured exactly, so no caller changed -- protects the pod without editing foundry_runner.py); additive queue.claimer so reap_expired(now, owner) never frees the caller's own rows; enqueue REFUSES a NULL/empty-sport hash at seed time with a named reason; undrainable_queued() reports pre-S135 leftovers. BAR MET: the double-claim probe returns 0 for runner B (reap_expired(+901) 3 -> 0). seed_queue 3,564 hypotheses, 0 without a sport. 21 tests. NOT VERIFIED: renew has no production caller; the pod queue was not inspected; results_db.py is 372 LOC (over the rail; was 301) |

| S136 | data | THE TENNIS SCHEDULE-DENSITY AND TRAVEL BUILDERS LEAK BY CONSTRUCTION (S122): Sackmann match dates are the tourney START date, so any trailing-window count over a player's rows includes rounds played AFTER the match, and the grouped rolling result is assigned onto a duplicated date index (out-of-order rows). Both frozen ATP tables (schedule_density.parquet 61,232 rows, travel_scouting.parquet 55,446) carry the defect and would screen +0.020 vs the close if their families ever crossed the coverage floor. Bar: rebuild both with a within-tourney ROUND order (Sackmann round codes R128..F ordered) and a strictly-before rule at (tourney start, round) grain, counting only rounds already played; where rest days are unrecoverable at tourney grain, drop that member (CLOSED AT LIMIT per member); the S122 leak test must pass with the registration declared; re-screen both families vs the devigged close (expect NULL) | S122 memo | CLOSED AT LIMIT (rest_days) / ROUND-GRAIN REBUILD LANDED 2026-09-03 60757655c (memo S136_tennis_roundgrain_builders_2026-09-03.md). Premise reproduced verbatim (one date per tourney 1,451/1,451 ATP and 974/974 WTA; 0.4618 rest_days == 0; champion 0,3,4,5,1,6,2; corr +0.2616) and the fix's premise HOLDS (zero-null Sackmann round column on both spines; no builder orders by it). domains/tennis/schedule_density_roundgrain.py orders each player's history by (tourney start date, round) and counts only rows strictly before at that grain; four new *_rg tables with identical row counts, nothing frozen rewritten. rest_days DROPPED (unrecoverable at tourney-date grain); round-robin ties and the 0-mile within-tourney hop recorded as under-counts. After: champion 0..6; corr +0.2616 -> +0.0481 (14 d +0.2112 -> +0.0685; miles -0.0944 -> +0.0565), all inside +/- 0.0693 at n = 800. Re-screen NULL vs the devigged close: coverage 800/800 and 785/800 over the unmoved floor; best members -0.001620 and -0.001949; 0 of 32 improve, 0 of 32 CIs clear zero (S122's leaky run: 5 of 40). Registry entries + catalogue names for the orchestrator in memo sec 5 (NOT applied; asof_supply is under the S128/S129 lane). Uncharged, ledger 18; S136b 505b7c6af: section 5 APPLIED -- asof_supply declares tennis_schedule_density and tennis_travel_scouting over the four _rg tables with _load_tennis_sides and the S129 pregame basis; catalogue NAMED 33 -> 37; the S122 leak test is green WITH both entries declared; the screen re-runs from the landed tree with ZERO differences from S136 (800/800 and 785/800; 0 of 32 improve on the close); asof_supply.py held at exactly 300 LOC by moving pure data to asof_supply_columns.py |

| S137 | harness | POST-FIX RE-BASELINE: the red-team round-2 fixes changed five instruments at once (clean NBA/MLB close S132/S133, real-game split 392 -> 360 S131, tick-level partition S121, same-row null S126, season-grain prior S128 + side guard S129, UTC stamps S125). No single table shows every landed headline re-quoted on the corrected instruments. Bar: one memo + JSON re-quoting from the archives (no refit except where an instrument makes the archive unusable -- state which) every headline of S82 S86 S87 S94 S96 S97 S98 S101 S102 S103 S106 S112 S113 S114 S115 S116 S117 S119 S121 S123 with old vs new n, n_eff, improvement, CI, verdict; S127 (S116's 63 clusters over 2 dates) re-labelled here; the count of verdicts that change (expect 0 AHEAD before and after); SIGNAL_INVENTORY appended with a 'post-fix' column | S118 fixes | DONE 2026-09-03 9bd7e0552 (memo S137_rebaseline_2026-09-03.md; SIGNAL_INVENTORY section 7 appended; data/cache/eval_gate/s137_rebaseline_2026-09-03.json). 0 VERDICTS CHANGE; 0 AHEAD before, 0 AHEAD after. A2: 14 of 14 published intervals reproduce from their archives (12 at 1e-9, 2 at 5e-7 vs printed digits; S117 has no stored interval on 2 clusters). Nine artifacts move a number: S82/S119/S121 = one measurement at three stages, 15,702 ticks / 41 tickers +0.003332 -> fully corrected 15,336 ticks / 73 real games +0.001951 CI [-0.004611, +0.008512] (the leader's 41 pct fall is the tick partition, not the split); S87 trial A 315 -> 284 clusters; S106 392 -> 360; S112 NBA +0.025606 -> +0.021819 (n 351 -> 171); S113 147 -> 154 of 240; S114 re-run landed (k=5 -0.000400 -> -0.000243, still behind the line and its same-rows null); S116 re-quoted for the first time 63 -> 54 real-game clusters. Two labels change, neither a verdict: S112 MLB close-vs-Elo becomes an honest NULL (p 0.048 -> 0.056); S127 DISCHARGED by re-labelling the S116 MLB readout SINGLE-DATE-PAIR. S85's soccer screens carry S128's numbers; S125 measured no-op (0 of 22 folds differ). Module 379 LOC (90 of pure-data archive specs; over the rail, stated). New defect S143 |

| S138 | signals-ingame | THE NBA IN-GAME GRAMMAR (S102, 576 hypotheses) HAS NO INTERACTION FORMS AND THREE TRANSFORMS NOT_ENUMERATED; combining state features was only tried as a linear ensemble (S114 null). Bar: pairwise interaction hypotheses (product / safe ratio x conditionings) frozen as a NEW family, screened on the S86 SCREEN side through the gated NBA tier vs the raw market with the recal null; >= +0.004 with a CI excluding zero -> prereg DRAFT, else honest NULL | S102 S114 S124 | FALSIFIED 2026-09-03 (codex a13 14af19df6, verified + landed 36398230b): the premise 'no interaction forms' is false -- margin_x_rem (margin * rem) and margin_over_sqrt_rem are frozen BASE members of the 576-hypothesis S102 grammar (36 hypotheses each; verifier reproduced 16 x 6 x 6 = 576 and the three NOT_ENUMERATED transforms rank_in_league / z_vs_league / ratio_to_opponent). Stopped at step 0; no family frozen, no pin moved, no screen run. Residual = systematic pairwise enumeration, S144 |
| S139 | harness | (reserved: S137 re-baseline follow-ups) | S137 | RESERVED |
| S140 | harness | THREE FOUNDRY MODULES BREACH THE 300-LOC RAIL: results_db.py 372, combo/corpus_cache.py 487, screen_predictor.py 332. Bar: each split into the named module + one sibling with byte-identical behaviour (equality scripts vs the pre-split copy), re-exports so zero importers change, existing tests unchanged and green | S110 S113 S135 | LANDED WITH CORRECTIONS 2026-09-03 (codex a10, verified + landed 8c977bdc7; memo S140_loc_rail_2026-09-03.md): 3/3 under the rail -- results_db 372 -> 298 (+ results_db_sql 76), corpus_cache 487 -> 179 (+ corpus_cache_sources 165), screen_predictor 332 -> 283 (+ screen_predictor_supply 64); every moved name re-exported; zero importers edited (37 files name the modules, none in the diff). Verifier reproduced equality against the PRE-split copies on the REAL four-sport corpora (.equals on mlb 39,162 / nba 1,814 / soccer 25,834 / tennis 41,886 rows; freshness_report identical), corpus_states soccer + nba, 20-column check_feature_name refusals, a full results_db claim / record / reap sequence; all 64 base-defined names still reachable; 96 tests green in master. CORRECTION (637454e3c): both back-importing siblings crashed when imported FIRST (circular import) -- back-imports moved to file end; all six import in either order |
| S141 | data | scripts/platformkit/venue_history/nba_wallclock_join.py:122 does a backward as-of join with NO staleness tolerance (S104 pattern). Bar: routed through eval_gate/asof_join.asof_join_state at the 300 s rail (keyword default), stale_share reported, within-tolerance rows byte-equal to the pre-change join on the real inputs, test | S104 | LANDED ACCEPT WITH CORRECTIONS 2026-09-03 (codex a12 f90f7853d, verified + landed 333af3149; memo S141_nba_wallclock_tolerance_2026-09-03.md): join_game_states now routes through eval_gate.asof_join at max_staleness_s = 300 (keyword default) and reports stale_share + rail in attrs. Verifier-reproduced from base 4b7169973 on the main data tree: 53/53 events, 170,307 ticks, matched-state age p50 18 s / p90 196 s / max 1,026 s, 555 ticks (0.33 pct) over the rail, 162,013 with no prior state (stale_share 0.9546 over all input ticks). Non-tautology held: 7,739 within-tolerance rows byte-equal to the PRE-change output (per-game assert_frame_equal 53/53). CORRECTIONS: one existing test edited (a 3,600 s fixture necessarily fails the rail; narrowed to 60 s, assertions kept); module 313 LOC vs the 300 rail (S145); the rail also binds the SECOND caller nba_checkpoints_full.py:228 unmeasured (S146) |
| S142 | harness | EIGHT LANDED IN-GAME MODULES HAVE NO A2 ARCHIVE-REPRODUCTION TEST (S130 A5 sweep): s92, s94, s96, s97, s103, s115, s116, ingame_screen_soccer. Bar: one per-file test each recomputing the headline Brier / improvement / clustered CI from the archived SERIES with the reference DM, asserting the memo's digits; clone-safe skip when the archive is absent; 8/8 or each miss CLOSED AT LIMIT | S130 | CLOSED 8/8 -- ACCEPT WITH CORRECTIONS 2026-09-03 (codex a11, verified + landed 663bb104f; memo S142_a2_tests_2026-09-03.md). All eight archives present (no CLOSED AT LIMIT); verifier re-ran the eight files in master: 8 passed, 0 skipped. Non-tautology held: Brier, paired improvement and the game-clustered DM CI recomputed from the archived per-tick series with the reference dm_test.diebold_mariano; every asserted digit and n / n_informative grepped back to its own memo; assertions proved live by a 1e-4 perturbation (fails) and clone-safety by a missing-archive simulation (1 skipped, path named). CORRECTIONS: (1) S116's NBA n_informative 78,761 comes from the S94 archive because S116's CSV dropped its source-model column -- S149; (2) the S142 memo overstates the soccer test (S117 prints the null Brier + seven improvements + CIs, not seven candidate Briers; the asserted values equal null minus improvement to 1e-6, memo-consistent) |

| S143 | harness | LATENT ARCHIVE-READ TRUNCATION (S137): eval_gate/s106_requote.py:88, s121_requote.py and tick_informative.requote read archived series with pd.read_csv(comment='#') while the in-game cluster convention is mlb:<TICKER>#<real_game_seq>; measured on s116_pooled_ingame_2026-09-03.csv the loss column reads NaN on 9,669 of 9,669 MLB rows (silent truncation, not an exception); no landed number affected today because the archives those three read carry no '#', but every new real-game-keyed archive will. Bar: drop comment='#' (or condition it on a leading '#') in the three readers + one per-file test that a cluster id containing '#' survives the read | S137 | DONE 2026-09-03 bc778706b (memo S143_archive_read_2026-09-03.md). Premise 2 of 3 CONFIRMED (s106_requote x2, tick_informative.requote), 1 FALSIFIED (s121_requote already passed no comment argument). Measured: with comment='#' the S116 archive's loss column is NaN on 9,669 of 9,669 MLB rows and 63 real-game clusters collapse to 27; without it the headline reproduces at +0.012837. The header-comment convention is REAL (2 of 58 archives open with a '# prereg_sha256 ...' seal line and one is read by tick_informative._ARTIFACTS), so a flat deletion would have broken a live reader -- fix = archive_read.read_series skipping only LEADING '#' lines. MEASURED NO-OP: old vs new identical over 157 (s106) and 123 (S87) leaf fields; re-runs reproduce the S131-corrected archives exactly; verdicts unchanged. Tests 3 + 17 + 4 + 3 |

| S144 | signals-ingame | S102's grammar has two hand-picked combined-state bases but no SYSTEMATIC pairwise enumeration: 14 non-combined bases -> 91 pairs x {product, safe ratio} x 6 conditionings unenumerated (~1,000 hypotheses). Bar: frozen family ingame_nba_pairs (disjoint from the 576, excluding the two frozen forms), screened on the S86 SCREEN side through the gated NBA tier vs the raw market with the recal null; >= +0.004 with a CI excluding zero -> prereg DRAFT, else honest NULL | S138 S102 | DONE 2026-09-03 a99528699 -- ACCEPT WITH CORRECTIONS (codex a13, Opus verifier; memo S144_nba_pairwise_2026-09-03.md). Frozen family ingame_nba_pairs = 14 non-combined bases -> 91 pairs x {product, sign-preserving safe ratio} x 6 conditionings = 1,092 hypotheses, semantic-hash intersection with the 576 measured at 0, both frozen combined forms excluded; pin moved once 6e7878ae5 -> 9e05a449e. S86 SCREEN side, full grid in 1,145 s (no sampling): 1,076 screened / 16 UNSCORED (the phase-1 pace_ratio_p1 forms); 0 of 1,092 clear the unchanged +0.004 bar; best pair margin x rem product +0.000154 CI [-0.000023, +0.000332]; 4 BH discoveries all ~2 orders below the bar. Verifier recomputed the top ten from the archived series alone to 4.97e-11; null and candidate fit on identical rows; guards pass at 8 probes. NULL, SINGLE-WINDOW. Corrections: the memo's old pin was wrong (fixed); n_informative 68,925 is a corpus constant; ingame_screen_nba.py edited additively (pair enumerator joins the frozen gate set); test_family_bars gained three skip guards on an absent ledger -> S153 |

| S145 | harness | nba_wallclock_join.py is 313 LOC after S141 (was exactly 300); the empty-frame block is duplicated twice. Bar: <= 300 with byte-identical behaviour, the four per-file tests pass unedited | S141 | DONE (accept with corrections) 2026-09-03 -- codex a16, verified + landed 4ec129fd2: nba_wallclock_join.py 313 -> 282 LOC; verifier reproduced byte-identical join_game_states output over 60 case x rail combinations (bytes, columns, dtypes, attrs, raised exceptions); public API unchanged; 300 s rail unmoved; the four venue_history / asof_join tests pass unedited. CORRECTION: the bar was met by cutting the module docstring 36 -> 4 lines, not by the de-duplication (the dedup alone is net +4 LOC) -- the removed provenance (cdn.nba.com WAF block, tricode split-at-3, outcome_home_win source != games.parquet, output schema) is unrecorded elsewhere -> S151 |
| S146 | data | THE S141 300 s RAIL ALSO BINDS nba_checkpoints_full.py:228 (the Polymarket checkpoint corpus behind every NBA in-game result: 465,249 ticks / 1,593 games) and its state-age distribution was never measured; the existing parquet is unchanged (landed numbers stand) but any REBUILD would drop rows unmeasured. Bar: replay that corpus build path read-only and report p50 / p90 / max state age, the share > 300 s and the row count that a rebuild would lose, per period; if > 1 pct of ticks would be lost, a labelled keyword (or a documented rebuild note) before any rebuild | S141 | MEASURED 2026-09-03 09f6ebaf3 (memo S146_checkpoint_corpus_stale_share_2026-09-03.md): build path replayed read-only and fully offline (0 cache misses) with pre-S141 semantics; the replay REPRODUCES the landed parquet exactly (465,249 rows / 1,593 games, DataFrame.equals on all 13 shared columns). Matched-state age p50 646 s / p90 7,684 s / max 62,977 s; 250,989 of 465,249 rows (53.947 pct) exceed the 300 s rail; per period P1 0.0001, P2 0.20, P3 0.03, P4 0.78, OT 0.86-0.88; every game loses rows, none loses all. 93.83 pct of the loss is POST-FINAL-BUZZER ticks matched to the game's last play state (median game_clock_s 0.0) -- prices carrying the final score forward, the S99 defect the rail exists for; only 3.33 pct of the corpus is a mid-game pbp gap (13,775 = halftime). The four ingame_nba_winprob checkpoint buckets are near-insensitive; the 18 every-tick readers would each lose 53.947 pct of rows. REBUILD UNSAFE without a passthrough (proposed, not applied; alternative: build at inf with an additive state_age_s column). Existing parquet unchanged; landed numbers stand -- but see S148: half of every NBA tick headline's n is post-final ticks |

| S147 | harness | FOURTH INSTANCE of the S143 archive-read defect: s86_nba_every_tick.py:208 read its reference CSV with comment='#' (harmless today: NBA game_id keys carry no '#'). DONE 2026-09-03 by the orchestrator: re-pointed at archive_read.read_series; the S143 source scan now covers s86; tests 3 + 5 | S143 | DONE (2026-09-03) |

| S148 | signals-ingame | HALF THE NBA TICK CORPUS IS POST-FINAL (S146): 235,513 of 465,249 ticks are matched to the game's last play state at game_clock 0 with the final score known, so both the line and every arm are trivially near-certain there; the pooled Brier / n / n_eff quoted by S86 S94 S96 S97 S98 S101 S102 S103 S114 S115 S116 include them. Bar: re-quote every NBA tick headline from its archive (period, game_clock, margin are in the S86 CSV) on LIVE ticks only (game_clock > 0 or period < 4 -- state the exact rule; plus the informative-tick mask), old vs new n, n_eff, improvement, CI, verdict; S137-style, no refit; expect verdicts unchanged and CIs wider; add the live-only column to SIGNAL_INVENTORY section 7 | S146 S137 | DONE 2026-09-03 d0fff4010 (memo S148_live_requote_2026-09-03.md; SIGNAL_INVENTORY section 7b). LIVE RULE from the data (no status column): live iff game_clock_s > 0 OR period < 4; dead iff period >= 4 AND game_clock_s == 0. Exclusions 122,065 of 232,951 (52.40 pct) on the S86 screen CSV and 244,183 of 465,249 (52.48 pct) on the checkpoint corpus (8,670 more than S146's rail-based count; premise not falsified); 0 games lose all ticks; the rule joins onto every other NBA archive by (game_id, ts) with 0 unmatched. A2 first: 60 of 60 published readings reproduce at 1e-9. VERDICTS CHANGED: 0 of 15 headlines, 0 of 10 sweep hypotheses, 2 of 27 S86 cells (both NEGATIVE -> NULL: the 'far behind in the OT endgame' reading was post-buzzer prices); 0 AHEAD before and after; every n_eff roughly halves, every interval widens, 13 of 15 point estimates move FURTHER BEHIND: S86 pooled -0.004857 -> -0.007298 [-0.012501, -0.002096] (110,886 live ticks); S98 -0.002378 -> -0.004119; S103 -0.002117 -> -0.004461; S114 / S115 all further behind; the S94 target cell is IDENTICAL (23,561 rows, no P4/OT ticks -- the non-tautology check). S101 P4 grouped coverage reverses: market 0.98 -> 0.84, model 0.94 -> 0.76 -- both bands UNDER-cover on live P4 ticks; OT not scorable live. Module 415 LOC (stated, precedent S137). Uncharged, ledger 18 |

| S149 | harness | S116'S ARCHIVE CANNOT REPRODUCE ITS OWN NBA INFORMATIVE COUNT (S142 correction): data/cache/eval_gate/s116_pooled_ingame_2026-09-03.csv dropped the raw source model column on the NBA side (the MLB side is reconstructible from the archived gap by logit inversion), so n_informative for NBA is quoted from the S94 archive -- a Q9 completeness gap. Bar: the S116 writer archives the raw model and market columns per tick (additive); a regenerated archive (no refit needed for the columns) reproduces 78,761 from itself; the S142 S116 test re-pointed at S116's own archive | S142 S116 | DONE (accept with corrections) 2026-09-03 -- codex a16 (same landing 4ec129fd2): s116_pooled_ingame's writer appends model_raw / market_raw per tick (additive, no refit); the a2 test re-pointed at those columns with a clean skip on the pre-additive archive. Verifier recomputed n_informative(NBA) = 78,761 from the regenerated scratch archive's own columns, matching S116's own summary; premise CONFIRMED (a logit inversion of the archived gap gives 77,795, so the raw columns are needed). Landed archive unrewritten. CORRECTIONS: the scratch archive was a no-refit reconstruction joining the S94 model column (a 30 s host command ceiling blocked the real S116 run), so writer-produced self-reproduction is unproven end to end; the a2 test now SKIPS in master where it passed -> S152 |

| S150 | harness | DEAD RUNNERS HOLD THE QUEUE FOR 12 HOURS (S135 side effect, found in the quota incident): the default lease is now LEASE_SECONDS per claimed row, so a 50-row batch claimed by a runner that then dies (four relaunches tonight) is held ~12.5 h; after the incident 9,503 rows were claimed-without-result and the runner idled at unclaimed 0 until the orchestrator released them by hand (rows with a claim and no result at that tier). Bar: the runner releases its outstanding claims on SIGTERM / normal exit (atexit + signal handler calling release(claimer)), reap_expired also reaps claims whose claimer pid is not alive when claimer encodes host:pid, and the default lease reverts to LEASE_SECONDS x min(batch, 5) with renew() actually called per hypothesis by the runner (S135's renew has zero callers); tests for each | S135 pod incident | DONE 2026-09-04 53dbe8d24 -- ACCEPT (codex a10 abe3ccfb2; verified Sonnet under contract A/B/Q, Opus 529 x2; memo S150_runner_leases_2026-09-03.md). Premise reproduced on master: renew() 0 production callers, lease = LEASE_SECONDS x len(hashes). After: claim_lifecycle registers atexit + SIGTERM/SIGINT release(claimer) (runner_leases.py:52-71, wired foundry_runner.py:292-296), reap_expired frees dead SAME-HOST claimers only (other-host row asserted untouched), default lease LEASE_SECONDS x min(batch,5) (4500 s for 50 rows), renew() per hypothesis in the loop's finally; tests 5/5 new + results_db 21/21 + s16 7/7 + tiers 12/12; max touched file 299 LOC. Deploy to the pod at md5 parity = the 2026-09-04 bring-up |

| S151 | docs | nba_wallclock_join.py's provenance was cut to meet the 300-LOC rail (S145): the cdn.nba.com WAF-block rationale, the Kalshi tricode split-at-3 rule, the 'outcome_home_win comes from the ticker's settled result, NOT games.parquet' anti-landmine note, the no-leak join semantics and the output schema now live nowhere. Bar: restore them verbatim to docs/evidence/harness/nba_wallclock_join_PROVENANCE.md (or a sibling module docstring) linked from the module's 4-line docstring; or declare the rail CLOSED AT LIMIT for this module | S145 | DONE 2026-09-04 d8511e226 -- ACCEPT (codex a12 71e591325; verified Sonnet under contract A/B/Q, Opus 529 x2; memo S151_wallclock_provenance_2026-09-03.md). 5/5 items (n = 5 CONSTRUCT) restored VERBATIM from the pre-S145 blob 333af3149 into docs/evidence/harness/nba_wallclock_join_PROVENANCE.md (whitespace-collapsed diff empty), module docstring links it, module 280 LOC, 0 code lines changed, 0 non-ASCII; tests provenance 1/1 + loc 1/1 + tolerance 1/1. The codex commit's edits to this register and the ledger were DROPPED at landing (orchestrator-only files) |
| S152 | harness | S116 END-TO-END RE-RUN PENDING (S149): the writer now archives model_raw / market_raw but the landed archive predates it, so tests/platformkit/ingame/test_s116_pooled_ingame_a2.py SKIPS and the NBA informative count (78,761) is asserted nowhere. Bar: re-run `python -m scripts.platformkit.eval_gate.s116_pooled_ingame` to a NEW archive path (never overwrite the landed one), confirm the headline reproduces (+0.012837 MLB, NBA -0.000343) and n_informative(NBA) = 78,761 from the new archive's own columns; re-point the a2 test at the new archive so it PASSES | S149 | DONE (accept with corrections) 2026-09-03 0eede4f55 (memo S152_s116_rerun_2026-09-03.md): s116_pooled_ingame re-run unmodified to the stem s116_pooled_ingame_2026-09-03_rerun (53 s; 202,304 rows x 25 cols); landed archive never overwritten. Both headline POINT estimates reproduce exactly (MLB +0.012837, NBA -0.000343); 500 of 560 JSON leaves identical; all 60 differences MLB-side with ONE cause: s116_corpus.load_mlb reads real_game_split, which S131 changed after the archive was written, so MLB clusters are 63 -> 54 scored and the MLB interval moves [-0.002273, +0.027948] -> [-0.002376, +0.028051] (the landed interval is RE-LABELLED pre-S131 cluster unit); n_informative MLB 2,622 -> 2,613. n_informative(NBA) = 78,761 recomputed from the new archive's OWN model_raw / market_raw -- S149's Q9 gap closed end to end from the writer; the a2 test re-pointed and PASSES (1 passed, was skipped). Verdict unchanged: NULL. Follow-up (unassigned): the pre-S131 MLB interval quoted in the S116 memo and read by s137/s148 needs the label or a re-quote on the corrected unit |

| S153 | harness | MISSING EVIDENCE READS AS A PASS in test_family_bars.py (S144): three tests -- the 18-row FWER ledger integrity assertion and the two K-counter agreement tests -- now skip when data/cache/eval_gate/backtest_fwer.jsonl is absent instead of failing. Bar: in the main repo the tests ASSERT presence and fail loudly; skip only under an explicit worktree marker (an env set by codex-sport, or .git being a file rather than a directory); test for both modes | S144 | DONE 2026-09-03 35ae9d327 -- ACCEPT (memo S153_absent_ledger_guards_2026-09-03.md). Premise CONFIRMED: the guards were file-existence-only; main repo .git is a DIRECTORY with the ledger present (18 rows, md5 a4ae7c13, 20 passed / 0 skipped), a codex worktree .git is a FILE (gitdir pointer) with no data/cache/eval_gate at all. New eval_gate/worktree_marker.py::is_worktree_checkout (.git is a file, or FOUNDRY_WORKTREE=1) drives ONE shared require_ledger() replacing the three guards: worktree -> skip naming the path; main repo with the ledger absent -> AssertionError naming the resolved path. 22 passed / 0 skipped in master (20 + a skip-mode test + a fails-not-skips test); FOUNDRY_WORKTREE=1 gives 19 passed / 3 skipped, exactly the ledger tests. No bar or count moved; ledger read-only. Follow-up (unassigned): test_ledger_schema_s13.py:26 carries the identical existence-only skip |

## Quant additions to the verifier contract (Q1-Q6)

These are appended to docs/evidence/tracking/VERIFIER_CONTRACT.md and apply to
every S-row exactly as B1-B10 apply to every G-row. Codex self-checks them before
reporting; the verifier applies A + B1-B10 + Q1-Q6 and nothing else.

- **Q1 PREREG SEALED BEFORE SCORING.** Any scored comparison names its prereg
  artifact and the SHA-256 seal embedded in it, and the seal predates the first
  metric. No seal, no scored claim.
- **Q2 LEDGER CHARGED BEFORE THE METRIC.** A charged trial appends its ledger row
  before it computes anything, and reports the K it read AT LAUNCH. K read after
  scoring is an automatic reject (nine unrelated trials once moved K 3 -> 12
  between prereg and launch and flipped a verdict).
- **Q3 NO BAR OR THRESHOLD MOVED.** Every bar in the spec is byte-identical to the
  bar in the artifact. A bar discovered to be unmeetable is reported as CLOSED AT
  LIMIT, never lowered (the ">= 95 pct tennis join" is the standing example).
- **Q4 LEAK CONTRACT VIA CPCV.** Anything scored out-of-sample runs through
  `walk_forward` or `cpcv_evaluate` with purging and a symmetric embargo, and any
  meta-learner consumes OOF series only, asserted to reproduce each arm's own
  reported metric to 1e-9.
- **Q5 TWO CORPORA FOR ANY AHEAD.** An AHEAD names its second corpus or
  `corpus_unit` and prints `min_corpora_eff` at the current K. If it cannot be
  satisfied, the verdict is labelled SINGLE-WINDOW in the artifact AND the
  register row.
- **Q6 CALIBRATION LANGUAGE ONLY.** No dollar, ROI, profit or "edge" language in
  any artifact, memo, ledger line or register row; none of the retracted figures
  (+18.38, 0.119, +54, 78.11, 8.94, 54.57) appears outside an explicit retraction
  context. Accuracy is not edge; an honest REJECT, NULL or BEHIND is a success.
| S154 | harness | MISSING EVIDENCE READS AS A PASS in test_ledger_schema_s13.py (S153 follow-up, the register's own open item): the ledger_copy fixture (:23-30) skips EVERY test in the file when data/cache/eval_gate/backtest_fwer.jsonl is absent, so in the main repo a missing charge ledger reads as a green file. Bar: skip only under worktree_marker.is_worktree_checkout() (the S153 helper), pytest.fail otherwise; two CONSTRUCT tests (worktree -> skip, main -> fail); every pre-existing test still passes with the ledger present; ledger.py / backtest_runner.py / the ledger file byte-identical | S153 memo; test_family_bars.py:32-39 | DONE 2026-09-04 97c5f6cc2 -- ACCEPT (Sonnet build in a16 0a72f9956 because codex was 404; verified Opus, contract A/B/Q; memo S154_ledger_schema_skip_2026-09-04.md). Premise reproduced (unconditional skip at :25-26). After: _require_real_ledger skips only under worktree_marker.is_worktree_checkout(), pytest.fail otherwise; 2/2 CONSTRUCT modes reproduced UNPATCHED in master by the verifier; master run 6 passed 0 skipped -> 8 passed 0 skipped; FWER ledger byte-identical (18 rows, md5 a4ae7c13); 130 LOC. The memo's two NOT VERIFIED items are discharged by the verifier's A1 run in master. Follow-up filed as S156 |
| S155 | harness | tests/platformkit/foundry/test_results_db.py is 391 LOC against the 300-LOC rail (359 on master before S74; pre-existing, filed by the S74 verifier). Bar: split into two files by topic (claims/leases vs archive/p-values) with the same 22 tests passing and 0 tests dropped; no module change | S74 verifier NEW GAP | DONE 2026-09-04 ea7dd705f -- ACCEPT WITH CORRECTIONS (codex a10 979ea8926; verified Opus, contract A/B/Q; memo S155_split_test_results_db_2026-09-04.md). 391 LOC -> 209 (kept name, 15 claim/lease/reap tests) + 161 (test_results_db_archive.py, 7 archive/p-value/schema/migration); 22 names identical to master by sorted collect-only diff, all 22 bodies / 7 helpers / 64 asserts AST-identical; results_db.py + results_db_sql.py md5-identical. Corrections applied at landing: memo states `_db` duplicated (no conftest), the tmp_path-only ledger note restored in both docstrings. NEW GAP (housekeeping, no row): a package conftest.py for tests/platformkit/foundry |
| S156 | harness | test_ledger_schema_s13.py test_next_k_family_counts_aliased_rows_s89 (:104) keeps a private `if not path.exists(): return` absent-evidence escape that S154 did not route through _require_real_ledger -- same B3 shape as S153/S154, still silent; test_calibration_report.py:176 and test_tick_informative.py:66 carry the same existence-only skip on rebuildable caches (S153 memo follow-up). Bar: every absent-evidence escape in these three files routes through the worktree marker (skip in a worktree, fail in the main repo); counts before/after per file; no module change | S154 verifier + builder NEW GAP | DONE 2026-09-04 827477cc9 -- ACCEPT (codex a16 d7d935748; verified Opus, contract A/B/Q; memo S156_absent_evidence_escapes_2026-09-04.md). 3/3 escapes routed: test_ledger_schema_s13.py:106 -> _require_real_ledger, test_calibration_report.py:186 -> _require_default_report_evidence, test_tick_informative.py:77 -> _require_requote_artifacts (skip under the worktree marker, pytest.fail naming the path otherwise); main repo 8/9/17 -> 9/10/18 passed with 0 new skips; worktree skips only, 0 failures; FWER ledger 18 rows md5 a4ae7c13 unchanged; tests only, no module change. Verifier NEW GAP: ~25 same-shape escapes remain outside eval_gate (combo/, ingame/, intel_validation/, vault_feed/, improve/, signals/, data_frontier/, interaction_factory/, pod_sprint/, test_clv_*) -- housekeeping, no row until a lane is free |
| S157 | signals-ingame | L14 RE-KEY HAS NO ROW: event_key is market-type-specific so 0 events carry two markets; a series-prefix strip yields MLB moneyline + total on 99 of 3,792 events (2.61 pct coverage) and soccer_intl all three markets on 96 of 96 games (measured S90 2026-09-04). The market-consistent sim's premise needs one game-level key carrying every in-play market. Bar: a game-level key column added additively to the joined in-play stores (old event_key kept), coverage per sport reported with exact denominators, and a CONSTRUCT test that a re-keyed game carries all its market types; no model, no charge | S90 memo; INGAME_GAP_PREMISES L14 | DONE 2026-09-04 cf1c57602 -- ACCEPT WITH CORRECTIONS (codex a16 a5c7a649b; verified Opus, contract A/B/Q; memo S157_game_key_rekey_2026-09-04.md). Premise reproduced by the verifier on both landed stores (mlb 13.5M rows / 3,932 event_keys, soccer_intl 2.26M / 288; 0 carrying >= 2 market types). One uniform rule event_key.split('-', 1)[1] applied to every row: mlb 99 / 3,792 games (58 with 2 market types, 41 with 3), soccer_intl 96 / 96 with 3, rule_nomatch 0. add_game_key() additive; event_key, default schema and default write path unchanged; game_key only under opt-in --game-keyed to *_price_series_gamekeyed.parquet; n = 5 CONSTRUCT; per-file 6 -> 7 passed, 13 sibling files 100 passed; FWER ledger 18 rows a4ae7c13 unchanged. Corrections at landing: the vacuous self-comparison assert now compares against a copy; memo split + census command added; the lane's pre-written verdict in this row was dropped. NEW GAP (housekeeping): s99_corpus.py:56 duplicates the strip rule -- one shared helper owed |
| S158 | harness | CLUSTERED n_eff EXCEEDS ITS OWN CLUSTER COUNT: the S88 pooled readout quotes n_eff = 179.8 from ingame/gap_effective_n.effective_sample_size over 127 games; an effective sample size above the number of clusters is not interpretable, and every in-game readout that quotes n_eff (S58 batch 2, S80, S87, S116, S137 re-baselines) used the same function. Bar: audit the function (ICC sign/clamp, the design-effect formula, what happens when ICC <= 0), a CONSTRUCT test that n_eff <= n_clusters whenever ICC >= 0 and n_eff <= n_ticks always, and a re-quote table of every published n_eff with before/after (verdicts expected unchanged; label honestly if not); no threshold moves | S88 verifier NEW GAP | PREMISE REFRAMED 2026-09-04 (Sonnet spec-writer, reproduced on master): gap_effective_n.py uses the standard design effect deff = 1 + rho*(m_bar - 1), m_bar = n_ticks/n_games, n_eff = n_ticks/deff (:53-59, :77; rho clipped to [0,1] at :50), so n_eff - n_games = (n_ticks - n_games)*(1 - rho)/deff >= 0 for EVERY rho -- n_eff >= n_clusters is guaranteed and equals it only at rho = 1. The S88 179.8 reproduces exactly (11,087 informative / 127 games / rho 0.7031 / deff 61.68) and every other published readout behaves the same (23 numeric n_eff readouts enumerated across 9 evidence files, e.g. S58 T2 2444.2/3 div clusters, S87 831.5/227, S116 103.1/63, S137 up to 3239.8/673). The row's implied bar (n_eff <= n_clusters) was mathematically unreachable; the correct invariant is n_clusters <= n_eff <= n_ticks. Remaining bar (small lane, spec S158_spec.md): ONE CONSTRUCT test asserting that invariant plus an additive n_eff_bound_ok diagnostic; no formula change, no re-quote needed, verdicts untouched -- DONE 2026-09-04 76bf53062 -- ACCEPT WITH CORRECTIONS (codex a10 2f6e41b62; verified Opus, contract A/B/Q; memo S158_neff_audit_2026-09-04.md): construct test sweeps rho {0, 0.3, 0.7, 1.0} on clusters 2/3/5/8 and asserts n_games <= n_eff <= n_ticks + n_eff_bound_ok; S88 pooled reproduced exactly (11,087 / 127 / rho 0.703096 / deff 61.6767 / n_eff 179.760038, diff 0.0); additive fields only, 21 readers key-index unchanged, MIN_N_EFF untouched; 16 of 61 enumerated readouts re-quoted byte-identical, 45 NOT VERIFIED because their per-unit archives are absent -> S161; memo arithmetic (60/44 -> 61/45) fixed at landing; tests 33 passed 3 skipped over 7 files |
| S159 | docs | PUBLIC REPO NEVER SWEPT AS A WHOLE (user 2026-09-04: every detail perfect, backup + public as new as possible): no measurement exists of broken links from the entry points, register/ledger citations whose memo or artifact is missing, tracked-path hygiene (data/ vault/ .planning/ docs/research docs/strategy, secrets patterns), retracted figures outside retraction framing, INDEX gaps, README-vs-packet number drift. Bar: the six counts measured on HEAD, then 0 broken links / 0 hygiene hits / 0 unframed retracted figures / 0 INDEX gaps, missing artifacts LISTED not fabricated, packet numbers byte-identical; one link-walk test | user directive 2026-09-04 | CLOSED AT LIMIT 2026-09-04 4877bf958 (codex a14 on gpt-5.6-sol ddc5d459d; verified Opus, contract A/B/Q; memo S159_public_repo_sweep_2026-09-04.md). Measured and fixed: 8 entry points 311 links / 1 broken -> 322 / 0; docs/INDEX.md 65/77 listed -> 77/77 (12 gaps closed); README intelligence-corpus count reconciled to the packet's 151 (packet byte-identical, 0 numbers invented); link-walk test 1 passed skip-free. At the limit of a docs lane: (c) 61 tracked-prefix hits are intentional public files (data/README.md, data/__init__.py, jersey_name_map.json, models/*_metrics.json; 0 real secrets) -- the bar's '0 hits under data/' contradicted its own must-not-move and is restated as 0 secrets + 0 private-corpus paths; (d) 1 unframed retracted figure remains in the HUMAN-GATED api/courtvision_router.py:1384 -> S169 (user decision); (b) 343 cited-but-untracked artifact occurrences (165 unique) are gitignored-by-design locals, relabelled cited-but-not-public and listed, none fabricated |
| S160 | harness | FUNNEL CONNECTIVITY NEVER MEASURED END TO END ON ONE DATE (user 2026-09-04: make sure everything is connected): 11 links enumerated (eval-gate core, calibration report, cross-sport benchmark, signal audit, foundry seed + runner one pass, results_db -> family_bars chain, predict_service produce dry, mcp harness/system health, PUBLIC_EVIDENCE artifact paths, in-game flag_ticks -> n_eff -> CI, paper CLV readout). Bar: 11/11 measured with command, exit, number and verdict (pass / fail / NOT TESTABLE HERE with reason), 0 fabricated; no code change unless a trivial import/path break; table docs/evidence/CONNECTIVITY_2026-09-04.md + one test | user directive 2026-09-04 | DONE 2026-09-04 fdc38f725 -- ACCEPT WITH CORRECTIONS (codex a15 d2ade0761; verified Opus, contract A/B/Q; table docs/evidence/CONNECTIVITY_2026-09-04.md). 11/11 links measured with command/exit/number/verdict: L1 eval-gate 47/47 green, L3 cross-sport 4 sports, L5 foundry one pass screens=50 tracebacks=0, L6 results_db->family_bars p_values=1, L7 produce_sport(mlb) ok/31 predictions, L9 PUBLIC_EVIDENCE 14/14 unique artifact paths, L10 in-game chain n=557 informative=128, L11 paper CLV readout no_data/INSUFFICIENT (honest day-0); L2 NOT TESTABLE without a data/ write (write=True hard-coded); L4 FAIL = worktree artifact (gitignored bet_grades.py exists in master); L8 FAIL = wrong module path (artifact_tools.harness_health reachable, status=no_data). 0 code changes, 0 fabricated numbers; verifier reproduced L1/L7/L9. Follow-ups -> S167 |
| S161 | harness | PUBLISHED n_eff READOUTS ARE NOT RE-QUOTABLE (S158 verifier): 45 of 61 enumerated n_eff values across S58 batch 2, S87, S87b, S116, S152 and S137 cite per-unit series under data/cache/eval_gate/ that were never copied under docs/evidence, so no artifact on the tracked tree can reproduce them (Q9 archive durability, the S54 shape on the local side). Bar: enumerate the 45 with their source paths; for each whose local series still exists copy the per-unit table (or its sha256 + row count + the re-quoted n_eff) under docs/evidence/harness/neff_requote_2026-09-04/, re-quote n_eff from it byte-identical or honestly re-labelled; list the ones whose series are gone as LOST with the date they were last seen; no verdict moves | S158 memo NOT VERIFIED list | DONE 2026-09-04 78bfaa588 -- ATTEMPT 2 ACCEPT WITH CORRECTIONS (codex a10 22cd8950f + 6280c833f; verified Opus, contract A/B/Q; memo S161_neff_requote_2026-09-04.md). Attempt 1 was REJECTED: 43 LOST labels were false because worktree data/ never links data/cache/eval_gate; the orchestrator copied the 11 cited series into a10 (never the ledger) and attempt 2 re-ran: 45/45 accounted = 22 RE-QUOTED + 23 RE-LABELLED + 0 LOST; 6 rows across 5 sources recomputed by the verifier at 0.0 delta (e.g. S58_T2_pooled 2444.1628, S152_S116_nba 2370.0394, S87_S80_after 62.5571); the 3 S87b_calibration_* rows re-quoted algebraically (483.03 / 434.66 reproduced from the real series); 5 copied tables sha256-identical to source; archive 38 MB under docs/evidence/harness/neff_requote_2026-09-04/. Corrections at landing: memo states the 23 re-labelled series exist in the MAIN repo (worktree-scoped wording retired), s06_stacker_series added to source_inventory, the test no longer forbids LOST rows. Follow-up (noted, no row yet): re-quote the 23 directly from their per-unit series in the main repo -- LANDING FIX c6e72b030: the archived tables had been line-ending-converted by git on commit, so their sha256 no longer matched the sources on master (test failed after landing); the directory is now marked -text in .gitattributes and every table re-copied byte-for-byte; test passes in master |
| S162 | ops | factory_source_manifest.py:76 passes an ABSOLUTE pattern to Path.glob (NotImplementedError: non-relative patterns), so tests/platformkit/ops/test_factory_source_manifest.py fails 2/5 in every codex worktree while passing in the main repo -- no worktree lane can run the factory-source probe (found by the S76 verifier as an unattributable failure). Bar: the glob keyed on a repo-root Path (relative pattern), 5/5 in a worktree and in master, module byte-identical otherwise | S76 verifier NEW GAP | DONE 2026-09-04 a770f62de -- ACCEPT (codex a12 9fb7e37ff; verified Opus, contract A/B/Q; memo S162_manifest_worktree_glob_2026-09-04.md). Glob keyed on the repo root with a relative pattern (+ _rel via absolute() so a junctioned data/ does not resolve into the main repo): test_factory_source_manifest 3/5 -> 5/5 in the worktree, 5/5 in master; --pod-path-only byte-identical (63 lines = 2 summary + 61 sources); no test rewritten; the only reader (pod_bootstrap_check) unchanged. At landing one statement was folded to keep the module at 299 LOC. Verifier NEW GAP (noted, no row yet): tests/platformkit/ops/test_private_helpers_tracked.py asserts a non-empty untracked-helper precondition and fails 1/1 in any clean worktree |
| S163 | harness | THE 300-LOC RAIL IS ENFORCED ON 3 NAMED MODULES ONLY (tests/platformkit/test_s140_loc_rail.py): family_combo_screen.py reached 301 unguarded in S76 attempt 2 and was caught by the verifier, not by a test. Bar: the rail test enumerates every scripts/platformkit/**/*.py (spec DATA modules exempt by an explicit allowlist with their current LOC), reports the count over the cap today (before), and fails on any file over 300 not on the allowlist; no module edited by the lane | S76 verifier NEW GAP | DONE 2026-09-04 f670fbf70 -- ATTEMPT 2 ACCEPT WITH CORRECTIONS (codex a13; verified Opus, contract A/B/Q; memo S163_loc_rail_scope_2026-09-04.md). Attempt 1 rejected (55 s runtime from heavy imports; allowlist frozen at a stale base). Attempt 2: the LOC walk lives in the NEW import-free tests/platformkit/test_loc_rail_scope.py (the original test_s140_loc_rail.py restored byte-identical); N = 2,203 modules on disk (2,200 tracked), 137 over 300 LOC, all 137 allowlisted at their exact current-master counts with zero headroom (0 uncovered, 0 mismatches), no frozen file total. At the limit: cold pytest wall 6.0-6.2 s vs the 5 s bar (walk itself 0.75-4.3 s; page cache, not imports) -- recorded, not hidden. The 300 cap is hereby ADVISORY for the 137 legacy modules; burn-down = later housekeeping rows |
| S164 | harness | THE EVENT-KEY STRIP RULE EXISTS TWICE (S157 verifier): build_price_series.add_game_key (S157) and eval_gate/s99_corpus.py:56 implement the same series-prefix strip independently and can drift. Bar: one shared helper called from both, derived-column sha identical before/after per store, 2 -> 1 implementations | S157 verifier NEW GAP | DONE 2026-09-04 edc79d229 -- ACCEPT (codex a16; verified Opus, contract A/B/Q; memo S164_shared_strip_helper_2026-09-04.md). venue_history/game_key.py (9 LOC) exports game_key_from_event_key; build_price_series.add_game_key and eval_gate/s99_corpus.rekey import it (named implementations 2 -> 1); the verifier re-derived the mlb column over all 13,473,591 rows: before add_game_key = before s99 = after helper (sha bb61c1ef, 0 nulls); soccer_intl 2,261,903 rows memo-only; tests 1 / 7 / 8+1 skip; event_key, schema, readers, thresholds unchanged; ledger untouched. Verifier NEW GAPs -> S168 (a third pre-existing copy at ingame/s90_microstructure_screen.py:69; home package for the helper) |
| S165 | harness | tests/platformkit/foundry HAS NO conftest.py; the S155 split duplicated the `_db` helper in two files (S155 verifier). Bar: one conftest fixture, the two S155 files use it, 15 + 7 still pass with names identical by collect-only diff, every other file in the directory still passes one at a time, 0 module edits | S155 verifier NEW GAP | DONE 2026-09-04 32353874e -- ACCEPT (codex a17 95a97cccd; verified Opus, contract A/B/Q; memo S165_foundry_conftest_2026-09-04.md). tests/platformkit/foundry/conftest.py (12 LOC) provides the results_db fixture; _db copies 2 -> 0; 15 + 7 passed with sorted collect-only names identical to master; all 20 files in the directory run one at a time match the memo (test_ingame_grammar_nba_pairs fails in worktrees only: the S86 CSV is gitignored; 3 passed in master); 0 edits under scripts/platformkit/foundry. Corrections at landing: an unused ResultsDB import removed; the lane's edits to this register and the ledger dropped. Verifier NEW GAP (noted): test_ingame_grammar_nba_pairs.py hard-depends on a gitignored CSV -- route through the worktree marker (same shape as S156) |
| S166 | docs | FOUR SMALL VERIFIER FINDINGS OPEN FROM 2026-09-04 LANDINGS: (a) S36_repro_2026-09-04.py still asserts the pre-correction 'tick_date raises' claim; (b) the S36 row quotes shipped Briers 0.207033 / 0.252261 that master's default mode does not return today (0.206629 / 0.251859); (c) the S90 memo's reproduction command writes under data/cache/eval_gate by default while calling itself read-only; (d) ingame_baseline_lock.py:133 carries a second unguarded shape of the effective_sample_size dict. Bar: 4/4 fixed with before/after quotes, S36 repro reproduces the Briers + 52.86 pct to a scratch path, one construct test for (d); the register row itself is corrected by the orchestrator | S36/S90/S158 verifier NEW GAPs | DONE 2026-09-04 4877bf958 -- ACCEPT (codex a18 db708b4cd; verified Opus, contract A/B/Q; memo S166_verifier_housekeeping_2026-09-04.md): 4/4 -- S36 repro asserts self_leak_pct 52.86 / 43.49 instead of a raise; S36 memo carries master's default-mode values 0.206629 / 0.251859 beside the register's 0.207033 / 0.252261 (the register numbers came from the S06 preflight slice); S90 reproduction command passes --out-dir; ingame_baseline_lock fallback carries n_eff_bound_ok; verifier repro e4 0.206785778212713 / e2 0.254350980569173, 0 writes under data/; tests 1 + 3. Verifier NEW GAPs (noted): effective_sample_size has no empty-input contract (raises), the fallback is a 3-key subset of the 6-key return; land-by-archive-only rule reaffirmed (a18 was 28 files behind master) |
| S167 | harness | S160 FOLLOW-UPS (verifier): (a) re-measure L4 signal audit in the MAIN repo where the gitignored src/prediction/bet_grades.py exists, labelling absent-because-gitignored separately from broken; (b) locate system_health (not in scripts/platformkit/mcp_server/artifact_tools.py) and re-measure L8 with the right module path; (c) give the L9 PUBLIC_EVIDENCE path check a unique-path denominator (14) and an explicit scope incl. backticked citations; (d) the calibration scoreboard CLI has no read-only path (__main__ hard-codes write=True) -- add a --no-write flag (additive) so L2 is measurable, byte-identical output otherwise. Bar: the four rows re-quoted in the CONNECTIVITY table with pass/fail and exact denominators; one test for the flag | S160 verifier NEW GAPs | DONE 2026-09-04 254cb5520 -- ACCEPT WITH CORRECTIONS (codex a15 dc745e447 + 28d064cc1; verified Opus then codex-sol: sol REJECTED on one B2 item -- calibration_scoreboard.main moved to the new CLI module without an alias -- and supplied the 4-line fix, applied at landing; memos S167_connectivity_followups + S167_VERIFY_2026-09-03.md). 4/4 rows re-quoted in a dated CONNECTIVITY section: L2 measured read-only via --no-write (NBA n=4,846 ECE 0.02614 -> 0.01755, MLB 8,395 0.01732 -> 0.01379, TENNIS 9,006 0.04951 -> 0.01856, SOCCER 25,834 0.03294 -> 0.03128; data/ unchanged), L8 PASS (harness_health no_data, system_health ok), L9 31/34 in the worktree = 34/34 in the main repo (three gitignored paths), L4 FAIL exit 1 with bet_grades.py restored (V1 calibration failure NOT independently reproduced -- NOT VERIFIED). calibration_scoreboard.py 281 LOC + calibration_scoreboard_cli.py 34; tests 1 + 1 + 35. NEW GAP (noted): CLI stdout is not ASCII-safe (method text passes through) |
| S168 | harness | A THIRD COPY OF THE EVENT-KEY STRIP RULE SURVIVES (S164 verifier): scripts/platformkit/ingame/s90_microstructure_screen.py:69 predates S164; and the shared helper lives under venue_history/ while its consumers are eval_gate/ and ingame/. Bar: s90 imports game_key_from_event_key with byte-identical derived values and its per-file test passing (3 -> 1 implementations), and the helper's home package decided and recorded in the module docstring (keep or move with a re-export alias, every importer named) | S164 verifier NEW GAP | DONE 2026-09-04 3319c16cf -- ACCEPT (codex a16; verified Opus, contract A/B/Q; memo S168_third_strip_copy_2026-09-04.md). s90_microstructure_screen imports game_key_from_event_key (implementations besides the helper 1 -> 0, whole-tree grep); verifier re-derived the mlb column in 500k batches: 13,473,591 rows, helper digest == old s90 expression digest; helper docstring names its three importers; home = venue_history/game_key.py (kept, no alias needed); tests 5 + 1. Verifier NEW GAP (noted): eval_gate/s100_microstructure.py:98 carries a differently-shaped last-segment event parse -- census owed |
| S169 | docs | UNFRAMED RETRACTED FIGURE IN A HUMAN-GATED TREE (S159 verifier): api/courtvision_router.py:1384 docstring calls +18.38% a live walk-forward result with no retraction framing; api/** is human-gated so no lane may edit it. Bar: Neel applies the one-line docstring fix (PROPOSED diff in the S159 memo) and a hygiene_lint rule covering .py docstrings lands additively; until then the figure is reported here as a known unframed occurrence | S159 verifier NEW GAP | OPEN (new 2026-09-04) -- USER DECISION (human-gated api/) |
| S170 | harness | TWO VERIFIER FOLLOW-UPS (S162, S166): (a) tests/platformkit/ops/test_private_helpers_tracked.py asserts a non-empty untracked-helper precondition and fails 1/1 in any clean worktree while passing in the main repo; (b) gap_effective_n.effective_sample_size raises on empty input and has no empty-input contract, while ingame_baseline_lock.py:133 returns a 3-key subset of its 6-key return. Bar: (a) worktree -> skip via the worktree marker, main repo -> pass or fail naming the path; (b) one 6-key empty-input shape behind an additive empty_ok keyword (default still raises), baseline_lock uses it, existing tests pass one file at a time | S162 + S166 verifier NEW GAPs | DONE 2026-09-04 eef6295cb -- ACCEPT (codex a12; verified Opus, contract A/B/Q; memo S170_worktree_test_and_ess_contract_2026-09-04.md). (a) test_private_helpers_tracked: worktree -> skip via the marker, main repo -> pass (2/2 modes, swapped into master by the verifier and restored); (b) empty-input ESS shapes 2 -> 1: a six-key dict {n_ticks 0, n_games 0, rho 0.0, design_effect 1.0, n_eff 0.0, n_eff_bound_ok True} behind keyword-only empty_ok=False, the default still raises ValueError, all 18 other call sites untouched, every numeric constant byte-identical; 6 test files pass one at a time. Verifier NEW GAP (noted): the main-repo fail branch of the marker-gated tests is source-only -- one shared construct test that empties the helper list would cover all three |
| S171 | signals | S73 PREMISE CHECK -- can period-grain corpora be built at all: mlb_inning (period/total) and nba_quarter_shape (period/spread) need per-inning / per-quarter state joined to a period market line; nobody has counted the games that carry both on local disk. Bar: a decision table per family (state games, period-line games, both, date range, columns the frozen family expects vs supplied) with exact denominators and paths, verdict BUILDABLE (n >= 30) / NOT BUILDABLE today / DECISION drop from the frozen spec; memo only, no code, no build, no store loaded whole | S73 | DONE 2026-09-04 2ebfbaa52 -- ACCEPT WITH CORRECTIONS, PREMISE FALSIFIED (codex a18; verified Opus, contract A/B/Q; memo S171_period_grain_premise_2026-09-04.md). mlb_inning: state 178/227 grade-log games (2026-06-28..07-12), period total lines 0/99, both 0/178; nba_quarter_shape: state 1,593/1,593 checkpoint games (2024-10-22..2026-06-13), period spread lines 0/9, both 0/1,593; zero tickers matching inning|period|quarter across 21.87M price rows, so NOT BUILDABLE today against the unmoved n >= 30 rail; frozen definitions quoted (6 + 15 members, all present in their sources). Correction at landing: the checkpoints column list omitted `traded` |
| S172 | harness | ABSENT-EVIDENCE ESCAPES OUTSIDE eval_gate (S156 + S165 verifiers): ~25 same-shape skips/returns on gitignored data/ or docs/evidence paths under combo/, ingame/, intel_validation/, vault_feed/, improve/, signals/, data_frontier/, interaction_factory/, pod_sprint/, test_clv_*, plus tests/platformkit/foundry/test_ingame_grammar_nba_pairs.py. Bar: N enumerated by grep, N/N routed through worktree_marker (skip in a worktree, fail naming the path in the main repo), 0 module edits, counts per file unchanged with evidence present (main-repo commands listed for the verifier) | S156/S165 verifier NEW GAPs | CLOSED AT LIMIT 2026-09-04 968f21327 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S172_VERIFY): measured 61/62 file:line escapes routed (98.39%; bar 62/62). REJECT B1/B3/Q7/Q8: omitted test_contradiction.py:119-122 made premise/enumeration false and left absent evidence skippable. CORRECTION: add worktree_marker/_require_store there; change N 61->62, add row/command, rerun. NEW GAP: 4 named candidate test files exceed 300 LOC. NEW GAP: isolate 2 unrelated fixture stabilizations. |
| S173 | harness | S161 FOLLOW-UP -- 23 n_eff rows were RE-LABELLED from summary JSON because their per-unit series were absent from the worktree; the series exist in the main repo and have now been copied into a13. Bar: every present series re-quoted DIRECTLY with effective_sample_size under the published tick rule, rows converted to RE-QUOTED (count reached stated), byte-identical or honest delta, files sha256-listed in source_inventory, 0 fabricated, test_s161_neff_requote_manifest still passes | S161 verifier NEW GAP | CLOSED AT LIMIT 2026-09-04 7de326e40 (codex a13 b4b495958 + correction 11df23ace; codex-sol verifier REJECT twice: first on B2 -- four prior COPIED statuses had been flipped to ARCHIVE_ONLY, corrected; second on the bar -- 0/23 converted because only 4 of the 23 cited series ever reached the worktree; the eval_gate cache is never junctioned by design). The LIMIT is the worktree, not the data: every series exists in the main repo, so the direct re-quote moves to a main-repo lane -> S196. Memo + both VERIFY memos landed |
| S174 | signals | FROZEN FAMILY SPEC v2 -- DROP THE TWO UNBUILDABLE FAMILIES (S73 decision after S171): mlb_inning (period/total, 6 members) and nba_quarter_shape (period/spread, 15 members) can never be screened on their own market locally. Bar: a new spec version (s14-families-v2) in the frozen spec DATA module that keeps every v1 entry byte-identical under its v1 pin and adds a DROPPED status + reason for the two families; load_families(version) defaults to the CURRENT behaviour (v1) and returns v2 only when asked; the runner/tiers read v2 only behind an explicit flag defaulting OFF; a construct test that v1 pin sha is unchanged and v2 drops exactly 2 families / 21 members; the 18-row FWER ledger byte-identical; no charge | S73 + S171 | DONE 2026-09-03 2cc58675e -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S174_VERIFY): PREMISE PASS: v1 37 families/396 members; targets mlb period/total 6 and nba period/spread 15. v1 pin unchanged; v2 drops 2/37 families and 21/396 members, leaving 35 active; default v1 and 5 direct readers unchanged. Tests: 2+22+8+3+12 passed. Corrections: none. NEW GAP: family_bars.py 355 LOC (baseline 324). NEW GAP: absent s86 input had blocked importer test; now present and 3 passed. |
| S175 | harness | SWEEP SURVIVOR (SWEEP_00, 3-lens refutation survived 2026-09-04): screen p writer. Premise: re-measure first. Measured 2026-09-04 on master HEAD fae6adb76: results_db.record. Metric: fraction of T1 result rows whose result.screen_p equals that trial's artifact JSON | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 d6f85a7cb -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S175_VERIFY): premise PASS: 20 cols lacked screen_p; 1,302 rows (T0 958/T1 344), raw_p non-NULL 0; archive.screen_p 344/344. Reproduced before 0/344 and rerun 344/344 at 1e-12 (max diff 0.0; 344 unique ids/hashes/paths); ordered families 120/112/48/32/32, 5/5 exact. Tests: 55 passed incl LOC rail. Corrections: none. NEW GAP: none. |
| S176 | harness | SWEEP SURVIVOR (SWEEP_02, 3-lens refutation survived 2026-09-04): screen failure rows. Premise: census the READ-ONLY shipped DB data/cache/eval_gate/s85_screen_2026-09-03.sqli. Metric: queue rows with no outcome row at any tier / queue rows in one seeded replay | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 570bd44f8 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S176_VERIFY): premise FALSIFIED: exact S85 store absent (0 exact-name stores), no store opened/substituted; required 167/1,125 = 14.8444 pct unavailable; tests 0 files/0 tests (stop precedes absent spec test); corrections none; NEW GAP none |
| S177 | harness | SWEEP SURVIVOR (SWEEP_09, 3-lens refutation survived 2026-09-04): s88 cluster unit. Premise: re-measured on master 2026-09-04, read-only; reproduce all of it before touching code. A2 FIRST -- the published S88 numbers reproduce EXACTLY from docs/evidence/harness/s88_phase_recal_2026-09-04.csv (33,920 eval ticks, 11,087 informative, 127 tickers) using . Metric: the per-phase verdicts + cluster count of the S88 readout, denominator 11,087 informative ticks of docs/evidence/harness/s88_phase_recal_2026-09-04.csv (33,920 eval ticks) | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 e9a2af412 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S177_VERIFY): premise PASS: 77,327 unique joined rows, 227 tickers, 360 real games, 112 multi-game tickers, 21,284 reassigned; 33,920 eval / 11,087 informative, 0 unmatched, 234 real-game clusters. Ticker: pooled NO_CHANGE, late|leading_big IMPROVED 50, mid|trailing WORSE 66; real-game: pooled NO_CHANGE 234, late|leading_big IMPROVED 52, mid|trailing NO_CHANGE 76. Tests 50 passed; corrections none. NEW GAP: JSON source path is absolute; make repository-relative |
| S178 | harness | SWEEP SURVIVOR (SWEEP_10, 3-lens refutation survived 2026-09-04): ticker date cover. Premise: very Kalshi ticker encodes its own ET game date (KXMLBGAME-<yy><MON><dd><hhmm><away><home>, plus a G1/G2 doubleheader suffix on 2 files). Load all 227 files of data/cache/ingame_grade_joined/mlb, drop duplicate (game_id, ts), run assign_real_game_seq on master. Metric: share of scored MLB in-game ticks whose real-game segment does not cover its ticker's own encoded ET date. Denominators: 77,327 unique (game_id, ts) rows of data/cache/ingame_grade_joined/mlb; 47,104 rows of data/cache/e | gap-sweep workflow wf_c2b291e7 | CLOSED AT LIMIT 2026-09-04 92e7dc4b2 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S178_VERIFY): 18776/77327 non-covering ticks (24.2813%); S06 30618/180, -0.111315 CI [-0.153490,-0.069140]; S88 10483/149, -0.005074 CI [-0.013236,0.003019]. REJECT B2: JSON fields removed/renamed sans aliases; requote gained required positional arg. CORRECTION: keep legacy aliases/reader; restore positional-compatible requote. NEW GAP: module 338 LOC. NEW GAP: relative --prereg rejected. |
| S179 | harness | SWEEP SURVIVOR (SWEEP_11, 3-lens refutation survived 2026-09-04): fold key purge. Premise: re-measure both, read-only, before touching code. On data/cache/eval_gate/s06_stacker_series_2026-09-03.csv (47,104 rows / 158 tickers / 0 null y) reproduce stacker._first_dates (stacker.py:53-57, str(timestamp)[:10] min per game) and outer_walk_forward's rule. Metric: train-row-slots whose own tick timestamp is >= their fold's first test tick | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 38c8bd5a3 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S179_VERIFY): premise FALSIFIED: required S06 source absent; S88 archive 4,311,731 bytes, 33,920 rows, 127 games. Tests: 0 files/0 tests (premise stop; no module touched). Corrections: none. NEW GAP: memo command exits 1 without printing NOT_TRACKED; later docs-only cleanup can add fallback label. |
| S180 | signals | SWEEP SURVIVOR (SWEEP_12, 3-lens refutation survived 2026-09-04): mlb gate current era. Premise: re-read data/cache/combo/gate_corpus_mlb.parquet and recompute df.groupby('corpus_unit')[c].notna().sum(). Measured 2026-09-03 on master HEAD: 39,162 rows, 9 columns, era_2010_2021 27,983 / era_2022_2026 11,179; sp_first6_diff_ew 24,521 / 0; sp_ra_diff_asof 26. Metric: non-null count per corpus_unit of each mlb gate-corpus feature column in data/cache/combo/gate_corpus_mlb.parquet; denominators era_2010_2021 = 27,983, era_2022_2026 = 11,179, total 39,162 | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 7f833413f -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S180_VERIFY): PREMISE PASS: 39,162x9; era rows 27,983/11,179. Current-era coverage: park_factor 10,517/11,179; sp_ra_diff_asof 9,765/11,179; sp_first6_diff_ew 0/11,179 CLOSED AT LIMIT. Control: 39,162 unique ids; legacy 27,983 rows identical. Tests: 63 passed across 7 per-file commands. Corrections: none. NEW GAP: none. |
| S181 | signals | SWEEP SURVIVOR (SWEEP_13, 3-lens refutation survived 2026-09-04): corpus denominator. Premise: re-run coverage_report('soccer') (scripts/platformkit/eval_gate/close_join.py:262). MEASURED 2026-09-04 in master: denominator 16322, joined 16322, unjoined 0,. Metric: coverage_report('soccer') corpus-side coverage; denominator = ALL 25,834 rows of data/cache/combo/gate_corpus_soccer.parquet | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 846b391d5 -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S181_VERIFY): premise reproduced from unchanged parent: prior report was odds-side 16,322/16,322/0, not corpus-side; full soccer corpus 25,834 rows, 16,322 joined, 9,512 unjoined, rate 0.6318030502; six unit rates reproduced (D1/E0/E1/I1/SP1 0.636364, F1 0.605809); tests 21 passed (4 files); correction applied: historic ref 282b7a5a1^; NEW GAP: restore nonfunctional comments/docstrings compacted in close_join.py:2-205. |
| S182 | signals | SWEEP SURVIVOR (SWEEP_14, 3-lens refutation survived 2026-09-04): nba spine census. Premise: ; re-measured 2026-09-04, reconfirm before any work -- if falsified STOP, write the memo, commit, report FALSIFIED. Metric: rows in gate_corpus_nba_full.parquet and its per-season coverage; denominator 4,846 games.parquet rows with home_win non-null (1,230/1,230/1,230/1,156) | gap-sweep workflow wf_c2b291e7 | CLOSED AT LIMIT 2026-09-04 fbd10efe0 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S182_VERIFY): full spine 4846/4846 (100%; seasons 1230/1230/1230/1156), legacy 1814/4846 (37.4329%), holes 3032/4846; 44/44 rows. REJECT: test failed on missing FWER; B2 removed built_at. CORRECTION: restore/reuse built_at only with unchanged sources; summary CLOSED AT LIMIT->OPEN. NEW GAP: hash unchecked when sources exist. NEW GAP: test writes defaults. NEW GAP: candidate owned verifier memo. |
| S183 | signals | SWEEP SURVIVOR (SWEEP_15, 3-lens refutation survived 2026-09-04): gate corpus coverage census. Premise: re-measure today. Over the four live gate corpora data/cache/combo/gate_corpus_{nba,mlb,soccer,tennis}.parquet, excluding the four spine columns event_id/corpus_unit/event_date/y, there are 52 feature columns (nba 11, mlb 5, soccer 29, tennis 7) spanning 220 (. Metric: feature columns carrying a per-corpus_unit non-null count and rate; denominator 52 columns / 220 (column, corpus_unit) cells over the four live corpora | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 557416574 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S183_VERIFY): premise reproduced: 52 feature columns, 220 (column, corpus_unit) cells, 41/52 globally incomplete, 1 globally zero, and the same 6 zero cells. Census reports 52/52 columns and 220/220 cells; exactly 6 named zero cells. 69 tests passed across 8 files; corrections: none. NEW GAP: evidence JSON is an exact grouped extract, not the 4 complete freshness_report dictionaries requested outside the acceptance rule. |
| S184 | ops | SWEEP SURVIVOR (SWEEP_16, 3-lens refutation survived 2026-09-04): mlb slate date. Premise: , re-measure before anything else): offline 24 h sweep over 2026-09-02 hours 00..23, fake live_games_fn, no network -- count hours where capture_once's resolved date differs from (now - 10 h). Re-measured 2026-09-04 = 10 of 24 (hours 00Z-09Z). If it is not 10,. Metric: UTC hours (0..23) at which capture_once's resolved slate date differs from gumbo_mlb_poller's own baseball-date default; offline sweep, fake live_games_fn, no network | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 3b837296a -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S184_VERIFY): premise PASS, fake-caller before mismatch 10/24 UTC hours (00Z-09Z); after 0/24 across 24 unique hours. Context 405 files, 79,566 parseable rows; 41,202/79,566 (51.7834%) in 00Z-09Z; 321/401 any and 118/401 only; 23Z 15,595. Tests: 22 passed (4 files). Corrections: none. NEW GAP: date provenance labels say 2026-09-04 while candidate and verification are 2026-09-03; reconcile labels without changing the construct. |
| S185 | ops | SWEEP SURVIVOR (SWEEP_17, 3-lens refutation survived 2026-09-04): pod pull sync. Premise: , re-measure and print every number before any edi. Metric:  | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-04 a56c793f2 -- ACCEPT (codex a17 3c076e196; verified codex-sol twice, contract A/B/Q; memo S185_VERIFY): CANDIDATE: 3c076e1966fd75bbfc779e740284416c95486544; three-path diff inspected in full. A1 FAIL: in MASTER, `python -m pytest tests/platformkit/test_pod_pull_sync.py -q` -> exit 4, 0 tests; candidate-only file is absent there (tests/platformkit/test_pod_pull_sync.py:1). A2 PASS: claimed/measured parent = 2/7 checked and 0/4 named stores; candidate = 8/8 and 1/4, with required absent store 1/1 (S185_spec.md:17-23). A3 PASS / A4 PASS: Q7 reproduction replaces rendering; 8/8 unique targets were exhaustively enumerated (pod_pull_sync_durability_2026-09-04.md:21-33). A5 PASS: repository test/refere |
| S186 | ops | SWEEP SURVIVOR (SWEEP_19, 3-lens refutation survived 2026-09-04): cycle history. Premise: re-measure both halves. (a) Over every file under data/cache/ingame_shadow_history, nothing excluded: 34 files (mlb 9, tennis 13, wnba 12), 24,612 rows, and 0 of 24,612 carry a bridge/error reason (no_live_state, bridge_date_mismatch, no_home_leg, error:*). Pr. Metric: share of driven cycles whose failure reasons and cycle counters are readable from disk AFTER the next cycle has run, over cycles driven | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 331837c03 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S186_VERIFY): premise PASS: all 34 files, 24,612/24,612 unique rows, 0 malformed or bridge/error reasons; heartbeat 866 B/18 keys, n_live=0,n_pairs=0. Headline 0/30->30/30 cycles retained 8 fields; cycle 1 readable after 30. Tests after extraction 1/61/16/2/1/4 pass; one absent per memo. Corrections none. NEW GAPS: verifier bridge test 15p/1f (n_bets 0 vs 1); bridge test omitted heartbeat_path and changed defaults; capture loop 1,238 LOC vs 1,234 allowance; backtest_fwer artifact absent. |
| S187 | ops | SWEEP SURVIVOR (SWEEP_20, 3-lens refutation survived 2026-09-04): deploy pathspec. Premise: re-measure today. Closure = transitive first-party imports of the paper profile's 14 py roots (`load_profile('paper').specs()`, kind=="py"); walk ast.Import, ImportFrom(level 0), and each `from X import Y` as a submodule candidate; first-party = resolves to a . Metric: module-bearing top-level first-party trees named by the deploy pathspec, out of the 11 in the paper-profile closure | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 6a664db0248194d026617a39f444fb9009905253 -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S187_VERIFY): premise PASS: 14 roots; with-probe 524 modules/11 trees, without-probe 406/11; identical tree sets. Closure 11/11; bootstrap 4/11; corrected S21b/S21c 3/11 (scripts, supervisor, predict_service); 7 omissions hold 56/524 modules (10.69%). Test test_deploy_pathspec.py: 1 passed, 5 warnings. Corrections: S21c :47 and three-tree scope. NEW GAP: A1 master rerun unavailable before landing; reran in track-a14. |
| S188 | ops | SWEEP SURVIVOR (SWEEP_21, 3-lens refutation survived 2026-09-04): preflight tree gate. Premise: re-measure first, read-only. Measured 2026-09-04 on this box. (a) The boot gate at pod_bootstrap.sh:56-59 is `pod_bootstrap_check --profile paper`, whose only import set is the 14 kind=py ProcSpec modules of config/boot/paper.json (profile_modules, pod_bootstr. Metric: number of tree-absent CONSTRUCT cases in which the pod_bootstrap.sh step-3 boot gate (existing check AND the new tree gate, composed) exits NONZERO. Denominator = 8 cases: each of ops, kernel, governance, data_registry,  | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 709ad418c -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S188_VERIFY): PREMISE PASS 2/8 before independently reproduced; exhaustive tree-absence CONSTRUCT 8/8 nonzero after across seven roots plus union; control 1/1; test tests/platformkit/ops/test_deploy_tree_gate.py 2 passed; corrections none. NEW GAP: S188 spec/evidence dated 2026-09-04 while candidate and verifier memo dated 2026-09-03; normalize chronology separately. |
| S189 | ops | SWEEP SURVIVOR (SWEEP_22, 3-lens refutation survived 2026-09-04): preflight probe floors. Premise: reproduce these exact numbers before touching anything. `scripts/platformkit/ops/pod_bootstrap_check.py:68-116` defines 7 functional probes and `run_probe` (:199-212) decides ok on `proc.returncode` alone, so a body that only prints exits 0. Assert census meas. Metric: probes carrying an EXPLICIT existence-shaped floor that raises when the floor is unmet, out of the 7 in `_FUNCTIONAL_PROBES` (:68-116). espn_live_state_mlb's type-only assert does NOT count as a floor; boot_packages' 5 i | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 45fed2287 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S189_VERIFY): premise PASS, live corpus 27,983 rows x 10 columns; baseline assert census 0,0,0,1,1,0,2. Explicit-floor census 3/7 -> 6/7; named fail-open probe was sole exclusion. Reproduction 9/9 premise and 7/7 degraded; boot decisions changed 0. Tests passed: 1, 4, 1 (LOC rail included). Corrections: none. NEW GAP: pod_bootstrap_check.py is 300 lines but allowlisted at 311, permitting 11 above the cap. |
| S190 | ops | SWEEP SURVIVOR (SWEEP_23, 3-lens refutation survived 2026-09-04): deploy eol parity. Premise: `git archive` emits CRLF for part of the deploy set while the worktree holds LF, and the sanctioned FULL-TREE deploy-parity metric normalises that away before hashing, so it cannot see it. RE-MEASURED TODAY (2026-09-04, read-only, HEAD a3704ca00): build `git a. Metric: deploy-set archive members reported as differing, in TWO columns (raw bytes, and CR-normalised), plus the classification of every differing member. Denominator: every member of `git archive <rev>` over scripts/platformki | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 8d5620fd8 -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S190_VERIFY): premise confirmed; 22 content members prevent the isolated EOL measurement. Exhaustive n=4,950 unique: identical=4,926, eol_only=2, content=22, absent=0; raw/normalized differing=24/22; shebang/cr_shebang=11/1. LOC rail: 1 passed; implementation test absent after required limit stop. Corrections: none. NEW GAP: none. |
| S191 | harness | SWEEP SURVIVOR (SWEEP_26, 3-lens refutation survived 2026-09-04): ledger sha trace. Premise: re-measure docs/evidence/RESULTS_LEDGER_SYSTEM.md; data line = regex ^\d{4}-\d{2}-\d{2}\s*\/. Measured 2026-09-04 at master f670fbf70: 384 data lines (391 physical); final pipe-field resolves via `git cat-file --batch-check` to a commit for 276/384 = 71.88 pct. Metric: untraceable data lines (no token in the line resolves to a commit via `git cat-file`, AND the line carries no `uncommitted:` token), over every data line matching ^\d{4}-\d{2}-\d{2}\s*\/ in docs/evidence/RESULTS_LEDGER_S | gap-sweep workflow wf_c2b291e7 | CLOSED AT LIMIT 2026-09-04 fcc0b9958 (codex a14 on gpt-5.6-sol 8831a62a1; codex-sol verifier REJECT on the unmet 0 bar; memo ledger_sha_trace_2026-09-04.md + S191_VERIFY). Measured: 66 of 390 RESULTS_LEDGER_SYSTEM data lines carry no token that resolves to a commit and no uncommitted: label; the utility and evidence landed. The bar (0) needs edits to the orchestrator-only ledger, which no lane may make -> S197 (delegated annotation lane in the main repo) |
| S192 | signals | SWEEP SURVIVOR (SWEEP_30, 3-lens refutation survived 2026-09-04): wnba ingame census. Premise: re-measure and print all of these; if any headline is falsified, STOP, write the memo, commit, report FALSIFIED. Measured read-only 2026-09-04 by the spec-writer, all reproduced: data/cache/inplay_odds/wnba_price_series.parquet = 967,102 rows / 287 events (spr. Metric: count of in-play WNBA moneyline ticks (ts < close_time, denominator 186,736 over the 85 intersect events; full-store denominator 217,408 over 98) that receive a non-null as-of state (period, game_clock_s, score_home, sco | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-04 15a97fa56 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S192_VERIFY): Premise PASS: 18,650/186,736 joined ticks across 85 games (15,000/60 cleared); in-play source 217,408/98. Ages median 15 s, p90 132 s, >300 s 0/18,650. Tests: 1 passed join; 1 passed LOC. Corrections: none. NEW GAP: 43 projected duplicates despite unique source rows. NEW GAP: WNBA parquet lacks market_ticker and venue present in NBA reference. |
| S193 | signals | SWEEP SURVIVOR (SWEEP_31, 3-lens refutation survived 2026-09-04): teacher census. Premise: re-measure and print all of it. data/tracking = 408 dirs + 15 loose files, only 393 match. Metric: as-of prior-game teacher rows written, denominator = the 1,814 rows of gate_corpus_nba.parquet; | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 1c3a08ea3 -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S193_VERIFY): premise FALSIFIED: data=yes, tracking=no, gate corpus=no, protected comparison=no; required stop. Census counts and 158-row output unavailable; no scored denominator formed. Tests: 0 files, 0 tests. Correction applied: appended NOT VERIFIED bullets for absent census/158-row output and unrun calibration scoring/next gate. NEW GAP: none. |
| S194 | signals | SWEEP SURVIVOR (SWEEP_32, 3-lens refutation survived 2026-09-04): mlb close bridge. Premise: re-measure first. Measured 2026-09-04 read-only on a clean tree. data/cache/line_history holds 188 files = 180 *.jsonl + 8 per-sport `_freshness.json`, 1.6 GB, 5,298,086 rows across 8 sports (mlb 2,164,041 / soccer 870,688 / tennis 847,692 / soccer_intl 509,82. Metric: distinct in-window MLB games carrying a pre-commence close (last quote strictly before commence_time) for BOTH moneyline AND total, joined to a settled outcome in games_current (target_home_win plus home_runs and away_ru | gap-sweep workflow wf_c2b291e7 | CLOSED AT LIMIT 2026-09-04 490522427 -- attempt 1 verifier verdict CLOSED AT LIMIT (codex-sol, contract A/B/Q; memo S194_VERIFY): MLB close bridge: 641 raw in-window ids, settled event capacity 337 below the fixed 400 bar; evidence memo + mlb_close_bridge_2026-09-04.{md,json} landed inside the S195 landing commit (shared a12 stack); no code |
| S195 | signals | SWEEP SURVIVOR (SWEEP_33, 3-lens refutation survived 2026-09-04): unpaired delta. Premise: re-measure first. Measured 2026-09-03 read-only on a clean tree: scripts/platformkit/analytics_showcase/out/micro_closing_decay.json wnba buckets are T-24h n=0, T-6h n=26 brier 0.245445, T-1h n=29 brier 0.227515, close n=29 brier 0.228045, with n_games_joined . Metric: published cross-arm Brier delta quote sites whose stated n equals the row count actually differenced. Denominator = 4 enumerated sites: micro_closing_decay.json verdict wnba sentence; same verdict soccer_intl sentence; d | gap-sweep workflow wf_c2b291e7 | DONE 2026-09-03 490522427 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S195_VERIFY): premise PASS: raw WNBA bucket 26/29, joined 30; soccer_intl 7/13, joined 13. Paired reproduction: WNBA n=25, Brier 0.235641/0.234696, delta +0.000945; soccer_intl n=7, Brier 0.135518/0.156807, delta -0.021289. Exhaustive source sentences 8/8. Tests: focused 1 passed; LOC rail 1 passed. Corrections: none. NEW GAP: A1 unavailable on pre-extraction master; protected private gate files absent, so byte identity unverified. |
| S196 | harness | S173 CONTINUED IN THE MAIN REPO: 23 RE-LABELLED n_eff rows re-quoted directly from their per-unit series (all present under the main repo eval_gate cache), one series at a time, statuses converted with byte_identical or the honest delta, files sha256-listed; the lane runs with cwd = main repo, writes only under docs/evidence/harness/neff_requote_2026-09-04/ + its memo, never commits (orchestrator pathspec commit), verifier via the nba-track-main junction. Bar: 23/23 converted or each absent path named; 0 fabricated; no verdict moves; test_s161_neff_requote_manifest passes | S173 limit | DONE 2026-09-04 646cae4c8 -- ATTEMPT 2 ACCEPT WITH CORRECTIONS (codex terra main-repo lane 02c6adf5a + fix pass; verified codex-sol via the nba-track-main junction, contract A/B/Q; memo S196_neff_direct_requotes_main + S196_VERIFY). Attempt 1 rejected on provenance (4 S119/S121 rows read a sibling source; 7 sources recorded file counts as selected counts) -- both corrected. Result: 23/23 RE-LABELLED rows converted to direct RE-QUOTED from their named per-unit series (manifest 45 rows = 22 + 23 direct, 0 LOST), 5/5 values and 3/3 sha256 pairs reproduced by the verifier, manifest test 2/2; memo stale lines replaced at landing. With S161 + S197 this closes the Q9 durability chain for every published n_eff |
| S197 | harness | LEDGER LINES WITHOUT A LANDING SHA (S191): 66 of 390 RESULTS_LEDGER_SYSTEM data lines cannot be traced to a commit. Bar: each flagged line gets an APPENDED ` / landed:<sha>` token that git cat-file resolves (found by S-id in a commit subject or git log -S on the line text) or an honest ` / uncommitted:` label when both lookups fail; 0 other characters change; utility re-run reads 0 untraceable; delegated main-repo lane (codex-sol), orchestrator commits by pathspec | S191 limit | DONE 2026-09-04 0a2dd662a -- ACCEPT (codex-sol main-repo lane; verified codex-sol via the nba-track-main junction, contract A/B/Q; memo S197_ledger_sha_annotate_2026-09-04.md + S197_VERIFY). 66 of 66 flagged RESULTS_LEDGER_SYSTEM lines got an appended landed:<sha> token (0 uncommitted labels; git cat-file resolves every one); the diff is suffix-only (66 lines changed, 0 other characters); the S191 utility now reads 0 untraceable of 390. Verifier NEW GAP (noted): the utility accepts any resolvable sha without checking that the commit touches the row's files -- linkage check owed |
| S198 | nba | RED TEST ON MASTER: scripts/platformkit/ingame/test_inplay_capture_bridge.py::test_bridge_enables_ingame_bet fails on a clean tree (n_bets 0, expected 1; measured 2026-09-04 14:35) after the 2026-09-04 ingame landings (S158/S166/S168/S170/S177/S184); it refused the S186 landing although S186 never touches the bridge. Bar: bisect names the first bad commit + hunk; 0 failing without weakening an assertion (or LIMIT evidence that the test is wrong); rail passes | S186 landing refusal | DONE 2026-09-03 0ae6c290d6329b1c1e01e21b41a765ff05913b49 -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S198_VERIFY): premise PASS: HEAD 14e124e97 and lower 0b6181cf7 each 1/16 failed; candidate 0/16; first bad c1b3f4358; historical samples 4/4, 7/7, 7/7, 11/11 across a 31-commit union; bisect construct 11/11 commits; tests 16+41+1=58 passed; corrections applied: attempt 1 superseded, endpoint count 1/16, 11 SHAs, NOT VERIFIED deployment/live external behavior; NEW GAP: none. |
| S199 | signals-ingame | WNBA IN-GAME STATE CEILING (S192 census, memo line 82: 'No state can be recovered for the 167,280 ticks outside cached play-by-play wallclock spans'): only 18,650 of 186,736 in-play moneyline ticks over the 85 intersect games carry an as-of state (9.99 pct), so 89.6 pct of the largest untouched in-game denominator on disk cannot enter a calibration comparison. Bar: all 186,736 ticks classified into exactly one of PRE_FIRST_ACTION / JOINED / INTERIOR_GAP / POST_LAST_ACTION with 0 unclassified and 0 interpolated states, and a recoverable count (JOINED plus POST_LAST_ACTION with a known final) strictly above 18,650; census only, no Brier, no charge | wnba_ingame_census_2026-09-04.md:82 + _summary.json | DONE 2026-09-03 eb7bc8b8bed88db9bf8f09bc308117472165ba94 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S199_VERIFY): premise FALSIFIED: 707/17,943 joined rows post-final and 790/19,456 in-span FUTURE_ONLY make four classes non-exhaustive; census 186,736/186,736 ticks across 85/85 games, classes PRE 165,191, JOINED 17,943, GAP 723, POST 2,089, FUTURE_ONLY 790; tests: wallclock 1 passed, LOC rail 1 passed; corrections applied: none; NEW GAP: none. |
| S200 | harness | THE REGIME KEY IS FITTED ON THE SCORED ROW (S05 memo line 144, corroborated SF-5/SF-18): regime_calibration.buckets assigns confidence terciles from a whole-corpus ranking, so only the isotonic map is out-of-fold and the key is not. Every published four-sport calibration number rests on it (ECE after: nba 0.024843 on 1,814, mlb 0.008077 on 39,162, soccer 0.009302 on 25,834, tennis 0.008403 on 41,886). Bar: 4/4 re-scored under train-only tercile cut points with the per-sport label-change count printed, the DEFAULT path reproducing the four published after-ECEs at max abs diff exactly 0.0, 0 rows dropped; a worse ECE under the honest key is the valid result | S05_calibration_report_2026-09-03.md:144 | CLOSED AT LIMIT 2026-09-04 0c4468c5b -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S200_VERIFY): date-group train-only keys built; archived after-ECE nba 0.019514 / mlb 0.009282 / soccer 0.009538 / tennis 0.016937 NOT reproduced by the clean rerun (0.022205 / 0.009672 / 0.009192 / 0.016928); causes = mutable cache at s200_regime_key_oof.py:145 and copied reference rows at :209-216; leaked-key published table stays provisional; continuation S212 |
| S201 | signals-ingame | NBA FATIGUE WAS SCREENED IN ONE FORM ONLY (S92 memo line 205): fatigue enters as a raw home-minus-away minutes difference with no interaction (period, remaining time, margin, rest, back-to-back), so only that construction is refuted -- fatigue_min -0.000212, fatigue_share -0.000098, unit_onoff -0.000397, all SCREEN_NULL on 79,554 ticks / 661 game clusters. Bar: the three S92 improvements reproduce from the archived per-tick CSV at max abs diff <= 1e-9, then exactly three conditioned forms (x period, x remaining time, x absolute margin) all reported with improvement, DM p, clustered CI and n_eff; prereg DRAFT only at >= +0.004 with a CI excluding zero | S92_nba_lineup_dynamic_2026-09-03.md:205 | DONE 2026-09-03 4b27ec7f4 -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S201_VERIFY): premise confirmed: permitted S92 archive directory absent, 0/2 required files present; archive metrics and conditioned forms unavailable, so no fit or score and the fixed +0.004 bar was not evaluated or moved. Tests: S201 test absent per memo (0 tests, exit 4); LOC rail 1 passed. Corrections: none. NEW GAP: specified S201 test path absent. |
| S202 | harness | EVERY CLUSTERED CI RESTS ON A ONE-WAY ICC ON A STAND-IN LOSS (red-team memo lines 163 and 165): the ICC table uses (y - p_base)**2 rather than a real paired Brier differential, and the crossed home/away factor was never computed, so NBA n_eff 752 (n 1,814) and MLB 10,580 (n 38,800) are a LOWER bound on the correction. Bar: 4/4 corpora print a two-way bootstrap n_eff (>= 1,000 resamples, seed recorded) beside the one-way one, on both the stand-in loss and the real paired differential, the one-way figures reproducing within 1 pct; a NEW table only, no verdict restated | REDTEAM_SIGNAL_FACTORY_2026-09-03.md:163,:165 | DONE 2026-09-03 791edd270 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S202_VERIFY): premise PASS; one-way n_eff NBA 751.5553, MLB 10802.5823, soccer 9733.4018, tennis 37961.8277; n=1814/39162/25834/41886 unique and complete; two-way stand-in/paired n_eff NBA 399.9880/533.1017, MLB 5704.7837/12052.5891, soccer 7052.5754/8306.7166, tennis 4241.9073/12884.4292; 1000 bootstraps, seed 20260904; tests 1+1 passed; corrections none; NEW GAP: 3 archives repeat the one-way label header; dedupe detail_columns in future |
| S203 | harness | FIVE OF SIX AHEAD WRITERS STILL SKIP THE REPLICATION GATE (S08 memo line 42 NEW GAP + :106): stacker.py:224 emits min_corpora_eff and single_window=True with n_corpora hard-coded to 1 and never applies replication_verdict; mlb_winprob_v6.py, mlb_winprob_v7.py, frontend/slate.py and clv_daily_readout.py are unwired too, so Q5 is a memo convention at 5 of 6 writers and a single-window result can keep an AHEAD label. Bar: 6 of 6 writers downgrade, proved by 12 CONSTRUCT cases (n_corpora=1 -> SINGLE-WINDOW, n_corpora=2 -> AHEAD unchanged), every pre-existing output key byte-identical in name and value, unreachable branches named as such | S08_replication_gate_2026-09-03.md:42,:106 | DONE 2026-09-03 66ccf59a6 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S203_VERIFY): premise FALSIFIED: 0/6 enumerated AHEAD writers wired vs stated 1/6; 2 unrelated helper callers measured. Tests: S203 test absent per premise stop; replication_gate 4 passed/1 pre-existing failed unchanged; LOC rail 1 passed. Corrections: none. NEW GAP: replication_gate test expects absent verdict_of_replicated; hedge artifact 12,542 bytes says BEHIND, K=12, no single_window vs spec K=14/single-window. |
| S204 | signals-pregame | NO SPORT IS SCORED AGAINST THE CLOSE (S05 memo line 148, verbatim: 'No sport is compared to a market close -- no gate corpus carries one'): that is false on disk -- S02 joined the soccer devigged close 16,322/16,322 (Brier close 0.239460 vs p_base 0.262703), S108 ran soccer and tennis with the close as incumbent (16,322 / 33,685 states), and gate_corpus_{nba,mlb}_close.parquet carry p_close (measured 2026-09-04: nba 563 of 1,814 non-null, 343 first_inplay_tick + 220 pregame_last_tick_before_commence; mlb 910 of 39,162). So the quantity this program optimises -- the calibration gap to the best available predictor -- has never been computed with reliability bins. Bar: every scorable sport reports ECE, Brier, log-loss and a 10-bin reliability table for BOTH the model and the close on identical rows under the ONE S05 bin-edge rule, 0 rows dropped after the printed pairing step, each sport labelled MATCH / BEHIND / NOT SCORABLE by its own corpus_unit-clustered CI; a model BEHIND everywhere is the valid result | S05_calibration_report_2026-09-03.md:148 + S02 + S108 | DONE 2026-09-03 4a81fce8f -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S204_VERIFY): premise PASS: S05 bins reproduced (NBA 0.024842542/1814, MLB 0.008076825/39162, soccer 0.009301789/25834, tennis 0.008403090/41886) and close census NBA 563 (220 pregame), MLB 910, soccer 16322, tennis synthetic. Paired deltas: NBA n=220 -0.015620 n_eff=2; MLB n=910 -0.003557 n_eff=1; soccer n=16322 -0.007546 n_eff=6; tennis n=0; all NOT SCORABLE. Tests 3+1 passed; corrections none. NEW GAP: build retains all four sport frames before scoring despite one-store-at-a-time. |
| S205 | signals-pregame | ONLY ONE CALIBRATOR HAS EVER BEEN TRIED (S05 section 2): per-regime isotonic buys ECE and pays Murphy resolution on three of four sports (nba -0.0026823, soccer -0.0005781, tennis -0.0006802) and on mlb made BOTH ECE and reliability WORSE, so the sealed rule reads FLATTENED 4/4 and no sport reaches IMPROVES. Items 3, 2 and 1 of docs/research/model_quality_methods_2026-09-04.md name the untried fix as the calibrator family, not more features (Guo 2017 temperature; Kull 2017 beta; Zadrozny and Elkan 2002 isotonic). Bar: 12 cells (4 sports x isotonic / temperature / beta) scored on 1,814 / 39,162 / 25,834 / 41,886 rows with 0 dropped, the isotonic column reproducing the four published after-ECEs (0.024843, 0.008077, 0.009302, 0.008403) at max abs diff exactly 0.0, every cell carrying the sealed verdict and a resolution-tax column; 0/8 new cells reaching IMPROVES is the expected valid result and the rule is never relaxed | S05_calibration_report_2026-09-03.md section 2 + model_quality_methods_2026-09-04.md items 1/2/3 | CLOSED AT LIMIT 2026-09-04 218e3f7d6 -- verified codex-sol (12/12 cells + 0 dropped rows reproduce; bar unreachable: S05 isotonic exact reproduction differs by 0.020533142 under the purged CPCV partition; memo S205_VERIFY): every cell FLATTENED (reliability gained by spending resolution); prereg SHA-256 4477066E64105687647CF3E55B72E25727589E8635518BEEDDABBDDE9EF8D5D2; 1-day symmetric embargo; fit_history fixed to actual per-row source counts; NEW GAP: S205-MASTER-TEST resolved by landing the focused test path |
| S206 | signals-ingame | WNBA HAS NEVER BEEN SCORED IN-GAME (wnba_ingame_census_2026-09-04.md, verbatim: 'WNBA in-play ticks with an action-derived as-of state | 0 | 18,650'): 18,650 state-joined ticks over 85 intersect games now exist, state age median 15 s / p90 132 s / 0 above 300 s, 84 of 85 games at or above 100 in-span ticks, settlement resolving 98 of 98 priced events -- more game clusters than the MLB store's 41-88 (S82/S119) and the largest unscored in-game denominator on disk. Bar: the frozen +0.004 in-game bar applied as written to the improvement of a Stern-1994 state arm over the null [1, logit(market)] fitted on the same rows and folds, on >= 30 game clusters, with the market's own Brier and ECE beside both arms, the as-of guard passing at 8 evenly spaced probes, and all 186,736 in-play ticks kept in the reported denominator; a SCREEN NULL and a model BEHIND the market are the expected valid results | wnba_ingame_census_2026-09-04.md + _summary.json + model_quality_methods item 5 | DONE 2026-09-03 b2b634bec -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S206_VERIFY): premise PASS 18,650 joined ticks/85 games, 186,736 in-play; scored 16,571/75 clusters. Candidate-null Brier +0.000384828, CI [-0.000210271,+0.000979927], n_eff 167.968; SCREEN NULL; market/null/candidate Brier 0.147718/0.155244/0.154859. Tests 2+1 passed; corrections none. NEW GAP: A1 unavailable before landing; document round-trip CSV ECE parsing or archive bin index. |
| S207 | signals-ingame | THE IN-GAME GAP IS DECOMPOSED FOR TWO CORPORA ONLY (calibration-decomposition.md): mlb n 78,986 model 0.237684 vs market 0.206653, gap +0.0310 = reliability +0.0066 and resolution -0.0235; soccer_intl n 9,003 model 0.227887 vs market 0.142726, gap +0.0852 = reliability +0.0394 and resolution -0.0446. NBA (465,249 ticks / 1,593 games, S86) and WNBA (18,650 ticks / 85 games) were never decomposed, so no lane knows whether recalibration can pay there, and S43's max-loser-WP has never run on its correct per-tick input. Bar: 4 of 4 corpora reported with model / market Brier, the three gap terms, n-weighted ECE both sides, a tick-age bucket axis and the per-path max-loser-WP share above 0.8, the two published rows reproducing at max abs diff <= 1e-6, each corpus labelled RELIABILITY-DOMINATED or RESOLUTION-DOMINATED | calibration-decomposition.md + S43 + model_quality_methods_2026-09-04.md item 10 | DONE 2026-09-03 85737c5be -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S207_VERIFY): PREMISE FALSIFIED: existing NBA baseline has Murphy + per-path max-loser-WP (661 games/79,554 ticks). Published rows reproduced: MLB 78,986 ticks, Brier .237684/.206653, gaps +.031031/+.006640/-.023496; soccer 9,003 ticks, .227887/.142726, gaps +.085161/+.039414/-.044576; ECE state subsets MLB 52,646/78,986 raw=.0790/.0591, soccer 3,658/9,003=.3609/.2511. Tests: S207 test absent per STOP; LOC 1 passed. Corrections: NOT VERIFIED list + named ECE subsets/raw totals. NEW GAP: none. |
| S208 | signals-ingame | NBA WAS EXCLUDED FROM PER-PHASE RECALIBRATION FOR A REASON THAT NO LONGER HOLDS (S88 memo section 5, verbatim: 'NBA (probe_R12_B32*) is untouched: no S06-equivalent leak-free partition or incumbent exists for it'): S123 has since landed foundry/ingame_incumbent_nba.py, and per-phase recalibration is the one in-game method on record whose CI excluded zero (S88 MLB late|leading_big +0.031643 [0.0088, 0.0572]). It has never run where the power is -- 1,593 game clusters vs MLB's 41-88, which S93 closed at limit against a +0.004 bar. Bar: the global single-bucket recalibration null re-scored FIRST with a game-clustered CI (S94 found it BEHIND the raw line; if the per-phase arm cannot beat it, CLOSED AT LIMIT), then every phase|margin bucket and the pooled row with CI, n_eff and ECE, BH-adjusted at q 0.05 across the enumerated family; a pooled NO_CHANGE or a WORSE bucket is the expected valid result | S88_phase_recal_2026-09-04.md section 5 + S123 + S86 | CLOSED AT LIMIT 2026-09-04 67c997374 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S208_VERIFY): NBA per-phase recalibration: nine buckets, all >= 30 game clusters; Benjamini-Hochberg survivors none; late|blowout_gt12 recal worse. Rejected: no sealed prereg artifact with embedded SHA-256 and walk_forward lacked embargo cutoff / purge assertions; measured NULL stands uncertified; embargo + sealed-prereg utility becomes a shared harness row (next allocation). |
| S209 | signals-ingame | THE ONE MEASURED IN-GAME EFFECT HAS NO FAMILY BAR (S88 memo section 4, verbatim: 'this is 15 buckets scored NO_CHANGE / IMPROVED (1) / WORSE (1) with no multiple-comparison correction applied'): late|leading_big +0.031643 [0.0088, 0.0572] and mid|trailing -0.011964 [-0.0232, -0.0010] both stand uncorrected and single-window on 33,920 eval ticks / 127 informative game clusters, pooled -0.002890 [-0.0114, 0.0052] NO_CHANGE. Bar: 15 of 15 buckets carry a BH-adjusted p at q 0.05 over the enumerated family and a replication label (REPLICATED / NOT REPLICATED / SINGLE-WINDOW), the three quoted S88 figures reproducing from the archived per-game series at max abs diff <= 1e-9, and any bucket that loses its label under BH reported as losing it; late|leading_big failing BH is the expected valid result | S88_phase_recal_2026-09-04.md section 4 | CLOSED AT LIMIT 2026-09-04 d16abcb94 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S209_VERIFY): MLB phase recalibration, FDR: 15 buckets, equal-game DM p aligned with deltas, BH q=0.05 survivors NONE (late|leading_big raw p 0.014277608, BH 0.214164126), week-28 replication NOT REPLICATED; all acceptance items PASS/reproduce <=4.4e-14; Q1:no sealed prereg before scoring; Q4:archive not purged/embargoed; successor: protocol re-run via landed S233 utility; code NOT landed, memos landed. |
| S210 | signals-ingame | NO IN-GAME SCREEN CARRIES A POWER STATEMENT (memory note ingame_signals_first_2026_09_03, verbatim: 'MLB in-game: every screen is NULL by construction at 41 game-clusters (CI half-width ~0.005 > the 0.004 bar)'; register row S93 says the same and is CLOSED AT LIMIT): the standing screens S80, S82, S84, S94, S96, S97, S99-S103, S114-S117, S119 and S123 are all recorded NULL or NEGATIVE with no minimum detectable effect, so the program cannot separate a refuted hypothesis from an unmeasurable one and keeps spending lanes on the second kind. Bar: every enumerated screen (grep printed with its file list) reported with n_ticks, n_game_clusters, n_eff, observed improvement, observed CI half-width, the 80 pct power minimum detectable Brier delta and the label UNDERPOWERED or REFUTED-AT-BAR, or an explicit NO SERIES ARCHIVED reason; two anchors reproduce at max abs diff <= 1e-9; no existing verdict is edited | ingame_signals_first_2026_09_03 memory + S93 + S82 + S117 | OPEN (new 2026-09-04) -- codex terra, spec S210_spec.md |
| S211 | signals-ingame | THE FLAGSHIP IN-GAME PAIRS WERE NEVER RE-RUN (SIGNAL_INVENTORY_REDTEAM memo line 199, verbatim: 'The two static->conditional Brier pairs (0.209->0.159, 0.241->0.126) were read from docs/evidence/ingame-conditioning.md, not re-run'): those two pairs and their attribution (~73 pct NBA and ~99 pct MLB of the lift to the realized score; model prior ~0.014 NBA / ~0.001 MLB) are quoted in JOB_EVIDENCE_PACKET and on the public page with no n, no CI and no archived per-game differential, so Q9 cannot be satisfied for either. Bar: both sports re-derived through the existing proof harnesses unmodified (or explicitly NOT REPRODUCIBLE with the reason), every published figure reproduced at max abs diff <= 1e-6 or reported NOT REPRODUCED with its honest value, the prior share carrying a game-clustered CI, and a per-game differential archived for all three arms; a CI covering zero is the expected valid result, published as a retraction | SIGNAL_INVENTORY_REDTEAM_2026-09-03.md:199 + ingame-conditioning.md | CLOSED AT LIMIT 2026-09-04 c7b166591 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S211_VERIFY): NBA per-phase recalibration, nine buckets, >=30 game clusters each; BH survivors none; late|blowout_gt12 recal worse. Q1 sealed-prereg and Q4 purge+embargo corrections DID land; rejection was memo acceptance omissions plus B1/B2. The measured NULL stands uncertified. Original run lacked sealed prereg and walk_forward embargo/purge assertions; shared harness utility is next allocation. |
| S212 | all | S200 CONTINUATION (regime-key OOF leak): attempt 2 archived date-group after-ECE nba 0.019514 / mlb 0.009282 / soccer 0.009538 / tennis 0.016937 but the verifier's CLEAN rerun gave 0.022205 / 0.009672 / 0.009192 / 0.016928 -- a mutable cache (s200_regime_key_oof.py:145) makes results call-order dependent and :209-216 copies reference rows. Bar: fresh-process rerun reproduces the memo to <= 1e-9; call-order invariance test; one result section; rail passes. The published leaked-key ECE table stays PROVISIONAL until this lands | S200 limit | DONE 2026-09-03 6226fb042 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S212_VERIFY): premise PASS: clean-first ECE NBA 0.022204500165 (n=1,814), MLB 0.009672349829 (n=39,162), soccer 0.009192347229 (n=25,834), tennis 0.016928256992 (n=41,886), zero dropped; reproduction max abs diff 4.79e-13, bin losses exact; tests 5+10+5+3+1 passed; corrections none; NEW GAP: memo lines 45-46 retain pre-alias calibration_report.py SHA B5E41FE9CF344975314A9153EF4645C97520D9E250378E7A9CA620CC0140AACD; expected 11A015825A6E9874F5B8389797C33B7C8E1A3CD0BE55C81F0C28CB4E7CD46CA0 |
| S213 | signals-ingame | NO SPORT CARRIES A VENUE-TRUTH-TO-FEATURE LATENCY (INGAME_CAPABILITY_2026-09-01.md, verbatim: 'schema_has_venue_ts=false for every sport in inplay_tick_latency.json, so no sport carries a real lag_p90 field at all -- nor an src_ts_coverage_pct field'), so EVENT_REACTIVE is failed closed on both halves and every cadence number in the in-game census is an OUR-POLL interval, not a freshness bound; the independent API census reaches the same conclusion (ranked gap 8). Two stores hold the raw material: the GUMBO poller stamps captured_at beside ts, and the Kalshi trade tape is ms-precision. Bar: 5 of 5 sports reported with either a measured lag distribution over >= 30 game clusters or an explicit NOT MEASURABLE reason naming the absent field, 0 sports proxied, and the frozen EVENT_REACTIVE gates (5.0 s, 95.0 pct) re-printed unchanged; a table in which four sports are NOT MEASURABLE is the expected valid result | INGAME_CAPABILITY_2026-09-01.md + ingame_api_census_2026-09-04.md gap 8 + INGAME_DATA_ENGINE_PROGRAM 1f | CLOSED AT LIMIT 2026-09-04 d0a66d7af -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S213_VERIFY): in-game latency ledger measured for all five sports (paired-tick counts, p50/p90/max, coverage, zero proxies), but verifier re-measured PREMISE as FALSIFIED (spec assumed schema_has_venue_ts=false everywhere), so step 2 should not have run; helper/test not landed; CSV/summary/memos landed as evidence; corrected-premise successor row is next allocation. |
| S214 | signals-ingame | TWO SOCCER IN-PLAY PRICE STORES HAVE NEVER BEEN READ: soccer_price_series.parquet 204,435 rows / 89 events (S81_market_move:52) and soccer_intl_price_series.parquet 2,261,903 rows (parquet metadata 2026-09-04) sit on disk while a grep of scripts/platformkit/ and docs/evidence/ for soccer_price_series returns 0 hits, and the only scored soccer in-game corpus is S117's 163 ticks over 2 game clusters (CLOSED AT LIMIT on corpus size). The S192 WNBA census is the proven method and has never been applied here. Bar: all 2,466,338 rows classified into exactly one of PRE_MATCH / IN_PLAY_JOINED / IN_PLAY_NO_STATE / POST_MATCH / UNRESOLVED_KEY with 0 unclassified and 0 interpolated states, and a recoverable count printed with its game-cluster count; census only, no Brier, no charge; a recoverable count of 0, or one spanning <= 29 clusters, closes the row at limit | S81_market_move_2026-09-03.md:52 + S117 + S192 census method | DONE 2026-09-03 03ffb2001573d225caa3d5496af4cddc37c49a7d -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S214_VERIFY): PREMISE FALSIFIED: 0/2 named stores present; 1 guarded Python reader; census stopped before implementation. Exhaustive store check 0/0 claimed and measured; archived S117 counts 9,003/51/29/3,658/163/2 reproduced from memo only. Tests: S214 test absent per memo (0 tests, exit 4); LOC rail 1 passed. Corrections: none. NEW GAP: S117 source artifacts absent; six counts not re-measurable from source. |
| S215 | signals-ingame | TENNIS IS RECORDED AS HAVING NO IN-PLAY CLOSE (MODEL_QUALITY_PROGRAM_2026-09-04.md section 2, verbatim: 'NO in-play close on disk') YET 1,854,100 PRICE ROWS SIT UNCLASSIFIED: tennis_price_series.parquet (parquet metadata 2026-09-04), whose event count is quoted as 1,864 by S81:52 and 986 by S80:79 -- the two artifacts disagree and neither figure is a measured in-play count; the grade store is degenerate (1,255 rows, 1,237 a single state_summary=FINAL, 18 priced with home_score=0 away_score=0, S80:78). Bar: all 1,854,100 rows classified into the five S214 classes with 0 unclassified, the event-count disagreement adjudicated with the measured number and its reason, and the recoverable-state count printed; census only, no fetch, no purchase; a recoverable-state count of 0 is the expected valid result and closes the state side at limit | MODEL_QUALITY_PROGRAM_2026-09-04.md section 2 + S81:52 + S80:78,:79 | DONE 2026-09-03 e405395bb -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S215_VERIFY): premise confirmed: 1,854,100 metadata rows, 986 events, 1,864 tickers; grades 1,255/1,237 FINAL/18 priced zero-score. Census classified 1,854,100/1,854,100 rows over 10 row groups: claimed/immediate/future/unresolved/interpolated 1,851,294/0/2,806/0/0; 986 unique events; recoverable state 0. Tests: census 2 passed; LOC rail 1 passed. Corrections: none. NEW GAP: per-event CSV omits unresolved rows, so a future nonzero UNRESOLVED_KEY total would not be reconstructible from that CSV alone. |
| S216 | signals-ingame | THE POSSESSION ENGINE HAS NEVER PRICED A TICK: nothing under scripts/platformkit/ imports src.sim, so basketball_sim.simulate_game and the CUDA-batched fast_sim.simulate_game_fast are absent from the in-game path, and every NBA in-game arm on record (S94, S96, S97, S102, S115, S123) is a logistic or ladder form over period / clock / margin / prior. The nearest sim-side evidence is S99 (MLB rest-of-game distribution BEHIND both markets, CIs excluding zero). Bar: the frozen +0.004 in-game bar applied as written to the sim's improvement over the S123 incumbent with a game-clustered CI on >= 30 clusters, the market's own Brier and ECE and the max-loser-WP share above 0.8 for all three arms beside it, src/ byte-identical (import-only) and asserted; a step-1 count of qualifying as-of rates snapshots below 30 clusters is CLOSED AT LIMIT; BEHIND is the expected valid result | S123 + S86 + S99 + S115 (no src.sim importer under scripts/platformkit/) | CLOSED AT LIMIT 2026-09-04 9c78f4d72 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S216_VERIFY): simulator-vs-line never scored: attempt 1 could not see the S92 archives (eval_gate, visibility artefact), attempt 2 saw them but the archives measured corpus differs from the spec premise numbers and the stop rule halted the lane; the S123 Brier ordering DOES reproduce from the archives; successor S247 carries the scoring with archive-measured premises; code NOT landed, memos landed. |
| S217 | signals-ingame | MICROSTRUCTURE WAS CLOSED ON A CAPTURE DEFECT, NOT A MARKET FINDING (S100 'premise falsified: depth captured pre-game only'): S105 measured why -- data/cache/book_depth/ does not exist on the pod, m37_ingame_enrichment is not a supervisor child and its stack_specs comment still reads 'NOT YET RUNNING -- restart pending', and the only per-level ladders come from one depth_capture pass per 300 s (DEPTH_CAPTURE_EVERY_N_TICKS 15 x LIVE_INTERVAL_SEC 20); local book_depth/kalshi/ holds 0 files (measured 2026-09-04). Bar: one full live MLB slate captured end to end with achieved inter-pass cadence p50 / p90 printed per game, 0 duplicate (date, ticker, ts) rows and 0 lost windows across 3 enumerated restart cases, 0 un-backed-off 429s, and the measured pass floor reported -- a floor above 15 s is a valid CLOSED AT LIMIT; never poll faster than the venue's 5-10 s guidance to meet the bar | S105_depth_capture_cadence_2026-09-03.md + S100 + book_depth/kalshi listing 2026-09-04 | DONE 2026-09-03 a54367a0f -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S217_VERIFY): premise FALSIFIED: required book_depth/kalshi and depth_history/mlb stores absent (0 files vs required 15), so Q8 stop; no metric, sampling, deploy, schema/code change, charge, or AHEAD claim. Constants 15/20.0/5.0/60.0 unchanged; S217 test absent per memo (0 tests/exit 4); LOC rail 1 passed. Corrections: none. NEW GAP: none. |
| S218 | signals-ingame | THE FIELDS THE LIVE PATH KEEPS HAVE NEVER BEEN COUNTED AGAINST THE FIELDS THE PAYLOAD CARRIES: ingame_live_state.live_state returns only (state_diff, frac_elapsed, p0) while the NBA/WNBA CDN payloads carry team.inBonus, players[].oncourt, timeoutsRemaining and game.actions[] substitutions -- which is why every NBA in-game arm is a function of margin and clock. The API census (ingame_api_census_2026-09-04.md) ranks the lineup delta first and foul-trouble / timeouts fourth among uncaptured fields, as assertions, not counts. Bar: 8 of 8 candidate fields classified for both the NBA CDN and the ESPN parser into exactly one of KEPT / PRESENT_BUT_DROPPED / ABSENT_FROM_PAYLOAD / NO_PAYLOAD_ON_DISK with 0 cells unclassified and a source line number on every KEPT cell; read-only, no network call, no extraction added; an all-NO_PAYLOAD_ON_DISK grid is the expected valid result | ingame_api_census_2026-09-04.md ranked gaps 1 and 4 + ingame_live_state.py + boxscore_read.py | CLOSED AT LIMIT 2026-09-04 9062195c2 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S218_VERIFY): NBA census: raw archive has 2,008 ESPN payloads + 168 CDN payloads; on_court_five ABSENT from ESPN; CDN substitution events PRESENT_BUT_DROPPED (parser ignores route); attempt 1 visibility artefact; attempt 2 hardcoded CDN cells (caught); attempt 2b rejected on citations/status schema; S253 derives on-court five from archived CDN substitutions; code NOT landed, memos landed. |
| S219 | signals-ingame | THE TAIL GUARD HAS NEVER RUN WHERE THE POWER IS: both in-game model sides fail by premature confidence on the eventual loser -- S58 trial B measured the NBA model above 0.8 on the loser in 11.4 pct of lost games against the market's 5.6 pct, and S43 found the same defect on MLB (loser-peak share above 0.9: raw 0.358, e4 0.111, market 0.074) -- and INGAME_SIGNAL_PROGRAM ranks a TAIL-ASYMMETRIC GUARD fourth but only ever proposed it for MLB's 158 games, which S93 closed at limit against a +0.004 bar. NBA has 1,593 clusters. Bar: the frozen +0.004 bar applied as written to the inner-selected composite's improvement over the S123 incumbent with a game-clustered CI excluding zero AND BH at q 0.05 across the enumerated 6-member family (d_hi in {0.05, 0.10} x d_lo in {0.15, 0.25, 0.35}, confident-side cut FROZEN at 0.3), every member reported including the worst, scored over ALL ticks not only the clamped ones; SCREEN NULL is the expected valid result | S58 trial B + S43 + INGAME_SIGNAL_PROGRAM_2026-09-03.md rank 4 + S123 + S86 | DONE 2026-09-03 ef2202422 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S219_VERIFY): Premise PASS: S58 81/711=0.113924 and 40/711=0.056259; S86 465249 ticks/1593 games; S92 orderings hold. Composite improvement -0.102920, 95 pct CI [-0.105850,-0.099991], tails 0/0.238636/0.209280 on 306735 ticks/1056 games; 1+1 tests passed; corrections none. NEW GAP: F1 labels 80259 unscored seed rows scored_ticks. NEW GAP: 14 repeated archive rows have tail_market=True with stored maximum 0.8. |
| S220 | signals-ingame | THE MLB LINE'S OWN REACTION LATENCY IS UNMEASURED: S96 measured the NBA in-play line UNDER-reacting to scoring events and drifting (slopes +0.23 / +0.30 / +0.48 at k = 3/5/10, positive placebo on non-event ticks), and the same quantity has never been computed for MLB, where the state feed is the fastest we have (GUMBO sub-250 ms server response, poll floor 5 s) and the price tick is slow (p50 31.0 s / p90 82.0 s over 371 games / 79,441 ticks, INGAME_CAPABILITY); INGAME_GAP_MAP names it L7 and L10 and both are unrun. Bar: 3 of 3 event classes (run scored, out recorded, pitching change) reported with n, lead-time p50 / p90 / max, a matched non-event placebo beside each, the observation floor (tick p50) printed on every row, and events whose line never moved reported as a named right-censored count; no arm, no Brier, no charge; a placebo indistinguishable from its event class is the expected valid NULL | S96 + INGAME_CAPABILITY_2026-09-01.md + INGAME_GAP_MAP_2026-09-03.md L7,L10 | DONE 2026-09-03 ac4132ba7 -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S220_VERIFY): premise falsified: GUMBO 0 JSONL files, 0/30 clusters, 0 joined rows; full store 405 files, 401 games, 79,566 ticks, cadence p50 31.0 s, p90 82.0 s, max 869.0 s. Tests: S220 2 passed; LOC rail 1 passed. Corrections: none. NEW GAP: outs-increase logic omits third out on 2-to-0 inning transition. |
| S221 | harness | MULTI-RUNNER IS PROBABLY FIXED AND NOT PROVEN: S135 reproduced runner B claiming runner A's live hypotheses at +901 s (900 s lease, no renewal, global reap_expired) and a sport-NULL hypothesis claimable only by an unbound runner; S135 landed a renew() heartbeat and S150 landed lease scaling, host:pid claimer identity and exit / SIGTERM / SIGINT cleanup, but S150 is recorded finished and UNVERIFIED at session close (commit 3c72a5bbc). The pod's measured single-runner ceiling is 3,780 screens in one hour (bar 200), 9,331.5 screens/hour sustained (S16_pod_hour section 4) and 3,266.7 screens/hour on the in-game tier (S102:208). Bar: 12 CONSTRUCT cases (2 horizons x 3 lifecycle cases x 2 sport bindings) with 0 double-claimed rows and 0 sport-NULL queued rows, run in an ISOLATED temp sqlite with no pod contact, plus a REPORTED (never executed) two-runner launch recipe whose aggregate rate is labelled an estimate; any non-zero cell stops the row | S135 + S150_runner_leases_2026-09-03.md + S16_pod_hour_2026-09-03.md + S102:208 | DONE 2026-09-03 30bade068 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S221_VERIFY): premise confirmed: A claimed/renewed 3/3, reaped +901s 0, B claims +901s 0, refused true, undrainable 0. Exhaustive 12/12 CONSTRUCT cases: runner-B claims 0/12 and sport-NULL startup claims 0/12; SIGTERM cases 4/4. Tests: probe 1 passed; LOC rail 1 passed. Corrections: none. NEW GAP: candidate memo dated 2026-09-04 while commit/verification are 2026-09-03; result unaffected. |
| S222 | signals-ingame | THE REFRESH LAYER HAS NO CENSUS AND ONE OF ITS TABLES IS SILENTLY STALE: the in-game engine is conditioned through as-of joins by four regularly-refreshed classes -- play-by-play archives (pbp_states_*, pbp_foul_states_*, possession_states_*), Statcast pitch grain (mlb_pitch_states__2022..2026, whose within-start velocity trend is a pitcher-state signal), workload / fatigue (bullpen_relief_chains.parquet, pitch counts, rest) and lineup minutes (team_system/player_rates.parquet) -- and none is documented for refresh cadence, endpoint or as-of provability; the one table anyone checked ends 2026-07-02 holding 8 rows over 2 teams against S82 folds dated 07-03..07-05 (S119 section 0b), and S81:54 records the pitch tables joining to no price. Bar: every enumerated table reported with rows, date span, staleness in days, declared-as-of YES/NO with its asof_supply line number and joined-to-a-tick YES/NO with the module named, 0 blank cells, at least one table classified STALE with its exact end date, and 0 tables wired or modified by the row | S119_mlb_ingame_supply_2026-09-03.md section 0b + S81_market_move_2026-09-03.md:54 + asof_supply | DONE 2026-09-03 6f9851888 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S222_VERIFY): premise FALSIFIED: 12/13 sources present; player_rates.parquet absent. Bullpen 71,523 rows, 2022-04-07..2026-07-02; final day 8 rows/2 teams. Tests: premise-stop test absent per memo (exit 4/0 tests); LOC rail 1 passed. Corrections: none. NEW GAP: spec names a test path while premise-stop forbids implementation; path absent and command runs 0 tests. |
| S223 | harness | THE INTELLIGENCE POOL HAS NEVER BEEN INVENTORIED WITH AS-OF SAFETY: 8 of 45 data/cache/atlas_*.parquet stores sampled evenly carry a SINGLE as_of 2026-05-31 (min == max), and only 5 of the 1,593 games in nba_checkpoints_full.parquet post-date it (915 ticks), so an atlas join to the one corpus with a market anchor is look-ahead on 1,588 of 1,593 games; meanwhile ~14 data/intelligence/ sidecars DO carry a per-row game_date/asof_date (momentum_signals 673,204; player_def_archetype_sidecar and player_opp_splits_sidecar 99,498 each; confidence_ensemble and per_player_calibration 307,643 each) and ~20 further stores carry no date column at all. No artifact records the split, so every family below is guessing which half it may use. Bar: all 45 atlas stores plus every present data/intelligence/ parquet enumerated one file at a time, 0 skipped and 0 unreadable without a printed reason, each labelled AS-OF SAFE / SNAPSHOT-ONLY / UNDATED with its grain key, rows and n distinct as_of; the 5-of-1,593 count reproduced exactly; every SNAPSHOT-ONLY store naming its producer module or the string NONE; a CLOSED AT LIMIT for the atlas half is the expected valid result | INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 1a/1b | DONE 2026-09-04 196aed9e1 -- ACCEPT WITH CORRECTIONS (verified: codex-sol acceptance PASS; Q6 block resolved by orchestrator ruling; memo S223_VERIFY): 158/158 paths, 0 errors; atlas 4 AS-OF SAFE / 55 SNAPSHOT-ONLY / 0 UNDATED (59), intelligence 45 / 0 / 54 (99). Harness lessons: attempt 1 missed both stores; attempt 2 missed atlas through worktree visibility; attempt 2b scanned row-group 0 only. NEW GAP: exact-path/Q6 identifier conflict resolved by the Q6 NOTE; no open follow-up. |
| S224 | signals-ingame | NO CALIBRATION EXISTS INSIDE THE TAIL STATES THE PRODUCT IS ASKED ABOUT (longshots, comebacks): measured 2026-09-04 on nba_checkpoints_full.parquet (465,249 ticks / 1,593 games), the trailing-side cell market_prob <= 0.10 holds 136,809 ticks / 775 games with realized outcome_home_win 0.006652, and the two-sided extreme cell (<= 0.10 or >= 0.90) holds 308,756 ticks / 1,590 games -- neither has a reliability table, an ECE or a power statement, and S210 audits only EXISTING screens. Bar: both tails reported symmetrically over 20 frozen 1 pct bins with every bin present (a zero-count bin printed as zero), market and incumbent Brier plus n-weighted ECE per bin, n_eff under game clustering and the 80 pct-power minimum detectable Brier delta per bin, 0 ticks dropped from the 465,249 denominator without a printed reason, each bin labelled SCORABLE or UNDERPOWERED; every bin UNDERPOWERED at the +0.004 bar is the expected valid result | INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 2 F1 + S210 | DONE 2026-09-04 4e88e7a2f -- ACCEPT WITH CORRECTIONS (verified: codex-sol acceptance PASS; single Q6 prose word corrected per the verifier diff; memo S224_VERIFY): low tail 136,809 ticks/775 games, realized rate 0.006652; both tails 308,756 ticks; 20/20 bins UNDERPOWERED, 0/20 SCORABLE at unchanged +0.004 bar, 0 dropped. NEW GAP: memo lacks NOT VERIFIED list. NEW GAP: construct test does not exercise every internal 1 pct boundary. |
| S225 | signals-ingame | TWO IN-GAME CONDITIONING LAYERS EXIST AND WERE NEVER SCORED AGAINST A MARKET: ingame_hypothesis_{hot_night,scheme_fit}.json plus row parquets hold 7,350 rows / 1,225 games (2024-25) and 444 / 74 (2025-26), with own caveats 'IN-SAMPLE LEAK: the prior (QUALIFY_SEASON=2024-25) is fit on the SAME season it is scored against', 'No in-play odds -> verdict is CALIBRATION ... never a market edge' and, for hot_night, 'PLANTED NULL SURVIVED: the shuffled conditioning prior ALSO beat BASE' (recorded hot_night 0.1723 -> 0.1689, delta 0.00345, CI [0.00201, 0.0049]; scheme_fit -0.00027, CI [-0.00052, -0.00002], n_clusters 919). Measured 2026-09-04: espn_nba_game_bridge.parquet (1,299 rows, exact on 1,299) joins the checkpoint corpus on 635 of 1,593 games = 187,203 ticks and 579 of the 1,225 hot_night games are in it, so the no-odds caveat is false on disk. Bar: every real arm reported beside its planted-null (shuffled prior) arm on identical rows and folds with a game-clustered DM CI, n_eff and the market's own Brier and ECE, the prior fitted strictly on earlier data, the tick-time alignment tolerance printed, >= 30 game clusters, 0 ticks dropped without a printed reason; a surviving planted null reported REJECT is the expected valid result | ingame_hypothesis_hot_night.json + INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 1e | DONE 2026-09-04 97aed062f -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S225_VERIFY): premise confirmed: bridge 1,299/1,299; 187,203 ticks/635 games, matched 579/1,225. Real hot_night dBrier vs incumbent +0.000061914, scheme_fit -0.000038239 at fixed 0.004; references incumbent/market Brier 0.069998316/0.069468467; 6 folds, 0 ticks dropped. Tests 2/2 focused + 1/1 LOC. Corrections none. NEW GAP: memo says all artifacts embed seal though per-game CSV does not; non-root --output-dir fails after writing. |
| S226 | signals-ingame | THE CLUTCH CELL HAS NEVER BEEN SCORED AS ITS OWN PARTITION: measured 2026-09-04 on nba_checkpoints_full.parquet, period 4 with |margin| <= 5 and game_clock_s <= 300 holds 62,465 ticks / 702 games against 284,586 ticks / 1,593 games for period 4 overall, and two as-of-safe state stores have never entered a screen -- data/cache/inplay_foul_state.parquet (5,010 rows, team foul totals by game_id + period) and possession_states_{2024_25,2025_26}.parquet (30,383 / 30,199 rows, seconds_remaining, pace, run_diff). The atlas foul and rotation stores are SNAPSHOT-ONLY at as_of 2026-05-31 and must not be joined. Bar: market and incumbent Brier plus n-weighted ECE printed for the clutch cell BEFORE any arm, then every enumerated hypothesis of a NEW sibling grammar module (ingame_grammar_nba.py byte-identical) reported with improvement over the S123 leak-free incumbent, DM p, game-clustered CI, n_eff and a BH-adjusted p at q 0.05 across the family, >= 30 game clusters, 0 ticks dropped without a printed reason; SCREEN_NULL is the expected valid result | INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 2 F3 + S123 | DONE 2026-09-04 f08630ccf -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S226_VERIFY): premise confirmed: clutch n=62,465 ticks/702 games of period-4 n=284,586/1,593; foul state 0/62,465, possession 58,981/62,465 (94.4225%), 664 games, 3,484 missing. Brier 0.024561402553, ECE 0.003328567998, n_eff 2007.019659; MDE 0.094496128575 > fixed 0.004, no arm scored. Tests 2/2 pass. Corrections none. NEW GAP: candidate memo lacks NOT VERIFIED list; add candidate-arm metrics and snapshot-only atlas inputs. |
| S227 | signals-ingame | EVERY IN-GAME ARM ON RECORD IS BINARY AND BLIND TO THE MARGIN TAIL: S58 trial B and S103 record that the repricer's FIXED margin sigma of 13.5 'over-states confidence at halftime' and was 'half the model-vs-line gap', with a per-cell fitted sigma bringing the state-priced prior to -0.0021 vs the line (CI including zero), yet sigma has only ever been judged through a binary Brier, which cannot see distributional width -- no CRPS or tail-coverage number exists for NBA in-game margin on 465,249 ticks / 1,593 games. Bar: the fixed-sigma and the walk-forward per-cell fitted-sigma arms both reported at all 6 points of a ladder frozen before any fit (m in {5, 10, 15, 20, 25, 30}) with empirical coverage, nominal coverage, CRPS, a game-clustered CI on the CRPS difference and n_eff, with the per-point game count printed and 0 games dropped without a printed reason; a fitted sigma no better than 13.5 is the expected valid result | S58 trial B + S103 + INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 2 F4 | DONE 2026-09-04 f45ea8554 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S227_VERIFY): PREMISE PASS: 465249 ticks, 1593 unique games, 0 required-field nulls, 0/1593 final-margin mismatches. Fixed CRPS 2.831222; fitted 2.827759; difference 0.003463, 95 percent CI [0.000419,0.006641]; six frozen ladder points; 0 drops. Tests: 3 focused + 1 LOC passed. Corrections: none. NEW GAP: summary per-game archive path is worktree-absolute; use a repository-relative path. |
| S228 | signals-pregame | A PLAYER-LEVEL MARKET SITS ON DISK AND HAS NEVER ENTERED THE HARNESS: measured 2026-09-04, data/cache/cv_fix/closing_props/ holds 77 JSON files, one per game, each an odds-API payload with commence_time, home_team, away_team and bookmakers, and nothing in this register references closing_props; the only prop scoring on disk, prop_calibration_history.parquet (4,942 rows, player_id + stat), is against our OWN baseline with no market column. As-of-safe conditioning exists (momentum_signals 673,204; per_player_calibration 307,643 carrying rolling bias and sigma_resid; gt_weighted_forms 99,157; schedule_strength_7d 99,498). Bar: all 77 files parsed one at a time into a tidy (game, player, stat, line, over_price, under_price, book, capture_ts) table with 0 unparsed and none skipped silently; if >= 30 game clusters settle, the stat distribution scored by CRPS and pinball plus Brier with 10 reliability bins on P(over the closing line), and the tail target P(a named player outscores that game's pregame favourite scorer) by log-loss against the observed base rate, all on identical rows with clustered CIs and the market's own loss beside the model's; if fewer, NOT SCORABLE with the exact n is the expected valid result | closing_props census 2026-09-04 + INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 1f | CLOSED AT LIMIT 2026-09-04 facdc4e94 -- attempt 2 REJECTED by codex-sol (contract A/B/Q; memo S228_VERIFY): NBA per-phase recalibration scored on nine phase buckets, all >= 30 game clusters; Benjamini-Hochberg survivors none; late\|blowout_gt12 recal worse; rejected: no sealed preregistration artifact with embedded SHA-256, and walk_forward lacked embargo cutoff / purge assertions; measured NULL stands uncertified; embargo + sealed-prereg utility becomes a shared harness row (next allocation). |
| S229 | signals-pregame | THE PERSON-TO-PERSON MATCHUP HAS NEVER BEEN SCREENED AND THE SERVED SURFACE CANNOT ANSWER IT: matchup_preview's own style_matchup block states it returns 'sport-level style-pairing statistics ... not filtered to these two specific teams -- no team/player-to-style-category resolver is wired in this composer' (verified live 2026-09-04). Two as-of-safe leak-safe sidecars exist and have never entered a screen: player_def_archetype_sidecar.parquet (99,498 rows, player_id + game_date, 2022-10-18 .. 2026-05-24, player-vs-HELP_DEF / PACE_CONTROL / SWITCH_HEAVY deviations) with an identical-shape null-control twin, and player_opp_splits_sidecar.parquet (99,498 player-vs-opponent rows); the atlas matchup stores are SNAPSHOT-ONLY (as_of 2026-05-31) and thin (106 rows). Bar: the defender-join coverage table printed first as a share of 99,498; base and candidate differing ONLY by the scheme x opponent interaction, proved by an identical-columns assertion; the null-twin arm reported beside the real arm on the same rows and folds with a game-clustered CI and n_eff over >= 30 game clusters; the atlas half labelled BLOCKED-ON-S223, never joined; a null result is the expected valid outcome | matchup_preview envelope 2026-09-04 + INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 2 F6 | DONE 2026-09-03 be5633e6b -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S229_VERIFY): premise PASS: 99,498 sidecar rows; direct 79,597/99,498; scorable 69,339/99,498; 3,049 clusters, n_eff 2,921.877. SCREEN NULL: real delta RMSE -0.000033, MAE -0.000084; null delta RMSE 0.000061, MAE 0.000208. Reproduction max diff 5.68e-14; tests 1+1 passed; corrections none. NEW GAP: result memo omits confirmed 2022-10-18..2026-05-24 sidecar date range. |
| S230 | signals-pregame | S108 REFUTED SINGLE-COLUMN ADDITIONS, NOT PAIRWISE INTERACTIONS: it fit every numeric as-of column the NBA gate corpus supplies (178) with logit(incumbent) as a true offset under nested walk-forward and the inner CV drove EVERY coefficient to zero in 20 of 23 outer folds (elastic net +0.001360, n 619), which no single-column sweep can extend to a scheme x scheme interaction. An as-of-safe pairing table has never been used: matchup_grid.parquet (4,900 rows, game_id + game_date + team_id + opp_team_id, 2024-10-22 .. 2026-04-12, offense-z vs defense-z per SCHEDULED game), and S204 measured gate_corpus_nba p_close non-null on 563 of 1,814 rows with only 220 pregame_last_tick_before_commence. Bar: the joined-row census printed first; ONE frozen family of offense-z x defense-z interaction terms scored against the incumbent on all joined rows with a corpus_unit-clustered CI and n_eff, and against p_close only on the pregame subset with its n stated; 10-bin reliability under the one S05 bin-edge rule; the undated archetype and position grids used only to freeze the hypothesis list, never as fitted values; 0 rows dropped after the printed pairing step; NULL is the expected valid result | S108 + S204 + INTELLIGENCE_SIGNALS_PROGRAM_2026-09-04.md section 2 F7 | DONE 2026-09-04 e236352ce -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S230_VERIFY): premise PASS: grid 4,900 rows/2,450 games; joined 1,814/1,814 unique, 563 closes, pregame n=220. Incumbent/interaction ECE 0.053328/0.026757, Brier 0.210960/0.213678, log-loss 0.609781/0.632114; pregame n_eff=2, NOT SCORABLE. Tests: S230 1 passed; LOC rail 1 passed. Corrections: none. NEW GAP: memo lacks required NOT VERIFIED list; no qualifying close comparison or deployment verified. |
| S231 | harness | SIGNAL SELECTION HAS BEEN TESTED AND SIGNAL WEIGHTING HAS NOT: S114's nested top-k over 564 screened NBA in-game hypotheses gave a best arm k=5 at -0.000400 vs the raw in-play line (game-clustered DM CI [-0.000934, +0.000133]) with k=5 beating k=1 by +0.0000830, CI [+0.0000027, +0.0001634], p 0.0429 -- 'the one CI in this memo that excludes zero', 48x below the bar -- yet no weighting layer exists anywhere: signal_ensemble.py is a TRACKING-arm ensemble, family_combo_screen.py (S79) scores its top-k on the same partition ('an in-sample ceiling, not a verdict'), stacker.py stacks OOF gap ARMS, and confidence_ensemble.parquet (307,643 as-of rows of per-signal multipliers) has never been an arm. Bar: the two S114 anchors reproduced from the archived 192,635-row series at max abs diff <= 1e-9; inverse-variance, James-Stein-shrunk and inner-fold-penalised ridge/logit arms each scored on identical rows and folds with a game-clustered CI, n_eff and its weight vector archived, weights fitted on strictly earlier windows only; every new column OPT-IN with the full caller list printed (bundle-widening guard, feedback_bundle_widening_regression_2026_07_11: a 2-column widening regressed the tuned NBA improved-ECE 0.01755 -> 0.03113 via an unlisted consumer); no arm beating +0.0000830 is the expected valid result | S114_ingame_ensemble_2026-09-03.md + signal_ensemble.py + bundle-widening memory | DONE 2026-09-03 8eb2b6ccc -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S231_VERIFY): premise FALSIFIED: max anchor diff 0.000156850160871 vs required 0.000000001; 192635/192635 rows, 673/673 games, 192635/192635 unique pairs; raw-k1 -0.000537356817711, raw-k5 -0.000243149839129, k5-over-k1 +0.000294206978582; LOC rail 1/1 passed; correction: added NOT VERIFIED list; NEW GAP: none. |
| S232 | harness | INTELLIGENCE-DERIVED CANDIDATES CANNOT REACH THE POD SCREENING RUNNER: the canonical queue data/cache/eval_gate/hypotheses.sqlite is 0 bytes and no intelligence store is in catalogue.NAMED or matches catalogue.GLOBS, so every family in S223-S231 stays a one-off script rather than a queue row screened in batch (recorded throughput, S16b_pod_factory_head_2026-09-03.md: 2,168 T1 SCREEN rows across all four sports in the first 15 minutes, 0 StaleCorpusError, 0 charges). Three extension points need no shared-module edit: a parquet matching GLOBS is enumerated with zero code change, a new FWER family is one appended block in the git-blob-pinned FWER_FAMILIES_SPEC_2026-09-03.md (as S89/S102/S144 did), and a new predictor may live in a new file via backtest_runner._load_callable. Bar: every store declared by S223-S231 classified GLOB-REACHABLE / NEEDS-ONE-NAMED-LINE / NOT-ENUMERABLE with its reason and none dropped; a dry-run seed into a SCRATCH sqlite producing a hypothesis count that matches grammar.enumerate_family for the same columns exactly; catalogue.py, seed_queue.py, foundry_runner.py and tiers.py byte-identical; the canonical hypotheses.sqlite untouched | foundry module map 2026-09-04 + S16b_pod_factory_head_2026-09-03.md | DONE 2026-09-03 30e7c122f -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S232_VERIFY): premise corrected: canonical hypotheses.sqlite is absent, not 0 bytes; exhaustive construct classified 24/24 sources as 2 GLOB-REACHABLE, 11 NEEDS-ONE-NAMED-LINE, 11 NOT-ENUMERABLE, 0 missing/extra/misclassified; dry-run 3510 = 1755 + 1755 from 13 + 13 columns; tests 2/2 focused and 1/1 LOC passed; corrections none; NEW GAP none. |
| S233 | harness | S208 was REJECTed on Q1 (no prereg artifact/embedded SHA-256 seal) AND Q4 (its own walk_forward asserts only train date < test date, no purge cutoff, no symmetric embargo). 17 files define a local `def walk_forward`; 5 modules duplicate `seal = hashlib.sha256(Path(prereg_path).read_bytes()).hexdigest()` verbatim (s58_clamp_family_trial.py:222, s58_e2_slice_trial.py:87, s58_nba_halftime_asof_trial.py:129, stacker.py:202/261). No shared tick-grain purge+embargo+seal utility exists | S208_VERIFY_2026-09-03.md (a16 worktree, Q1/Q4 FAIL); scripts/platformkit/eval_gate/walkforward.py + cpcv_engine.py read on disk | DONE 2026-09-03 63d5ec4b7 -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S233_VERIFY): premise FALSIFIED under Q8; full source scan found 18/18 definition files and 4/4 exact duplicate seal sites versus specified 17/5; cpcv callers 7/7, walkforward callers corrected 23 to 27; focused test absent per memo (0 tests, exit 4), LOC rail 1/1 passed; corrections: all-caller list + NOT VERIFIED (six-cell construct, two fixtures, focused test, scoring comparison); NEW GAP: none. |
| S234 | signals | REDTEAM SF-1 (screen/verdict score the SAME rows) is LANDED via S12's partition_corpus/ScreenPartitionLeak/required screened_n. SF-2's other half -- "charge the screen" -- is NOT landed: backtest_runner.py:192 hard-codes `cumulative_k(prior, 1)` for every charged call regardless of the printed screened_n, so K still undercounts the width the family's screens actually searched | REDTEAM_SIGNAL_FACTORY_2026-09-03.md SF-1/SF-2; scripts/platformkit/foundry/tiers.py (S12, d55c03da3); scripts/platformkit/eval_gate/backtest_runner.py:192 | DONE 2026-09-04 0c42b0db2 -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S234_VERIFY): premise confirmed: tier tests 12/12; increment remains and screened count is not forwarded. Construct n=3: at n=200 current K+1 vs screened K+200; evaluator 8 records, train sizes [4,5]. Tests: tiers 12/12, screen charge 1/1, LOC 1/1. Corrections: none. NEW GAP: refresh 2 stale byte/hash lines; add NOT VERIFIED list for unexercised shared-counter/real-ledger behavior. |
| S235 | harness | S50 (LANDED, opt-in `--per-unit`) switches the gate-corpus walk from row order to true per-corpus_unit event_date chronology but is not the default; calibration_report.py:340-342 / batch_gate.py:193 still walk row order. S50's own four-sport after-ECE (positional -> per-unit): nba 0.024843 -> 0.026583, mlb 0.008077 -> 0.012666, soccer 0.009302 -> 0.028722 (6-division interleave), tennis 0.008403 -> 0.015403 | S50_per_unit_chronology_2026-09-03.md lines 85-88; scripts/platformkit/eval_gate/calibration_report.py:340-342 | OPEN (new 2026-09-04) -- codex terra, spec S235_spec.md |
| S236 | harness | REDTEAM SF-11: min_corpora_eff caps at n_corpora; NBA/MLB/tennis each have 2 corpus_units (nba 2024-25 1,225 / 2025-26 589; mlb era_2010_2021 27,983 / era_2022_2026 11,179; tennis ATP 25,764 / WTA 8,002), so if S234's partition consumes one unit for screening, T3 (replication) has zero left and every verdict on those three sports becomes SINGLE-WINDOW by construction; soccer (6 units) is the unaffected control | REDTEAM_SIGNAL_FACTORY_2026-09-03.md SF-11; scripts/platformkit/combo/fwer_budget.py:77-90; S202 n_eff table (751.5553/10,802.5823/9,733.4018/37,961.8277) | DONE 2026-09-04 f5204918b -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S236_VERIFY): Premise FALSIFIED: full-gate units NBA n=1814 [1225,589], MLB n=39162 [27983,11179], soccer n=25834 [3366,4180,6072,3856,4180,4180], tennis n=41886 [30616,11270], not [25764,8002]; 0 null units/rows dropped. Tests: 6 passed; LOC 1 passed. Corrections: cap qualified for n_corpora>=2; NOT VERIFIED list added. NEW GAP: memo format list; spec-template Q8 no-test exception. |
| S237 | signals-ingame | JP's max-loser-WP diagnostic (memory jp_calibration_feedback_2026_08_31) is described as never run on a tick stream; measured 2026-09-04 this is only partly true -- s86_nba_every_tick.py:31,192,198,283-284 already calls wp_diagnostics.max_loser_wp on the S86 per-tick NBA series, but the archived JSON has no max_loser_wp key and the S86 memo never quotes it, so the diagnostic runs but was never published, and a reliability-diagram-BIN breakdown of it (JP's actual ask) has never been produced at all | jp_calibration_feedback_2026_08_31 memory; MODEL_QUALITY_PROGRAM_2026-09-04.md section 2 named cause 2; scripts/platformkit/eval_gate/s86_nba_every_tick.py; scripts/platformkit/wp_diagnostics.py | DONE 2026-09-04 ccf95d641 -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S237_VERIFY): premise FALSIFIED: S86 JSON has the key in 5/5 period and 15/15 period-margin cells (missing 0); OT n=19, q50/q75/q90/q95=0.615/0.750/0.787/0.8465, above 0.8/0.9=2/1; tests 5+4+1 passed; corrections applied: artifact coverage clarified, NOT VERIFIED added, peak-WP reliability table unadjudicated; NEW GAP: no S237-specific test (non-blocking under Q8). |
| S238 | signals-ingame | simulation_methods_2026-09-04.md names an unallocated row: "split-conformal band around the S123 incumbent, empirical coverage on held-out ticks, reported beside Brier" -- "absent on every predict_live path; point Brier/ECE only". S101 (LANDED) already builds grouped split-conformal coverage but its ARMS tuple is ("market", "model") only -- never the S123 incumbent -- and reports coverage deviation only, never interval width | simulation_methods_2026-09-04.md gap table + item 7; scripts/platformkit/eval_gate/s101_aci_coverage.py:54; scripts/platformkit/foundry/ingame_incumbent_nba.py | OPEN (new 2026-09-04) -- codex terra, spec S238_spec.md |
| S239 | execution | quant_principles_2026-09-04.md: "track 'days until first real close-based CLV reading' as an explicit countdown metric ... rather than an implicit future date buried in a do-not-claim note." Measured 2026-09-04: data/frontend/clv_ledger.jsonl = 20 rows (2 legacy, 18 open, 0 settled); execution_status.json n_settled=INSUFFICIENT; S32 (pairing bridge) CLOSED 2026-09-04 but S20's week bar (>=200 settled/>=2 sports/>=7 days) is still unmet and S18 stays BLOCKED on it | quant_principles_2026-09-04.md section 4; data/frontend/clv_ledger.jsonl; data/frontend/analytics/execution_status.json; register rows S18/S19/S20/S32 | OPEN (new 2026-09-04) -- codex terra, spec S239_spec.md |
| S240 | signals-pregame | BOX-SCORE PROGRAM CENSUS: user directive 2026-09-04 20:20 asks for the FULL BOX SCORE, not just game predictions. Measured 2026-09-04: NBA closing_props/ = 77 JSON files; prop_history_corpus_mlb.jsonl and _tennis.jsonl = 3,000 rows each (market_prob null on the sampled row); prop_history_corpus_soccer.jsonl = 0 rows. No cross-sport census exists. | BOX_SCORE_PROGRAM_2026-09-04.md section (a); data/cache/cv_fix/closing_props/; data/frontend/prop_history_corpus_{mlb,soccer,tennis}.jsonl | DONE 2026-09-03 8cbbb59e6 -- ACCEPT (verified: codex-sol, contract A/B/Q; memo S240_VERIFY): premise confirmed: NBA 77 files/6,491,336 bytes; MLB 3,000/1,283,918; soccer 0/0; tennis 3,000/1,230,398. Census: NBA 77/77 priced clusters SCORABLE; MLB/soccer/tennis 0 priced clusters NOT SCORABLE; zero unparsed units; 4/4 summaries reproduced byte-for-byte. Tests: census 2 passed; LOC rail 1 passed. Corrections: none. NEW GAP: duplicate rows NBA 48,513/48,515, MLB 2,994/3,000, tennis 2,969/3,000; expose counts. |
| S241 | signals-pregame | NBA MINUTES DISTRIBUTION (the box-score keystone, points = minutes x rate): four point-estimate minutes modules exist (minutes_predictor.py, minutes_floor_model.py, minutes_aware_props.py, pts_minutes_model.py, none read this session) but no quantile/CRPS minutes target exists on disk. opp_minutes_predictions.parquet is opponent-facing only. | BOX_SCORE_PROGRAM_2026-09-04.md section (c); src/prediction/minutes_predictor.py, minutes_floor_model.py, minutes_aware_props.py, pts_minutes_model.py | OPEN (new 2026-09-04) -- codex terra, spec S241_spec.md |
| S242 | signals-pregame | PTS/REB/AST CONDITIONAL ON MINUTES: the 7 prop models (_PROP_STATS, player_props.py) are point estimates only; quantile_props.py / quantile_calibration.py (_CQR_STATS = pts/reb/ast) exist but live per-player quantile coverage is unconfirmed (quantile_pergame_metrics.json is a 4 KB summary, not raw rows). Depends on S241. | BOX_SCORE_PROGRAM_2026-09-04.md section (b); src/prediction/quantile_props.py, quantile_calibration.py; data/models/quantile_pergame_metrics.json | OPEN (new 2026-09-04) -- codex terra, spec S242_spec.md |
| S243 | harness | BOX-SCORE COHERENCE (sum-to-team): distributions must keep roster minutes <= 240 (5x48) + OT and summed player pts/reb/ast tracking team totals. scripts/platformkit/boxscore_crosscheck.py + its test file already exist and were not read this session -- may already cover this at the point level. | BOX_SCORE_PROGRAM_2026-09-04.md section (c); scripts/platformkit/boxscore_crosscheck.py; tests/platformkit/test_boxscore_crosscheck.py | OPEN (new 2026-09-04) -- codex terra, spec S243_spec.md |
| S244 | signals-pregame | MLB BATTER/PITCHER LINE DISTRIBUTION: prop_history_corpus_mlb.jsonl has 3,000 rows (strikeouts etc.) with market_prob null on the sampled row; gate_corpus_mlb.parquet (39,162 rows) is team/game grain only. Real-market-price count across all 3,000 rows is unmeasured; the row may resolve NOT SCORABLE vs a market. | BOX_SCORE_PROGRAM_2026-09-04.md section (d); data/frontend/prop_history_corpus_mlb.jsonl; data/cache/combo/gate_corpus_mlb.parquet | OPEN (new 2026-09-04) -- codex terra, spec S244_spec.md |
| S245 | signals-ingame | IN-GAME LIVE BOX-SCORE UPDATE (state -> remaining-game distribution): no store on disk carries a 5-man on-floor stamp keyed to period+clock, so lineup-grain updates are BLOCKED. possession_states_{2024_25,2025_26} (30,383 / 30,199 rows) and garbage_time_segments (1,226,606 rows) give game-grain state; scoped to game-state grain only. | BOX_SCORE_PROGRAM_2026-09-04.md section (d); data/cache/ingame/possession_states_*; garbage_time_segments.parquet | OPEN (new 2026-09-04) -- codex terra, spec S245_spec.md |
| S246 | harness | BOX-SCORE SCORING HARNESS (CRPS/pinball/coverage vs the prop close): S233 landed scripts/platformkit/eval_gate/walkforward_embargo_prereg.py (purge_embargo_walk_forward, seal_prereg, assert_sealed) 2026-09-04 (63d5ec4b7); no box-score-specific CRPS/pinball wrapper exists. Consumes S228's tidy prop-close table rather than re-parsing closing_props. | BOX_SCORE_PROGRAM_2026-09-04.md section (b); scripts/platformkit/eval_gate/walkforward_embargo_prereg.py; S233_walkforward_embargo_prereg_2026-09-04.md; S228_spec.md | DONE 2026-09-03 10cef6055 -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S246_VERIFY): premise FALSIFIED: prerequisite utility absent across tree/index/export-reference/path-history (0/0 each); cited commit paths 2/2. Score outputs none/none; expected module, fixtures, score table, and focused test absent. Tests: focused test absent per memo (0 tests, exit 4); LOC rail 1 passed. Corrections: none. NEW GAP: S246_spec.md:25 overlooks CRPS/pinball functions in scripts/platformkit/dist_metrics.py:38,104. |
| S247 | signals-ingame | S216 re-issue: attempt 2 (S216_nba_sim_engine_vs_line_2026-09-04.md) FALSIFIED because the program-memo premise 465,249 ticks / 1,593 games does not match the archive on disk (s92_nba_lineup_dynamic_2026-09-03_all.csv = 79,554 ticks / 661 games / 2024-10-25..2026-04-06, market 0.142876712852 < recal_null 0.144293050901 < ladder_base 0.146849530547, order CONFIRMED). Re-issued with archive-measured numbers only and no stop rule so the lane proceeds to price the archived ticks with the possession simulator (import-only from src.sim) and score market / recal_null incumbent / simulator. | S216_nba_sim_engine_vs_line_2026-09-04.md (a13, attempt 2) | DONE 2026-09-03 fc0084261 -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S247_VERIFY): premise confirmed archive 79,554 unique ticks / 661 games, 2024-10-25..2026-04-06; rates eligibility 0/661 games and 0 scored ticks vs fixed minimum 30, so no three-arm score. Descriptive Brier all ticks market/recal_null/ladder_base 0.142876712852/0.144293050901/0.146849530547; rated 33,713 ticks / 284 games = 0.144100776926/0.146842905353/0.153323943143. Tests: 1+1 passed. Corrections: none. NEW GAP: none. |
| S248 | signals-ingame | S201 re-issue: CLOSED AT LIMIT only because the two S92 archive CSVs (_all 79,554 ticks / 661 clusters, _rated 33,713 rows) were absent from that worktree, not because the fatigue premise was wrong; S216 attempt 2 independently reconfirmed the _all archive denominator and Brier order from a different worktree. Re-issued with the archive present, hard-linked into the worktree by the dispatcher. | S201_VERIFY_2026-09-03.md; S201_spec.md; S216_nba_sim_engine_vs_line_2026-09-04.md (a13, attempt 2) | OPEN (new 2026-09-04) -- codex terra, spec S248_spec.md |
| S249 | signals-ingame | S218 attempt 2 census: 2,008 archived ESPN payloads (398 scoreboard + 1,610 summary, 777,521,676 bytes, 2024-10-22..2026-06-23) but 0 raw NBA CDN liveData boxscore/playbyplay archive on disk; on_court_five ABSENT_FROM_PAYLOAD 0/2,008 ESPN and substitution_event PRESENT_BUT_DROPPED 1,606/1,610 ESPN (dropped, never captured raw), so lineup-on-floor cannot be derived. Build a resumable, rate-limit-safe pod-side capture of raw NBA CDN liveData payloads (gzip, one file per poll, timestamp manifest). | S218_nba_live_field_census_2026-09-04.md (a16, attempt 2) | OPEN (new 2026-09-04) -- codex terra, spec S249_spec.md |
| S250 | signals-pregame | S240 census: NBA closing_props/ = 77 files / 48,515 tidy rows, 0/48,515 (0.0 pct) null prices, SCORABLE n=77; MLB and tennis prop_history_corpus_*.jsonl = 3,000 rows each, 100.0 pct null market_prob, NOT SCORABLE n=0; soccer = 0 rows. Build a prop-close capture census plus a resumable capture plan for MLB/tennis player props reusing the odds-API tier already used for the 77 NBA payloads, with a scorability forecast. | S240_boxscore_prop_census_2026-09-04.md | OPEN (new 2026-09-04) -- codex terra, spec S250_spec.md |
| S251 | ops | POD_BATCH_2026-09-04_pass3.md: the foundry runner died silently for 41 min 52 s (2026-09-04T00:51:15Z..2026-09-04T01:33:07Z), found only by an ad hoc ssh probe, not by any routine local check; the runner already writes data/ab_reports/foundry_runner.heartbeat.json on the pod (advanced to 2026-09-04T01:37:32Z after relaunch, 48 screens in 134.693 s). Build a local, ssh-free heartbeat-age check parsed from the pass report, with an alert line in pipeline.log when stale. | POD_BATCH_2026-09-04_pass3.md; POD_BATCH_2026-09-04_pass2.md | DONE 2026-09-03 3b1345c24 -- CLOSED AT LIMIT (verified: codex-sol, contract A/B/Q; memo S251_VERIFY): premise FALSIFIED: m50 heartbeat age already checked by fleet harness/sentinel. Historical silence 2,512 s (41m52s); isolated age 601.000 s, diff 0; stale/fresh CONSTRUCT 1/0 alerts. Tests: focused 1 passed; LOC rail 1 passed. Corrections: landed VERIFY memo only; helper/test/result memo excluded; census 3/3 timestamp memos, log bytes path-dependent. NEW GAP: unify 900s/1800s thresholds; local check needs transport/report refresh. |
| S252 | signals-ingame | S215 CLOSED AT LIMIT: 0 recoverable tennis point-level state rows keyless (price series 1,854,100 rows / 986 event_key, IN_PLAY_JOINED 0, all three state families 0 tick-joinable timestamp columns); ingame_api_census names Sportradar Tennis (tier-gated coverage), Enetpulse, Data Sports Group, livetennisapi.com / tennis-api.com with no cost/cadence/terms comparison anywhere. DECISION row: enumerate the 4 candidates with cost, cadence, terms and minimum viable capture, ending in a one-page decision brief; no code. | S215_tennis_inplay_census_2026-09-04.md (a13); ingame_api_census_2026-09-04.md | OPEN (new 2026-09-04) -- codex terra, spec S252_spec.md |
| S253 | signals-ingame | S218 attempt 2b census: on_court_five ABSENT_FROM_PAYLOAD 0/2,008 ESPN, PRESENT_BUT_DROPPED 168/168 CDN boxscore; substitution_event PRESENT_BUT_DROPPED 168/168 CDN playbyplay siblings + 1,606/1,610 ESPN. Only raw CDN archive on disk: data/domains/wnba/cdn_backfill (168 boxscore.json + 168 playbyplay.json pairs, 2026-04-25..2026-07-04). Derive a per-tick on-court-five stamp from the archived CDN substitutions + starting lineups; unblocks S225/S229/S245 and lowers S249's new-capture priority. | S218_nba_live_field_census_2026-09-04.md (a16, attempt 2b) | DONE 2026-09-03 4dd9e24eb -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S253_VERIFY): premise reproduced: 168 archive pairs, 168 starting-five payloads, 168 substitution payloads; 167 clusters, 5,647/5,647 complete ticks (100.00 pct), duplicates 0, imbalances 0, one named exclusion; 30 evenly spaced games, 844 exact minutes rows, max abs diff 0.000 seconds; tests 3/3 S253 + 1/1 LOC; correction applied: NOT VERIFIED section; NEW GAP: none. |
| S254 | harness | S209_VERIFY REJECTed on Q1 (no sealed prereg predating scoring) and Q4 (inherited archive not purged, no symmetric embargo) though every ACCEPTANCE item passed: 15 buckets, 0 BH survivors, reproduced to <= 4.44e-14. NEW GAPs: S88 CSV omits the pre-burn 47,104/158 rows; ISO weeks 27/28 share four game IDs. S233 (FALSIFIED) found no shared purge+embargo+seal utility landed. Re-run under a named SHA-256-sealed prereg, purged walk-forward with a symmetric nonzero embargo, and whole-game-cluster replication. | S209_VERIFY_2026-09-03.md; S209_mlb_phase_recal_fwer_2026-09-04.md (a18); S233_walkforward_embargo_prereg_2026-09-04.md (a17, FALSIFIED) | OPEN (new 2026-09-04) -- codex terra, spec S254_spec.md |
| S255 | signals-ingame | S247 CLOSED AT LIMIT: 0/661 archive game clusters (79,554 ticks, 2024-10-25..2026-04-06) have player_snapshot_date < game_date and team_snapshot_date < game_date; player_rates.parquet and team_rates.json are a single undated snapshot (fs-dated 2026-06-07). S223's census found the same defect pool-wide: atlas 4/55/0, intelligence 45/0/54 AS-OF SAFE/SNAPSHOT-ONLY/UNDATED (158 paths). Build a walk-forward as-of rate-snapshot producer from corpora already on disk; unblocks S247 and the atlas-conditioned families S224-S231. | S247_nba_sim_engine_vs_line_v2_2026-09-04.md (a13); S223_intel_pool_asof_census_2026-09-04.md | DONE 2026-09-03 b559352ed -- ACCEPT WITH CORRECTIONS (verified: codex-sol, contract A/B/Q; memo S255_VERIFY): premise confirmed: both legacy stores have zero date/as-of fields and postdate all 661 archive clusters; 355/661 clusters (53.7065%) received qualifying strictly-prior snapshots; exact rebuilds matched 76,820 player rows, 1,434 team rows, and 661 qualification rows; tests 2/2 files passed; corrected 27->28 columns and 240 nested keys->6,244 recursively inspected key occurrences; NEW GAP: future-value test checks one player snapshot, not all player/team values |
| S256 | signals-ingame | S247 successor: score the possession simulator as a third arm on the 355/661 archive clusters that S255 (b559352ed) gave strictly-prior as-of rate snapshots (player_rate_snapshots.parquet, team_rate_snapshots.parquet, cluster_qualification.csv). Three arms market / recal_null / simulator on a frozen tick grid; frozen +0.004 bar vs recal_null, game-clustered CI, >= 30 clusters; src/ byte-identical; legacy team_system stores unread. SCREEN NULL or BEHIND is the expected valid result. | S247_nba_sim_engine_vs_line_v2_2026-09-04.md; S255_asof_rate_snapshot_producer_2026-09-04.md | OPEN -- allocated 2026-09-03; spec docs/evidence/tracking/specs/S256_spec.md; queued after S254 |
