# G355 preregistration

Date: 2026-09-08
Lane: a12
Spec: docs/evidence/tracking/specs/G355_spec.md
Contract: docs/evidence/tracking/VERIFIER_CONTRACT.md

This is a robustness-only run. No scored comparison, evaluator call, corpus
measurement, or video decode precedes this seal.

Construct 1 fixes the ORB supplier so its first call returns two keypoints and
a 2-by-32 uint8 descriptor array, and its second returns one keypoint and a
1-by-32 uint8 descriptor array. The binding before-condition is that
`_static_inlier_fraction` raises ValueError at the two-value unpack of matcher
results. The change treats a descriptor array of length one as unobservable:
the router retains the frame with fraction 0.0, and floor propagation records
the pair as UNOBSERVABLE with reason too_few_descriptors. None keeps the
existing no_descriptors reason.

Construct 2 is one textured 180-by-320 BGR frame with one RouteRecord in shot
0. The binding before-condition is `propagate_frames(...) == []`. The change
adds exactly one ANCHOR record for every shot's first frame, with the existing
MotionRecord columns and zero geometric diagnostics.

The golden rule is six G341 named sections, only if each source exists under
this worktree's data/footage_corpus: shots.csv is byte-identical; every old
propagation.csv row is byte-identical; the only added propagation.csv rows are
first-frame ANCHOR records. Counts are reported per section. No threshold,
flag, data file, landed G341 artifact, or gated source tree changes.
SEAL sha256 b9751ebdd75b3ebbb7de055d0b07d841d412e9dbbc2435c99c0dce23458bfb1c
