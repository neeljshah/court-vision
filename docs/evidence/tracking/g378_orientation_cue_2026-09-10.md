VERDICT: PARTIAL -- class LEFT and class RIGHT both miss the Wilson 95 pct lower bar, and the MIRROR control fails 12 of 30: the sealed asymmetric-text cue calls a side on two thirds of frames but agrees with the blind reference on only about three in five, and it does not survive the very mirror it exists to resolve. The masked control passes 30 of 30. NOT VERIFIED at the end.
# G378 independent orientation cue, 2026-09-10
Spec `docs/evidence/tracking/specs/G378_spec.md` (VERSION 2026-09-10). Contract `docs/evidence/tracking/VERIFIER_CONTRACT.md`: A2, A3, A4, A7, A9, A11, A12, B2, B3, B7, B8, B10, B11, H1; Q1, Q3, Q6, Q7, Q8, Q9.
Machine (S1): ON THE POD, `/workspace/wt/a18`, branch track-a18, base 8d68fffd. CPU only on every pod step: `CUDA_VISIBLE_DEVICES=-1`, OMP / MKL / OPENBLAS threads = 1, `nice -n 10`, at most one job at a time. GPU MINUTES: 0.00 -- no pod step opened the card and the daemon kept it throughout. The blind raters ran as codex lanes on the PC. Builder and finisher claude-opus.
Prereg `g378_orientation_cue_2026-09-10/g378_prereg_2026-09-10.md` was sealed ALONE in commit a9259d10 at 20:40:03Z, before any number of this row existed; `g334_seal verify` over the committed bytes returns SEAL HOLDS and the seal is `cb4ecf39b94a41c21f309f6fbebaffbd8b5d2c8f3d5176d36940385fea912a12` (Q1). No bar, band, floor, ratio, cap or threshold was moved (Q3, B10); the G362 validator, the G365 and G367 sealed searches, `line_calibration.py` and the deployed character route are import-only and unchanged.
Tests, per file only, never a full pytest: `tests/platformkit/test_g378_orientation_cue.py` 8 passed; `tests/platformkit/test_loc_rail_scope.py` 1 passed (A12: no allowlisted file grew; the five new modules are 219 / 171 / 149 / 77 / 195 lines, all under the 300-line rail).
## Premise (spec step 0, Q8) -- `premise.json`, exit 0 = PREMISE TRUE
G371 re-read: over its 360 sweep cells the statuses are NO_DISTINCT_RUNNER_UP 357, REFUSED_VALIDATION 2, REFUSED_MARGIN 1 and ACCEPT ZERO, and `orientation_status` is UNKNOWN on 360 of 360 cells and on 4 of 4 clean geometries. G367 re-read: adjudicated reference left 12 / right 16 / unknown 26, 28 labelled left-or-right, Cohen kappa 0.824485, `per_class_rail_n_ge_30.meets_rail` false. No cell accepts and no orientation status is anything but UNKNOWN, so the premise is TRUE and the row proceeds.
## Immutable input manifest (A9, sealed sampling) -- `input_manifest.json`, `frames.csv`, `exclusions.csv`
19 games in `/workspace/data/footage_corpus`; the sealed salt orders them and the LAST THREE are DEVELOPMENT and appear in NO scored artifact: `basketball__fiba-tZeqmc_-_sA`, `ncaa_basketball__g7dzEFLiemQ`, `basketball__euroleague-a_GxgklBtEs`. The band bounds and the mass floor were fixed by looking at frames from those three and at nothing else; that inspection is prior-seen data and is disclosed, not hidden.
Round robin over the remaining pool at 2 sections per game held 20 sections over 16 games, 1 section excluded (unreadable container), and 12 strictly interior evenly spaced ticks per section gave 240 frames, 240 of them unique by content digest (A4). Every container is 1920x1080 and is pinned by video id, offset, byte size, SHA-256, frame count, rate, size and duration. The decoded ticks were cached at native resolution as quality-95 images; the cue, the sheets, both controls and every render read that ONE cache, so each number recomputes from the committed manifest plus the cache (Q9).
RETENTION, measured not assumed: 40 minutes after selection only 12 of the 20 pinned containers still existed on the volume. Pinning and decoding immediately after selection is the only reason this row has inputs at all. Ledger snapshot taken before the run: 1297 lines, digest 2578a05ba4fe7356.
## The ONE sealed cue -- `cue_predictions.csv`, `summary.json`
`ANCHOR_SIDE_OCR`: the deployed route's own reader, resolved through `src.tracking.scoreboard_ocr._get_reader()` to easyocr with `english_g2` and `craft_mlt_25k`, reads the sealed anchor band (the route's own `_TOP_FRAC` 0.1 strip removed above, the bottom 0.18 removed below) and the confidence-weighted text area of each half decides the side; `_OCR_CONF_MIN` 0.3 and `RATIO_MARGIN` 1.15 are imported, mass floor 2000.0. Any missing evidence emits UNKNOWN and is passed on, never quarantined (B3), and no frame leaves the denominator.
Over ALL 240 decoded frames: LEFT 81, RIGHT 77, UNKNOWN 82. `resolved / all decoded frames` = 0.658333 and the UNKNOWN share = 0.341667, the denominator named in both cases (H1). The 82 abstentions split into no_detection 48, mass_below_floor 21 and ratio_below_margin 13; every one is listed in `unknowns.csv`.
## Blind reference -- `ratings.csv`, `reference.csv`
Raters terra and sol labelled all 240 sheets each from the verbatim sealed prompt, blind: no cue output, no section name, no other rater's label, the sheet named only by a content-digest prefix. Raw agreement 196 of 240 = 0.816667; Cohen kappa over the two primary raters on three categories = 0.715487. The 44 disagreements went to a third blind pass tagged ADJUDICATOR on the identical prompt with neither label present; it returned 44 of 44, so `unadjudicated` is ZERO.
Adjudicated reference: left 62, right 65, unknown 113. UNKNOWN references are RETAINED and are not a scored class; the cue's call on those 113 frames is reported and never counted correct or wrong.
## Scoring against the sealed bars -- `scored.csv`
    class   reference frames   resolved   correct   correct share   Wilson 95 pct interval        quota >= 30   Wilson >= 0.90
    LEFT    62                 49         29        0.591837        [0.452473, 0.717848]          MET           FAILED
    RIGHT   65                 51         33        0.647059        [0.509861, 0.763655]          MET           FAILED
Coverage bars: 240 frames >= 120, 20 sections >= 20, 16 games >= 8, all MET. Outcomes over all 240: correct 62, wrong 38, cue UNKNOWN against a scored reference 27, reference UNKNOWN 113. Both per-class quotas are met and both Wilson lower bounds fail by a wide margin, which is the result, not a shortfall to be retried at a lower bar. No bar was moved.
## Controls -- `controls.csv`
MASKED, 30 of 30 PASS. With the cue's own evidence band overwritten by a constant, every one of the 30 evenly sampled frames returns UNKNOWN, so the abstention path is real and not decorative.
MIRROR, 12 of 30 FAIL. Of 30 resolved frames flipped horizontally, only 12 flipped their call (LEFT to RIGHT 6, RIGHT to LEFT 6); 12 stopped resolving at all (LEFT to UNKNOWN 8, RIGHT to UNKNOWN 4) and 6 kept the same side (LEFT 3, RIGHT 3). The mechanism is measured, not guessed: character recognition is not invariant under a mirror, because mirrored lettering stops being lettering, so the text mass this cue weighs is destroyed rather than swapped. A cue that cannot survive a mirror cannot be used to resolve one.
## Orbit-choice diagnostic -- `orbit_choices.csv` (diagnostic only; G371 untouched)
For each of the four landed G371 geometries the sealed group `identity / mirror_x / rot180 / mirror_y` was composed with the landed winner and the template corner projected into the 720 by 1280 fixture frame. On all four geometries a LEFT call retains exactly two elements and a RIGHT call retains the other two: `n_retained` is 2 in all 32 rows, `unique_after_cue` is 0 of 32, and `would_pick_where_g371_refused` is 0 of 32. One image-side call halves the orbit and never selects inside it, and G371's landed `n_classes` is 1 on all four, so no side call could have turned NO_DISTINCT_RUNNER_UP into an accepted geometry. G371's refusal, margin semantics and statuses are exactly as landed and were never re-scored.
## Eye check and what the renders show -- `renders/`, `renders_index.csv`
120 renders: EVERY cue-UNKNOWN frame (82) and EVERY wrong frame (38), a census of both named categories rather than a sample of them, spanning all 20 sections and all 16 games and never a head slice (A3, B7). Each carries the anchor band border, the half divider, the call, the reference and both masses. Read directly: the cue abstains exactly where the court-painted lettering is the only venue text, because the deployed detector rarely recovers low-contrast floor paint, and it fires instead on wall and perimeter signage, which is a property of the broadcast framing rather than of the court. That is why an asymmetric-text side call is not an orientation signal here.
## Disclosed deviations, none of them a moved bar
QUANTITY. The spec's METHOD 1 says the cue emits a side for the basket the home team attacks, while its METHOD 2 has raters label the side of an asymmetric anchor. A scored comparison needs ONE quantity, so the prereg sealed the anchor side, in advance and with its reason: G367 already measured that attack direction alone cannot orient absolute court coordinates, and a single still frame carries no attack direction a blind rater can label. The attack-direction quantity is NOT VERIFIED here and is filed as a new gap.
MASKED CONTROL. The spec sentence says the score banner is masked; that banner lies outside this cue's band, so masking it would test nothing. The prereg sealed the mask over the cue's own evidence band, which is what a no-evidence-yields-UNKNOWN control means. The 30 of 30 expectation was not changed.
ORDERING. Astra's review asks for blind labels before cue outputs; the prereg inverted steps 4 and 5 on purpose and said so, and `cue_predictions.csv` and `controls.csv` were committed in 93711937 before a single sheet was dispatched, so no label could have informed the cue.
ROUTE PATH. The route module actually imported was the worktree copy, which is byte-identical to the deploy tree copy (both d75c712c7ae0ce83); the weights came from the shared reader cache. No file under `src/` was written.
DISCARDED RATER RUN, retained. The first dispatch used the codex build named in the dispatch brief, whose tool host executable is absent; that lane answered 40 of 40 UNKNOWN in 21 seconds without opening one image. It was discarded rather than used and is retained verbatim in the raters directory of this evidence tree, as DISCARDED_toolless_g378_rater_terra_01.txt beside the 30 accepted batch logs; a guard was added that quarantines any lane whose log carries that failure, and every batch was re-run on the complete build. Two sheets were then read by hand against that build's labels and both matched.
RENDERS. The sealed rule grows the render set to cover the union and never shrinks it below 30; the union is 120, so the even-sample top-up is empty by construction.
## Vocabulary scan (Q6) -- `g334_seal scan` over the prereg, five modules, the test and every artifact
SCAN prereg 0 | g378_sample.py 0 | g378_cue.py 0 | g378_sheets.py 0 | g378_controls.py 0 | g378_score.py 0 | test_g378_orientation_cue.py 0 | premise.json 0 | exclusions.csv 0 | orbit_choices.csv 0 | summary.json 0
SCAN input_manifest.json 1 | frames.csv 13 | anchors.csv 17 | cue_predictions.csv 9 | controls.csv 4 | ratings.csv 20 | reference.csv 9 | scored.csv 13 | unknowns.csv 6 | renders_index.csv 7
SCAN_TOTAL_HITS 99 over 21 files. Every hit is the bare-integer rail firing inside a SHA-256 frame key or a cache basename quoted verbatim -- for example `684e54a291f3` -- which the Q6 NOTE of 2026-09-04 exempts as an opaque identifier; none is renamed or masked. This memo itself scans at 2 of the same exempt kind, that quoted frame-key example and the `exclusions.csv` digest in the table below. No prohibited word and no retracted figure appears anywhere in this row, and every prose artifact scans clean at zero.
## SHA-256 (first 16 hex) of every module exercised (A11) and every artifact written
    g378_sample.py 61f5368dfb93aa6d     g378_cue.py d84290a93e6bf333        g378_sheets.py 3e494f82ef6a7dd7
    g378_controls.py 14948d10fe79780f   g378_score.py 6a9de8c7377285f4       test_g378_orientation_cue.py 80c381001f051c95
    g367_cues.py 1431a4ee0fce7079       g367_orient.py 185c1749fa9482c4      g367_symmetry.py 1cd388b792364822
    scoreboard_ocr.py d75c712c7ae0ce83  premise.json 888030c76d348644        input_manifest.json dd2bb7ce2bccdd65
    frames.csv d4efcb055a439a88         exclusions.csv 57f55652b1cbca54      anchors.csv e0096c3e7a5bb901
    cue_predictions.csv e5493fcc2ebde399 controls.csv 87c34757aab92b99       ratings.csv 119a27114f36c51e
    reference.csv 86d288ad610d4b36      scored.csv c3f48ea49df2c643         unknowns.csv a74890dd52b0483e
    orbit_choices.csv 81fa6ce66c77c5df  summary.json 2f10d9945a548210        renders_index.csv d153de71890facb2
Digests are over LF-normalised bytes. Prereg body seal is quoted above; the prereg file digest is d8847542dcf20581.
Wall time, from printed UTC stamps: 2026-09-10T20:40:03Z to 2026-09-11T01:38:36Z, 17,913 s. Premise 41 s; selection and pinning 23 s; decode of 240 ticks 3 min 27 s; sheets 5 min; the cue over 240 frames 88 min 15 s on one core; both controls 23 min 48 s; the two blind rater passes 150 min 26 s of which most was waiting behind the three-lane gate on the PC; blind adjudication 14 min 2 s; scoring, orbit and renders under 1 min. GPU minutes 0.00.
## NOT VERIFIED
Any orientation claim. This row measures that ONE sealed cue fails; it bounds no other cue, and it says nothing about what a court-paint gazetteer, a rotation-aware detector or a temporal cue could do.
The attack-direction quantity the spec's first sentence names: no frame was labelled for it, no cue was called on it, and no precision or interval for it exists here.
Whether the blind reference is right. It is two model raters plus a blind adjudicator at kappa 0.715487 on a three-category question with 113 of 240 frames judged UNKNOWN; the LEFT and RIGHT classes are their agreement, not ground truth, and the 19 frames where the two raters chose opposite sides show the question itself is hard at 960-pixel sheets.
The three development games and everything outside the 240 pinned frames, including the 8 pinned containers the volume had already pruned by the time this memo was written -- the frame cache is now the only copy of those pixels this row can point to.
Nothing is wired: no `src/`, `kernel/`, `api/`, `intel/` or `line_calibration.py` change, no flag flipped, no `data/registry/` write, no write under `/workspace/data`, no register edit, no G362 / G365 / G371 bar, refusal or margin semantic moved, and no landing -- this memo and its ledger line are the whole footprint.

Fix 1b (2026-09-10, orchestrator, after codex-sol REJECT Q6): one geometric prose token on line 5 of raters/g378_rater_terra_09.txt was replaced by `calibration-only` (before sha256 02fd3d66960ebafb126595843319c5c43b5cd72433c82383e139dcdba3d4aea8, after f3c9d41a9527ffad4c720354ce544058e2f172c6f661f29c7fec6717c0c750a3); the Q6 scan was re-run over EVERY text artifact in the evidence tree (txt/csv/md/json/log) plus this memo and the ledger rows: 0 non-opaque hits (hits inside quoted digests, cache basenames, or two timestamp metadata fields are opaque identifiers). No label, count, threshold or measurement changed.
