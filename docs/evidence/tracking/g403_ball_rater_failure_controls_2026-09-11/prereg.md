# G403 preparation preregistration

Status: PREPARE-ONLY. This artifact seals the prospective diagnostic and control-construction protocol. No archived answer replay, reliability calculation, rating, inference, scoring, control administration, source-video decode, pod job, deployment, restart, register update, or ledger update is authorized in this lane.

## Binding source and population

- Required G400 handoff object: `6b1a7601a:docs/evidence/tracking/g400_ball_reference_growth_stage1_2026-09-11/`.
- Required immutable files and expected SHA-256 values: `ratings.csv` `3a746f5976cc44f9b86990c97edffcb1191454984f16d153dd2933f14bd20181`; `kappa.csv` `808d26134409926c178e7ec628df7f4fad028d7220003bde059c31e7826f96e4`.
- Required baseline file in this worktree: `docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/dev_boxes_v3.csv`, expected SHA-256 `e151f932c3b4bffe84c89aa8fde18f08c346a1e3a6e65d27ed4b36f095b7b4fa`.
- Before any future measurement, the finisher must independently join all 300 sealed keys, ten paired rounds, 20 raw answer archives, batch/runtime receipts, and eight redaction records; verify the G400 disposition and native-sheet hashes; and disclose any unavailable originals or sanitized copies with reproducible semantic fields.

## Locked future method and bars

- Preserve raw state labels independently of box validity; report duplicate ids, missing labels, invalid geometry, and image-open evidence separately.
- Future replay uses all 300 states, retains 299 paired observations if and only if that is what verified bytes produce, and preserves round 8 n=30, round 1 n=29, 125 both-VISIBLE centre gaps, and 288 valid VISIBLE diameter observations as stated in G403.
- Future centre gaps are `sqrt(dx*dx + dy*dy)` for every raw both-VISIBLE pair. Native diameter is `(box_w + box_h) / 2` for every valid raw VISIBLE judgement. No mismatched object may be excluded from the primary centre-gap median.
- Future reason categories are fixed to `RIM_HARDWARE`, `BENCH_BALL`, `JERSEY`, `OTHER_OBJECT`, `SAME_OBJECT_LOCALISATION`, and `UNRESOLVED`; dual-category evidence stays explicit. Shot-specific accounting retains `UNCLASSIFIED`.
- The constructed package is exactly six scenes by five layouts: active ball alone; ball beside rim hardware; active plus bench ball; ball beside jersey patch; distractors without ball; identity obscured; and 1080p centre/left/right plus 720p centre/right. It has 30 cases, 20 VISIBLE known centres, native coordinates, native `sheet_scale=1.0`, explicit geometry and occlusion masks.
- Controls are instructional development material only. A future G404 blind attempt requires 30/30 state decisions and inherited centre matches on all 20 VISIBLE controls, with no feedback or retry. Controls cannot alter G400 judgement, score, reference acceptance, or historical verdict.

## Future evidence and verdict rule

- The finisher may create G403 diagnostic evidence only after the binding before-condition passes. Missing audit trail is `PARTIAL`; changed verified bytes require recording the updated before-condition and stopping for a new seal.
- The G403 memo will state that every prospective comparison has sign convention `improvement = baseline loss minus candidate loss; positive = candidate better`. No comparison is planned in this preparation lane.
- The memo skeleton verdict is `NOT VERIFIED - PREPARE-ONLY`. It must end with an explicit `NOT VERIFIED` list.
- All future scoring, if any, requires a newly sealed preregistration before the first metric, the shared evaluator with purge and symmetric nonzero embargo, one stable evaluator state per scored tick, and archived losses derived only from evaluator records.

## Verification plan

- The focused test will normalize CRLF to LF when it verifies this preregistration seal from the file; it will not depend on `git show HEAD` during landing.
- The finisher will reproduce any saved tables and delivered renders twice, scan every landable text artifact including runtime logs with constructed Q6 patterns, and preserve repeat receipts and semantic-field parity.
- This preparation has no result claim. It does not satisfy the G403 acceptance rule, and it does not authorize G404.
SEAL sha256 fc0b4f458c6fe7bf0661fdfb930ae95da082641f6837b98c3592562e55a1b205
