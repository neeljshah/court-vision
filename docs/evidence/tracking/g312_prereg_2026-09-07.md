# G312 preregistration -- sealed BEFORE any G312 number exists (2026-09-07)

Sealed in worktree a12 at base commit 558c8db6d (the G309 landing), BEFORE the premise pair was
printed, before the ledger writer was touched and before the re-adjudication table was built.
Everything below the seal line is a hash of everything above it.

## What is fixed before any G312 number exists

1. PREMISE, binding. ONE named game -- `den_phx_2025` -- read from the pod READ-ONLY. Print, side
   by side: the ledger line's `evaluated_frames` and `coverage_pct`, and the recomputed
   `attempted_frames_capped` and `coverage_attempted_capped_pct`. If the two attempted counts are
   EQUAL, or if that game's `evaluated_frame_count.json` carries no positive `max_frames`, the
   premise is FALSE: STOP, write the memo, commit, report PREMISE FALSE.
2. THE CAPPED FORMULA, fixed now and not movable after the seal:
   `attempted_frames_capped = min(ceil(decoded_frames / stride), ceil(max_frames / stride))`
   `coverage_attempted_capped_pct = frames_emitted / attempted_frames_capped`, UNROUNDED.
   `frames_emitted` = distinct `frame` values in `tracking_data.csv`, the G309 convention.
   Rationale fixed now: G309 measured `frames_emitted` never exceeding `ceil(3000/stride)` on any
   game, so the route's `max_frames` bounds SOURCE frame reads. This is the census formula,
   re-derived here, not inherited: it was NOT in the G309 sealed metric list.
3. THE CAP IS READ, NEVER HARDCODED. `route_max_frames` comes from the game's own
   `evaluated_frame_count.json` (`max_frames`). The re-adjudication table uses the value read from
   the pod sidecars; the census states all 15 games carry 3000 and that is re-verified, not assumed.
4. EXACTLY THREE NEW PAYLOAD KEYS in `scripts/platformkit/track_daemon_done.py::adjudicate`, with
   these exact spellings: `route_max_frames`, `attempted_frames_capped`,
   `coverage_attempted_capped_pct`. No other key is added.
5. NULL ON MISSING, NEVER A GUESS. Absent sidecar, unreadable sidecar, absent/null/non-positive
   `max_frames`, or an unknown stride -> all three new keys are `null`. No default cap is invented.
6. NO EXISTING KEY MOVES. `passed`, `failure_heads`, `coverage_pct`, `harness_coverage_pct`,
   `coordinate_space`, `rung`, `evaluated_at`, `csv_fsynced`, `decoded_frames`, `evaluated_frames`
   and `stride` keep their exact values and spellings. The test asserts BYTE-IDENTITY of the
   pre-existing subset against the unmodified writer on the same input, not equality by eye.
7. THE RE-ADJUDICATION POPULATION, fixed now: the 13 rows of the COMMITTED
   `g309_multigame_census_2026-09-07.csv` with `ledger_row=present`. The live pod ledger has grown
   past the census and is a DIFFERENT population; it is not used for the table.
8. THE 1e-9 CHECKS, fixed now. For each of the 13 rows, from the committed census columns:
   (a) `frames_attempted == ceil(decoded_frames / stride)` EXACTLY, as integers;
   (b) `|coverage_attempted_frames_pct - frames_emitted / frames_attempted| <= 1e-9`;
   (c) `|coverage_decoded_pct - frames_emitted / decoded_frames| <= 1e-9`.
   "13/13 recomputed within 1e-9" means (a), (b) and (c) hold for all 13 rows. Separately, the
   capped share across the 13 must reproduce the census memo headline at 4 decimals: median 0.9790,
   min 0.7210 at `ncaa_basketball_sRtHQbywiTE`, max 1.0000, n = 13. Any disagreement is REPORTED,
   never patched.
9. SCOPE, fixed now. The capped share is a SCHEDULING number, not a quality one: a game may attempt
   100 percent of its capped frames and track the wrong objects entirely. No recall, precision,
   accuracy or registration claim is made or may be read in. Image space only
   (`coordinate_space = image_px`). All 13 rows are `passed = false` and none becomes a pass.
10. THE POD IS READ-ONLY. Nothing under `data/tracking/` is written, deleted or renamed; the
    running `track_daemon` is never stopped or signalled; fetched CSVs go to a scratchpad, never
    into `data/` in the repo. No GPU, no GPU lease. Nothing on the pod is re-adjudicated by this
    row: no completed run has yet written the new fields.
11. NO PRODUCTION CHANGE beyond the three additive keys. No harness edit, no threshold, no register
    edit, no adoption, no `--force`.
12. TEST: `tests/platformkit/test_g312_coverage_denominator.py`, a synthetic construct pinning the
    capped arithmetic, the null-on-missing-cap branch and the pre-existing-key byte-identity.
    n = 3 (CONSTRUCT). That ONE file is run. Never a full pytest.

## SEAL
SHA-256 of the LF-normalized bytes above this line: e04867c94d7e8b30cdec89acd809be71937536b86538446fa53dbaa1acaa9213
