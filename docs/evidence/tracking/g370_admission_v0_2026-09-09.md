VERDICT: PARTIAL -- the POSITION task mask stays DISABLED (planted-corruption detection 0.0000 FROZEN, 0.0000 COAST, 0.1000 ID_MERGE against the sealed 0.80, so the plant bar is unmet) and the GEOMETRY task mask stays DISABLED (every section is image-only, NOT_APPLICABLE); the schema, the declared-frame denominator, the observed-label rule, the usable-control bar and the deterministic export all met their sealed bars.

# G370 teacher-admission v0
Spec `docs/evidence/tracking/specs/G370_spec.md` VERSION 2026-09-09; contract A/B and Q1/Q3/Q4/Q6/Q7/Q8/Q9. Prereg
`.../g370_admission_v0_2026-09-09/g370_prereg_2026-09-09.md`, LF-normalized seal `4c0e630cdab4f3eedff078fab955e33c70c371c3531cfcd8c54daf5ccf471479`,
unchanged and re-checked by the test. Machine: pod worktree `/workspace/wt/a18` (branch track-a18), CPU only, one job at a time,
OMP/MKL/OPENBLAS/OPENCV threads = 1; no GPU, no daemon touched, no `data/` write, no `src/` edit, no threshold moved, no flag flipped.

The prereg's G366 fresh set (59 sections) no longer exists (pod loss 2026-09-10); the diagnostic fresh set below is a replacement
sealed from this pod's ledger with the same eligibility rule, and G366's expected values (stationary 12/59, tick-ratio median 0.153)
are comparison points, not this set's before-condition.

## Premise (Q8) -- TRUE, recomputed from the archived per-section evaluator records
Over the sealed 34 plus G366's archived 59 (93 sections): `zero_step_share` A0/M1 rejection 0/93 = 0.000000 and `distinct_position_ratio`
A0/M1 rejection 0/93 = 0.000000. On G366's archived fresh rows, `stationary_track_share` A0/M1 rejection is 12/59 = 0.203390 and the tick
ratio median is 0.153195 over n = 59. Explicit position provenance is present on 0 of the 34 sealed sections: no `position_source`,
`producer_branch_file_line` or `attribution_evidence_sha256` column exists. The producer does carry an `observation` column, but it is the
constant string `observed` on every row of every section inspected -- degenerate (B9), so the schema reads UNKNOWN, never DETECTION. Artifact `premise_sealed.json`.

## Schema, scorer and the replacement fresh set
34 G358 sealed sections / 17 games (commit 61bee9124): 45,842 scheduled evaluated ticks DECLARED from the ledger (`stride` x
`evaluated_frames`, H1 -- never from the frames that carry rows); 45,842 represented; 0 missing. 25,873 track and 23,558 ball observations,
every one `position_source` UNKNOWN and 0 labelled observed. 32,158/45,842 = 0.7014 declared ticks carry zero track observations. Verified
evaluated ticks 5,226/45,842 = 0.1140. All rows PARTIAL, 0 scorable, geometry IMAGE_ONLY, 0 ABSENT-induced quarantines. Export sha256 `747c93f26c007373fe7b459b39d97c9fdfbc69de527fadc6b625fda87db44db2`.
69 replacement fresh sections / 16 games: 89,655 declared scheduled ticks, 89,655 represented, 0 missing; 37,817 track and 50,980 ball
observations, all UNKNOWN, 0 labelled observed; 68,534/89,655 = 0.7644 declared ticks carry zero track observations; verified ticks
41,605/89,655 = 0.4641; all rows PARTIAL, 0 scorable. Export sha256 `b35fa87982db5a4c9d7c3b02131b6eb06f11a423eb1e270d6641582a47cd6729`.
The census ran over `ledger_snapshot_fresh.jsonl` (sha256 `25a81476c2ec9ed6f0a981dfb4aaa0f0c7c55da1224f8cd647e0c900cd2ae77e`, 880 ledger
sections) under G366's sealed ladder with only the freshness epoch changed to this pod's daemon start 1789053053: 69 eligible over 16 games,
and because 69 <= 80 every eligible section is selected (k = 1). `fresh_sections.csv` pins each source by video id, offset, table path, byte
size and table sha256. The pod's `data/tracking` is LIVE: 25 of 118 pinned tables changed and 1 vanished within 20 minutes of the first seal, so the row was re-sealed from an immutable lane snapshot (`/workspace/g370_snapshot`) and all 138 snapshot pins re-verified stable after scoring. 0 sections were excluded for an absent ledger cadence.

## Controls and plants
Blind usable-court cards (69; table-derived counts and an uncalibrated occupancy map only -- no admission score, no arm, no candidate, no
section id) were rated independently by terra and sol in an isolated directory outside the repository. Both returned 51 USABLE / 11 UNSURE /
7 NOT_USABLE and agreed on 69/69 cards, so no adjudication was needed. USABLE-CONTROL BAR MET: 51 usable rows over 16 games, v0 rejection
0/51 = 0.0000 (bar: 30 rows, 10 games, <= 0.05). PLANT BAR UNMET: 30 corruptions per kind applied before adaptation and scored after M1
collapse give FROZEN 0/30 = 0.0000, COAST 0/21 = 0.0000 (9 sections PLANT-UNCONSTRUCTIBLE, reported not hidden) and ID_MERGE 3/30 = 0.1000,
against the sealed 0.80. The sealed 34 reproduce this independently: FROZEN 0/30, COAST 0/24, ID_MERGE 1/30. The sealed v0 gate set separates
planted from unplanted content essentially not at all after M1 collapse, so the position mask stays disabled; no bar was lowered and no
threshold moved. Artifacts: `controls.csv`, `plants.csv`, `sealed_plants.csv`, `ratings.csv`, `ratings_terra.csv`, `ratings_sol.csv`, `card_map.csv`, `controls_gate_pass.csv`.

## Determinism, strips, vocabulary, deviations
Two exports on identical input are byte-identical for both sets (`export_hashes.json`, `sealed_export_hashes.json`). 30 strips evenly spread
over the 89,655-row decision ordering, each under 2,019 bytes and including rows with zero observations and unverified ticks: `strips/`.
Tests: `tests/platformkit/test_g370_admission_v0.py` 15 passed; `tests/platformkit/test_loc_rail_scope.py` 1 passed. Contract Q6 vocabulary scan over every touched file, output pasted verbatim:

Wall time about 2 h 18 min (reported first pod command 15:27 UTC, candidate commit 17:45 UTC), of which about 60 minutes was waiting for the daemon to produce a fresh set and about 25 minutes was the blind raters queued behind the PC process limit.
DEVIATION: the prereg orders blind ratings before admission scores; both scorer passes ran before the verdicts were merged, because the
raters were queued behind a binding three-process limit on the 16 GB PC. Blindness is intact -- the cards were frozen before either scorer ran
and live outside the repository, so no rater could reach any score -- and the export is invariant to the ordering, checked rather than assumed:
with the final `controls.csv` the bars are position control TRUE and position plant FALSE and the mask is `{position: false, geometry: false}`,
identical to the mask at scorer time. Raters judged table-derived cards, not pixels, because this pod holds no footage for these sections.

NOT VERIFIED: any calibration or outcome conclusion (none was computed; no `walk_forward` comparison was run); whether the 0.7644 share of
declared ticks with no track observation is a producer defect or a scheduling mismatch; the tick-verification derivation itself, which
resolves only 0.1140 of sealed ticks because 30 of 34 sealed sections report a non-integral `decoded_frames / evaluated_frames`; the pairing
of each snapshot table with the ledger record read after the copy; the geometry task in every form; and any claim that the v0 gate set could
ever enable the position mask.
