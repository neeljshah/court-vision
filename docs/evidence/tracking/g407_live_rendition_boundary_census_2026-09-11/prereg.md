# G407 preregistration

GAP: G407
LANE: a10
DATE: 2026-09-11
MODE: PREPARE-ONLY. This protocol authorizes no producer traversal, rating, inference, scoring, pod job, deployment, restart, register write, ledger write, or training call in this landing.

CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md sections A, B, A7, A9, A11, B11, H1, S1, S2, S4, S5, and Q1, Q3, Q6 apply. The finisher must repeat the binding before-condition before any live observation.

BINDING BEFORE-CONDITION: Hash the current discovery recipe and selector, record their activation UTC, and identify the exact live results and queue paths plus field names. Freeze ALL terminal ORIGINAL section attempts from activation through census UTC. Join fetched-format receipts to independently measured native source digests. An intended format, requested format, or itag alone is not an actual rendition. The preparation worktree reports these required stores absent and therefore records no observation.

POPULATION AND DRAW: Deduplicate by section identity, retain the earliest terminal ORIGINAL attempt, preserve failures, pruned sources, and unresolved joins. For every actual rendition, sort by competition, canonical game, section, and source digest. At most 30 entries per rendition are selected at ordinal j=0..29 using floor(j*(N-1)/29+0.5). No replacement, refill, pooling, or post-observation stratum selection is allowed. Explicitly enumerate 270, 232, 312, 301, 311, 300, and generic fallback. Each actual rendition needs 30 distinct retained sections; empty, under-30, or unresolved strata remain PARTIAL.

INTERVALS AND ACCOUNTING: Seal one centered two-second native-PTS interval per selected section, using earlier ties. Retain source warmup/context reads separately. Decode native dimensions, codec, PTS, and fps before scratch work. For actual rendition 232, count all planned sections into 29..31 fps, 25 fps, other, and UNKNOWN bins. Preserve raw overrun receipts, while derived rows are bounded to the sealed interval. Record decoded PTS, attempted reads, evaluated ticks, suspension/no-output ticks, provenance, and shared-track pairs. UNKNOWN is never converted to zero. Held pairs require the same track and consecutive evaluated ticks.

IDENTITY AND REPLAY: Before launch, hash every exercised route, model, and configuration asset. Observer wrappers must preserve the imported function return exactly and must first match the G402 receipt shape on saved fixtures. Scratch replay is described only as saved-input replay; it does not establish unseen live internal behavior or producer repeatability. Regenerate delivered tables and cards in two fresh processes from saved bytes without a producer rerun.

ACCEPTANCE BARS (VERBATIM): Exact source-format binding and 30 unique sections for EVERY admitted rendition; all empty/missing strata explicit. All selected windows reconcile decoded, attempted, evaluated, and no-output units; zero derived out-of-window rows; UNKNOWN never scored as zero. Every exercised asset is sealed before launch; receipt/trace agreement holds on every measured window; two identical saved-input regenerations complete. These bars do not move.

OUTPUTS RESERVED FOR THE FINISHER: ops_snapshot.json, population.csv, source_format_join.csv, draw.csv, source_receipts.csv, model_hashes.csv, route_hashes.json, pts.csv, launch_receipts.csv, raw_traces/, window_counts.csv, provenance_counts.csv, held_pairs.csv, coverage.csv, eye_index.csv, renders/, summary.json, repeats.json, runtime_receipts/, q6_scan.json, and SHA256SUMS under docs/evidence/tracking/g407_live_rendition_boundary_census_2026-09-11/.

SIGN CONVENTION: No delta is calculated in this preparation landing. For any later comparison, improvement means baseline loss minus candidate loss; positive means candidate better.
SEAL sha256 a411f5905fbce243d2198889752e40c5025a9b1abff74105976bbdd82606978c
