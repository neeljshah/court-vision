VERDICT: **NOT PINNED** -- no preregistered factor restores batch/single bit-identity (`pad` 3/120 and MEASURED NOT APPLIED, `fp32` 1/120, `nmsord` 3/120, against a `batch8` baseline of 3/120; the bar was >= 110/120 for exactly one arm) -- and the daemon route is **DETERMINISTIC 120/120**: the repeated single-image pass is bit-identical on every one of 3 games x 40 decoded frames, 0.0000 px and 0.000000 score delta on all 1245 IoU-matched pairs. Denominators: 120 frames, 6 GPU arms = 720 detector passes, 7 arms = 840 arm-frame comparisons. Confidence is the route's OWN `_fill_conf_threshold` **0.22**, which CONFLICTS with the registered 0.3 (G303's finding, restated not resolved).

**SCREENING ONLY. THERE IS NO GROUND TRUTH HERE.** This row says whether two passes AGREE, never which one is CORRECT. No recall, precision, registration, accuracy or pass claim; neither pass is better. Nothing adopted, no production default moved, no input size, confidence or batch size changed anywhere in `src/`. **No render, no eye check** -- this row is arithmetic over box coordinates and tensor shapes.

**PREREG, THREE SEALS, ALL COMMITTED ALONE BEFORE ANY NUMBER.** Attempt 1 `g327_prereg_2026-09-08.md` seal `3f19f817c4...` (`dfb190cc6`); attempt 2 `...-08b.md` seal `458026976e...` (`2288cecbf`); attempt 3, the scored one, `...-08c.md` seal `6548ebe7d340cb94c021ab363d574438f976628ab5d52ba7c7ebceac6f130534` (`cfdaa6c0b`). **Every arm, kwarg, the bit-identity rule, IoU 0.5 one-to-one, and the 110/120 and 120/120 bars are BYTE-IDENTICAL across all three seals. No bar was ever moved (contract Q3).** Only the construct's source selection changed, for the measured reason below.

**PREMISE, PARTLY FALSIFIED (contract Q8/S2) -- a valid result, and the row continued.**
1. **G324's evidence is NOT ON MASTER.** `6dc55b929` was REJECTED (`G324_VERIFY_2026-09-08.md`, `76772791f`); `g324_artifact/` exists only on branch `track-a2`. This row lands no G324 file.
2. **G324's committed CSVs carry the SINGLE-IMAGE pass ONLY** (`g324_arms.py:measure` writes `run_single`'s rows; the batch rows were compared and discarded), so **the spec's premise recompute is IMPOSSIBLE from any committed artifact.** Carried as an INHERITED SUMMARY, never recomputed: production `yolov8n` `batch_vs_single` was identical_box_counts true / max_abs_coord_delta_px **310.5** (wnba_01_1080p), true / **861.0** (ncaa IB-_u4gW3ds), false / null (wnba_06); RTMDet true / **0.0** on all three.
3. **RTMDet was NOT re-run** (its stack is not installed in this worktree); its role is only to show the effect is not universal.
4. The production call path is INHERITED from G303 (`b27d89028`) and was **re-confirmed live**: `classes=[0]`, `verbose=False`, `imgsz=640`, `half=True`, `device=0`, `TOPCUT=60`, fed array `[1020, 1920, 3]`, network tensor `[1, 3, 352, 640]` `torch.float16`.

**TWO CORRECTIONS THIS ROW OWES, both self-inflicted and both retracted in writing before scoring.**
- **Attempt 1's abort was MY BUG, not proof of anything.** `ffprobe` was called with `-show_entries` TWICE, and the second silently overrides the first, so `streams` was never emitted and the probe could not have succeeded on any file. Attempt 2's prereg wrongly cited that traceback as evidence of source absence; **that attribution is retracted.** The absence is real but rests on independent `ls`/`find`/census evidence (`g327_artifact/g327_attempt1_source_absence.txt`).
- **Attempt 3's prereg expected 1280x720 sources and said the fed array would be `[660, 1280, 3]`. The realised selection is 1920x1080** (the corpus rotated again between seal and run), so the fed array is `[1020, 1920, 3]` -- the SAME shape G303 recorded on production. The sealed rule (`width >= 1280`, mtime descending, first clip per distinct game id) executed exactly as written; only its expected outcome was wrong, which **strengthens** comparability rather than weakening it.

**WHY THE CONSTRUCT STOPPED NAMING FILES: THE POD CORPUS IS A LIVE ROTATING QUEUE.** Measured, not inferred: the corpus fell from **119 files to 85 in about forty minutes**, and **two of attempt 2's three freshly sealed clips were deleted before their own run**. The tracking daemon deletes each clip after tracking it, so **no prereg that seals source FILE NAMES can survive to its own run.** Attempt 3 therefore seals a deterministic SELECTION RULE plus an IMMEDIATE SNAPSHOT (per-frame SHA-256 recorded, so the construct is reproducible from the artifact after the clips are gone). This is the D2 landmine as a standing property of the machine, and it is this row's most consequential finding.

**SOURCES AS REALISED (contract A9, full path under `/workspace/nba-ai-system/data/footage_corpus/`, ffprobe at run time; anchors MEASURED by the readable-anchor rule, never ffprobe `nb_frames`).** `nba__XzIzQZBrPBY_s398.mp4` 96,353,880 B, 1920x1080, 60000/1001, 130.432 s, nb_frames 7816, anchor **7815**; `nba__6cNq5d_NOXs_s772.mp4` 100,854,836 B, 1920x1080, 60000/1001, 135.109 s, nb_frames 8097, anchor **8096**; `nba__eaKqXq9dfig_s60.mp4` 60,652,830 B, 1920x1080, 60/1, 131.549 s, nb_frames 7891, anchor **7890**. 40 evenly spaced indices per clip spanning 0..anchor, **40 of 40 decoded on every game**, one decode per game reused by every arm (decoded-stack SHA-256 `a8f05cb40dc9f52f...`, `2160b82abe76179b...`, `a712b2e5c5a9283e...`).

**THE ISOLATION TABLE. Denominator is 120 decoded frames in EVERY cell; zero-box frames are IN the denominator (1 of 120, in game 1 only) and two zero-box frames count as bit-identical.**

| arm | batch | network tensor | dtype | factor applied? | bit-identical vs single | non-empty-frame cut (post-hoc) |
|---|---|---|---|---|---|---|
| `single_repeat` | 1 | [1,3,352,640] | float16 | reference repeat | **120/120** | 119/119 |
| `batch2` | 2 | [2,3,352,640] | float16 | baseline | 4/120 | 3/119 |
| `batch8` | 8 | [8,3,352,640] | float16 | baseline | 3/120 | 2/119 |
| `pad` | 8 | [8,3,352,640] | float16 | **NO -- measured no-op** | 3/120 | 2/119 |
| `fp32` | 8 | [8,3,352,640] | **float32** | yes | 1/120 | 0/119 |
| `nmsord` | 8 | (batch8 boxes, sorted) | float16 | yes, comparison-side | 3/120 | 2/119 |

**WHAT IS FALSIFIED BY MEASUREMENT, and it is most of the row's hypothesis space.**
- **DYNAMIC PER-BATCH LETTERBOX: FALSIFIED.** The network tensor is `[B, 3, 352, 640]` at every batch size -- H and W never vary. ARM PAD, which pins the batch to the single pass's measured shape, is therefore a **measured no-op** (its shapes equal `batch8`'s), and it is excluded from the pinning decision rather than counted.
- **NMS / EMISSION ORDERING: FALSIFIED.** Sorting both sides by score then coordinates changes the count by **nothing** (3/120, identical to the unsorted `batch8` baseline). **The boxes are genuinely different values, not the same boxes reordered.**
- **HALF PRECISION: NOT THE CAUSE.** `fp32` was really applied (dtype float32) and made agreement **worse** (1/120), so single/batch disagreement is not simply fp16 rounding.

**HOW BIG THE DISAGREEMENT ACTUALLY IS -- small, and the frame-level bar is strict.** Over IoU >= 0.5 one-to-one matched pairs pooled across all three games: `batch8` **79.08 pct of matched boxes are BIT-EXACT**, median coordinate delta **0.000 px**, p90 **0.525 px**, p99 **1.500 px**, max **78.750 px** (1243 pairs); `batch2` 80.55 pct exact, p99 1.500 px, max 78.750 px; `fp32` 0.00 pct exact, median 0.436 px, max 59.080 px. Box COUNTS are equal on 2 of 3 games, and both-way IoU >= 0.5 agreement is **1.000000 / 1.000000** on games 1 and 2 and **0.995349 / 0.995349** on game 3. A frame fails bit-identity if ANY of its ~10 boxes moves, which is why frame-level identity fails on about 97 pct of frames while box-level agreement is near-total.

**THIS ALSO EXPLAINS G324's HEADLINE NUMBER, and my own first pairing reproduced the artifact.** Pairing the two passes BY RANK instead of by identity gives max coordinate deltas of 1133.6250 / 1824.0000 / 1826.0713 px on these same clips -- the same order as G324's 310.5 and 861.0 px -- because a max score perturbation of only **0.103760** re-ranks near-tied boxes and compares unrelated pairs. **Under IoU-matched pairing the same data gives 78.750 px.** CONSISTENT WITH G324's figure being a pairing artifact; this row did not re-run G324's code and does not prove it. **NO FIX IS PROPOSED and no PROPOSED diff is written.** The production route is single-image and is DETERMINISTIC 120/120, so nothing in `src/` is implicated. The implication is a constraint, not a defect: **batching this route would change what the tracker sees**, and no configuration tested here removes that.

**INTEGRITY / MUST-NOT-MOVE.** Deployed `src/tracking/player_detection.py` `c3bc2f7d4c4fda36...`, `src/tracking/advanced_tracker.py` `df2ae698ae03e804...` and `yolov8n.pt` `f59b3d833e2ff32e...` are **byte-identical before and after** (`gated_unchanged: true`). GPU lease honoured: `3394 MiB, 24576 MiB` at pre-flight (21,182 MiB free, above the 8,192 MiB gate) with 7 daemon compute apps listed verbatim, `4558 MiB, 24576 MiB` after; `track_daemon` was never stopped or signalled. `du_workspace_mb=UNKNOWN` (UNKNOWN, never 0, never a stop) while `pod_run` reported `POD /workspace 36797 MB used of 50000`; `dd conv=fsync` rc=0 at 299 MB/s. All compute in `/workspace/wt/a21/jobs/20260908025412_745458_5112`; no corpus file was written, moved or deleted. Pod route SHA-256 as deployed: `g327_arms.py` `dc9efb4bd347f7b8...`, `g327_sources.py` `b02053243c0e6996...`, `g327_detector_batch_stability.py` `ca4f9d93ad5b2769...`. Env: Python 3.12.3, torch 2.8.0+cu128, ultralytics 8.4.143, cv2 4.14.0, RTX 3090.

**TESTS.** `tests/platformkit/test_g327_batch_stability.py` -- **10 passed** (chunk coverage with a short last chunk; every frame kept at batch 1/2/8; bit-identity true for an identical pair and false at one ulp; a reorder failing the baseline rule and passing NMSORD; both-way agreement asymmetry; greedy one-to-one at the sealed threshold; the zero-box denominator; an EXACT CSV round-trip; the selection-rule game-id parse; the anchor-spanning index list). `tests/platformkit/test_loc_rail_scope.py` -- **1 passed**. New modules 181 / 139 / 267 lines, all inside the 300-line rail; the LOC allowlist was not edited.

**FIVE ABORTED RUNS, all BEFORE any scored number, recorded so the history is complete.** Jobs `...7433` and `...14859` died on the double `-show_entries` bug; `...8630` on a `NameError` left by a module split; `...2224` and `...22094` on lost tensor instrumentation -- ultralytics runs a fused `AutoBackend` copy and rebuilds its predictor when kwargs change, so neither a predictor wrapper nor a raw-module hook stayed attached. The fix hooks the LIVE predictor module per arm after a warm call. **No arm number from any aborted run is quoted here.**
**NOT VERIFIED.**
- **Which pass is CORRECT. There is no ground truth in this row**, so nothing here ranks single against batched, and no detection-quality claim is made.
- **The cause remains unpinned.** Only three factors were preregistered; cuDNN kernel selection by batch shape, reduced-precision reduction order and non-deterministic CUDA reductions are UNEXAMINED. **NOT PINNED means "not among these three", never "no cause exists".**
- **The 78.750 px outlier was not investigated** -- it is a max over 1243 pairs whose median is 0.000 px, and this row did not look at which box it is.
- **G324's inherited figures were NOT reproduced from artifacts** (the batch rows were never written) and were NOT re-measured on G324's clips (deleted). The pairing-artifact explanation is CONSISTENT WITH them, not a proof about G324's code.
- **The realised corpus was not chosen and cannot be re-selected.** A rerun of the sealed rule on a rotated corpus will pick different clips; only the committed per-frame hashes make THIS construct reproducible.
- Determinism was measured **in one process on a card shared with a 16-worker daemon**; it says nothing about across-process or across-driver repeatability.
- ms/frame (`single` 8.74-11.85, `batch8` 2.97-8.85, `fp32` 3.04-13.71) is contended-card, warm-up-included and **not a benchmark**; it is reported because it was recorded, and no cost claim rests on it.
- ONE corpus, three clips, 120 frames, one GPU, one driver, one ultralytics version, one seed.

**ARTIFACT SHA-256 (all under `docs/evidence/tracking/g327_artifact/`).** `g327_summary.json` 24,035 B `d4b5b1a27cef14e7d3a7ba400326e67e7fdc22827e0ad1f487872a20d1848e70`; `g327_score.json` 29,534 B `b4d3b6ab5d0828ce660cddd1f3b0f7d15348aeb0e02c1310b94c3a3b9897aaf3`; `g327_boxes_g1.csv` 149,154 B `599923cc43b97d1585dbbea6b1a783d8b78b9177309d4b3f4b61a26476d94905` (2,268 rows); `g327_boxes_g2.csv` 172,444 B `93085be0631a13b02255947feabc74a9b226b7b32bf864afdff93e1a6a642703` (2,622 rows); `g327_boxes_g3.csv` 169,661 B `4110a137dc45f20ff17ba53b2d2420c585a1029553e8bb0f497d627418983dae` (2,580 rows); `g327_attempt1_source_absence.txt` 911 B `544e30676b99148c503d74906b7793bd23ed4124971b93c19c0e8fac3d282433`. **Every box behind every cell above is in those CSVs, in shortest round-tripping form, arm-labelled and in emission order, so a verifier can recompute bit-identity, the count deltas and the greedy IoU matching from coordinates alone.** **TIME.** About 2 h 45 min of lane wall (2026-09-08 00:20 - 03:05 CDT), of which roughly 2 h was `pod_run`'s own untimed `du` walk across six launches (about 20 min each on the MooseFS volume). The scored run itself was job `20260908025412_745458_5112`. Vocabulary follows contract Q6; automated scan required.
