# G340 Preregistration

Date: 2026-09-08. Scope: local environment only; no packages, route files, data, pod, or flags change.

Binding before-condition, run by the finisher after this file is sealed: `conda run -n basketball_ai python -c "from ultralytics import YOLO"`; `conda run -n basketball_ai python -c "import src.tracking.osnet_reid"`; `python -c "from ultralytics import YOLO"`; `python -c "import src.tracking.osnet_reid"`. Capture each complete traceback and quote its last 15 lines in the memo.

After condition, run only after the binding commands: repeat those four commands with the prefix `from scripts.platformkit.env_shim import prepare_route_imports; prepare_route_imports(); ` in each `-c` program. The table has exactly four rows: conda/system x YOLO/osnet_reid. `before_ok` and `after_ok` are integer 000000 or 000001. No metric comparison, evaluator, or scoring occurs in this row.

Decision rule: if all four binding imports succeed, report PREMISE FALSE and make no shim claim. Otherwise the intended workaround is accepted only if both imports succeed in conda after the shim; system success is recorded if it occurs, otherwise its exception is named and the verdict is PARTIAL. The shim must be a no-op when its preflight import succeeds.

Diagnosis inputs are the exact commands `conda run -n basketball_ai python -m pip show onnx ml_dtypes torch torchvision ultralytics torchreid tensorboard numpy` and `python -m pip show onnx ml_dtypes torch torchvision ultralytics torchreid tensorboard numpy`; their outputs are copied to the memo, not interpreted as package changes. The proposed commands are documentation only and must not be executed in G340.

The test is `python -m pytest tests/platformkit/test_g340_env_shim.py -q -p no:cacheprovider`, once and alone, after sealing. It verifies the current-interpreter route after the shim and a monkeypatched clean preflight no-op. EYE CHECK: NONE.
SEAL sha256 b024ba52bfda1b38e06038d28b79d8f86c79ee21820fd542c7544e245104dcd7
