VERDICT: ACCEPT WITH CORRECTIONS
Candidate 752c616ed (branch track-a6, on cfd98e657 / 68fa15009). Verified from scratch by Opus; the prior codex verifier run on this candidate is void (it re-reported attempt 1). Branch-only diff = 8 files, 709 insertions, 0 deletions (git diff --stat master...HEAD).

TESTS (run from the worktree root; every module the test imports -- tracking_harness.py, tracking_schema.py, liveness_metrics.py, attempted_frame_count_source.py, metric_local_profile.py -- is byte-identical to master, so this is master-equivalent. A1 cannot be run literally in master: the candidate module does not exist there.)
  python -m pytest scripts/platformkit/tracking/test_g305_additive_registration_verdict.py -q -p no:cacheprovider  -> 1 passed in 2.39s
  python -m pytest tests/platformkit/test_loc_rail_scope.py -q -p no:cacheprovider                                 -> 1 passed in 1.99s

PREMISE (Q8, step 0): PASS with correction 1.
  1 PASS tracking_harness.py:272 is "Return self-consistency health metrics for a recognized tracking table."; :395 passes a literal True for self_consistency_only beside passed, so it survives passed=True.
  2 PASS tracking_harness.py:25-28 bounds (0,94,0,50), min_players 6, coverage_min 0.60, oob_max 0.05, jump_max_max 6.0, min_median_track_len 3.0; ball at :332,:366,:375; under-30-frame override at :257-262 and :405-408.
  3 PASS domains/basketball/tracking/geometry.py:28-29 gates x <= COURT_WIDTH_FT (50) and y <= COURT_LENGTH_FT (94) against the harness x <= 94, y <= 50.
  4 FALSIFIED IN THIS TREE (the spec permits a different hash): recomputed harness SHA-256 = c5a86154da32177f00b72c8b54651ce73b4d68c48001348848ff4df3c6bd2f95, identical in this worktree and in master (git diff master -- scripts/platformkit/tracking_harness.py is empty). No 8217037... file exists here, so the spec's "WT omits median track length / has no attempted-frame gate" reading describes nothing in this tree.
  before-clause PASS: outside this row's own artifacts, no ledger row carries liveness_sha256 or producer_sha256 (grep over docs/evidence).

ACCEPTANCE RULE
  metric (a) PASS -- I rebuilt the reflection myself (x' = 94 - x, every other column asserted equal) and ran the unchanged harness: original PASS / passed=True / failures=[] / self_consistency_only=True; reflected PASS / passed=True / failures=[] / self_consistency_only=True. g305_additive_registration_verdict.py:92-111.
  metric (c) PASS -- 4 named corners plus the asymmetric interior point (13.25,72.5) <-> (72.5,13.25) round-trip in both directions, worst error 0.0 ft. verdict.py:62-69, test:44-54.
  metric (b) PASS -- the record emits registration_passed=null / player_tracking_passed=true / ball_tracking_passed=true, q / r / q_times_r (all null) with "q * r >= 0.60", the four hashes, and a mode block with the sealed 30-frame manifest b1ac28da603c0bfee91b59a30280a2b861ab0e781aec366949a2fdad61b64b96, alongside harness_verdict_json byte-identical to a fresh report.to_json(); parsed key sets equal, 0 keys missing. verdict.py:148-172.
  bar PASS -- two independent run_construct() calls are byte-identical; round-trip 0.0 <= 1e-9 ft; all three committed JSON artifacts equal a fresh run exactly.
  n PASS (Q7) -- CONSTRUCT_CASES enumerates exactly 4 corners + 1 interior point + 2 harness runs + 1 worked record = 8/8, enumerated not sampled. verdict.py:29-38.
  eye check n/a (no frames, no renders); A2 reproduction performed above.
  must not move PASS (B10) -- harness SHA-256 before == after == master; no branch commit touches tracking_harness.py, SHARED_MODULE_TOKEN.md, TRACKING_GAPS_2026-09-01.md, src/, domains/ or kernel/; CONFIG_VERSIONS 2026-09-01-v1 unchanged; RESULTS_LEDGER.md append-only (+2 / -0).
  NON-TAUTOLOGY PASS -- all 30 sealed manifest frames stay in the denominator; nothing excluded.

CONTRACT: B1 PASS (no exclusion) - B2 PASS (key-set equality, nothing renamed or removed) - B3 PASS (absent packet yields null, not False) - B4 PASS - B5 PASS (local only, no pod) - B6 PASS - B7 PASS - B8 PASS - B9 PASS (30 unique frame ids) - B10 PASS. Q1/Q2/Q4/Q5 n/a (nothing scored) - Q3 PASS (12 px, 24 px, 1e-9, 0.60 and x' = 94 - x byte-identical to the spec) - Q6 PASS (token grep clean) - Q7 PASS - Q8 PASS. A5 PASS (no reader outside this row). A7 PASS (all four memo-named paths exist). A12 PASS (203 and 86 LOC, both <= 300; no allowlisted file grew; rail green).  NOT VERIFIED list PASS -- all four required items present at memo:174-182.

CORRECTIONS (documentation only; no code change is needed to land)
  1 memo:22-24 -- replace "The named alternate WT source is absent ... read from the prior memo and not re-diffed" with: premise 4's WT reading is FALSIFIED here, because this worktree's harness is byte-identical to master at c5a86154..., so there is no second file to diff.
  2 memo -- name the denominator: ball_tracking_passed is computed from ball_valid_pct (emitted_frames denominator, verdict.py:154-156), not from ball_valid_attempted_frames_pct; the two coincide at 1.0 only because this construct emits every attempted frame.
  3 memo -- state that player_tracking_passed mirrors the harness passed field verbatim (verdict.py:161), so it also carries ball and liveness failures and is not a player-only bar.

PROPOSED RESULTS_LEDGER line:
2026-09-07 | basketball | G305 | 8/8 CONSTRUCT reproduced independently: the original and the x'=94-x reflected table both PASS the unchanged harness (failures [], self_consistency_only true on both), 5 named axis points round-trip at 0.0 ft against a 1e-9 bar, and the additive record emits registration_passed=null / player_tracking_passed=true / ball_tracking_passed=true with q, r and q*r null, the four code hashes and a sealed 30/30-frame manifest alongside a byte-identical copy of the original verdict JSON; harness SHA-256 c5a86154da32177f00b72c8b54651ce73b4d68c48001348848ff4df3c6bd2f95 unchanged before and after. No real registration result was scored. | ACCEPT WITH CORRECTIONS (verified: Opus, contract A/B/Q) | 752c616ed

NEW GAP: G305-N1 -- the homography test asserts only harness_to_geometry_homography(geometry_to_harness_homography(M)) == M; the axis swap is an involution, so an identity implementation would pass it too. Bind the matrix transform to the point transform instead (test:55-56).
NEW GAP: G305-N2 -- two spec (b) prose requirements are absent from the memo: that even a perfect map cannot satisfy q * r >= 0.60 when q < 0.60, and that whole-broadcast yield is published as a separate denominator from eligible-frame yield. Outside the ACCEPTANCE RULE, so filed rather than rejected.
NEW GAP: G305-N3 -- the falsifier's ball row sits at x = 47.0, exact court centre, so it is invariant under x' = 94 - x and carries no end-identity signal; only the six player tracks discriminate. Move the ball off centre in any future falsifier.
NEW GAP: G305-N4 -- H1 inside the wrapper: ball_tracking_passed divides by emitted frames because it reads ball_valid_pct, while the H1-compliant ball_valid_attempted_frames_pct sits in the same record.
