# G314 preregistration -- sealed BEFORE any count is computed (2026-09-07)

Sealed by the Opus builder in worktree a19 at base commit b3e2f01ba, BEFORE any
per-game count for G314 existed. Everything below the seal line is a hash of
everything above it. LOCAL row: the pod is READ-ONLY, no GPU, no GPU lease, and
`track_daemon` (pid 25560) is never stopped or signalled.

## Disclosure made before the seal (honesty over theatre)

Two facts were observed before this file was written and are recorded here so the
seal is not read as covering them:
1. `scripts/platformkit/tracking/g309_multigame_census.py:181` reads the ball
   columns from `ball_tracking.csv`, NOT from `tracking_data.csv` as the G314 spec
   text says. Both files carry `ball_x2d` / `ball_y2d`; only `ball_tracking.csv`
   carries `ball_inferred`. The premise is therefore measured on BOTH files, and
   the census-source file (`ball_tracking.csv`) is the one the acceptance rule binds.
2. `head -5` of the `ball_inferred == 1` rows of `wnba_05/ball_tracking.csv` was
   printed while locating the columns. NO count, share or aggregate was computed
   before this seal.

## What is fixed before any number exists

1. PREMISE, binding, from the spec verbatim: on `wnba_05` and `wnba_02`, print
   `ball_rows`, `ball_detected`, `ball_inferred`, and the count of `ball_inferred`
   rows carrying a finite `ball_x2d` AND `ball_y2d`. **If that last count is not 0
   on both games, the premise is FALSE: STOP, write the memo, commit, report
   PREMISE FALSE.** No fix, no adoption, no threshold moves on a FALSE premise.
2. UNIT OF ANALYSIS: one row of `ball_tracking.csv` = one emitted ball row. Counts
   are over ALL rows of the file, head to tail, no sampling and no exclusion.
3. DEFINITIONS, fixed now and byte-identical to the G309 census reader:
   `ball_rows` = data rows in the file; `ball_detected` = rows whose `detected`
   field is one of "1", "1.0", "True", "true" after strip; `ball_inferred` = the
   same test on `ball_inferred`; a coordinate is FINITE when `float()` succeeds on
   both `ball_x2d` and `ball_y2d` and neither is NaN or +/-inf; `ball_valid_share`
   = rows with a finite pair over `ball_rows`.
4. INPUTS, fixed now (A9): the pod files
   `/workspace/nba-ai-system/data/tracking/wnba_05/ball_tracking.csv` (24,473 B) and
   `.../wnba_05/tracking_data.csv` (1,759,850 B), `.../wnba_02/ball_tracking.csv`
   (23,410 B) and `.../wnba_02/tracking_data.csv` (2,398,304 B), fetched by `scp`
   into a scratchpad outside the repo. Byte sizes are asserted against the pod
   listing before counting; a mismatch is a STOP.
5. NO PASS BAR ON THE PREMISE. A FALSE premise is a VALID result (contract Q8) and
   is reported as a success, not a failure.
6. SCOPE, fixed now: image space only (`coordinate_space = image_px`). No court,
   foot, metre or registration claim. **A coordinate is not a correct coordinate:**
   this row measures neither recall, precision, accuracy nor registration, and no
   ball position in it has ever been checked against an image. No frames are
   decoded and no renders are produced -- eye check NONE.
7. `src/`, `domains/`, `api/`, `kernel/`, `intel/` are READ and IMPORT only. Any
   fix that would touch them is written as a PROPOSED diff under
   `docs/research/organization-sprint/` and applied NOWHERE.
8. No threshold, production default, register row or `TRACKING_GAPS` line is
   edited by this row.

## SEAL
SHA-256 of the LF-normalized bytes above this line:
47bc38d36a4c5612fc6c5473983893a58fdcd04a6cbe08c6decb564efa1ee81e
