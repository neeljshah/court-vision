# G309 preregistration -- sealed BEFORE the pod run (2026-09-07)

Sealed by the orchestrator/builder in worktree a12 at base commit 08b103a86, BEFORE
`scripts.platformkit.tracking.g309_multigame_census` was executed against the pod's
`data/tracking` tree. Everything below the seal line is a hash of everything above it.

## What is fixed before any pod number exists

1. PREMISE, binding: ledger lines in `data/tracking/track_daemon_ledger.jsonl` with
   `passed != null` must be >= 6, printed at census time. Below 6 -> PREMISE FALSE, STOP.
   Orchestrator measured 8 at allocation (16 lines: 8 thin/null, 8 tracked/false).
2. UNIT OF ANALYSIS: one census row per `data/tracking/<game>/` directory holding a
   `tracking_data.csv`. A game with a CSV and no ledger line still gets a row, marked
   `ledger_row=absent`. No game is dropped for any reason.
3. METRIC LIST AND DENOMINATORS, fixed now: rows; frames_emitted (distinct `frame`);
   frames_attempted (harness/ledger `evaluated_frames`, else ceil(decoded/stride));
   coverage_attempted_frames_pct = frames_emitted/frames_attempted;
   coverage_decoded_pct = frames_emitted/decoded_frames; distinct_track_ids;
   median_track_len_rows; id_churn_per_detection = distinct_track_ids/rows;
   ball_rows / ball_detected / ball_inferred / ball_none; ball_valid_share = rows with a
   finite ball_x2d AND ball_y2d over ball_rows; n_steps; p95_disp_norm; zero_step_share;
   share_frames_ge6_boxes = frames with >= 6 player rows over frames_emitted.
4. CONVENTIONS, fixed now: footpoint = bbox bottom-centre ((bbox_x1+bbox_x2)/2, bbox_y2);
   a step is a consecutive observation of the SAME player_id ordered by frame, with NO
   interpolation across a frame gap; displacement is Euclidean image px divided by the
   CSV's `source_height`; p95 is NEAREST-RANK, sorted[ceil(0.95*n)-1], no interpolation;
   zero-step means displacement exactly 0.0.
5. LEDGER AGREEMENT, fixed now: recomputed `rows` must equal the ledger `rows` EXACTLY;
   recomputed `coverage_decoded_pct` must equal the ledger `coverage_pct` at 4 decimals
   (the ledger stores it rounded, so 1e-9 equality is not attainable). A disagreement is
   REPORTED as a NEW GAP, never patched.
6. NEW GAP TRIGGERS, fixed now and evaluated automatically: ball_detected == 0; an
   id_churn_per_detection above 2x the cross-game median; a zero_step_share above 2x the
   cross-game median; a rows or coverage disagreement with the ledger; a CSV with no
   ledger row.
7. NO PASS BAR. This row is descriptive. Finding nothing anomalous is a FULL SUCCESS and
   will be reported as one. No threshold in this file may be moved after the seal.
8. SCOPE: image space only (`coordinate_space = image_px`). No court, foot, metre or
   registration claim. No ground truth of any kind exists here, so no metric is recall,
   precision, accuracy or registration; every one is an internal-consistency proxy.
9. THE POD IS READ-ONLY for this row. Nothing under `data/tracking/` is written, deleted
   or renamed; the running `track_daemon` (pid 25560) is never stopped or signalled. No
   GPU is used and no GPU lease is taken.
10. The test `tests/platformkit/test_g309_multigame_census.py` (3-game synthetic
    construct) passed BEFORE the pod run: 1 passed.

## SEAL
SHA-256 of the LF-normalized bytes above this line: f79cb63ab17a93d1b219b2e020af68c764d670241189d40a61bf0f387eca50e0
