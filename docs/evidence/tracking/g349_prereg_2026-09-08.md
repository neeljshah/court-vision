# G349 preregistration - ball ownership survival (2026-09-08)

Scope: preparation only in `C:/Users/neelj/nba-track-a16`, LOCAL conda `basketball_ai`; no real-window execution in this lane. This artifact cites `docs/evidence/tracking/VERIFIER_CONTRACT.md` sections B and Q.

Binding diagnosis: rerun the G344 frozen 24 NBA windows under its committed lexical every-15th selection and frames 600-659 inclusive. For every integer frame F, record these cumulative stages without excluding missing rows: `frames`, `any_ball_row`, `detected_ball`, `player_rows`, `player_within_radius`, `prerequisite_available`, `motion_agreement`, `accepted_ownership`. `any_ball_row` means a raw ball-table row exists; `detected_ball` means a detected row has finite ball_x2d and ball_y2d; `prerequisite_available` means the selected nearest player and exactly one finite ball point each exist at frame-3 for the same player id. The first below-0.10F stage decides the stated diagnosis only when it is `any_ball_row`; otherwise no join defect is declared.

Frozen state rule: retain G344 radius `60 * source_height / 720`, candidate ambiguity margin 10 pixels, 3-frame history, cosine >= 0.5, magnitude ratio [0.5, 2.0], MAX_AGE 30, and no cross-cut ownership. `prerequisite_available` is additive provenance only; it changes no state, reason, threshold, or owner decision.

Resealed evaluation rule for the finisher: before a real comparison, write and seal the resolved >=30 live-window list from >=3 games spanning NBA and WNBA. Retain every full frame in F, require >=100 eligible observed-ball motion pairs in total, and use one evaluator state per stable window-frame tick. Report full-frame and eligible-observed-pair denominators together for baseline, observed-only and inferred-inclusive ablations, plus shifts -30 and +30 frames. The sealed shuffle bar is a >=200 additive per-mille (20 pp) drop in motion agreement; it is reported as met or not met, never changed.

Outputs: survival.csv keeps counts as zero-padded integer cells and shares as additive per-mille columns. resealed_states.csv retains every existing G344 state column and adds `prerequisite_available`; per-row records must include stable window_id and frame keys. The test seal checker normalizes CRLF to LF and hashes bytes above the seal line, never HEAD content.

Sign convention: improvement = baseline loss minus candidate loss; positive = candidate better. This preparation performs no scored comparison and makes no calibration claim.
SEAL sha256 1465df2d7c688c8bb3ef822fd81fd43eec6ce11eed0dbb417933e52924e599c0
