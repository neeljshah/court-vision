VERDICT: DONE, HEADLINE REVISED AT FIX PASS 1b -- the declaration DOES over-count (NOT_SCHEDULED 0.428166 SEALED34 / 0.406057 FRESH69, UNKNOWN 0.000000 against the <= 0.05 bar, all three controls exact, the controlled re-run reproducing its own schedule 1.000000, 0 src edits and 0 thresholds moved), but under the G370 M1 route that over-count explains only 0.610361 (SEALED34) and 0.531196 (FRESH69) of G370's gap: 0.483449 and 0.603362 of the ticks the producer ACTUALLY evaluated still carry no M1-admitted player observation. The attempt-1 categorical claim "a scheduling mismatch, NOT a producer defect" is WITHDRAWN; it was computed over RAW `tracking_data.csv` frames, not over M1.

# G376 declared-tick observation audit
Row G376 | pod worktree `/workspace/wt/a12`, branch `track-a12` | Spec `docs/evidence/tracking/specs/G376_spec.md` VERSION 2026-09-10 | contract A/B and Q1/Q3/Q6/Q7/Q8/Q9 plus A9/A11/B11/H1/S1/S4. Prereg `g376_declared_tick_observations_2026-09-10/g376_prereg_2026-09-10.md`, `SEAL sha256 32bcad44e602ac870f06e02cf5118272bf1343797906905f0ed4d8d4705173ee`, committed ALONE as `3546f3b3` at 20:22:33 UTC before any number existed, and NOT edited by the fix pass. Fix pass 1b (after `G376_VERIFY_att1_REJECT_2026-09-10.md`) adds `g376_amendment_A1_2026-09-10.md`, `SEAL sha256 d5e8a9214995ab6ac48c55953543958e5000c68515751c49693022c5b2f260c4`, likewise committed ALONE as `b2677399` at 21:13:35 UTC before the first M1 number existed (Q1). Builder and finisher: claude-opus. CPU only for every archival step (threads = 1, nice 10); ONE GPU re-run, not repeated by the fix pass.

## Inputs (A9), immutable
`data/g376_snapshot/` (gitignored), built once: 103 sections, ALL 103 `tracking_data.csv` hashing to their landed G370 pin (`snapshot_sources.csv`), 62 with their route sidecar and verdict beside the pinned tables and 41 whose tables came from `/workspace/g370_snapshot` and therefore carry no sidecar. Re-verified at the start of fix pass 1b against the landed `snapshot_manifest.csv`: 330 present files, 0 hash mismatches, 82 recorded absences still absent. Every snapshot file was also re-hashed after the original scoring: 0 of the 330 present files of 412 moved (`snapshot_manifest.csv`, `snapshot_manifest_after.csv`). Re-run source `/workspace/g376_scratch/wnba__G8bPOW37Sp0_s615.mp4`, 63816235 bytes, 1920x1080, 3886 container frames, sha256 `3af384191a6083714de2ced41996374d4cf1641c147af3ef04ec418b33bbfa36`. Route identity (A11), byte-identical worktree and deploy: `src/pipeline/unified_pipeline.py` `c4eae385bfeebd4e44c8161c791ef7ed4589697020f424d3dde7aec6816d9226`, `scripts/run_clip.py` `ccd08d32c8c4cb8625ec5ef67535206f74f4df307a8e191ec01f5700fb40d8c2`.

## 1. Premise (Q8) -- HOLDS, and G370's headline reproduces exactly
Recomputed from the snapshot by IMPORTING G370's own route (`load_merged`, `adapt_production_section`, `construct_arm("A0")`, `collapse_held`, `cls == "player"`): M1 zero-observation ticks 32158/45842 = 0.701496 on the sealed 34 and 68534/89655 = 0.764419 on the replacement fresh 69 -- the same tick counts G370 published as 0.7014 / 0.7644 (A2). Non-integral `decoded_frames / evaluated_frames`: 30 of 34 sealed, as G370 reported, and 37 of 69 fresh. The RAW share, over distinct `tracking_data.csv` frames before the M1 collapse, is 0.459513 and 0.428074; both far above the 0.10 stop, so the row proceeded. Artifacts `premise.csv`, `premise.json`.

## 2. The reconstructed rule (`schedule_rule.md`, every clause cited and re-read)
DECLARED is `[index * stride for index in range(evaluated_frames)]` (`g370_manifest.py:31-36`) over ledger fields the daemon copies from the harness verdict (`track_daemon.py:316-318`), where `evaluated_frames = len(range(0, min(decoded, stride * 30000), stride))` (`track_daemon_done.py:141-149,203-209`) and `stride = max(1, round(fps * 0.1))` (`tracking_timebase.py:30-37`). It is arithmetic over the SOURCE. The producer evaluates a frame only when it is on ITS lattice (`unified_pipeline.py:271-278`, stride from `:1532-1533`, a fixed 3 below 35 fps), AND passes the gameplay gate (`:1700`, `:996-1036`), AND is not suspended (`:1836-1853`), AND arrives before the cap, which counts EVALUATED frames rather than source frames (`:1536-1537,1680`; `run_clip.py:417`). Its true count exists (`:2079-2080`, returned at `:3063`, written into the sidecar by `run_clip.py:226-244`) and no consumer reads it. `ball_tracking.csv` carries one row per frame reaching the tracking body -- `live = 1` evaluated (`:1991-2001`), `live = 0` suspended (`:1836-1849`) -- so the producer schedule E is directly observable; `tracking_data.csv` gets a row only per surviving non-referee track (`:2657-2662`).

## 3. Classification of every declared tick (`ticks.csv` 135497 rows, `classification.csv`)
SEALED34, 45842 declared ticks over 34 sections: OBSERVED 0.540487, SCHEDULED_NO_DETECTION 0.031347, NOT_SCHEDULED 0.428166, UNKNOWN 0.000000. NOT_SCHEDULED sub-reasons: BEYOND_ROUTE_CAP 11763, OFF_PRODUCER_LATTICE 5000, GATED_OR_UNREACHED 2865, SUSPENDED 0.
FRESH69, 89655 declared ticks over 69 sections: OBSERVED 0.571926, SCHEDULED_NO_DETECTION 0.022018, NOT_SCHEDULED 0.406057, UNKNOWN 0.000000. Sub-reasons: GATED_OR_UNREACHED 17149, BEYOND_ROUTE_CAP 13740, SUSPENDED 5516, OFF_PRODUCER_LATTICE 0.
These four classes are RAW: OBSERVED means at least one row in `tracking_data.csv`, which is NOT the same thing as an M1 player observation (section 5b). OFF_PRODUCER_LATTICE is a genuine stride disagreement on five sealed sections at 25.0 fps: the adjudicator declared stride 2, the producer used 3, so 1000 declared ticks per section name frame indices that were never decoded.

## 4. Controls (`controls.csv`), all exact, none a tolerance
a RE-RUN REPRODUCTION: the producer's own logged evaluated indices against the `live = 1` frames of the run's own ball table -- 1000 vs 1000, intersection 1000, symmetric difference 0, reproduction 1.000000, and its sidecar independently records `evaluated_frames = 1000` against a declaration of 1296 (`rerun_summary.json`). The deployed route has no schedule log, so `g376_rerun.py` wrapped `UnifiedPipeline.run` FROM THIS LANE at import time and read `results["predictions"]`; the deploy tree was not edited and its SQLite path was redirected into the scratch so nothing under `/workspace/deploy` was written. The producer was NOT re-run by fix pass 1b; that run stands, and `rerun_log.txt` now records the full argument vector including the `--log` and `--summary` arguments that `g376_rerun.build_parser` requires, with a note naming exactly which of them are evidenced and which is not.
b FULLY OBSERVED SYNTHETIC: OBSERVED 1.000000, other classes 0, zero-observation ticks 0.
c DOUBLED DECLARATION: NOT_SCHEDULED 0.500000 and OBSERVED 0.500000 exactly, SCHEDULED_NO_DETECTION 0, UNKNOWN 0.

## 5a. Consequence -- the RAW denominators (`consequence.csv`, `summary.json`)
SEALED34: declared 45842, producer-evaluated |E| 28276, route-capped 32200, RAW distinct table frames 26683 (24777 of them on the declared lattice, 1906 off it). The RAW zero share restates from 0.459513 over the declaration to 0.056338 over |E|. FRESH69: declared 89655, |E| 53250, route-capped 20500 over the 28 sections that carry a cap, RAW 51276; 0.428074 restates to 0.037070. **These two numbers are RAW `tracking_data.csv` frame shares. Attempt 1 labelled them G370 M1 shares; they are not, and the correct M1 numbers are in 5b.** The columns `observed_ticks`, `zero_share_over_declared` and `zero_share_over_producer_evaluated` keep their names and their values (AMENDMENT A1.2, B2); `summary.json` carries the same statement in `raw_column_semantics`. Sections whose ledger `evaluated_frames` should be restated: 34 of 34 and 69 of 69 -- every section in both sets, because the field is computed arithmetically and never from the producer. G368's motion share is a per-consecutive-pair quantity over RAW OBSERVED ticks; its pair denominator is 0.943595 (SEALED34) and 0.962881 (FRESH69) of the producer-evaluated pair denominator, so that row's medians are computed over 26649 of 28242 and 51207 of 53181 available pairs.

## 5b. Consequence -- the CORRECTED G370 M1 denominators (AMENDMENT A1.1; `m1_denominators.csv`, `m1_denominators.json`)
M1 is the G370 M1 player-frame set, built by IMPORTING `load_merged` -> `adapt_production_section` -> `construct_arm(table, "A0")` -> `collapse_held` -> `cls == "player"`, the construction `g370_scorer.py:104-117` performs; nothing is copied. E is the section-2 producer-evaluated schedule. 0 of 103 sections lacked an E.

| set | G370 published, over the DECLARATION | corrected, over E = producer-evaluated | size of E | size of M1 and E |
|---|---|---|---|---|
| SEALED34 | 32158 / 45842 = 0.701496 | 13670 / 28276 = **0.483449** | 28276 | 14606 |
| FRESH69  | 68534 / 89655 = 0.764419 | 32129 / 53250 = **0.603362** | 53250 | 21121 |

Decomposition of G370's M1 gap (every NOT_SCHEDULED tick is necessarily M1-zero, because the M1 frames are a subset of the `tracking_data.csv` frames): of SEALED34's 32158 M1-zero declared ticks, 19628 = 0.610361 were never evaluated by the producer and 12530 = 0.389639 were evaluated -- 1437 of those with no track row at all and 11093 with RAW rows that do not survive the M1 held-collapse. Of FRESH69's 68534, 36405 = 0.531196 were never evaluated and 32129 = 0.468804 were evaluated, 1974 with no track row and 30155 lost to the held-collapse. So the declaration over-count is real and is the largest single component of G370's gap, but it is a MAJORITY, not the whole, and the remainder sits on frames the producer genuinely evaluated. This row does not adjudicate whether that remainder is a detector defect, a held-collapse doing its job, or frames legitimately empty of non-referee players; it shows only that it is not explained by the declaration.
`PROPOSED_evaluated_tick_ids.md` carries three additive proposals, applied to NOTHING: P1 write `evaluated_tick_ids` in the route sidecar; P2 carry the already-computed `attempted_frames_capped` into the ledger entry; P3 the stride disagreement, left for a human because either side could move.

## 6. Eye check, tests, vocabulary, machine
10 strips spaced evenly across the whole 135497-tick ordering, each at most 5861 bytes, together carrying 441 OBSERVED, 74 SCHEDULED_NO_DETECTION, 160 NOT_SCHEDULED/BEYOND_ROUTE_CAP and 125 NOT_SCHEDULED/GATED_OR_UNREACHED ticks (the SUSPENDED and OFF_PRODUCER_LATTICE sub-reasons fall outside these ten windows and are read from `ticks.csv`): `strips/`. Tests, ONE FILE PER COMMAND:
`PYTHONPATH=/workspace/wt/a12 python3 -m pytest tests/platformkit/test_g376_declared_tick_observations.py -q -p no:cacheprovider --basetemp /tmp/g376fix3` -> 13 passed in 2.09s
`PYTHONPATH=/workspace/wt/a12 python3 -m pytest tests/platformkit/test_loc_rail_scope.py -q -p no:cacheprovider --basetemp /tmp/g376loc` -> 1 passed in 3.45s
Contract Q6 scan, patterns built from single characters at runtime (word-boundary for the five vocabulary words, literal for the six retracted figures and the currency sign), over every file fix pass 1b touched plus the appended ledger row: **13 files scanned, 0 hits**; the run is pasted verbatim in `q6_scan_fix1b.txt`. A substring-only variant of the same scan reports 15 apparent hits, every one of them the letters inside the column identifier `ledger`, which the Q6 NOTE of 2026-09-04 exempts as an opaque identifier.
Wall time: attempt 1 about 55 min (prereg commit 20:22:33 UTC); fix pass 1b about 40 min from the REJECT being read to this commit, of which the M1 pass over all 103 sections was about 4 min on CPU. Every module stays <= 300 lines (`g376_m1.py` 120, `g376_report.py` 194, `g376_rerun.py` 119, test 203), so no `test_loc_rail_scope.py` allowlist entry moves (A12).

NOT VERIFIED: any calibration or outcome conclusion (none was computed; no `walk_forward` call was made, so Q4 and Q5 are not engaged); WHY the M1 remainder of section 5b exists -- whether the held-collapse is correct on those frames, whether the detector failed, or whether the frames are legitimately empty -- which is a successor row, not this one; the sub-reason split on the 41 fresh sections that carry no route sidecar, where a cap-truncated tick is reported GATED_OR_UNREACHED rather than BEYOND_ROUTE_CAP; whether the 34 sealed sections truly had zero suspensions or their producer version predates the `live = 0` row; the repeatability of the single controlled re-run, which is n = 1 on one source (B11) and was not repeated by the fix pass; the exact scratch FILENAME the re-run `--log` was written to, which went with the scratch container under prereg section 1 (`rerun_log.txt` says so on its own line); and every G370 and G368 conclusion beyond the denominators restated here, which were not re-scored.

SHA-256 (fix pass 1b): `consequence.csv` `db0025b7723e13a7ddbcb1dd65f538dd67fba9a196f922db24865dcb9a501ad7`; `summary.json` `313367560d6c64b6da168edc57793fb05b940f6ab3c364a2bccf07b3cf07ae52`; `m1_denominators.csv` `61be1ee0101371bdfaf8afd5cafd1638fd5002e59e0bbde8b7a7d1bce29b2ad2`; `m1_denominators.json` `4acce1ed696e07659c3a8a7856480e995b5af75647ee32773bd1dd4ecdb86949`; `rerun_log.txt` `20f2092d5876d12551e2c268faf4aba0dfd18d8e3c7a5be12bec14c0b5412a16`; `g376_amendment_A1_2026-09-10.md` `686aeb42f0be9350d315cc72eef32601c7bee06ad3dfd2e3aa873dc339b09551`; `ticks.csv` `674c0eefd01eb5ec85934992be982600b4949ac2604a0d440f44a422fa3d174b`; `classification.csv` `3c8076584a532e3288427e4224b6e08f81010de20b4596b31563c4413e1650f1`; `controls.csv` `5d83da9e5ce8f985b4cbe91b12e854a1ec9f335c8df845c65685a1b365703ce3`; `premise.csv` `87680fa39a474880b872d3d2b877b3fe485e0d0c1a37a341902e001ab6e2d11a`; `rerun_summary.json` `c536680cc209b44f650f7d5951ef66465330b9f5d55ab3cf2975f3c69ee098fd`; `schedule_rule.md` `0ee606e53b1605198f3de45c02eec4457dc3692c31591645ad800078c2d70859`; `PROPOSED_evaluated_tick_ids.md` `f94808fc182e7fbd69eb00978e166c328d88e2a80e33f3f25b9983ce099d40e9`; `snapshot_sources.csv` `09a2c7ac630f6927b166262313c3f4801df43caa16621ed1c33c8a245840d9d4`; `g376_schedule.py` `e609ed21c6b95d1c592e446730daba47f4e7d4e749a0315b9e85c7579563e815`; `g376_classify.py` `9d37b1fe8dbf6fb2cdd764d08141ee3a5c14eb25d6967530d7611202ff835d46`; `g376_controls.py` `1a98452164c365b6ad777251807ec67028382a6c89130d83134cabda6fae11e9`; `g376_rerun.py` `ccb9e266238f6b3c129e11a5502dedbb177bf6087e204ac7624f465add75a69e`; `g376_report.py` `ba79383f2c2df22c0b49b0b0d17dc341668642561f0dce52aec69c36dc994a99`; `g376_premise.py` `bcc73d0dbda086927fa84e5883cb52f5c030315aa7815a6222dc2fb243ddab94`; `g376_snapshot.py` `35a6cb18fea6a8f7eda28f0dbe60e1cc8ccf119780a44a173bb33ed30e2bc88f`; `g376_m1.py` `a6a6d4aad33ef02a906e78b4d7cc6d2ffe742a7ed71391ee6eaf6abf2ada5ee3`; test `1a47bafac78ef97f7a8e9be560b852e9180d6935ee1faa605c52454932fee992`
