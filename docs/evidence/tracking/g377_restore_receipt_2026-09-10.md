VERDICT: CLOSED AT LIMIT (adjudicated) -- restore receipt complete; the sealed 8-native-pixel eye bar is unmeetable for the G370 closure (SVG-only evidence)
# G377 restore receipt over G363 / G364 / G367 / G370, plus a proposed retention contract
Spec `docs/evidence/tracking/specs/G377_spec.md` VERSION 2026-09-10. Contract `.../VERIFIER_CONTRACT.md` (A2, A7, A9, A11, A12, B1, B3, B7, B8, B9, B11, D2; Q1, Q3, Q6, Q7, Q8). Seed: `docs/research/astra_next_rows_review_2026-09-10.md`, RETAIN_AND_RESTORE.
Preregistration `.../g377_restore_receipt_2026-09-10/g377_prereg_2026-09-10.md`, `SEAL sha256 627133fcc95cfa9fcfc851ec95df9d4e8c19af46684613155ee16e1cebb5b696`, COMMITTED ALONE at `c35db2ae0` 15:36:50 CDT, before any number below existed (Q1). No bar, threshold or seal was moved (Q3, B10).
Machine (S1): the PC receiver, worktree `C:/Users/neelj/nba-track-a19` at master `8d68fffd1`, CPU only, one python process at a time, free RAM 4.0-4.3 GB throughout. Pod `213.192.2.120:40117` READ ONLY (`sha256sum`, `ls`) -- no byte written, created, moved or removed there. Local interpreter 3.10.0, cv2 4.11.0, numpy 2.2.6.
## Premise (step 0, sealed protocol) -- the recovery part is NOT vacuous
    closure  files named  present off-pod  present on pod  bytes restored  mismatch  missing
    G363          1039           1037            1036        161,814,683       0        2
    G364           278            273             273         13,597,319       1        5
    G367            23             21              20            148,690       0        2
    G370            51             47              47          1,446,467       0        4
1,391 files named over the four closures; 1,378 restored; 13 MISSING. The sealed stop rule (all four closures 100 pct present off-pod) did NOT fire, so the recovery part ran in full. The manifest also carries 74 non-file tokens the two sealed rules necessarily catch, labelled and kept rather than dropped: 39 NOT_A_PATH (ratios such as `125/125`), 21 DIRECTORY_REF (prose references to `src/`, `api/`, trees this row's closures never wrote) and 14 TEMPLATE_FRAGMENT (runtime filename patterns such as `strip_{:02d}.svg`). Expanding a DIRECTORY_REF outside the closure evidence tree would have put thousands of unrelated files in the denominator (B9); the restriction is disclosed in `g377_manifest.py` and changes no bar.
## Manifest, restore and byte verification -- 1,377 of 1,378 verified
Rules M1 (memo-named) and M2 (reader-opened) exactly as sealed, each entry carries every `file:line` reference plus additive `off_pod_source`, presence and digest fields; attempt-1 role and status strings remain unchanged. One disclosed deviation from the sealed resolution list, in one direction only: a UNIQUE-SUFFIX fallback resolves a shorthand token (`recovery.csv`, `g364_census.py`, `.../x.md`) when it is the suffix or basename of exactly ONE tracked path, inside the closure tree first and then repository-wide; an ambiguous token stays UNRESOLVED. Without it, present files read as lost, which is the worst error this row can make. It only finds files; it hides none.
Sources actually used, in the sealed priority order: S1 committed master blob 1,375 files; S6 gitignored local working tree 2 files (`models/weights/yolov8n_ball.pt`, `docs/research/astra_pod_loss_review_2026-09-10.md` -- both POD_ABSENT, held only on this PC); S4 synced backup 1 file (the pod ledger). S2 lane branches `pod/track-a7 a13 a10 a18` and S3 the private remote's twelve `backup/*` refs were available and consulted; nothing needed them, because master already carried every committed artifact. Zero network re-fetches of native sources, zero replenished held-out executions, zero deletions.
Every restored file was written into the isolated scratch root, READ BACK from disk and then checked three ways: V1 git object identity against the committed blob id (computed in process, equal to `git hash-object --no-filters`), V2 sha256 of the readback against the source bytes, V3 sha256 against the live pod copy where the pod still holds the path (1,373 of 1,378 answered). 1,377 VERIFIED on every applicable check. The single MISMATCH is V3 only and is named below.
## The one mismatch: a live file has no identity (measured, not argued)
`/workspace/data/tracking/track_daemon_ledger.jsonl` answered with THREE different sha256 values inside this one session: `4333934b...` at 1,510,452 bytes / 1,281 lines (the synced receiver copy, the bytes restored), then `4a22a981...`, then `fc766205...` at 1,523,029 bytes / 1,293 lines on the pod at the last read. The receiver's own synced copy also advanced between two G377 runs. V1 and V2 pass on it; V3 cannot, because the producer is still appending. This is a property of the path, not a restore failure, and it is exactly the case the retention contract's versioned-identity clause exists for.
## The four archived decisions, recomputed from restored bytes with the landed code
    closure  archived decision (recomputed by importing the landed scorer)                 fields agree  memo digests agree
    G363     held-out C0 = 0 TP / 549 = 0.000000 (A0), fp 8, 285 / 243 / 21                     9 / 9              9 / 9
    G364     development confusion: 125 COURT / 115 NON_COURT / 0 ABSTAIN of 240, kappa 0.916186  12 / 12          17 / 17
    G367     recovery from the four synthetic fixtures                                          8 / 10             2 / 5
    G370     usable-control rejection 0 / 51 = 0.0000 over 16 games, 69 rated                    7 / 7              0 / 0
G363 reproduces every archived cell including `fp_per_absent` 0.03292181069958848 to the last digit. G364 reproduces its confusion, its kappa and all seventeen digests its memo prints. G370 reproduces the aggregation at `g370_usable.py:66-70` exactly; its scoring pass is NOT re-run and no claim is made that it could be, because the tables `control_row()` reads live only on the pod snapshot.
G367 is where the receipt earns its keep. Seven of eight modulo cells reproduce exactly; `G1_SYNTH_QUAD corner4` gives 1.651641 here against the archived 1.651646, so `recovery.csv` is not byte-identical (`d68e2b6e...` against `a42b1a69...`). The cause is NOT the restore, and this is measured rather than asserted: `g367_search.premise()` renders its four fixtures from code with no input file at all, and two independent local runs produced the identical digest, so the difference lies between the pod's interpreter stack and this PC's. Separately, three digests in the landed G367 memo -- `g367_premise.json` `536710c905bfc17c`, `g367_search.json` `e3866835d3f58358`, `summary.json` `912ee59f72ae16e1` -- match neither the raw nor the LF-normalised bytes committed under those names (`27345916391b8cf2`, `f2ea9514158df52a`, `8185a48d77ced1b2`), while `recovery.csv` and `search.csv` in the same table match exactly. Master and the pod agree on all five files, so the memo's table is what has drifted.
## Missing off-pod: 13 files, each named with the row it disables (`missing.csv`)
Nine are POD_ABSOLUTE roots or scratch paths that the 2026-09-10 loss took with it: `/workspace/wt/a7`, `/workspace/wt/a13`, `/workspace/wt/a10`, `/workspace/wt/a18`, `/workspace/deploy`, `/workspace/nba-ai-system`, `/workspace/g364_scratch/ledger_snapshot.jsonl`, `/workspace/g364_scratch/weights/resnet18-f37072fd.pth`, `/workspace/g370_snapshot`. Four are unresolved names: `frames_scored.csv` (G363's memo states it was deliberately never written, so its absence is by design), `sweep.csv` (G367's noise sweep was never run), and `tracking_data.csv` / `ball_tracking.csv` (G370 per-section table BASENAMES opened at `g370_manifest.py:42-43`, `g370_premise.py:26`, `g370_scorer.py:138`; the 118 real identities are pinned with digests inside `fresh_sections.csv`, which restored and verified). No missing file was inferred, substituted or re-fetched.
## Eye check (REQUIRED) -- 8 restored sealed entries: 6 raster + 2 SVG
Two per closure by the sealed even draw (sorted native-pixel paths, index 0 and floor(n/2)); all eight are VERIFIED byte-exact. `pixel_kind` discloses six raster entries and G370's two SVG strips. G370 has no committed raster dependency, so its sealed "8 restored native pixels" bar is unmeetable; this receipt preserves the eight entries and lands CLOSED AT LIMIT by adjudication, without changing the bar.
## Retention contract -- `PROPOSED_retention_contract.md`, PROPOSAL ONLY
Six sections fixed by the prereg before measurement: identity before prune (fails closed); the receiver acknowledgement record, whose digest must come from a READBACK of the receiver's copy, never from the producer; reader leases for sealed sets; versioned identities for moved, re-supplied or appended sources; and a byte ledger derived from the roughly 41 GB quota and roughly 1.8 GB/h of table growth, which bounds INTAKE first and never destroys evidence. Nothing is wired, no prune runs, no flag moves, no `src/` hook, no register write. The whole four-closure receipt costs 177,007,159 bytes, about 0.43 pct of the quota.
## Wall time, artifacts and code identity (SHA-256 of LF-normalised bytes)
Wall time 15:24 to 16:03 CDT, about 39 minutes, of which the prereg occupied the first 12 and was sealed and committed alone at 15:36:50 before any measurement. Restore 1 min 5 s over 1,378 files; recompute 29 s including two G367 runs; no GPU, no daemon touched.
 12aecb4ba9aa86a9206799dd78da26ec5696621c1c294f241f13bbd3a663f6d5 dependency_manifest.csv | 390ecacdb44f303a5272836688748697607d096f463e48f1a59b3f3eb4a1f0bc restore_hashes.csv | e8ed863efb751ec476bc5ed8c83a3ba3f9d9340a2d632142c35f11c7fc4986c1 decision_diff.csv
 956722b3d6e7a6be5e350aa7aa97df5af0a26ae5f09f074fc0d2e3cca16e369b missing.csv | 97c2933ab366da9c1c4a267a80fbe905cb0493228581e8bd3bf170f5c05a6f8c summary.json | 6ae39d369e04e69448ec2db1975d755032cf5ec052cee8dfed91dfc5dda9816b PROPOSED_retention_contract.md | 0db902142ef599428291223c36f4e785d9f7aa9662227c2c58777b37df0f2b3a eye_manifest.csv
Modules remain at or under the 300-line rail: `scripts/platformkit/tracking/g377_manifest.py` 271 lines, `g377_restore.py` 300, `g377_recompute.py` 208; test `tests/platformkit/test_g377_restore_receipt.py` 101. Every scorer is IMPORTED from the row that landed it (`g363_score`, `g364_score`, `g367_search`, `g370_usable`); none is reimplemented. Tests run per file only, never a full pytest: `test_g377_restore_receipt.py` 7 passed; `test_loc_rail_scope.py` 1 passed.
Nothing was written under `src/`, `kernel/`, `api/`, `intel/`, `data/registry/` or `data/` outside the isolated scratch root; no flag was flipped; the register was not edited; `git archive` was not used and the git force flag was never typed.
## Q6 vocabulary scan, pasted verbatim
SCAN_TOUCH_TOTAL 125 over 9 touched files, using the single-character Q6 patterns in `g334_seal.scan_text`.
All hits are pre-existing ledger prose or opaque identifiers in generated artifact fields; no new claim language is introduced. Opaque identifiers remain verbatim under the Q6 NOTE.
## NOT VERIFIED
- That anything lost is recoverable. This row restored nothing that was gone. It proved which NAMED bytes are still held off-pod and which are not, and the 13 missing files stay missing.
- WHY the G367 `G1_SYNTH_QUAD` cell differs. Two local runs agree with each other and the fixtures need no input file, which places the difference in the environment rather than in the restored bytes; the pod's own interpreter, cv2 and numpy versions were not read, so no specific cause is named.
- WHY three G367 memo digests do not identify their committed files. The disagreement is measured on master and on the pod alike; whether the files were rewritten after the table was printed, or the table was computed elsewhere, is not established here and no file was edited to close the gap.
- That the manifest is the true dependency closure. It is exactly what the sealed M1 and M2 rules find. A dependency a memo never names and no reader literal opens cannot enter it -- G367's 56 committed sheets are the demonstrated case -- so the closure is a lower bound on what each row actually needs.
- G370's admission scoring, and G363's and G364's held-out behaviour. Only one archived aggregate per closure was recomputed, from artifacts the rows already wrote; no arm, head, gate or rater was re-run, and no accuracy, calibration or tracking-quality claim is made or implied anywhere above.
- The proposed retention contract. It is untested, unwired and unadopted, and adopting it would not by itself improve any measured number.
