GAP S11 | sport all (signals) | worktree a13 | log cx_s11_grammar | creates the NEW package `scripts/platformkit/foundry/` (S12/S15 land beside it; the flat `foundry_runner.py` and `signal_foundry.py` STAY where they are)
REDTEAM PREREQUISITES (2026-09-03, REDTEAM_SIGNAL_FACTORY memo 187101ea -- binding for this pass):
- SF-5/SF-6: do NOT enumerate conditioning from regime_calibration.buckets at runtime (open alphabet, transductive terciles). The grammar takes a CLOSED conditioning alphabet declared in the family spec file (explicit frozensets); reusing a buckets key means snapshotting its exact value set into the spec.
- SF-8/SF-9: runtime_available comes ONLY from an explicit per-column declaration in the family spec; runtime_contract.classify_feature returning UNKNOWN (751 of 806 catalogue columns) is NOT runtime-available -- UNKNOWN enumerates for the teacher lane only.
- SF-12: enumerate_family raises ValueError on any off-grid param; the hash always uses the declared grid value, so two distinct proposals can never silently collapse.
- 7 of 42 catalogue parquets are ABSENT (soccer asof_discipline_features + 6 tennis _wta mirrors; schedule_density is ATP-only; serve_return_profiles has a tour column, no mirror) -- enumerate only what exists and print the absent list.
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): no hypothesis grammar and no dedup: `foundry_runner.py:23` rebuilds ONE hardcoded matrix and `signal_foundry.register` (:51) is hand-called, so throughput is 13 charged trials in the program's lifetime. (CORRECTION verified on disk 2026-09-03: the hardcoded `PASS_CONFIGS` matrix is at :20, not :23. Use the symbol, not the line.)
READ (read every signature on disk; line numbers drift): scripts/platformkit/regime_calibration.py `buckets(df) -> list[str]` -- the conditioning vocabulary is reused VERBATIM (`phase=<period|quarter|inning>`, `rest=B2B|RESTED|NORMAL`, `month=<YYYY-MM>`, `confidence=T1|T2|T3`, joined by `|`); scripts/platformkit/foundry_runner.py `PASS_CONFIGS`, `run_pass`; scripts/platformkit/signal_foundry.py `register`; data/registry/signal_registry.parquet for `signal_id` resolution -- READ ONLY, never written (the NBA registry is 86 rows, not 85, and `coverage_pct` is entirely NULL, so no coverage gate can run off it today).
PREMISE (step 0): `scripts/platformkit/foundry/` does NOT exist (verified 2026-09-03) and no module produces a hypothesis identifier -- before = 0 deduped hypotheses enumerable. If the package or a `semantic_hash` already exists, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): ENUMERATE THE CATALOGUES FROM DISK. For each parquet below, verify the path EXISTS and list its columns; a path that is absent is NAMED and DROPPED, never assumed. All of these were verified present 2026-09-03: gate spines `data/cache/combo/gate_corpus_{nba,mlb,soccer,tennis}.parquet`; `data/domains/basketball_nba/{asof_team_adv,asof_defender_rollup,boxdetail_asof,carryover_asof,asof_quarter_shape,asof_player_adv,player_value_features}.parquet`; `data/domains/mlb/{asof_inning,bullpen_relief_chains,catcher_framing_index}.parquet`; `data/domains/soccer/{asof_xg_proxy,style_fingerprints,referee_card_foul_profiles}.parquet`; `data/domains/tennis/{asof_features,asof_hold,asof_return,asof_setdetail,asof_meta,schedule_density,serve_return_profiles,travel_scouting}.parquet`; plus whatever `data/cache/pit/opp_allowed_asof_*` and `data/cache/ingame/*states*` actually match today (glob them; the plan's `asof_discipline_features` parquet is NOT materialized -- say so). Print the column count kept per file. If the surviving columns cannot reach the bar arithmetically (columns x 6 transforms x conditionings x 3 horizons), STOP and report CLOSED AT LIMIT with the arithmetic.
CHANGE (step 2): NEW `scripts/platformkit/foundry/__init__.py` and `scripts/platformkit/foundry/grammar.py` (<=300 LOC):
  @dataclass(frozen=True) Hypothesis(sport, feature, transform, params: tuple, conditioning: frozenset, horizon, market)
  semantic_hash(h) -> str; enumerate_family(spec: dict) -> Iterator[Hypothesis]
  transform in `raw | ew(halflife in {3,5,10,20}) | rank_in_league | z_vs_league | delta_vs_prior | ratio_to_opponent`; horizon in `pregame | period | live_tick`; market in `ml | total | spread | prop | inplay`; conditioning = `regime_calibration.buckets` keys VERBATIM as a frozenset of `k=v`.
  SEMANTIC_HASH RULES: SHA-256 of `json.dumps(canonical, sort_keys=True)` where canonical = sport; feature resolved to its `signal_registry.parquet` `signal_id` where one exists, else the raw column name; transform as an enum; params SNAPPED to the grid with unused params DROPPED (`raw` carries `()`); conditioning SORTED; horizon and market as enums. Argument order and unused parameters cannot move the hash BY CONSTRUCTION.
  `spec` = `{sport, columns|parquet, transforms, conditionings, horizons, markets, family, runtime_available}`; `family` is the FWER family (S13 stores it, S14 prices it); `runtime_available=False` columns enumerate for the teacher lane only.
TEST: NEW tests/platformkit/foundry/test_grammar.py (with `tests/platformkit/foundry/__init__.py`; the foundry package is not eval_gate, so its test lives under tests/): permuted conditioning plus an unused parameter hash IDENTICALLY; one grid step apart hash DIFFERENTLY; enumeration over the on-disk catalogues yields >= 1,000 distinct rows with 0 hash collisions. Run ONLY that file: `python -m pytest tests/platformkit/foundry/test_grammar.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = deduped distinct hypotheses enumerated; denominator = the enumeration over the on-disk catalogues verified in step 1
  before        = 0 (`scripts/platformkit/foundry/` absent; `foundry_runner.PASS_CONFIGS` rebuilds ONE hardcoded matrix)
  bar           = >= 1,000 deduped distinct rows AND 0 collisions on the permutation test
  n             = 1,000 (scored)
  eye check     = n/a (S-row); reproduction = verifier re-runs `enumerate_family` over the same catalogue spec in master and recounts distinct `semantic_hash` values and collisions
  must not move = `regime_calibration.buckets` and its key vocabulary, `signal_foundry.register`, `foundry_runner.PASS_CONFIGS` (S16 changes the runner, not this row), every threshold under scripts/platformkit/eval_gate/, data/registry/** (READ ONLY, never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows)
NON-TAUTOLOGY: the denominator is every column of every parquet that EXISTS on disk; a parquet that is absent is named and excluded in the memo, never silently dropped to make the count look clean, and no hypothesis is filtered out after hashing. No `_charge_ledger` call anywhere -- enumeration is not a trial.
EVIDENCE: docs/evidence/harness/S11_grammar_2026-09-03.md -- the per-file existence and column table from step 1, the enumeration arithmetic, the distinct-hash count, the collision count, the permutation and grid-step checks, test output, and a NOT VERIFIED list. Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local parquet only. No deploy before ACCEPT (B5).
COMMIT: explicit pathspec (the package files, the test, the memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
