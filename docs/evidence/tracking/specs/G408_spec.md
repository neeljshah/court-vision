GAP G408 | sport basketball | worktree a11 | log cx_g408_pts_duration_stop_proposal
**RANK 3 - REPLACE THE DURATION ASSUMPTION WITH A REVIEWABLE STOP CONTRACT. Landed docs/evidence/tracking/g401_fps_cap_duration_shadow_2026-09-11/summary.json SHA-256 d996ecbfb544b5bc3ce5e462efb42df06aa1a10fdca1299e2bb508fa77276d10 records 29/30 mechanics; docs/evidence/tracking/g401_fps_cap_duration_shadow_2026-09-11/paired_caps.csv SHA-256 fc2c521aae5a8943d490aa25159943ea666fd879a5aaf30be32ffed82a04cceb preserves the failed source. G401's deployment decision remains USER-ONLY.**
WHERE THIS ROW RUNS: PC preparation/receipt replay 30 min, retained-source presentation-timestamp decode <=55, proposal/reader survey/controls <=55, independent recomputation/test/memo <=35; total <=175 min. Pod read-only retention fetch only if PC originals are missing; no GPU or producer execution.
OWNS: scripts/platformkit/tracking/g408_*.py; tests/platformkit/test_g408_pts_stop_proposal.py; docs/evidence/tracking/g408_pts_duration_stop_proposal_2026-09-11.md and matching directory, including PROPOSED_g408_pts_duration.diff only.
PREMISE step 0 (BINDING before-condition): reproduce G401's exact 30 source identities and paired cap results, including nba__1rZZ_7buX_Y_s5474, 26 duplicate PTS/26 dropped frames and 100.0885 s endpoint. Require accessible retained native originals or complete decoded presentation schedules with readback receipts for all 30; no substitute sources.
Rehash current daemon/caller/parser/producer route and inspect every duration/frame-count reader; establish that elapsed-PTS stopping is absent. If an equivalent implementation already exists, STOP FALSIFIED and cite it; changed G401 bytes or missing source/timeline evidence -> PARTIAL before comparison.
LIMIT: duration is an interval in presentation time, not a promise of an evaluated tick at exactly 100.000 s. Preserve boundary undershoot and any gap; a sparse schedule cannot meet a one-native-interval proximity bar by inventing frames. G401's failed fixed-count verdict never changes.
METHOD (sealed; mechanics/proposal only):
1. Use all 30 G401 sources, in the original even draw, with decoded PTS in presentation order, original frame index and native pixels. Distinguish packet/DTS order from presentation order; archive duplicates, gaps, nonfinite/missing timestamps and actual EOF.
2. Seal t0 as the first valid presented frame at the requested start and deadline=t0+100 s. Proposed stop admits processing only while PTS < deadline, checks BEFORE stride/detector work, and records the first boundary-crossing PTS separately without processing it. No fps multiplication sets the deadline.
3. Duplicate PTS retain original stable order but cannot advance elapsed time; missing/nonfinite PTS or backwards presentation timestamps terminate UNKNOWN, with explicit reason and no silent 30-fps fallback. Genuine EOF before the deadline records EOF_SHORT separately from CAP_REACHED.
4. Replay legacy 3000-frame, G401 fps-count and proposed PTS rules once on the SAME 30 schedules/start positions. Report all admitted indices, last admitted PTS, first boundary PTS, over/undershoot, duplicate/gap counts, termination reason and UNKNOWN. Never select a favorable subset of sources.
5. Keep two outcomes separate: temporal containment (no processed frame outside [t0,deadline)) and endpoint proximity (last admitted/first boundary gaps). Proximity is descriptive unless the inherited G401 bar passes on all 30; no exact-duration success claimed merely because containment passes.
6. Add 30 exhaustive CONSTRUCT schedules: five timestamp cases (regular, duplicate, missing, backward, boundary gap) x six endpoints (before start, start, inside, just below deadline, exact deadline, above deadline). Enumerate expected stop/admission/error for every case; real-source n remains 30 and cannot be inflated by controls.
7. Write ONE complete unified PROPOSED diff spanning required scripts/platformkit/track_daemon.py, scripts/run_clip.py and src/pipeline/unified_pipeline.py interfaces; include opt-in duration argument/receipts and early PTS check. With duration enabled, omit the caller's implicit 3000-frame limit and explicitly bypass its default downstream; an explicitly supplied --frames remains an independent earlier limit with FRAME_CAP termination. Without duration, preserve legacy argv/behavior exactly. Proposal bytes only; do not edit or run patched modules, even in scratch.
8. Record exact base hashes, context anchors and every caller/reader of touched arguments/receipt fields; mechanically check all hunks in memory against those bytes. Keep receipt fields additive, existing aliases, explicit --frames semantics, stride and _VRAM_FLUSH_INTERVAL=3000 unchanged; tests must prove implicit default versus explicit cap precedence. Flag any incompatible source decoder as unresolved, not silently supported.
9. Enumerate >3000-frame runtime/receipt/quality smoke and rollback prerequisites for the existing G401 user review; this row supplies mechanics and a proposal, not those runtime results. Keep G393 parser repair and G395 live overlay separately pending.
10. Inspect 30 even real-source timeline/native boundary cards, including the duplicate/dropped case; regenerate all admitted-index tables/cards twice from immutable schedules. Decode itself and producer repeatability remain distinct from table reproduction.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| PTS temporal containment | G401 fps-count proximity 29/30; one endpoint 100.0885 s | 30/30 complete schedules: zero processed PTS outside sealed interval and exact stop reasons; all anomalies retained | 30 original sections; 30 CONSTRUCT separately | All 30 native/timeline cards | Original failed counts, interval/start semantics | Met DONE mechanics only; incomplete PARTIAL; violation NOT VALIDATED |
| Endpoint honesty | Fixed-count rule misses inherited exact-duration bar | Publish last-admitted/first-boundary gaps for ALL 30; no containment-to-proximity substitution | ALL 30 real timelines | Same cards, gap case mandatory | Inherited proximity bar and missing-frame evidence | Complete DONE diagnostic even if proximity fails; unsupported success REJECT |
| Complete proposed integration | No validated PTS cap; no runtime crossing proof | All required interface hunks/base hashes/reader checks and 30/30 control outcomes; two exact saved-input reproductions | 30 controls plus all 30 source tables | Same cards and full diff review | Legacy default, cap/stride/flush, live tree | DONE PROPOSED ONLY; unresolved interface NOT VALIDATED; applied code REJECT |
EVIDENCE: docs/evidence/tracking/g408_pts_duration_stop_proposal_2026-09-11.md plus directory: prereg.md, source_receipts.csv, source_hashes.json, decoded_pts.csv, draw.csv, paired_stops.csv, admitted_indices.csv, construct_cases.csv, reader_survey.csv, base_hashes.json, PROPOSED_g408_pts_duration.diff, proposal_checks.json, runtime_review_requirements.md, eye_index.csv, renders/, summary.json, repeats.json, q6_scan.json, SHA256SUMS; NOT VERIFIED includes actual producer integration/cost/quality.
TEST: python -m pytest tests/platformkit/test_g408_pts_stop_proposal.py -q; duplicate/missing/backwards PTS, exact boundary exclusion, EOF_SHORT, stride-before-stop refusal, implicit/explicit frame-cap precedence, immutable 30-source denominator, hunk/base validation and all 30 enumerated controls.
VERSION 2026-09-11 v1 PROPOSED; attaches to the USER G401 decision. No new deployment, restart or application of gated code is authorized by this spec.

COMMON BINDING CONTRACT (astra round 9, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6. Each block inherits these rails; the orchestrator confirms NEXT_GAP_ID/free owner and measures its WHOLE-set premise before dispatch. Supplied idle GPU/lane counts alone do not satisfy S2.
Execution: codex terra PREPARE seals protocol/bars alone, then a Claude finisher seals exact inputs and completes measurement/receipts within 180 min. No held-out invocation or threshold sweep, training, daemon restart, flag or registry write. Missing supply -> PARTIAL with planned keys retained; equivalent completed work -> FALSIFIED. No retry or replacement after seeing outcomes.
Ownership: only each named new helper prefix, one test and evidence memo/directory; helpers <=300 LOC, full-package imports. No src/ or other gated-tree edits; proposed changes stay in named evidence diffs, applied nowhere. Shared modules remain untouched. Every finisher commits the COMPLETE explicit file list, including imported helpers, fixtures, logs, renders and result text; no bulk add or push.
Retention/identity: retain selected sources OFF-POD, independently rehash and decode at native resolution BEFORE any pod stage; record original/receiver paths, bytes, dimensions, actual PTS and failures. G402's 16/60 mid-stage prunes make a pin insufficient. Hash EVERY exercised model, route and configuration before launch; post-hoc hashes cannot establish launch identity.
Pod steps use ~/bin/pod_run aXX --ship <owned helpers> --fetch <owned evidence> -- <sealed command>, only /workspace/wt/aXX/; deployed source/model trees are read-only. Gate on measured VRAM/CPU, not directory counts; <=6 CPU cores, threads 1, sequential GPU, <2 GB incremental scratch, successful pod write probe before bulky staging, UNKNOWN quota never zero. Finishers poll their own jobs to completion; no unattended leftovers or peer interruption.
Evidence: bound EVERY derived table to sealed source-frame/PTS windows; retain raw overruns separately, all no-output ticks and UNKNOWNs. Keep full parent schemas and repeat-receipt shape, including identical, runs and each run's stdout/exit/digests, adding aliases where needed. Two fresh processes regenerate delivered tables/cards from delivered bytes; arithmetic/render reproduction never proves inference repeatability.
Text: LF and ASCII; inventory every landable text/code/CSV/JSON/log/result path. Field-aware Q6 scans must inspect prose and claim fields, exempt only exact opaque identifiers and appropriately typed numerical data, and emit pattern indices/counts without matched text. Build all scan patterns/positive fixtures from character codes; no blanket CSV/JSON exemptions. Preserve redaction hashes/semantic parity, SHA256SUMS byte domain and NOT VERIFIED.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage -- the quota guard prunes ledgered sources ~45 min after tracking; hash EVERY exercised model, route file and config BEFORE any launch); finishers poll their OWN background pod jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed source-frame/PTS window and retain overruns separately; a failed planned unit stays an UNKNOWN row; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; receipts emit pattern indices, never token text); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round9_2026-09-11.md (local-only). G402 and G400 are landed on master; G405's evidence lands after its verify (lane a14 tip 051d2f5a6); G403 is running on lane a3.
