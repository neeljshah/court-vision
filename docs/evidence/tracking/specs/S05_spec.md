GAP S05 | sport all (harness) | worktree a10 | log cx_s05_calibration_report
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): every calibration piece exists and nothing composes them per (sport, regime): reliability bins, `max_loser_wp`, Murphy decomposition, per-regime isotonic, ECE, sharpness, resolution all present; `max_loser_wp` has never gated a promotion and `docs/evidence/calibration/` holds only `foundry_run_*`.
READ (platformkit ROOT unless noted; read every signature on disk): scripts/platformkit/regime_calibration.py `buckets(df)` and `fit_per_regime(preds, outcomes, keys, ...)`; scripts/platformkit/recalibration.py `walk_forward_recalibrate`; scripts/platformkit/wp_diagnostics.py `reliability`, `max_loser_wp`, `isotonic_check`; scripts/platformkit/calib_decomp.py `decompose`; scripts/platformkit/eval_gate/scoring.py the ECE function (import the module symbol -- find its exact name), `resolution`, `sharpness`; inputs READ-ONLY = the four gate corpora via scripts/platformkit/combo/corpus_cache.py `load_gate_corpus(sport)` for nba, mlb, soccer, tennis (data/cache/combo/gate_corpus_<sport>.parquet).
PREMISE (step 0): `ls docs/evidence/calibration/` holds README.md, calibration_audit.mp4, foundry_run_2026-09-01, reliability_diagram.png and ZERO per-sport reliability JSON -> before = 0/4. If a per-sport reliability JSON already exists there, STOP and report FALSIFIED.
LIMIT (step 1): per sport, print the corpus row count with a finite prediction column and a binary outcome, and the name of the prediction column you will score (the corpus model probability; if only `p_base` exists say so; the devigged close too where the corpus carries one). A sport under 200 usable rows is INSUFFICIENT in the artifact -- never a number.
CHANGE (step 2): NEW scripts/platformkit/eval_gate/calibration_report.py (<=300 LOC): `build_report(records, sport, *, bins=10, min_n=200) -> dict` plus a `__main__` that runs all four sports and writes docs/evidence/calibration/<sport>_reliability_2026-09-03.json. It COMPOSES only the existing functions above: 10 reliability bins with n per bin; max_loser_wp; ECE before and after per-regime isotonic fitted on INNER folds via walk_forward_recalibrate (keep fit_per_regime's GLOBAL fallback below min_n exactly as it is); the three Murphy terms (reliability, resolution, uncertainty) from calib_decomp.decompose; sharpness; regime keys = regime_calibration.buckets VERBATIM. Verdict IMPROVES iff ECE down AND Murphy reliability term down AND resolution term NOT down; else FLATTENED. Invent nothing; touch no serving path.
TEST: NEW scripts/platformkit/eval_gate/test_calibration_report.py (beside the module): a synthetic predictor deliberately flattened toward 0.5 lowers ECE yet is reported FLATTENED (resolution down); the artifact dict carries exactly 10 bins with n per bin, max_loser_wp, ece_before, ece_after, the three Murphy terms and the verdict. Run ONLY `python -m pytest scripts/platformkit/eval_gate/test_calibration_report.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = artifact completeness + verdict rule firing; denominator = one artifact per sport (4)
  before        = 0/4
  bar           = 4/4, each JSON with 10 bins + n per bin, max-loser-WP, ECE before/after, three Murphy terms, sharpness, verdict, the prediction column named, the dropped-row count
  n             = corpus rows per sport, each >= 200 (scored); a sport below 200 is INSUFFICIENT and the shortfall is reported honestly (it does not count toward 4/4)
  eye check     = n/a (S-row); reproduction = verifier recomputes ECE and the Murphy terms from the JSON bins
  must not move = every serving path, scripts/platformkit/serving_calibration.py, all flags, every threshold under scripts/platformkit/eval_gate/, data/registry/**, data/cache/eval_gate/backtest_fwer.jsonl (13 rows; no `_charge_ledger` call -- EVIDENCE ONLY)
NON-TAUTOLOGY: every corpus row with a finite prediction and a binary outcome is scored; rows dropped for missing values are COUNTED in the JSON, never silently removed.
EVIDENCE: docs/evidence/harness/S05_calibration_report_2026-09-03.md (per-sport table: n, ECE before/after, three Murphy terms, max_loser_wp, verdict; commands; test output; NOT VERIFIED list) plus the four JSON artifacts. Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local parquet only.
COMMIT: explicit pathspec (module, test, memo, the JSON artifacts), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
