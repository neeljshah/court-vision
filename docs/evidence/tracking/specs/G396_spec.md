GAP G396 | sport basketball | worktree a3 | log cx_g396_third_rater_paint_qualification
**RANK 3 - ONE THIRD-RATER QUALIFICATION, THEN CONDITIONAL REAL PAINT. Landed docs/evidence/tracking/g392_per_rater_paint_qualification_2026-09-11/qualification/scores.csv SHA-256 27481b1475c2df30ea977188158bb7001c969d2a92b9d2e464194a6d543bd243 reproduces sol 30/30 and terra 20/30; docs/evidence/tracking/g392_per_rater_paint_qualification_2026-09-11/summary.json SHA-256 21ea609a26db600c625d25265da11db3f240e262c807d2f10d5b2ee5280e3e62 records real paint NOT SCORED. This changes the rater tier while preserving the G392 coordinate protocol.**
WHERE THIS ROW RUNS: PC only, PREPARE/source checks 25 min, practice/fresh qualification 65, conditional real rating/audit 70, receipts 20. Use retained native images; no pod, GPU, fitter or source-rotation wait.
OWNS: scripts/platformkit/tracking/g396_*.py; tests/platformkit/test_g396_third_rater_paint.py; docs/evidence/tracking/g396_third_rater_paint_qualification_2026-09-11.md and matching directory. Fixed independent raters: existing sol tier plus ASTRA tier; terra prepares only.
PREMISE step 0 (BINDING before-condition): recompute all 60 G392 qualification rows, sol 30/30, terra 20/30, joint 20/30, real_scored false; require no subsequently qualified pair. Rehash all 49 retained native images, 30 context identities and 180 tile identities, retaining the 11 parent failures.
The third rater is a fresh isolated ASTRA process using the existing C:/Users/neelj/.codex-astra home, with exact model/runtime/config digest recorded. It receives no review history, truth files, old answers or other rater output. No home/config edits; failed unscored image-open/output fixture -> PARTIAL before scored dispatch.
METHOD (sealed before new controls; a new row, not a G392 retry):
1. Fix sol and ASTRA before opening controls; no substitution after observing scores. Reuse the G392 tile/native conversion and instructions; 640x540 tiles, integer offsets, native scale, separate actual image-open receipts. ASTRA is a rater, not its own adjudicator/verifier.
2. Generate 30 PRACTICE and 30 QUALIFICATION controls with seed 396 on the same 30 retained contexts, six tile positions and 0/30/60-degree cycle. Enforce no repeated pixel hash or truth tuple within/between new sets or against G392; archive the full generated set before dispatch, with hidden known finite bands.
3. ASTRA receives practice feedback and exactly one documented instruction/display correction; freeze it before qualification. Sol keeps its qualified G392 instructions. No fitted offset, blanket subtraction or changes after qualification output begins.
4. Both independently rate the SAME 30 fresh qualification controls once, blind and feedback-free. Three independently observed points: every point <=3 native px perpendicular, projection within the finite band, first/second span >=60 px, third strictly interior. Missing output fails; a sandbox fault is not a free retry.
5. Qualify each at >=27/30 AND require >=27/30 joint successes on these SAME controls. G392's old sol score cannot be joined to new ASTRA controls. Any failed gate -> PARTIAL, pause real paint; no Opus replacement or new control seed within this row.
6. Only after both qualify, issue the unchanged 30 real G392 contexts in their original even selection from 49. Preserve the 60-state parent census and 11 decode failures separately; the real denominator is explicitly conditional on retained images, not a new 60-frame result.
7. Each rater independently marks up to two visible straight fragments per fixed tile/family order, using G392's family set and three-point protocol. Keep ABSENT/UNKNOWN, occlusions and unmatched fragments; no templates, old traces or inferred continuation through missing paint.
8. Freeze pairing by tile/family and smallest symmetric line distance with stable ties; independent third point <=3 px from own line, every point <=6 px from the other's line. Do not substitute endpoint agreement or relax finite-band/span requirements.
9. Claude finisher records native visibility before seeing traces, then audits every candidate pair: same physical paint band AND >=8/9 evenly spaced native points on EACH fragment within 3 px of visible paint. Archive per-pair audit clicks and unresolved cases; orientation remains UNKNOWN.
10. Report recovered/all 30, family disagreement, UNKNOWN and descriptive visible-conditioned recovery; all 30 accounted for even if recovery is zero. Repeat rendering/scoring from raw judgments in two fresh processes; this checks arithmetic, not independent rater repeatability.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Third-rater and joint qualification | Sol 30/30; terra excluded 20/30; ASTRA unmeasured | Sol>=27/30 AND ASTRA>=27/30 AND joint>=27/30 on fresh controls, all point clauses | 30 distinct qualification contexts per rater; practice separate | All 30 overlays per rater in even order | 3 px, finite band, 60 px span, frozen instructions, no retry | Gate failure PARTIAL; pause paint |
| Qualified real-paint accounting | G392 not scored; G388 diagnostic 0/30 remains historical | Two qualified raters, 30/30 states accounted, every proposed pair independently pixel-audited; no invented recovery minimum | Same 30 retained real contexts; conditional scope stated | All 30 native frames, both traces and failure/absence cards | 6 px pairing, 3 px audit, 8/9 rule, UNKNOWN retained | Complete audit DONE even at zero recovery; otherwise PARTIAL |
| Identity, blindness and reproduction | 49 retained natives/30 contexts; 11 original failures | Exact identities, disjoint control pixels/truth, no answer leakage, fresh-process score/render bytes identical | All parent states; all 30 new qualification controls and 30 real contexts | 30 even transform/control cards | G392/G388 historical evidence, no production or fitter changes | Identity/blinding fault NOT VALIDATED; no registration claim |
EVIDENCE: docs/evidence/tracking/g396_third_rater_paint_qualification_2026-09-11.md plus directory: prereg, model/runtime receipts, input_manifest.csv, control_disjointness.csv, practice/, correction_receipt.json, frozen_instructions, qualification/truth.csv, raw_answers/, scores.csv, eligibility.json, conditional real pairs/visibility/audit clicks, repeats.json, summary.json, renders/ and common receipts; hash manifest excludes its own entry.
TEST: python -m pytest tests/platformkit/test_g396_third_rater_paint.py -q; old/new control joins rejected, one-rater failure blocks real dispatch, missing response fails, finite-band overrun fails, displaced parallel band fails, pair audit cannot be reused across candidates, scorer repeat from raw answers.
VERSION 2026-09-11 v1 PROPOSED; one fixed third-rater tier, one practice correction, one fresh joint qualification. Failure pauses this paint line; success qualifies only this reference protocol and sample.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only; the G395 overlay is applied ONLY to /workspace/deploy under the 2026-09-08 user authorization with backup, smoke, ONE restart and the watchdog revert); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round5_2026-09-11.md (local-only). G391's evidence lands on master after its verify; a build that needs it waits for that landing.
