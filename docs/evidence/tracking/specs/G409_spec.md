GAP G409 | sport basketball | worktree a12 | log cx_g409_box_coordinate_cause
**RANK 1 - TRACE THE BOX ORIGIN. Landed docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11/target_mask.csv SHA-256 c2c4f57a56330afbc9a7772b61743864aa04d746d9715e0374743e724a79dc71 and eye_index.csv in that same evidence directory SHA-256 d0019a5b0460cca99fe5afade3e414173c104eba5997fc1683f43d1de2bcd436 bind the export/draw. Pending docs/evidence/tracking/g406_masked_target_pixel_audit_2026-09-11/summary.json SHA-256 734e2ed8f092942a86c68b64dfaaf8047dfe84a63b551a37ade2ef97c9d70e0f supplies the -58.2 px diagnostic, not an accepted cause.**
WHERE THIS ROW RUNS: PC premise/seal/off-pod retention 25 min; pod a12 actual-path observer replay <=90; PC transforms/native review/reproduction/test 55; total <=170 min. Use existing archived route bytes; observer wrappers are owned helpers, not edits to src/.
OWNS: scripts/platformkit/tracking/g409_*.py; tests/platformkit/test_g409_box_coordinate_cause.py; docs/evidence/tracking/g409_box_coordinate_cause_2026-09-12.md and matching directory only.
PREMISE step 0 (BINDING before-condition): require accepted G406 handoff; independently reproduce 60 distinct exact-even ticks, 206 producer rows, 592 comparator boxes, 51 associations and their dx/dy from ALL sealed tables; verify all G402 source/window/row joins and archived route digests. Missing acceptance/bytes -> PARTIAL before launch; a reconciled coordinate contract with no discrepancy -> FALSIFIED.
LIMIT: enumerate native -> crop/resize -> detector input/output -> tracker box -> CSV -> G402 join/render -> comparator stages and actual runtime tracker class. A current source-code resemblance or same weight digest cannot identify historical preprocessing.
METHOD sealed before replay:
1. Freeze the existing 60 ticks, 30/kind over 53 windows; retain full native originals and raw parent outputs, all producer rows/silences, start-frame/frame-cap argv, absolute frame indices and PTS. No success-only redraw or rematching to improve agreement.
2. Bind archived UnifiedPipeline/AdvancedFeetDetector and any alternate active path, including pose/detector cadence, prefetch cache keys, crop constant, dimensions, padding, bbox tuple order and effective library configuration. Resolve every loaded model before launch; absent identity -> NOT VALIDATED.
3. Observer-only replay of the 53 sealed short windows through their actual producer route captures decoded native array/hash, post-crop array/hash, model input key/shape, raw model box, post-tracker box, CSV values and serialized join key at the 60 ticks. Preserve surrounding in-window state; no fresh tracker reset at each selected tick.
4. Read model-call/cache frame identity at the call site, including prefetched frames; preserve UNKNOWN historical decoder identity and any replay nondeterminism. If those binds disagree, stop coordinate-cause attribution and publish the stage of the mismatch.
5. Independently verify the export identity join against original G402 raw CSV by source digest/window/frame/track/event; duplicate or absent keys remain failures. Replay G406 preprocessing separately as a recorded comparator, never silently call it the producer path.
6. Derive transforms from executed code/constants before comparing offsets: report origin, scale, padding and xyxy/yxyx at each stage. Use exact integer/rational arithmetic for crop/serialization; record detector float values separately. Do not estimate a translation from the 51 matches.
7. Demonstrate each hypothesized transform with known-geometry CONSTRUCT cases covering origin, corners, crop boundary and tuple order through the actual relevant functions; controls are additional to all 60 native ticks, not their replacement.
8. At native pixels display stored boxes, code-mapped boxes and actual detector input separately on all 60 ticks. Reuse the frozen associations for descriptive before/after residuals; a new detector assignment is separate and cannot improve the old denominator.
9. A cause names the first erroneous frame-contract boundary, its responsible code/export site and >=30 distinct native frames showing that executed transform. Separate an omitted export origin conversion from detector localisation, tracker ageing and frame-cache mismatch; mixed or unexplained causes remain PARTIAL. No corrected training export in this row.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Input/export binding | 60 ticks; 206 rows; 51 matched pairs | All 60 keys and every row/silence accounted; exact source/window/CSV joins; zero silently substituted frames | 60 distinct ticks, 30/kind | All 60 full native frames | G402 draw, masks, raw coordinates | Complete DONE diagnostic; missing PARTIAL; selective loss REJECT |
| Causal coordinate trace | dy -58.2 px; cause unsealed | First erroneous boundary identified by executed code; exact declared transform on >=30 distinct frames; every residual/mismatch retained, no fitted offset | All 60 planned ticks; supported-frame n separate | All 60 plus all CONSTRUCT cases | Frozen route/args, historical association and geometry | Cause established DONE only at bar; otherwise PARTIAL, never claim a universal correction |
| Identity/reproduction | Same weights do not bind full path | Pre-launch model/route/input hashes; stage records complete; two saved-output reproductions identical | All exercised assets and 60 keys | Stage-labelled native overlays | Production, library settings, all old verdicts | Exact DONE; absent launch identity NOT VALIDATED |
EVIDENCE: docs/evidence/tracking/g409_box_coordinate_cause_2026-09-12.md plus matching directory: prereg.md, input_hashes.csv, source_receipts.csv, draw.csv, stage_trace.jsonl, export_join.csv, transforms.json, construct_cases.csv, residuals.csv, summary.json, repeats.json, eye_index.csv, renders/, launch_receipts.json, runtime_receipts/, q6_scan.json, SHA256SUMS. NOT VERIFIED explicitly covers historical inference equality, court geometry and training suitability.
TEST: python -m pytest tests/platformkit/test_g409_box_coordinate_cause.py -q; crop/tuple transforms, absolute window indices, duplicate/absent event joins, stale cache-frame identity, silence and exact parent repeat shape, character-code scanner fixtures.
VERSION 2026-09-12 v1 PROPOSED; no retrospective G406 change, no deployment or source patch. Orchestrator checks prerequisite acceptance before dispatch.

COMMON BINDING CONTRACT (astra round 10, inherited by this block):
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6; each block inherits these rails. Orchestrator rechecks free owner, accepted dependencies and WHOLE-set premise before dispatch; changed artifact bytes require an explicit prospective handoff, never silent substitution.
Execution: codex terra PREPARE seals protocol/bars alone; Claude finisher retains inputs and completes measurements, test, reproduction and memo within 180 min. Each row owns only its named helper prefix, one test, memo/directory and any named PROPOSED diff. They are not alone in the repo; never revert peer edits.
Retention: BEFORE ANY pod stage, finishers retain every selected original OFF-POD, independently rehash/read back native bytes and record paths, bytes, dimensions, rational PTS and failures. A pin/hash alone is insufficient; no replacements after outcomes. Missing inputs -> PARTIAL immediately, not a wait.
Identity/resources: hash EVERY exercised model, route, library configuration and actual loaded asset before launch. Pod steps use ~/bin/pod_run aXX --ship <owned helpers> --fetch <owned evidence> -- <sealed command>, only /workspace/wt/aXX; original source trees remain read-only. Gate on measured free VRAM/CPU and a pod write probe, not lane count; <=6 cores, threads 1, sequential GPU, <2 GB incremental scratch, quota UNKNOWN never zero.
Bound every derived table to sealed frame/PTS windows; archive overruns separately and retain silence, failed keys and UNKNOWNs. Preserve full parent table schemas and repeat shape: identical, runs, each command/stdout/exit/digests. Two fresh processes reproduce delivered tables/cards from delivered bytes; this does not prove inference repeatability.
All helpers <=300 LOC, full-package test imports, exactly the named per-file test. No src/ or other gated-tree edits; only PROPOSED diffs under docs/research/organization-sprint/, applied nowhere. No daemon restart, shared-module change, held-out invocation/sweep, training, flag change or registry write.
Write LF/ASCII. Field-aware Q6 scans cover every delivered code/text/log/table/result claim field; exempt only exact opaque identifiers and typed numerical data, never whole file types. Build patterns and positive fixtures from character codes; emit pattern indices/counts only. Preserve semantic redaction receipts and the declared SHA256SUMS byte domain.
Finishers commit the COMPLETE explicit file list, including imported helpers, fixtures, raw observations, renders and receipts; no bulk adds/push. Poll owned work to completion; never stop to wait. Premise false -> FALSIFIED memo; supply/resource failure -> scoped PARTIAL/NOT VALIDATED closure; second failed attempt -> CLOSED AT LIMIT, no repeated discovery loop.

ORCHESTRATOR FOOTER (binding, 2026-09-12): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage; hash EVERY exercised model, route file and config BEFORE any launch); finishers poll their OWN background pod jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed source-frame/PTS window and retain overruns separately; a failed planned unit stays an UNKNOWN row; inherited bars byte-identical (no rounding, no added tolerance); never change an existing field's semantics -- add a new field with an alias; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY touched text path incl. the scanner's own source and .log.txt receipts (patterns built from character codes; receipts emit pattern indices, never token text); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round10_2026-09-12.md (local-only). G402, G405 and G406 are landed on master; G403's evidence lands next (lane a3 tip 0a46c0e6c); G407/G408 are in fix cycles (lanes a10/a11); G404 runs on a18.
