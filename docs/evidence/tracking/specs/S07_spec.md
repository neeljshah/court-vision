GAP S07 | sport all (harness) | worktree a10 | log cx_s07_forward_primacy
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): no primacy rule: where a forward settled series and a backtest disagree, nothing says the forward number is the claim, and a retrospective-only number is not labelled.
READ (exact symbols, re-grepped on disk 2026-09-03): scripts/platformkit/ingame/forward_evidence_scoreboard.py (291 LOC) -- `build_scoreboard(now)` (:194), `_tail_rows_for_sport` (:126), `_enrichment_row` (:170), `distance_to_decidable` (:108), `write_doc` (:241), `render` (:249), `main` (:271), `OUT_PATH` = data/frontend/ops/forward_evidence_scoreboard.json, `COMPONENT`, `_NEVER`/`_NOW`/`_UNKNOWN`, `__all__` (:287); the row keys EXACTLY as built today: gate, sport, pre_registered_at, days_accruing, forward_n, verdict, distance_to_decidable, source. READERS to check before you touch a field (A5): scripts/platformkit/predictive_validity/validity_ladder.py and scripts/platformkit/ingame/test_forward_evidence_scoreboard.py (the only two on disk -- re-grep `forward_evidence_scoreboard` under scripts/ and tests/ and say what you found).
PREMISE (step 0): grep the module and every reader for a primacy or provenance symbol (`primacy`, `RETROSPECTIVE`, `provenance`, `claim_source`). Premise holds iff ZERO hits outside this spec AND `build_scoreboard()` rows carry no field naming which number is THE claim -> before = 0/4 cases labelled. Print the grep output and one real row. If a primacy rule already exists, STOP, write the memo, commit, report FALSIFIED (a valid result).
LIMIT (step 1): n/a -- CONSTRUCT row; the four cases below are the whole decision set. Also print the CURRENT count of rows whose `forward_n` is 0 (today's real state: forward evidence is empty, S20 has 0 settled rows) so the memo states that every live claim is RETROSPECTIVE today.
CHANGE (step 2): NEW scripts/platformkit/eval_gate/claim_primacy.py (<=300 LOC, additive, no dependency beyond stdlib):
  `primacy(forward: dict | None, retro: dict) -> dict` returning {"claim", "value", "provenance", "label", "conflict", "note"}. RULE: a forward series with a usable settled count is THE claim and provenance="FORWARD"; the retro number is carried as context under "retro", never as the claim. Forward absent, empty, or below its own declared `min_n` -> claim = the retro number with provenance="RETROSPECTIVE" and label="RETROSPECTIVE" -- an explicit label, NEVER a silent fall-back that reads as forward. `conflict=True` whenever both exist and their verdict strings differ; the forward one still wins and the note names both.
  `label_row(row: dict, retro: dict | None = None) -> dict` -- returns a NEW dict with the additive keys `claim_provenance` and `claim_label` added; it renames and removes NOTHING. `build_scoreboard` calls it per row so every row prints its label. `render` gains one column; the existing keys, OUT_PATH, COMPONENT, honest_note and edge_claimed stay byte-identical.
TEST: NEW scripts/platformkit/ingame/test_forward_evidence_primacy.py (beside the module, like test_forward_evidence_scoreboard.py). Four enumerated cases: (a) forward AHEAD / retro AHEAD -> claim FORWARD, conflict False; (b) forward AHEAD / retro BEHIND -> claim FORWARD, conflict True, retro carried as context; (c) forward BEHIND / retro AHEAD -> claim FORWARD (BEHIND is a success, not a downgrade to the nicer number), conflict True; (d) forward None-or-below-min_n (INSUFFICIENT) -> claim RETROSPECTIVE, label RETROSPECTIVE, conflict False. Plus: `build_scoreboard()` on today's real artifacts returns every row carrying `claim_label`, and every pre-existing row key is still present with the same value. Run ONLY `python -m pytest scripts/platformkit/ingame/test_forward_evidence_primacy.py -q`, then rerun `python -m pytest scripts/platformkit/ingame/test_forward_evidence_scoreboard.py -q` (must stay green) -- two files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = label correctness; denominator = 4 constructed cases (agrees / forward AHEAD vs retro BEHIND / forward BEHIND vs retro AHEAD / forward empty-or-INSUFFICIENT)
  before        = 0/4 (module absent; no primacy or provenance symbol anywhere -- measured in step 0)
  bar           = 4/4, plus every existing scoreboard row key unchanged
  n             = 4 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns both test files in master and re-prints one real labelled row from build_scoreboard()
  must not move = the scoreboard's existing outputs (OUT_PATH, COMPONENT, every existing row key and its value, honest_note, edge_claimed), scripts/platformkit/predictive_validity/validity_ladder.py, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows; No `_charge_ledger` call -- EVIDENCE ONLY)
NON-TAUTOLOGY: all four cases are scored; nothing is excluded. Case (c) is the anti-cherry-pick case -- forward BEHIND must still win over a retro AHEAD, or the rule is a way to keep the flattering number. The plan states 3 cases (agrees / disagrees / empty); the disagreement case is split into BOTH directions here, so 4/4 is the same rule with the reverse direction added and no bar is loosened.
EVIDENCE: docs/evidence/harness/S07_forward_primacy_2026-09-03.md -- the step-0 grep output, the four-case table, the count of rows with forward_n = 0 today, exact commands, both test outputs, and a NOT VERIFIED list (say plainly: no forward settled series exists yet, S20 has 0 settled rows, so every live claim is RETROSPECTIVE today and the FORWARD branch is exercised only by construction). Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local artifacts only. Do not ssh anywhere.
COMMIT: explicit pathspec (module, the scoreboard edit, test, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
