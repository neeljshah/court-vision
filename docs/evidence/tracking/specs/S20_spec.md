GAP S20 | sport all (execution) | worktree a15 | log cx_s20_paper_readout
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): maker pool EMPTY; the forward CLV series has 0 settled rows locally, so sizing, capacity, adverse selection, decay monitoring and the program's only claim surface are all downstream of a week that has not run. RE-SCOPED 2026-09-03: the pod capture runner IS alive (pid 8608 since 08-31) and 0 bets exist all-time; THIS job builds the DAILY READOUT + its rollup test so the week is reported honestly from day 0 with n_settled = 0. The pod launch/keeper decision is the orchestrator's (S30), not yours.
READ (read every signature on disk): scripts/platformkit/clv_ledger.py `DEFAULT_LEDGER` and its row reader; scripts/platformkit/pm_trading/clv_beatrate_rollup.py `build_rollup(rows)` and `build_mode_rollup(rows)`; scripts/platformkit/grade_paper_one.py `gross_legacy_rows()` and `_resolve_fee_units`; scripts/platformkit/ingame/forward_evidence_scoreboard.py (the AHEAD / PAR / BEHIND / INSUFFICIENT rules); scripts/platformkit/mcp_server/artifact_tools.py (confirm the FIRST path its `execution_status` handler reads is data/frontend/analytics/execution_status.json); scripts/platformkit/ingame/inplay_tick_latency.py (tick_latency_sec p50/p90 source); docs/AI_CONSUMER_CONTRACT.md (envelope fields source_artifact, as_of, status).
PREMISE (step 0): scripts/platformkit/pm_trading/clv_daily_readout.py is ABSENT and data/frontend/analytics/execution_status.json is ABSENT locally (print both). Print whether DEFAULT_LEDGER exists on this box and its row count (the pod's copy is absent, so 0 settled rows is the honest before). If the module already exists, STOP and report FALSIFIED.
LIMIT (step 1): n/a (CONSTRUCT row).
CHANGE (step 2): NEW scripts/platformkit/pm_trading/clv_daily_readout.py (<=160 LOC): `rollup(rows, *, now_iso) -> dict` (pure: settled count; per-sport and per-day settled counts; maker / taker / legacy split by the row's order mode; is_clv_suspect share; gross-legacy count via the same rule grade_paper_one uses; median clv in units and beat rate with a Wilson 95 pct CI ONLY when n_settled >= 8 -- the paper_today min_clv_n -- else the literal string "INSUFFICIENT"; capture tick_latency p50/p90 when cadence rows are supplied; verdict per forward_evidence_scoreboard rules or INSUFFICIENT) and `write_readout(ledger_path, out_json, memo_md, *, now_iso)` that reads the ledger (absent file = 0 rows, never an exception), writes data/frontend/analytics/execution_status.json carrying source_artifact (the ledger path), as_of (max settled timestamp, or the run time labelled "no rows"), generated_at, staleness_days, status ok | no_data, and appends the day's table to docs/evidence/execution/PAPER_LIVE_<date>.md. Units only. No dollar, ROI, profit or edge word in code, JSON keys, or memo text. Additive: no existing module edited.
TEST: NEW tests/platformkit/execution/test_paper_week_rollup.py: (1) a synthetic ledger of 24 settled maker rows across 2 sports over 8 days -> settled 24, maker 24, taker 0, suspect share and fee-net flag reported, Wilson CI present; (2) a 0-row and an absent ledger -> every number field is "INSUFFICIENT", status no_data, no exception; (3) a row with unit_result but no outcome is COUNTED as an integrity flag, never as settled; (4) a legacy gross row is counted under legacy, never maker. Run ONLY `python -m pytest tests/platformkit/execution/test_paper_week_rollup.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = readout correctness; denominator = 4 enumerated cases
  before        = module absent (0/4)
  bar           = 4/4 AND one real run `python -m scripts.platformkit.pm_trading.clv_daily_readout` on this box writes execution_status.json + docs/evidence/execution/PAPER_LIVE_2026-09-03.md with n_settled 0 reported as INSUFFICIENT (the honest day-0 readout)
  n             = 4 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns the test and the module in master and reads the JSON
  must not move = maker-only semantics (scripts/platformkit/execution/thresholds.py ORDER_MODE), the one-writer rule (this job NEVER writes the ledger), every threshold under scripts/platformkit/eval_gate/, data/registry/**, data/cache/eval_gate/backtest_fwer.jsonl (13 rows; no `_charge_ledger` call)
NON-TAUTOLOGY: every ledger row is classified into exactly one of settled / open / integrity-flag / legacy; none dropped.
EVIDENCE: docs/evidence/harness/S20_paper_readout_2026-09-03.md (premise, test output, the day-0 JSON, NOT VERIFIED list) plus the day-0 PAPER_LIVE file. Language rail (Q6): a CLV SERIES in units, never dollars, ROI or edge.
POD: none. Do not ssh anywhere.
COMMIT: explicit pathspec (module, test, memo, the PAPER_LIVE file), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
