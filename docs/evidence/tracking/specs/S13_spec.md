GAP S13 | sport all (harness) | worktree a12 | log cx_s13_ledger_schema | REQUIRES THE SHARED-MODULE TOKEN (docs/evidence/SHARED_MODULE_TOKEN.md) for `scripts/platformkit/eval_gate/ledger.py` AND `scripts/platformkit/eval_gate/backtest_runner.py` -- if this account does not hold the token, QUEUE the gap and stop
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): the FWER ledger is FLAT and GLOBAL: 13 rows of `{at, predictor, sport, start, end, k_cumulative}`; nine unrelated basketball trials charged between prereg and launch took K 3 -> 12 and flipped a verdict.
READ (read every signature on disk; line numbers drift): scripts/platformkit/eval_gate/ledger.py (113 LOC) `append_row`, `load`, `settled`, `drift_report`; scripts/platformkit/eval_gate/backtest_runner.py `_charge_ledger(path, spec, sport, start, end) -> dict` -- read-max + append under one `ledger_lock`, writing `{at, predictor, sport, start, end, k_cumulative}` with `cumulative_k(prior, 1)`; scripts/platformkit/combo/fwer_budget.py `cumulative_k`; then GREP EVERY READER of the ledger rows before you touch the shape (contract B2/A5): at minimum `hedge_trial_runner.py`, `eval_gate/test_ledger.py`, `tests/platformkit/test_eval_gate_ledger_drift.py`, and anything else `grep -rl backtest_fwer` finds under scripts/ and tests/. Name every reader you checked in the memo.
PREMISE (step 0): `data/cache/eval_gate/backtest_fwer.jsonl` holds exactly 13 rows today and none of them carries `family`, `k_family`, `hypothesis_hash`, `tier` or `prereg_sha256`. Print the row count and the union of keys across all 13. If any of the five fields already exists, or the count is not 13, STOP, write the memo, commit, report FALSIFIED -- a valid result.
LIMIT (step 1): n/a -- CONSTRUCT row; the 13 existing rows plus one new row ARE the enumerated case set.
CHANGE (step 2): ADDITIVE ONLY. Add five OPTIONAL fields defaulting to `None`: `family`, `k_family`, `hypothesis_hash`, `tier` (`"T2"|"T3"`), `prereg_sha256`. `_charge_ledger(path, spec, sport, start, end, *, family=None, hypothesis_hash=None, tier=None, prereg_sha256=None)` -- keyword-only, so every existing call site is unchanged. `k_family = 1 + max(k_family over rows of that family)`, absent family -> `None`. The global `k_cumulative` keeps `cumulative_k(prior, 1)` and stays monotone; it is the audit trail anyone can recompute. `ledger.load` fills absent keys with `None` so the 13 existing rows load byte-identical. Rename or remove NOTHING (B2). This row changes NO correction procedure -- that is S14, and it ships last.
TEST: NEW scripts/platformkit/eval_gate/test_ledger_schema_fields.py (beside the module, like the other eval_gate tests). It copies the REAL ledger to `tmp_path` and works ONLY on that copy -- `data/cache/eval_gate/backtest_fwer.jsonl` is never opened for writing: the 13 copied rows load without raising and their `k_cumulative` values are unchanged; one new row charged onto the tmp copy round-trips all five new fields; a row written without them loads with `None` and does not raise; `k_family` increments within a family and stays `None` outside one. Run ONLY that file: `python -m pytest scripts/platformkit/eval_gate/test_ledger_schema_fields.py -q`, then ALSO rerun `python -m pytest scripts/platformkit/eval_gate/test_ledger.py -q` (must stay green) -- two files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = ledger rows readable with the five new optional fields; denominator = 13 existing rows + 1 new row on the tmp copy
  before        = 0/5 fields present on 13/13 rows (module has no such fields today)
  bar           = 14/14 rows readable, all five fields round-trip on the new row, absent fields default to None without raising, and `k_cumulative` is UNCHANGED on all 13
  n             = 14 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns the test in master and diffs the real `data/cache/eval_gate/backtest_fwer.jsonl` before and after -- it must be byte-identical, 13 rows
  must not move = `k_cumulative` and its monotonicity, the Bonferroni correction in `deflated_metrics.deflated_p`, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), and data/cache/eval_gate/backtest_fwer.jsonl itself (13 rows, byte-identical -- the test uses a tmp COPY only)
NON-TAUTOLOGY: all 13 existing rows are in the denominator; none is excluded or rewritten to make the round-trip pass. No `_charge_ledger` call against the real ledger path anywhere -- the tmp copy only; this row is a schema change, not a charged trial.
EVIDENCE: docs/evidence/harness/S13_ledger_schema_2026-09-03.md -- the premise row count and key union, the list of EVERY reader you grepped and what each expects, the before/after field table, the byte-identity check on the real ledger, test output for both files, and a NOT VERIFIED list. Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local file only. No deploy before ACCEPT (B5).
COMMIT: explicit pathspec (ledger.py, backtest_runner.py, the new test, the memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
