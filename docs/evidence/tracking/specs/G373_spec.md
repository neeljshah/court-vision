GAP G373 | sport basketball | worktree a11 | log cx_g373_ball_detector_v2
ALLOCATED 2026-09-10 (orchestrator; from the astra-reviewed v2 draft docs/research/spec_drafts/). CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md B1-B10; Q1/Q6.
SUCCESSOR TO: G363 phase 2 LIMIT (recomputed over all 3,754 frame-score rows): A0 held-out C0 = 0 TP / 8 FP over 549 frames; development-selected A7 (the best SEALED arm, dev is 1/332 Wilson lower 0.00220997552, NOT a heldout tournament -- only A0 and A7 carry heldout rows) reaches held-out TP=2 / FP=178, C = 2/549 = 0.00364298725, Wilson lower precision = 0.00305237485, ALL FP/N_absent = 178/243 = 0.732510288. [E363,M363]
CENTRE DISTANCES (corrected attribution): nearest-centre median 487.50 px is A7 on 103 VISIBLE held-out frames carrying a prediction, NOT the deployed A0 (A0's own nearest-centre median is 776.35 px on n = 4 -- too few to generalize). Matching tolerance is 12.33 px, the rounded median half-diameter over the 285 VISIBLE held-out frames at 720p. Primary-rater centre gap is 36.39 px, the rounded median over 242 both-VISIBLE held-out pairs, ratio 2.9513 (about 3x tolerance) -- a property of the reference, not excused by anything scored here. [E363/centre_diagnostic.json,M363]
CORPUS (byte-identical to E363): 881 unique reference frames = 332 development (156 VISIBLE / 159 ABSENT / 17 UNKNOWN, 28 games) + 549 held-out (285 VISIBLE / 243 ABSENT / 21 UNKNOWN, 32 games); all frames.csv dimensions 1920x1080. Decode cache: 2,643 PNGs = 6,283,057,902 bytes = 5.85 GiB (NOT "5.9 GB" decimal); current live cache identity/availability is UNVERIFIED. [E363/frames.csv,E363/cache_manifest.csv,M363]
A9 GATE, corrected: v1 named A9 an MIT-licensed TrackNet-style detector "per LICENCE_LEDGER.md"; that ledger has ZERO TrackNet entries (checked, not assumed): `$m=@(Select-String -LiteralPath 'docs/evidence/LICENCE_LEDGER.md' -Pattern 'TrackNet' -Context 3,5); 'TrackNet_entries=' + $m.Count; $m` returns zero matches. A9 is now CONDITIONAL: it runs only once a concrete implementation, pinned version, and a licence receipt are named in the same commit as the prereg seal; absent that receipt A9 = LIMIT before heldout, with no substitute arm, and A10 (= A8 + temporal) is likewise unavailable whenever A8 lacks boxes. [B,LL]
GATE, corrected: the draft's A0 screening gate C0 >= 0.05 is a NEW threshold, distinct from G363's own gate (>= 0.50). Crossing 0.05 does NOT "resume" the historical x3 row (C1 >= 3 x C0): that row's executions are consumed and its verdict is LIMIT, final. Before any new A0 inference, seal this successor's own execution accounting and reproduce the archived A0/A7 counts against today's deployed route/weight hashes (A11) first. [B,C363,M363,V B10/B11]
SUPPLY MATH, corrected: 500 audited boxes cannot come from the 332 sealed development frames alone -- only 156 have a VISIBLE reference, so >= 344 additional distinct positive frames are required if every one of the 156 yields a usable box. At the observed 156/332 visibility rate, roughly 733 extra evenly sampled frames are needed in expectation before audit losses (not a quota guarantee); extra footage must be freshly pinned and decoded, sealed as a fixed larger sample before rating, negatives/UNKNOWN retained, adjacent contexts deduplicated, and never replenished after a detector score is seen. [B,E363/frames.csv,M363]
REFERENCE v2 PROCEDURE (astra): a BLIND FULL-FRAME pass first, independent NATIVE boxes per rater, optional 256-px crops only afterward (crop-first leaks the previous centre into a supposedly independent rating). diameter = max(box_w, box_h) in native px; name the population for BOTH medians separately; report per-frame disagreement, p90, and missingness on all scheduled keys, not median-only (a median alone can hide nearly half the bad localizations). Labels stay v1, or a separately declared and sealed v2 relabeling is used, with both censuses archived side by side -- never silently repair v1 while claiming it was kept. [B,C363,V B1/B8]
MATCHING, corrected: primary pass rule is G363's one-to-one CENTRE rule ONLY, centre within max(3 px, half the reference diameter) at 720p; box IoU >= 0.30 is a separately sealed SECONDARY diagnostic and must never enter the primary pass/fail decision (C363 implements centre-distance only; v1's "IoU OR centre" is not that rule). UNKNOWN-frame predictions count FP; N includes ABSENT and UNKNOWN. [C363,V B10]
BAR PRECISION, corrected: C >= 0.25 over 285 VISIBLE implies integer TP >= 138, i.e. recall >= 138/285 = 48.4211 pct -- report it as 48.42 pct, NEVER as "50 pct" (that needs TP >= 143, C >= 0.2604736). ALL FP/N_absent <= 0.01 permits at most 2 FP at TP >= 138, which also comfortably clears Wilson lower >= 0.90.
BUDGET: cumulative training across A8/A9 <= 120 GPU minutes, preregistered capped steps/batch/seed; report inference minutes separately and archive checkpoints by sha256 under the evidence dir, never under `models/`.
Q1 SEAL ORDER (astra): seal the design/premise protocol before any new metric. Seal exact frame/context hashes and the development/held-out split before ratings begin. Freeze the adjudicated v2 reference and every development-only model choice (arms, A9 receipt) before scoring. Seal the winner/weights/config before the ONE new held-out candidate run; mirror every phase off-pod. Reusing the already-inspected G363 held-out set is benchmark reuse, not a fresh independent corpus -- preserve its execution history and treat any new game-disjoint sample as a separate, later confirmation.
ACCEPTANCE RULE (astra's rewritten block; primary matching unchanged):
  metric/before = integer OBSERVED rank-0 TP/FP/FN, C = TP/549, Wilson95 TP/(TP+FP), ALL FP/N_absent, TP/N_visible; v1 A0 = 0 TP/8 FP and A7 = 2 TP/178 FP remain historical; evaluate A0 and the selected candidate against the same frozen v2 reference, archiving all 549 per-frame pairs. [C363,E363]
  reference = all 881 original unique keys and the 332/549 game split retained; native boxes and adjudication procedure sealed; both-VISIBLE median centre disagreement <= 0.5 x median native diameter, populations named, >= 30 pairs, plus per-frame/p90/missingness diagnostics; no arm scores if reference completeness or usability fails. [C363,V B1/B8]
  bar = C >= 0.25 AND Wilson lower >= 0.90 AND ALL FP/N_absent <= 0.01, >= 150 VISIBLE / >= 150 ABSENT held-out; all 549 present, UNKNOWN predictions count FP; G363 one-to-one centre <= max(3 px, diameter_720p/2), no IoU alternative in the primary pass rule. [C363,V B10]
  training/selection = >= 500 unique adjudicated development boxes / >= 5 games; fixed extra-frame sample, no held-out game/context leakage; cumulative training <= 120 GPU minutes; freeze the full arms list and tie/fallback rule before scores; A10 unavailable whenever A8 lacks boxes; A9 needs a concrete licence/dependency receipt, never substituted after seeing results. [LL]
  n/eye = 881 unique reference frames, 549 scored held-out frames (not doubled by rater count); >= 30 applicable units per scored submetric, smaller groups labelled descriptive; 30 evenly spaced held-out native renders with both ratings, the reference, and the winner, across all outcome types. [V Q7/B7]
  must not move/outcome = deployed weights/code/settings, G344 thresholds, G357 radius, every flag, the registry, the daemon frozen; no missing-reference or held-out-dependent exclusion. DONE only on every bar; failed measurements PARTIAL/NOT VALIDATED; an infeasible reference or arm is CLOSED AT LIMIT as scoped; retain baseline, skipped-arm reasons, both prereg seals, all outputs, and off-pod hash receipts.
EVIDENCE: `docs/evidence/tracking/g373_ball_detector_v2_2026-09-10.md` (<= 60 lines; VERDICT line 1; NOT VERIFIED; GPU minutes; SHA-256s) + `.../ratings_v2.csv,reference_v2.csv,dev_boxes.csv,arms.csv,predictions.csv,frame_scores.csv,licence_receipt.json,summary.json,weights/,sheets/`. ADD ONE RESULTS_LEDGER.md ROW SAME COMMIT.
TEST: `tests/platformkit/test_g373_ball_detector_v2.py` alone (centre-only matching; UNKNOWN prediction = FP; even sampler never a head slice; A10 unavailable when A8 lacks boxes; the seal). NEVER a full pytest. Every new file <= 300 lines. Vocabulary follows contract Q6; automated scan required.
COMMIT: explicit pathspec only; prereg sealed as its OWN commit first (`SEAL sha256 <hex>`). ASCII stdout. NEVER PARK.
VERSION 2026-09-10
