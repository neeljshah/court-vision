GAP S192 | sport wnba | worktree a12 | log cx_s192_wnba_ingame_census
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check against every line of section B AND section Q (Q1-Q9) before you report.
PREMISE (step 0): re-measure and print all of these; if any headline is falsified, STOP, write the memo, commit, report FALSIFIED. Measured read-only 2026-09-04 by the spec-writer, all reproduced: data/cache/inplay_odds/wnba_price_series.parquet = 967,102 rows / 287 events (spread 484,729/130, total 264,671/59, moneyline 217,702/98); moneyline in-play (ts < close_time epoch) = 217,408 ticks / 98 events, median 2,380 ticks/event, 98/98 at >= 100. scripts/platformkit/ingame/wnba_outcome_resolver.py resolves home_win on 95/98; an ESPN-FREE label (ticker side suffix + result_where_known) settles 98/98 and agrees with the resolver on 95/95, 0 disagreements. data/domains/wnba/cdn_backfill/*/playbyplay.json = 168 games / 84,143 actions, 84,143 (100.00 pct) carrying timeActual + scoreHome + scoreAway (plus clock, period, personId), wallclock 2026-04-25T19:03:10.0Z..2026-07-04T04:31:59.3Z. data/domains/wnba/cdn_backfill_states.parquet = 504 rows / 168 games / exactly 3 checkpoints each (end_q1 168, half 168, end_q3 168), lineup_home and lineup_away nonnull 504/504 -- three CHECKPOINTS, not a lineup series. Bridges: 95/98 priced events to an ESPN event_id via the resolver name index + espn_scoreboard.parquet (800 rows); 166/168 state games via linescores.parquet (768 rows) on date +/- 1 day + the six quarter-cumulative scores; INTERSECT = 85 games. Those 85 hold 186,991 moneyline rows of which 186,736 are in-play, and 19,456 (10.42 pct) fall inside their own game's pbp wallclock span (per game: 85/85 > 0, median 250, p90 274, 84/85 at >= 100, 67/85 at >= 150) -- i.e. 89.58 pct of the in-play ticks are OUTSIDE the pbp span and are not recoverable state. before = 0 is established by two closed rows, not by absence of prior work: S80 (register line 150) measured WNBA at 0.00 pct of in-game ticks carrying player/lineup state, S82 (line 153) measured 0 settled WNBA ticks in the joined stores; 'wnba' appears in exactly 3 register rows (S80:150, S82:153, S95:174) and in no state-joined store (data/cache/ingame_grade_joined holds mlb, mlb_clean, soccer_intl only; no wnba checkpoints parquet exists in data/cache/inplay_odds).
LIMIT (step 1): the pbp wallclock span is the ceiling -- only 19,456 of 186,736 in-play ticks (10.42 pct) can ever receive an as-of state from this corpus, and the live capture has written only 3 WNBA games (data/cache/ingame_grade/wnba, 280 rows) so no second route to n exists. If the joined count lands below the bar, report CLOSED AT LIMIT and do not fix.
CHANGE (step 2): the smallest thing that works -- one new module under scripts/platformkit/ (mirror scripts/platformkit/venue_history/nba_wallclock_join.py: states_from_payload -> join_game_states -> assemble_output) that reads the pbp JSON offline (NO network), derives per-action (ts, period, game_clock_s, score_home, score_away, margin) and calls the EXISTING scripts/platformkit/eval_gate/asof_join.asof_join_state at max_staleness_s = 300.0, writing ONE new parquet data/cache/inplay_odds/wnba_checkpoints_full.parquet. Additive only: new file plus new parquet, no existing column renamed or removed, no existing module edited. Reuse the resolver and asof_join as they are; do not reimplement either.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = count of in-play WNBA moneyline ticks (ts < close_time, denominator 186,736 over the 85 intersect events; full-store denominator 217,408 over 98) that receive a non-null as-of state (period, game_clock_s, score_home, score_away, margin) from cdn_backfill playbyplay through asof_join_state at max_staleness_s = 300.0, plus the printed state-age distribution (median, p90, share above 300 s) and the per-game joined-tick counts
  before        = 0 (S80: WNBA 0.00 pct of in-game ticks carry player/lineup state; S82: 0 settled WNBA ticks in the joined stores; no wnba directory in data/cache/ingame_grade_joined, no wnba checkpoints parquet in data/cache/inplay_odds)
  bar           = >= 15,000 state-joined ticks over >= 60 of the 85 intersect games, AND the census reproduces EXACTLY: 98 priced moneyline events, 217,408 in-play ticks store-wide, 95 resolver-labelled, 95 bridged to event_id, 168 pbp games, 84,143 actions at 100.00 pct timeActual+score, 504 lineup rows over 168 games at 3 checkpoints each, 166/168 state games bridged, 85 intersect games, 186,991 all-ticks and 186,736 in-play ticks on those 85; the two span-dependent counts (19,456 inside-span, 10.42 pct) reproduce WITHIN 1 pct, not byte-exact, because they move with the span boundary convention -- 186,991 is the ALL-ticks denominator (pre-tipoff quotes included) and must never be printed as the in-play one
  n             = 85 (games/clusters, >= 30)
  eye check     = n/a (S-row); reproduction = the verifier reloads the new parquet and recomputes the joined-tick count, the game count and the state-age median directly from it, and re-derives the 85-game intersect from the five source files named above
  must not move = scripts/platformkit/eval_gate/asof_join.py:25 max_staleness_s stays 300.0; asof_join.py, wnba_outcome_resolver.py, nba_wallclock_join.py, nba_checkpoints_full.parquet and every existing gate corpus byte-identical; data/cache/eval_gate/backtest_fwer.jsonl (18 rows) untouched and K unread -- this is a CENSUS, not a scored trial, so no ledger row is charged, no prereg is needed and no Brier or improvement number may be published from this lane
NON-TAUTOLOGY: the metric covers ALL 186,736 in-play ticks on the 85 intersect events -- the 167,280 ticks outside the pbp wallclock span are counted in the denominator and reported as unjoined, never excluded. The 13 priced events and 83 state games that do not bridge are named in the memo and excluded from n only because they have no counterpart, which the memo states with counts. If the joined share is reached by narrowing the denominator to the in-span ticks, the metric is circular -- say so and report REJECT yourself.
EVIDENCE: docs/evidence/harness/wnba_ingame_census_2026-09-04.md -- before/after table, n, exact denominators, the per-game joined-tick table (85 rows), the state-age distribution, the list of unbridged events/games, and a "NOT VERIFIED" list.
REQUIRED EVIDENCE DURABILITY: copy under docs/evidence/ the summary JSON and the per-game rows behind every number; the new parquet alone is not enough if it is not reproducible from the named sources.
RE-EMITTED TABLES: the new parquet carries the FULL nba_checkpoints_full column set (game_id, game_date, ts, period, game_clock_s, score_home, score_away, margin, market_prob, traded, outcome_home_win) plus event_key, not only the columns this lane reads.
TEST: exactly one new per-file test; run only that file.
POD: not needed -- this is a local read plus one parquet write. No scp of any module, no git on the pod.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
