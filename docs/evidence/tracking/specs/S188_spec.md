GAP S188 | sport harness | worktree a10 | log cx_s188_preflight_tree_gate
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- self-check every line of section B AND section Q (Q1-Q9) before you report.
PREMISE (step 0): re-measure first, read-only. Measured 2026-09-04 on this box. (a) The boot gate at pod_bootstrap.sh:56-59 is `pod_bootstrap_check --profile paper`, whose only import set is the 14 kind=py ProcSpec modules of config/boot/paper.json (profile_modules, pod_bootstrap_check.py:119-122; check_imports :142-161; exit `1 if problems else 0` :303-307). (b) An 8-case sys.meta_path blocker over the 7 unshipped trees (ops, kernel, governance, data_registry, improve, frontend, src) each blocked ALONE plus all 7 together gives: ops 14/14, kernel 14/14, governance 14/14, data_registry 14/14, improve 14/14, frontend 14/14 -- exit 0, the stack boots with the tree absent; src alone 13/14 and all-7 13/14 -- exit 1 (the single FAIL both times is scripts.platformkit.ingame.inplay_capture_runner, ModuleNotFoundError: No module named 'risk'). So 2 of 8 cases fail closed and 6 are blind. (c) The sharp case: with ops blocked the gate reports 14/14 OK, yet `import supervisor.supervisor` -- the module `-m supervisor` loads at pod_bootstrap.sh:70-76 -- raises ImportError: No module named 'ops', because supervisor/supervisor.py:26 imports scripts/platformkit/autonomy/heartbeat_reaper.py, whose line 33 is a MODULE-LEVEL `from ops.circuit_breaker import CircuitBreaker, OPEN`. The gate never imports supervisor.supervisor. If any of (a)-(c) fails to reproduce, STOP and report FALSIFIED.
CONTEXT (measured, not the bar): the closure share is convention-dependent and both published numbers reproduce from one walker. Rule = seed the 14 ProcSpec modules; module-level means ast top-level body plus the bodies of top-level if/try/with/for/while/class; full adds function bodies; first-party = resolves to a .py or package __init__.py under the repo root. With `from X import Y` resolving ONLY to X: 128/436 = 29.36 pct loaded, 50 unshipped-tree modules, 41 (82.00 pct) lazy-only. Expanding X.Y when that submodule file exists: 189/553 = 34.18 pct, 58 unshipped-tree modules, 48 (82.76 pct) lazy-only. State which convention you used wherever you print it. Do NOT make the closure share the acceptance metric -- it is a description, not a gate.
LIMIT (step 1): measured 2026-09-04 -- importing the 7 tree roots plus supervisor.supervisor takes 0.32 s total on this box and all 8 succeed today, so cost is not a bound and the bar below is reachable. If any of the 8 fails to import on a COMPLETE tree, the gate would false-alarm: report that module and STOP as CLOSED AT LIMIT rather than weakening the check.
CHANGE (step 2): the smallest additive change, in the safe area only. New file scripts/platformkit/ops/deploy_tree_gate.py (~60 LOC): a named tuple of the 7 tree roots plus "supervisor.supervisor" (each entry commented with why it is on the list), imported in ONE child via the existing check_imports from pod_bootstrap_check (reuse it, do not re-implement the probe), printing one `TREES: k/8 OK` line plus a `FAIL <mod> -- <err>` line per failure, exit nonzero on any failure. Wire it as a SECOND boot gate in pod_bootstrap.sh step 3, immediately after the existing check, in the same `if ! ...; then echo ...; exit 1; fi` shape. Do NOT edit pod_bootstrap_check.py: it is 311 LOC already and its default output must stay byte-identical. Separately, the one-line doc fix: pod_bootstrap.sh:75 and docs/operations/runbook step 3 (runpod-runbook.md:341) both say "expect 15 children" while load_profile('paper').specs() returns 14 -- correct both to 14, and say in the memo that 15 was consistent with counting the supervisor parent plus its 14 children (S30). That echo gates nothing; it is a doc string, not a criterion, so do not report it as a broken check.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = number of tree-absent CONSTRUCT cases in which the pod_bootstrap.sh step-3 boot gate (existing check AND the new tree gate, composed) exits NONZERO. Denominator = 8 cases: each of ops, kernel, governance, data_registry, improve, frontend, src blocked alone under a sys.meta_path blocker, plus all 7 blocked together
  before        = 2 of 8 (measured 2026-09-04: src alone and all-7 exit 1 at 13/14; ops, kernel, governance, data_registry, improve, frontend each exit 0 at 14/14)
  bar           = 8 of 8 exit nonzero, and 1 of 1 control case -- no tree blocked -- exits 0 with `TREES: 8/8 OK`
  n             = 8 (CONSTRUCT -- the unshipped-tree set is enumerated exhaustively; the verifier checks exhaustiveness, not n>=30)
  eye check     = n/a (S-row); reproduction = run the 8-case blocker harness against the composed step-3 gate and read the exit status of each case, then re-run the control with no blocker
  must not move = scripts/platformkit/ops/pod_bootstrap_check.py byte-identical; the 14 ProcSpec names in config/boot/paper.json; the 7 functional probe names and _PROBE_TIMEOUT_S = 60.0; the default `IMPORTS (%s): %d/%d OK` line format and the existing gate's exit-status semantics; data/cache/eval_gate/backtest_fwer.jsonl byte-identical (18 rows) with no ledger row charged and K unread; data/registry untouched; no flag flipped ON
NON-TAUTOLOGY: the metric covers exactly the 7 top-level trees named in the register row plus their union, and nothing else. A root import proves the tree ROOT is present, not that every module inside it is -- say so; the CONSTRUCT being closed is tree ABSENCE, which is what the row claims and what a fresh `git archive` deploy actually gets wrong. Excluded and named: partial trees (a tree present but one module stale) stay uncaught, and the 364 lazy-only modules of the full closure stay unloaded by design. If you find a tree root that imports on an otherwise-absent tree (a stray namespace package on the pod), report REJECT yourself.
EVIDENCE: docs/evidence/harness/preflight_tree_gate_2026-09-04.md -- the 8-case before/after exit-status table plus the control row, the ops-blocked supervisor.supervisor ImportError quoted verbatim, both closure conventions with their rule stated, the blocker harness script copied under docs/evidence/ so a verifier can reproduce, and a NOT VERIFIED list (at minimum: nothing was measured on the pod).
TEST: exactly one new per-file test; run only that file.
POD: not needed -- local construct only. No ssh, no scp, no git on the pod. Report the files you would deploy; do not deploy them.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
