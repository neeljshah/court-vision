GAP G400 | sport basketball | worktree a7 | log cx_g400_ball_reference_growth_stage1
**RANK 1 - GROW THE AUDITED REFERENCE BEFORE ANOTHER ARM. Landed docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/dev_boxes_v3.csv SHA-256 e151f932c3b4bffe84c89aa8fde18f08c346a1e3a6e65d27ed4b36f095b7b4fa fixes 530 boxes/27 games; docs/evidence/tracking/g386_pin_copy_one_pass_2026-09-10/summary.json SHA-256 e0e6119766899786a4004cd34e7587bacc615853cc87674c85ce4d06d297803f establishes retained replay supply. Landed G398 docs/evidence/tracking/g398_a8_high_resolution_dev_shadow_2026-09-11/summary.json SHA-256 6e2feaa75587429934b857d6b86d73da7536ac31f5e0b6e28286138e1eee0baf closes the fixed scale option.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 20 min; pod a7 retention/native decode and PC receiver 35; PC paired ratings/adjudication 100; PC export/reproduction/test 25. No model inference or training; no GPU reservation while raters work.
OWNS: scripts/platformkit/tracking/g400_*.py; tests/platformkit/test_g400_ball_reference_growth.py; docs/evidence/tracking/g400_ball_reference_growth_stage1_2026-09-11.md and matching directory. Full source bytes stay in a row-owned off-repo receiver directory named in its receipts.
PREMISE step 0 (BINDING before-condition): re-count 530 unique old boxes/27 games and all old DEV/held-out/context identities; census the whole available native source pool using /workspace/data/tracking/track_daemon_ledger.jsonl fields game_id/source_height/source_fps plus actual object probes and resolved game identities.
Require >=30 retained, decodable, genuinely NEW basketball games across >=2 competitions, with a source span admitting ten distinct interior targets; exclude unresolved game identity and alternate uploads in a named census, BEFORE drawing. The 30 old G386 objects are tools' evidence, not new games. Insufficient supply -> PARTIAL inventory, no rating dispatch or live-feed wait.
METHOD (sealed before collection/rating metrics):
1. Sort eligible new games by competition/canonical game/source digest; draw 30 at floor(j*(N-1)/29+0.5), j=0..29, and one section/game by the sealed median-PTS section rule. Freeze all exclusions and the full population; select no frame by a ball detector or visibility cue.
2. Retain each selected source with the committed G386 one-pass receiver sequence; PC independent raw-byte hash and native decode must precede release of the pod copy. Batch transfer within <2 GB; preserve failure rows and acknowledge only decoded receiver objects.
3. Probe the retained object's full decoded PTS schedule. Draw ten native frames nearest fractions k/11, k=1..10, ties earlier; require >=1 s spacing, distinct raw pixel hashes, no old causal-context overlap. Keep all 300 planned keys even when decode fails; never resample, upscale, replace or use old centres to crop.
4. Freeze native sheets/manifest with actual dimensions and sheet_scale=1.0 for EVERY new key. Preserve the old reference separately; do not pass native centres through G373's inherited 0.5 sheet metadata. Native-to-720p uses actual source height, including 720p sources.
5. Ten paired rounds each contain one target from every game, giving 30 NEW unique frames per rater batch. Seal all round orders before dispatch; terra and sol independently rate all 300 full native frames VISIBLE/ABSENT/UNKNOWN, with native boxes or centre/diameter, without peer answers, predictions or prior labels.
6. Archive raw answers and per-batch image-open/runtime receipts before reconciliation. Compute pre-adjudication three-label Cohen kappa >=0.60 on EACH complete n=30 batch and overall n=300; this is a prospective reliability gate, not an inherited G389 measured bar. Undefined kappa is UNKNOWN, never agreement=1.
7. After raw judgments seal, Claude independently resolves EVERY disagreement, invalid box and discrepant centre from native pixels; retain uncertainty and both originals. Audit every proposed retained box at native zoom, plus 30 even all-state cards including silence/UNKNOWN. Adjudication cannot erase failed raw agreement.
8. Apply inherited usability: median primary centre gap <=0.5 times median native diameter, naming both populations; >=30 both-VISIBLE pairs overall, per-key gaps, p90/max and missingness. Report per-resolution diagnostics; groups <30 are descriptive only. Count one unique audited valid box/frame, no causal-neighbour duplicates.
9. Complete all 300 states; yield = accepted new boxes/300, not boxes/VISIBLE or judgments. At <=0.40 STOP this uniform-growth route AT LIMIT; >0.40 plus every reliability/usability bar permits proposing the next fixed collection stage only. No quota-based early success; deadline/incomplete judgments -> PARTIAL with unvisited distinct from reviewed UNKNOWN.
10. Preserve all 530 old boxes unchanged and export an additive full-schema DEV reference, new boxes, negative/UNKNOWN states and game contributions. Full milestone remains >=970 added boxes from >=30 new games and >=1,500 total/>=57 games; stage 1 cannot be represented as meeting it.
11. Run 30 even native planted-centre/transform controls: exact roundtrip within numerical tolerance and 30/30 centre-rule matches with zero FP. These are scorer controls, never detector evidence. Reproduce all tables/render trees twice; archive original judgments and disclose rating/auditor repeatability unmeasured.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| New-game reference identity/completeness | 530 boxes/27 old games | 300/300 planned states accounted; 30 new games; zero old game/context overlap; raw receiver identities exact | 300 frames/30 games; failures retained | All proposed boxes plus 30 even all-state cards | Old reference, split keys, native pixels | Fault NOT VALIDATED; incomplete PARTIAL |
| Raw rater reliability and scale | Prior native centre median 17.33 px; new kappa unmeasured | Every batch and overall kappa >=0.60; inherited median rule; >=30 paired centres; 30/30 transform controls | Ten n=30 batches; pooled n=300 | Native audits above and 30 controls | Raw judgments; sheet_scale=1.0; no adjudicated kappa | Failure NOT VALIDATED for supply use |
| Stage yield and training lock | 530 total; planning yield about 0.40 | >120 audited new boxes/300 AND reliability pass permits next-stage proposal; full lock >=1,500 boxes/>=57 games | All 300; game contributions explicit | Same whole-stage review | No arm allocation, training, score or hidden top-up | <=120 CLOSED AT LIMIT for this sampling route; complete passing stage DONE only |
EVIDENCE: docs/evidence/tracking/g400_ball_reference_growth_stage1_2026-09-11.md plus directory: prereg.md, census.csv, game_disjointness.csv, draw.csv, source_receipts.csv, pts.csv, native_manifest.csv, batch_plan.csv, rater_raw/, batch_receipts.csv, ratings.csv, kappa.csv, adjudications.csv, box_audit.csv, reference.csv, new_boxes.csv, transform_controls.csv, yield.csv, summary.json, repeats.json, renders/, common_receipts/, SHA256SUMS with byte-domain declaration.
TEST: python -m pytest tests/platformkit/test_g400_ball_reference_growth.py -q; cross-upload game overlap, no post-draw substitution, UNKNOWN versus unvisited, undefined kappa, duplicate completed answers, native scale roundtrip, whole-stage yield denominator and training lock.
VERSION 2026-09-11 v1 PROPOSED; one bounded annotation stage, not a promise of 970 boxes in three hours; no lower agreement bar, repeat rating or automatic next batch after completion.

COMMON BINDING CONTRACT (astra round 7, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B including A7/A9/A11/B11/H1/S1/S2/S4/S5, plus Q1/Q3/Q6; each block inherits these rails. Each is codex terra PREPARE plus Claude finisher MEASURE, then independent verification; <=180 minutes including preparation and receipts, no unattended leftovers.
Orchestrator confirms the named free owner and performs the binding whole-set premise census BEFORE dispatch. These read-only proposals do not assert fresh source availability. Missing supply -> PARTIAL with exact inventory; an already completed equivalent -> FALSIFIED. Never replace a failed draw or silently extend a budget.
Each OWNS only its named new helper prefix, one test, memo/evidence directory and named PROPOSED diff; no shared-module, src/, daemon, registry, flag or deploy-tree edits. Future finishing uses explicit pathspecs in its assigned worktree and contract C; this review changes no register or git state.
PREPARE seals protocol/bars alone before any metric, then exact input, source, code, model, environment and configuration identities before dependent work. Keep raw answers, full inherited schemas, all ABSENT/UNKNOWN/failed/planned states, exclusions and NOT VERIFIED; missing is not zero.
Pod work only through ~/bin/pod_run <owner> --ship <owned paths> --fetch <owned outputs> -- <command> in /workspace/wt/<owner>/; source/deploy paths read-only. Gate actual VRAM/CPU load, threads 1, <=6 cores; bulky writes require a successful pod write probe, scratch <2 GB; quota usage UNKNOWN is never zero. Never restart or interrupt a peer.
PC raters share the <=3-active-codex/free-RAM>=2.8-GB gate with G399; two raters at most, queued sequentially if capacity is occupied. No nested rating fleet. Poll own work to completion or terminate it normally at the sealed deadline and report exact unfinished keys.
Durable evidence includes summary.json, per-unit tables, seals, full paths/bytes/native dimensions/fps/PTS/raw digests, runtime/log receipts, even render index, resource costs and receiver readback. Keep replay objects off-pod until all declared readers finish; G386 does not prove same-size in-place mutation detection.
Two fresh processes must reproduce saved-table arithmetic and all delivered render digests from committed code, including matching the delivered tree; this proves arithmetic/rendering only. Single producer runs and ratings remain descriptive. Scan ALL landable text including .log.txt with character-code-built Q6 patterns; print counts only; helpers <=300 LOC, full-package imports, one per-file test each.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy); **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; counts only); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round7_2026-09-11.md (local-only). G397's evidence lands on master after its verify; G399's after its finisher + verify; a premise step that needs either reads the lane tip named by the orchestrator until then.
