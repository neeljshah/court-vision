GAP S29 | sport all (ops) | worktree a12 | log cx_s29_fwer_ledger_backup
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): the FWER audit trail has no backup: `data/cache/eval_gate/backtest_fwer.jsonl` (13 rows verified) is what every `deflated_p` in the program is computed against, `hypotheses.sqlite` (S15) will join it, both are gitignored and pod-authoritative, and a volume loss or bad write makes every past verdict unreproducible.
READ (exact paths, re-checked on disk 2026-09-03): data/cache/eval_gate/ holds exactly backtest_fwer.jsonl (13 lines), backtest_fwer.jsonl.lock, e4_promotion_trial_2026-09-01.json, hedge_trial_2026-09-01.json, gate_manifest.json -- no backup. scripts/platformkit/eval_gate/backtest_runner.py `_charge_ledger(path, spec, sport, start, end) -> dict` -- THE only writer of that file; you do not call it, you do not import it. scripts/platformkit/eval_gate/ledger.py (the reader). The row schema you assert on: {at, predictor, sport, start, end, k_cumulative}. CORRECTION to the register wording: the ledger is LOCAL-authoritative on this box, verified 2026-09-03 -- the pod does not hold it; say so in the memo.
PREMISE (step 0): list data/cache/eval_gate/ and any existing backup directory; print the line count of backtest_fwer.jsonl (expect 13), its sha256, and the `k_cumulative` of every row in order. Premise holds iff ZERO backups exist -> before = 0 backups. If a backup directory already exists, STOP, write the memo, commit, report FALSIFIED (a valid result).
LIMIT (step 1): n/a -- CONSTRUCT row (3 enumerated nightly runs). Print the ledger's size in bytes so the memo states the per-night cost honestly.
CHANGE (step 2): NEW scripts/platformkit/eval_gate/ledger_backup.py (<=300 LOC, stdlib only -- pathlib, hashlib, json, shutil, argparse):
  `backup_once(root: Path, *, out_root: Path | None = None, now=None) -> dict` -- copies data/cache/eval_gate/backtest_fwer.jsonl (and data/cache/eval_gate/hypotheses.sqlite when it exists; absent is FINE and is recorded, never an error -- S15 has not landed) into <out_root>/<UTC date>/ and writes manifest.json {source_path, sha256, bytes, rows, k_cumulative_max, copied_at, prior_night}. Default out_root = data/backups/eval_gate/ (gitignored under data/; NOTE the roadmap wrote data/cache/eval_gate/_backup/<date>/ -- this spec puts the copy OUTSIDE the directory it is protecting, which is the point of a backup; say so in the memo).
  NEVER WRITES THE SOURCE: open the source read-only ("rb"), never "a"/"w"/"r+", never through `_charge_ledger`, never take the .lock. Compute sha256 of the source BEFORE and AFTER the copy and ASSERT they are equal; a mismatch raises and the partial backup directory is removed.
  ASSERTIONS on the COPY (never on the original): row count >= the prior night's manifest row count, `k_cumulative` non-decreasing down the file, `k_cumulative_max` >= the prior night's. A violation raises with the two nights named and the copy is kept for inspection.
  `restore(backup_dir: Path, dest: Path, *, verify: bool = True) -> dict` and CLI `--restore <dir> --dest <path> --verify` -- re-checks the manifest sha256 against the copied bytes and refuses to write `dest` if it does not match, or if `dest` already exists unless `--dest` points somewhere new. `--verify` alone checks a backup dir and writes nothing.
  CLI: `--once` (one nightly pass), `--root`, `--out-root`, `--restore/--dest/--verify`. No loop, NO daemon. SCHEDULING: the memo gives the exact command line for the ORCHESTRATOR to arm and the job does NOT create it -- `schtasks /Create /SC DAILY /ST 03:00 /TN CourtVision-FwerLedgerBackup /TR "<python> -m scripts.platformkit.eval_gate.ledger_backup --once"`.
TEST: NEW scripts/platformkit/eval_gate/test_ledger_backup.py (beside the module). Everything on a tmp copy of a synthetic ledger; the real data/cache/eval_gate/ is NEVER touched: (a) three consecutive `backup_once` calls at three simulated dates over a ledger that grows 13 -> 14 -> 15 rows with non-decreasing k_cumulative -> 3 dated directories, 3 manifests, each row count >= the prior night's, k_cumulative_max monotone; (b) the source sha256 is IDENTICAL before and after all three passes (the read-only guarantee) and the source mtime is unchanged; (c) a ledger whose k_cumulative goes BACKWARDS, and one with FEWER rows than the prior night, each raise and name both nights; (d) `restore --verify` on a good backup returns OK, and on a backup whose bytes were tampered with after the manifest was written it REFUSES and writes nothing; (e) hypotheses.sqlite absent is recorded, not an error. Run ONLY `python -m pytest scripts/platformkit/eval_gate/test_ledger_backup.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = a restorable copy exists and its `k_cumulative` sequence is monotone and >= the previous night's max; denominator = 3 consecutive nightly runs
  before        = 0 backups exist (measured in step 0)
  bar           = 3/3 with a row count never below the prior night's
  n             = 3 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns the test in master, then runs `--verify` on the three tmp backup dirs and re-prints each manifest's sha256, rows and k_cumulative_max
  must not move = the live ledger file data/cache/eval_gate/backtest_fwer.jsonl (13 rows, sha256 byte-identical before and after -- No `_charge_ledger` call, no lock taken, opened read-only), `k_cumulative` in every row, backtest_runner.py, ledger.py, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written). The backup itself is gitignored and NEVER committed.
NON-TAUTOLOGY: all three nights are checked, and the two failure constructs (c) are the cases a lenient rule would exclude -- a backup that accepts a shrinking or non-monotone ledger proves nothing. Say so. The assertions run on the COPY, so a failing night can never be "fixed" by editing the source.
EVIDENCE: docs/evidence/harness/S29_fwer_ledger_backup_2026-09-03.md -- step-0 listing, the source sha256 before and after, the three-night manifest table (rows, k_cumulative_max, sha256), the restore --verify output, the exact schtasks command line NOT run, test output, and a NOT VERIFIED list (say plainly: no OS task was created, no real nightly run has happened, hypotheses.sqlite does not exist yet (S15), and the ledger is LOCAL-authoritative -- the pod does not hold it, verified 2026-09-03). Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Do not ssh anywhere and do not scp anything.
COMMIT: explicit pathspec (module, test, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
