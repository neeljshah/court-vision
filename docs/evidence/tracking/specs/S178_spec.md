GAP S178 | sport mlb | worktree a15 | log cx_s178_ticker_date_cover
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check against every line of section B AND section Q (S-row) before you report.
PREMISE (step 0): every Kalshi ticker encodes its own ET game date (KXMLBGAME-<yy><MON><dd><hhmm><away><home>, plus a G1/G2 doubleheader suffix on 2 files). Load all 227 files of data/cache/ingame_grade_joined/mlb, drop duplicate (game_id, ts), run assign_real_game_seq on master, and compare each segment's ET tick-date range with its ticker's encoded ET date. Expect (re-measured 2026-09-03, read-only): 78,986 raw -> 77,327 unique rows, 227 game_ids -> 360 segments (n_multi 112, boundary_reasons {ts_gap 129, inning_decrease 4}); 247 segments cover their ticker's date, 113 do NOT; the 113 hold 18,776 of 77,327 ticks = 24.2813 pct across 98 tickers and ALL 113 carry a non-null settled `outcome`. Direct label proof (the lead exhibit, not the date argument): restricted to segments reaching inning >= 7 with a decisive last-tick score, the stored outcome disagrees with the leading team on 47 of 94 non-covering segments (50.0 pct, a coin flip) vs 10 of 172 covering (5.8 pct) -- a ~9x gap on the same code path; every store row has side == "home" so the leading-team test is unambiguous. Corroboration: 112 of 113 non-covering end at an unsettled in-play price (0.05 < p < 0.95) vs 141 of 247 covering ending settled-looking; 106 of 113 reach inning >= 5 (18,513 ticks), so these are substantially complete real games, not pre-game polling. Worked example KXMLBGAME-26JUL011310TEXCLE: seq 1 = 189 ticks 2026-06-30T22:42:03Z..2026-07-01T00:19:37Z (ET 06-30 only, max inning 7, ends home 2 - away 3, market 0.510, never settles) carries outcome 1.0, which is the 07-01 game's result (seq 2 = 366 ticks, ends 7-4, market 0.975). If falsified, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): count the covering-only clusters each affected readout keeps. Measured today: S06 284 -> 180 clusters (30,618 of 47,104 rows); S88 234 -> 149 clusters, informative 11,087 -> 10,483 rows on 149 clusters. If either covering-only cluster count is < 30, STOP and report CLOSED AT LIMIT with the measured count -- do not re-quote a readout that cannot support an interval.
CHANGE (step 2): ONE new module scripts/platformkit/eval_gate/ticker_date_cover.py. (a) `add_covers_ticker_date(frame)` returns a copy with an ADDITIVE boolean `covers_ticker_date` column computed per (game_id, real_game_seq) segment; reuse scripts/platformkit/ingame/ticker_date.ticker_first_pitch_utc for the encoded date (it already parses the G1/G2 suffix and already owns the ET conversion -- do not write a second ticker parser) and take the segment's ET tick-date range from its own ts values; a ticker with no parseable date yields False and is COUNTED and NAMED, never silently dropped. (b) a `--requote` main that recomputes the S06 and S88 headlines from their ARCHIVED series on covering segments only. Do NOT modify real_game_split.py, stacker.py, or any archived series. Do NOT reconstruct the true outcome of a non-covering segment and do NOT refit any model: non-covering ticks are QUARANTINED (excluded and reported), never re-labelled. No ledger row is appended -- this is an uncharged re-quote, K stays as read.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = share of scored MLB in-game ticks whose real-game segment does not cover its ticker's own encoded ET date. Denominators: 77,327 unique (game_id, ts) rows of data/cache/ingame_grade_joined/mlb; 47,104 rows of data/cache/eval_gate/s06_stacker_series_2026-09-03.csv; 33,920 rows of docs/evidence/harness/s88_phase_recal_2026-09-04.csv (11,087 of them informative)
  before        = 18,776/77,327 = 24.2813 pct store-wide (113 of 360 segments, 98 of 227 tickers); 16,486/47,104 = 34.9992 pct of the S06 charged partition (89 of 158 tickers); 11,634/33,920 = 34.2983 pct of the S88 eval set all-ticks but only 604/11,087 = 5.4478 pct of its INFORMATIVE ticks
  bar           = (i) `covers_ticker_date` produced for all 360 segments, reproducing 247 covering / 113 not and 18,776 non-covering ticks exactly, and joining to both series on (game_id, ts) with 0 unmatched rows; (ii) one re-quote artifact recomputing the S06 and S88 headlines on covering segments ONLY and publishing old vs new n, n_clusters, improvement, CI and verdict side by side, with BOTH the all-tick and the informative-subset non-covering share printed per readout (the S88 informative share is 5.4478 pct, not 34.2983 pct -- publishing only the all-tick share misstates that readout); (iii) each archived headline reproduced to < 1e-9 from its own archive BEFORE the covering-only number is computed; (iv) verdicts unchanged or honestly re-labelled -- no bar moved, no charge; (v) per-cluster paired-loss rows archived beside the summary (Q9)
  n             = 360 (CONSTRUCT, every segment in the store enumerated) + 47,104 + 33,920 scored ticks
  eye check     = n/a (S-row); reproduction = the verifier re-runs `python -m scripts.platformkit.eval_gate.ticker_date_cover --requote` in master and recomputes 247/113, 18,776/77,327, 16,486/47,104, 11,634/33,920 and 604/11,087 from the store and the two named series alone, then recomputes both covering-only improvements and CIs from the archived per-cluster rows without re-running any model
  must not move = data/cache/eval_gate/backtest_fwer.jsonl byte-identical (18 rows, no charge, K unchanged); stacker.BAR 0.004 and stacker.INCUMBENT 0.2070329295167757 (stacker.py:35-36); real_game_split.DEFAULT_GAP_HOURS 5.0 and DEFAULT_MIN_GAP_MINUTES 20.0 (real_game_split.py:49-50); every archived series file (s06_stacker_series_2026-09-03.csv, s88_phase_recal_2026-09-04.csv and .json) byte-identical
NON-TAUTOLOGY: the metric covers EVERY tick of the store and of both series -- nothing is excluded before it is computed, and the 5 segments of the 2 doubleheader tickers are included by name. The covering-only subset is the CONCLUSION, not the metric's denominator; it is reported with its own n and cluster count beside the full-set number so the exclusion is visible. A covering-only headline that improves is NOT evidence the model got better -- say so in the memo. Note that the earlier check of stored labels against each segment's terminal price found 0 disagreements and was VACUOUS BY SELECTION: the contaminated segments are exactly the ones with no settled terminal price. Do not repeat it; the leading-team test replaces it.
EVIDENCE: docs/evidence/harness/ticker_date_cover_2026-09-04.md -- before/after table, n, exact denominators, the 113 non-covering segments listed with ticker, encoded date, ET range, tick count, max inning, last score, market_prob and stored outcome, and a "NOT VERIFIED" list.
REQUIRED EVIDENCE DURABILITY: copy under docs/evidence/harness/ the summary JSON, the full per-segment coverage table (360 rows) and the per-cluster paired-loss rows behind every re-quoted CI. A number a verifier cannot recompute from these files alone is not a result.
RE-EMITTED TABLES: preserve the FULL column set of anything re-emitted; the coverage table keeps game_id, real_game_seq, n_ticks, encoded date, ET first, ET last, covers_ticker_date, max_inning, last home score, last away score, last market_prob and outcome.
TEST: exactly one new per-file test (scripts/platformkit/eval_gate/test_ticker_date_cover.py); run only that file. Cover: a segment whose ET range contains its encoded date -> True; the TEXCLE seq-1 shape (range strictly before the encoded date) -> False; a G1/G2 doubleheader ticker parsed, not dropped; an unparseable ticker -> False and counted.
POD: not required -- the whole measurement is 78,986 jsonl lines plus two CSVs, well under the local memory rail. If you do use the pod: heavy compute only, own nohup setsid nice job, unique /tmp log, never kill anything, no git on the pod, and NO scp of any module until the verifier accepts.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
