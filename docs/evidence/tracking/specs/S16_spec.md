GAP S16 | sport all (signals) | worktree a13 AFTER S11, S12 AND S15 LAND | log cx_s16_runner | EXTEND the existing runner; never write a second one
REDTEAM PREREQUISITES (2026-09-03, REDTEAM_SIGNAL_FACTORY memo 187101ea -- binding for this pass):
- The runner records screened_n per family per pass in its summary line (SF-2) and never runs a T2 on rows inside that family's screen partition (SF-1; partition comes from the tiers module).
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): the runner cannot run continuously on grammar: `run_pass` (:51) rebuilds a fixed matrix and sleeps 900 s (:88).
READ (read every signature on disk; line numbers drift): scripts/platformkit/foundry_runner.py (108 LOC) -- `PASS_CONFIGS` (verified at :20, NOT :23 as the S11 register row says), `_append_summary` (:45, already the DB seed), `run_pass(number)` (:51, takes ONLY `number` today), `run(max_passes, sleep_seconds=900.0)` (:88 default, `time.sleep` at :94, CLI default at :102), `main()` (:98); the EXISTING test `scripts/platformkit/test_foundry_runner.py` (it lives BESIDE the module at the platformkit root -- it must stay green); scripts/platformkit/foundry/{grammar,tiers,results_db}.py as landed by S11/S12/S15 -- if any is absent, STOP and report BLOCKED; scripts/platformkit/ops/pod_supervisor.py `build_specs` (it ALREADY keeps `foundry_runner` alive -- do NOT add a new Spec); eval_gate/backtest_runner.py `ledger_lock`.
PREMISE (step 0): `run_pass` today takes only `number` and rebuilds `PASS_CONFIGS[number % len(PASS_CONFIGS)]`; `run` sleeps 900 s between passes; screens/hour = 0 and the program's lifetime total is 13 charged trials. Print the current signature and the sleep default. If `run_pass` already accepts a queue, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): time ONE T0+T1 screen on a real gate corpus locally and print the per-screen wall. `3600 / per_screen_seconds` is the ceiling on screens per pod hour. If that ceiling is below 200, STOP and report CLOSED AT LIMIT with the arithmetic -- do not lower the bar (Q3).
CHANGE (step 2): ADDITIVE extension of `scripts/platformkit/foundry_runner.py` only. `run_pass(number, queue=None, batch=50)`: with claimable rows it pops up to `batch` hashes via `results_db.claim` and runs `tiers.run_tier` T0 then T1 on each; with an EMPTY queue it IDLES (`time.sleep(poll_seconds)`, default 60) and does NOT rebuild `PASS_CONFIGS`. A `--legacy` CLI alias keeps the OLD behaviour byte-for-byte -- the hardcoded matrix plus the 900 s sleep -- so no existing caller or Spec changes (B2). T2/T3 run ONLY from `tiers.promote`'s list, SERIALLY under `ledger_lock`. Cadence documented in the memo: T0/T1 continuous; promotion + T2 batch Monday 06:00 UTC; T3 immediately after a T2 AHEAD. Writes: `result` rows + trial JSONs, one `_append_summary` line per pass carrying `screens, promotions, charges, idle`, and a heartbeat file. No new dependency, no second runner, no new pod Spec.
TEST: NEW tests/platformkit/foundry/test_foundry_runner_queue.py: an EMPTY queue produces exactly one IDLE pass -- it does not crash, does not rebuild the old matrix (assert `PASS_CONFIGS` was not consulted), and appends 0 rows to a TMP ledger; a queue of 3 hashes runs 3 T0+T1 screens, appends 0 ledger rows, and writes a summary line carrying the four counters; `--legacy` reproduces today's matrix and sleep semantics. Run ONLY that file: `python -m pytest tests/platformkit/foundry/test_foundry_runner_queue.py -q`, then ALSO rerun `python -m pytest scripts/platformkit/test_foundry_runner.py -q` (must stay green) -- two files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = T0/T1 hypotheses screened per pod hour, and ledger charges arising from T0/T1; denominator = one wall-clock pod hour
  before        = 0 screens/hour (the runner rebuilds a fixed matrix and sleeps 900 s); 13 charged trials in the program's lifetime
  bar           = >= 200 T0/T1 screens in ONE pod hour AND 0 ledger charges from T0/T1
  n             = 200 (scored)
  eye check     = n/a (S-row); reproduction = verifier counts `result` rows with `verdict="SCREEN"` written inside the one-hour pod window and diffs `backtest_fwer.jsonl` across it (must be unchanged)
  must not move = the legacy path's matrix and 900 s sleep semantics (kept as the `--legacy` alias), `pod_supervisor.build_specs`, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows)
THE POD HOUR IS THE VERIFIER'S STEP, NOT YOURS: you deliver the module change, the new test, and the EXACT pod command that runs one hour and logs the counters. Report the files you would deploy; deploy NOTHING before ACCEPT (B5).
NON-TAUTOLOGY: every claimed hash in the hour counts toward the denominator, including screens that fail T0 -- a failed screen is a screen with its reason, never dropped from the count. No `_charge_ledger` call from the T0/T1 path at all; T2/T3 charges are S12's writer, serial under the lock.
EVIDENCE: docs/evidence/harness/S16_runner_2026-09-03.md -- the premise signature, the per-screen wall and the ceiling arithmetic, the four summary counters, the exact pod command, test output for both files, and a NOT VERIFIED list that says "the 200-screen pod hour has not been run". Calibration language only (Q6): no dollar, ROI, profit or edge word; a SCREEN is never a finding.
POD: heavy compute only, and only AFTER ACCEPT -- own nohup setsid nice job, unique /tmp log, pid from /proc, never kill anything, no git on the pod.
COMMIT: explicit pathspec (foundry_runner.py, the new test, the memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
