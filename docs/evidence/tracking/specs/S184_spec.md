GAP S184 | sport mlb | worktree a10 | log cx_s184_mlb_slate_date
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- self-check section B (B1-B10) AND section Q (Q1-Q9) before reporting. S-row: eye check = n/a, REPRODUCTION replaces render (A2 applies, A3 does not); n = 24 (CONSTRUCT) is exempt from the n>=30 rail.
DEFECT: mlb_book_capture.py:204 resolves the slate as UTC-today and passes that one string EXPLICITLY to :177 list_live_game_pks and to :181 -> :170 build_bridge, overriding the baseball-date default (UTC minus 10 h) that gumbo_mlb_poller.py:107-116 documents precisely because today-UTC "went blind every evening". Second blind caller, same shape: ingame_enrichment_runner.py:83 calls build_bridge() with no date.
PREMISE (step 0, re-measure before anything else): offline 24 h sweep over 2026-09-02 hours 00..23, fake live_games_fn, no network -- count hours where capture_once's resolved date differs from (now - 10 h). Re-measured 2026-09-04 = 10 of 24 (hours 00Z-09Z). If it is not 10, STOP, write the memo, report FALSIFIED.
EXPOSURE (context, never the bar): data/cache/ingame_grade/mlb, 405 files, 79,566 rows with a parseable ts; 41,202 = 51.7834 pct fall in a mismatching UTC hour; 321/401 game_ids have >= 1 such tick, 118/401 only such ticks; per-hour 00Z 15,240 / 01Z 11,510 / 02Z 7,425 / 03Z 4,806 / 04Z 1,985 / 05Z 236 / 06Z-11Z 0 / 12Z 1 / 13Z 1 / 14Z 2 / 15Z-23Z 38,360. This is the TICK STORE's own clock distribution, a PROXY for capture exposure -- never restate it as "the capture missed 51.78 pct of its ticks". Blind band 00Z-09Z = 20:00-05:59 ET: the evening slate is captured for its first hour (23Z, 15,595 ticks, the largest SEEN bucket) and goes blind at UTC midnight, mid-game.
LIMIT (step 1): none -- the metric is a clock enumeration, not a sample.
CHANGE (step 2): one new file scripts/platformkit/ingame/slate_date.py holding a single pure slate_date(now=None) -> 'YYYY-MM-DD' = (UTC now - 10 h), docstring citing gumbo_mlb_poller.py:107-116 as the rule's source; call it at mlb_book_capture.py:204 and at ingame_enrichment_runner.py:83. Additive: nothing renamed or removed, an explicit date_str still overrides, both edited files stay <= 300 LOC.
DATE SEMANTICS (do not invert this): a Kalshi MLB stem encodes the ET game date, so the bridge filter game_pk_bridge_live.py:233 (its default at :222) must compare against the SAME baseball date -- on-disk stem KXMLBGAME-26JUL012010MINHOU is a 20:10 ET start on 2026-07-01 that is 00:10Z on 2026-07-02. Fixing only the gumbo side trades a blind live-game lookup for a blind ticker resolution.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = UTC hours (0..23) at which capture_once's resolved slate date differs from gumbo_mlb_poller's own baseball-date default; offline sweep, fake live_games_fn, no network
  before        = 10 of 24 (hours 00Z-09Z), re-measured 2026-09-04
  bar           = 0 of 24, fixed now, never lowered
  n             = 24 (CONSTRUCT -- every hour enumerated, none excluded)
  eye check     = n/a (S-row); reproduction = re-run the 24-hour sweep in MASTER with a fake live_games_fn and recount the mismatches from the printed 24-row table
  must not move = TARGET_CADENCE_SEC 5.0, MAX_CADENCE_SEC 60.0, IDLE_CHECK_SEC 30.0, BRIDGE_TTL_SEC_DEFAULT 600.0, PRE_REGISTERED_UNIT 1.0, MIN_SNAPSHOTS 200, MAX_FETCH_CONCURRENCY 4 (mlb_book_capture.py:34-40); gumbo_mlb_poller.py and game_pk_bridge_live.py byte-identical (:233 filter semantics unchanged in kind); data/cache/eval_gate/backtest_fwer.jsonl untouched
NON-TAUTOLOGY: the metric covers all 24 hours, excludes none, and reads the resolved date string alone -- not capture output. The 51.78 pct denominator is every parseable-ts row in the tick store with nothing dropped; it is exposure context, not a re-scoring of capture output.
FORWARD READOUT (NOT the acceptance bar; SINGLE-WINDOW; an orchestrator step AFTER ACCEPT): distinct game_pk in the pod's data/cache/ingame_books/mlb/<date>.jsonl, before 6 on 2026-09-02 (S105 memo:62-63), watch for >= 12 on one live slate. That directory is absent locally (0 files). No pod deploy before ACCEPT (B5). Calibration language only (Q6).
EVIDENCE: docs/evidence/harness/mlb_slate_date_2026-09-04.md -- the full 24-row before/after table, the tick-store histogram with its denominator, and a NOT VERIFIED list. Copy the sweep output under docs/evidence/ so the verifier reproduces from the tracked tree alone.
TEST: exactly one new file tests/platformkit/ingame/test_mlb_slate_date.py -- the 24-hour sweep asserts 0 mismatches, plus one case that an explicit date_str still wins. Run only that file; never a full pytest.
FOLLOW-UP (file as a row, do NOT do it here): gumbo_mlb_poller.py:115-116 keeps a second inline copy of the -10 h rule -- dedupe to slate_date(), same shape as S164/S168.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
