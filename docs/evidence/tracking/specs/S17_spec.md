GAP S17 | sport all (harness) | worktree a10 | log cx_s17_decay_monitors | BUILD NOW, ARM AFTER S20 HAS >= 200 SETTLED ROWS | verifier LIGHT
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): decay monitoring alarms on n, not ESS: at the measured ICC 0.291 / design effect 87.4, 47,104 ticks are n_eff 539, so a 7-day tick window can be n_eff < 10 and the alarm is noise. One monitor exists where three are needed.
READ (read every signature on disk; line numbers drift): scripts/platformkit/eval_gate/ledger.py `LedgerRow` (fields `prob`, `outcome`, `ts`), `load`, `settled`, `brier_of`, and `drift_report(rows, now_iso, recent_days=7.0, ...)` -- it is ALREADY CORRECT (disjoint windows, baseline SE) and its window logic and `k_sigma` MUST NOT MOVE; scripts/platformkit/ingame/gap_effective_n.py `intraclass_correlation`, `design_effect`, `effective_sample_size`; scripts/platformkit/regime_calibration.py `buckets(df)` for the regime keys. scipy is already installed (chi-square) -- no new dependency.
PREMISE (step 0): `scripts/platformkit/eval_gate/decay_monitors.py` is ABSENT (verified 2026-09-03) and `drift_report` is the ONLY monitor, gated on raw n rather than ESS -- before = 1 monitor of 3, 0 ESS gates. If the module exists or `drift_report` already consults `effective_sample_size`, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): n/a -- CONSTRUCT row; the three constructed windows (thin / drifted / stable) ARE the enumerated case set.
CHANGE (step 2): NEW `scripts/platformkit/eval_gate/decay_monitors.py` (<=300 LOC):
  `monitor_all(rows, now_iso, fit_window) -> dict[str, Monitor]`; `Monitor(status, stat, n, n_eff, threshold)` with status in `INSUFFICIENT | OK | ALARM`.
  ESS GATE FIRST, before any monitor runs: `rho = intraclass_correlation(rows, "game_id", loss)` computed FROM THE MONITORED WINDOW -- never a stored constant, never either of the two published ICCs -- then `design_effect(rho, mean_rows_per_game)` and `n_eff = effective_sample_size(...)`. `n_eff < 30` -> INSUFFICIENT for that monitor, and it does NOT alarm.
  THEN three monitors: (a) calibration decay -- `ledger.drift_report(rows, now_iso, 7.0, 30.0, k_sigma=1.0)` called UNTOUCHED, with the standard error taken on `n_eff` rather than n; (b) crowding -- ALARM when the trailing-30-day mean of `|prob - p_close|` falls below 0.5 x its first-30-day mean; (c) regime drift -- scipy chi-square on the share of rows per `regime_calibration.buckets` key versus the fitting window, ALARM at p < 0.05, with expected >= 5 per cell (smaller cells merged into `OTHER`).
  ALL THREE ALARM; NONE disables anything -- a monitor that silently turns a model off is a monitor nobody trusts. Output `docs/evidence/calibration/decay_<date>.json` plus one line in docs/evidence/RESULTS_LEDGER_SYSTEM.md at landing. Additive only: `drift_report` itself is not edited.
TEST: NEW scripts/platformkit/eval_gate/test_decay_monitors.py (beside the module, like the other eval_gate tests): a thin synthetic window at n_eff ~ 9 reports INSUFFICIENT on all three and does NOT alarm; a drifted window reports ALARM; a stable window reports OK. Run ONLY that file: `python -m pytest scripts/platformkit/eval_gate/test_decay_monitors.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = INSUFFICIENT vs ALARM vs OK classification; denominator = 3 constructed windows (thin / drifted / stable)
  before        = 1 monitor of 3, 0 ESS gates (module absent; `drift_report` alarms on raw n)
  bar           = 3/3 -- thin -> INSUFFICIENT and no alarm, drifted -> ALARM, stable -> OK, each reproduced by the verifier
  n             = 3 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns the test in master and recomputes rho, the design effect and n_eff for the thin window from the JSON
  must not move = `drift_report`'s window logic and `k_sigma`, `gap_effective_n`'s formulas, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows)
ARMING IS A LATER ROW: build and land the module now with ZERO live callers; it is armed only after S20 has >= 200 settled forward CLV rows. Say so in the memo's NOT VERIFIED list.
NON-TAUTOLOGY: all three windows are classified and reported; none is excluded, and the thin window's INSUFFICIENT is a RESULT, not a skipped case. No `_charge_ledger` call anywhere -- monitoring is not a trial.
EVIDENCE: docs/evidence/harness/S17_decay_monitors_2026-09-03.md -- the three windows with n, rho, design effect, n_eff, each monitor's status and stat, the JSON artifact, test output, and a NOT VERIFIED list stating that no real settled series exists yet (S20). Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. No deploy before ACCEPT (B5).
COMMIT: explicit pathspec (module, test, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
