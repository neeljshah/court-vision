GAP G403 | sport basketball | worktree a3 | log cx_g403_ball_rater_failure_controls
**RANK 1 - EXPLAIN THE RAW FAILURE BEFORE MORE RATINGS. Landed docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/dev_boxes_v3.csv SHA-256 e151f932c3b4bffe84c89aa8fde18f08c346a1e3a6e65d27ed4b36f095b7b4fa fixes the accepted 530-box baseline. Pending G400 supplies docs/evidence/tracking/g400_ball_reference_growth_stage1_2026-09-11/ratings.csv SHA-256 3a746f5976cc44f9b86990c97edffcb1191454984f16d153dd2933f14bd20181 and kappa.csv SHA-256 808d26134409926c178e7ec628df7f4fad028d7220003bde059c31e7826f96e4 in that same directory; require its verified handoff, preserving NOT VALIDATED.**
WHERE THIS ROW RUNS: PC only; step 0/PREPARE 20 min, raw replay/diagnostic 50, existing-card inspection/control construction 50, reproduction/test/memo 25; <=145 min. No pod, detector, new rating or source-video decode.
OWNS: scripts/platformkit/tracking/g403_*.py; tests/platformkit/test_g403_ball_rater_controls.py; docs/evidence/tracking/g403_ball_rater_failure_controls_2026-09-11.md and matching directory.
PREMISE step 0 (BINDING before-condition): independently join ALL 300 sealed keys, ten paired rounds, 20 raw answer archives, batch/runtime receipts and eight redaction records. Verify G400 disposition and native-sheet hashes; require accessible originals or disclosed sanitized copies with reproducible semantic fields. Missing audit trail -> PARTIAL, no reliability explanation from substituted answers.
Reproduce pooled 299 pairs/kappa 0.750519, round 8 n=30/kappa 0.360465, round 1 n=29, 125 centre pairs and 288 raw VISIBLE diameter observations. Different verified bytes -> record updated before-condition and STOP for a new seal; never silently make the input match.
METHOD (sealed; retrospective DEV diagnostic, not a new performance claim):
1. Parse every archived answer by explicit frame_key and sealed batch order; retain raw label independently of box validity in a NEW diagnostic table. Keep the original G400 parsing/output untouched; enumerate duplicate ids, missing labels, invalid geometry and image-open evidence separately.
2. Recompute each round's three-by-three confusion counts, marginals, observed/expected agreement and kappa, plus missingness. Round 8 has 19/30 state agreements, 11 disagreements and VISIBLE marginals 16/16; balanced visibility counts do not establish the cause of disagreement.
3. Audit order/binding against all 20 sealed batches; inspect all 30 round-8 cards and 30 evenly indexed cards across the full ordered 300, plus every existing disputed-object crop. This is inspection of existing receipts/judgments, not a fresh rater vote or replacement label.
4. Recompute gap sqrt(dx*dx+dy*dy) on ALL 125 raw both-VISIBLE pairs, and native diameter (box_w+box_h)/2 on ALL 288 valid raw VISIBLE judgments. Report median/p90/max, missingness, native dimensions and per-round counts; never remove mismatched objects to lower the primary median.
5. Produce an exhaustive join of existing reasons to fixed categories RIM_HARDWARE, BENCH_BALL, JERSEY, OTHER_OBJECT, SAME_OBJECT_LOCALISATION, UNRESOLVED; dual-category evidence stays explicit. Distinguish an existing adjudicator assertion from a fact independently established by receipts. Unexplained round-8 cases remain UNRESOLVED; no invented dominant cause.
6. Reconcile the reported 61 pct shot claim with existing shot-specific reasons, retaining an UNCLASSIFIED bin; 126 ABSENT +58 UNKNOWN is not itself a non-play census. Report descriptive counts only for bins with n<30, without claiming prevalence from the selected conflict crops.
7. Freeze instructions: locate the active on-court game ball, name the chosen object and visible support; rim hardware, spare bench balls and jersey patches cannot supply a centre. Multiple plausible objects or obscured identity -> UNKNOWN; a clearly ball-free frame -> ABSENT. Preserve the full native view and coordinates; no peer labels or prediction markers.
8. Seal a CONSTRUCT catalogue of exactly 30 cases: six scenes (active ball alone, ball beside rim hardware, active plus bench ball, ball beside jersey patch, distractors without ball, identity obscured) x five layouts (1080p centre/left/right, 720p centre/right). Every case has explicit pixel geometry, occlusion mask and expected VISIBLE/ABSENT/UNKNOWN; hide only the answer key from future raters.
9. For four VISIBLE scene types, every layout has one designated on-court ball and exact known centre/diameter; distractors-only has no ball; identity-obscured has no recoverable centre. Preserve sheet_scale=1.0. Document these are exhaustive designed controls, not independent real-ball truth; G400 example cards are instructional DEV material only.
10. Do NOT administer the controls in G403. G404 gets one blind attempt per rater, 30/30 state decisions and inherited centre matches on all 20 VISIBLE controls; no feedback/retry after qualification. These controls cannot change any G400 judgment, score, reference acceptance or historical verdict.
11. Reproduce the archived metric tables and exact delivered control renders twice; log every sanitized field mapping, keep originals locally by hash, and make both the current text and independent reproduction accessible to the verifier.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Raw accounting/reproduction | 300 keys; 299 pairs; round 8 0.360465; round 1 incomplete | All keys/rounds/20 archives accounted; exact confusion integers and rounded G400 metrics; invalid box distinct from missing label | 300 states; round 8 30 | All round 8 plus 30 even all-state cards | Original labels, order, missingness, raw verdicts | Exact diagnostic DONE; missing evidence PARTIAL; altered answer REJECT |
| Centre and object explanation | 24.021 px vs13.75; 125 gaps; 288 diameters | Exact original denominator/median reproduction; every existing reason categorized or UNRESOLVED; no causal claim without evidence | All 125 pairs and 115 adjudications | Every existing disputed-object crop | No exclusion of large gaps; 0.5 diameter rule | Complete attribution DONE even if cause unresolved; unsupported explanation NOT VALIDATED |
| Instruction control package | No object-specific qualified control set | Exhaustive 6x5 catalogue; 30 exact render/answer bindings; 20 known VISIBLE centres; two identical regenerations | 30 CONSTRUCT cases | All 30 control images | Native scale, original reference, G400 failed bars | DONE for prepared controls only; ambiguity/incomplete bindings NOT VALIDATED |
EVIDENCE: docs/evidence/tracking/g403_ball_rater_failure_controls_2026-09-11.md plus directory: prereg.md, input_hashes.csv, answer_binding.csv, semantic_parity.csv, confusion_by_round.csv, gaps.csv, diameter_population.csv, reason_categories.csv, shot_claim_audit.csv, instructions.md, control_catalogue.csv, control_answers.csv, renders/, eye_index.csv, summary.json, repeats.json, runtime_receipts/, SHA256SUMS; include NOT VERIFIED and no-new-rating receipt.
TEST: python -m pytest tests/platformkit/test_g403_ball_rater_controls.py -q; valid label with invalid box remains distinct, missing/duplicate id detection, pooled pass cannot erase a failed/incomplete round, all 125 gaps retained, exhaustive 6x5 catalogue and native coordinate binding.
VERSION 2026-09-11 v1 PROPOSED; no G400 repair or retrospective qualification. G403 completion supplies sealed instructions/controls only; independent review is required before G404 uses them.

COMMON BINDING CONTRACT (astra round 8, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6. Orchestrator alone confirms NEXT_GAP_ID and free ownership, then verifies each whole-set premise BEFORE dispatch; these are prospective specs, not an assertion that today's supply qualifies.
Each row is codex terra PREPARE plus Claude finisher, <=180 minutes including receipts, with independent verification afterward. Seal protocol/bars alone before new metrics; bind exact population, code, model/config and input digests before dependent steps. Missing prerequisites -> PARTIAL; already completed equivalent -> FALSIFIED, no fix.
Own only the named new helper prefix, one test and evidence memo/directory. Helpers <=300 LOC; full-package imports; no src/, shared module, daemon, registry, flag or deploy-tree changes. Any necessary gated change is a separately named PROPOSED diff, applied nowhere. No held-out threshold sweeps, training or daemon restart.
Pod compute uses ~/bin/pod_run <owner> --ship <owned helpers> --fetch <owned evidence> -- <command>, solely in /workspace/wt/<owner>/; sources/deployed tree read-only. Measure actual CPU load/VRAM; <=6 CPU cores, threads 1, sequential GPU use. Bulky writes require a successful pod write probe and <2 GB scratch; unknown quota usage is UNKNOWN, never zero. Summary-only probes need no disk guard.
PC raters obey free RAM >=2.8 GB and <=3 active codex processes, at most two raters simultaneously; defer until capacity exists. Finishers poll their OWN background pod jobs to terminal status, including during transfer/ratings; never wait for a notification or leave a job unattended. Deadlines are built into owned jobs; do not kill/restart peers.
Evidence retains full schemas, all planned failures/ABSENT/UNKNOWN, exact input paths/bytes/native dimensions/fps/decoded PTS, source and route digests, raw answers, eye indices, exclusions and NOT VERIFIED. Retain source objects until every named reader finishes; no pruning in these rows. Fresh-process arithmetic/render reproductions prove only those operations, not rating or producer repeatability.
Reproduce saved tables and delivered renders twice from committed code; preserve pre/post-redaction hashes and semantic-field parity. Scan EVERY landable text artifact including runtime logs with Q6 patterns constructed from character codes, counts-only output. Finishers commit explicit owned paths and the required result row only; no push, bulk add or unrelated edits.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD before a pod stage -- the quota guard prunes ledgered sources ~45 min after tracking); finishers poll their OWN background pod jobs with bounded loops and never stop to wait for a notification; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; counts only; field-aware numeric handling with claim-field detection parity); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round8_2026-09-11.md (local-only). G404 runs on lane a18 (a9 is held for the user-only G395). G400's evidence lands on master after its verify; a premise step that needs it reads lane a7 tip 6b1a7601a until then; G402's after its verify (lane a13 tip b749346af).
