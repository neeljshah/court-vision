GAP S179 | sport mlb | worktree a13 | log cx_s179_fold_key_purge
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read A1-A7, B1-B10 and section Q (Q1-Q9); self-check every line of B and Q before you report.
PREMISE (step 0): re-measure both, read-only, before touching code. On data/cache/eval_gate/s06_stacker_series_2026-09-03.csv (47,104 rows / 158 tickers / 0 null y) reproduce stacker._first_dates (stacker.py:53-57, str(timestamp)[:10] min per game) and outer_walk_forward's rule (stacker.py:138-141, train_games = {g : first[g] < date}), then count train rows whose OWN timestamp is >= that fold's first test tick.
Reproduced 2026-09-03: 12 folds, 27,571 / 334,844 = 8.2340 pct; 149 of 957 train-game-fold slots carry >= 1 post-cut tick; worst 2026-07-01 5,515/7,118 = 77.48 pct, 2026-07-02 3,314/12,476 = 26.56 pct, 2026-07-04 3,277/19,217 = 17.05 pct.
The same rule sits in s88_phase_recal.walk_forward_inner_selected:94-128; on docs/evidence/harness/s88_phase_recal_2026-09-04.csv (33,920 rows / 127 games) it gives 9 folds, 18,043 / 183,410 = 9.8375 pct, worst 2026-07-04 3,277/6,033 = 54.32 pct. If either is falsified, STOP, write the memo, commit, report FALSIFIED.
MECHANISM (do not restate it wrong): each key is ONE real game. The cause is that a key's tick series spans a long PRE-GAME quoting window (median 22.4 h, max 50.59 h on KXMLBGAME-26JUL061915NYMATL), so first[g] is the date of its earliest pre-game tick while its in-game ticks land past the next fold's first tick. Re-keying on the real game's first tick is NECESSARY BUT NOT SUFFICIENT -- the LAST-tick purge plus embargo is what reaches 0.
stacker.py:141's game-disjoint assert cannot see this: disjointness is checked at KEY level and there is no last-tick purge. The correct rule already exists in-tree at foundry/ingame_screen.py:159-173 (last_ts :159, edge :166, train selection :168, purge assert :173) with a 1-day embargo. REUSE it by import; do NOT reimplement it and do NOT edit that module.
LIMIT (step 1): measured today -- under that rule with EMBARGO_DAYS 1, 10 of 12 S06 folds keep n_train >= MIN_TRAIN 1000; 2026-07-01 and 2026-07-02 fall to n_train 0 (NO_TRAIN). Expect 10 scored folds, not 12, and say so in the memo rather than leaving it to verification. If fewer than 6 of 12 folds keep n_train >= 1000, STOP and report CLOSED AT LIMIT with the fold table.
CHANGE (step 2): one additive opt-in fold rule (e.g. fit_window='game_last_tick_purged') in stacker.py and s88_phase_recal.py, defaulting OFF so the current ticker-first-date path and every landed archive reproduce byte-identically. Additive only: no rename, no removal, no reader left unchecked.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = train-row-slots whose own tick timestamp is >= their fold's first test tick
  before        = 27,571 / 334,844 = 8.2340 pct (S06, 12 folds); 18,043 / 183,410 = 9.8375 pct (S88, 9 folds)
  bar           = 0 such rows on BOTH corpora under the opt-in rule, the default path byte-identical, plus a per-fold n_train before/after table and the S88 re-quote below
  n             = 21 folds (CONSTRUCT, every fold enumerated) over 334,844 + 183,410 train-row-slots
  eye check     = n/a (S-row); reproduction = rerun the counter above on both CSVs and recompute the S88 phase Briers from the re-quoted per-tick series
  must not move = stacker.BAR 0.004, stacker.INCUMBENT 0.2070329295167757, MIN_TRAIN/MIN_REGIME/MIN_PATTERN 1000/200/200, EMBARGO_DAYS 1, data/cache/eval_gate/backtest_fwer.jsonl (18 rows, K stays 14, nothing charged), the archived series CSVs
SCOPE: S06's verdict is BEHIND -0.089910 against bar +0.004 and this row charges nothing, so no S06 number or ledger row can move -- the S06 half is a discipline re-quote only. The leverage case is S88: re-quote late|leading_big (+0.0316 [0.0088, 0.0572] IMPROVED), mid|trailing (-0.0120 [-0.0232, -0.0010] WORSE) and pooled (-0.0029 NO_CHANGE) under the purged rule with per-fold n_train before/after (before: 6,033 / 14,005 / 15,702 / 16,708 / 20,973 / 23,711 / 24,385 / 28,211 / 33,682); verdicts unchanged or honestly re-labelled, still SINGLE-WINDOW. The S88 9.8375 pct is a FLOOR: that archive holds only post-burn-in rows.
NON-TAUTOLOGY: the metric covers EVERY train-row-slot of every outer fold after the first (the first date has no train set); nothing is excluded. A fold that becomes NO_TRAIN under the purge is reported as 0/0 with its status, never dropped to flatter the percentage.
EVIDENCE: docs/evidence/harness/fold_key_purge_2026-09-04.md -- per-fold before/after table (date, n_train before/after, post-cut rows before/after, status), the S88 re-quote with CIs, n, exact denominators, and a NOT VERIFIED list. Copy under docs/evidence/ every artifact a verifier needs to reproduce a number (summary JSON + the per-fold rows), not only the memo prose.
TEST: exactly one new per-file test; run only that file. <= 300 LOC per file.
POD: not needed -- both corpora are local CSVs. Deploy nothing.
Q1/Q2/Q6: nothing is charged and the FWER ledger is never opened; calibration language only -- no dollar, ROI, profit or edge word, and none of the retracted figures anywhere.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha. NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
