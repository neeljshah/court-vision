GAP S189 | sport mlb | worktree a13 | log cx_s189_preflight_probe_floors
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check against every line of section B AND section Q (this is an S-row) before you report.
PREMISE (step 0): reproduce these exact numbers before touching anything. `scripts/platformkit/ops/pod_bootstrap_check.py:68-116` defines 7 functional probes and `run_probe` (:199-212) decides ok on `proc.returncode` alone, so a body that only prints exits 0. Assert census measured 2026-09-04, exhaustive over all 7: parquet_mlb_games 0, mlb_predictor_init 0, produce_mlb_dry 0, espn_live_state_mlb 1 (type-only `isinstance(st, list)`, fail-open BY DESIGN per the comment at :84-86), factory_sources 1, boot_packages 0 (its 5 imports ARE the check), supervisor_lock_env 2. Plant matrix measured 2026-09-04 by running each REAL probe body through the REAL `run_probe` with `domains.mlb.predictor._corpus_path` redirected in the child: against a 0-row schema-correct parquet, parquet_mlb_games ok=True "rows=0 cols=10", produce_mlb_dry ok=True "status=ok", mlb_predictor_init ok=False IndexError; against a 0-row 1-column parquet, same three verdicts (mlb_predictor_init's cause is KeyError 'date'); against an absent file, both parquet probes ok=False FileNotFoundError (`domains/mlb/predictor.py:88-91`). With produce.py's own unavailable branch forced (`predict_service/produce.py:230-233`), produce_mlb_dry ok=True "status=unavailable predictions=0 markets=0". Live corpus today: rows=27,983 cols=10. If any of this is falsified, STOP, write the memo, commit, and report FALSIFIED -- a valid result that earns its own register row.
LIMIT (step 1): `scripts/platformkit/ops/pod_bootstrap.sh:64-65` boots anyway on any number of functional FAILs, so the reachable effect of ANY floor added here is 0 boot decisions changed. Confirm that from the file and state it plainly in the memo: this fixes REPORT correctness only, never the boot gate. Do not widen the boot gate to compensate.
CHANGE (step 2): add one explicit existence-shaped floor to exactly 3 probe bodies -- parquet_mlb_games `assert len(df) > 0`; mlb_predictor_init `assert p.n_games > 0 and len(p.teams) > 0`; produce_mlb_dry `assert e.status == 'ok' and len(e.predictions) > 0` -- and extend the espn_live_state_mlb comment to say in words that it stays fail-open BY DESIGN. Additive only: no probe added, removed, renamed or reordered, no printed line changed, no other file touched.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = probes carrying an EXPLICIT existence-shaped floor that raises when the floor is unmet, out of the 7 in `_FUNCTIONAL_PROBES` (:68-116). espn_live_state_mlb's type-only assert does NOT count as a floor; boot_packages' 5 imports DO.
  before        = 3 of 7 (factory_sources :92-96, boot_packages :97-103, supervisor_lock_env :104-115), measured 2026-09-04.
  bar           = 6 of 7, with espn_live_state_mlb as the one named exclusion (fail-open by design). Floors are existence-shaped only -- rows > 0, n_games > 0, len(teams) > 0, status == 'ok', len(predictions) > 0 -- never a tuned number.
  n             = 7 (CONSTRUCT -- every probe enumerated; Q7 exempts this from the n >= 30 rail, so the verifier checks the enumeration is exhaustive instead)
  eye check     = n/a (S-row); reproduction = for each of the 3 newly-floored probes, call `run_probe` on its real body with the degraded input planted in the child and assert ok is False on both plants (the forced-unavailable branch for produce_mlb_dry), then re-run the assert census and count 6 of 7.
  must not move = the 7 probe NAMES and their dict order; `_PROBE_TIMEOUT_S = 60.0`; `run_probe`'s returncode semantics at :199-212; the reported-not-blocking wiring at pod_bootstrap.sh:64-65; the live values rows=27,983 / cols=10, which stay REPORTED and are never encoded as thresholds; `data/cache/eval_gate/backtest_fwer.jsonl` byte-identical.
NON-TAUTOLOGY: the census covers all 7 probes and excludes none. Only espn_live_state_mlb is excluded from the BAR, named above with its reason; the bar is 6 of 7 rather than 7 of 7 precisely so that dropping the one fail-open-by-design probe cannot be what makes the number good.
EVIDENCE: docs/evidence/harness/preflight_probe_floors_2026-09-04.md -- the 7-row assert census before and after, the full plant matrix (probe x plant -> ok, cause) also copied as a JSON summary under docs/evidence/ so a verifier can reproduce without re-planting, the boot-gate limit from step 1, and a NOT VERIFIED list.
TEST: exactly one new per-file test; run only that file. Calibration language only per contract Q6; none of the retracted figures appears.
POD: none -- this lane is local only; no ssh, no scp, no deploy. COMMIT: explicit pathspec in the worktree, no push; report the sha. NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
