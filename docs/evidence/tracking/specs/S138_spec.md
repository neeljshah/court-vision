GAP S138 | sport nba (in-game) | worktree a13 | log cx_s138_nba_grammar_ext
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md. Calibration language only: no dollar, ROI, profit or edge words. Never touch data/registry, src/, kernel/, api/, intel/, scripts/team_system/. Per-file tests only (python -m pytest <one file> -q); NEVER the full suite. data/ in this worktree is a read-only junction to the main repo's data/ (no data/cache/eval_gate junction -- the S86 CSV was COPIED into this worktree's data/cache/eval_gate/) -- never write under data/. Scratch outputs go under C:/Users/neelj/AppData/Local/Temp/cx_s138/. NEVER PARK: run everything to completion this turn; never end waiting. COMMIT: explicit pathspec (git add <paths> && git commit -m "..." -- <paths>), in this worktree, no push. Last line of your report: SHA: <sha>.
GAP (verbatim from the register): THE NBA IN-GAME GRAMMAR (S102, 576 hypotheses) HAS NO INTERACTION FORMS AND THREE TRANSFORMS NOT_ENUMERATED; combining state features was only tried as a linear ensemble (S114 null). Extend the grammar with PAIRWISE interaction hypotheses (product and ratio of two base columns, conditioned by phase) and the missing transforms where tick-grain data allows, frozen as a NEW family, and screen locally on the S86 tick CSV through the GATED NBA tier.
READ: scripts/platformkit/foundry/ingame_grammar_nba.py (base columns, transforms, conditionings, semantic_hash, enumerate_hypotheses), foundry/ingame_screen_nba.py (load_screen, sweep, the S124 gate_features, incumbent options), foundry/ingame_screen.py + ingame_guards.py (assert_tick_asof, assert_label_blind, walk_forward_feature, BAR), eval_gate/family_bars.py (how S89/S102 froze a family with kind: and moved the pin; the count test in scripts/platformkit/eval_gate/test_family_bars.py), docs/evidence/harness/S102_nba_pod_sweep_2026-09-03.md, eval_gate/s94_nba_early_shrinkage.py (the _recal null). The S86 tick CSV must be present at data/cache/eval_gate/s86_nba_every_tick_2026-09-03.csv; if absent STOP and report NO STORE.
PREMISE (step 0): print the current grammar counts (bases, transforms, conditionings, 576), which 3 transforms are NOT_ENUMERATED and why; confirm no interaction hypothesis exists (grep for interaction / product / ratio in the grammar). If interactions exist, STOP and report FALSIFIED.
LIMIT (step 1): n/a.
CHANGE (step 2): NEW scripts/platformkit/foundry/ingame_grammar_nba_ext.py (<= 300 LOC): pairwise interactions over the base columns (product and safe ratio with a floor), x the existing conditionings, deduped by semantic_hash, expect 300-900 hypotheses; each feature builder tick-time as-of (reuse the base builders; the truncation guard + label-blind guard must pass at 8 probes); freeze as family ingame_nba_tickgrid_ext (kind: tickgrid) in family_bars.py moving the pin ONCE and updating the count test (record old/new pin); no bar moved. Then screen locally through ingame_screen_nba.sweep with incumbent=market and the S94 recal null on identical rows (charges off, scratch sqlite under C:/Users/neelj/AppData/Local/Temp/cx_s138/, NOT data/), on the S86 SCREEN side; within-family BH over the new family; report the count clearing +0.004 (expect 0), best 10 with clustered CIs, n / n_informative / n_eff.
TEST: NEW tests/platformkit/foundry/test_ingame_grammar_nba_ext.py: count and dedupe, semantic hashes disjoint from the 576, tick-time invariance on 8 probes, label-blindness, the family freezes and the pin moves once. Run ONLY that file plus scripts/platformkit/eval_gate/test_family_bars.py and tests/platformkit/foundry/test_ingame_screen_nba.py.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = interaction hypotheses screened on the SCREEN side vs the raw market with the recal null; denominator = the new family's hypothesis count
  before        = 0 screened
  bar           = >= +0.004 vs the raw market with a game-clustered CI excluding zero AND beating the null -> prereg DRAFT (unsealed); else honest NULL (a NULL is a PASS of the process)
  n             = the family count (report it)
  eye check     = n/a; reproduction = verifier recomputes the top 10 from the archived series
  must not move = BAR 0.004, the S86 partition, purge/embargo, the ledger (never open it), data/registry/**
NON-TAUTOLOGY: the null is fit on the identical rows; a zero-coefficient interaction equals the raw market to 1e-12.
EVIDENCE: docs/evidence/harness/S138_nba_grammar_ext_2026-09-03.md (grammar spec, pin old/new, screen table, verdict, NOT VERIFIED); archive series under the scratch path named in the memo (never under data/).
POD: none. Do not ssh anywhere.
