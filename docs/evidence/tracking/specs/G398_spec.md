GAP G398 | sport basketball | worktree a11 | log cx_g398_a8_high_resolution_dev_shadow

**RANK 2 - ONE UNMEASURED INFERENCE-SCALE CHANGE, DEV ONLY. Landed docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/dev_boxes_v3.csv SHA-256 e151f932c3b4bffe84c89aa8fde18f08c346a1e3a6e65d27ed4b36f095b7b4fa fixes 530 boxes. Landed docs/evidence/tracking/g390_ball_a8_sealed_pass_2026-09-11/summary.json SHA-256 8b7ef84d6739b7f6b4c5684fa47b5bdf57e124ff12451f90bf3b52217e8cfe7e fixes A8. Verification-pending docs/evidence/tracking/g394_ball_person_negatives_2026-09-11/summary.json SHA-256 9c3641f9f4508ac7785994be715787204c124c79668dde5fc51f666f6752bc62 fixes A10's failed single run.**

WHERE THIS ROW RUNS: PC step 0/PREPARE 30 min; pod scratch a11 paired DEV inference <=60 min; PC scoring, native eye review/test/receipts <=90 min. No training and no held-out inference.
OWNS: scripts/platformkit/tracking/g398_*.py; tests/platformkit/test_g398_a8_high_resolution_dev_shadow.py; docs/evidence/tracking/g398_a8_high_resolution_dev_shadow_2026-09-11.md and matching directory only.
PREMISE step 0 (BINDING before-condition): require G394 independent disposition/handoff and reproduce all 549 saved counts; verify the full 1,071-key DEV manifest, 530-box/27-game subset and original split identities. No unresolved identity or silently corrected reference.
Require archived A8 raw weights SHA-256 0f05a61618687bda2005ce4ac8343c5987f60ba0076245597de59f7380398762 plus actual retained path/bytes/readback. Already executed matching DEV experiment -> FALSIFIED; missing inputs -> NOT VALIDATED, no substitute checkpoint.

METHOD (sealed before any new DEV inference; fixed alternative, not a search):
1. Freeze ALL 1,071 DEV frame keys, their labels, boxes, source-game/section/PTS and native 1920x1080 pixel receipts. Exclude every held-out/context identity from tensors; archive assertions against all 549 held-out keys and 32 games.
2. Keep every original ABSENT/UNKNOWN and no-detection state; preserve uncertain reference labels without repair. Score the full DEV reference, not only the 530 training-positive frames; report training-set reuse and correlated frames.
3. Baseline = archived A8 weights with G390 inference args, imgsz 960. Candidate = identical weights/args with imgsz 1920 ONLY; no tiles, crop stage, person veto, augmentation, temporal input or threshold change.
4. Freeze conf 0.05, IoU 0.70, rank-0 OBSERVED rule, max_det 300, original precision mode and native-to-720p transform. Record the same environment/library/GPU hashes for both arms; never change production seeds or settings.
5. Charge ONE paired SHADOW launch prospectively, comprising one baseline and one candidate traversal. Process sorted game/section/numeric-frame/key order in 30 interleaved quantile bins; alternate arm order between bins, preserve launch and partial-failure receipts.
6. A failed/OOM/partial launch spends the allowance: no lower resolution, second launch, alternate seed or replenishment. Sequential GPU use only; hard 60-minute GPU-stage deadline including both traversals.
7. Archive raw predictions and timings before scores. One-run status is explicit: no seed replicate, no causal or repeatable-system conclusion; fresh-process scorer replay establishes arithmetic only.
8. Use the frozen G363 centre rule max(3 px, diameter_720p/2), UNKNOWN predictions as FP, and full DEV denominators. Record TP, FP, FN, C0=TP/N_dev, Wilson95 precision lower and ALL FP/N_absent_dev, plus ABSENT-only FP separately.
9. DEV continuation decision is fixed: C0 strictly higher AND Wilson lower no smaller AND ALL FP/N_absent no larger than the paired baseline. Ties fail continuation; this is a diagnostic planning rule, not the historical benchmark acceptance gate.
10. Keep historical benchmark bars 0.25/0.90/0.01 printed unchanged and UNTESTED by this row. Passing the DEV rule permits proposing a separately sealed experiment only; it does not allocate one or qualify a detector.
11. Failure closes this fixed resolution attempt AT LIMIT; return to reference growth via G386 to >=1,500 audited DEV boxes before proposing another training arm. Do not launch collection or another design in this row.
12. Inspect 30 paired native cards evenly over all DEV keys, including silence and UNKNOWN; independently rescore saved predictions twice. Disclose per-video counts and latency/peak VRAM without converting this unreplicated timing to an operational promise.

ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| DEV isolation and coverage | 1,071 DEV states; 530 training boxes/27 games | Exact keyed input identity; zero held-out/context overlap; all planned states accounted | All 1,071 DEV keys; >=30 unique native frames | 30 even paired native cards | G389 labels, splits, pixels and all historical outputs | Identity failure NOT VALIDATED |
| Fixed DEV continuation | Paired 960 baseline measured once; held-out A8 C0 0.164 is context only | 1920 C0 > 960 C0 AND precision lower >= baseline AND ALL FP/N_absent <= baseline | Entire DEV set; fixed denominators, all classes retained | Same 30, never just changed detections | conf 0.05; scorer; historical 0.25/0.90/0.01 bars | Rule met DONE diagnostic only; otherwise CLOSED AT LIMIT for this option |
| Run discipline/reproduction | A8/A10 training and benchmark tokens spent | One paired DEV launch, <=60 GPU min, counts identical in two saved-output rescores; zero new held-out calls | All planned keys and every launch/failure | Same 30 plus failure index | Weights, final epoch, production, no new training | Missing seal/hidden retry REJECT; incomplete output NOT VALIDATED |

EVIDENCE: docs/evidence/tracking/g398_a8_high_resolution_dev_shadow_2026-09-11.md plus directory: prereg, dev_manifest.csv, split_assertions.json, weight_receipts.json, frozen_args.json, launch_accounting.json, predictions_960.csv, predictions_1920.csv, paired_scores.csv, timing.csv, eye_index.csv, summary.json and common receipts; weights stay off-repo.
TEST: python -m pytest tests/platformkit/test_g398_a8_high_resolution_dev_shadow.py -q; held-out/context rejection, UNKNOWN FP, missing-key accounting, scale roundtrip, immutable one-launch token and strict DEV tie failure.
VERSION 2026-09-11 v1 PROPOSED; one DEV inference-scale diagnostic on the old reference. G397 takes operational priority; this row uses only existing native DEV pixels.

COMMON BINDING CONTRACT (astra round 6, inherited by this block):
Common binding contract: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5, Q1/Q3/Q6; each block inherits this paragraph and the following execution rules.
Each row: codex terra PREPARE, Claude finisher MEASURE, independent verifier; <=180 minutes including receipts. PREPARE seals protocol alone before metrics, then exact input/code/config identities before dependent work.
OWNS only its named new helper prefix, one test, memo and matching evidence directory. Helpers <=300 LOC, full-package imports; per-file tests only. Shared modules and production trees remain unchanged; gated changes only as named PROPOSED diffs.
Preserve full inherited schemas, all planned missing/failed/ABSENT/UNKNOWN states, exact paths/bytes/dimensions/PTS, hashes, raw judgments, even render index, exclusions and NOT VERIFIED. A missing denominator is UNKNOWN, never zero.
Pod steps use ~/bin/pod_run <lane> with explicit --ship/--fetch under /workspace/wt/<lane>/; deployed trees read-only. Gate actual VRAM/CPU needs, threads 1, <=6 CPU cores, scratch <2 GB and successful pod write probe before bulky writes; no restart or peer interruption.
PC free RAM >=2.8 GB and <=3 active lanes including raters; block/poll own work to completion within the fixed budget. Deterministic arithmetic is not repeatable inference or rating; single-run outputs remain descriptive.
Durable per-unit tables, summary.json, seals, time/resource receipts and readback belong under the row's evidence directory. Landable text/logs must pass ASCII and character-code-built Q6 scans; do not print matched text.
DONE means research completion; landing uses contract C. A failed evidentiary bar stays failed. No flags, registry writes, production deployment, threshold sweeps or renewed spent allowances.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round6_2026-09-11.md (local-only). G394's and G396's evidence lands on master after their verifies; a premise step that needs it reads the lane tips named in the spec until then (G394 a7 87a497e94; G396 a3 fead21999).
