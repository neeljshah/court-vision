GAP S195 | sport harness | worktree a12 | log cx_s195_unpaired_delta
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- self-check every line of section B AND section Q (Q1-Q9) before you report.
PREMISE (step 0): re-measure first. Measured 2026-09-03 read-only on a clean tree: scripts/platformkit/analytics_showcase/out/micro_closing_decay.json wnba buckets are T-24h n=0, T-6h n=26 brier 0.245445, T-1h n=29 brier 0.227515, close n=29 brier 0.228045, with n_games_joined 30 and close_vs_t24h_paired n=0. Its own "verdict" string publishes "T-6h=0.2454 -> close=0.2280 (delta=+0.0174, n_games=30)" -- a delta across a 26-row and a 29-row arm labelled with the 30-row join count. soccer_intl has the same defect (T-24h n=7 vs close n=13, delta -0.0019 labelled n_games=13, while that file's own paired block gives n=7 delta -0.021289).
Root cause is micro_closing_decay.py:206-221 build_verdict: it differences seq[0] and seq[-1] of the populated anchors and takes n from res['n_games_joined']. Source data is present (data/cache/line_history/wnba 15 jsonl, soccer_intl 29 jsonl, plus both outcome parquets) so a local recompute is possible.
OUT OF SCOPE, ALREADY FALSIFIED: the "10 of 101 scored artifacts / 0 registered / staleness gate" half of the filed row. Measured today the scored set is 14 of 101 (files with a numeric brier/logloss/log_loss-keyed field), and data/cache/eval_gate/gate_manifest.json is 214/214 rows all carrying a non-null measured_at and indexes JSON only. Do NOT touch gate_manifest.json. If the four sites below already carry a matching n, STOP and report FALSIFIED with the memo.
LIMIT (step 1): if the first populated anchor and close share zero games for a sport, that paired delta is undefined -- emit n=0 and have the verdict say "no paired games", never a cross-arm number. That is a valid result, not a failure.
CHANGE (step 2): the smallest additive change. In analyze_sport, alongside a byte-identical close_vs_t24h_paired, emit a new close_vs_first_paired block: close paired against the FIRST POPULATED anchor, same math, plus first_anchor and n. build_verdict then quotes that block's delta and its own n. Re-quote docs/evidence/true-intelligence.md:157 and docs/ANALYTICS_CATALOG.md:217 from the new block. No key renamed or removed; grep every reader before you edit.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = published cross-arm Brier delta quote sites whose stated n equals the row count actually differenced. Denominator = 4 enumerated sites: micro_closing_decay.json verdict wnba sentence; same verdict soccer_intl sentence; docs/evidence/true-intelligence.md:157; docs/ANALYTICS_CATALOG.md:217
  before        = 0 of 4 (measured 2026-09-03: n=30 against arms of 26 and 29; n=13 against arms of 7 and 13; both doc lines quote n=30)
  bar           = 4 of 4, each quoting the paired n and the paired delta
  n             = 4 (CONSTRUCT -- every published cross-arm delta site is enumerated; the verifier checks exhaustiveness, not n>=30)
  eye check     = n/a (S-row); reproduction = recompute close_vs_first_paired for wnba and soccer_intl from data/cache/line_history plus the outcome parquets, then confirm each of the 4 sites quotes exactly that n and that delta
  must not move = every existing bucket n/brier/logloss/mean_prob/underpowered value; the whole close_vs_t24h_paired block; data/cache/eval_gate/gate_manifest.json (214 rows) and data/cache/eval_gate/backtest_fwer.jsonl byte-identical; no ledger row charged and K unread; no retracted figure, calibration language only
NON-TAUTOLOGY: the metric covers all four sites in this artifact family. The other 13 of the 14 numeric-brier-keyed out/*.json are excluded because none differences two differently-sized Brier arms -- verified today only for tennis_showcase (its brier_delta is base vs prior on one test set). Before reporting, check the five other delta-carrying artifacts (cross_sport_scoreboard, mechanism_ledger_export, novel_market_foresight_premium, site_manifest, why_attribution); if any publishes a cross-arm delta, ADD it to the denominator and report the raised n of n.
EVIDENCE: docs/evidence/harness/unpaired_delta_2026-09-04.md -- before/after table for all 4 sites, the paired n and delta per sport, the exhaustiveness check over the 14, a NOT VERIFIED list, and the regenerated micro_closing_decay.json copied under docs/evidence/ so a verifier can reproduce the headline.
TEST: exactly one new per-file test; run only that file.
POD: not needed -- local recompute only. No scp, no git on the pod.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
