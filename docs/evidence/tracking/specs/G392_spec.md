GAP G392 | sport basketball | worktree a13 | log cx_g392_per_rater_paint_qualification
**RANK 2 - CALIBRATE EACH RATER BEFORE QUALIFYING PAINT. Landed docs/evidence/tracking/g388_paint_band_protocol_2026-09-11/summary.json SHA-256 fe7b3add11869425045d403328b48bc288447b005f63b01ddcc8475085f848bc records unqualified 0/30 real recovery; docs/evidence/tracking/g388_paint_band_protocol_2026-09-11.md SHA-256 03dbc0d2c9031a155efa42bdedad67843b24eba64780aebc6b545bfdd364fe38 records 10/30 sealed controls, sol 28/30 and terra's 30 px perpendicular p90. Landed docs/evidence/tracking/g386_pin_copy_one_pass_2026-09-10/summary.json SHA-256 e0e6119766899786a4004cd34e7587bacc615853cc87674c85ce4d06d297803f defines later capture durability.**
WHERE THIS ROW RUNS: PC only, step 0/PREPARE 25 min, practice/qualification 65, conditional real rating 65, receipt 25; use existing preserved native pixels, no fitter/GPU/pod.
OWNS: scripts/platformkit/tracking/g392_*.py; tests/platformkit/test_g392_per_rater_paint.py; docs/evidence/tracking/g392_per_rater_paint_qualification_2026-09-11.md and matching directory only.
PREMISE step 0 (BINDING before-condition): reproduce G388's 10/30 original and 13/30 LIMIT accounting separately, per-rater control distribution and 0/30 real; verify all 49 retained native files and 30 selected context identities. Require no qualified successor already present.
If only one rater has working image access/output permissions, STOP PARTIAL before controls; test permissions using an unscored plumbing fixture. Never issue a scored packet into a failing sandbox or convert a missing answer to UNKNOWN paint.
METHOD (sealed; a new calibrated protocol, historical attempts remain closed):
1. Fix terra and sol as the two independent raters. Use one native 640x540 tile at a time with explicit display dimensions and tile-to-native integer offsets; record actual image-open and coordinate-conversion receipts, no contact-sheet clicking.
2. Build 30 PRACTICE and 30 distinct QUALIFICATION synthetic controls on the retained context set; archive seed 392, pixels, known finite bands, six tile positions and 0/30/60 degree cycle. No repeated pixel/truth tuple across sets.
3. PRACTICE gives each rater feedback against known centreline coordinates and permits one documented rater-side instruction/display correction. Freeze the resulting mapping and instructions before opening qualification; no post-hoc fitted offset or blanket 30 px subtraction.
4. QUALIFICATION is blind and feedback-free, with all three points independently observed, first/second span >=60 px, third interior; every point must be <=3 native px perpendicular and project within the finite band. Missing output fails.
5. EACH rater must pass >=27/30, and >=27/30 contexts must pass for BOTH. Exclude a failing rater before any real paint task is issued; with fewer than two qualified raters STOP PARTIAL, PROTOCOL NOT QUALIFIED. No substitute/retry in this row.
6. If qualification passes, retain G388's 30 real contexts in their sealed even selection from 49; retain the 11 original decode failures in the 60-state parent census. Reuse is explicit, not independent-corpus confirmation.
7. Qualified raters independently annotate up to two visible straight fragments, fixed tile order, family SIDELINE/BASELINE/CENTRE/LANE/FREE_THROW/THREE_POINT_STRAIGHT/UNKNOWN; keep ABSENT/UNKNOWN and unmatched fragments. No historical traces or control feedback during real rating.
8. Freeze pairing by tile/family and smallest symmetric line distance with stable ties; third point <=3 px from own line and every point <=6 px from the other's line. Never compare arbitrary endpoints or extrapolate through occlusion.
9. Claude records visibility before viewing traces, then audits every candidate pair on native pixels: same physical band AND >=8/9 evenly spaced points on EACH fragment within 3 px of visible paint. Audit counts attach to pairs, never reusable frame-level assertions.
10. Publish all 30 real states, audited recovery/30, visible-conditioned recovery descriptively, family disagreements and UNKNOWN. Repeat geometry/render arithmetic in separate processes; rater repeatability is not established by deterministic arithmetic.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Per-rater qualification | G388 joint 10/30; sol 28/30; terra LIMIT p90 30 px | Each >=27/30 AND joint >=27/30 before real tasks; every point <=3 px with span/finite-band clauses | 30 distinct qualification controls per rater; practice n=30 separate | All 30 control overlays per rater, ordered evenly | No feedback/retry on qualification; no historical threshold edit | Failure PARTIAL, protocol not qualified; pause paint |
| Qualified real band audit | G388 diagnostic 0/30, 0/23 visible | Both raters qualified; account for 30/30 and audit every pair; no minimum recovery invented | 30 unique real contexts, conditional on preserved pixels | All 30 native contexts, both traces and absence cards | 6 px pairing, 3 px audit, 8/9, 60 px span | Complete audit DONE; zero recovery is measured only if controls pass |
| Identity and independence | 49 retained natives; 11 parent failures | Exact input identities, disjoint control sets, two fresh-process outputs identical | All 60 parent states plus 60 synthetic controls | 30 even transform cards | G388/G387 results, G362/G374, orientation UNKNOWN, all production files | Missing identities NOT VALIDATED; no registration claim |
EVIDENCE: docs/evidence/tracking/g392_per_rater_paint_qualification_2026-09-11.md plus directory: prereg, source/selection manifests, practice/, correction_receipt.json, frozen_instructions, qualification/truth and ratings, per_rater_scores.csv, eligibility.json, conditional real ratings/pairs/audits, summary.json, renders/, common receipts and NOT VERIFIED.
TEST: python -m pytest tests/platformkit/test_g392_per_rater_paint.py -q; one-rater failure blocks real dispatch, no qualification feedback, 30 px planted displacement fails, arbitrary extent along same band passes, parallel wrong band fails, audit pairs isolated.
VERSION 2026-09-11 v1 PROPOSED; one practice/calibration phase then one sealed qualification attempt. Failure pauses this paint line until a separately qualified third rater/protocol; no third-rater search within this budget.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round4_2026-09-11.md (local-only). NOTE (astra A.2): G390's 169/188 = ALL false calls / N_absent; the ABSENT-only false-call rate is 50/188 = 0.266 (FP partition VISIBLE 100 / ABSENT 50 / UNKNOWN 19).
