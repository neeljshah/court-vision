GAP S183 | sport all | worktree a12 | log cx_s183_gate_corpus_coverage_census
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- self-check section B AND section Q (S-row) before reporting.
PREMISE (step 0): re-measure today. Over the four live gate corpora data/cache/combo/gate_corpus_{nba,mlb,soccer,tennis}.parquet, excluding the four spine columns event_id/corpus_unit/event_date/y, there are 52 feature columns (nba 11, mlb 5, soccer 29, tennis 7) spanning 220 (column, corpus_unit) cells (11x2, 5x2, 29x6, 7x2). Measured 2026-09-04: 41 of 52 below 100 pct non-null; 1 (mlb park_factor) 0.0000 globally; exactly 6 cells at 0.0000 -- mlb park_factor on BOTH eras, mlb sp_first6_diff_ew and sp_ra_diff_asof on era_2022_2026, tennis diff_return_won_asof and diff_break_pct_asof on WTA. The tennis pair reads a plausible 0.6966 / 0.6967 globally, so GLOBAL rates hide it. Sidecar provenance entries: soccer 17, nba 0, mlb 0, tennis 0; sole producer _build_soccer (corpus_cache_sources.py:127), join_rate GLOBAL only. If falsified: STOP, memo, commit, report FALSIFIED.
LIMIT (step 1): none -- every value is a deterministic notna() count over frames already read. If a parquet cannot be read, report CLOSED AT LIMIT.
CHANGE (step 2): add pure helper `column_coverage(df) -> dict` in scripts/platformkit/combo/corpus_cache_sources.py: per feature column {n_rows, n_non_null, rate} globally plus a per-corpus_unit block, and a `zero_coverage` list naming every (column, corpus_unit) cell at exactly 0.0. Call it from build_gate_corpus (corpus_cache.py:99-102) into two NEW sidecar keys "coverage"/"zero_coverage", and from freshness_report (corpus_cache.py:154, which already loads the cached frame) into two NEW report keys of the same names -- so all four live sports report a census with ZERO writes to data/. Additive only: no existing key, column, field or value renamed or removed.
DECIDED, state it in the memo: a 0.0 cell is RECORDED and named, never raised on. park_factor is 0.0 on every mlb unit, so a refusal would turn a recorded defect into an mlb build outage (S41). The named list IS the instrument.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = feature columns carrying a per-corpus_unit non-null count and rate; denominator 52 columns / 220 (column, corpus_unit) cells over the four live corpora
  before        = 0 of 52 columns and 0 of 220 cells (measured 2026-09-04; 17 of 52 carry a GLOBAL-only provenance entry, soccer alone)
  bar           = 52 of 52 columns and 220 of 220 cells present, and zero_coverage names exactly the 6 cells listed in PREMISE, no more and no fewer
  n             = 52 (CONSTRUCT -- every feature column of every live corpus enumerated, not sampled)
  eye check     = n/a (S-row); reproduction = the verifier calls freshness_report(sport) for all four sports and recomputes df.groupby('corpus_unit')[col].notna().sum() and .mean() from each parquet, asserting equality on all 220 cells
  must not move = the four live parquets byte-identical (sha256 prefixes 716f6f5f3f21 nba / ac60c9cb1895 mlb / e0d2f13e7a53 soccer / 22d006f2b4f7 tennis), row counts 1,814 / 39,162 / 25,834 / 41,886; the four live .sources.json byte-identical (this lane writes NO sidecar); every existing freshness_report key unchanged in name and value including provenance (soccer 17 entries verbatim) and load_provenance; load_gate_corpus unchanged for all four sports including portable=True; data/cache/eval_gate/backtest_fwer.jsonl untouched
NON-TAUTOLOGY: the metric covers all 52 feature columns of all four live corpora. Excluded: the 4 spine columns per corpus (join keys and target, 100 pct by construction) -- including them would add 16 always-full columns and dilute the rate, so the exclusion makes the number WORSE, not better. All 6 zero cells are INSIDE the denominator and must be named.
EVIDENCE: docs/evidence/harness/gate_corpus_coverage_census_2026-09-04.md -- per-sport table of all 52 columns with global and per-unit rate, the 220-cell count, the 6 named zero cells, a NOT VERIFIED list. Copy the four freshness_report outputs under docs/evidence/harness/ as JSON so the verifier reproduces from the artifact alone.
TEST: exactly one new per-file test, scripts/platformkit/combo/test_corpus_cache_coverage.py; run only that file. Use the tmp_path + monkeypatched cache-dir pattern already in test_corpus_cache_freshness.py; never build into data/cache/combo.
POD: not needed -- four local parquet reads.
LOC: both touched files stay <= 300 (corpus_cache.py 179, corpus_cache_sources.py 165 today). Calibration language only: no dollar, ROI, profit or edge word in any artifact, memo or row.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
