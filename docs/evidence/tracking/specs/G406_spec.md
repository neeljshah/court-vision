GAP G406 | sport basketball | worktree a7 | log cx_g406_masked_target_pixel_audit
**RANK 1 - AUDIT THE TARGETS WITHOUT INVENTING A TEACHER. Landed docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11/target_mask.csv SHA-256 c2c4f57a56330afbc9a7772b61743864aa04d746d9715e0374743e724a79dc71 supplies the candidate export; docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11/eye_index.csv SHA-256 d0019a5b0460cca99fe5afade3e414173c104eba5997fc1683f43d1de2bcd436 supplies 60 exact-even native ticks. Both landed through G402 at 856e2620c/540d0f1c7 with PARTIAL scope.**
WHERE THIS ROW RUNS: PC premise/seal/retention 25 min; pod a7 frozen detector inference <=20; PC blind native audit, joins, reproduction/test/memo <=125; total <=170 min. No producer rerun or ball inference.
OWNS: scripts/platformkit/tracking/g406_*.py; tests/platformkit/test_g406_target_pixel_audit.py; docs/evidence/tracking/g406_masked_target_pixel_audit_2026-09-11.md and matching directory only.
PREMISE step 0 (BINDING before-condition): reproduce 3,075/19,087, 59/60 windows, zero forbidden admissions and the exact 30/kind card draw from ALL bounded evaluated ticks ordered by sealed draw then source-frame index. Require all 60 selected native frames and retained originals; 29 source windows can supply 30 distinct ticks, but cannot become 30 completed windows.
Verify all mask rows against draw/window manifests by kind/section/frame; target_mask.csv has empty bound fields, so join the authoritative bounds rather than trusting those cells. Reconcile planned source frames, independently decoded frames, producer attempted/evaluated ticks and UNKNOWN traversal separately. Missing binding -> PARTIAL before inference, no replacement ticks.
LIMIT: independent-teacher availability is NOT ESTABLISHED. Seal this row as person-target pixel diagnostic only; if a separate detector is offered, record its identity as future work rather than silently replacing the comparator or changing this protocol.
METHOD (sealed before any new inference or pixel judgment):
1. Freeze all 60 existing even keys, full images/digests, source manifests and ALL corresponding producer rows with target_weight/mask_reason; zero-output frames remain in the 60. Preserve raw boxes, including out-of-frame coordinates; no clipping to manufacture valid geometry.
2. Freeze the G402 YOLOv8n person comparator, expected raw weight hash f59b3d833e2ff32e194b5bb8e08d211dc7c5bdf144b90d2c8412c47ccfc83b36, with actual path/bytes/readback and inherited detector args. Seal class selection, preprocessing, inference size, confidence and suppression exactly from the exercised route; absent args -> PARTIAL, no defaults substituted.
3. Before opening any prediction overlay, Claude marks visible person instances and uncertainty on every full native frame; include sidelines/crowd under the frozen person class, not a retrospectively narrowed player-only class. Save these blind judgments and timestamps before exposing either producer boxes or comparator detections.
4. Run the frozen comparator once on the 60 native frames in pod scratch with pre-launch model/config/route receipts. Save every image-space detection and silence. A matching producer component measures replay consistency, not independent corroboration; its mismatch cannot prove the historical model changed.
5. Associate boxes in native pixels by sealed maximum-IoU one-to-one assignment, IoU >=0.50, lexicographic ties; publish unmatched boxes and ambiguities, never drop them. This fixed association rule is not a target-quality acceptance threshold and gets no sweep.
6. Reveal anonymized box sets in sealed per-frame order only after the blind marks. Adjudicate EVERY unmatched/disagreeing box, EVERY candidate target and EVERY silence frame at native pixels; retain both the pre-overlay judgment and later adjudication with uncertain class/extent explicit.
7. Publish per-kind candidate-supported/ALL candidate rows, incorrect/uncertain candidate rows, missed visible persons/ALL blind visible persons, and silence/ALL 30 ticks. Report matched-instance geometry descriptively, with its conditional n; bins below 30 are descriptive, never separate qualified metrics. Audit excluded rows without unmasking them.
8. Restore source labels after adjudication; preserve all CLAMP/PREDICTION/SUBPIXEL/repeated/unbound masks. Report decoded/evaluated full-window denominators and sampled-tick denominators separately. G402's 60-card transport audit is not reused as a pixel-correctness judgment.
9. Regenerate all tables/cards twice from archived model outputs and blind/adjudicated marks; freeze review provenance and unresolved cases. Emit a diagnostic-only target-audit export with zero training callers and no accepted-reference promotion.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Sample/mask accounting | 3,075/19,087; 59/60; 60 transport cards | Exactly 30 distinct even native ticks/kind; all corresponding rows/silence accounted; zero forbidden target admissions | 60 ticks, ALL selected rows | All 60 full native images | G402 bounds/masks/UNKNOWN window | Complete DONE diagnostic; missing binding PARTIAL; changed mask REJECT |
| Pixel review completeness | No independent target qualification | Blind marks precede overlays on 60/60; every candidate/disagreement/silence reviewed; all support/error/UNKNOWN counts reproduce | 30 ticks/kind; instance counts reported separately | Every candidate plus every mismatch/silence | Native pixels, frozen class/association, original judgments | DONE even with poor targets; omitted cases REJECT; no training acceptance |
| Identity/reproduction | Five G402 models receipted post hoc | New comparator identity sealed pre-launch; two identical saved-output/table/card reproductions; zero new teacher claim | All 60 outputs and all exercised assets | Same complete set | Frozen args/weights and production | Exact DONE; missing launch proof NOT VALIDATED |
EVIDENCE: docs/evidence/tracking/g406_masked_target_pixel_audit_2026-09-11.md plus matching directory: prereg.md, input_hashes.csv, source_receipts.csv, window_accounting.csv, draw.csv, frozen_args.json, launch_receipts.json, all_masked_rows.csv, comparator_detections.csv, blind_marks.jsonl, adjudications.csv, per_tick.csv, summary.json, repeats.json, eye_index.csv, renders/, runtime_receipts/, q6_scan.json, SHA256SUMS; NOT VERIFIED names teacher independence and target suitability for training.
TEST: python -m pytest tests/platformkit/test_g406_target_pixel_audit.py -q; exact-even keys, blank-bound authoritative join, silence retained, one-to-one matching, blind-before-overlay timestamps, masks immutable, field-aware scanner positive cases, parent repeat fields retained.
VERSION 2026-09-11 v1 PROPOSED; G402 accepted-export prerequisite only. This is explicitly the available pixel-diagnostic substitute for round 8's unsatisfied independent-teacher condition.

COMMON BINDING CONTRACT (astra round 9, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6. Each block inherits these rails; the orchestrator confirms NEXT_GAP_ID/free owner and measures its WHOLE-set premise before dispatch. Supplied idle GPU/lane counts alone do not satisfy S2.
Execution: codex terra PREPARE seals protocol/bars alone, then a Claude finisher seals exact inputs and completes measurement/receipts within 180 min. No held-out invocation or threshold sweep, training, daemon restart, flag or registry write. Missing supply -> PARTIAL with planned keys retained; equivalent completed work -> FALSIFIED. No retry or replacement after seeing outcomes.
Ownership: only each named new helper prefix, one test and evidence memo/directory; helpers <=300 LOC, full-package imports. No src/ or other gated-tree edits; proposed changes stay in named evidence diffs, applied nowhere. Shared modules remain untouched. Every finisher commits the COMPLETE explicit file list, including imported helpers, fixtures, logs, renders and result text; no bulk add or push.
Retention/identity: retain selected sources OFF-POD, independently rehash and decode at native resolution BEFORE any pod stage; record original/receiver paths, bytes, dimensions, actual PTS and failures. G402's 16/60 mid-stage prunes make a pin insufficient. Hash EVERY exercised model, route and configuration before launch; post-hoc hashes cannot establish launch identity.
Pod steps use ~/bin/pod_run aXX --ship <owned helpers> --fetch <owned evidence> -- <sealed command>, only /workspace/wt/aXX/; deployed source/model trees are read-only. Gate on measured VRAM/CPU, not directory counts; <=6 CPU cores, threads 1, sequential GPU, <2 GB incremental scratch, successful pod write probe before bulky staging, UNKNOWN quota never zero. Finishers poll their own jobs to completion; no unattended leftovers or peer interruption.
Evidence: bound EVERY derived table to sealed source-frame/PTS windows; retain raw overruns separately, all no-output ticks and UNKNOWNs. Keep full parent schemas and repeat-receipt shape, including identical, runs and each run's stdout/exit/digests, adding aliases where needed. Two fresh processes regenerate delivered tables/cards from delivered bytes; arithmetic/render reproduction never proves inference repeatability.
Text: LF and ASCII; inventory every landable text/code/CSV/JSON/log/result path. Field-aware Q6 scans must inspect prose and claim fields, exempt only exact opaque identifiers and appropriately typed numerical data, and emit pattern indices/counts without matched text. Build all scan patterns/positive fixtures from character codes; no blanket CSV/JSON exemptions. Preserve redaction hashes/semantic parity, SHA256SUMS byte domain and NOT VERIFIED.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage -- the quota guard prunes ledgered sources ~45 min after tracking; hash EVERY exercised model, route file and config BEFORE any launch); finishers poll their OWN background pod jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed source-frame/PTS window and retain overruns separately; a failed planned unit stays an UNKNOWN row; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; receipts emit pattern indices, never token text); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round9_2026-09-11.md (local-only). G402 and G400 are landed on master; G405's evidence lands after its verify (lane a14 tip 051d2f5a6); G403 is running on lane a3.
