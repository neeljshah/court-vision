GAP G395 | sport basketball | worktree a9 | log cx_g395_provenance_authorized_live
**RANK 2 - G380'S SEPARATE AUTHORIZED DEPLOY PHASE. Landed docs/evidence/tracking/g376_declared_tick_observations_2026-09-10/classification_summary.json SHA-256 51af9a53a931afa01385e310aec63fa967de483c938aebc66c3ea25f8f5c1b9c identifies the denominator defect; docs/evidence/tracking/g386_pin_copy_one_pass_2026-09-10/summary.json SHA-256 e0e6119766899786a4004cd34e7587bacc615853cc87674c85ce4d06d297803f supports source retention. Pending docs/evidence/tracking/g380_producer_provenance_2026-09-10/replay_pairs.csv SHA-256 f928b1ad14d6765dab759f4f57e3d7df8e74ee7030e89aed037d415455d412de gives 30 pairs at 1.017054; docs/evidence/tracking/g380_producer_provenance_2026-09-10/PROPOSED_g380_producer_provenance.diff SHA-256 174e3c203cf84e658d7e0c2fb81e9d175aaf063b5ca7bc6f3d23f51de499481b is the current A3 proposal, not accepted deploy bytes.**
WHERE THIS ROW RUNS: PC PREPARE 20 min; pod finisher only, accepted overlay under /workspace/deploy/nba-ai-system, backup/smoke 10, live 60, capped verification/replay 75, PC receipt 15. PREPARE tonight; morning live window after acceptance, not after renewed permission.
OWNS: scripts/platformkit/tracking/g395_*.py; tests/platformkit/test_g395_provenance_live_watch.py; docs/evidence/tracking/g395_provenance_authorized_live_2026-09-11.md and matching directory. a19 retains exclusive G380 code ownership until explicit handoff; a9 owns observation/rollback packaging only.
PREMISE step 0 (BINDING before-condition): inspect the entire pre-deploy hour of the actual producer output and confirm missing source labels/receipts; already covered with an accepted live receipt -> FALSIFIED. Record actual daemon executable, arguments, cwd, PID/start time and all exercised file hashes.
Before any overlay require landed G380 adjudication, independent code ACCEPT explicitly permitting its separate live trial, >=30/30 exact attempt-trace receipt sets over >=10 videos, updated diff/treatment hashes and the standing 2026-09-08 authorization receipt. Rehash after a19 handoff; 21/21, draft text or missing receipts -> NOT VALIDATED, no restart.
Code acceptance must preserve historical 0/30 identity and unreachable live HELD as unmet; it cannot call G380 wholly DONE. Resolve any ACCEPT condition forbidding deployment before dispatch. No row-count tolerance or retrospective identity waiver is licensed here.
METHOD (sealed before the window; authorized finisher only):
1. Archive the full pre-deploy-hour event/output census, completion semantics and G372 repeat definition; record exact ledger path and fields game_id, status, evaluated_tick_receipt_path, attempted_frames_capped, route_max_frames, plus their actual timestamp field. Missing mapping blocks launch; never silently use another ledger.
2. Define section key = exact section-valued game_id, not the underlying video stem. Per window unique share U = distinct game_id/new event rows; repeat share R = 1-U, plus rows repeating a key from the preceding hour/all new rows. Freeze these denominators and baseline R0, retaining errors and repeat attempts.
3. Confirm no live pod measurement lane, enough resource for the actual work, G386 retaining receiver, tested write probe and an active rollback operator. Capture sources/outputs as each selected section completes, before 45-minute rotation; no deletion of other readers' objects or corpus copy.
4. Archive pre-overlay backups and exact accepted deployment manifest; smoke the accepted treatment with retained input in /workspace/wt/a9 before applying ONLY its named overlay under /workspace/deploy/nba-ai-system. Verify resulting hashes. Master gated files remain PROPOSED only; exclude the G393 parser change.
5. Re-identify the daemon by executable AND arguments, then one authorized controlled restart. Preserve the existing 20-minute watchdog and record recovery. Start a fixed 60-minute live window; check receipts/labels as completions arrive, and repeat-share windows at +30 and +60 minutes.
6. Census ALL completed sections, all emitted rows and all attempted sections. Valid receipt = readable schema, unique integer evaluated_tick_ids within decoded/capped bounds and completion binding; empty-output sections still require a receipt. Every emitted row needs position_source/source_branch; missing events are UNKNOWN with counts, never removed.
7. Require >=30 distinct completed sections from >=10 underlying videos during the window; repeated attempts cannot fill n. At minute 60 stop collection regardless of throughput. Archive all pre/post rows and statuses, no success-only filter and no continuation until the quota happens to pass.
8. Select ten live sections evenly over completion time for the requested identity re-check; preserve their complete inputs/caps/weights and original live outputs. Replay ORIGINAL once per section in scratch under the same environment/cap, compare every legacy keyed value, key set and order against its live PATCHED output. Bound total replay time to 75 minutes; incomplete checks remain failed verification.
9. Ten is the explicitly requested diagnostic check, not an n>=30 identity validation. Archive the prior A/A evidence and environment/cuDNN settings; known route nondeterminism leaves differences unattributed, including possible downstream races. Retain zero as the bar; any difference or incomplete required check -> REVERT, never a noise allowance.
10. REVERT on any missing/malformed receipt, missing row label, manifest/smoke/watchdog failure, U<0.80 or R>R0 in either 30-minute window, failed identity or deadline/quota failure. Restore exact backups, restart original only as required for rollback, verify original hashes/heartbeat and record rollback receipt; no second treatment attempt.
11. The one restart is the planned treatment restart; emergency restoration is separately recorded, not concealed to preserve that count. No causal claim about a changed repeat share. If all checks pass, retain only the accepted overlay and report measured live coverage, not general tracking correctness.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Launch acceptance and receipt equality | Supplied A3 21/21; local A3 results absent; prior 0/4 used row-filtered trace | Independent code ACCEPT, final A3 exact sets 30/30 and accepted hashes before overlay | >=30 scratch sections/>=10 videos | 30 even A3 receipt/timeline cards | A3 attempt definition, old failed bars, authorization scope | Missing condition NOT VALIDATED, no deploy |
| Live receipt and label coverage | Original live phase unrun; replay receipts 30/30, labels 109060/109060 | 100 pct valid receipts/all completed sections AND labels/all emitted rows, >=30 unique sections/>=10 videos in 60 min | Full window census; unique sections, decoded-frame counts named | 30 even native live overlays; absent cards retained | Labels additive; weights, coordinates, clamps, cadence, masks, flags | All met DONE for coverage only; any failure REVERT |
| Repeat/watch and availability | Fresh pre-hour R0 mandatory; inherited U bar 0.80 | U>=0.80 AND R<=R0 at +30/+60; watchdog healthy, no observation gap | All event rows; window n<30 descriptive but watch still binding | 30 even event cards across entire window | Completion/repeat definitions, pre-hour baseline, fixed duration | Failure REVERT; incomplete window NOT VALIDATED |
| Legacy identity check | Replay 0/30; A/A also differs; identity NOT VALIDATED | Zero differing legacy values/key sets/order on all ten live pairs | 10 explicitly requested diagnostic sections; not an n>=30 claim | All ten paired tables plus 30 live overlays above | Zero bar and exact original caps/environment; no seeded-route substitution | Difference or missing check REVERT; equality remains scoped to ten |
EVIDENCE: docs/evidence/tracking/g395_provenance_authorized_live_2026-09-11.md plus directory: authorization/code_accept references, prereg, before/after manifests, backup_receipt.json, smoke.json, pid_receipts.json, full event snapshots, live_receipts.csv, label_census.csv, watch.csv, identity_pairs.csv, receiver_receipts.csv, rollback.json, summary.json and renders/; raw route logs retained locally by hash.
TEST: python -m pytest tests/platformkit/test_g395_provenance_live_watch.py -q; >=30 sealed event fixtures, duplicate completions cannot fill quota, no-row completion needs receipt, malformed/absent receipt and unlabeled row trigger rollback, identity mismatch cannot pass as noise, deadline cannot extend, pre-hour comparison uses the named field.
VERSION 2026-09-11 v1 PROPOSED; preserves original bars and standing authorization. No current deployment claim; failed identity may make the authorized attempt end in REVERT. Do not dispatch until the complete code-acceptance package exists.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only; the G395 overlay is applied ONLY to /workspace/deploy under the 2026-09-08 user authorization with backup, smoke, ONE restart and the watchdog revert); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round5_2026-09-11.md (local-only). G391's evidence lands on master after its verify; a build that needs it waits for that landing.
