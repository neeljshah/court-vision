GAP G394 | sport basketball | worktree a7 | log cx_g394_ball_person_negatives_a10
**RANK 1 - A10, THE A8 RECIPE WITH DEV PERSON HARD NEGATIVES. Landed docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/dev_boxes_v3.csv SHA-256 e151f932c3b4bffe84c89aa8fde18f08c346a1e3a6e65d27ed4b36f095b7b4fa contains 530 boxes; docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/reference_v3.csv SHA-256 ad00670c3601706d5d817de734fc85d82e8fdc379ea03b21bbe1a46e272ad09e fixes all 1,620 labels. Landed docs/evidence/tracking/g390_ball_a8_sealed_pass_2026-09-11/summary.json SHA-256 8b7ef84d6739b7f6b4c5684fa47b5bdf57e124ff12451f90bf3b52217e8cfe7e fixes A8's failed bars. Pending docs/evidence/tracking/g391_ball_false_call_audit_2026-09-11/object_counts.csv SHA-256 275aaf1432c67ea768cdf022c19457b9810037cd8f9500bd30f52f5072451766 supplies the design rationale only after verification.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 25 min and negative audit 40; pod scratch a7 DEV inference/training/one held-out inference <=90 min; PC reproduction/receipt 25. No production write or GPU contention with G395.
OWNS: scripts/platformkit/tracking/g394_*.py; tests/platformkit/test_g394_ball_person_negatives.py; docs/evidence/tracking/g394_ball_person_negatives_2026-09-11.md and matching directory.
PREMISE step 0 (BINDING before-condition): full joins must reproduce 530 DEV boxes/27 games, 425 DEV ABSENT frames, 386 of those in the same 27 games, and held-out 302/188/59. The 386 supply count was independently checked here; masked-call eligibility is NOT YET MEASURED.
Require G391's verifier disposition and handoff to resolve its stale missing-G390-disposition clause; verify G390's NOT VALIDATED status and all training/pixel receipts. Use current docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/readiness.json SHA-256 d11a1c8cdd52098ee6de16aec6df103ca22990c00cd45cd4bb4f8712bd7c8ef6, not G390's superseded readiness digest.
Require no A10 launch, explicit prospective orchestrator allocation, source/weight availability and complete DEV/held-out game/context separation. Missing dependency -> NOT VALIDATED without training; already executed A10 -> FALSIFIED, no replacement allowance.
METHOD (sealed before any new DEV metric; no held-out tuning):
1. Freeze all 386 eligible ABSENT DEV keys, their native pixels and source-game identities; freeze read-only G389/G390 artifacts. Confirm their original retained pixels by hash/decode; absent bytes remain missing, never substitute footage.
2. Bind the existing yolov8n.pt person checkpoint by full path/hash before launch, no automatic download/fallback. One DEV pass: person boxes class 0, imgsz 640, conf 0.25, IoU 0.70; A8 final weights 0f05a61618687bda2005ce4ac8343c5987f60ba0076245597de59f7380398762 with its original inference settings, conf 0.05.
3. Candidate = A8's rank-0 call centre inside at least one person box; use the union of rectangular person masks only for selection. Extract one native 320x320 crop centred on that call, clipping and padding deterministically; retain a full-context view and exact transform. No pixel masking or invented ball label.
4. Sort eligible keys by game/section/numeric frame/key. Select min(120,N) evenly at indices round(j*(N-1)/(n-1)), including both ends; N<30 -> CLOSED AT LIMIT before training. Archive the entire candidate census and selected negatives before rating.
5. Two independent blind pixel raters confirm NO_BALL in each selected crop/full context; BALL or uncertainty disqualifies that crop and remains recorded, with no replacement. Require >=30 jointly confirmed crops, unique parent frames, >=5 DEV games; otherwise CLOSED AT LIMIT. Archive disagreements, not coerced agreement.
6. A10 changes only training data: retain all 530 original positives and 425 original ABSENT negatives, append each accepted crop exactly once with an empty ball-label file. Reproduce docs/evidence/tracking/g390_ball_a8_sealed_pass_2026-09-11/args.yaml SHA-256 463157bf0730a6457e5515c8fb609b1c16900210accae7a851742d10e6709aec except explicit data/output identities; same initialization checkpoint, seed 373, ten epochs, no augmentation, final epoch only.
7. Seal the actual negative manifest, recipe, current readiness, route/checkpoint hashes, context/evaluator inputs and one-run allowance before training. This is a new training run from A8's original initialization, not additional epochs on A8. Charge training launch first; failure/deadline closes this run, no alternate seed or crop quota.
8. Freeze final weights and config before consuming an immutable held-out token; charge it BEFORE one candidate inference over all 549 keys. Partial/failed launch spends the token. No rerun, threshold sweep, epoch selection, held-out negative mining, reference revision or other candidate arm.
9. Use G390's frozen native-to-720p centre rule, rank-0 OBSERVED semantics and context/evaluator contract; archive paired per-key records with times and unchanged reference. A8 is an archived baseline rescore; scorer replay is not fresh detector inference. Reproduce counts in two fresh processes from saved predictions only.
10. Report reused-benchmark scope and training stochasticity; no operational adoption. GPU stage capped at 90 min, training also within the original 120-minute cumulative training allowance after charging prior actual usage; output not ready by the fixed deadline -> NOT VALIDATED, no extension.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Audited DEV negative supply and isolation | 386 ABSENT frames/27 same DEV games; masked eligibility unmeasured | >=30 joint NO_BALL crops from >=5 games, zero held-out/context overlap; all selected states retained | 30-120 unique parent frames, both ratings | 30 even selected context/crop cards including exclusions | Original positives, labels, split and candidate selection rule | Supply short CLOSED AT LIMIT; identity fault NOT VALIDATED |
| Ball quality on fixed benchmark | A8 TP/FP/FN 90/169/212; C0 0.164, precision lower 0.292, ALL FP/188 0.899 | C0>=0.25 AND Wilson95 precision lower>=0.90 AND ALL FP/188<=0.01 | All 549 frames; VISIBLE 302, ABSENT 188, UNKNOWN 59 | 30 even paired native renders, no-detection included | Centre tolerance max(3 px, diameter_720p/2), conf 0.05, all bars | Bars met DONE on reused set only; otherwise NOT VALIDATED |
| Run discipline and reproduction | A8 consumed; A10 unrun | One training launch and at most one charged candidate inference; saved counts repeat exactly | All 549 planned keys, missing inference named | Same 30 plus failure cards | Original production files/weights, final-epoch rule, time caps | Missing seal/token/receipt REJECT; timeout NOT VALIDATED |
EVIDENCE: docs/evidence/tracking/g394_ball_person_negatives_2026-09-11.md plus directory: prereg, eligibility.csv, masks.csv, negative_manifest.csv, ratings/adjudications, split_assertions.json, launch_accounting.json, train/config logs, checkpoint_receipt.json, context.csv, predictions.csv, paired_frame_scores.csv, summary.json, renders/ and common receipts; weights retained off-repo by digest.
TEST: python -m pytest tests/platformkit/test_g394_ball_person_negatives.py -q; held-out game/context rejection, crop transform, uncertain crop exclusion without replenishment, missing-key denominator, spent-token restart refusal, original bar identity.
VERSION 2026-09-11 v1 PROPOSED; ONE data-design arm conditional on verified G391 and complete launch seals. New-game boxes or a veto require a different prospective row.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only; the G395 overlay is applied ONLY to /workspace/deploy under the 2026-09-08 user authorization with backup, smoke, ONE restart and the watchdog revert); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round5_2026-09-11.md (local-only). G391's evidence lands on master after its verify; a build that needs it waits for that landing.
