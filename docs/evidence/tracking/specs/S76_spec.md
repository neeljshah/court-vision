GAP S76 | sport all | worktree a12 | log cx_s76_charge_path_followups
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md sections B and Q -- read it; self-check against every line of section B before you report. S-row: eye check = n/a.
PREMISE (step 0): re-measure all four sub-items on master; none falsified. (a) screen_predictor.py:103,122-125 caches RealScreenPredictor's fit by len(train)//refit_every with no reset hook; s58_t2_first_trial.py:49-64 (PerPathPredictor) resets it by hand outside the module. (b) tiers.py charge_tier:139-148 / _run_charged:255-283 still pass rule.prereg_sha256 (the tiers spec pin) as the ledger row's prereg_sha256; the per-trial seal (7125552f...) never reaches the ledger. (c) tiers.py CLUSTER_KEYS["soccer"]="div" (line 42); _run_charged's _cluster_ids clusters the verdict side by div only, G=3; home-team clustering exists only in s58_t2_first_trial.py's per-unit _unit() (outside the factory path). (d) _run_charged's TierResult return (255-283) sets no archive=; only _run_screen (232-249) does.
LIMIT (step 1): tiers.py is 300 LOC, already at the file cap; any additive charge-path field or helper must live in a new sibling module that tiers.py calls into -- tiers.py's own line count must not grow past 300.
CHANGE (step 2): additive only, via the sibling module: (a) key RealScreenPredictor's fit cache on id(train)+first/last game_id, not len(train)//refit_every, so it self-resets across CPCV paths; (b) charge_tier gains an optional trial_prereg_sha256 kwarg written to the ledger row as a NEW field beside the existing prereg_sha256 (old field untouched); (c) _run_charged reports a second, home-team-keyed n_eff/DM beside the existing div-keyed one (div stays primary); (d) _run_charged's TierResult gains an archive= dict built the way _run_screen already builds one. No existing field renamed or removed.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = per-sub-item pass/fail on a DRY charged trial (tiers.run_tier, real factory path, synthetic 2-corpus_unit fixture, allow_charge False / a tmp copy of backtest_fwer.jsonl -- the real ledger is never opened for write)
  before        = (a) cache reset only by hand outside screen_predictor.py; (b) ledger prereg_sha256 == tiers spec pin, no trial-seal field; (c) div-only clustering (G=3); (d) TierResult.archive is None -- 0/4
  bar           = same DRY trial: (a) two CPCV paths with disjoint train sets force a re-fit, no external reset call; (b) tmp ledger row carries prereg_sha256 unchanged PLUS a non-null trial-seal field; (c) a home-team-keyed n_eff/DM sits beside the div-keyed one; (d) TierResult.archive is non-None with a per-unit differential -- 4/4
  n             = 4 (CONSTRUCT: a, b, c, d -- the row's own exhaustive list)
  eye check     = n/a (S-row); reproduction = verifier re-runs the DRY trial from the fixture seed and byte-diffs the tmp ledger row + archive JSON against the committed copy
  must not move = tier thresholds (charged_bars), rule.prereg_sha256 as the PRIMARY ledger field, div as the primary cluster_key, backtest_fwer.jsonl (18 rows, byte-identical before/after)
NON-TAUTOLOGY: covers only the 4 named sub-items on the T2 charge path; does not re-score the S58 T2 #1 MATCH verdict (already landed) and does not touch results_db.py (S74, in flight elsewhere).
EVIDENCE: docs/evidence/harness/S76_charge_path_followups_2026-09-04.md -- before/after per sub-item, the DRY trial's tmp ledger row + archive JSON copied in beside it, a NOT VERIFIED list. Calibration language only.
TEST: exactly one new per-file test (test_s76_charge_path_followups.py) asserting all 4 CONSTRUCT cases on the DRY trial; run only that file.
POD: none needed (the DRY trial is cheap); if used anyway, heavy compute only, no scp before ACCEPT.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: run the DRY trial and tests to completion this turn; never end waiting.
