GAP G413 | sport basketball | worktree a7 | log cx_g413_native_box_reaudit
**RANK 2 - MEASURE THE REMAINING DISAGREEMENT. Landed docs/evidence/tracking/g406_masked_target_pixel_audit_2026-09-11/summary.json SHA-256 734e2ed8f092942a86c68b64dfaaf8047dfe84a63b551a37ade2ef97c9d70e0f and docs/evidence/tracking/g406_masked_target_pixel_audit_2026-09-11/associations.csv SHA-256 1f7a44cd573c4273724765e3f31ac95c2196500ea5dbbd7986fcdcbe05e20114 bind 206 rows, 592 comparator boxes and 51 historical pairs. G412's accepted transform manifest is a dispatch prerequisite, not an invented current hash.**
WHERE THIS ROW RUNS: PC step 0/retention/seal 25 min; transform/pairing 30; native review 70; reproduction/test/memo 45; <=170 min. No inference, pod or GPU; use the delivered comparator output.
OWNS: scripts/platformkit/tracking/g413_*.py; tests/platformkit/test_g413_native_box_reaudit.py; docs/evidence/tracking/g413_native_box_reaudit_2026-09-12.md and matching directory only.
PREMISE step 0 (BINDING before-condition): require G412's accepted contract handoff with full hashes, preserved G406 outputs and 60 original draw keys; reproduce 206/592 boxes, 51 pairs/28 frames, five producer-silent ticks and original signed residuals. Missing prerequisite -> PARTIAL before scoring; already completed identical re-audit -> FALSIFIED.
LIMIT: comparator is the same detector family/weights, not an independent teacher. Historical associations give <30 frames; any new sampled residual claim requires >=30 distinct matched frames and explicit full-set unmatched counts.
METHOD sealed:
1. Retain/redecode ALL 60 original native frames, compare source/PTS/frame receipts and freeze input hashes. Preserve G406's blind marks and raw judgments as historical; do not relabel them after seeing corrected overlays.
2. Transform each historical stored box with G412's exact origin, inward-pad and clip rules; preserve original coordinates, target_weight and position_source alongside additive native coordinates, transform_status and clip flags. Fractional/predicted boxes are labelled nominal detector-form, never fresh detections.
3. Reproduce old-pair before/after centre residuals without rematching as a descriptive appendix. Keep all 51 pairs and identify their 28 unique frames; no n inflation or residual-based exclusion.
4. Independently apply the inherited one-to-one maximum-IoU association at 0.50 with the SAME deterministic tie rule to ALL contract-transformed producer boxes and all 592 saved comparator boxes. Seal this one pass before scoring; no threshold search, alternate assignment or subset rescue.
5. Keep EVERY unmatched/invalid/UNKNOWN box and every silent tick in per_tick.csv. Report matching fraction /206 and /592, matched-frame coverage /60, all-frame candidate coverage, clipping counts and per-branch/resolution counts; never convert conditional residual agreement into whole-frame recall.
6. For newly matched frames compute median signed dx/dy per frame, then equal-frame-weighted signed medians and absolute residual p50/p90/max pooled and by kind. Prospective translation budget: abs(median dx)<=5 px AND abs(median dy)<=5 px; no per-box tolerance or extent qualification follows.
7. Require >=30 distinct newly matched frames for pooled residual acceptance; any subgroup with <30 is descriptive. Budget is sealed before this row's scoring and is not fit to G406's old-pair result. Sparse matching -> PARTIAL, failed budget -> residual NOT VALIDATED; neither triggers tuning.
8. Review every native frame and every transformed producer/comparator box with overlays; save fresh extent/support/UNKNOWN judgments separately from old blind marks, flag overlay-informed review, enumerate silence and wrong-object matches. Use all 60 exact-even cards, no visual success sample.
9. Publish candidate counts over 27,000 decoded frames, 5,310 evaluated ticks, 19,087 bounded rows and 3,075 masked candidates as inherited population, separately from sampled 60/206/592 denominators. No target export for training, mask relaxation or accepted-reference promotion.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Complete native re-audit | 206 producer rows, 592 comparator boxes, five silent ticks | All 60 keys/all boxes and failed transforms accounted; source/frame identity exact; native transforms follow accepted G412 | 60 distinct frames, 30/kind | ALL 60 cards/all boxes | Parent draw, raw judgments, masks, weights and positions | Complete DONE diagnostic; missing input PARTIAL |
| Residual translation | Old-pair dy -58.2 on 28 frames, descriptive | New single-pass matches span >=30 frames; abs(equal-frame median dx), abs(equal-frame median dy) <=5 px; all tails/unmatched disclosed | >=30 matched distinct frames; planned denominator 60 | Same full set plus every failed transform | IoU 0.50/ties, transform, comparator bytes, fixed budget | Pass is scoped consistency only; sparse PARTIAL; failed budget NOT VALIDATED |
| Reproduction/schema | Saved parent outputs reproducible | Two fresh-process table/card reproductions identical; old fields and repeat shape retained; zero hidden exclusions | All 60 frames and full box populations | Same cards | Old verdicts, producer, all parent schemas | Complete DONE; semantic changes REJECT |
EVIDENCE: docs/evidence/tracking/g413_native_box_reaudit_2026-09-12.md plus directory: prereg.md, input_hashes.csv, source_receipts.csv, draw.csv, transformed_rows.csv, old_pair_residuals.csv, associations.csv, unmatched_boxes.csv, per_tick.csv, residuals.csv, review.csv, denominator_table.csv, eye_index.csv, renders/, summary.json, repeats.json, runtime_receipts/, q6_scan.json, SHA256SUMS. NOT VERIFIED: independent teacher, physical court geometry, production equality and training suitability.
TEST: python -m pytest tests/platformkit/test_g413_native_box_reaudit.py -q; fixed pairing/ties, silent frames, unmatched denominators, 51-pair/28-frame guard, frame weighting, branch/clip flags, window bounds and code-built scanner fixtures.
VERSION 2026-09-12 v1 PROPOSED; sequential dispatch after G412 handoff, no waiting lane. DONE diagnostic and residual-budget verdict are separate fields.

COMMON BINDING CONTRACT (astra round 11, inherited by this block):
COMMON CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6 bind every block. Orchestrator verifies the WHOLE-set premise, accepted prerequisites and free ownership before dispatch; supplied lane status is not a reservation.
Execution: codex terra PREPARE seals prereg alone, then Claude finisher completes the bounded measurement, test and memo within 180 min. Seal SHA-256 of every LF byte above the seal line, no intervening blank line; changed input hashes need a prospective handoff, never silent substitution.
Ownership: only each block's helper prefix, one test, evidence memo/directory and named PROPOSED diff; finishers also append their required RESULTS_LEDGER.md line in the same targeted commit. No register edit. They are not alone in the repo and must preserve peer edits; this reviewer writes none of those files.
Retention: before ANY pod stage, independently retain/read back/hash every selected original OFF-POD; record full paths, bytes, dimensions, rational PTS, all failures and existing receiver mappings. These three rows run on PC from retained bytes; no pod stage is needed or licensed by these specs.
Identity: hash every exercised model, route and configuration BEFORE launch; use an explicit empty model set when inference is absent. No new model invocation here. Source/source-frame/window joins are mandatory; branch ancestry never transfers across nondeterministic replays.
Bound all derived tables to sealed frame/PTS or census-time windows; list excluded history and archive raw overruns separately. Keep full parent columns and field meanings, ABSENT/UNKNOWN/silence and failures. Repeat receipts retain identical/runs, each run's stdout/returncode/command and per-table digests, with new fields additive.
No src/, kernel/, api/, intel/, scripts/team_system/, scripts/run_clip.py or shared-module edits; gated changes exist only as named PROPOSED diffs, applied nowhere. No training, held-out threshold sweeps, daemon restart, flag changes, registry writes or source pruning. Helpers <=300 LOC; full-package test imports.
Field-aware Q6 scans cover EVERY delivered code/text/log/table/claim, ledger addition, proposal and scan helper with a complete path manifest. Build ALL patterns and positive fixtures from character codes; emit pattern indices/counts only. Exempt exact opaque identifiers and typed numerical data only, not whole files; preserve numeric claim-field detection parity.
Write ASCII/LF; deliver declared byte-domain SHA256SUMS, all reproduction inputs, receipts and NOT VERIFIED. Run only each named test. Commit COMPLETE explicit paths including helpers/import dependencies, fixtures, raw observations, renders, scans and receipts; no bulk adds or push.
Two fresh processes reproduce delivered tables/cards from saved bytes; that does not establish inference repeatability. Never stop to wait: use bounded owned work, finish or close PARTIAL/NOT VALIDATED at deadline; false completed premise -> FALSIFIED; no repeated discovery loop or automatic extra attempt.

ORCHESTRATOR FOOTER (binding, 2026-09-12): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage; hash EVERY exercised model, route file and config BEFORE any launch); the PC pool receiver (C:/Users/neelj/pool_receiver/: objects/, receipts.jsonl, state.json; one section per new video id = the earliest-tracked section) is the accumulated off-pod source pool; finishers poll their OWN background jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed window and retain overruns separately; a failed planned unit stays an UNKNOWN row; inherited bars byte-identical; never change an existing field's semantics -- add a new field with an alias; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY touched text path incl. the scanner's own source and .log.txt receipts (patterns and restricted constants built from character codes; receipts emit pattern indices, never token text, with a complete path manifest); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round11_2026-09-12.md (local-only). COORDINATE CONTRACT (from G410, lane a13 tip 92f5894fc, verify pending; astra's sign correction applies: for a stored xyxy box B the native padded box is (Bx1, By1+60, Bx2, By2+60) and the native unpadded detector-form box is (Bx1+15, By1+75, Bx2-15, By2+45), clipped using the cropped dimensions BEFORE restoring the origin; fresh box-based court writes project ((x1c+x2c)//2, y2c, 1) through M then M1; ankle / flow / prediction / retained-point branches are exceptions). G402, G406 and G407 are landed on master; G409 (lane a12) and G411 (lane a19) are in verification.
