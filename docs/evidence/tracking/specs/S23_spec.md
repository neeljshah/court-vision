GAP S23 | sport all (analytics) | worktree a11 | log cx_s23_harness_health | verifier LIGHT
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): the `harness_health` MCP artifact has no generator.
READ (read every signature on disk; line numbers drift): scripts/platformkit/mcp_server/artifact_tools.py -- the `harness_health(args, root)` handler, the `_HEALTH` path tuple it reads IN ORDER (`data/frontend/analytics/harness_health.json`, then `data/cache/analytics_verify/harness_health.json`, then `scripts/platformkit/analytics_showcase/out/harness_health.json`), the helpers `_load`, `_as_of`, `_no_data`, `_field`, and the tool DESCRIPTION in `tool_specs()` which promises "golden, null-ship, retro-correction, and multiplicity ledger values"; the four field names the handler pulls -- `golden_verdicts` (alias `golden`), `null_ship_calibration` (alias `null_ship`), `retro_correction_survivors`, `multiplicity_ledger_K` (alias `K`). The producer modules the register row names, all present under scripts/platformkit/eval_gate/: `run_gate.py` (`run_gate_in_process`, `_verdict`, `evaluate_corpus`) and `golden_loader.py`/`gen_golden.py` for GOLDEN; `null_ship_calibration.py` (`run_calibration`, `render_report`, and its `post_hardening_revalidation_report.txt`) for NULL-SHIP; `retro_correction.py` (`catalog_signals`, `write_report`, and `retro_correction_report.txt`) for RETRO-CORRECTION; `data/cache/eval_gate/backtest_fwer.jsonl` (13 rows -- max `k_cumulative`) for the MULTIPLICITY LEDGER.
PREMISE (step 0): call `artifact_tools.harness_health({})` and show it returns `status="no_data"`. Confirm on disk that NONE of the three `_HEALTH` paths exists -- `data/frontend/analytics/` does not exist at all locally, and `scripts/platformkit/analytics_showcase/out/` holds no `harness_health.json` (verified 2026-09-03). Print the `ls` for all three. Before = 0/1 calls return ok. If any of the three artifacts exists, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): n/a -- CONSTRUCT row; ONE handler call is the enumerated case set. But before writing anything, print WHERE each of the four values comes from today (the producer's own output file or return value) and say plainly if one cannot be sourced without running new compute -- an unsourceable field is reported as `null` with a `note`, never invented.
CHANGE (step 2): NEW generator under `scripts/platformkit/eval_gate/` (e.g. `harness_health_report.py`, <=300 LOC) that COMPOSES EXISTING OUTPUTS ONLY and writes `scripts/platformkit/analytics_showcase/out/harness_health.json` -- the third `_HEALTH` path, chosen because it is the only one of the three whose parent directory exists. Invent no number and recompute no verdict: golden verdicts from the gate runner's own rows; null-ship from `null_ship_calibration`'s report/result; retro-correction survivors from `retro_correction`'s catalog/report; `multiplicity_ledger_K` = max `k_cumulative` across `data/cache/eval_gate/backtest_fwer.jsonl` (READ ONLY -- the file is never written). The artifact carries `source_artifact` (the list of files it composed), `as_of` (the OLDEST measurement stamp among its inputs, so staleness cannot be laundered by the newest one) and `generated_at`, plus the four fields under the exact names the handler reads. `artifact_tools.py` itself is NOT edited -- the handler already finds this path.
TEST: NEW tests/platformkit/mcp_server/test_harness_health_generator.py: the generator writes a JSON into `tmp_path`; `artifact_tools.harness_health({}, root=tmp_path)` returns `status="ok"` with non-empty `source_artifact` and `as_of` and all four promised fields present; a missing input yields that field as `null` with a `note` and still returns ok; with no artifact at all the handler still returns `no_data` (the fall-through is preserved, B3). Run ONLY that file: `python -m pytest tests/platformkit/mcp_server/test_harness_health_generator.py -q`, then ALSO rerun `python -m pytest tests/platformkit/mcp_server/test_artifact_tools.py -q` (must stay green) -- two files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = the `harness_health` handler returns a non-`no_data` envelope carrying every field its tool description promises; denominator = 1 call
  before        = 0/1 (all three `_HEALTH` paths absent; the handler returns no_data)
  bar           = 1/1 -- `status="ok"` with `source_artifact` and `as_of` set and `golden_verdicts`, `null_ship_calibration`, `retro_correction_survivors`, `multiplicity_ledger_K` all present (a value may be `null` WITH a note; the key may not be missing)
  n             = 1 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns the generator in master, calls `artifact_tools.harness_health({})`, and traces each of the four values back to the producer file the artifact names
  must not move = the `_HEALTH` path tuple and its fall-through order, the field names and aliases the handler reads, the `no_data` path when the artifact is absent, `artifact_tools.py` itself, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows, READ ONLY)
NON-TAUTOLOGY: all four promised fields are in the denominator; a field that cannot be sourced is reported `null` with its reason, never omitted from the artifact and never filled with a placeholder number to make the call look complete. No `_charge_ledger` call anywhere -- composing a scoreboard is not a trial.
EVIDENCE: docs/evidence/harness/S23_harness_health_2026-09-03.md -- the premise `ls` of all three paths and the no_data envelope, the step-1 provenance table (field -> producer -> file), the generated artifact, the before/after envelope, test output for both files, and a NOT VERIFIED list (the live server restart is the human's step). Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local only. No deploy before ACCEPT (B5); the courtvision MCP server restart is NOT yours.
COMMIT: explicit pathspec (the generator, the test, the memo, the generated JSON), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
