GAP S185 | sport all (ops) | worktree a17 | log cx_s185_pod_pull_sync
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check against every line of section B AND section Q (Q1-Q9) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md. Calibration language only: no dollar, ROI, profit or edge words; never the retracted figures. Never touch data/registry, src/, kernel/, api/, intel/, scripts/team_system/, and never data/cache/eval_gate/backtest_fwer.jsonl. Per-file tests only; NEVER the full suite. NEVER PARK. COMMIT: explicit pathspec in this worktree, no push. Last line of your report: SHA: <sha>.
GAP (measured 2026-09-04): scripts/platformkit/pod_pull_sync.sh is the only puller covering data/frontend, data/cache/benchmarks, data/cache/ingame_grade and the track-daemon ledger, and it (a) silently swallows 5 of its 7 scp failures, (b) prints "pass complete" unconditionally, (c) is pinned to a pod that is recorded dead, and (d) pulls none of the pod-side book-capture stores.
PREMISE (step 0, re-measure and print every number before any edit):
  - `grep -n 'scp' scripts/platformkit/pod_pull_sync.sh` -> 7 invocations (:31 :32 :33 :34 :35 :39 :45); exactly 2 have their exit status checked (`if ! scp`, :39 :45); 5 end in `2>/dev/null` with no check (:31-:35); :51 echoes "pod_pull_sync: pass complete" unconditionally; :19 is `set -u` with no `set -e` and no `pipefail`, and pull_once has no explicit return, so the function exits 0 even with every target failed.
  - Host/port literals :24-:25 are `root@213.192.2.83` / `40048`. BOTH are stale: ~/.ssh/config.pod (read today) names the live pod `213.192.2.123` port `40034`; docs/evidence/tracking/RESULTS_LEDGER.md:147 records the .83 pod dead 2026-09-03; docs/evidence/tracking/specs/G155_spec.md:6 records that pod as `.83:40193`, a third port literal.
  - Pull-list store coverage: 0 of the 4 pod-side capture stores (data/cache/ingame_books, depth_history, book_depth, ingame_shadow_history) appear in :31-:45. Of the four, exactly ONE has no local copy at all -- `ls data/cache/ingame_books` returns "No such file or directory", while depth_history, book_depth and ingame_shadow_history all exist locally. Repo-wide grep for `ingame_books` hits only docs plus scripts/platformkit/ingame/mlb_book_capture.py and s90_microstructure_screen.py: no sync, backup or archive path anywhere, and none of the five other pod->local pullers (scripts/runpod_pull.sh, sync_tracking_to_laptop.py:34, local_sync_watchdog.sh, fetch_local.py:40, platformkit/tracking/loop_status.sh:26) covers it either.
  - Context, not part of the bar: `grep -rn "213\.192\.2\.[0-9]*" --include=*.sh --include=*.py scripts/` -> 5 distinct addresses across 15 tracked files (.83 x4, .86 x6, .107 x3, .121 x1, .123 x1); only footage_bridge.py:65 reads the ssh config.
  If any of the three headline counts (7 / 2 / 0-of-4) is falsified, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): confirm `scp` returns non-zero for an unreachable host on this box (`scp -P 1 -q -o ConnectTimeout=3 root@127.0.0.1:/nope /tmp/x; echo $?`). If scp cannot signal failure, the checked form buys nothing -- report CLOSED AT LIMIT and do not fix.
CHANGE (step 2, smallest that holds; additive only):
  - Add one shell helper in the same file that runs an scp, echoes `pod_pull_sync: WARN <target> pull failed` to stderr on non-zero, and sets a `fail` accumulator; route ALL 7 existing invocations through it. Keep every existing target and every existing WARN string. Do not add `set -e`.
  - Make the final line conditional: `pass complete` only when fail=0, otherwise `pod_pull_sync: pass INCOMPLETE (<k> target(s) failed) <UTC>`; `pull_once` returns the accumulator, and the non-loop branch exits with it (the `--loop` branch keeps looping, unchanged).
  - Add exactly ONE new pull target, `data/cache/ingame_books` (mkdir -p its parent, same `-r` form), through the same helper -- it becomes the 8th invocation and is checked like the rest.
  - Replace the two literals with overridable variables keeping a default: `POD="root@${CV_POD_HOST:-213.192.2.123}"`, `PORT="${CV_POD_PORT:-40034}"`. Refresh the :20-:23 comment to say which values are live and dated. Do NOT delete the default; a clean clone must still name one.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric (a)    = scp invocations in scripts/platformkit/pod_pull_sync.sh whose exit status is checked AND whose failure makes pull_once print a non-success final line, over ALL scp invocations in the file (denominator = every line matching `scp ` that is not a comment)
  before (a)    = 2 of 7, measured 2026-09-04
  bar (a)       = 8 of 8 (7 existing + the new ingame_books target); no target removed
  metric (b)    = the required pod-side capture store present in the pull list, over the 1 store with no local copy (data/cache/ingame_books)
  before (b)    = 0 of 1 (0 of 4 across the named four; depth_history, book_depth and ingame_shadow_history each have a local copy today and are OUT OF SCOPE for this row)
  bar (b)       = 1 of 1
  n             = 8 (CONSTRUCT, every scp invocation in the file enumerated; none excluded)
  eye check     = n/a (S-row); reproduction = the verifier re-greps the scp lines in master and counts checked vs total, then runs `pull_once` against an intentionally unreachable host (CV_POD_HOST=127.0.0.1 CV_POD_PORT=1) and confirms a per-target WARN line on stderr, the final line reading `pass INCOMPLETE`, and a non-zero exit status
  must not move = the script never pushes (pod -> laptop only, no scp with a local source); no existing pull target removed or renamed; the two existing WARN strings byte-identical; `--loop` still loops on failure; docs/evidence/tracking/RESULTS_LEDGER.md and data/cache/eval_gate/backtest_fwer.jsonl byte-identical; no other pod-address literal in scripts/ touched by this lane
NON-TAUTOLOGY: the 2-of-7 denominator is EVERY non-comment scp line in the file with none excluded; the store count names the four capture stores up front and narrows to the one measured absent locally BEFORE the change, not after.
EVIDENCE: docs/evidence/harness/pod_pull_sync_durability_2026-09-04.md -- before/after table for (a) and (b), the enumerated 8 invocations with checked yes/no, the unreachable-host transcript (stderr WARN lines + final line + exit status), and a "NOT VERIFIED" list (at minimum: no live pod was contacted, so no real pull of ingame_books was demonstrated; the other 14 hardcoded pod addresses are untouched).
REQUIRED EVIDENCE DURABILITY: the unreachable-host transcript and the invocation enumeration live in the memo under docs/evidence/, not only in the terminal.
TEST: exactly one new per-file test, tests/platformkit/test_pod_pull_sync.py -- runs the script with CV_POD_HOST/CV_POD_PORT pointed at an unreachable endpoint and a temp DST, asserting non-zero exit, at least one WARN on stderr, `pass INCOMPLETE` present and `pass complete` absent; plus a static assertion that the file contains 8 scp invocations and 0 unchecked `2>/dev/null` scp lines. Run only that file.
POD: none. This lane contacts no pod, runs no ssh and deploys nothing.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: run to completion this turn.
