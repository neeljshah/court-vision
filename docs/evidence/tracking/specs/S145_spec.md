GAP S145 | sport nba (harness) | worktree a16 | log cx_s145_wallclock_loc
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md. Calibration language only: no dollar, ROI, profit or edge words. Never touch data/registry, src/, kernel/, api/, intel/, scripts/team_system/. Per-file tests only (python -m pytest <one file> -q); NEVER the full suite. data/ in this worktree is a read-only junction to the main repo's data/ -- never write under data/. NEVER PARK: run everything to completion this turn; never end waiting. COMMIT: explicit pathspec (git add <paths> && git commit -m "..." -- <paths>), in this worktree, no push. Last line of your report: SHA: <sha>.
GAP (verbatim from the register): scripts/platformkit/venue_history/nba_wallclock_join.py is 313 LOC after S141 (was exactly 300); the empty-frame block is duplicated twice. Bar: <= 300 with byte-identical behaviour, the four per-file tests pass unedited.
ALSO (second half of this row): S149 -- scripts/platformkit/eval_gate/s116_pooled_ingame.py's archive writer dropped the raw source model column on the NBA side of data/cache/eval_gate/s116_pooled_ingame_2026-09-03.csv (the MLB side is reconstructible from the archived gap by logit inversion), so its NBA n_informative cannot be reproduced from its own archive (S142 quotes it from the S94 archive). Make the writer archive the raw model and market columns per tick (ADDITIVE columns; no refit; do NOT regenerate the landed archive under data/ -- write a regenerated copy to C:/Users/neelj/AppData/Local/Temp/cx_s145/ and show it reproduces n_informative 78,761 from itself), and re-point tests/platformkit/ingame/test_s116_pooled_ingame_a2.py's NBA informative assertion at S116's own archive columns with a clean skip when the landed archive lacks them.
READ: scripts/platformkit/venue_history/nba_wallclock_join.py (whole file) and its four test files under tests/platformkit/venue_history/ (test_nba_wallclock_join.py, test_nba_wallclock_join_tolerance.py, test_nba_checkpoints_full.py) plus tests/platformkit/eval_gate/test_asof_join.py; scripts/platformkit/eval_gate/s116_pooled_ingame.py + s116_corpus.py and tests/platformkit/ingame/test_s116_pooled_ingame.py + test_s116_pooled_ingame_a2.py; docs/evidence/harness/S141_nba_wallclock_tolerance_2026-09-03.md and S142_a2_tests_2026-09-03.md (correction 1).
PREMISE (step 0): print wc -l of nba_wallclock_join.py (expect 313) and the duplicated empty-frame block (line numbers); print the S116 archive's columns and confirm no raw NBA model column. If the module is already <= 300 or the column already exists, say so and skip that half.
LIMIT (step 1): n/a.
CHANGE (step 2): (a) de-duplicate the empty-frame block into one helper (or hoist it) so the module is <= 300 lines with NO behavioural change: save the pre-change outputs of join_game_states on the four test fixtures and on one real event replay (read-only, cached inputs) and assert byte-equality after; (b) the S116 writer gains model_raw / market_raw columns (additive), regenerate to the scratch path only, assert n_informative(NBA) == 78,761 from the scratch archive alone; re-point the a2 test with the skip.
TEST: the four venue_history/asof_join test files must pass UNEDITED; tests/platformkit/ingame/test_s116_pooled_ingame.py and test_s116_pooled_ingame_a2.py pass; add one test asserting the module is <= 300 lines.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = (a) module <= 300 LOC with byte-identical outputs, (b) S116 archive self-reproduces its NBA informative count; denominator = 2
  before        = 0/2
  bar           = 2/2 with every named test green and no test edited except the a2 re-point
  n             = 2 (CONSTRUCT)
  eye check     = n/a; reproduction = verifier re-runs the tests and the equality check in master
  must not move = the 300 s rail, any published number, the landed archive under data/ (never rewritten), the ledger (never open it), data/registry/**
NON-TAUTOLOGY: the equality check runs against outputs saved from the PRE-change module; the informative count is computed from the regenerated archive's own columns, not copied.
EVIDENCE: docs/evidence/harness/S145_S149_wallclock_loc_s116_archive_2026-09-03.md.
POD: none. Do not ssh anywhere.
