GAP S31 | sport all | worktree a12 | log cx_s31_student_gate_defects
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): S04 landed with two verifier-filed defects: the CONSTRUCT tests give every row a unique game_id so ICC=0 and the INSUFFICIENT branch (n_eff<30, clusters<20) is never exercised; and _student_plus_ids hardcodes prior_strength=50.0 while run_student_gate never threads the parameter, so the two id-using arms silently diverge for any other value.
READ (exact paths, re-checked on disk today): scripts/platformkit/eval_gate/student_gate.py -- run_student_gate signature (:143-152, no prior_strength kwarg), id_fixed_effect_baseline call (:165, uses its own default), _student_plus_ids (:97-103, :102 hardcodes `_id_summary(train, id_key, 50.0)`). scripts/platformkit/eval_gate/test_student_gate.py -- `_states()` (:20-45, :39 `"game_id": f"g{index}"` unique per row, n=1000). scripts/platformkit/ingame/gap_effective_n.py -- `intraclass_correlation` (:37 `if count < 2 or total <= count: return 0.0`) -- with 1000 singleton game groups this line fires and short-circuits before any real ICC math.
PREMISE (step 0, RE-MEASURED today): (a) TRUE -- test_student_gate.py:39 gives every row a distinct game_id; total rows (1000) == group count (1000) so gap_effective_n.py:37's `total <= count` guard returns ICC=0.0 by construction, design_effect=1.0, n_eff=n_ticks=1000, n_clusters=1000; student_gate.py:186 `n_eff < 30 or dm.n_clusters < 20` is never true in either existing test -> INSUFFICIENT is dead code today. (b) TRUE -- student_gate.py:102 hardcodes `50.0` inside `_student_plus_ids`; `run_student_gate` (:143) has no `prior_strength` parameter and cannot change it; `id_fixed_effect_baseline` (:81) DOES accept `prior_strength` but is called at :165 with no override either, so both arms are currently coupled only by accident (both silently default to 50.0) with no way to move one without breaking parity with the other. Neither half is FALSIFIED.
LIMIT (step 1): n/a -- CONSTRUCT row, 2 enumerated cases.
CHANGE (step 2), additive only: (1) add `prior_strength: float = 50.0` keyword-only param to `run_student_gate`; thread it into the `id_fixed_effect_baseline` walk_forward call (:165) and give `_student_plus_ids` a new `prior_strength: float = 50.0` parameter (replacing the hardcoded literal at :102) threaded from the same `run_student_gate` argument, so both id-using arms always see the same value. Default omitted call = byte-identical old behavior (50.0/50.0). (2) ONE new test in test_student_gate.py: a clustered CONSTRUCT corpus of <=19 distinct game_ids with multiple correlated ticks per game (so real between/within variance exists, not the singleton short-circuit) -- assert `result.verdict == "INSUFFICIENT"` (clusters<20) and `result.detail["icc"] > 0.0` (real ICC math executed, not the :37 short-circuit). In the same test, call `run_student_gate(..., prior_strength=X)` for one X != 50.0 and independently recompute the student_plus_ids arm brier by calling `_student_plus_ids(train, test, student_fn, id_key, prior_strength=X)` per walk-forward fold; assert it equals `payload["detail"]["arm_briers"]["student_plus_ids"]` -- this fails under today's hardcoded 50.0 and passes once threaded.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = the 2 enumerated defect cases each independently reproduced fixed: (i) INSUFFICIENT verdict reached with icc>0.0 on a <20-cluster corpus, (ii) student_plus_ids arm brier at a non-default prior_strength matches the independently-recomputed value at that same prior_strength
  before        = 0/2 (both dead today: INSUFFICIENT unreached per gap_effective_n.py:37 short-circuit on unique game_ids; prior_strength hardcoded at student_gate.py:102 with no threading path)
  bar           = 2/2
  n             = 2 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns test_student_gate.py, confirms the new test's asserted verdict=="INSUFFICIENT" and icc>0.0, then independently calls `_student_plus_ids` at the same non-default prior_strength and reproduces the payload's student_plus_ids brier to within 1e-9
  must not move = `_DELTA_BRIER_BAR` 0.004, `_P_BAR` 0.05, `_MIN_N_EFF` 30.0, `_MIN_CLUSTERS` 20 byte-identical; the two existing tests (`test_two_construct_corpora_and_preregistered_ledger_order`, leak/refusal tests) unmodified and still passing; `run_student_gate` and `_student_plus_ids` both default to prior_strength=50.0 when the kwarg is omitted, reproducing today's committed S04 numbers exactly
NON-TAUTOLOGY: the two existing CONSTRUCT tests stay unique-game_id rows covering only NULL/TEACHES/leak/refusal -- they are excluded from this metric on purpose because they are the rows that make INSUFFICIENT look untestable; the new test is the only row that can prove it, and dropping it would leave the branch dead again.
EVIDENCE: docs/evidence/harness/S31_student_gate_defects_2026-09-04.md -- step-0 premise with the exact line numbers above, the new test's verdict/icc printout, the two brier values (hardcoded-50.0 vs threaded-X) that used to collide and now diverge correctly, full test output, and a NOT VERIFIED list (no real trial is sealed by this row; S26 still blocks any real teacher).
TEST: exactly one new test in scripts/platformkit/eval_gate/test_student_gate.py. Run only `python -m pytest scripts/platformkit/eval_gate/test_student_gate.py -q`.
POD: none.
COMMIT: explicit pathspec (student_gate.py, test_student_gate.py, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
