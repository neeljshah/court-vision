GAP S03 | sport tennis (harness) | worktree a11 AFTER S02 LANDS (same module, second JoinSpec) | log cx_s03_close_join_tennis
VERIFIER-FILED PREREQUISITES (2026-09-03, from the S02 landing -- part of THIS pass, same module):
- Fix S35 in close_join.py: by_corpus_unit rates must use the FULL spine per unit as denominator (ATP 30,616 / WTA 11,270), never matched rows only; a per-unit join_rate of exactly 1.0 with unjoined > 0 overall is an automatic self-REJECT.
- Label S34 in every artifact this pass writes: states built by gate_corpus_states carry `vintage: SYNTHETIC` until a real odds timestamp exists; the coverage_report JSON and the memo both print that label.
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): tennis same, with two disjoint `corpus_unit`s: ATP 25,831/30,616 = 84.4 pct joinable, WTA 8,028/11,270 = 71.2 pct. `b365w`/`b365l` are winner/loser oriented and LEAKY beside the de-leaked `_p1`/`_p2` (verified row 0).
READ (read every signature on disk; line numbers drift): scripts/platformkit/eval_gate/close_join.py AS LANDED BY S02 -- `JoinSpec`, `close_column`, `gate_corpus_states`, `coverage_report`; if that file is ABSENT then S02 has not landed, STOP and report BLOCKED. scripts/platformkit/combo/corpus_cache.py `_build_tennis` and `load_gate_corpus` (read-only for this job); kernel/validation/proof_metrics.py `devig2` (decimal, FAILS OPEN to (0.5, 0.5) on any price <= 1.0 -- drop bad rows FIRST); scripts/platformkit/eval_gate/walkforward.py `walk_forward`, `LeakError`. Data verified present 2026-09-03: data/domains/tennis/matches.parquet, data/domains/tennis/wta_matches.parquet, data/domains/tennis/odds.parquet (33,952 rows; columns b365w, b365l, psw, psl, maxw, maxl, avgw, avgl, b365_p1, b365_p2, ps_p1, ps_p2), data/cache/combo/gate_corpus_tennis.parquet (41,886 rows). kernel/** is READ-ONLY (human-gated).
PREMISE (step 0): reproduce ATP 25,831/30,616 and WTA 8,028/11,270 by a raw `event_id` merge of odds onto each spine, printed SEPARATELY; and confirm on row 0 that `b365w == b365_p2` and `b365l == b365_p1` (the winner/loser orientation IS the leak). If either count or the row-0 orientation differs, STOP, write the memo, commit, report FALSIFIED -- a valid result that earns its own register row.
LIMIT (step 1): the two raw join rates you just printed ARE the limit. If ATP < 84.4 pct or WTA < 71.2 pct, STOP and report CLOSED AT LIMIT with both numbers. ">= 95 pct is UNMEETABLE and never enters a prereg" -- do not aim at it and never write it as a bar.
CHANGE (step 2): a SECOND `JoinSpec` in the landed close_join.py (`ps_p1`/`ps_p2`, fallback `b365_p1`/`b365_p2`), plus a HARD RULE inside `close_column`: every chosen price column name must end `_p1` or `_p2`, and any `_w`/`_l` column raises `ValueError`. Additive ONLY -- do not rename or alter S02's soccer JoinSpec, the JoinSpec fields, or any landed signature; add optional parameters if you need them. ATP and WTA stay TWO `corpus_unit`s and are NEVER merged into one rate, one Brier or one denominator.
TEST: NEW scripts/platformkit/eval_gate/test_close_join_tennis.py (beside the module, like the other eval_gate tests): a `_w`/`_l` column raises ValueError; per-year AND per-corpus_unit keys are present in `coverage_report("tennis")`; a row whose `ps_*` price is null joins through the `b365_*` fallback. Run ONLY that file: `python -m pytest scripts/platformkit/eval_gate/test_close_join_tennis.py -q`, then ALSO rerun `python -m pytest scripts/platformkit/eval_gate/test_close_join_soccer.py -q` (S02 must stay green) -- two files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = join rate by corpus_unit; denominator = ATP 30,616 spine rows and WTA 11,270 spine rows, reported SEPARATELY
  before        = 0 (no close column on the tennis gate corpus)
  bar           = ATP >= 84.4 pct AND WTA >= 71.2 pct, with per-year and per-corpus_unit rates printed; ">= 95 pct is UNMEETABLE and never enters a prereg"
  n             = 41,886 (scored)
  eye check     = n/a (S-row); reproduction = verifier reruns `coverage_report("tennis")` in master and reprints both unit rates, the per-year table and the bad-price drop count
  must not move = S02's soccer JoinSpec and its measured rate, backtest_runner._devig, corpus_cache (the gate-corpus builder), every threshold under scripts/platformkit/eval_gate/, kernel/**, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows)
NON-TAUTOLOGY: the denominators are the FULL spines (30,616 and 11,270); unjoined rows and bad-price drops stay in the denominator and are COUNTED, never removed, and the two units are never pooled to lift the weaker one. No `_charge_ledger` call anywhere -- nothing here is a charged trial.
EVIDENCE: docs/evidence/harness/S03_close_join_tennis_2026-09-03.md -- premise counts, the row-0 orientation check, both limit rates, the coverage_report output per unit and per year, the bad-price and null-close drop counts, exact commands, both test outputs, and a NOT VERIFIED list. Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local parquet only. No deploy before ACCEPT (B5).
COMMIT: explicit pathspec (the module, the new test, the memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
