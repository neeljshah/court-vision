GAP G416 | sport basketball | worktree a10 | log cx_g416_court_point_proposal
**RANK 2 - PREPARE COURT RECOMPUTATION WITHOUT A FALSE STALENESS WIN. Landed docs/evidence/tracking/g410_position_box_frame_consistency_2026-09-12/summary.json SHA-256 16e779c520ad2ac41dc2e4f79012cabbf23143107c74afde53f09b7a6103573d and docs/evidence/tracking/g410_position_box_frame_consistency_2026-09-12/matrices.jsonl SHA-256 138e5840ad8809fcb60a97d58de53db8a5bca03a1074674591537679176dfc3b bind replay-only observations; G412 transforms at the full path/hash in G415 bind geometry. Original-row retention and replay CLAMP guards are different populations.**
WHERE THIS ROW RUNS: PC PREPARE steps 0-1, 20 min; pod stage NONE; PC FINISH steps 2-7, <=75 min; codex-sol VERIFY 20 min; total <=115 min. Arithmetic and retained low-memory native renders only; EMPTY model set.
OWNS: scripts/platformkit/tracking/g416_*.py; tests/platformkit/test_g416_court_point_proposal.py; docs/evidence/tracking/g416_court_point_proposal_2026-09-12.md and matching directory; docs/research/organization-sprint/PROPOSED_g416_court_point.diff, applied nowhere.
PREMISE step 0 (BINDING before-condition): reproduce all 279 independent checks across 37 sections and the 7 CLAMP_GUARD_REPRODUCED rows/5 sections; census EVERY original/replay matrix and emitted-row key. Original bindings must not be asserted from matching section/frame alone. Missing files -> PARTIAL; an existing equivalent additive proposal -> FALSIFIED.
LIMIT: already fewer than 30 independent CLAMP sections, so empirical CLAMP improvement is NOT VALIDATED at entry. This finite PC row can finish mechanical transport, a PROPOSED receipt and a full binding deficit; it cannot repair historical pre-launch model identity or assert that reducing point retention improves tracking.
METHOD sealed:
1. Sort all 37 observed sections by source digest/section id and draw 30 exact-even sections using floor(j*(N-1)/29+0.5); take each section's middle saved tick independent of branch or outcome. Retain the full 279-check census separately; missing selected bindings stay UNKNOWN, without substitution.
2. Join replay M/M1, box, observed point and predecessor ONLY through exact same-run source/window/frame/slot/call identities from saved traces. Keep original G402/G410 per_row records in a separate namespace. Emit missing call identity, ambiguous joins and cross-run matrices in binding_deficits.csv; never join replay matrices onto landed original rows.
3. Derive nominal native unpadded bottom-centre from G412, then convert BACK to cropped coordinates before M1 @ M projection; use archived clipping, integer centre, homogeneous division and int32 rules. Never add 60 to a court-map point. Missing/invalid/singular/nonfinite inputs yield UNKNOWN and preserve the old point.
4. Add shadow_court_x/y, shadow_status, matrix/source/call receipts and would_violate_existing_guard fields; no replacement of x_position/y_position. Preserve 250/350 guard semantics and ankle/non-box exceptions; a nominal box-foot alternative is explicitly hypothetical on those routes.
5. Compare independently calculated homogeneous-corner oracle values against the proposal expression on all 30 drawn matrix/box snapshots, and 32 exhaustive CONSTRUCT cases: 2 sizes x 2 clip states x 2 matrix validity states x 2 receipt-binding states x 2 box states. Missing binding is a tested UNKNOWN outcome, never a numerical agreement.
6. Report all original 118 CLAMP rows/97 moving boxes descriptively. For independently bound replay CLAMP pairs only, publish old/new predecessor equality and paired point deltas with denominator and UNKNOWN counts; any <30-section subset is descriptive only. No comparison to the same recomputed box-foot may be called an independent localisation test.
7. Inventory all readers of proposed fields; extract/exercise the additive PROPOSED expression without applying it. Render the full 30-section draw with old/shadow points and identity status, reproduce twice, and finish with a precise per-section missing-receipt list for any future quality experiment.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Projection/receipt mechanics | No bound additive shadow proposal | 32/32 controls; all drawn cases agree with independent oracle OR prescribed UNKNOWN; zero foreign-run numerical outputs | 32 CONSTRUCT; 30 distinct section snapshots, numerical n separate | All 30 exact-even cards | Guard constants, physical-map meaning, original points/boxes | DONE PROPOSED ONLY; unsafe projection REJECT |
| Binding census and compatibility | 279 checks/37 sections; 7 guard rows/5 sections | Every check/failed join accounted; all original fields unchanged; source/run/call constraints explicit | Whole 279; 30 sampled sections | Same 30 plus named binding failures | Original/replay namespaces, parent schemas and statuses | DONE diagnostic; incomplete census PARTIAL |
| Empirical CLAMP improvement | Original holds 118/118; replay guard sample only 5 sections | NOT EVALUABLE tonight: independent comparison on >=30 bound CLAMP sections absent; report actual paired counts only | Actual <=5 guard sections, never promoted to 30 | All available guard cases are descriptive | No automatic point replacement or new accuracy claim | NOT VALIDATED fixed at entry; proposal may still finish |
EVIDENCE: docs/evidence/tracking/g416_court_point_proposal_2026-09-12.md and directory containing COMMON plus binding_deficits.csv, replay_shadow_points.csv, original_retention.csv, paired_clamp_counts.csv, construct_cases.csv, oracle_checks.csv, compatibility.csv, reader_manifest.csv and a byte-identical evidence copy of the named PROPOSED diff. NOT VERIFIED: CLAMP quality, physical homography, original model identity, live behavior.
TEST: python -m pytest tests/platformkit/test_g416_court_point_proposal.py -q; foreign-run rejection, crop/native inversion, singular matrices, all 32 controls, guard preservation, missing/predecessor states, full schemas and character-code Q6 fixtures.
VERSION 2026-09-12 v1 PROPOSED; common contract binds; finite PC mechanism work, no new pod capture and no attempt to pass a five-section quality sample.

COMMON BINDING CONTRACT (astra round 12, inherited by this block):
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B including A7/A9/A11/B11/H1/S1/S2/S4/S5, plus Q1/Q3/Q6; docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md. Each block inherits these lines. Orchestrator verifies the WHOLE-set premise and free ownership before dispatch.
Execution: codex terra PREPARE on PC seals protocol alone, then at most ONE self-contained pod script, then codex terra FINISH on PC, then codex-sol VERIFY. At most TWO concurrent codex jobs, including verification; no Claude Opus finisher. Run pod stages sequentially, G415 first; release PC agent slots during compute and relaunch FINISH from durable receipts.
Pod override authorized in this brief: orchestrator launches the sealed script over ssh with nohup in /workspace/g<N>_scratch and polls launch.json, progress.json and terminal.json; no lane waits for user input. Script uses only its own stdlib code plus installed libraries; archive its full text/hash before launch. No imported unshipped helper or second pod stage.
Identity/retention: before launch retain, independently read back and hash every selected ORIGINAL off-pod; record full PC/pod paths, bytes, dimensions and source/window keys. PREPARE obtains read-only hashes of every exercised model, route, script and configuration BEFORE the ssh job launch; the script checks them again before loading assets. No inference means explicit EMPTY model set, not an omitted receipt.
Pod tree /workspace/deploy/nba-ai-system is read-only. Use existing matching sources read-only or stage retained bytes in owned scratch; no fetch from the internet, producer-table regeneration, training, threshold sweep, daemon restart, process kill, registry write, flag change or source pruning. Operational resource checks cannot alter a measurement bar.
Resource budget: one sequential GPU stream, CPU threads <=2, inspect available VRAM and scratch-write viability inside the script; <=2 GB incremental scratch and <1 GB TOTAL pulled outputs per row. PC streams tables, holds one decoded frame at a time and caps its worker at 256 MiB beyond the agent. Record failures and stop within the sealed stage budget, with terminal receipts even on failure.
Seal SHA-256 of exact ASCII/LF prereg bytes above its seal line, before scoring. All hashes below are LF-text SHA-256 unless explicitly raw. Record BOTH raw and LF input digests; changed content requires prospective resealing before any outcome, never a retrospective substitution.
Preserve FULL parent columns, meanings, status values, original serialized fields, every failed key and UNKNOWN reason. Snapshot each parent schema, nested receipt paths and historical runs; additions never overwrite them. Keep repeats.json identical/runs, commands, stdout, returncode, baseline_digests and manifests; nest new runs additively.
Two fresh PC processes reproduce delivered tables/cards from saved inputs, with complete per-table digests; this proves saved-output reproduction only. Exact source-digest/window/frame/PTS/slot/call joins are mandatory; missing or ambiguous identity remains UNKNOWN. All coverage uses decoded frames; identify evaluated ticks and row denominators separately.
Q6: field-aware scan EVERY delivered code/text/log/table/claim, proposal, results-log addition, scanner and fixture. Build ALL scan patterns and positive fixtures from character codes; output indices/counts only. Exempt exact opaque identifiers and typed numerical data only; preserve numeric claim detection. ASCII/LF and a complete path manifest are mandatory.
Ownership below is exclusive; preserve peers' changes. Helpers <=300 LOC/file; full-package test imports; only the named per-file test. Future finishers commit complete explicit paths, including dependencies, observations, figures and receipts, plus their own RESULTS_LEDGER.md addition; orchestrator owns register updates. No bulk adds or push.
Every row delivers COMMON = prereg.md, input_hashes.csv, parent_schema.json, source_receipts.csv, draw.csv, exclusions.csv, denominator_table.csv, eye_index.csv, renders/, summary.json, repeats.json, runtime_receipts/, q6_scan.json, file_manifest.csv, SHA256SUMS with declared byte domain, and NOT VERIFIED. Existing source files stay at retained absolute paths with verified readback recipes; all numerical reproduction inputs are in evidence.
No implicit retry: false completed premise -> FALSIFIED; missing inputs/resources -> PARTIAL; failed measurement -> NOT VALIDATED. At the stage deadline preserve all planned keys as measured/failed/NOT MEASURED and finish. Never extend supply windows or replace failed draws to meet n.

ORCHESTRATOR FOOTER (binding, 2026-09-12): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage; hash EVERY exercised model, route file and config BEFORE any launch); the PC pool receiver (C:/Users/neelj/pool_receiver/: objects/, receipts.jsonl, state.json; one section per new video id = the earliest-tracked section) is the accumulated off-pod source pool; finishers poll their OWN background jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed window and retain overruns separately; a failed planned unit stays an UNKNOWN row; inherited bars byte-identical; never change an existing field's semantics -- add a new field with an alias; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY touched text path incl. the scanner's own source and .log.txt receipts (patterns and restricted constants built from character codes; receipts emit pattern indices, never token text, with a complete path manifest); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round12_2026-09-12.md (local-only). COORDINATE CONTRACT: G412 (LANDED master 5a0b84cba) seals TOPCUT = 60 / PAD = 15 / tuple order / clip order in docs/evidence/tracking/g412_box_frame_contract_2026-09-12/transforms.json; G413 (lane a7, verify pending) measured residual translation dx -0.75 / dy -0.375 px on 48 frames; G410 landed PARTIAL + NOT VALIDATED; G411 landed ADJUDICATED; G409 lands adjudicated tonight. G402, G406 and G407 are landed on master; G409 (lane a12) and G411 (lane a19) are in verification.
