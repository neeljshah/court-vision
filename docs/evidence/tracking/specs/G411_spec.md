GAP G411 | sport basketball | worktree a19 | log cx_g411_integer_pts_extent_audit
**RANK 3 - RESOLVE TIME REPRESENTATION WITHOUT MOVING THE BAR. Landed docs/evidence/tracking/g401_fps_cap_duration_shadow_2026-09-11/pts.csv SHA-256 687227893a878fdd403710dd7b2ce5dbb8367e486086cddc8dffb4c765a16b50 and draw.csv in that directory SHA-256 f1d0bdfa752697662d8d093e2d15876c058f7f83840c239a1b5f9742ea35a26c bind 30 timelines. Pending docs/evidence/tracking/g408_pts_duration_stop_proposal_2026-09-11/paired_stops.csv SHA-256 26f0b53193edc25f753629c02dc421944a924d512b8d4e8df683dd10a5f352e0 records 26/30 under the inherited bar.**
WHERE THIS ROW RUNS: PC premise/seal/receiver retention 20 min, CPU exact timestamp extraction <=90, arithmetic/30 native endpoint cards/reproduction/test <=60; total <=170 min. No pod, GPU or patched producer invocation; unavailable off-pod originals -> PARTIAL immediately.
OWNS: scripts/platformkit/tracking/g411_*.py; tests/platformkit/test_g411_integer_pts_extent.py; docs/evidence/tracking/g411_integer_pts_extent_audit_2026-09-12.md and matching directory; docs/research/organization-sprint/PROPOSED_g411_exact_pts_note.md only if a text proposal is needed.
PREMISE step 0 (BINDING before-condition): require accepted G408 handoff and independently reproduce ALL 30 arm-C rows, 30/30 containment, 26/30 extent passes, and the four excesses near 0.000000333 s from its unrounded stored values. Retain every anomaly and source hash; changed counts -> reconcile before any extraction, missing bindings -> PARTIAL.
LIMIT: six-decimal seconds cannot establish integer timestamp precision. Require stream time_base and frame presentation timestamps from the same retained source bytes; never manufacture missing integer values by multiplying a rounded seconds column.
METHOD sealed before extraction:
1. Retain the unchanged G401 30-source exact-even draw and G408 archived streams/manifest. List source paths, bytes, hashes and first/last frame indices; no new source or failure-based replacement.
2. Seal two representations: historical decimal-seconds input exactly as archived, and integer frame PTS times stream time_base as exact rational numbers. Record ffprobe/decoder versions and exact extraction commands before launch; no model is exercised, explicitly record model set empty.
3. Extract every presented frame's integer PTS, best-effort timestamp separately, time_base and frame index. Missing PTS remains missing; best-effort timestamps are diagnostics, not an automatic substitution. Preserve duplicates, gaps, backwards events and source order.
4. Bind every exact frame to G408's stream by frame index and source hash; compare displayed decimal values using the archived serialization rule. Record all mismatches and anomaly classifications before computing endpoint outcomes.
5. Replay unchanged A/B/C mechanics using each representation; elapsed time is PTS minus the sealed first origin, C validates every PTS before stride filtering, preserves explicit frame-cap precedence, and stops at the first deadline boundary. Malformed PTS terminates UNKNOWN.
6. Preserve G408's first_boundary_extent definition and the EXACT bar abs(first_boundary_extent_s - 100) <= 1/validated_fps. Archive last-admitted and first-excluded endpoints separately; no alternate endpoint, epsilon, extra interval, rounding-to-pass or fitted tolerance.
7. Use exact integer/rational comparison with the original validated FPS value; keep any alternative rate metadata descriptive. Report every decimal/rational verdict change and whether timestamp serialization alone explains it; genuine missing-frame extent failures remain failures.
8. Enumerate CONSTRUCT boundary cases for each observed time_base/rate pair: exactly at deadline, one timestamp unit on either side, duplicate, missing, backwards and dropped deadline neighbor, including each skipped-by-stride anomaly. All cases are additional to the 30 real timelines.
9. Publish a mechanics recommendation attached to the SAME user G401 decision. Preserve G408 26/30 historically; an exact-timebase result gets its own version/denominator. Runtime after frame 3000 and the decoder's live PTS behavior remain NOT VALIDATED.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Timeline/representation binding | 30 streams; four marginal extent misses | 30/30 original source binds; every frame/anomaly retained; decimal stream reproduced or discrepancies explicitly unresolved | Same 30 sources; all frame rows | 30 exact-even native endpoint cards with PTS annotations | Draw, archived streams, anomaly rules | Complete DONE diagnostic; missing precision/binding PARTIAL |
| Exact-time extent | Historical C 26/30; containment 30/30 | Same unrounded one-native-interval bar on 30/30, plus temporal containment 30/30; no epsilon or endpoint switch | 30 distinct source timelines; shared games identified | All 30 endpoint cards | G408 bar, FPS basis, cap precedence, stride validation | DONE mechanics only if both pass; any real miss PARTIAL; moved bar REJECT |
| Reproduction/control completeness | Integer precision unexplained | Two exact saved-input reproductions identical; every enumerated CONSTRUCT outcome correct | 30 timelines plus fully enumerated cases | Every anomalous endpoint and all 30 cards | Old verdicts, producer, _VRAM_FLUSH_INTERVAL=3000 | Exact DONE; missing extraction identity NOT VALIDATED |
EVIDENCE: docs/evidence/tracking/g411_integer_pts_extent_audit_2026-09-12.md plus matching directory: prereg.md, input_hashes.csv, source_receipts.csv, draw.csv, integer_pts/, time_bases.csv, stream_join.csv, anomalies.csv, paired_stops.csv, construct_cases.csv, summary.json, repeats.json, eye_index.csv, renders/, extraction_receipts.json, runtime_receipts/, q6_scan.json, SHA256SUMS. NOT VERIFIED includes any patched-module behavior or deployment.
TEST: python -m pytest tests/platformkit/test_g411_integer_pts_extent.py -q; rational boundary comparison, decimal serialization loss, genuine deadline gaps, malformed skipped PTS, first-boundary versus last-admitted distinction, and code-built scanner fixtures.
VERSION 2026-09-12 v1 PROPOSED; this audits precision, not a new tolerance or authorization. Any eventual runtime adoption remains the existing USER G401 decision.

COMMON BINDING CONTRACT (astra round 10, inherited by this block):
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6; each block inherits these rails. Orchestrator rechecks free owner, accepted dependencies and WHOLE-set premise before dispatch; changed artifact bytes require an explicit prospective handoff, never silent substitution.
Execution: codex terra PREPARE seals protocol/bars alone; Claude finisher retains inputs and completes measurements, test, reproduction and memo within 180 min. Each row owns only its named helper prefix, one test, memo/directory and any named PROPOSED diff. They are not alone in the repo; never revert peer edits.
Retention: BEFORE ANY pod stage, finishers retain every selected original OFF-POD, independently rehash/read back native bytes and record paths, bytes, dimensions, rational PTS and failures. A pin/hash alone is insufficient; no replacements after outcomes. Missing inputs -> PARTIAL immediately, not a wait.
Identity/resources: hash EVERY exercised model, route, library configuration and actual loaded asset before launch. Pod steps use ~/bin/pod_run aXX --ship <owned helpers> --fetch <owned evidence> -- <sealed command>, only /workspace/wt/aXX; original source trees remain read-only. Gate on measured free VRAM/CPU and a pod write probe, not lane count; <=6 cores, threads 1, sequential GPU, <2 GB incremental scratch, quota UNKNOWN never zero.
Bound every derived table to sealed frame/PTS windows; archive overruns separately and retain silence, failed keys and UNKNOWNs. Preserve full parent table schemas and repeat shape: identical, runs, each command/stdout/exit/digests. Two fresh processes reproduce delivered tables/cards from delivered bytes; this does not prove inference repeatability.
All helpers <=300 LOC, full-package test imports, exactly the named per-file test. No src/ or other gated-tree edits; only PROPOSED diffs under docs/research/organization-sprint/, applied nowhere. No daemon restart, shared-module change, held-out invocation/sweep, training, flag change or registry write.
Write LF/ASCII. Field-aware Q6 scans cover every delivered code/text/log/table/result claim field; exempt only exact opaque identifiers and typed numerical data, never whole file types. Build patterns and positive fixtures from character codes; emit pattern indices/counts only. Preserve semantic redaction receipts and the declared SHA256SUMS byte domain.
Finishers commit the COMPLETE explicit file list, including imported helpers, fixtures, raw observations, renders and receipts; no bulk adds/push. Poll owned work to completion; never stop to wait. Premise false -> FALSIFIED memo; supply/resource failure -> scoped PARTIAL/NOT VALIDATED closure; second failed attempt -> CLOSED AT LIMIT, no repeated discovery loop.

ORCHESTRATOR FOOTER (binding, 2026-09-12): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage; hash EVERY exercised model, route file and config BEFORE any launch); finishers poll their OWN background pod jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed source-frame/PTS window and retain overruns separately; a failed planned unit stays an UNKNOWN row; inherited bars byte-identical (no rounding, no added tolerance); never change an existing field's semantics -- add a new field with an alias; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY touched text path incl. the scanner's own source and .log.txt receipts (patterns built from character codes; receipts emit pattern indices, never token text); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round10_2026-09-12.md (local-only). G402, G405 and G406 are landed on master; G403's evidence lands next (lane a3 tip 0a46c0e6c); G407/G408 are in fix cycles (lanes a10/a11); G404 runs on a18.
