GAP G387 | sport basketball | worktree a21 | log cx_g387_paint_localization_controls
**PAINT LOCALIZATION WITH COORDINATE CONTROLS. Landed G379 summary SHA-256 a61b9190343cb6a5303545d355e3670a9738a7e3f7b02d28b6fabd114a0fe4cb establishes the unsolved screen. G382 lane PARTIAL inputs, independently hash-checked: docs/evidence/tracking/g382_court_stroke_specificity_2026-09-10/summary.json 79975c5917af6a175be97032035e6a7b8cdeb05fcdbde3060db9053b016a7ee0; docs/evidence/tracking/g382_court_stroke_specificity_2026-09-10/reference_agreement.json 7308ab5a3bb66fabb69f66f8016ef47e4d7a19e3d1d4cb46dcd11c471ba195a3; docs/evidence/tracking/g382_court_stroke_specificity_2026-09-10/marking_only.json 9bc53aa82046834770436c51d7f0823f34a747b21fac288b86a0f3072e9602a2. These diagnose missing reference, not a validated oracle.**
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, Q1/Q3/Q6, A9/A11/B11/H1/S1/S2; self-check every B clause. DONE is measurement completion; landing uses contract C.
WHERE THIS ROW RUNS: PC step 0 census, PREPARE, sheets, blind rating, adjudication, arithmetic and tests; no GPU. Pod only if native recovery is needed, via ~/bin/pod_run a21 in /workspace/wt/a21, read-only source access, never the deployed tree.
BUDGET/OWNERSHIP: codex terra PREPARE 30 min; Claude finisher capture 20, two isolated blind pixel raters plus adjudication 100, receipts 30; hard total 180 min. Orchestrator confirms a21 handoff from G383 before dispatch; one owner, no duplicate run.
OWNS: scripts/platformkit/tracking/g387_*.py, tests/platformkit/test_g387_paint_localization.py, docs/evidence/tracking/g387_paint_localization_controls_2026-09-11.md and matching directory only, plus the required result row at finishing; no shared-module edits. New files <=300 lines; gated changes only as PROPOSED diffs under docs/research/organization-sprint/.
PREMISE step 0 (BINDING before-condition): reproduce 60 planned/49 retained/11 failed from docs/evidence/tracking/g382_court_stroke_specificity_2026-09-10/frames.csv field retained, all 57 paired IDs/34 zero overlaps from adjudication.csv, and inventory every marking_only.json frame for G383 eligibility; absent independent correspondences are UNKNOWN, never invented.
Author's whole-manifest availability check: identity_map.csv lists 49 PC native files at 1920x1080, all 49 SHA-256 matches, 135,177,764 bytes; 11 planned failures have no native file. Recheck all paths, hashes and decoded dimensions before any labels; seal this before-condition and the full eligibility table.
If a complete independent reference now meets G383's >=30-frame FIT/VALIDATION supply, report PREMISE FALSE with receipt. Missing identity or fewer than 30 usable native files -> NOT VALIDATED, no substitute sample; read any G382 verifier correction before accepting its inputs.
METHOD (sealed): terra commits prereg alone, SEAL sha256 over LF-normalized bytes above seal line, before new rating/scoring. Seal source/reference/code hashes, selection, controls, rater instructions, transforms and bars; Claude measures only after seal verification.
1. Sort all 49 retained frames by (video_id, section_id, numeric frame_order); take indices floor(j*48/29+0.5), j=0..29. Archive all 60 original states and every excluded key. This is a conditional decoded-frame study, not a replacement G382 draw; no substitutions after seal.
2. Copy the 30 selected native PNGs to the row's immutable evidence cache, verify sender/receiver hashes and record exact paths, sizes, resolution and frame indices/available PTS. Preserve original bytes; export six fixed 640x540 tiles (3 columns x 2 rows) with integer offsets and no rescaling, plus context image. Missing PTS is explicit.
3. Raters receive only opaque native tiles/context, no old labels, strokes, matrices or one another's answers. Independently annotate up to two straight painted markings in the fixed G382 identifier order; use the longest visible contiguous segment, ties topmost then leftmost. Each gets two endpoint clicks and one independently clicked interior midpoint, native coordinates, physical ID and VISIBLdocs/evidence/tracking/ABSENT/UNKNOWN reason.
4. Occlusion ends a visible segment; never bridge it or infer a court template. No visible straight marking permits an empty list, with ABSENT or UNKNOWN recorded separately. Raters record displayed tile dimensions and offsets; labels and display receipts are sealed before control answers or old traces are revealed.
5. Control set: the same 30 backgrounds, one added white 6 px straight band with black 10 px underlay each, length 240 px, orientation cycling 0/30/60 degrees; centre cycles the six tile centres, endpoint order fixed. Seed 387 permutes opaque order; archive exact rendering code, pixels and known endpoints/midpoint before rating, hidden from raters.
6. After real labels are sealed, each rater uses the identical tile-click protocol on all 30 control images, targeting the added band; no feedback, recalibration, instruction change or retry. Control success per image requires BOTH raters' endpoint errors <=3 native px (allow reversal) and midpoint distance to the band centreline <=3 px; missing clicks fail.
7. On every real frame, pair IDs once; a candidate pair needs corresponding endpoints <=6 px apart (allow reversal), and each independent midpoint within 3 px of both reported segments. Unmatched, absent, uncertain or missing observations remain in all-frame counts; report each rater and per-ID counts without treating segments as independent frames.
8. Claude checks ALL 30 real frames in tiles, initially without rater traces: record visible straight paint YES/NO/UNKNOWN. Then reveal clicks and old polygons; audit nine evenly spaced points along each paired segment, including endpoints, within 3 native px of visible physical paint. A frame localizes only if >=1 same-ID pair meets step 7 and BOTH traces have >=8/9 audited points on that paint; uncertainty fails localization, stays UNKNOWN in visibility.
9. Archive originals and every adjudication; report control success/30, real localization/30, pixel visibility states/30, same-sample old-reference agreement and matched old/new overlays. Real success demonstrates recovery on those cases only; control success plus real failure is unresolved visibility/localization, never proof of invisible paint.
10. No extractor, filter or fitter runs; no new H, no tuning against unlabelled broadcast strokes. Endpoint reference is diagnostic, not G383's full oracle. Repeat rendering and scoring in two fresh processes; require byte-identical canonical outputs, without claiming repeatable independent rater judgments.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Input/accounting identity | 49 hash-matched native files; 11 original failures | All 60 states retained; all 30 selected real frames and 30 controls attempted; hashes and transforms exact | 30 unique real frames across evenly sampled source order; 30 paired control contexts, never pooled as 60 independent frames | All 30 selected frames in order, missing cards included | G382 inputs and historical evidence | Incomplete/identity failure: NOT VALIDATED |
| Known-position localization | NOT MEASURED | >=27/30 controls pass step 6 | 30 controls; no scored subgroup below 30 | All 30 control overlays, errors shown | 3 px control tolerance; sealed instructions | Below bar: PARTIAL, protocol not qualified |
| Audited real-paint localization | G382 34/57 pairs disjoint, p50 IoU zero over 49; new click metric NOT MEASURED | >=24/30 real frames pass step 8 AND control bar met | All 30 real frames; failures/ABSENT/UNKNOWN charged | All 30 native contexts, tile views, both click sets, old traces and adjudication | 6 px pairing, 3 px audit, 8/9 rule; no post-score changes | Bar met: DONE for reference feasibility; otherwise PARTIAL with visibility/protocol ambiguity |
| Reproducible measurement | G382 extraction repeats identical; new route unmeasured | Canonical rendering/scoring bytes identical in two fresh processes; all source/route hashes archived | Same full 30 pairs, no recycled frame keys | Repeat checksums bound to every card | G362/G365/G371/G374 code/bars, threshold 0.30, producer, weights, flags, orientation UNKNOWN | Mismatch: NOT VALIDATED; no registration claim on any result |
NON-TAUTOLOGY: control images never fill real n; all planned real failures count against 24/30. Subgroups and individual marking counts are descriptive. Deadline exhaustion -> PARTIAL/NOT VALIDATED, no extension or threshold change; complete negative findings may land under contract C with their limitations.
EVIDENCE: docs/evidence/tracking/g387_paint_localization_controls_2026-09-11.md plus matching directory: prereg/seal chronology, source census, selection/eligibility.csv, native cache/tiles, transforms.csv, controls/known_points.csv, blind ratings, visibility.csv, adjudication.csv, per_frame.csv, repeats.json, summary.json, renders/manifest.csv, SHA256SUMS and receiver readback. Preserve full inherited schemas; include all-frame numerators/denominators and NOT VERIFIED (paint recall, G383 oracle supply, H, deployment, other corpora).
TEST: python -m pytest tests/platformkit/test_g387_paint_localization.py -q only; full package imports; test tile/native roundtrip with planted points, deliberate offset failure, missing labels charged, controls excluded from real n, seal-change detection and two-process identity.
FINISH: PC free RAM >=2.8 GB, <=3 active lanes total; any pod recovery <=6 CPU cores, threads 1, nice 10, peak scratch <2 GB, disk-write probe before cache writes, no process interruption. Poll to completion; no src/ edits, flags, registry writes, deployment or push. Finisher adds one docs/evidence/tracking/RESULTS_LEDGER.md row with memo in explicit-path worktree commit; independent verifier reruns the per-file test and recomputes artifacts. Scan vocabulary from character-code patterns, ASCII only.
VERSION 2026-09-11 - proposed G387 replacement execution; allocation/handoff and later landing belong to orchestrator. This review creates no spec file or register entry.

Review receipts and material corrections
- Independent arithmetic reproduced 6,860 strokes, 460,484 supports, zero intersection on-marking strokes, all category totals and 34/57 zero-overlap pairs; all 12 source pre/post pins match. This is not a replay of extraction or a formal verifier run.
- Header checks: strokes.csv includes frame_key, stroke_id, family, length_px, n_supports, on_supports, on_fraction, on_marking, physical_marking_ids; support_labels.csv.gz includes frame_key, stroke_id, support_index, x, y, base_label, on_dilated_painted.
- Opened six evenly spaced render positions 0,6,12,17,23,29 (filename prefixes 00,12,24,35,47,59): four visible thickets and two explicit DECODE_FAILED cards; also opened adjudication/X5w3V6rh26s_s1376_000042.jpg, which shows displaced traces beside visible paint.
- The complete render-to-frame join gives 25 pixel cards plus five failure cards, prefixes 04,24,39,49,59. The memo's claim that all 30 have pixels is false; preserve these cards and correct the text. frames.csv uses retained for outcomes; selection_status remains PLANNED throughout.
- marking_only.json contains 49 SCORED frames, all with empty strokes, and omits the 11 decode failures; consumers must join the full 60-frame manifest. No valid marking-only oracle was supplied. Intersection/union are not truth bounds, including for the phrase at most 1.06 pct.
- G382 memo raw SHA-256 at read: 334eb5d495ff7bdfcf403b1c071b930e4f5c9cc14aff2923cec01e9068ab8c2d; frames.csv raw SHA-256 80e4b930578cbf42eb8b0e30029c1dacb10e775a830e53b32079172173da15bb. Native file bytes were hashed; the direct native PNG viewer failed, so visual conclusions here use the named JPG cards only.
- Supplied adjacent-row updates do not satisfy this dependency: G378 supplies no independent orientation, G384 closes its arm supply, G385 is SHADOW, G381 motivates content identity, G386 can provide later capture receipts, and G380's unfinished replay pool does not validate paint. These status updates were not independently reverified here.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` over every byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/` READ only (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_g382_followup_2026-09-11.md (local-only). G383 stays allocated; its execution is REPLACED by this row (astra decision B.5): its reference supply is recorded CLOSED AT LIMIT after the eligibility inventory, no fitter arm runs.
