GAP S14 | sport all (harness) | worktree a12 | log cx_s14_fwer_families | LAST -- dispatch after S06 and S13 have landed | THE ONLY BAR-LOOSENING CHANGE IN THE PROGRAM | `combo/fwer_budget.py` is a SHARED MODULE: hold the token in docs/evidence/SHARED_MODULE_TOKEN.md or QUEUE the gap
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): no within-family FDR: only Bonferroni `deflated_p` exists, so a family of hundreds of related hypotheses is priced as if every one were independent.
READ (read every signature on disk; line numbers drift): scripts/platformkit/combo/fwer_budget.py `eps_eff`, `min_corpora_eff`, `cumulative_k`; scripts/platformkit/eval_gate/deflated_metrics.py `deflated_p(raw_p, k)`; scripts/platformkit/eval_gate/null_ship_calibration.py (the planted-null pattern: `pure_noise_predictor`, `run_calibration`, `candidate_ships` -- it measured 0/200 false ships); scripts/platformkit/eval_gate/ledger.py and backtest_runner.py `_charge_ledger` as extended by S13 (the `family` and `k_family` fields) -- if S13 has not landed, STOP and report BLOCKED. DEPENDENCY: statsmodels 0.14.4 is ALREADY INSTALLED, BSD 3-Clause -- verify with `python -m pip show statsmodels` and print the version; this is NOT a new dependency and you must add none.
PREMISE (step 0): `combo/fwer_budget.py` exposes no BH function and `deflated_p` is Bonferroni-only -- before = 0 family-relative bars, every hypothesis priced independently. Print `pip show statsmodels` and the current `fwer_budget` symbol list. If a BH path already exists, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): n/a -- CONSTRUCT row; the 200 planted nulls ARE the enumerated case set.
CHANGE (step 2), and NOTHING SHIPS UNTIL ALL THREE CONDITIONS HOLD -- VERBATIM: "(i) families are frozen in the spec with a commit timestamp EARLIER than the first family-relative trial, (ii) every AHEAD prints BOTH bars (global-K deflated p AND family BH), (iii) no past verdict is re-scored."
  (a) NEW versioned `docs/research/organization-sprint/FWER_FAMILIES_SPEC_2026-09-03.md` freezing the family definitions with a `spec_version`; it is COMMITTED FIRST and the verifier checks its timestamp.
  (b) ADDITIVE in `scripts/platformkit/combo/fwer_budget.py`: `bh_within_family(p_values, q=0.05)` via `statsmodels.stats.multitest.multipletests(method="fdr_bh")`, and `across_families(p, n_families) = deflated_p(p, n_families)`. Add nothing else; rename and remove nothing (B2); `deflated_p` itself is untouched, and the global `k_cumulative` is still written and reported on every trial.
  THE DANGER, STATED PLAINLY IN THE MODULE DOCSTRING AND THE MEMO: a family-relative bar is strictly LOOSER than today's global one. No past verdict may be re-scored under it -- that is a self-serving reinterpretation.
TEST: NEW tests/platformkit/combo/test_fwer_families_bh.py: 200 planted nulls following the `null_ship_calibration` pattern yield <= 10 discoveries at q=0.05; a family whose members are all null yields no discovery; the global `k_cumulative` is UNCHANGED by the switch (read a tmp ledger copy before and after). Run ONLY that file: `python -m pytest tests/platformkit/combo/test_fwer_families_bh.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = false discoveries on a planted-null family; denominator = 200 planted nulls
  before        = 0 family-relative bars exist (Bonferroni `deflated_p` only)
  bar           = <= 10 discoveries at q=0.05 AND `k_cumulative` unchanged AND FWER_FAMILIES_SPEC_2026-09-03.md committed with a timestamp EARLIER than the first family-relative trial
  n             = 200 (scored)
  eye check     = n/a (S-row); reproduction = verifier reruns the 200-null test in master with the same seed, recounts discoveries, and re-reads `k_cumulative` from the ledger before and after
  must not move = `k_cumulative` and its monotonicity, `deflated_p`, `eps_eff`, `min_corpora_eff`, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows -- the test uses a TMP ledger copy only). NO PAST VERDICT IS RE-SCORED under the looser bar.
NON-TAUTOLOGY: all 200 planted nulls are in the denominator; none is dropped, re-seeded or re-labelled after the fact to bring the discovery count under 10. No `_charge_ledger` call against the real ledger path -- the tmp copy only.
EVIDENCE: docs/evidence/harness/S14_fwer_families_2026-09-03.md -- the `pip show statsmodels` output, the frozen families and spec_version, the two commit timestamps (spec before first family trial), the discovery count at q=0.05 with the seed, the both-bars example, the `k_cumulative` before/after, test output, and a NOT VERIFIED list. State plainly in the memo that this is the only change in the program that loosens a bar. Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. No deploy before ACCEPT (B5).
COMMIT: explicit pathspec (the families spec doc, fwer_budget.py, the test, the memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
