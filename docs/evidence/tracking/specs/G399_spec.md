GAP G399 | sport basketball | worktree a12 | log cx_g399_qualified_paint_larger_sample

**RANK 3 - FINAL RETAINED-SOURCE PAINT SUPPLY MEASUREMENT. Landed docs/evidence/tracking/g386_pin_copy_one_pass_2026-09-10/summary.json SHA-256 e0e6119766899786a4004cd34e7587bacc615853cc87674c85ce4d06d297803f supplies 30 retained sources. Landed docs/evidence/tracking/g387_paint_localization_controls_2026-09-11/selection.csv SHA-256 730e3af2bfffd030c9f6c281c3007a12655458829f6de6cdbcd7f61407ff2f49 names the old sample. Verification-pending docs/evidence/tracking/g396_third_rater_paint_qualification_2026-09-11/summary.json SHA-256 19c8f71d6c1648121405b6d7fdc700cfea55953ed8f8855d9bba758a53a341c6 fixes the qualified pair and 3/30 recovery.**

WHERE THIS ROW RUNS: PC step 0/PREPARE 25 min; retained-source CPU decode 20; two blind qualified raters plus Claude pixel audit 110; arithmetic/test/receipts 25. No pod job, GPU or fitter.
OWNS: scripts/platformkit/tracking/g399_*.py; tests/platformkit/test_g399_qualified_paint_larger_sample.py; docs/evidence/tracking/g399_qualified_paint_larger_sample_2026-09-11.md and matching directory only.
PREMISE step 0 (BINDING before-condition): require G396 independent disposition, reproduce astra 30/30, sol 27/30, joint 27/30 and three real audit passes; verify both frozen instructions/runtime configurations and working native-image access.
Census ALL 30 G386 retained source objects, currently present on the PC; rehash/decode, verify actual 1920x1080 and source PTS spans. Require >=30 sources with bound identities before rating; missing/changed bytes -> NOT VALIDATED, no substituted footage.

METHOD (sealed; one larger sample under the qualified protocol, no correction/requalification tournament):
1. Freeze the 30 sources in competition/video/section/source-digest order; all 30 are used, with two interior targets per source at one-third and two-thirds of the recorded usable PTS span. Select nearest decoded PTS, ties earlier; archive all 60 planned states before labels.
2. Derive usable span by a read-only decoded-PTS census of each retained object, reconciled to capture/receiver bounds; a single receiver interior frame or duration header cannot establish the span. Reject duplicated pixels/old context identities as named failures without replacement; no visibility-conditioned draw.
3. Keep original 60/49/11 parent accounting separate. New decode failures stay in the new planned 60-state census; report recovery/60 plus recovery/N_decoded and visible-conditioned recovery. Require >=30 decoded unique contexts for any scored statement.
4. Export the unchanged native 640x540 tiles with integer offsets and context view; preserve source bytes, frame hashes, actual resolution/fps and transforms. This row is 1080p-only; no inference about 720p paint.
5. Use astra and sol at their G396 high settings with frozen instructions; verify image-open/output fixtures before dispatch. No practice feedback, correction, changed family menu, substituted rater or qualification-score carry from another setting.
6. Two raters independently issue VISIBLE/ABSENT/UNKNOWN and up to two contiguous straight fragments per context under the G388/G396 family protocol. No old traces, peer answers, detector overlays or inferred lines through occlusions.
7. Freeze tile/family pairing by smallest symmetric line distance with stable ties, third point <=3 px from own line, span >=60 px, strictly interior third point and all points within the other's 6 px band; archive every unmatched fragment.
8. Claude records native visibility for all 60 states BEFORE seeing traces, then independently audits EVERY candidate pair: same physical paint band and >=8/9 evenly spaced native points on EACH fragment within 3 px of visible paint.
9. Store one unique audit token per pair, all per-point judgments, native zoom cards and disagreements. Candidate agreement alone never counts as recovery; the finisher's own pixel audit decides. Family disagreement and absence remain explicit.
10. Recovery = states with >=1 audited pair, counted once. Continuation requires >=30 such states, the necessary sample supply for G383; this is not sufficient for its four non-collinear FIT plus six separate VALIDATION points per frame.
11. Fewer than 30 recovered states -> CLOSED AT LIMIT for this reference route/sample; pause paint-based fitter work. At least 30 -> reference assembly may be proposed separately; no H fit, orientation inference or registration acceptance here.
12. Repeat pairing/scoring arithmetic twice from immutable answers; inspect all 60 contexts in even order and every candidate audit. Report section/video clustering, reused source membership and unmeasured rater/adjudicator repeatability.

ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Qualified rating and identity | G396 astra 30/30, sol/joint 27/30; old recovery 3/30 | Same qualified configurations/instructions; all source identities exact; all planned states retained; >=30 decoded unique contexts | 60 planned contexts/30 sections, video count explicit | All 60 native/absence cards, evenly ordered | 3 px, 6 px, 8/9, span, family protocol and prior results | Configuration/identity fault NOT VALIDATED |
| Audited reference supply | Old sample 3/30; remaining 19 can add at most 19 | >=30 new states with >=1 pair passing BOTH independent fragment audits | Planned 60; decoded and visible denominators separately named | Every candidate's nine-point audit plus all empty states | No invented paint, no visibility selection, no replacement | Below supply bar CLOSED AT LIMIT; met DONE for necessary supply only |
| Completeness/reproduction | No larger qualified sample measured | Two raw answers or explicit failure per state; every candidate audited; canonical scores identical twice | All 60 states and every candidate | Same full set; no start-only inspection | G383 independent FIT/VALIDATION prerequisite, all production files | Missing review PARTIAL; arithmetic mismatch NOT VALIDATED |

EVIDENCE: docs/evidence/tracking/g399_qualified_paint_larger_sample_2026-09-11.md plus directory: prereg, source_receipts.csv, pts_bounds.csv, selection.csv, runtime_receipts.json, frozen_instructions/, native_manifest.csv, blind_ratings.csv, visibility.csv, pairing.csv, audit_points.csv, per_frame.csv, repeats.json, summary.json, renders/ and common receipts.
TEST: python -m pytest tests/platformkit/test_g399_qualified_paint_larger_sample.py -q; PTS spacing/dedup, failed decode retained, configuration drift blocks dispatch, wrong parallel band fails, per-pair audit isolation, full planned denominator and necessary-versus-sufficient supply.
VERSION 2026-09-11 v1 PROPOSED; one final larger sample, not a re-score of G396 and not a claim that broadcast paint is inherently unobservable.

COMMON BINDING CONTRACT (astra round 6, inherited by this block):
Common binding contract: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5, Q1/Q3/Q6; each block inherits this paragraph and the following execution rules.
Each row: codex terra PREPARE, Claude finisher MEASURE, independent verifier; <=180 minutes including receipts. PREPARE seals protocol alone before metrics, then exact input/code/config identities before dependent work.
OWNS only its named new helper prefix, one test, memo and matching evidence directory. Helpers <=300 LOC, full-package imports; per-file tests only. Shared modules and production trees remain unchanged; gated changes only as named PROPOSED diffs.
Preserve full inherited schemas, all planned missing/failed/ABSENT/UNKNOWN states, exact paths/bytes/dimensions/PTS, hashes, raw judgments, even render index, exclusions and NOT VERIFIED. A missing denominator is UNKNOWN, never zero.
Pod steps use ~/bin/pod_run <lane> with explicit --ship/--fetch under /workspace/wt/<lane>/; deployed trees read-only. Gate actual VRAM/CPU needs, threads 1, <=6 CPU cores, scratch <2 GB and successful pod write probe before bulky writes; no restart or peer interruption.
PC free RAM >=2.8 GB and <=3 active lanes including raters; block/poll own work to completion within the fixed budget. Deterministic arithmetic is not repeatable inference or rating; single-run outputs remain descriptive.
Durable per-unit tables, summary.json, seals, time/resource receipts and readback belong under the row's evidence directory. Landable text/logs must pass ASCII and character-code-built Q6 scans; do not print matched text.
DONE means research completion; landing uses contract C. A failed evidentiary bar stays failed. No flags, registry writes, production deployment, threshold sweeps or renewed spent allowances.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round6_2026-09-11.md (local-only). G394's and G396's evidence lands on master after their verifies; a premise step that needs it reads the lane tips named in the spec until then (G394 a7 87a497e94; G396 a3 fead21999).
