GAP S158 | sport all | worktree a10 | log cx_s158_neff_audit
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md sections B and Q -- read it; self-check against every line of section B before you report. S-row: eye check = n/a.
PREMISE (step 0): reproduce S88 pooled from docs/evidence/harness/s88_phase_recal_2026-09-04.json pooled block -- n_informative 11,087, n_games 127, rho 0.703096, deff 61.6767, n_eff 179.760 -- 1e-6. ALGEBRAIC FACT (prove it, do not re-derive from scratch): scripts/platformkit/ingame/gap_effective_n.py:53-59 design_effect(rho, m_bar)=1+rho*(m_bar-1), m_bar=n_ticks/n_games, rho clipped to [0,1] at :50; n_eff=n_ticks/deff (:77) then satisfies n_eff-n_games = (n_ticks-n_games)*(1-rho)/deff >= 0 for EVERY rho in [0,1], equality only at rho=1. So n_eff >= n_games is GUARANTEED by this formula, not an anomaly -- the row's implicit "n_eff <= n_clusters" bound holds only at the single point rho=1. If you find a counterexample (any rho in [0,1] with n_eff < n_games on real archived data), STOP and report it -- that supersedes everything below.
LIMIT (step 1): enumerate every published numeric n_eff in these 9 files (grep -n "n_eff" on each, list file:line:value): S58_T2_FIRST_CHARGE_2026-09-03.md, S58_trial1_e2_slice_2026-09-03.md, S58_trialA_clamp_family_2026-09-03.md, S87_tick_informative_2026-09-03.md, S87b_informative_enforced_2026-09-03.md, S116_pooled_ingame_2026-09-03.md, S152_s116_rerun_2026-09-03.md, S137_rebaseline_2026-09-03.md, S88_phase_recal_2026-09-04.md + its json. Spec-writer's own count: 23 distinct readouts (e.g. S58-T2-div-pooled 2444.2/3cl; S58-trialA 566.18/158; S87-allrows 831.5/227; S80-via-S87b 62.56/13; S116 103.1/63 and rerun 95.09/54; S137 table S102 2295.57/673, S103 2120.08, S115 3239.80; S88 179.76/127) -- treat as the floor, not the ceiling; codex's grep is authoritative.
CHANGE (step 2): additive only in gap_effective_n.py -- new field on effective_sample_size()'s return dict, `n_eff_bound_ok: bool` = (n_eff <= n_ticks) and (rho < 1.0 or math.isclose(n_eff, n_games)), computed from the SAME rho/deff already in the dict; no existing key renamed, no existing value changed. render() gains one column for it. Every existing caller (s88_phase_recal, s58_clamp_family_trial, s58_e2_slice_trial, tick_informative, stacker, s116/s137 requote paths) keeps reading n_eff unchanged -- grep and name each reader you touched.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = the 23+ enumerated n_eff readouts re-quoted with the audited function (n_eff unchanged in value) plus the new n_eff_bound_ok flag, over the 9-file enumerated set
  before        = current published values per the LIMIT list (n_eff_bound_ok does not exist yet)
  bar           = (i) CONSTRUCT proof that n_eff <= n_ticks holds on every enumerated readout -- true by construction (deff>=1); (ii) CONSTRUCT proof that n_eff <= n_clusters holds ONLY where rho==1 (none of the 23 do) -- report this honestly as the audit's finding, not as a per-row fail; (iii) every re-quoted n_eff value is byte-identical (1e-6) to its published value; (iv) verdict labels on every downstream row (S58/S80/S87/S116/S137/S88) stay unchanged -- this is a labeling/diagnostic audit, not a re-score
  n             = 23 (CONSTRUCT: the enumerated readouts) + 1 (CONSTRUCT: the rho-sweep {0, 0.3, 0.7, 1.0} algebraic identity check)
  eye check     = n/a (S-row); reproduction = re-run effective_sample_size on each artifact's archived per-unit series (csv/json cited in the file list) and diff n_eff against the published number
  must not move = every Q1-Q9 threshold and harness bar; every landed artifact byte-identical except the new column/field; backtest_fwer.jsonl (18 rows) untouched; n_eff's existing name and value in every reader
NON-TAUTOLOGY: covers exactly the 9 named files' numeric n_eff readouts; the ~48 OTHER files that grep "n_eff" only in table headers/prose with no scored number are NOT ENUMERATED -- list them, don't silently drop them. Do not re-score any S58/S80/S87/S116/S137/S88 model comparison.
EVIDENCE: docs/evidence/harness/S158_neff_audit_2026-09-04.md -- the algebraic proof, the full re-quote table (file:line | before n_eff | n_clusters | rho | n_eff_bound_ok | verdict unchanged?), and a NOT VERIFIED list. Calibration language only.
TEST: exactly one new per-file test (tests/platformkit/ingame/test_gap_effective_n_bound.py) asserting n_eff<=n_ticks always and the n_eff>=n_games identity at rho in {0,0.3,0.7,1.0} on a synthetic unequal-cluster-size fixture; run only that file.
POD: none needed (audit is pure re-computation on archived CSV/JSON); no scp of any module before ACCEPT.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: enumerate, prove, and test to completion this turn; never end on a partial table.
