GAP G412 | sport basketball | worktree a3 | log cx_g412_box_frame_contract
**RANK 1 - SEAL THE WRITER CONTRACT. Landed docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11/target_mask.csv SHA-256 c2c4f57a56330afbc9a7772b61743864aa04d746d9715e0374743e724a79dc71 and docs/evidence/tracking/g380_producer_provenance_2026-09-10/summary.json SHA-256 2155b6f861fd2a611e61488f6ce7bb42af7d231c1860bfde324bee7b7c47dba9 bind landed meanings. Pending docs/evidence/tracking/g410_position_box_frame_consistency_2026-09-12/contract_checks.csv SHA-256 81ca65ab015ad1f50cdc8158ae8bec5022dc10e0dc9bf2fd1f26491d1203d6bb supplies same-call observations, not accepted completeness.**
WHERE THIS ROW RUNS: PC step 0/retention/seal 25 min; code trace/proposal/CONSTRUCT 50; native demonstrations 60; reproduction/test/memo 35; <=170 min, CPU only from retained originals.
OWNS: scripts/platformkit/tracking/g412_*.py; tests/platformkit/test_g412_box_frame_contract.py; docs/evidence/tracking/g412_box_frame_contract_2026-09-12.md and matching directory; docs/research/organization-sprint/PROPOSED_g412_box_frame_contract.diff.
PREMISE step 0 (BINDING before-condition): rehash ALL G402/G406 draw/row/source inputs and archived crop/tracker/writer files; reproduce 60 distinct ticks, 206 stored rows, five silent ticks and the writer's absence of a frame receipt. G410 remains pending; independently establish code/trace agreement before using its result. Missing route binding -> PARTIAL; equivalent receipt already present -> FALSIFIED.
LIMIT: 51 historical comparator pairs occupy 28 frames, never n=51 independent frames. Require >=30 distinct native frames with stored boxes for geometric demonstration; include all 60 planned frames, empty or failed, in accounting.
METHOD sealed:
1. Freeze G406's existing 60 exact-even ticks, 30/kind, source digests and windows. Retain native originals and all archived rows; use historical raw boxes, not regenerated tracker outputs.
2. Trace native crop, detection integer conversion, clipping, PAD expansion, previous_bb updates and writer tuple permutation from archived bytes; record fresh detector, ankle, flow, prediction and retained-point scope separately. Survey every reader of bbox fields.
3. Write a PROPOSED additive writer diff adding box_frame, box_crop_origin_y_px and box_padding_px; for this bound route use topcut60_pad15/60/15, UNKNOWN with reason for an unbound route, empty receipt for absent boxes. Existing bbox/position values, ordering and interpretation remain byte-identical; the receipt specifies coordinate representation and nominal stored padding, not freshness or a guarantee that a predicted box is an observed detector box.
4. Publish transforms.json with the exact equations in A and clip order. Differentiate native padded display from native unpadded detector-form coordinates; never apply a pixel translation to a court-map point or assert box projection on a non-box branch.
5. CONSTRUCT n=32 exhaustive cases within this declared matrix: two native sizes x four locations (interior, top crop boundary, right boundary, bottom boundary) x two box states (fresh integer, fractional prediction) x two empty/nonempty statuses. Known geometry separately checks tuple order, inward padding, clipping, native origin and correct missing-value handling.
6. Exercise the proposed writer expression in an isolated fixture extracted from the diff, without applying it to src/; compare all legacy serialized fields and parse-added receipts. Independently calculate expected coordinates; a copy of the helper's formula is not the test oracle.
7. Render ALL 60 native frames with raw cropped box, native padded box and native unpadded box distinctly labelled; verify crop-array dimensions/hash binding. On all 55 nonsilent frames show the code-implied +60 centre translation before any clipping; log clipping effects separately.
8. Re-derive G406's OLD associations unchanged from docs/evidence/tracking/g406_masked_target_pixel_audit_2026-09-11/associations.csv: signed centre dy increases exactly 60 before clipping; pooled median -58.2 becomes +1.8 subject to original precision. This is descriptive 51-pair/28-frame arithmetic; residual localisation remains G413's task.
9. If G409 has completed, record agreement/conflict against its exact archived route. A contradictory executed stage prevents universal contract attribution; no waiting and no fitted constant. Produce complete reader/diff/construct evidence with an explicit pending-dependency limit.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Coordinate accounting | 60 ticks, 206 boxes, five silent ticks; no receipt | All keys/failures retained; code-derived transform on >=30 distinct native frames; zero unexplained origin/tuple errors | 60 planned, 55 nonsilent; actual supported n named | All 60 even native cards | G402 masks/windows and every raw box/court point | Complete DONE PROPOSED ONLY; missing binding PARTIAL |
| Writer/geometry compatibility | Cropped xyxy exported without origin | 32/32 enumerated controls; every legacy field byte-identical; all bbox readers inventoried | 32 CONSTRUCT plus all 206 real rows | Every construct plus native cards | Field semantics, branch labels, thresholds, production | All pass DONE PROPOSED ONLY; incompatibility REJECT |
| Comparator re-derivation | Historical median dy -58.2, 51 pairs/28 frames | Exact +60 algebra before clipping, all clipping deltas explicit; no fitted translation or frame-count inflation | All 60 planned frames; 51 pairs descriptive only | Same 60 cards | Old associations, comparator output, original verdict | Arithmetic complete DONE diagnostic; geometry quality NOT VERIFIED |
EVIDENCE: docs/evidence/tracking/g412_box_frame_contract_2026-09-12.md plus directory: prereg.md, input_hashes.csv, source_receipts.csv, route_chain.json, reader_manifest.csv, transforms.json, construct_cases.csv, writer_compatibility.csv, per_frame.csv, paired_residuals.csv, eye_index.csv, renders/, summary.json, repeats.json, runtime_receipts/, q6_scan.json, SHA256SUMS; retain the named PROPOSED diff and its digest. NOT VERIFIED: live integration, physical court registration, branch freshness and training suitability.
TEST: python -m pytest tests/platformkit/test_g412_box_frame_contract.py -q; 32 controls, sign/tuple/clip-order errors, legacy-column parity, missing boxes, bound source frames, repeat shape and code-built scanner fixtures.
VERSION 2026-09-12 v1 PROPOSED; all common rails bind; pending G410 is explicitly scoped and independently checked, no G409 completion dependency.

COMMON BINDING CONTRACT (astra round 11, inherited by this block):
COMMON CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6 bind every block. Orchestrator verifies the WHOLE-set premise, accepted prerequisites and free ownership before dispatch; supplied lane status is not a reservation.
Execution: codex terra PREPARE seals prereg alone, then Claude finisher completes the bounded measurement, test and memo within 180 min. Seal SHA-256 of every LF byte above the seal line, no intervening blank line; changed input hashes need a prospective handoff, never silent substitution.
Ownership: only each block's helper prefix, one test, evidence memo/directory and named PROPOSED diff; finishers also append their required RESULTS_LEDGER.md line in the same targeted commit. No register edit. They are not alone in the repo and must preserve peer edits; this reviewer writes none of those files.
Retention: before ANY pod stage, independently retain/read back/hash every selected original OFF-POD; record full paths, bytes, dimensions, rational PTS, all failures and existing receiver mappings. These three rows run on PC from retained bytes; no pod stage is needed or licensed by these specs.
Identity: hash every exercised model, route and configuration BEFORE launch; use an explicit empty model set when inference is absent. No new model invocation here. Source/source-frame/window joins are mandatory; branch ancestry never transfers across nondeterministic replays.
Bound all derived tables to sealed frame/PTS or census-time windows; list excluded history and archive raw overruns separately. Keep full parent columns and field meanings, ABSENT/UNKNOWN/silence and failures. Repeat receipts retain identical/runs, each run's stdout/returncode/command and per-table digests, with new fields additive.
No src/, kernel/, api/, intel/, scripts/team_system/, scripts/run_clip.py or shared-module edits; gated changes exist only as named PROPOSED diffs, applied nowhere. No training, held-out threshold sweeps, daemon restart, flag changes, registry writes or source pruning. Helpers <=300 LOC; full-package test imports.
Field-aware Q6 scans cover EVERY delivered code/text/log/table/claim, ledger addition, proposal and scan helper with a complete path manifest. Build ALL patterns and positive fixtures from character codes; emit pattern indices/counts only. Exempt exact opaque identifiers and typed numerical data only, not whole files; preserve numeric claim-field detection parity.
Write ASCII/LF; deliver declared byte-domain SHA256SUMS, all reproduction inputs, receipts and NOT VERIFIED. Run only each named test. Commit COMPLETE explicit paths including helpers/import dependencies, fixtures, raw observations, renders, scans and receipts; no bulk adds or push.
Two fresh processes reproduce delivered tables/cards from saved bytes; that does not establish inference repeatability. Never stop to wait: use bounded owned work, finish or close PARTIAL/NOT VALIDATED at deadline; false completed premise -> FALSIFIED; no repeated discovery loop or automatic extra attempt.

ORCHESTRATOR FOOTER (binding, 2026-09-12): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage; hash EVERY exercised model, route file and config BEFORE any launch); the PC pool receiver (C:/Users/neelj/pool_receiver/: objects/, receipts.jsonl, state.json; one section per new video id = the earliest-tracked section) is the accumulated off-pod source pool; finishers poll their OWN background jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed window and retain overruns separately; a failed planned unit stays an UNKNOWN row; inherited bars byte-identical; never change an existing field's semantics -- add a new field with an alias; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY touched text path incl. the scanner's own source and .log.txt receipts (patterns and restricted constants built from character codes; receipts emit pattern indices, never token text, with a complete path manifest); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round11_2026-09-12.md (local-only). COORDINATE CONTRACT (from G410, lane a13 tip 92f5894fc, verify pending; astra's sign correction applies: for a stored xyxy box B the native padded box is (Bx1, By1+60, Bx2, By2+60) and the native unpadded detector-form box is (Bx1+15, By1+75, Bx2-15, By2+45), clipped using the cropped dimensions BEFORE restoring the origin; fresh box-based court writes project ((x1c+x2c)//2, y2c, 1) through M then M1; ankle / flow / prediction / retained-point branches are exceptions). G402, G406 and G407 are landed on master; G409 (lane a12) and G411 (lane a19) are in verification.
