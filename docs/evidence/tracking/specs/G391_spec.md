GAP G391 | sport basketball | worktree a10 | log cx_g391_ball_false_call_audit
**RANK 1 - BLIND FALSE-CALL AUDIT AND SUPPRESSION LIMIT. Landed docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/dev_boxes_v3.csv SHA-256 e151f932c3b4bffe84c89aa8fde18f08c346a1e3a6e65d27ed4b36f095b7b4fa supplies 530 boxes; docs/evidence/tracking/g389_ball_reference_completion_2026-09-11/reference_v3.csv SHA-256 ad00670c3601706d5d817de734fc85d82e8fdc379ea03b21bbe1a46e272ad09e fixes labels. Pending L390/predictions_A8.csv SHA-256 58a315bdb63020dce401517fe048a072e6a142e08d18eb54832f3068c1bee1b6 and paired_frame_scores.csv SHA-256 afc203e7346e94af875fb101049b94b98e5905e922f52b31af9b15cffbbede0b fix the tested output.**
WHERE THIS ROW RUNS: PC only, step 0/PREPARE 25 min, blind rating 105, adjudication/reproduction/receipts 50; no GPU, fresh inference or pod wait.
OWNS: scripts/platformkit/tracking/g391_*.py; tests/platformkit/test_g391_ball_false_call_audit.py; docs/evidence/tracking/g391_ball_false_call_audit_2026-09-11.md and matching directory only.
PREMISE step 0 (BINDING before-condition): rehash all cited files; full join must reproduce 549 keys, 259 calls, 90 TP and FP split 100/50/19. Require read-only a11 handoff and its verifier disposition; disputed provenance -> diagnostic-only NOT VALIDATED, never silent promotion.
Census native sheets for all 259 called keys and causal prediction availability for ALL 549; missing pixels remain missing, never replacements. Complete prior blind characterization -> FALSIFIED with receipt; no prior audit is evidenced by the present package.
METHOD (sealed before labels; fixed sample and categories):
1. Freeze all 259 called keys, retaining all 549 states for scoring. Sort by game/section/frame_index/key, partition into 30 equal-quantile bins and round-robin across bins; opaque packet ids hide reference status, confidence, split and scorer outcome.
2. Two independent blind raters inspect full native context then a fixed candidate-centred crop, with a neutral candidate marker; neither sees reference annotations, score tables, other answers or the 169/90 assignment.
3. Label the candidate object BALL / PERSON_OR_APPAREL / CROWD / SIGNAGE_OR_GRAPHIC / EQUIPMENT / COURT_OR_LOGO / OTHER / UNKNOWN; separately locate any visible ball with native centre/diameter and uncertainty. No inferred occluded ball.
4. Seal raw judgments before revealing reference or scoring. Claude resolves disagreements from pixels; archive both originals. BALL can still be a centre-rule miss, a second ball or a disputed reference; record these separately, including unresolved cases.
5. Report the complete 169-FP category census, separate VISIBLE/ABSENT/UNKNOWN numerators, 90-TP control agreement and all 259 review states. Keep original G389 reference immutable; suspected errors are an audit output, never grounds to erase FPs.
6. Recompute G390 counts from raw boxes using native-to-720p centre matching, rank-0 OBSERVED only, no-detection frames retained; independently reproduce score rows twice. Prove the suppression-only ceiling TP<=90, FP>=0 over all 549.
7. SHADOW feasibility census: require same-checkpoint OBSERVED predictions with exact section/PTS identity at causal offsets within 0.2 s for every target, plus disjoint DEV context outputs. Sparse target snapshots are not neighbors; missing history is not evidence against a call.
8. Present archive fails that before-condition: record temporal suppression NOT MEASURED and its missing-key census; no candidate inference or manufactured history in this row. This does not prevent accepting a complete blind audit.
9. Conditional rule, sealed before audit labels: use the two immediately preceding decoded frames, both within 0.2 s; retain current call only if both have OBSERVED calls whose speed to it is <= nearest-rank p95 native-to-720p speed from >=30 disjoint DEV reference pairs. Missing archive passes raw through; confirmed no-detection suppresses. Insufficient DEV pairs -> step 8. No sweep, interpolation or future frames.
10. For that conditional SHADOW only, score unchanged raw versus suppressed outputs on ALL 549, report retained TP/90, removed FP/169 and unchanged three quality bars. Benchmark reuse only; mathematical coverage failure stands whatever the result.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Blind object/location accounting | 169 FP, 90 TP; object taxonomy unmeasured | 259/259 independently reviewed twice and reconciled or explicitly UNKNOWN; every FP accounted for | 259 distinct frames, full 169 FP census | 30 even FP cards plus 30 even all-call cards, duplicates named | Original labels, 0.05 confidence, categories and packet order | Complete audit DONE; missing review PARTIAL |
| Raw reproduction and ceiling | 90/169/212; C0 0.164 | Exact counts on all keys in two fresh processes; suppression cannot add TP | 549 unique held-out keys | Same even 30 all-call cards plus failure census | C0>=0.25, precision lower>=0.90, ALL FP/188<=0.01 | Reproduction mismatch NOT VALIDATED; suppression quality CLOSED AT LIMIT |
| Causal SHADOW feasibility | No dense A8 history in package; target gaps >=244 frames | Complete immutable causal+DEV archive and prospective rule seal required before SHADOW | Census all 549 targets; no subset quality claim | 30 even timeline cards, MISSING shown | No new inference/token, no future context, missing history passes raw call through | Missing supply NOT MEASURED; conditional complete diagnostic only, no adoption |
EVIDENCE: docs/evidence/tracking/g391_ball_false_call_audit_2026-09-11.md plus directory: prereg, native_manifest.csv, packets.csv, blind_ratings.csv, adjudication.csv, object_counts.csv, reference_disputes.csv, raw_reproduction.csv, context_census.csv, suppression_limit.json, optional shadow_rule/paired_outputs, summary.json, renders/ and common receipts.
TEST: python -m pytest tests/platformkit/test_g391_ball_false_call_audit.py -q; false call on VISIBLE also incurs FN, UNKNOWN FP retained, packet labels hidden, absent history passes through, suppression cannot create TP, all 549 keys survive.
VERSION 2026-09-11 v1 PROPOSED; diagnostic successor, not A10 allocation or a fresh independent benchmark. Conditional SHADOW requires the complete archive and DEV cutoff seal before launch; current supply does not meet that condition.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round4_2026-09-11.md (local-only). NOTE (astra A.2): G390's 169/188 = ALL false calls / N_absent; the ABSENT-only false-call rate is 50/188 = 0.266 (FP partition VISIBLE 100 / ABSENT 50 / UNKNOWN 19).
