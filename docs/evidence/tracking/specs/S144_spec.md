GAP S144 | sport nba (in-game) | worktree a13 | log cx_s144_nba_pairwise
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md. Calibration language only: no dollar, ROI, profit or edge words. Never touch data/registry, src/, kernel/, api/, intel/, scripts/team_system/. Per-file tests only (python -m pytest <one file> -q); NEVER the full suite. data/ in this worktree is a read-only junction to the main repo's data/ (no data/cache/eval_gate junction -- the S86 CSV was COPIED into this worktree's data/cache/eval_gate/) -- never write under data/. Scratch outputs go under C:/Users/neelj/AppData/Local/Temp/cx_s144/. NEVER PARK: run everything to completion this turn; never end waiting. COMMIT: explicit pathspec (git add <paths> && git commit -m "..." -- <paths>), in this worktree, no push. Last line of your report: SHA: <sha>.
GAP (verbatim from the register): S102's grammar contains two hand-picked combined-state bases (margin_x_rem, margin_over_sqrt_rem) but no SYSTEMATIC pairwise enumeration: 16 bases -> 120 base pairs x {product, safe ratio} x 6 conditionings are unenumerated. Enumerate the pairwise grid explicitly EXCLUDING the two already-frozen forms, freeze it as a new family, and screen it on the S86 SCREEN side through the gated NBA tier.
READ: scripts/platformkit/foundry/ingame_grammar_nba.py (BASE incl. margin_x_rem / margin_over_sqrt_rem at line ~65 and their builders ~156; transforms; conditionings; semantic_hash; enumerate_hypotheses; the 3 NOT_ENUMERATED transforms rank_in_league / z_vs_league / ratio_to_opponent), foundry/ingame_screen_nba.py (load_screen, sweep, gate_features, incumbent options), foundry/ingame_screen.py + ingame_guards.py (assert_tick_asof, assert_label_blind, walk_forward_feature, BAR), eval_gate/family_bars.py (how S89/S102 froze a family with kind: and moved the pin; scripts/platformkit/eval_gate/test_family_bars.py count test), docs/evidence/harness/S102_nba_pod_sweep_2026-09-03.md and S138_nba_grammar_ext_2026-09-03.md (the falsified premise), eval_gate/s94_nba_early_shrinkage.py (_recal null).
PREMISE (step 0): print the 16 bases, confirm exactly two combined forms exist (name them), and that no systematic pair enumeration exists. If a pairwise enumeration already exists, STOP and report FALSIFIED. Confirm the S86 CSV is present (else STOP: NO STORE).
LIMIT (step 1): n/a.
CHANGE (step 2): NEW scripts/platformkit/foundry/ingame_grammar_nba_pairs.py (<= 300 LOC): for every unordered pair of the 14 non-combined bases (91 pairs; exclude margin_x_rem and margin_over_sqrt_rem as bases so the two frozen forms are not re-enumerated), product and safe ratio (denominator floored at 1e-3 in standardised units), x the 6 conditionings, deduped by semantic_hash and asserted DISJOINT from the 576; expect ~1,000 hypotheses (report the exact count); each builder tick-time as-of (the truncation + label-blind guards must pass at 8 probes); freeze as family ingame_nba_pairs (kind: tickgrid) in family_bars.py moving the pin ONCE (record old/new pin; update the count test); no bar moved. Screen locally through ingame_screen_nba.sweep with incumbent=market and the S94 recal null on identical rows (charges off, scratch sqlite under C:/Users/neelj/AppData/Local/Temp/cx_s144/), SCREEN side; within-family BH over the new family; report the count clearing +0.004 (expect 0), best 10 with game-clustered CIs, n / n_informative / n_eff, throughput. If the full grid would take > 90 minutes locally, screen a seeded random third and say so (the family stays frozen whole).
TEST: NEW tests/platformkit/foundry/test_ingame_grammar_nba_pairs.py: count, dedupe, disjointness from the 576, exclusion of the two frozen forms, tick-time invariance on 8 probes, label-blindness, the family freezes and the pin moves once. Run ONLY that file plus scripts/platformkit/eval_gate/test_family_bars.py and tests/platformkit/foundry/test_ingame_screen_nba.py.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = pairwise hypotheses screened on the SCREEN side vs the raw market with the recal null; denominator = the family count
  before        = 0 screened
  bar           = >= +0.004 vs the raw market with a game-clustered CI excluding zero AND beating the null -> prereg DRAFT (unsealed); else honest NULL (a NULL is a PASS of the process)
  n             = the family count (report it)
  eye check     = n/a; reproduction = verifier recomputes the top 10 from the archived series under the scratch path
  must not move = BAR 0.004, the S86 partition, purge/embargo, the ledger (never open it), data/registry/**
NON-TAUTOLOGY: the null is fit on the identical rows; a zero-coefficient pair equals the raw market to 1e-12.
EVIDENCE: docs/evidence/harness/S144_nba_pairwise_2026-09-03.md (grammar spec, pin old/new, screen table, verdict, NOT VERIFIED); archive series under the scratch path (never under data/).
POD: none. Do not ssh anywhere.
