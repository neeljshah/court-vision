GAP S180 | sport mlb | worktree aXX | log cx_s180_mlb_gate_current_era
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q9, S-row) before you report.
PREMISE (step 0): re-read data/cache/combo/gate_corpus_mlb.parquet and recompute df.groupby('corpus_unit')[c].notna().sum(). Measured 2026-09-03 on master HEAD: 39,162 rows, 9 columns, era_2010_2021 27,983 / era_2022_2026 11,179; sp_first6_diff_ew 24,521 / 0; sp_ra_diff_asof 26,559 / 0; park_factor 0 / 0 (0/39,162 globally); y, p_base, p_home_elo 27,983 / 11,179. Root cause on disk: scripts/platformkit/combo/corpus_cache_sources.py:43-44 pins asof_park.parquet + asof_features.parquet and the per-era loop at :50-56 left-joins those two frames to BOTH eras; asof_park.parquet is a 29-row stub, park_factor 0/29 non-null, 0 of its ids in the 39,162-id union. If any number differs, STOP, write the memo, commit, report FALSIFIED -- a valid result that earns its own register row.
LIMIT (step 1): measure the ceiling of the unused _current siblings against the 11,179 era_2022_2026 ids BEFORE changing anything. Measured 2026-09-03: asof_park_current.parquet 10,826 rows -> park_factor non-null on 10,517/11,179 = 94.08 pct; asof_features_current.parquet 10,458 rows -> sp_ra_diff_asof non-null on 9,765/11,179 = 87.35 pct. Also measure whether build_sp_form_features() serves ANY era_2022_2026 id; if it cannot, sp_first6_diff_ew is reported CLOSED AT LIMIT for that era and is NOT part of the bar. If either measured ceiling falls below its bar, STOP and report CLOSED AT LIMIT. Do not fix.
CHANGE (step 2): the smallest additive change in _build_mlb -- load the _current siblings when present and use them for the era_2022_2026 leg ONLY (combine_first over the legacy join so a legacy value is never overwritten); append both new paths to `sources`. Same 9 output columns, same order, same row count. Additive only: no column, field or status value renamed or removed; if unavoidable keep the old name as an alias in the same commit and name every reader you checked.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = non-null count per corpus_unit of each mlb gate-corpus feature column in data/cache/combo/gate_corpus_mlb.parquet; denominators era_2010_2021 = 27,983, era_2022_2026 = 11,179, total 39,162
  before        = park_factor 0/27,983 and 0/11,179; sp_ra_diff_asof 26,559/27,983 and 0/11,179; sp_first6_diff_ew 24,521/27,983 and 0/11,179 (measured 2026-09-03, n = 39,162)
  bar           = era_2022_2026 park_factor >= 10,517/11,179 AND sp_ra_diff_asof >= 9,765/11,179; sp_first6_diff_ew measured and reported, CLOSED AT LIMIT if unservable
  n             = 39,162 rows (full census, not sampled)
  eye check     = n/a (S-row); reproduction = the verifier rebuilds the corpus, re-reads gate_corpus_mlb.parquet and recomputes df.groupby('corpus_unit')[col].notna().sum() for the five feature columns
  must not move = the 9-column schema of gate_corpus_mlb.parquet (0 columns added or renamed); row count 39,162; y and p_base non-null 39,162/39,162; every era_2010_2021 value of every column DataFrame.equals-identical to the pre-change build (archive that slice first); mlb_gate in docs/evidence/harness/FWER_FAMILIES_SPEC_2026-09-03.md UNCHANGED at 5 features / 45 hypotheses; data/cache/eval_gate/backtest_fwer.jsonl byte-identical (18 rows)
NON-TAUTOLOGY: the metric covers all 39,162 rows of both corpus_units, no row excluded; era_2010_2021 is held fixed as a control so a gain cannot come from dropping rows. The three gate-corpus feature columns contribute 0 screenable rows on the 910-row close window today, which leaves 27 of mlb_gate's 45 hypotheses unscoreable there and park_factor (9) unscoreable everywhere; do NOT claim the whole mlb close screen surface is empty -- S113 ran 48 mlb screens from the asof_supply families. Coverage is not a result: no scored screen and no verdict change in this row.
EVIDENCE: docs/evidence/harness/mlb_gate_current_era_2026-09-04.md -- before/after non-null table per corpus_unit, the two ceiling measurements, the era_2010_2021 equality check, n, denominators, and a NOT VERIFIED list.
REQUIRED EVIDENCE DURABILITY: copy the before/after summary JSON under docs/evidence/ so the verifier can reproduce every number from the memo alone.
RE-EMITTED TABLES: preserve the FULL 9-column set, not only the columns this lane touches.
TEST: exactly one new per-file test; run only that file.
POD: not needed, local build only. No scp of any module.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
