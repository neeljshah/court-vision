GAP G388 | sport basketball | worktree a12 | log cx_g388_paint_band_protocol
**RANK 1 - REPLACE EXTENT AGREEMENT WITH AUDITED BAND MEMBERSHIP. Landed docs/evidence/tracking/g382_court_stroke_specificity_2026-09-10/summary.json SHA-256 79975c5917af6a175be97032035e6a7b8cdeb05fcdbde3060db9053b016a7ee0 and docs/evidence/tracking/g382_court_stroke_specificity_2026-09-10/marking_only.json SHA-256 9bc53aa82046834770436c51d7f0823f34a747b21fac288b86a0f3072e9602a2 are memo-cited and byte-checked. Unlanded docs/evidence/tracking/g387_paint_localization_controls_2026-09-11/visibility.csv SHA-256 4c3040d34902877c2f375dfcc044ed61e5dca11acd3828b278d1f1fac7f2766e is also memo-cited and byte-checked; its 19/30 controls do not qualify the endpoint protocol.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 30 min, control ratings 45, real ratings/adjudication 80, scoring/receipts 25; no GPU/pod required. Start only after exclusive a21 handoff from G387; retain its unlanded contents.
PREMISE step 0 (BINDING before-condition): reproduce all 60 original states, 49 native hash/dimension matches, zero marking-only strokes, 19/30 controls, 1/30 real localization and 22/6/2 visibility from the complete tables; read the actual G387 verifier disposition before using its claims.
Author verified the native universe and recomputed the control count; finisher must repeat them. If new independently qualified reference already exists, report FALSIFIED; identity failure or fewer than 30 native contexts -> NOT VALIDATED, no substitute draw.
METHOD (sealed; new protocol, never a G387 threshold amendment):
1. Retain G387's 30 real contexts: sort all 49 by video_id/section_id/numeric frame_order and take floor(j*48/29+0.5), j=0..29. Hash the literal selection.csv bytes; retain all 11 original decode failures outside this explicitly conditional native study.
2. Keep its six 640x540 native tiles and integer offsets. A visible fragment gets three independently observed native points, with first-to-second separation >=60 px and third point interior; no arithmetic midpoint. Raters see pixels only, no historical traces or extractor output.
3. Create 30 fresh opaque controls on those backgrounds, seed 388, with the frozen G387 6 px white/10 px black band, length 240, six tile centres and 0/30/60 degree cycle; archive generated pixels and truth before rating. Annotate an arbitrary visible fragment, not the longest segment.
4. EACH rater must put all three control points within 3 native px perpendicular to the known centreline, with projections within the finite band and >=60 px span; control success requires BOTH raters. Seal all answers without feedback; >=27/30 is required BEFORE any new real rating/scoring.
5. A failed control bar ends this protocol attempt PARTIAL, PROTOCOL NOT QUALIFIED. Preserve every missing response as failure; permit no post-score instruction change, retry or tolerance widening. Test image access/output-path permissions before the sealed packets are issued.
6. After controls pass, two isolated raters independently mark up to two real fragments per context in fixed tile order. Use family SIDELINdocs/evidence/tracking/BASELINdocs/evidence/tracking/CENTRdocs/evidence/tracking/LANdocs/evidence/tracking/FREE_THROW/THREE_POINT_STRAIGHT/UNKNOWN without left/right orientation; retain ABSENT/UNKNOWN and unmatched fragments.
7. Pair fragments once by tile, family, then smallest symmetric line distance, stable ties. Require each third point within 3 px of its own first-two-point line and all three points of each fragment within 6 px of the other infinite line; never require endpoint coincidence or infer an occluded continuation.
8. Claude first records paint visibility without traces, then audits all candidate pairs against native pixels: SAME physical band and >=8/9 evenly spaced points per reported fragment within 3 px of visible paint. Collinear different bands, logos and UNKNOWN fail; retain pair-specific audit rows, not frame-level reusable audit counts.
9. Report frames with >=1 audited pair/all 30, family disagreements/all 30, ABSENT/UNKNOWN/all 30 and descriptive visible-conditioned recovery. Only after new labels are sealed, rescore both old trace generations under this rule as retrospective diagnosis; never claim independent confirmation or regenerate missing old annotations.
10. Inventory audited bands and unresolved geometry over the full sample; no extractor/filter/fitter/H run. New controls qualify this measurement only; each real reference remains individually audited, and G383 stays closed on its old supply.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Known-band point localization | Old endpoint protocol 19/30; NEW point metric unmeasured | >=27/30, both raters, every point <=3 px; no real scoring first | 30 unique control contexts, separate from real n | All 30 control overlays | Sealed points/span/tolerance; historical G387 bars | Below bar PARTIAL, protocol not qualified |
| Audited band/family recovery | Old real rule 1/30; 4/12 shared-ID cases same-band, descriptive | Account for 30/30 and audit every proposed pair; no minimum recovery invented for an honest diagnostic | 30 unique real frames; smaller subgroups descriptive | All 30 native contexts plus both traces and pair-specific audit | 6 px collinearity, 3 px paint distance, 8/9 rule, 60 px span | DONE for complete diagnostic only if controls pass; otherwise PARTIAL |
| Identity and repeatability | 49 matched natives, 11 original failures | Full byte receipts; independent recomputation identical; all planned states retained | All 60 states; same 30 control/real contexts | Even order including absence cards | G362/G365/G371/G374, orientation UNKNOWN, producer/weights | Any missing receipt or mismatch NOT VALIDATED; no registration claim |
EVIDENCE: docs/evidence/tracking/g388_paint_band_protocol_2026-09-11.md and matching directory: prereg, input/selection manifests, controls/truth/pixels, click receipts, raw ratings, family/pair/audit tables, historical rescoring labelled retrospective, per_frame.csv, full repeat hashes, summary.json, renders/ and common receipts.
TEST: python -m pytest tests/platformkit/test_g388_paint_band_protocol.py -q; test extent invariance for the same band, parallel-band false match, line/native transforms, control failure blocks real scoring, independent third-point handling, pair-specific audit isolation and scanner positive/negative controls.
VERSION 2026-09-11 v1 proposed; one attempt plus at most one named LIMIT measurement, no repeated protocol search disguised as this row.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` over every byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/` READ only (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round3_2026-09-11.md (local-only).
