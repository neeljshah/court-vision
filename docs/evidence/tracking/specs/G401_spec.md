GAP G401 | sport basketball | worktree a11 | log cx_g401_fps_cap_duration_shadow
**RANK 2 - DECIDE DURATION POLICY WITHOUT A FULL PRODUCER REPLAY. Landed docs/evidence/tracking/g386_pin_copy_one_pass_2026-09-10/summary.json SHA-256 e0e6119766899786a4004cd34e7587bacc615853cc87674c85ce4d06d297803f supplies the retention method. Verification-pending G397 at 030313484 supplies docs/evidence/tracking/g397_mixed_source_producer_census_2026-09-11/summary.json SHA-256 b6c31fc1a1f2341c37165bcee83357ec20b9fe11f4a3d267a4cfb32de68ac251: 100/50 s cap spans and insufficient schedules, not a validated quality comparison. Require its independent handoff before scoring.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 25 min; pod a11 read-only current-policy census, retained-source CPU decode shadow 100; PC proposed diff/reader review 25, render/reproduction/test 30. No GPU, full tracker call, daemon edit or restart.
OWNS: scripts/platformkit/tracking/g401_*.py; tests/platformkit/test_g401_fps_cap_duration.py; docs/evidence/tracking/g401_fps_cap_duration_shadow_2026-09-11.md and matching directory; docs/research/organization-sprint/PROPOSED_g401_fps_cap.diff only for the daemon change.
PREMISE step 0 (BINDING before-condition): resolve current daemon cwd and /workspace/data/tracking/track_daemon_ledger.jsonl; hash build_command and its callers, verify active cap 3000 and capture exact feeder-selector bytes/activation UTC for 270 -> 312 -> 301 -> 311 -> 300 with probe bar 720.
Whole-set census must supply >=30 completed unique post-preference sections for the policy measurement AND >=30 retained native 59-61 fps sections for paired shadows, each >=100 s PTS span; the latter may predate the policy. Do not assert the old 22/60 retained objects satisfy either quota. Missing timing/source/cap evidence -> PARTIAL, no sufficiency claim.
METHOD (sealed; one policy metric, one fixed mechanics check):
1. Freeze a completed post-preference window ending at census UTC; enumerate EVERY terminal attempt and select the earliest terminal ORIGINAL attempt/section, keeping repeats/failures separately. Retain/probe all section sources needed for the window; missing originals remain UNKNOWN, never inferred from format identifiers.
2. Per section derive available span from decoded native PTS, and cap-limited span from the exact recorded start/cap and frame order. Define loss=max(0,min(100 s,available span)-cap-limited span); primary policy metric = count(loss>5 s due to cap)/all window sections. Archive attribution for EOF, malformed PTS and failed attempts separately.
3. Zero such losses with zero UNKNOWN sections supports keeping preference alone for THIS window. Any positive count supports recommending the fps-aware PROPOSED diff; UNKNOWN prevents a sufficiency verdict. Report by measured fps/height, but do not add another policy-selection metric or tune the 5 s bar.
4. Independently sort the retained 60 fps population by competition/game/section/digest; draw 30 evenly using floor(j*(N-1)/29+0.5), no replacement, with game counts and clustering explicit. Pin/retain before rotation; verify every source's raw bytes and full PTS schedule before either arm.
5. CPU-decode shadow A uses source-frame cap 3000; B uses ceil(100*validated_native_fps), preserving native order, start index and the existing stride algorithm. Reuse the same decoded PTS stream for the exact paired mechanics comparison; no image interpolation, source replacement or tracker-output claim.
6. Fixed candidate uses only independently cross-validated positive fps; invalid/variable-rate sources that fail the PTS check retain legacy 3000 with an explicit cap_basis=UNKNOWN receipt. Freeze unknown handling before measurement, not an optimistic 30 fps default. Keep source-length termination separate from requested cap.
7. Archive the last admitted/first excluded PTS and actual read-frame counts for both caps; require B's processed time extent to reach 100 s within one native frame interval on all 30 valid 60 fps sources. Report stride opportunities as DECLARED, never producer-EVALUATED ticks; actual emitted coverage remains NOT VERIFIED.
8. Prepare the smallest additive daemon build_command proposal and its caller plumbing from current hashes: original 30 fps behavior, validated-fps cap, explicit requested duration/frame cap/basis receipt fields, UNKNOWN fallback. Enumerate all readers of the existing cap/count fields and preserve aliases; shared token needed only for a later actual shared-module edit.
9. The proposal must preserve _VRAM_FLUSH_INTERVAL=3000, worker count, stride, models, thresholds and source metadata semantics. More than 3000 processed frames crosses the flush interval: full runtime cost, receipt retention and quality across that boundary remain NOT VERIFIED and must be reviewed in the user's deploy path.
10. Seal legacy and candidate argv fixtures; exercise 30 fps, 29.97, 59.94, 60, unknown, variable-rate and short-source cases without invoking the daemon. Render 30 evenly ordered source timelines with native frames around both cap endpoints; show loss and missing states explicitly.
11. Recompute policy tables and paired timelines twice from immutable PTS, preserving raw source-count metadata and actual shadow read counts separately. This measures duration mechanics, not fps-invariant tracking quality or production throughput; retain the proposal unused until the user applies the accepted version via their deploy path.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Post-preference cap loss | New preference impact unmeasured; old 60 fps cap about 50 s | Exact loss>5 s fraction with every window section accounted; zero UNKNOWN needed for zero-loss sufficiency | Entire sealed window, >=30 unique sections | 30 even native endpoint/timeline cards | Policy activation boundary; 100 s target; 5 s decision | Zero loss: preference sufficient in window; positive: recommend proposal; missing: PARTIAL |
| Paired 60 fps mechanics | Cap 3000 reaches about 50 s | Candidate reaches 100 s within one frame interval in 30/30; exact source order/read-count accounting | 30 retained 60 fps sections, two cap schedules each | All 30 paired timelines/endpoints | Native fps, start, stride; no threshold alternatives | Pass DONE mechanics; failure NOT VALIDATED, no deploy |
| Additive proposed cap path | Constant argv 3000; source-count field is not capped processing | Anchors exact; legacy callers/aliases retained; all sealed argv fixtures exact; proposal only | All fixture cases CONSTRUCT; 30 native sources above | Same 30, plus proposal review | Flush=3000, daemon bytes, models, flags, workers | Complete proposal DONE; runtime performance NOT VERIFIED |
EVIDENCE: docs/evidence/tracking/g401_fps_cap_duration_shadow_2026-09-11.md plus directory: prereg.md, policy_receipt.json, window_ledger.jsonl, census.csv, policy_per_section.csv, draw.csv, source_receipts.csv, pts.csv, paired_caps.csv, argv_fixtures.json, reader_survey.csv, proposal_receipt.json, eye_index.csv, summary.json, repeats.json, renders/, common_receipts/, SHA256SUMS; exact PROPOSED diff path above.
TEST: python -m pytest tests/platformkit/test_g401_fps_cap_duration.py -q; rational fps/rounding, short-source EOF, irregular PTS, unknown fallback, first excluded frame, source-count versus capped count, zero-loss UNKNOWN refusal and additive argv/receipt behavior.
VERSION 2026-09-11 v1 PROPOSED; duration diagnosis and reviewable diff only. No autonomous deploy, no new live phase, no claim that a longer cap improves target quality.

COMMON BINDING CONTRACT (astra round 7, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B including A7/A9/A11/B11/H1/S1/S2/S4/S5, plus Q1/Q3/Q6; each block inherits these rails. Each is codex terra PREPARE plus Claude finisher MEASURE, then independent verification; <=180 minutes including preparation and receipts, no unattended leftovers.
Orchestrator confirms the named free owner and performs the binding whole-set premise census BEFORE dispatch. These read-only proposals do not assert fresh source availability. Missing supply -> PARTIAL with exact inventory; an already completed equivalent -> FALSIFIED. Never replace a failed draw or silently extend a budget.
Each OWNS only its named new helper prefix, one test, memo/evidence directory and named PROPOSED diff; no shared-module, src/, daemon, registry, flag or deploy-tree edits. Future finishing uses explicit pathspecs in its assigned worktree and contract C; this review changes no register or git state.
PREPARE seals protocol/bars alone before any metric, then exact input, source, code, model, environment and configuration identities before dependent work. Keep raw answers, full inherited schemas, all ABSENT/UNKNOWN/failed/planned states, exclusions and NOT VERIFIED; missing is not zero.
Pod work only through ~/bin/pod_run <owner> --ship <owned paths> --fetch <owned outputs> -- <command> in /workspace/wt/<owner>/; source/deploy paths read-only. Gate actual VRAM/CPU load, threads 1, <=6 cores; bulky writes require a successful pod write probe, scratch <2 GB; quota usage UNKNOWN is never zero. Never restart or interrupt a peer.
PC raters share the <=3-active-codex/free-RAM>=2.8-GB gate with G399; two raters at most, queued sequentially if capacity is occupied. No nested rating fleet. Poll own work to completion or terminate it normally at the sealed deadline and report exact unfinished keys.
Durable evidence includes summary.json, per-unit tables, seals, full paths/bytes/native dimensions/fps/PTS/raw digests, runtime/log receipts, even render index, resource costs and receiver readback. Keep replay objects off-pod until all declared readers finish; G386 does not prove same-size in-place mutation detection.
Two fresh processes must reproduce saved-table arithmetic and all delivered render digests from committed code, including matching the delivered tree; this proves arithmetic/rendering only. Single producer runs and ratings remain descriptive. Scan ALL landable text including .log.txt with character-code-built Q6 patterns; print counts only; helpers <=300 LOC, full-package imports, one per-file test each.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy); **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; counts only); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round7_2026-09-11.md (local-only). G397's evidence lands on master after its verify; G399's after its finisher + verify; a premise step that needs either reads the lane tip named by the orchestrator until then.
