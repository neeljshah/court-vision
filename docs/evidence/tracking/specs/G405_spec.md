GAP G405 | sport basketball | worktree a14 | log cx_g405_discovery_format_admission
**RANK 3 - MEASURE RENDITIONS AT DISCOVERY. Landed docs/evidence/tracking/g401_fps_cap_duration_shadow_2026-09-11/summary.json SHA-256 d996ecbfb544b5bc3ce5e462efb42df06aa1a10fdca1299e2bb508fa77276d10 records 16/34 cap losses and failed 29/30 mechanics. Landed docs/evidence/tracking/g401_fps_cap_duration_shadow_2026-09-11/PROPOSED_g401_fps_cap.diff SHA-256 dca612ca4a739a8f5ada20e90ed2e89c7555e5bc8db54e1b1c7e578cf5d4cce0 is reviewable, unused and not a validated duration fix.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 20 min; pod a14 discovery/format probes 75; PC parsing, queue sidecar, reproduction/test/memo 35; <=130 min. No video download/decode, GPU, live queue mutation or daemon work.
OWNS: scripts/platformkit/tracking/g405_*.py; tests/platformkit/test_g405_discovery_formats.py; docs/evidence/tracking/g405_discovery_format_admission_2026-09-11.md and matching directory. The discovery_queue.jsonl is an owned SHADOW feeder queue, not an installed feeder integration.
PREMISE step 0 (BINDING before-condition): read/hash the current feeder discovery recipe, selector, probe eligibility and a complete bounded discovery/queue snapshot on the pod. Identify exact queue paths and field names before counting; if compatible-30fps evidence already exists at discovery for >=30 current unique sources, STOP FALSIFIED and cite it.
Require access to the same discovery queries/configuration and >=30 distinct basketball-family source ids in one bounded discovery pass; raw completed-section records cannot stand in for discovered sources. Missing queries/auth/format access or <30 sources -> PARTIAL, preserve inventory, no waiting for the live queue to fill.
METHOD (sealed before discovery/probing):
1. Freeze the existing competition query list and discovery settings; run a discovery-only pass through the same public search recipe in pod scratch, max 15 minutes, with no download or enqueue to the live feeder. Archive the entire returned source-id population, original order, query/competition and discovery UTC; deduplicate by source id, retaining all query memberships.
2. Sort unique source ids by competition/query/source id and draw 30 evenly using floor(j*(N-1)/29+0.5), j=0..29. Freeze exact ids before probing, no rating/content-based selection, no replacement for a missing format inventory; this samples one discovery population, not all future traffic.
3. For EACH selected source run yt-dlp -F on the POD immediately in this discovery pass, with --no-playlist and the frozen access settings; capture exact argv, executable/version, source id, start/end UTC, stdout/stderr/exit and timeout. Set 120 s/source and one attempt; never expose access material in receipts.
4. Also capture machine-readable format metadata using a read-only, no-download metadata request with the same settings, <=60 s/source. This validates protocol/codec/height/fps absent from human listings; retain both inventories, timing and disagreement. No available format may be inferred from its numeric id alone.
5. Compatible 30 fps means actual listed video format with HLS protocol, h264 codec, height 720..1080 inclusive and fps 29..31 inclusive. Record separate 720p30/1080p30/720p60/1080p60 availability; freeze these definitions from current admission intent, not from observed outcomes. A non-HLS 30 fps format alone does not qualify.
6. has_compatible_30fps=YES when both inventories bind a qualifying format; NO only when complete successful inventories exclude one; incomplete/ambiguous/disagreeing evidence is UNKNOWN. Preserve every format and reason, including no-video, missing fps, protocol mismatch and inaccessible sources; format supply is not proof of downloadability.
7. Build additive discovery_queue.jsonl entries containing source_id, discovered_at, format_probe_at, query, compatible_30fps, qualifying_format_ids, preferred_format_if_available, fallback_reason, raw_inventory_digest and probe_status. Preserve inherited queue fields; no consumer reads this sidecar and the live feeder queue/selector remain byte-identical.
8. Primary metric = sources with compatible_30fps YES / ALL 30 planned ids; publish YES/NO/UNKNOWN counts and per-query descriptive breakdowns. Section repetition cannot increase n; missing inventories cannot disappear from the denominator. No second cap or tracking-quality score is computed.
9. Only 30/30 known YES supports a scoped recommendation to consider 30-only admission without losing any source in this draw; otherwise retain 30 fps preference with 60 fps fallback. Record evidence for a missed available rendition without changing the selector. This is a user policy recommendation, never an automatic admission change.
10. Inspect all 30 raw listings alongside corresponding metadata/sidecar cards in even source order, including failures; independently recompute existence from the full format set. Repeat parsing/tables/cards twice from saved bytes; do not repeat network probes to erase errors or imply stable future availability.
11. Report projected loss of sources under 30-only as NO plus UNKNOWN counts, without treating UNKNOWN as proven unavailable. Include the current G401 29/30 mechanics failure and single-probe basis problem unchanged; do not propose applying that diff as if the new format census validated it.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Discovery-time coverage | G401 21/34 completed sections at59.94; discovery availability unmeasured | All 30 sealed unique source ids accounted; discovery precedes probe; complete raw inventory or explicit UNKNOWN | 30 sources, not sections | All 30 paired listing/metadata cards in even order | Sealed queries/order, no failed-source replacement | Complete protocol DONE diagnostic; missing discovery supply PARTIAL |
| Availability/policy evidence | Preference insufficient in G401 window | YES+NO+UNKNOWN=30; exact qualifying-format existence; only 30/30 YES supports scoped 30-only proposal | ALL 30 planned sources | Same 30 cards, every UNKNOWN explicit | HLS/h264/height/fps definitions; full denominator | Exact table DONE even at zero YES; UNKNOWN blocks sufficiency |
| Shadow queue and reproducibility | No accepted discovery-format sidecar | Full inherited fields preserved; all entries trace to raw receipts; two exact saved-input reproductions; live queue/selector unchanged | 30 entries/all format rows | Same cards with raw-to-queue joins | No daemon edit, download, restart or automatic policy | Additive unused export DONE; silent omission/schema loss REJECT |
EVIDENCE: docs/evidence/tracking/g405_discovery_format_admission_2026-09-11.md plus directory: prereg.md, premise_snapshot.json, discovery_config_hashes.json, discovery_population.csv, draw.csv, raw_formats/, metadata/, probe_receipts.csv, format_rows.csv, discovery_queue.jsonl, availability.csv, eye_index.csv, cards/, summary.json, repeats.json, runtime_receipts/, SHA256SUMS; NOT VERIFIED includes actual downloads and any future source population.
TEST: python -m pytest tests/platformkit/test_g405_discovery_formats.py -q; unavailable/omitted fps is UNKNOWN, non-HLS30 does not qualify, duplicate source cannot fill n, 29.97 recognized, numeric format id insufficient, inherited fields preserved, 29/30 YES cannot support the 30-only branch.
VERSION 2026-09-11 v1 PROPOSED; source-admission measurement and unused queue export only. No daemon change or duration-cap validation; the user retains the policy decision.

COMMON BINDING CONTRACT (astra round 8, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6. Orchestrator alone confirms NEXT_GAP_ID and free ownership, then verifies each whole-set premise BEFORE dispatch; these are prospective specs, not an assertion that today's supply qualifies.
Each row is codex terra PREPARE plus Claude finisher, <=180 minutes including receipts, with independent verification afterward. Seal protocol/bars alone before new metrics; bind exact population, code, model/config and input digests before dependent steps. Missing prerequisites -> PARTIAL; already completed equivalent -> FALSIFIED, no fix.
Own only the named new helper prefix, one test and evidence memo/directory. Helpers <=300 LOC; full-package imports; no src/, shared module, daemon, registry, flag or deploy-tree changes. Any necessary gated change is a separately named PROPOSED diff, applied nowhere. No held-out threshold sweeps, training or daemon restart.
Pod compute uses ~/bin/pod_run <owner> --ship <owned helpers> --fetch <owned evidence> -- <command>, solely in /workspace/wt/<owner>/; sources/deployed tree read-only. Measure actual CPU load/VRAM; <=6 CPU cores, threads 1, sequential GPU use. Bulky writes require a successful pod write probe and <2 GB scratch; unknown quota usage is UNKNOWN, never zero. Summary-only probes need no disk guard.
PC raters obey free RAM >=2.8 GB and <=3 active codex processes, at most two raters simultaneously; defer until capacity exists. Finishers poll their OWN background pod jobs to terminal status, including during transfer/ratings; never wait for a notification or leave a job unattended. Deadlines are built into owned jobs; do not kill/restart peers.
Evidence retains full schemas, all planned failures/ABSENT/UNKNOWN, exact input paths/bytes/native dimensions/fps/decoded PTS, source and route digests, raw answers, eye indices, exclusions and NOT VERIFIED. Retain source objects until every named reader finishes; no pruning in these rows. Fresh-process arithmetic/render reproductions prove only those operations, not rating or producer repeatability.
Reproduce saved tables and delivered renders twice from committed code; preserve pre/post-redaction hashes and semantic-field parity. Scan EVERY landable text artifact including runtime logs with Q6 patterns constructed from character codes, counts-only output. Finishers commit explicit owned paths and the required result row only; no push, bulk add or unrelated edits.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD before a pod stage -- the quota guard prunes ledgered sources ~45 min after tracking); finishers poll their OWN background pod jobs with bounded loops and never stop to wait for a notification; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; counts only; field-aware numeric handling with claim-field detection parity); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round8_2026-09-11.md (local-only). G404 runs on lane a18 (a9 is held for the user-only G395). G400's evidence lands on master after its verify; a premise step that needs it reads lane a7 tip 6b1a7601a until then; G402's after its verify (lane a13 tip b749346af).
