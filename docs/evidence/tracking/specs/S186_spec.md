GAP S186 | sport n/a (system) | worktree a11 | log cx_s186_cycle_history
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (S-row) before you report.
PREMISE (step 0): re-measure both halves. (a) Over every file under data/cache/ingame_shadow_history, nothing excluded: 34 files (mlb 9, tennis 13, wnba 12), 24,612 rows, and 0 of 24,612 carry a bridge/error reason (no_live_state, bridge_date_mismatch, no_home_leg, error:*). Present reasons: hold_existing 13,733 / no_model_prob 5,135 / ok 3,694 / below_floor 1,027 / edge_flipped_reenter 724 / ok_relaxed 245 / bad_price 37 / not_calibration_justified 17. Structural cause: inplay_capture_loop.py:851 sets row["reason"] and :852 returns BEFORE the shadow writes at :869-885, and shadow_history._has_shadow (:63-64, applied :99) drops any row whose five SHADOW_KEYS (:48-52) are all None. (b) data/cache/ingame_grade/_capture_heartbeat.json is 866 bytes, as_of 2026-09-02T22:04:36Z, n_live 0, n_pairs 0, grade_write_fail_by_reason {"no_live_state": 1} -- written by _write_heartbeat (:782, defined :1080) via os.replace, so the next cycle destroys it; the counters n_requests_total / n_429_total / cycle_duration_sec (:769-775) have no append store at all. If either half is falsified, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): confirm poll_once already computes every field the store needs -- grade_write_fail_by_reason, n_live, n_pairs, n_bets, n_requests_total, n_429_total, cycle_duration_sec are all in the heartbeat dict built at :755-782. If any is absent or unreachable, report CLOSED AT LIMIT; do not add producers.
CHANGE (step 2): smallest change, mirroring the in-repo precedent mlb_book_capture.py:280-286 (one "record_type": "cadence" row appended per tick). New file scripts/platformkit/ingame/cycle_history.py: append_cycle_row(heartbeat, now=None, history_dir=None) appends ONE compact JSON line per cycle to data/cache/ingame_cycle_history/<date>.jsonl -- fail-open, size-bounded, history_dir derived from heartbeat_path's parent exactly as the shadow hook does at :796-798. One additive call site in poll_once, inside the SAME fail-open try as the shadow hook. Do NOT loosen _has_shadow; this is a SEPARATE store, not a filter change.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = share of driven cycles whose failure reasons and cycle counters are readable from disk AFTER the next cycle has run, over cycles driven
  before        = 0 of 30 (no failure reason survives; the heartbeat is replaced each cycle)
  bar           = 30 of 30, each row carrying ts, n_live, n_pairs, n_bets, n_requests_total, n_429_total, cycle_duration_sec and the full grade_write_fail_by_reason map
  n             = 30 cycles, driven offline via serve_forever(max_ticks=30, clock=<no-op>, heartbeat_path=<tmp>) with injected live_state_fn/model_fn/inplay_fetch_fn; every driven cycle counted, none sampled; no network, no real sleep
  eye check     = n/a (S-row); reproduction = in MASTER, run the 30-cycle offline loop, count the appended rows, and assert cycle 1's no_live_state count is still readable after cycle 30
  must not move = _capture_heartbeat.json key set and values byte-identical; shadow_history.py SHADOW_KEYS, _has_shadow and MAX_BYTES_PER_DAY unchanged; the 24,612 existing shadow rows byte-identical; data/cache/eval_gate/backtest_fwer.jsonl untouched; the new append can never raise into the cycle
NON-TAUTOLOGY: the 24,612-row denominator is every row in every file under data/cache/ingame_shadow_history with nothing excluded, and the 30-cycle denominator is every cycle driven with none dropped. The claim is about which reasons are ABSENT, so no filtering could flatter it. Scope note the memo must state: no_home_leg (:888) and error:* (:926) are set AFTER the shadow block, so they are absent EMPIRICALLY; only no_live_state and bridge_date_mismatch are unrecordable BY CONSTRUCTION.
EVIDENCE: docs/evidence/harness/cycle_history_2026-09-04.md -- before/after table, n, denominator, the 30 appended rows copied under docs/evidence/, and a "NOT VERIFIED" list. Calibration language only: no dollar, ROI, profit or edge word anywhere.
TEST: exactly one new per-file test, tests/platformkit/ingame/test_cycle_history.py; run only that file.
POD: not needed (offline, no heavy compute). Deploy nothing.
COMMIT: explicit pathspec, in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
