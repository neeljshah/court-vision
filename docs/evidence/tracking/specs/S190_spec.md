GAP S190 | sport all (ops) | worktree a13 | log cx_s190_deploy_eol_parity
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check against every line of section B AND section Q (S-row) before you report.
PREMISE (step 0): `git archive` emits CRLF for part of the deploy set while the worktree holds LF, and the sanctioned FULL-TREE deploy-parity metric normalises that away before hashing, so it cannot see it. RE-MEASURED TODAY (2026-09-04, read-only, HEAD a3704ca00): build `git archive HEAD -- scripts/platformkit supervisor predict_service domains config ops kernel governance data_registry improve frontend` and compare every member's bytes against the same path in the worktree -> n = 4,950 members, 4,612 byte-identical = 93.17 pct, 338 differ = 6.83 pct, 338 of 338 EOL-only, 0 content differences, 0 absent. `git status --porcelain` over the same pathspec shows 0 tracked modifications (1 untracked file), so the worktree side IS HEAD content and the 338 are purely the archive's CRLF. Cause: core.autocrlf=true with a .gitattributes that has 4 lines, only one of which touches the deploy set (`*.sh text eol=lf`) -- which is why pod_bootstrap.sh is the single LF-arriving shell member. Blind spot: S21b_full_parity_deploy_2026-09-03.md:23 defines the metric as "files whose CRLF-normalised md5 is identical local-vs-pod" and :232-238 implements it as `tr -d` CR on BOTH sides (S21c:24/:312-317 the same), so the 338 hash identical and the 3,427/3,427 and 50/50 rows are claims about CR-stripped content, not about bytes on the pod. Scope correction the finder got wrong and this spec adopts: a raw-byte method ALREADY exists in the program -- RESULTS_LEDGER_SYSTEM.md:219 (S16b, 054eea081) deployed 32 files with `git -c core.autocrlf=false archive` and md5 32/32 against the tree blobs, and scripts/platformkit/ops/factory_source_manifest.py:186-214 classifies OK/DIFFERS/MISSING on raw sha256; neither covers the deploy set. Demonstrated functional consequence today: 11 archive members start with a shebang, exactly 1 arrives with a CR-terminated shebang line (scripts/platformkit/tracking/ledger_hook.py). If falsified, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): the archive-vs-worktree comparison only isolates EOL while the worktree matches HEAD over the pathspec. Before measuring, run `git status --porcelain` over the 11 deploy paths and count tracked modifications; if any member differs by CONTENT rather than EOL at the trial rev, STOP and report CLOSED AT LIMIT with the count and the file names -- do NOT reclassify a content difference as EOL to keep the number clean.
CHANGE (step 2): ONE new module scripts/platformkit/ops/deploy_eol_parity.py, additive and read-only. It shells `git archive <rev>` over the 11 deploy paths (rev defaults to HEAD; `--rev` accepts any tree-ish so a number stays reproducible after HEAD moves), reads members with stdlib `tarfile`, and for every member emits BOTH columns: raw md5 of the archive bytes vs raw md5 of the worktree bytes, AND the CR-normalised md5 of each (the S21b convention), plus a classification field in {identical, eol_only, content, absent} and a `cr_shebang` boolean. It prints the five counts and writes a summary JSON plus the per-member rows under docs/evidence/harness/. It does NOT ssh, does NOT deploy, does NOT touch .gitattributes, and does NOT alter or re-hash any value recorded in S21b/S21c. stdlib only (subprocess, tarfile, hashlib, json) -- no new dependency.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = deploy-set archive members reported as differing, in TWO columns (raw bytes, and CR-normalised), plus the classification of every differing member. Denominator: every member of `git archive <rev>` over scripts/platformkit supervisor predict_service domains config ops kernel governance data_registry improve frontend -- 4,950 members at rev a3704ca00.
  before        = one column only (CR-normalised) exists anywhere in the program for the full deploy set (S21b:23/:232-238, S21c:24/:312-317); it reports 0 differing, while the raw-byte truth today is 338/4,950 = 6.83 pct differing, 100 pct of them invisible to that instrument; 1 of the 11 shebang members arrives CR-terminated.
  bar           = at rev a3704ca00 the emitter reports raw differing = 338, CR-normalised differing = 0, classification eol_only = 338, content = 0, absent = 0, identical = 4,612, over n = 4,950 members, and shebang members = 11 with cr_shebang = 1 naming scripts/platformkit/tracking/ledger_hook.py; AND the two columns are printed side by side so the disagreement (338 vs 0) is visible in the artifact rather than normalised away. Additive only: the raw column, the classification field and the shebang field are NEW; every CR-normalised value the emitter prints reproduces the S21b convention unchanged. A bar found unmeetable is reported CLOSED AT LIMIT, never lowered.
  n             = 4,950 (CONSTRUCT -- every archive member of the pathspec at a3704ca00 compared byte-for-byte, no sampling, nothing excluded)
  eye check     = n/a (S-row); reproduction = the verifier runs `python -m scripts.platformkit.ops.deploy_eol_parity --rev a3704ca00` in master and independently recomputes, from `git archive` and the worktree alone, the five counts 4,950 / 4,612 / 338 / 0 / 0 and the shebang pair 11 / 1, then confirms every one of the 338 is EOL-only by checking that its two byte streams are equal after CRLF -> LF on both sides.
  must not move = .gitattributes byte-identical (the EOL policy is Neel's call, not this lane's); the deploy pathspec (the same 11 paths S21b used); docs/evidence/harness/S21b_full_parity_deploy_2026-09-03.md and S21c_delta_deploy_2026-09-03.md byte-identical, including every CR-normalised md5 already recorded there; data/cache/eval_gate/backtest_fwer.jsonl byte-identical (18 rows, no charge, K unchanged); scripts/platformkit/ops/factory_source_manifest.py and scripts/platformkit/ops/pod_bootstrap.sh unchanged.
NON-TAUTOLOGY: the metric covers EVERY member of the archive at the named rev -- no path, extension or directory is excluded, and nothing is filtered before counting. Members absent from the worktree are counted in their own class (0 today) rather than dropped, so the denominator cannot shrink to flatter the number. The comparison is archive-vs-worktree, not a stream against itself: the two byte streams come from different producers (git's smudge/clean path vs the checked-out file), so a member cannot be identical by construction. The reported 338 is the FAILING set, not the surviving set -- the lane is publishing the size of its own blind spot, and a bar of "raw = 0" would be the circular one.
EVIDENCE: docs/evidence/harness/deploy_eol_parity_2026-09-04.md -- the five counts with the exact denominator, the two columns side by side, the 11/1 shebang tally, the classification breakdown, the extension histogram of the 338, and a "NOT VERIFIED" list that states plainly that the POD side was never contacted (no ssh in this lane) and that this measures what `git archive` would ship, not what currently sits on the pod.
REQUIRED EVIDENCE DURABILITY: copy under docs/evidence/harness/ the summary JSON and the full per-member row table (path, class, raw md5 archive, raw md5 worktree, CR-normalised md5, cr_shebang) so the verifier can reproduce every count from the artifact alone without re-running git.
RE-EMITTED TABLES: preserve the FULL column set -- both raw md5 columns AND the CR-normalised column AND the class AND the shebang flag; a table that keeps only the class loses the evidence that the two hashing conventions disagree.
TEST: exactly one new per-file test, scripts/platformkit/ops/test_deploy_eol_parity.py; run only that file (`python -m pytest scripts/platformkit/ops/test_deploy_eol_parity.py -q`). NEVER run the full suite. Test the classifier on synthetic byte pairs (LF vs CRLF -> eol_only; a real content change -> content; equal -> identical; missing -> absent) plus the CR-shebang detector; do not make the test depend on the repo's live 338.
POD: none. This lane does no heavy compute and contacts no host. Do NOT ssh, do NOT scp, do NOT deploy any file. Report the files you would deploy; do not deploy them.
COMMIT: explicit pathspec in the worktree (the new module, the new test, the memo, the JSON and the row table), no push. Report the sha.
CALIBRATION LANGUAGE ONLY (Q6): this is a byte-parity instrument; no dollar, ROI, profit or edge word anywhere in the module, memo, ledger line or register row, and none of the retracted figures. A REJECT, NULL or CLOSED AT LIMIT is a success.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
