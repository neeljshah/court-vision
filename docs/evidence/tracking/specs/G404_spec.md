GAP G404 | sport basketball | worktree a18 | log cx_g404_play_gated_ball_growth_stage2
**RANK 2 - CHANGE SAMPLING BEFORE DRAWING. Landed docs/evidence/tracking/g386_pin_copy_one_pass_2026-09-10/summary.json SHA-256 e0e6119766899786a4004cd34e7587bacc615853cc87674c85ce4d06d297803f supplies retention. Landed docs/evidence/tracking/g375_corpus_sport_purity_2026-09-10/diagnostic.json SHA-256 2c2a4338d616b12e59433de25208272ac3f65a8ecec15f23cb874efc0926fc36 describes a frozen court-presence candidate, NOT a validated play gate. Pending G400 summary at docs/evidence/tracking/g400_ball_reference_growth_stage1_2026-09-11/summary.json SHA-256 604ab2c7abcc2c6bd421cab064adf9e787a860abbf41d93efca57a08b1851bb2 fixes yield 116/300 and failed usability; require its handoff and accepted G403 controls.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 20 min; pod a18 retention/native decode/frozen embeddings with PC receiver 30; PC gate audit/controls/ball ratings/adjudication 110; PC reproduction/test 20. Total <=180 min; no GPU reserved while PC raters work. a18 is used (the orchestrator confirmed it free; a9 is held for G395); the original note read: a9 is used only if the orchestrator confirms its supplied free status.
OWNS: scripts/platformkit/tracking/g404_*.py; tests/platformkit/test_g404_play_gated_growth.py; docs/evidence/tracking/g404_play_gated_ball_growth_stage2_2026-09-11.md and matching directory; named row-owned off-repo receiver only for retained source bytes.
PREMISE step 0 (BINDING before-condition): rehash accepted G403 instructions/control catalogue and G364 weights/reference/labels from G375's diagnostic identities; freeze their full hashes in the seal. No control handoff, source identity or gate assets -> PARTIAL before collecting/rating.
Census the ENTIRE available retained pool and old DEV/held-out/context canonical-game identities, including alternate uploads. Require >=30 resolved games disjoint from the original 530-box DEV games and every held-out/context game, >=2 competitions and native sources with distinct prospective targets; G400 sources may be reused as DEV games, not claimed as another 30 new games. Unresolved overlap -> excluded before drawing; video-id disjointness alone is insufficient.
METHOD (sealed; gate audit first, then a fixed 300-frame collection):
1. Order eligible games by competition/canonical game/source digest; use one median-offset section/game. Retain with G386 receiver/hash/readback before rotation; existing digest-equal retained objects need no second transfer. Decode the actual frame PTS schedule, never packet count; save decode indices, timestamps and pixel hashes.
2. Candidate grid is every 2 s strictly inside that decoded span, nearest decoded PTS with earlier tie; collapse duplicate pixels/indices and exclude frames within 1 s of every prior rated/control/context frame. Freeze ALL candidate keys and exclusions before evaluating the gate; no ball detector or ball-visibility cue selects candidates.
3. Candidate gate is EXACTLY G375's frozen ResNet-18/G364 240-reference cosine nearest class == USABLE_COURT, first reference index on ties; no new fit, score cutoff, smoothing or alternate classifier. Hash the reference order and label map. This is a candidate shot gate on known basketball games, not a sport-purity claim.
4. Before any ball draw, sort grid keys by game/PTS/digest and audit 30 gate-admitted plus 30 gate-excluded frames, evenly within each full stratum using floor(j*(N-1)/29+0.5). Missing >=30 in either stratum -> PARTIAL. Both raters independently label PLAY/NONPLAY/UNKNOWN from native full frames without gate output or ball coordinates.
5. PLAY means visible multi-player on-court basketball action; wide replay action qualifies, wipes do not. Bench/crowd/portrait/graphics/rim-only views are NONPLAY; uncertainty is UNKNOWN. Require >=27/30 admitted frames jointly PLAY and >=24/30 excluded jointly NONPLAY; unknown/disagreement fails its count. These are new prospective gate-audit bars, not inherited G375 performance or a whole-pool recall estimate.
6. Gate failure -> STOP this candidate AT LIMIT, retain all audit keys, no replacement classifier or ball collection. After gate pass, exclude its 60 audit frames and 1 s neighborhoods, then require >=30 games with >=10 distinct admitted targets each. Missing supply -> PARTIAL; no substitution based on later ball ratings.
7. Draw 30 games evenly from the complete eligible ordered set; within each game draw ten frames evenly over admitted PTS order at floor(k*(M-1)/9+0.5), k=0..9. Freeze all 300 keys, native manifests and ten rounds, one frame/game/round, BEFORE ball labels. Export eligible/grid/all-source counts so the conditional sampling population stays visible.
8. Independently administer G403's 30 controls to terra and sol once, no feedback: each must get 30/30 states correct and all 20 VISIBLE centre-rule matches. Failure -> NOT VALIDATED and STOP; no real rating retry. Freeze instruction/rater/executable hashes; qualification says nothing about unseen real-ball accuracy.
9. Rate all 300 native full frames independently, retaining state labels even when geometry is invalid; separate label completeness from usable-centre completeness. Kappa is three-label, raw and pre-adjudication, >=0.60 on every COMPLETE n=30 round and pooled n=300; undefined or incomplete never passes. No relabeling or rerating to rescue a round.
10. Inherited usability: primary gaps on ALL valid raw both-VISIBLE pairs, >=30; median gap <=0.5 times median diameter over ALL valid raw VISIBLE judgments, diameter=(box_w+box_h)/2. Publish both populations, p90/max and resolution diagnostics; do not condition on same-object agreement or adjudication. Native sheet_scale=1.0; inherited per-frame centre scoring remains unchanged.
11. After raw sealing, finisher audits every conflict/invalid box and EVERY proposed retained box from native full view plus zoom, preserving originals and uncertainty; also inspect 30 even all-state cards. One reviewer supplies audited candidate judgments, not independent truth; adjudication never repairs a failed raw gate.
12. Complete the sealed stage regardless of interim yield; audited new unique boxes/300 >0.40 AND every reliability/usability bar permits a later collection proposal. <=120 closes THIS gated rule AT LIMIT; any raw/control/usability failure -> NOT VALIDATED; deadline or missing ratings -> PARTIAL with all 300 planned keys and no yield conclusion from a subset. No automatic next batch.
13. Export new candidate boxes/negative/UNKNOWN states separately, preserve the 530 accepted boxes and G400's 116 unused additions unchanged. No G400/G404 box enters accepted DEV without independent acceptance. Full training lock remains >=1,500 accepted boxes/>=57 resolved games; no training or held-out invocation here.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Gate and source validity | G375 diagnostic only; G400 uniform yield 0.387 | 27/30 joint PLAY admissions; 24/30 joint NONPLAY exclusions; 30 resolved games/300 new keys after gate-audit exclusion | 60 audit frames; 300 planned/30 games | All 60 audit frames, native | Frozen gate/reference; no post-draw object filtering | Failed gate CLOSED AT LIMIT; missing assets/supply PARTIAL |
| Rater reliability/usability | G400 round 8 0.3605; gap24.021/bar13.75 | Both control sets 30/30; all ten n=30 kappas and pooled >=0.60; >=30 centre pairs; inherited 0.5-diameter median rule | 30 CONSTRUCT/rater; 300 real frames | All proposed boxes/conflicts plus 30 even all-state cards | Raw labels, centre rule, native scale | Failed control/raw/usability NOT VALIDATED; incomplete PARTIAL |
| Yield and reference status | 530 accepted; 116 unused additions | >120 audited new unique boxes/300 AND preceding bars; complete identity/audit chain; full lock remains >=1,500/>=57 | ALL 300 planned, including ABSENT/UNKNOWN/failed | Same whole-stage audit | Old boxes, old verdicts, accepted-reference boundary | <=120 CLOSED AT LIMIT; complete pass DONE for stage only |
EVIDENCE: docs/evidence/tracking/g404_play_gated_ball_growth_stage2_2026-09-11.md plus directory: prereg.md, input_hashes.csv, census.csv, disjointness.csv, source_receipts.csv, decoded_pts.csv, candidate_grid.csv, gate_outputs.csv, gate_audit_draw.csv, gate_raw/, gate_audit.csv, draw.csv, native_manifest.csv, batch_plan.csv, controls/, rater_raw/, batch_receipts.csv, ratings.csv, kappas.csv, gaps.csv, diameters.csv, adjudications.csv, new_boxes.csv, yield.csv, summary.json, repeats.json, renders/, SHA256SUMS; NOT VERIFIED and receiver paths mandatory.
TEST: python -m pytest tests/platformkit/test_g404_play_gated_growth.py -q; gate audit precedes ball draw, even draws and audit-context exclusions, invalid box preserves valid state, incomplete/undefined kappa cannot pass, all planned frames in yield, no held-out/game overlap and no implicit reference promotion.
VERSION 2026-09-11 v1 PROPOSED; this is the ONE new sampling rule, conditional on G403 and supply. No threshold search, fresh uniform retry, lowered centre bar or collection beyond the sealed stage.

COMMON BINDING CONTRACT (astra round 8, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6. Orchestrator alone confirms NEXT_GAP_ID and free ownership, then verifies each whole-set premise BEFORE dispatch; these are prospective specs, not an assertion that today's supply qualifies.
Each row is codex terra PREPARE plus Claude finisher, <=180 minutes including receipts, with independent verification afterward. Seal protocol/bars alone before new metrics; bind exact population, code, model/config and input digests before dependent steps. Missing prerequisites -> PARTIAL; already completed equivalent -> FALSIFIED, no fix.
Own only the named new helper prefix, one test and evidence memo/directory. Helpers <=300 LOC; full-package imports; no src/, shared module, daemon, registry, flag or deploy-tree changes. Any necessary gated change is a separately named PROPOSED diff, applied nowhere. No held-out threshold sweeps, training or daemon restart.
Pod compute uses ~/bin/pod_run <owner> --ship <owned helpers> --fetch <owned evidence> -- <command>, solely in /workspace/wt/<owner>/; sources/deployed tree read-only. Measure actual CPU load/VRAM; <=6 CPU cores, threads 1, sequential GPU use. Bulky writes require a successful pod write probe and <2 GB scratch; unknown quota usage is UNKNOWN, never zero. Summary-only probes need no disk guard.
PC raters obey free RAM >=2.8 GB and <=3 active codex processes, at most two raters simultaneously; defer until capacity exists. Finishers poll their OWN background pod jobs to terminal status, including during transfer/ratings; never wait for a notification or leave a job unattended. Deadlines are built into owned jobs; do not kill/restart peers.
Evidence retains full schemas, all planned failures/ABSENT/UNKNOWN, exact input paths/bytes/native dimensions/fps/decoded PTS, source and route digests, raw answers, eye indices, exclusions and NOT VERIFIED. Retain source objects until every named reader finishes; no pruning in these rows. Fresh-process arithmetic/render reproductions prove only those operations, not rating or producer repeatability.
Reproduce saved tables and delivered renders twice from committed code; preserve pre/post-redaction hashes and semantic-field parity. Scan EVERY landable text artifact including runtime logs with Q6 patterns constructed from character codes, counts-only output. Finishers commit explicit owned paths and the required result row only; no push, bulk add or unrelated edits.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD before a pod stage -- the quota guard prunes ledgered sources ~45 min after tracking); finishers poll their OWN background pod jobs with bounded loops and never stop to wait for a notification; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; counts only; field-aware numeric handling with claim-field detection parity); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round8_2026-09-11.md (local-only). G404 runs on lane a18 (a9 is held for the user-only G395). G400's evidence lands on master after its verify; a premise step that needs it reads lane a7 tip 6b1a7601a until then; G402's after its verify (lane a13 tip b749346af).
