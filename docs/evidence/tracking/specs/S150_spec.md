GAP S150 | sport all (harness) | worktree a10 | log cx_s150_runner_leases
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md. Calibration language only: no dollar, ROI, profit or edge words. Never touch data/registry, src/, kernel/, api/, intel/, scripts/team_system/. Per-file tests only (python -m pytest <one file> -q); NEVER the full suite. data/ in this worktree is a read-only junction to the main repo's data/ (no data/cache/eval_gate) -- never write under data/; use temp sqlite files for every test. NEVER PARK: run everything to completion this turn; never end waiting. COMMIT: explicit pathspec (git add <paths> && git commit -m "..." -- <paths>), in this worktree, no push. Last line of your report: SHA: <sha>.
GAP (verbatim from the register): DEAD RUNNERS HOLD THE QUEUE FOR 12 HOURS (S135 side effect, found in the pod quota incident): the default lease is now LEASE_SECONDS per claimed row, so a 50-row batch claimed by a runner that then dies is held ~12.5 h; after the incident 9,503 rows were claimed-without-result and the runner idled at unclaimed 0 until released by hand. Bar: the runner releases its outstanding claims on SIGTERM / normal exit; reap_expired also reaps claims whose claimer pid is not alive when claimer encodes host:pid; the default lease reverts to LEASE_SECONDS x min(batch, 5) with renew() actually called per hypothesis by the runner (S135's renew has zero callers); tests for each.
READ (every signature on disk): scripts/platformkit/foundry/results_db.py + results_db_sql.py (claim, renew, reap_expired, the claimer column, LEASE_SECONDS, release if any), scripts/platformkit/foundry_runner.py (run_pass, screen_queue, main, how the claim batch flows, where a per-hypothesis hook can call renew), docs/evidence/harness/S134_S135_S130_redteam2_fixes_db_alias_ticks_2026-09-03.md (the S135 design), tests/platformkit/foundry/test_results_db.py and test_foundry_runner_s16.py.
PREMISE (step 0): print the current default lease computation in claim(), confirm renew() has zero production callers (grep), confirm there is no release-on-exit path in foundry_runner, and reproduce on a temp sqlite: claim 50 rows with the default, simulate the claimer dying (no renew, no record), show reap_expired at +1 h frees 0 rows. If any of these is already handled, say so and skip that part.
LIMIT (step 1): n/a.
CHANGE (step 2): (a) results_db: `release(claimer)` sets claimed_at/lease_until/claimer NULL for rows claimed by that claimer with no result at that tier; claimer encodes `<host>:<pid>` (additive; existing callers that pass no claimer unchanged); reap_expired(now, owner) additionally reaps rows whose claimer is on THIS host and whose pid is not alive (os.kill(pid, 0) / /proc check; other hosts untouched); the DEFAULT lease becomes LEASE_SECONDS x min(batch, 5) (explicit lease_seconds still honoured exactly). (b) foundry_runner: register atexit + SIGTERM/SIGINT handlers that call release(self_claimer) once; call renew(hashes_in_flight) after each screened hypothesis (or every N seconds) so a live runner never loses a lease mid-batch. (c) tests: dead-claimer rows reaped by a second runner on the same host; release on normal exit and on SIGTERM (subprocess test with a temp sqlite); renew keeps a live claim past LEASE_SECONDS; the default lease value; existing test_results_db and test_foundry_runner_s16 stay green.
TEST: python -m pytest tests/platformkit/foundry/test_results_db.py -q and tests/platformkit/foundry/test_foundry_runner_s16.py -q plus the new per-file test.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = the three behaviours (release on exit, reap dead same-host claimers, renew called per hypothesis) each proven by a test; denominator = 3
  before        = 0/3
  bar           = 3/3 with every existing test green
  n             = 3 (CONSTRUCT)
  eye check     = n/a; reproduction = verifier runs the tests in master and repeats the dead-claimer reproduction
  must not move = lease semantics for explicit lease_seconds callers, the ledger (never open it), data/registry/**
NON-TAUTOLOGY: the dead-claimer test uses a REAL dead pid (a finished subprocess), not a flag.
EVIDENCE: docs/evidence/harness/S150_runner_leases_2026-09-03.md (premise reproduction, diff summary, tests, NOT VERIFIED).
POD: none. Do not ssh anywhere.
