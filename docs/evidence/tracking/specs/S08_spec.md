GAP S08 | sport all (harness) | worktree a11 | log cx_s08_replication_gate
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): replication is a convention, not a gate: `combo/fwer_budget.min_corpora_eff` (:55) exists and no call site consults it, so a single-window AHEAD is downgraded only by memo.
READ (exact symbols, re-grepped on disk 2026-09-03): scripts/platformkit/combo/fwer_budget.py `min_corpora_eff(n_corpora, k, cap=DEFAULT_CORPORA_CAP)` (:55), `DEFAULT_CORPORA_CAP` = 4, `_MIN_CORPORA_FLOOR` = 2, `eps_eff`, `cumulative_k` -- READ-ONLY, this is a SHARED MODULE (docs/evidence/SHARED_MODULE_TOKEN.md); you do NOT edit it. The AHEAD writers: scripts/platformkit/hedge_trial_runner.py `verdict_of(stats)` (:100-103, returns "AHEAD" if improvement >= BAR and dm_ci95 low > 0 and deflated_p < 0.05, else "BEHIND"; `stats` already carries `k_cumulative`), scripts/platformkit/ingame/mlb_winprob_v6.py (:105, :186-187), scripts/platformkit/ingame/mlb_winprob_v7.py (:179), scripts/platformkit/frontend/slate.py (:80). The EXISTING consumers that already gate on the floor and MUST NOT CHANGE: scripts/platformkit/combo/combo_gate.py (:149-152, rejects at L5) and scripts/platformkit/combo/stack_gate_pregame.py (:192-194, caveat only) and scripts/platformkit/improve/selfimprove_stage.py (:177).
PREMISE (step 0): the register says "no call site consults it". On disk that is TRUE ONLY of the AHEAD writers -- combo_gate.py:149 already REJECTS below the floor. So measure the SCOPED premise: grep every module that returns the literal verdict "AHEAD" (the five above) for an import or call of `min_corpora_eff`; premise holds iff ZERO of them consult it -> before = 0 of the AHEAD writers gate on replication. Print the grep. If any AHEAD writer already consults it, STOP, write the memo, commit, report FALSIFIED (a valid result).
LIMIT (step 1): n/a -- CONSTRUCT row. Print `min_corpora_eff(1, k)` and `min_corpora_eff(2, k)` for the K currently in data/cache/eval_gate/backtest_fwer.jsonl (13 rows; READ the last `k_cumulative`, never append) so the memo states the floor in force today.
CHANGE (step 2): NEW scripts/platformkit/eval_gate/replication_gate.py (<=300 LOC, stdlib + a read-only import of min_corpora_eff):
  `replication_verdict(verdict: str, n_corpora: int, k: int) -> str` -- returns "SINGLE-WINDOW" iff verdict == "AHEAD" (or endswith "_AHEAD") AND n_corpora < min_corpora_eff(n_corpora, k) is FALSE-safe; precisely: floor = min_corpora_eff(max(1, n_corpora), k); downgrade iff n_corpora < floor. Every other verdict (BEHIND, MATCH, NULL, INSUFFICIENT, REJECT) passes through BYTE-IDENTICAL. `n_corpora` counts disjoint `corpus_unit`s, not rows: tennis ATP vs WTA, soccer D1/E0/E1/F1/I1/SP1, MLB's two eras, NBA's two seasons -- replication needs no new data.
  `replication_fields(verdict, n_corpora, k) -> dict` -> {"verdict_replicated", "min_corpora_eff", "n_corpora", "k_cumulative"} for an artifact to embed.
  CALL SITE (exactly one, additive): scripts/platformkit/hedge_trial_runner.py -- keep `verdict_of` returning what it returns today (no rename, no removed status value) and add `verdict_of_replicated(stats, n_corpora)` that applies `replication_verdict` at `stats["k_cumulative"]`, plus the four `replication_fields` keys in the stats dict it writes. Name in the memo, as a follow-up row, every other AHEAD writer left unwired (mlb_winprob_v6/v7, slate.py) -- do NOT edit them in this gap.
TEST: NEW scripts/platformkit/eval_gate/test_replication_gate.py (beside the module). Two constructed verdicts at the SAME K (the bar) plus two invariance cases: (a) AHEAD, n_corpora = 1, K such that the floor is 2 -> "SINGLE-WINDOW"; (b) AHEAD, n_corpora = 2 at the SAME K -> "AHEAD" unchanged; (c) BEHIND, n_corpora = 1 -> "BEHIND" unchanged (a downgrade must never touch a non-AHEAD verdict); (d) `verdict_of(stats)` returns exactly what it returns on master for the same stats (byte-identical, no existing key changed). Run ONLY `python -m pytest scripts/platformkit/eval_gate/test_replication_gate.py -q`, then rerun `python -m pytest scripts/platformkit/test_hedge_trial_arms.py -q` (must stay green) -- two files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = the downgrade fires; denominator = 2 constructed verdicts at the same K
  before        = 0 of the 5 AHEAD writers consult min_corpora_eff (measured in step 0); module absent
  bar           = 2/2
  n             = 2 (CONSTRUCT) for the bar; cases (c) and (d) are additional invariance checks, not the bar
  eye check     = n/a (S-row); reproduction = verifier reruns both test files in master and re-prints replication_verdict on the two bar cases
  must not move = `min_corpora_eff` itself and every constant in scripts/platformkit/combo/fwer_budget.py (SHARED MODULE -- not edited), combo_gate.py's L5 reject, stack_gate_pregame.py, selfimprove_stage.py, `verdict_of`'s existing return values and BAR, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows; No `_charge_ledger` call -- READ the last k_cumulative only)
NON-TAUTOLOGY: both bar cases run at the SAME K, so only n_corpora differs -- the downgrade cannot be an artifact of a moved K. Cases (c) and (d) prove the rule touches nothing it should not; excluding them would make the metric circular.
EVIDENCE: docs/evidence/harness/S08_replication_gate_2026-09-03.md -- the step-0 grep, the floor at today's K, the four-case table, exact commands, both test outputs, a NOT VERIFIED list naming the AHEAD writers left unwired and the fact that no real AHEAD exists to downgrade today. Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local only.
COMMIT: explicit pathspec (module, the hedge_trial_runner edit, test, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
