GAP G372 | sport all (basketball fixtures first) | worktree a9 | log cx_g372_inline_source_pinning
ALLOCATED 2026-09-10 (orchestrator; from the astra-reviewed v2 draft docs/research/spec_drafts/). CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md B1-B10; Q1/Q6.
SUCCESSOR TO: G369 PARTIAL (32/35 attempted sections PINNED, 100 pct of 17 required fields on all 32, 0 collisions, byte-identical exports; 8 distinct source ids over 4 competitions, below the sealed >= 10-game bar; the >= 30-section and 3-competition clauses were met) [M369,E369].
PREMISE NUMBERS, corrected, byte-identical to sources.csv/attempts.csv/summary.json: of 55 fresh tracked sections, AVAILABLE (source bytes still on disk at PIN time) = 35/55 = 63.6364 pct, NOT "bound" as v1's acceptance called it. Actual binding is PINNED 32/55 = 58.1818 pct of fresh sections, or 32/35 = 91.4286 pct of attempts. 20/55 = 36.3636 pct were unavailable. The 3 ABSENT attempts had bytes on disk but no resolvable first_pts (leading packets carry negative PTS past the 5.0 s probe window) -- they are NOT part of the 20 missing sources and must not be merged with them. 8 distinct source ids is below the sealed 10-game bar; the bar was not lowered. [E369,M369]
FALSE PREMISE, corrected: v1 asserted "the daemon deletes a bridge file after tracking." track_daemon.py:327-328 calls retain(); track_daemon_done.py:262-276 retain() MOVES the video (video.replace) into footage_corpus -- it does not delete bytes. track_daemon.py:330 deletes only the per-job .log. Under the guard-zero/two-cause account, feeder cleanup upstream of the daemon is the remaining explanation for the 20 missing sections, NOT daemon post-track byte deletion; this attribution is UNVERIFIED here -- no feeder source, deletion event or deployed daemon digest is supplied by M369. A corpus move (retain) must never be counted as lost bytes. [D:327-330,H:262-276,M369]
BEFORE-CONDITION, exact, POD read-only, NOT executed in this draft (code identity): `cd /workspace/deploy/nba-ai-system && sha256sum scripts/platformkit/track_daemon.py scripts/platformkit/track_daemon_done.py && nl -ba scripts/platformkit/track_daemon.py | sed -n '323,389p' && nl -ba scripts/platformkit/track_daemon_done.py | sed -n '262,275p'`. Local track_daemon.py SHA-256 is 9747d9a085405c492f04c4e3b162ed8b1d897309da6f0f9c014cb7092056f5ac; the finisher must confirm the deployed digest matches before attributing loss. [D,H,V A11/S2]
BEFORE-CONDITION, exact, POD deploy-root cwd, read-only (survival distribution): `python3 -c 'import json,time; from pathlib import Path; t=time.time(); rows=[json.loads(s) for s in Path("data/tracking/track_daemon_ledger.jsonl").read_text().splitlines() if s.strip()]; r={x["game_id"]:x for x in rows if x.get("status")=="tracked" and 0<=t-float(x.get("finished_at",0))<=7200}; a=[x for x in r.values() if t-float(x["finished_at"])>=3600]; ok=lambda x:any((Path("data")/d/(x["sport"]+"__"+x["game_id"]+".mp4")).is_file() for d in ("footage_bridge","footage_corpus")); print("fresh_n",len(r),"available",sum(map(ok,r.values())),"aged_n",len(a),"aged_available",sum(map(ok,a)),"aged_share",sum(map(ok,a))/len(a) if a else "UNKNOWN","median_deletion_age","UNKNOWN")'`. Archive the snapshot and per-section probe rows; zero eligible aged sections cannot trigger the >= 0.95 false-stop; sampled survival needs >= 30. [D:31-45/285-307,H,V S4/B9]
HOOK LOCATION: track_daemon.py:373 (`log_path = path.with_suffix(".log")`), strictly AFTER duplicate handling at 367-372 and strictly BEFORE `subprocess.Popen` at 376. Reserve the claim and open the source handle immediately at the hook, timestamp it, dispatch bounded identity work off the tick poll loop, and commit identity (or a terminal diagnostic) before decode launches. Propagate `source_identity_path` through the job dict to the completion ledger write at track_daemon.py:285-323 as one ADDITIVE field. Completion rows omit in-flight/failed claims, so also keep a separate append-only claim journal covering every reservation, not only finished ones. [D:373-376,D:285-323,F156]
SIDECAR MUST-NOT LIST: never block the daemon tick on decode, a full-file hash, or a long ffprobe; never read ball/tracking tables; never infer identity from detector output; never quarantine or gate tracking on missing sidecar metadata (ABSENT passes the item through, per B3); never retry a failed claim indefinitely; never rewrite a sidecar once committed. Reuse low-level identity helpers only, never `g369_prospective_identity.cmd_pin`/`cmd_align` (pin depends on completed ledger rows; align reads tracking_data.csv; `_probe` can block up to 120 s). g369's manifest has NO negative-PTS start_time fallback; stream start_time is a labelled ESTIMATE, never proven-first-decoded-PTS. [C369,D,V B1/B3/B4,F156]
METHOD (sealed before any number, additive only): 1) SIDECAR at claim time writes source path/sha256/byte-size, ffprobe fields, crop rule, route+weight digests, deploy_manifest_sha256, claim UTC, daemon pid, one additive `source_identity_path` ledger field. 2) INDEPENDENT CHECK: finisher runs the G369 pin on >= 30 sections / >= 10 videos within 60 s of each claim and joins on game_id; sidecar sha256 == independent sha256 is the binding. 3) CONTROLS: byte-flip changes both hashes; two reads of every sidecar byte-identical; every producer table column-identical before/after the overlay. 4) nothing else changes; no flag; sidecar never read by the producer.
Q1 SEAL ORDER (astra): seal the premise census and the fixed window/sampling/stopping rules FIRST, before any metric is printed. Archive before-window code and schema hashes BEFORE the deploy overlay. Accept the code change independently; only then does the finisher apply the authorized deploy-only overlay, after any live measurement lane on the pod finishes (D5). Seal the actual claim manifest before joining or scoring. Never select a convenient successful window after seeing results.
ACCEPTANCE RULE (astra's rewritten block; inherited bars unchanged):
  metric/before = valid immutable sidecars / all unique claim attempts in fixed windows; full independent hash agreement; historical AVAILABLE 35/55 = 63.6364 pct, PINNED 32/55 = 58.1818 pct; current sidecar baseline measured separately; all missing/error/in-flight claims retained in the manifest. [E369,D]
  bar = coverage 1.000 on >= 30 sections / >= 10 distinct videos; independent agreement 100 pct on a preregistered even sample >= 30, zero omitted selected pins; byte-flip control changes both hashes; identical reads AND deterministic writes on identical fixed inputs; every existing producer table column unchanged. [V B1/B2/B8]
  throughput = fixed >= 60-min windows PLUS paired replay on >= 30 unique comparable preserved inputs, timing claim-to-finish including the sidecar; after/before median seconds-per-clip <= 1.10 (never slower); preserve arrivals, active load, decoded frames, censored jobs; insufficient comparability or n = NOT VALIDATED for this clause, never a pass from unchanged clips/hour; adopt this pairing requirement before scoring. [D,V B9/B10]
  n/eye = sampled live sections, NOT CONSTRUCT; >= 30 per scored metric, clustered by video with game identity disclosed; 10 sidecars/pins shown evenly across the whole claim window including failures; deterministic controls separately enumerated as CONSTRUCT. [V Q7/B7]
  must not move/outcome = every existing column, status, threshold, flag, `data/registry/`, and the landing repo's `src/`/`scripts/` (PROPOSED overlay only); terminal sidecar failures never stop tracking. Code ACCEPT precedes the authorized deploy-only measurement; landing is ADJUDICATED per VERIFIER_CONTRACT.md section C; DONE only when every measured clause passes, else PARTIAL naming the failing clause.
EVIDENCE: `docs/evidence/tracking/g372_inline_source_pinning_2026-09-10.md` (<= 60 lines; VERDICT line 1; NOT VERIFIED; wall time; SHA-256s incl. the PROPOSED diff) + `.../sidecars.csv,pins.csv,join.csv,controls.csv,throughput.csv,schema_diff.txt,claim_journal.csv,summary.json`. ADD ONE RESULTS_LEDGER.md ROW SAME COMMIT.
TEST: `tests/platformkit/test_g372_source_sidecar.py` alone (sidecar schema; byte-flip changes sha256; two writes byte-identical; negative-PTS fallback labelled ESTIMATE; claim journal covers a failed reservation). NEVER a full pytest. Every new file <= 300 lines. Vocabulary follows contract Q6; automated scan required.
COMMIT: explicit pathspec only; prereg sealed as its OWN commit first (`SEAL sha256 <hex>`). ASCII stdout. NEVER PARK.
VERSION 2026-09-10
