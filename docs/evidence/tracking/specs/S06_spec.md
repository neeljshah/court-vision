GAP S06 | sport mlb (harness) | worktree a10 | log cx_s06_stacker | LAST AND ALONE -- the ONLY charged trial; nothing else may call `_charge_ledger` while this runs
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): no nested-CV stacker over the arms; online Hedge trails its best arm (regret +2.063 inside a 66.79 bound over 158 rounds) with three named structural causes -- coverage asymmetry, two bad arms, regime heterogeneity (-0.0053 innings 1-3 vs +0.0484 innings 7+).
READ (read every signature on disk; line numbers drift): scripts/platformkit/combo/stack_fit.py `StandardizerParams`, `fit_logistic`, `score_with_fallback`; combo/nested_cv.py `select_then_score`; eval_gate/cpcv_engine.py `cpcv_evaluate(states, predictor, n_groups=8, n_test_groups=2, embargo_days=1)`; eval_gate/walkforward.py `walk_forward`, `assert_vintage`, `LeakError`; eval_gate/pbo.py `cscv_pbo`, `build_pred_matrix`; eval_gate/dm_test.py `diebold_mariano(d, cluster_ids)`; eval_gate/deflated_metrics.py `deflated_p(raw_p, k)`; eval_gate/scoring.py `brier` (import the module symbol -- it is defined twice behind a fallback); combo/fwer_budget.py `min_corpora_eff`; ingame/gap_effective_n.py `intraclass_correlation`, `design_effect`, `effective_sample_size`; scripts/platformkit/hedge_trial_arms.py `e4_blend_series`, `e1_offset_series`, `e2_regime_series`, `arm_series`; hedge_trial_runner.py `LEDGER` and `run_sport` (it charges BEFORE any metric -- copy that order); eval_gate/backtest_runner.py `_charge_ledger(path, spec, sport, start, end) -> dict` (THE only ledger writer, under `ledger_lock`). Also HEDGE_TRIAL_RESULT_2026-09-01.md and E4_PROMOTION_RESULT_2026-09-01 / E4_PROMOTION_PREREG (the seal pattern) if present in this worktree.
PREMISE (step 0), BEFORE ANY CHARGE -- the OOF PROOF, IN WRITING: prove each arm series out-of-fold ARM BY ARM. `e2_regime_series` carries its own `assert max(date(train)) < test_date`; `raw_model` is the in-game model's own `model_prob` and inherits that model's fold discipline -- NAME the model. `e4_blend_series` and `e1_offset_series` are UNCHECKED: trace their fit windows and state the finding. Any arm not PROVABLY out-of-fold is DROPPED and NAMED. Then assert every retained series reproduces its own arm's reported Brier to 1e-9. This proof is written before the seal check and before the charge. If no arm survives, STOP and report FALSIFIED.
LIMIT (step 1): with the surviving arms only, print each arm's in-window Brier and the UNIFORM-weight combination's. If no combination of surviving arms can reach the bar arithmetically, STOP and report CLOSED AT LIMIT -- never lower the bar (Q3).
CHANGE (step 2): NEW scripts/platformkit/eval_gate/stacker.py (<=300 LOC):
  fit_meta(arm_preds: np.ndarray, y: np.ndarray, *, method="logit_ridge", regime: np.ndarray | None = None) -> np.ndarray
  stack_predictor(arm_names: Sequence[str], *, fallback: str) -> Predictor
  run_stacker_trial(ticks, arms, *, sport: str, ledger_path: Path, prereg_sha256: str) -> dict
  Four structural requirements, each answering a measured Hedge failure: (1) meta-weights fit on INNER cpcv folds, verdict scored on an OUTER walk_forward; (2) weights conditioned on the regime key (inning bucket minimum); (3) an absent arm is a MASK, never a 0.5 imputation; (4) `fallback` = the single arm used until the inner folds hold MIN_TRAIN rows (regret <= 0 by construction).
  SEAL AND CHARGE ORDER, exactly: (a) verify the prereg file's SHA-256 and embed it in the trial JSON as `prereg_sha256` BEFORE any metric (Q1); (b) then, as the FIRST statement after the seal check, `row = _charge_ledger(LEDGER, "scripts.platformkit.eval_gate.stacker:mlb_stack_v1", "mlb", "2026-06-28", "2026-07-12")` -- `row["k_cumulative"]` is the ONLY K used anywhere and the JSON stores the whole row (Q2); (c) only then compute anything.
PREREG (sealed; SHA-256 embedded as `prereg_sha256` BEFORE any metric) -- VERBATIM: "One charged trial. Corpus: MLB 178-game / 52,558-tick window 2026-06-28..2026-07-12; scored after burn-in and pairing = 158 games / 47,104 ticks. Incumbent: e4_blend alone at in-window Brier 0.207033 on those ticks. AHEAD iff (i) paired Brier improvement >= 0.004, (ii) game-clustered DM 95 pct CI excludes 0, (iii) deflated p < 0.05 at the ledger K READ AT LAUNCH (K=13 at 2026-09-01T23:39:17Z; at K=14 raw p < 0.05/14 = 0.00357). Reported beside the verdict always: PBO via cscv_pbo, the UNIFORM-weight arm's Brier, the guard-only arm's Brier -- if the advantage over guard-only is under the bar the memo names the GUARD. BEHIND is valid and expected; the module is kept either way. SINGLE-WINDOW is labelled in the artifact and the register row; min_corpora_eff printed at launch K."
TEST: NEW scripts/platformkit/eval_gate/test_stacker.py (beside the module), synthetic, fixed seed, tmp `ledger_path` ONLY: one dominant arm -> the stacker's OUTER Brier <= that arm's; a masked arm never contributes 0.5; regime weights differ across two planted regimes. Run ONLY that file: `python -m pytest scripts/platformkit/eval_gate/test_stacker.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = paired Brier vs e4_blend on the outer walk-forward; denominator = 47,104 scored ticks
  before        = 0.207033 (e4_blend in-window on those 47,104 ticks / 158 games)
  bar           = >= 0.004 paired improvement AND the game-clustered DM 95 pct CI excludes 0 AND deflated_p < 0.05 at the launch K
  n             = 47,104 ticks / 158 scored games (the DM clusters; 178 is the CORPUS game count, never the scored one)
  eye check     = n/a (S-row); reproduction = verifier recomputes the paired Brier, the DM statistic and deflated_p from the trial JSON's per-tick series and re-reads k_cumulative from the ledger row stored in that JSON
  must not move = the +0.004 bar, data/cache/eval_gate/backtest_fwer.jsonl (exactly ONE row is appended, by the charge above), every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written)
REPORTED BESIDE THE VERDICT, ALWAYS: PBO via `cscv_pbo`; the UNIFORM-weight arm's Brier; the guard-only arm's Brier (if the advantage over guard-only is under the bar, the memo NAMES THE GUARD). The verdict is labelled SINGLE-WINDOW in the artifact AND the register row, with `min_corpora_eff` printed at launch K (Q5). ESS: report the SCORED predictor's own ICC / design effect / n_eff, labelled -- e4 is 0.207 / 62.4 / 754.5 and Hedge is 0.291 / 87.4 / 539.1; NEVER swap those labels. Do NOT read `ingame/arm_registry.verdict` as a gate (sign defect). BEHIND is a VALID outcome and the module is kept either way.
NON-TAUTOLOGY: all 47,104 scored ticks are in the denominator; ticks dropped by burn-in or pairing are counted and named, and dropping an arm is a step-0 decision written BEFORE the charge, never a post-hoc exclusion of the rows that would fail the metric.
EVIDENCE: docs/evidence/harness/S06_stacker_2026-09-03.md -- the step-0 OOF proof per arm and which arms were dropped, the 1e-9 reproduction check, the prereg sha256 and the ledger row with its K, every arm Brier, the paired Brier, DM CI, deflated_p, PBO, uniform-arm and guard-only Briers, the ESS triple, min_corpora_eff, the SINGLE-WINDOW label, test output, and a NOT VERIFIED list. Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local store only. No deploy before ACCEPT (B5).
COMMIT: explicit pathspec (module, test, prereg file, trial JSON, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
