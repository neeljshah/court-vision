GAP S194 | sport mlb | worktree a12 | log cx_s194_mlb_close_bridge
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- self-check every line of section B AND section Q (Q1-Q9) before you report.
PREMISE (step 0): re-measure first. Measured 2026-09-04 read-only on a clean tree. data/cache/line_history holds 188 files = 180 *.jsonl + 8 per-sport `_freshness.json`, 1.6 GB, 5,298,086 rows across 8 sports (mlb 2,164,041 / soccer 870,688 / tennis 847,692 / soccer_intl 509,823 / wnba 439,174 / nba 251,448 / npb 144,616 / kbo 70,604). MLB: 859 distinct game_id, books pinnacle 827,900 / espn:DraftKings 711,639 / fanduel 347,448 / kalshi 277,054, markets moneyline 1,267,619 / total 631,262 / spread 265,160, captured_at 2026-06-18T21:36:16+00:00..2026-07-27T20:00:28+00:00. Tennis: 480 matches, pinnacle 100 pct, moneyline 293,300 / spread 277,196 / total 277,196, captured_at 2026-07-03T14:56:53+00:00..2026-07-27T20:00:31+00:00.
THE ORPHAN, measured: MLB game_id is an ESPN event id (401815806); data/domains/mlb/games_current.parquet (11,179 rows; target_home_win, home_runs and away_runs all nonnull 11,179/11,179; 841 distinct dates 2022-04-07..2026-07-12) keys on event_id = '2022-04-07-ARI-SDG-7', so the raw key join matches 0 of 859. 686 of the 859 MLB game_ids carry a commence_time at all; 641 of those 686 (93.44 pct; 641/859 = 74.62 pct of the store) have a commence date inside games_current's date set. The 173 game_ids with no commence_time can never be time-anchored. The only derived artifact keeps drift and discards the level: data/domains/cross_sport_market/line_dynamics.parquet = 3,187 rows / 350 games, columns drift_60m / drift_30m / drift_10m / reversal_flag / is_longshot_start / longshot_down_flag / market_bucket -- no close probability; its builder filters minutes_to_close >= 0 at scripts/platformkit/quant/line_dynamics_prep.py:69 with _PRE_CLOSE_MAX_MIN = 2880.0 at :42.
ALREADY KNOWN, DO NOT RE-DISCOVER OR RE-CLAIM: the key-shape cause is recorded verbatim at scripts/platformkit/analytics_showcase/micro_closing_decay.py:61; the store is counted at docs/evidence/LICENCE_LEDGER.md:38 and :360 (180 files, 5,298,086 ticks, licence UNREAD); a partial close ALREADY exists -- micro_closing_decay.py materializes a pre-commence close (accept window 0.0-0.5 h, moneyline HOME side only) for wnba (n=30) and soccer_intl (n=13, flagged underpowered), published at docs/evidence/true-intelligence.md:154-159 with nba/mlb/soccer/tennis/kbo/npb recorded not_joinable; and S10 (161d87c43) already joined a modern MLB moneyline close from mlb_price_series/Kalshi at 913/11,179 = 8.17 pct overall, 63.18 pct of 2026. This lane's DISTINCT contribution is a multi-book, multi-MARKET (moneyline + total + spread) pre-commence close over 2026-06-18..2026-07-27 for MLB. Never claim a first-ever joined MLB close.
LIMIT (step 1): the structural ceiling is quote availability, measured today over the 641 in-window games: 600 already carry at least one quote strictly before commence_time for BOTH moneyline and total (any book), 582 for pinnacle on both, 594 for pinnacle moneyline alone. So the binding work is the (date, team-name) -> tricode bridge and games_current's game_seq doubleheader disambiguation, not the feed. If the bridge cannot reach the bar, report CLOSED AT LIMIT with the exact matched count and the full unmatched name list. Do not fix by widening.
INSTRUMENT HAZARDS, all measured today, all must be printed in the artifact:
  (a) POST-START LAST QUOTE. Taking the last quote per (game_id, market_type, side, book) as "the close" is post-commence for the majority of MLB: 3,630 of 6,174 tuples that have a commence_time = 58.79 pct, median lead -2.78 min; tennis 526 of 2,804 = 18.76 pct, median lead 48.15 min. The close MUST be the last quote STRICTLY BEFORE commence_time. Neither on-disk consumer takes a post-start quote (line_dynamics_prep.py:69, micro_absorption.py:24-26 both keep strictly pregame), so 58.79 pct describes the RAW FEED only -- say so, do not report it as a defect in an existing module.
  (b) captured_at_suspect IS ABSENT, NOT CLEAN. It is present on only 3,366 of 2,164,041 MLB rows and 4,794 of 847,692 tennis rows = 8,160 of 3,011,733 (0.27 pct); every present value is False; the other 99.73 pct carry exactly 12 keys and no such field. Report it as ABSENT (a weaker guarantee than a flag that fired zero times), never as "0 of 3,011,733 suspect".
  (c) NON-TEAM ENTITIES IN home/away. The 641 in-window games carry 44 distinct home/away name values against 30 franchises; 12 are futures/prop strings such as 'Away Runs (13 Games)' and 'Home Runs (14 Games)'. The bridge must REJECT any name not in the 30-franchise map and print the rejected count, never silently drop it.
CHANGE (step 2): the smallest additive change. One new module under scripts/platformkit/ that (i) reads data/cache/line_history/mlb read-only, (ii) takes per (game_id, market_type, side, book) the last quote strictly before commence_time, (iii) bridges game_id -> games_current.event_id on (commence date in US/Eastern, home tricode, away tricode) with game_seq disambiguation for doubleheaders and an explicit 30-franchise name map, (iv) writes ONE new parquet with one row per (event_id, market_type, side, book) carrying line, odds, devigged_prob, captured_at, commence_time, minutes_before_commence, plus the joined target_home_win, home_runs, away_runs. No existing file, column or status value is renamed, removed or rewritten.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = distinct in-window MLB games carrying a pre-commence close (last quote strictly before commence_time) for BOTH moneyline AND total, joined to a settled outcome in games_current (target_home_win plus home_runs and away_runs). Denominator = the 641 in-window games (of 686 with a commence_time, of 859 in the store)
  before        = 0 of 641. Measured 2026-09-04: raw key join 0 of 859; line_dynamics.parquet keeps drift only (3,187 rows / 350 games, no close-probability column); micro_closing_decay.py covers moneyline home side on wnba (n=30) and soccer_intl (n=13) only and records mlb not_joinable at true-intelligence.md:159; S10's 913-game join is moneyline only. Existing partial baseline to cite: scripts/platformkit/analytics_showcase/out/micro_closing_decay.json
  bar           = >= 400 of the 641
  n             = 641 (games; sampled/joined metric, >= 30 satisfied)
  eye check     = n/a (S-row); reproduction = from the new parquet alone, count distinct event_id with a row for market_type moneyline AND a row for market_type total, every such row having minutes_before_commence > 0 and a nonnull target_home_win, and confirm that count against the 641 denominator; independently recount the 641 from data/cache/line_history/mlb + games_current.parquet
  must not move = scripts/platformkit/quant/line_dynamics_prep.py:69 and data/domains/cross_sport_market/line_dynamics.parquet byte-identical; data/domains/mlb/games_current.parquet, every file under data/cache/line_history and every gate corpus read-only; data/cache/eval_gate/backtest_fwer.jsonl byte-identical with K unread and no row charged; scripts/platformkit/analytics_showcase/micro_closing_decay.py and its out/*.json byte-identical; ADDITIVE ONLY -- exactly one new parquet and one new module; calibration language only, no retracted figure
OUT OF SCOPE, AN AUTOMATIC REJECT IF PRESENT: any scored Brier or log-loss, any close-versus-model comparison, any freshness verdict, any sharpening claim. This lane MATERIALIZES and JOINS the close. Scoring it is a separate row that must seal a prereg first (Q1).
NON-TAUTOLOGY: the metric covers all 641 in-window games and nothing is dropped to make the number look good. Explicitly excluded and named in the artifact: the 173 game_ids with no commence_time, and the 45 game_ids that have a commence_time outside games_current's 2022-04-07..2026-07-12 date set (686 - 641). The bar is not free -- the measured ceiling is 600 of 641, so 400 sits 200 below it, and the denominator 641 is fixed before any bridge runs. If the bridge matches fewer than 400, print the exact matched count and the full unmatched name list and report CLOSED AT LIMIT; never lower the bar (Q3).
EVIDENCE: docs/evidence/harness/mlb_close_bridge_2026-09-04.md -- the before/after table against the 641 denominator, the per-book and per-market close counts, the three instrument hazards (a)(b)(c) with their exact numbers, the rejected non-team name list, the doubleheader disambiguation count, and a NOT VERIFIED list. Copy under docs/evidence/ a summary JSON plus a sample of the joined rows EVENLY SPACED over the matched games, so the headline is reproducible from the artifact alone (Q9).
TEST: exactly one new per-file test; run only that file. Never run the full suite.
POD: not needed -- local read of 2,164,041 MLB rows only. No scp, no git on the pod.
COMMIT: explicit pathspec, in the worktree, no push. Never `git add data/` or `vault/`. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
