GAP G402 | sport basketball | worktree a13 | log cx_g402_mixed_provenance_target_mask
**RANK 3 - MEASURE WHAT SURVIVES A CONSERVATIVE TARGET MASK. Landed G380 at 7fdafd0e: docs/evidence/tracking/g380_producer_provenance_2026-09-10/summary.json SHA-256 2155b6f861fd2a611e61488f6ce7bb42af7d231c1860bfde324bee7b7c47dba9 gives 85,333 CLAMP/2,749 SUBPIXEL; docs/evidence/tracking/g380_producer_provenance_2026-09-10/PROPOSED_g380_producer_provenance.diff SHA-256 174e3c203cf84e658d7e0c2fb81e9d175aaf063b5ca7bc6f3d23f51de499481b supplies scratch instrumentation. Verification-pending docs/evidence/tracking/g397_mixed_source_producer_census_2026-09-11/summary.json SHA-256 b6c31fc1a1f2341c37165bcee83357ec20b9fe11f4a3d267a4cfb32de68ac251 motivates measuring both native format groups.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 25 min; pod a13 bounded single-arm GPU replay 85; PC native audit/mask tables 45, reproduction/test/receipts 25. Uses idle lane GPU after actual VRAM check; no PC GPU and no G395 live dependency.
OWNS: scripts/platformkit/tracking/g402_*.py; tests/platformkit/test_g402_mixed_provenance_mask.py; docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11.md and matching directory. Existing G380 proposal is input; its application is permitted only inside pod lane scratch.
PREMISE step 0 (BINDING before-condition): require G397 independent disposition, reproduce the G380 class fraction and attempt-transport 30/30 while preserving identity 0/30. Census ALL retained native objects and receiver receipts for actual 1080p30 and 720p60; require >=30 unique sections EACH kind with bound bytes and >=10 s usable spans.
If source quota is absent, report PARTIAL supply before any GPU launch; do not create 30 recycled windows from a handful of sections or infer retention from old paths. Verify all G380 patch anchors against one read-only deploy snapshot before constructing scratch; drift blocks measurement pending a separately reviewed proposal.
METHOD (sealed; provenance diagnostic, no teacher or student training):
1. Sort each actual-kind population by competition/game/section/digest; take 30 evenly by floor(j*(N-1)/29+0.5). Archive all 60 planned sections and all rejected/unknown identities; distinct game counts and source competition mix remain explicit, with no causal resolution attribution.
2. For draw ordinal j=0..29 choose the 10 s window starting at usable_start+(j+1)*(usable_span-10)/31; select exact decoded PTS boundaries, retaining original frame-index mapping. Use native pixels/frame rate and source-aware frame limits; no resampling or label-conditioned window choice.
3. G386 retains each complete source off-pod before rotation; decode only the sealed window into row-owned scratch with pixel identity receipts. Apply the accepted G380 instrumentation to the scratch copy only; archive original/modified route-file hashes, weights, environment, actual argv, and patch application anchors.
4. Run ONE instrumented producer traversal/window, sequential GPU use, threads 1, <=600 source frames for 60 fps or <=300 for 30 fps, hard aggregate 85-minute stage deadline. Short window startup changes context: conclusions apply only to these scratch windows. Failures stay in the planned 60, no retry or top-up.
5. Save raw player/ball tables, position_source/source_branch/matched_event_id, evaluated_tick_receipt and note_attempt transport trace. Independently record window decoded PTS, attempted read-frame ids and suspend events; transport agreement alone is not an independent evaluated-boundary census.
6. Require receipt/transport equality for each complete window; reconcile attempted, evaluated, suspended, emitting and zero-output ticks without reconstructing evaluated ticks from player rows. Unknown/incomplete schedules remain UNKNOWN and block the complete per-kind comparison.
7. Held-step share uses byte-equal x/y for the same track on consecutive EVALUATED ticks, no bridging missing ticks; cross-tab current provenance and repeated-coordinate status. Keep CLAMP, SUBPIXEL, HELD and PREDICTION distinct; a live HELD label may again have zero support.
8. Freeze coordinate-target mask: CLAMP/HELD/PREDICTION/SUBPIXEL/UNKNOWN and ANY repeated-coordinate target receive weight 0; absent/invalid matched_event_id also receives 0. DETECTION with a valid matched box is only a CANDIDATE, never certified truth; masks are exports with zero training callers.
9. Export candidate rows/all decoded frames, decoded frames with >=1 candidate/all decoded frames, evaluated ticks with >=1 candidate/all evaluated ticks, zero-output coverage and full provenance frequencies by kind. Name both denominators; include no-row decoded frames and no-shared-player pairs, never only emitting rows.
10. Claude audits 30 evenly selected evaluated ticks/kind in native images, showing every emitted box, its matched detection and mask; include silence as an explicit card. This checks transport/coordinate mapping, not court registration. Suspect mappings stay UNKNOWN; no manual unmasking, inferred player identity or accuracy claim from provenance alone.
11. Historical 96.9 pct means repeated positions often reflect the motion clamp in that measured class; copying them as observed supervision would teach the clamp. Keep all such coordinates masked even if the smaller scratch held share looks better; optical-flow or independently labelled targets require another proposal.
12. Recompute mask/per-section tables and delivered renders twice from saved bytes; provide raw row joins, trace, denominator tables and all native eye cards durably. No A/B quality claim, no identity repair, no producer deployment; any submetric with n<30 unique units is descriptive only.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Mixed replay and denominator accounting | G397 schedules 29/30 and 26/30; historical G380 transport 30/30 | 30 complete unique windows EACH kind; all 60 planned accounted; exact receipt/transport agreement and decoded/evaluated distinction | 60 sections; >=30/kind | 30 even native ticks/kind, silence included | Native bytes, route settings, source/attempt identities | Complete DONE diagnostic; missing PARTIAL; schedule fault NOT VALIDATED |
| Mask correctness and candidate coverage | G380 CLAMP/(CLAMP+SUBPIXEL)=0.968790; mixed yield unmeasured | Zero masked-class/repeated/unbound rows admitted; exact full-denominator coverage table; no minimum surviving yield invented | All decoded frames/evaluated ticks/rows of 60 windows | Same 60 cards with raw-to-mask joins | Frozen mask, UNKNOWN retention, no training caller | Exact export DONE even at zero supply; any admission violation REJECT |
| Traceability and reproduction | Identity 0/30; full-route repeatability unresolved | Every opened source/output retained; tables and delivered renders reproduce exactly twice; no single-run generalization | All 60 planned windows and every failed launch | Same cards; all mapping faults retained | Historical failures, models, flags, deploy tree | Missing evidence NOT VALIDATED; completed diagnostic does not qualify targets |
EVIDENCE: docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11.md plus directory: prereg.md, census.csv, draw.csv, source_receipts.csv, window_pts.csv, route_hashes.json, launch_receipts.csv, raw_tables/, trace/, evaluated_ticks.csv, held_pairs.csv, provenance_counts.csv, target_mask.csv, coverage_per_section.csv, pixel_audit.csv, eye_index.csv, summary.json, repeats.json, renders/, common_receipts/, SHA256SUMS.
TEST: python -m pytest tests/platformkit/test_g402_mixed_provenance_mask.py -q; repeated DETECTION remains masked, missing event id, no bridge across missing tick, empty output denominator, source-to-window frame mapping, label-versus-held distinction and insufficient per-kind sample refusal.
VERSION 2026-09-11 v1 PROPOSED; bounded mixed-source scratch evidence and conservative target mask only. Teacher-student training remains unallocated; fresh detection labels still need independent target validation.

COMMON BINDING CONTRACT (astra round 7, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B including A7/A9/A11/B11/H1/S1/S2/S4/S5, plus Q1/Q3/Q6; each block inherits these rails. Each is codex terra PREPARE plus Claude finisher MEASURE, then independent verification; <=180 minutes including preparation and receipts, no unattended leftovers.
Orchestrator confirms the named free owner and performs the binding whole-set premise census BEFORE dispatch. These read-only proposals do not assert fresh source availability. Missing supply -> PARTIAL with exact inventory; an already completed equivalent -> FALSIFIED. Never replace a failed draw or silently extend a budget.
Each OWNS only its named new helper prefix, one test, memo/evidence directory and named PROPOSED diff; no shared-module, src/, daemon, registry, flag or deploy-tree edits. Future finishing uses explicit pathspecs in its assigned worktree and contract C; this review changes no register or git state.
PREPARE seals protocol/bars alone before any metric, then exact input, source, code, model, environment and configuration identities before dependent work. Keep raw answers, full inherited schemas, all ABSENT/UNKNOWN/failed/planned states, exclusions and NOT VERIFIED; missing is not zero.
Pod work only through ~/bin/pod_run <owner> --ship <owned paths> --fetch <owned outputs> -- <command> in /workspace/wt/<owner>/; source/deploy paths read-only. Gate actual VRAM/CPU load, threads 1, <=6 cores; bulky writes require a successful pod write probe, scratch <2 GB; quota usage UNKNOWN is never zero. Never restart or interrupt a peer.
PC raters share the <=3-active-codex/free-RAM>=2.8-GB gate with G399; two raters at most, queued sequentially if capacity is occupied. No nested rating fleet. Poll own work to completion or terminate it normally at the sealed deadline and report exact unfinished keys.
Durable evidence includes summary.json, per-unit tables, seals, full paths/bytes/native dimensions/fps/PTS/raw digests, runtime/log receipts, even render index, resource costs and receiver readback. Keep replay objects off-pod until all declared readers finish; G386 does not prove same-size in-place mutation detection.
Two fresh processes must reproduce saved-table arithmetic and all delivered render digests from committed code, including matching the delivered tree; this proves arithmetic/rendering only. Single producer runs and ratings remain descriptive. Scan ALL landable text including .log.txt with character-code-built Q6 patterns; print counts only; helpers <=300 LOC, full-package imports, one per-file test each.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy); **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; counts only); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round7_2026-09-11.md (local-only). G397's evidence lands on master after its verify; G399's after its finisher + verify; a premise step that needs either reads the lane tip named by the orchestrator until then.
