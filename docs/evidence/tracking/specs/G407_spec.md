GAP G407 | sport basketball | worktree a10 | log cx_g407_live_rendition_boundary_census
**RANK 2 - MEASURE THE MIX THE SELECTOR ACTUALLY DELIVERS. Landed docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11/summary.json SHA-256 1f5150d4120518b6645d7bb60a86d400795053e991bdea8e9ee070953c36a72b and docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11/provenance_counts.csv SHA-256 e9d54256a6a161710ae8100400f03def131cf5c25812798597de79740d239fa2 supply bounded accounting, not post-selector live results. Candidate docs/evidence/tracking/g405_discovery_format_admission_2026-09-11/summary.json SHA-256 71494eb07f12ef60c142837f01f7383b095f2abfa07e4910b9664dfcf2f70444 is context only until its verified handoff.**
WHERE THIS ROW RUNS: PC preparation 20 min; pod read-only live census/retention then a10 observer-only replay <=110; PC arithmetic/native audit/test/receipt <=50; total <=180 min, no wait for new daemon completions.
OWNS: scripts/platformkit/tracking/g407_*.py; tests/platformkit/test_g407_rendition_boundary.py; docs/evidence/tracking/g407_live_rendition_boundary_census_2026-09-11.md and matching directory only.
PREMISE step 0 (BINDING before-condition): snapshot/hash current discovery/selector files and activation UTC, exact live results/queue paths and field names, and ALL terminal ORIGINAL section attempts from activation to census UTC. Join selected/fetched format receipts to native source digests; an intended fmt or resolution alone cannot establish actual rendition.
Require >=30 distinct retained sections EACH actual admitted rendition before asserting a complete rendition census, including 232; enumerate 270/232/312/301/311/300 and any generic fallback explicitly. Count zero/under-30/unresolved strata as PARTIAL, not a merged 30-fps group; do not dispatch inference until the orchestrator confirms supply and the measured <=110-min replay budget.
LIMIT: at most 30 sections per named rendition, <=180 sections total and two native seconds/section; any extra observed rendition or larger required workload -> PARTIAL with full census, no post-outcome stratum selection. A sparse mix yields an honest supply result now, not a live-feed waiting task.
METHOD (sealed before draw or replay):
1. Freeze the entire post-change attempt population, including failures/pruned sources/unresolved joins, with exact source paths/bytes and terminal timestamps. Deduplicate section identity and use earliest terminal ORIGINAL attempt; repeat attempts cannot increase n. Archive query/game clustering.
2. Within each actual rendition, sort competition/canonical game/section/source digest and draw 30 at floor(j*(N-1)/29+0.5). Report all policy-allowed strata and missing counts; no refill. Retain/re-hash/decode these originals off-pod before the first scratch producer launch.
3. Probe actual native dimensions, codec, presentation timestamps and fps independently of itag; publish 232 counts at 29..31 fps, 25 fps, other and UNKNOWN over ALL 30 planned 232 sections. Keep exact rendition and measured fps as separate columns; no per-fps inference from smaller bins.
4. Seal one centered two-second interval per section using native PTS, earlier ties. Preserve section-start warmup/context if required by the producer; count and budget those reads separately. If bounded equivalent observation cannot fit the deadline, report PARTIAL rather than silently restarting state at the center.
5. Read/hash the ORIGINAL live producer and all five exercised model assets before launch. Reuse G402 event/attempt instrumentation through owned observer wrappers around read-only imported code, without editing source files or altering returns, cadence, caps or thresholds; unavailable observation points -> NOT VALIDATED, with a PROPOSED diff only.
6. Reconcile observer traces against the preserved G402 receipt semantics on saved fixtures before measuring. Separate the observational ORIGINAL live-output census from the instrumented scratch replay; the latter describes this replay, not unseen live internal events or live producer repeatability.
7. Per sealed interval record independent decoded-frame PTS, attempted read ids, evaluated ticks, suspension/no-output ticks, row provenance and shared-track coordinate pairs. --frames may apply AFTER stride; record all executed overruns in raw receipts and exclude them only from explicitly bounded derived tables.
8. Compute evaluated ticks/decoded source frames and held pairs/shared consecutive evaluated-tick pairs per section; publish the no-shared-pair population, zero output and schedule UNKNOWN. Keep HELD provenance distinct from repeated-coordinate held share; transport equality alone cannot certify schedule correctness.
9. Report each rendition separately, plus game/competition counts and failures. G402's 10 s windows and this 2 s live-mix sample are not a paired intervention; do not attribute differences to resolution, selector or fps causally.
10. Inspect 30 exact-even native cards per rendition over its full decision set, including silence/failures; regenerate complete tables/cards twice from saved traces and originals. No producer rerun to improve a rate or claim determinism.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Actual rendition supply/fps | Post-selector distribution unmeasured; 232 does not fix fps | Exact source-format binding and 30 unique sections for EVERY admitted rendition; all empty/missing strata explicit | >=30 sections/rendition, <=180 total | 30 even native cards/rendition | Activation boundary, full population, no replacements | Complete DONE census; sparse/unbound PARTIAL |
| Evaluated boundary/held share | G402 59/60 windows; sealed held pairs 9,063/15,571 | All selected windows reconcile decoded/attempted/evaluated/no-output units; zero derived out-of-window rows; UNKNOWN never scored as zero | 30 sections/rendition, all decoded ticks | Same cards plus every failure receipt | ORIGINAL route behavior, masks, stride, cap | Exact DONE diagnostic; incomplete schedule PARTIAL; observer perturbation NOT VALIDATED |
| Runtime identity/reproduction | G402 model hashes post hoc; live HELD 0 | Every exercised asset sealed before launch; receipt/trace agreement on every measured window; two identical saved-input regenerations | All launches/assets/selected windows | Same decision sets | No live edits/restart or inference-repeat claim | DONE scoped replay; missing identity NOT VALIDATED |
EVIDENCE: docs/evidence/tracking/g407_live_rendition_boundary_census_2026-09-11.md plus directory: prereg.md, ops_snapshot.json, population.csv, source_format_join.csv, draw.csv, source_receipts.csv, model_hashes.csv, route_hashes.json, pts.csv, launch_receipts.csv, raw_traces/, window_counts.csv, provenance_counts.csv, held_pairs.csv, coverage.csv, eye_index.csv, renders/, summary.json, repeats.json, runtime_receipts/, q6_scan.json, SHA256SUMS; NOT VERIFIED separates live observations from scratch inference.
TEST: python -m pytest tests/platformkit/test_g407_rendition_boundary.py -q; 232-at-25 fixture, actual/requested fmt separation, unique-section counts, n<30 refusal, overrun exclusion with raw preservation, silent ticks, observer return parity, parent-schema/Q6 field tests.
VERSION 2026-09-11 v1 PROPOSED; descriptive producer-boundary census only. Missing current supply is a valid PARTIAL outcome, never authority to pool renditions or change the feeder.

COMMON BINDING CONTRACT (astra round 9, inherited by this block):
Common CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6. Each block inherits these rails; the orchestrator confirms NEXT_GAP_ID/free owner and measures its WHOLE-set premise before dispatch. Supplied idle GPU/lane counts alone do not satisfy S2.
Execution: codex terra PREPARE seals protocol/bars alone, then a Claude finisher seals exact inputs and completes measurement/receipts within 180 min. No held-out invocation or threshold sweep, training, daemon restart, flag or registry write. Missing supply -> PARTIAL with planned keys retained; equivalent completed work -> FALSIFIED. No retry or replacement after seeing outcomes.
Ownership: only each named new helper prefix, one test and evidence memo/directory; helpers <=300 LOC, full-package imports. No src/ or other gated-tree edits; proposed changes stay in named evidence diffs, applied nowhere. Shared modules remain untouched. Every finisher commits the COMPLETE explicit file list, including imported helpers, fixtures, logs, renders and result text; no bulk add or push.
Retention/identity: retain selected sources OFF-POD, independently rehash and decode at native resolution BEFORE any pod stage; record original/receiver paths, bytes, dimensions, actual PTS and failures. G402's 16/60 mid-stage prunes make a pin insufficient. Hash EVERY exercised model, route and configuration before launch; post-hoc hashes cannot establish launch identity.
Pod steps use ~/bin/pod_run aXX --ship <owned helpers> --fetch <owned evidence> -- <sealed command>, only /workspace/wt/aXX/; deployed source/model trees are read-only. Gate on measured VRAM/CPU, not directory counts; <=6 CPU cores, threads 1, sequential GPU, <2 GB incremental scratch, successful pod write probe before bulky staging, UNKNOWN quota never zero. Finishers poll their own jobs to completion; no unattended leftovers or peer interruption.
Evidence: bound EVERY derived table to sealed source-frame/PTS windows; retain raw overruns separately, all no-output ticks and UNKNOWNs. Keep full parent schemas and repeat-receipt shape, including identical, runs and each run's stdout/exit/digests, adding aliases where needed. Two fresh processes regenerate delivered tables/cards from delivered bytes; arithmetic/render reproduction never proves inference repeatability.
Text: LF and ASCII; inventory every landable text/code/CSV/JSON/log/result path. Field-aware Q6 scans must inspect prose and claim fields, exempt only exact opaque identifiers and appropriately typed numerical data, and emit pattern indices/counts without matched text. Build all scan patterns/positive fixtures from character codes; no blanket CSV/JSON exemptions. Preserve redaction hashes/semantic parity, SHA256SUMS byte domain and NOT VERIFIED.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage -- the quota guard prunes ledgered sources ~45 min after tracking; hash EVERY exercised model, route file and config BEFORE any launch); finishers poll their OWN background pod jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed source-frame/PTS window and retain overruns separately; a failed planned unit stays an UNKNOWN row; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY text artifact incl. .log.txt receipts (patterns built from character codes; receipts emit pattern indices, never token text); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round9_2026-09-11.md (local-only). G402 and G400 are landed on master; G405's evidence lands after its verify (lane a14 tip 051d2f5a6); G403 is running on lane a3.
