GAP G410 | sport basketball | worktree a13 | log cx_g410_position_box_frame_consistency
**RANK 2 - SEPARATE POSITION BRANCHES FROM BOX COORDINATES. Landed docs/evidence/tracking/g380_producer_provenance_2026-09-10/summary.json SHA-256 2155b6f861fd2a611e61488f6ce7bb42af7d231c1860bfde324bee7b7c47dba9 and trace.csv in that evidence directory SHA-256 0b5a2113cd596b7382985945179eef0a5096b8a2b884fd88ad5ae1d062b24eb9 establish scoped branch labels, not geometry. Landed docs/evidence/tracking/g402_mixed_provenance_target_mask_2026-09-11/target_mask.csv SHA-256 c2c4f57a56330afbc9a7772b61743864aa04d746d9715e0374743e724a79dc71 carries those labels into bounded windows.**
WHERE THIS ROW RUNS: PC source/whole-set premise/seal/retention 25 min; pod a13 observer-only branch trace <=90; PC projection arithmetic/native review/reproduction/test 55; total <=170 min. Independent of G409 completion; no waiting for its result.
OWNS: scripts/platformkit/tracking/g410_*.py; tests/platformkit/test_g410_position_box_frame_consistency.py; docs/evidence/tracking/g410_position_box_frame_consistency_2026-09-12.md and matching directory only.
PREMISE step 0 (BINDING before-condition): enumerate ALL G402 bounded rows and verify its CLAMP 9,354/SUBPIXEL 1,399 counts and schema against accepted G380 branch semantics. Require source-bound labelled raw rows plus recoverable actual route; missing matrix/history is the measurement gap, not license to infer coordinates from screenshots.
LIMIT: court-map x_position/y_position and image-space bbox are different units. A CLAMP may retain an older court point while previous_bb is current; distance from box bottom to that retained point is not directly a localisation error. This row tests calculation/binding, not calibration of the court map.
METHOD sealed before replay:
1. From the full bounded population order distinct ticks by draw_order/frame; draw 30 exact-even CLAMP-bearing and 30 exact-even SUBPIXEL-bearing ticks using floor(j*(N-1)/29+0.5). Shared ticks are processed once but reported by class; insufficient unique class ticks -> PARTIAL without replenishment.
2. Keep all rows at every selected tick, plus each same-track predecessor needed by the branch inside the sealed parent window. Missing initial history is UNKNOWN, never bridged across a window; preserve full source/section/attempt/frame/track/event keys.
3. Seal raw positions, boxes and branch labels, G380 source_branch semantics, source hashes, native/cropped dimensions and archived code. Read the coordinate convention from the executed writer, not from an assumed image extent or column name.
4. Replay selected parent windows using the accepted provenance route already archived by G402, without modifying src/. Owned observer wrappers capture pre/post-update box, raw projected foot, M/M1, crop origin, map dimensions, prior position, jump/clamp/subpixel inputs, rounding and final CSV values. Hash all exercised models first.
5. Forward-project the actual image foot through the archived matrices in their declared coordinate frame; independently reproduce branch and writer operations. Compare exact serialized positions/boxes for the SAME observed invocation, not against a second nondeterministic run as if identities were equal.
6. Trace whether the matrices consume cropped or native pixels; test the declared origin with CONSTRUCT corner/foot points and an independently calculated homogeneous projection. Never add 60 to court coordinates or treat a shared wrong transform as native-pixel proof.
7. Classify every selected row as CONSISTENT_BY_CONTRACT, FRAME_MISMATCH, BRANCH_MISMATCH, STALE_BOX, or UNKNOWN with recorded reasons. Record current-box/retained-position age separately; retaining a prior point under CLAMP/SUBPIXEL is not automatically a defect.
8. Publish per-class counts, every unknown/history failure and coordinate-origin chain. Preserve G380 labels and G402 masks; zero geometric reference promotion, no label relabelling or unmasking. A future fix may be proposed separately after cause is established.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Sampling/branch accounting | 9,354 CLAMP; 1,399 SUBPIXEL rows | Exactly 30 distinct even ticks/class; all selected rows, predecessor requirements and failures retained | 30/class; shared ticks reported once pooled | All selected native frames with box/point frames named | Parent bounds/masks/labels | Complete DONE diagnostic; missing supply PARTIAL; exclusions after outcomes REJECT |
| Position/box contract | Geometry relation unmeasured | Independent same-invocation calculation for >=30 ticks/class; all serialized mismatches exposed; zero unit/frame conflations | 60 planned class-ticks; actual unique n reported | All selected ticks plus CONSTRUCT projections | Raw matrices, rounding, branch thresholds and original output | Complete classification DONE even with defects; missing bindings PARTIAL; no quality acceptance implied |
| Identity/reproduction | G380 cross-run identity 0/30 | Pre-launch identity complete; two table/card reproductions identical; no cross-run identity claim | Every asset/window and selected tick | Same full set | G380 identity limit, producer, all thresholds | Exact DONE; absent launch proof NOT VALIDATED |
EVIDENCE: docs/evidence/tracking/g410_position_box_frame_consistency_2026-09-12.md plus matching directory: prereg.md, input_hashes.csv, source_receipts.csv, population.csv, draw.csv, branch_trace.jsonl, matrices.jsonl, contract_checks.csv, unknowns.csv, construct_cases.csv, summary.json, repeats.json, eye_index.csv, renders/, launch_receipts.json, runtime_receipts/, q6_scan.json, SHA256SUMS. NOT VERIFIED names physical court registration and historical replay identity.
TEST: python -m pytest tests/platformkit/test_g410_position_box_frame_consistency.py -q; cropped/native projection, retained CLAMP point with moving box, SUBPIXEL rounding, missing history, duplicate keys, window bounds, and character-code scanner fixtures.
VERSION 2026-09-12 v1 PROPOSED; independent diagnostic using G380 semantics, not G395 live authorization or a geometric truth set.

COMMON BINDING CONTRACT (astra round 10, inherited by this block):
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6; each block inherits these rails. Orchestrator rechecks free owner, accepted dependencies and WHOLE-set premise before dispatch; changed artifact bytes require an explicit prospective handoff, never silent substitution.
Execution: codex terra PREPARE seals protocol/bars alone; Claude finisher retains inputs and completes measurements, test, reproduction and memo within 180 min. Each row owns only its named helper prefix, one test, memo/directory and any named PROPOSED diff. They are not alone in the repo; never revert peer edits.
Retention: BEFORE ANY pod stage, finishers retain every selected original OFF-POD, independently rehash/read back native bytes and record paths, bytes, dimensions, rational PTS and failures. A pin/hash alone is insufficient; no replacements after outcomes. Missing inputs -> PARTIAL immediately, not a wait.
Identity/resources: hash EVERY exercised model, route, library configuration and actual loaded asset before launch. Pod steps use ~/bin/pod_run aXX --ship <owned helpers> --fetch <owned evidence> -- <sealed command>, only /workspace/wt/aXX; original source trees remain read-only. Gate on measured free VRAM/CPU and a pod write probe, not lane count; <=6 cores, threads 1, sequential GPU, <2 GB incremental scratch, quota UNKNOWN never zero.
Bound every derived table to sealed frame/PTS windows; archive overruns separately and retain silence, failed keys and UNKNOWNs. Preserve full parent table schemas and repeat shape: identical, runs, each command/stdout/exit/digests. Two fresh processes reproduce delivered tables/cards from delivered bytes; this does not prove inference repeatability.
All helpers <=300 LOC, full-package test imports, exactly the named per-file test. No src/ or other gated-tree edits; only PROPOSED diffs under docs/research/organization-sprint/, applied nowhere. No daemon restart, shared-module change, held-out invocation/sweep, training, flag change or registry write.
Write LF/ASCII. Field-aware Q6 scans cover every delivered code/text/log/table/result claim field; exempt only exact opaque identifiers and typed numerical data, never whole file types. Build patterns and positive fixtures from character codes; emit pattern indices/counts only. Preserve semantic redaction receipts and the declared SHA256SUMS byte domain.
Finishers commit the COMPLETE explicit file list, including imported helpers, fixtures, raw observations, renders and receipts; no bulk adds/push. Poll owned work to completion; never stop to wait. Premise false -> FALSIFIED memo; supply/resource failure -> scoped PARTIAL/NOT VALIDATED closure; second failed attempt -> CLOSED AT LIMIT, no repeated discovery loop.

ORCHESTRATOR FOOTER (binding, 2026-09-12): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage; hash EVERY exercised model, route file and config BEFORE any launch); finishers poll their OWN background pod jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed source-frame/PTS window and retain overruns separately; a failed planned unit stays an UNKNOWN row; inherited bars byte-identical (no rounding, no added tolerance); never change an existing field's semantics -- add a new field with an alias; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY touched text path incl. the scanner's own source and .log.txt receipts (patterns built from character codes; receipts emit pattern indices, never token text); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round10_2026-09-12.md (local-only). G402, G405 and G406 are landed on master; G403's evidence lands next (lane a3 tip 0a46c0e6c); G407/G408 are in fix cycles (lanes a10/a11); G404 runs on a18.
