GAP S191 | sport n/a | worktree aXX | log cx_s191_ledger_sha_trace
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- self-check every line of section B AND section Q (S-row) before you report.
PREMISE (step 0): re-measure docs/evidence/RESULTS_LEDGER_SYSTEM.md; data line = regex ^\d{4}-\d{2}-\d{2}\s*\|. Measured 2026-09-04 at master f670fbf70: 384 data lines (391 physical); final pipe-field resolves via `git cat-file --batch-check` to a commit for 276/384 = 71.88 pct (108 = 28.12 pct do not); 66/384 = 17.19 pct carry NO resolvable sha in ANY field; histogram {4:1, 5:37, 6:308, 7:12, 8:13, 10:4, 11:2, 12:2, 13:2, 14:1, 17:1, 24:1} so 76/384 are not the six fields the header at :3 declares; 0 lines carry the literal `uncommitted:`. The 198 `| hook |` lines are 198/198 resolvable -- the defect is hand-written lines only. Examples: :219 and :232 put the memo path where the commit belongs, :112 `PENDING`, :307 `PENDING_SHA`, :309 `PENDING_SHA2`, :294 and :301 ` pending`, :44 `none (a10 working tree, uncommitted)`, plus 11 bare `LANDED` finals. The file is append-only and STILL GROWING (372 -> 378 -> 384 in one day), so re-measure N yourself and report YOUR N. If untraceable is already 0, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): for each untraceable line try in order (a) `git log --diff-filter=A --format=%H -- <memo path named in the line>` when any field is a docs/evidence/harness/*.md path, (b) `git log --format=%H --grep=<S-id>` kept only when the subject carries that S-id AND a verdict word, (c) neither. Report the (a)/(b)/(c) split. Do NOT source a sha from a register row: there is no S16b row in docs/evidence/HARNESS_GAPS_2026-09-03.md (054eea081 sits inside the S16 row prose at :57, whose own LANDED sha is 47a66bd7b). Class (c) is not a failure -- it lands as `uncommitted:<reason>`.
CHANGE (step 2): ONE new file scripts/platformkit/tracking/ledger_sha_audit.py (stdlib only, <= 300 LOC). Default = report the two counts, the histogram and the untraceable line numbers. `--fix` APPENDS ` | <sha>` or ` | uncommitted:<reason>` to the end of an untraceable line and nothing else. Never rewrite, reorder, delete or re-wrap an existing line; never touch a line that already resolves.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = untraceable data lines (no token in the line resolves to a commit via `git cat-file`, AND the line carries no `uncommitted:` token), over every data line matching ^\d{4}-\d{2}-\d{2}\s*\| in docs/evidence/RESULTS_LEDGER_SYSTEM.md
  before        = 66/384 = 17.19 pct untraceable (measured 2026-09-04, master f670fbf70); secondary: final-field-resolves 276/384 = 71.88 pct
  bar           = 0 untraceable lines over the lane's own re-measured N (N >= 384), and final-field-resolves not below 276/N. Fixed now, never moved.
  n             = 384 (CONSTRUCT -- every data line enumerated, none sampled)
  eye check     = n/a (S-row); reproduction = run `python scripts/platformkit/tracking/ledger_sha_audit.py` in MASTER and reprint the untraceable count, the final-field count and the histogram over the same denominator
  must not move = for every pre-existing data line the text BEFORE the appended field is byte-identical; the header at :3; the 198 `| hook |` lines untouched; data/cache/eval_gate/backtest_fwer.jsonl 18 rows md5 a4ae7c13995672e478d59770591b83ba
NON-TAUTOLOGY: the metric covers EVERY data line including the 198 hook lines that already pass; nothing is excluded. Appending `uncommitted:<reason>` where a sha is genuinely unrecoverable is the honest terminal state, not an exclusion -- report the class (c) count in the memo and in the ledger line.
EVIDENCE: docs/evidence/harness/ledger_sha_trace_2026-09-04.md -- before/after table, N, the (a)/(b)/(c) split, the untraceable line-number list before and after, a NOT VERIFIED list; copy the audit's summary JSON under docs/evidence/.
TEST: exactly one new per-file test scripts/platformkit/tracking/test_ledger_sha_audit.py; run only that file.
POD: none -- local text audit. No ssh, no scp, no deploy.
COMMIT: explicit pathspec in the worktree, no push. Report the sha.
NEVER PARK: poll your own jobs in a blocking loop; never end waiting.
