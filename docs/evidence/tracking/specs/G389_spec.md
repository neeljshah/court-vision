GAP G389 | sport basketball | worktree a11 | log cx_g389_ball_reference_completion
**RANK 2 - COMPLETE THE REFERENCE BEFORE DECIDING SUPPLY. Landed P384/queue_reconciliation.csv SHA-256 4c308dcbcbe8cdcc43391b1363979f2bb5bee82a276cb9e3beda60863a1da076 and P384/adjudications_g384.csv SHA-256 ee3fe128863f283411637151831bd08019187e923a4c93df6727f88300fffcf7 match their memo and current bytes: 506 pending, 90 completed decisions preserved. P373/reference_v2_summary.json SHA-256 d3bbf294e9f5f7c4fec9fa60e3ff9b232bf9aad39ecf992a644e727d0faeb250 binds the native-reference usability rule.**
WHERE THIS ROW RUNS: PC step 0/PREPARE 25 min, blind native adjudication 125, merge/audit/receipts 30; no training, inference, GPU or pod wait. a11 requires exclusive G384 handoff; raters consume the shared PC capacity.
PREMISE step 0 (BINDING before-condition): join all 1,620 manifest keys to P373 reference plus ALL 90 G384 decisions; reproduce 1,114 settled, 506 pending =301 development+205 held-out, 312 counted dev boxes, settled held-out 157/161/26 and zero candidate executions.
Author hash-checked all 506 pending native sheets in P373/sheets/: 96,460,210 bytes, all match sheet_manifest_all.csv. Recheck every pending sheet and actual decoded dimensions; missing pixels stay explicit. Queue already complete -> FALSIFIED with receipt; changed counts require a new whole-set seal, not stale dispatch.
METHOD (sealed before any new judgment):
1. Freeze pending keys by stable frame_key after completed-key subtraction, plus original 332/549 split and fixed 739 extra-development keys. Two adjudicators receive disjoint tasks; no concurrent G384/G373 consumer may claim these keys.
2. Sort pending keys by split/game/section/frame_index/frame_key, partition into 30 contiguous equal-quantile bins, and round-robin one next key per bin until exhausted. Archive the complete deterministic permutation and bins before dispatch; milestones 30/120/240/360/506 span the full queue.
3. Codex raters adjudicate from blind full native sheets FIRST, recording VISIBLdocs/evidence/tracking/ABSENT/UNKNOWN and native centre/diameter or box without predictions, old crops or split labels. Only after that judgment is sealed reveal the two primary annotations for reconciliation; Claude resolves conflicts from pixels, retaining uncertainty.
4. Duplicate an independently sealed even 30-key audit across the two adjudicators; these are audit repeats, never extra frames or boxes. Archive both original judgments and Claude's resolution; report agreement descriptively, not accuracy against truth.
5. Gate each batch on RAM and image-tool access; verify output-file count, key uniqueness and image-open receipts, not exit status. A pre-output refusal can be reassigned under the sealed failure policy; never repeat a completed judgment to improve its label. Append completed keys atomically and make restart allocation exclude them.
6. At each checkpoint merge by stable key, preserving the original 1,024 reference rows and 90 later decisions. Report settled/unsettled by split and reason, dev positives, UNKNOWN and usable boxes; do not stop at 500 positives or extrapolate from a completed prefix.
7. Export complete full-schema reference_v3.csv, frames_v3.csv (sheet_scale=1.0), dev_boxes.csv and task/accounting tables. Retain inherited centre/diameter representation and audit any derived square-box conversion; count unique, valid native boxes only, deduplicating causal neighbours and all held-out game/context overlap.
8. Preserve primary-rater usability: median disagreement <=0.5 x median native diameter, >=30 pairs with both populations named; retain p90/max and missingness. Adjudication cannot erase disagreement or automatically readmit the old centre-gap queue.
9. Audit 30 evenly selected VISIBLE keys with planted native matching and transform roundtrip; these are scorer controls, not detector results. Retain every ABSENT/UNKNOWN/missing key in the reference census; UNKNOWN is a final visual judgment only after actual review, never a synonym for an unvisited task.
10. Finish the entire 506-key sweep. At deadline report exact remaining keys PARTIAL; do not declare supply closed from the projection. Only a complete audited census below 500 boxes closes this fixed supply; >=500/>=5 games permits G390's premise check, not automatic scoring.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Reference task completeness | 1,114/1,620 settled; 506 pending | All 506 reviewed, 1,620 states represented, zero duplicates/dropped completed decisions | 506 distinct pending keys; all 1,620 retained | 30 even backlog native frames, plus 30 independent audit keys | G373/G384 decisions, split, primary labels, fixed sample | DONE for reference completion; incomplete PARTIAL |
| Audited development supply | 312/500; A1 projection 457 [414,502] | >=500 unique valid boxes/>=5 development games for readiness only | Full 1,071 development-key census; >=30 audit controls | 30 even VISIBLE native matches | Causal-neighbour dedup, no held-out context use | Complete below quota CLOSED AT LIMIT for this supply; otherwise READY FOR G390 CHECK |
| Reference usability/identity | Median 17.33 px; p90 287.09; 715 pairs | Inherited median rule, exact source bytes, native transforms, both held-out quotas >=150 | >=30 pairs; 549 held-out keys retained | All 30 independent audit comparisons | No fabricated agreement, historical evidence and detector settings | Failure NOT VALIDATED; detector quality NOT MEASURED |
EVIDENCE: docs/evidence/tracking/g389_ball_reference_completion_2026-09-11.md and matching directory: prereg, full key/sheet census, sweep/bins, raw judgments, reconciliations, checkpoint tables, reference_v3.csv, frames_v3.csv, dev_boxes.csv, duplicate/context audit, transform controls, readiness.json, summary.json, renders/ and common receipts.
TEST: python -m pytest tests/platformkit/test_g389_ball_reference_completion.py -q; test restart after partial batch, overlapping allocations rejected, every completed decision retained, unvisited versus reviewed UNKNOWN, full-schema merge, even checkpoints and no quota-based early stop.
VERSION 2026-09-11 v1 proposed; corrects the earlier supply-closure recommendation and preserves G384's execution history without reclassifying its past run.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` over every byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/` READ only (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round3_2026-09-11.md (local-only).
