GAP S10 | sport mlb (corpus) | worktree a10 | log cx_s10_mlb_modern_close
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): MLB odds end 2021-11-02 while `games_current.parquet` runs to 2026-07-12, so all 22 MLB mechanisms are NOT_TESTABLE against a modern close.
READ (exact symbols, re-checked on disk 2026-09-03): scripts/platformkit/eval_gate/close_join.py -- `JoinSpec(sport, spine, date_col, side_a, side_b, fallback_a, fallback_b, name_a, name_b)`, `close_column(odds, spec) -> pd.Series` (devig2 -> P(y=1); null / zero / <=1.0 prices DROPPED and COUNTED), `coverage_report(sport) -> dict`. This module DOES NOT EXIST YET -- it lands with S02 (spec docs/evidence/tracking/specs/S02_spec.md). Run this gap AFTER S02 has landed in master; if close_join.py is absent, STOP and report BLOCKED ON S02, do not write a second joiner. Also read scripts/platformkit/pm_trading/close_capture.py `CloseResult` (:86), `_kalshi_close` (:124), `_line_store_close` (:206), `_kx_venue_close` (:235), `capture_close` (:269) -- the existing close-derivation vocabulary; reuse it, do not reinvent devigging.
PREMISE (step 0) -- this is a CORPUS row, so step 0 is a MEASUREMENT, not a grep. Print, from disk, with exact row counts and min/max dates: (i) data/domains/mlb/odds.parquet (expected 28,004 rows, date 2010-04-04..2021-11-02); (ii) data/domains/mlb/games_current.parquet (expected 11,179 rows, date 2022-04-07..2026-07-12, the acceptance denominator); (iii) EVERY other MLB odds/close candidate on this box -- glob data/domains/mlb/*odds*.parquet and data/domains/mlb/*close*, and grep data/cache/inplay_odds/, data/domains/mlb/ and scripts/platformkit/pm_trading/close_capture.py for kalshi / close / devig. Already measured 2026-09-03 and to be re-measured, not assumed: data/cache/inplay_odds/mlb_price_series.parquet holds 13,473,591 rows, game_date 2023-03-30..2026-07-09, columns sport, venue, game_date, ticker_or_slug, event_key, market_type, side, ts, prob, traded, close_time, result_where_known -- a MODERN local price series, so the "no modern close exists" reading of this row is FALSE for 2023+. Its `ts` did not parse as datetime nanoseconds; determine the unit before using it. Premise holds iff odds.parquet still stops in 2021 and games_current still runs to 2026 -> before = 0.0 pct join rate on 2022+. If odds.parquet now covers 2022+, STOP, write the memo, commit, report FALSIFIED (a valid result).
LIMIT (step 1): the coverage ceiling. Season 2022 has NO row in mlb_price_series (it starts 2023-03-30), so the 2022 games in games_current are unjoinable from local sources and that shortfall is a FIXED part of the denominator -- print the 2022 game count and its share of 11,179. Then print, per season 2022..2026, the count of games_current rows for which a pre-first-pitch close can be derived (a moneyline/winner market row whose ts or close_time precedes the game's start, both sides present, prices strictly > 1.0 after devig). If the resulting overall achievable rate on 11,179 rows is BELOW the bar, STOP and report CLOSED AT LIMIT with the per-season table -- do NOT lower the bar and do NOT acquire a paid source. If NO modern close source exists locally at all, that is also CLOSED AT LIMIT and a valid result.
CHANGE (step 2): NEW scripts/platformkit/eval_gate/close_join_mlb.py (<=300 LOC). It IMPORTS JoinSpec / close_column / coverage_report from close_join.py and adds only: the MLB `JoinSpec` (spine = games_current.parquet, date_col = "date", key = event_id, sides = home/away), a `derive_modern_close(price_series_path, spine) -> pd.DataFrame` that reduces mlb_price_series to ONE pre-first-pitch two-sided quote per event_id (last tick strictly before game start; both sides required; a one-sided or post-start quote is DROPPED and COUNTED), and `coverage_report_mlb() -> dict` returning the overall and PER-SEASON join rate, the drop counts by reason, and the count of games with no candidate quote at all. No second joiner, no edit to close_join.py, corpus_cache.py, backtest_runner.load_states or the gate-corpus builder. Nothing is written under data/domains/ or data/registry/.
TEST: NEW scripts/platformkit/eval_gate/test_close_join_mlb.py (beside the module): a synthetic 40-game spine plus a synthetic price series where 30 games have a valid two-sided pre-start quote, 5 have only a post-start quote, 3 are one-sided, 2 have no quote -> join rate exactly 30/40, the three drop reasons counted exactly 5 / 3 / 2, per-season rates sum back to the overall, a 2.00/2.00 pair devigs to 0.5000, and no row is silently dropped (drops + joins == 40). Run ONLY `python -m pytest scripts/platformkit/eval_gate/test_close_join_mlb.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = join rate on 2022+; denominator = 11,179 `games_current` rows
  before        = 0.0 pct
  bar           = >= 60 pct with per-year rates printed (roadmap section 2, verbatim; fixed here now that the source is known, then never moved). Below it -> CLOSED AT LIMIT with the per-season table, never a lowered bar.
  n             = 11,179 spine rows (scored), plus the 40-row synthetic construct in the test
  eye check     = n/a (S-row); reproduction = verifier reruns coverage_report_mlb() in master and re-prints the overall and per-season rates and the drop counts
  must not move = data/domains/mlb/** (READ-ONLY; never rewritten), scripts/platformkit/eval_gate/close_join.py (S02's file, untouched), corpus_cache.py, backtest_runner.load_states, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows; No `_charge_ledger` call -- this is a coverage measurement, not a scored trial)
NON-TAUTOLOGY: the denominator is ALL 11,179 games_current rows, including the 2022 season that no local source can cover. Restricting the denominator to seasons that happen to have quotes is exactly the circular metric B1 forbids -- report the honest fraction of the full spine and name every excluded row with its reason.
EVIDENCE: docs/evidence/harness/S10_mlb_modern_close_2026-09-03.md -- the step-0 inventory of every local MLB close candidate with row counts and date ranges, the per-season limit table, the coverage_report_mlb() output, the drop counts by reason, exact commands, test output, and a NOT VERIFIED list (say plainly whether the derived quote is a true settled close or a last-pre-start tick, and that no mechanism has been re-scored in this gap). Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local parquet only.
COMMIT: explicit pathspec (module, test, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
