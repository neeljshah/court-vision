GAP S27 | sport all (analytics) | worktree a11 AFTER S23 | log cx_s27_envelope_test | THE TEST IS EXPECTED RED TODAY -- RED IS THE HONEST DELIVERABLE
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): the answer contract is documented but not mechanical: nothing enforces `source_artifact` + `as_of` + verdict on every answer, or a refusal in their absence.
READ (read every signature on disk; line numbers drift): scripts/platformkit/mcp_server/tools.py `TOOLS`, `handler_for(name)`, `tool_specs()` and the nine handlers it defines; scripts/platformkit/mcp_server/artifact_tools.py `tool_specs()` and the five handlers it defines; scripts/platformkit/answers/resolver_registry.py -- the envelope docstring naming `status` in `ok | refused | not_supported | no_data` plus `category, sport, source_artifact, as_of`, and the 7-day injuries/news staleness constant; scripts/platformkit/analytics_verify/answers.py `STALENESS_HOURS = 48.0`; docs/AI_CONSUMER_CONTRACT.md (129 lines); tests/platformkit/mcp_server/ (the package already exists: test_artifact_tools.py, test_server.py).
PREMISE (step 0): VERIFY EVERY TOOL NAME EXISTS before writing a probe for it. The 13 in scope are ask, scouting_report, comparables, matchup_preview, win_probability, injury_report, analytics_receipts, system_health (all in tools.py) and strength_atlas, mechanism_exposure, tracking_program_status, harness_health, execution_status (all in artifact_tools.tool_specs()); `run_burst` is EXCLUDED -- it is an action returning ok|aborted. Assert `handler_for(name)` is not None for all 13 and print the list. Also confirm `docs/evidence/answer_probe_50.json` is ABSENT and `tests/platformkit/mcp_server/test_envelope_contract.py` is ABSENT -- before = 0 probes enforced. If either exists, or a name resolves to None, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): run all 13 handlers ONCE with minimal arguments and record each returned envelope's `status`, `source_artifact` and `as_of`. That table IS the limit and it tells you exactly which probes will be red before you write one line of test.
CHANGE (step 2): TWO new files and NOTHING ELSE. No handler, resolver or dispatch module is edited by this job.
  (a) NEW `docs/evidence/answer_probe_50.json` -- 50 fixed questions spanning four sports x the supported categories, plus unsupported categories, plus edge/ROI-language probes. Each entry names the tool, the arguments, and the expected envelope class.
  (b) NEW `tests/platformkit/mcp_server/test_envelope_contract.py`, driving each probe through the SAME `tools.handler_for(name)` the server uses, asserting per probe: `status` is one of the four values and nothing else, ever; `ok` => `source_artifact` and `as_of` are non-empty and `staleness_days` is present when the artifact is a file; not-`ok` => `source_artifact` (or the category default) plus `note`, and NO numeric answer field; an edge/ROI probe => `refused`, citing `.claude/rules/no-edge-claims.md`; an unknown category => `not_supported`, never `ok`.
  STALENESS POLICY, asserted by the test: injuries/news 7 d -> refused (exists, resolver_registry); analytics_receipts 48 h -> refused (exists, analytics_verify/answers.py `STALENESS_HOURS`); execution_status 48 h -> refused (the same constant, NEW here); pinned corpora (win_probability, profiles, calibration scoreboard) -> report `staleness_days`, no refusal. `as_of` describes the DATA, never the call.
TEST: exactly one new per-file test -- `python -m pytest tests/platformkit/mcp_server/test_envelope_contract.py -q`. Run ONLY that file. It is EXPECTED TO FAIL TODAY.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = probes whose envelope carries `source_artifact` + `as_of` + a valid status/verdict; denominator = the fixed 50-question probe
  before        = 0/50 (no probe file, no contract test, nothing enforces the envelope across the MCP surface)
  bar           = 50/50 with every unsupported question REFUSED rather than answered -- AND, because that bar is not reachable today, the ACCEPTED deliverable is the probe file + the test + a memo naming EXACTLY which probes are red and why; a green-looking run is a REJECT
  n             = 50 (scored)
  eye check     = n/a (S-row); reproduction = verifier reruns the test in master and confirms the red set matches the memo's list probe-for-probe
  must not move = the envelope status semantics (`ok | refused | not_supported | no_data`), the refusal path, the 7 d and 48 h staleness constants, every handler in tools.py and artifact_tools.py, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows)
KNOWN DAY-1 REDS -- fix later, NEVER soften the assertion: `system_health` returns no `source_artifact`/`as_of` (P3); `win_probability` stamps `as_of = now()` over an ~80-day corpus (P2); `harness_health` is `no_data` until S23 lands its generator; `execution_status` reads a July backup until the execution readout lands.
OUT OF SCOPE: the P1/P2/P3 diffs in MCP_ADVANCE_2026-09-01.md section 2 (wiring `gate_manifest_tool.tool_specs()` into tools.py; the winprob corpus `as_of`/`computed_at`/`corpus_staleness_days`; `_system_health` citing its three artifacts) are HUMAN-GATED -- they need a live MCP server restart. Do NOT apply them, do NOT drop the `stable_as_of=False` exemption in `test_answer_consistency_intel.py`. Report them as proposed; the human applies them.
NON-TAUTOLOGY: all 50 probes are in the denominator; a failing probe is REPORTED, never deleted, softened or moved to an expected-failure marker to make the run green. No `_charge_ledger` call anywhere -- this is a contract test, not a trial.
EVIDENCE: docs/evidence/harness/S27_envelope_test_2026-09-03.md -- the 13-name existence check, the step-1 envelope table, the probe-by-probe pass/red list with the reason for every red, the exact command, the test output, and a NOT VERIFIED list. Calibration language only (Q6): no dollar, ROI, profit or edge word; RED here is a SUCCESS.
POD: none. Local only. No deploy before ACCEPT (B5).
COMMIT: explicit pathspec (the probe JSON, the test, the memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
