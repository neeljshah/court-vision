GAP G414 | sport basketball | worktree a14 | log cx_g414_retained_pool_day_census
**RANK 3 - MEASURE SUPPLY BEFORE BALL CONTINUATION. Landed docs/evidence/tracking/g403_ball_rater_failure_controls_2026-09-11/instructions.md SHA-256 7b73ab53a26c9e4a105223f4d633481d92d507c3215381204209c1789e2ddd12 fixes controls; docs/evidence/tracking/g407_live_rendition_boundary_census_2026-09-11/summary.json SHA-256 10991cc76784ad54f937603dd3ec859864ddd08479edd68254d605ba085c122d fixes historical supply limits. Pending docs/evidence/tracking/g404_play_gated_ball_growth_stage2_2026-09-11/premise.json SHA-256 8ec0803b8e1e315e5d184bf43d171e8a3888e1b40ca7cfc74a4906bf37c9f85d defines the 101 old identities; require accepted handoff before claiming G404 readiness.**
WHERE THIS ROW RUNS: PC only AFTER the 24-hour endpoint: snapshot/seal 20 min, whole-pool receipt/probe/identity census 80, native eye checks 35, reproduction/test/memo 35; <=170 min. No 24-hour sleeping job; an early dispatch closes PARTIAL and schedules nothing.
OWNS: scripts/platformkit/tracking/g414_*.py; tests/platformkit/test_g414_retained_pool_census.py; docs/evidence/tracking/g414_retained_pool_day_census_2026-09-12.md and matching directory. Receiver objects/state/logs remain read-only.
PREMISE step 0 (BINDING before-condition): enumerate the COMPLETE receiver manifest and establish T0 from its first actual receipt, currently 2026-09-11T21:25:17Z; T1=T0+24 h=2026-09-12T21:25:17Z. Require stable archived receiver rule/config receipts and snapshot coverage through T1; never select a later favorable window. Missing coverage/identity -> scoped PARTIAL.
LIMIT: supplied ~30 games/day is an expectation, not a measurement. Today's local snapshot has 19 retained video ids and no known titles; successful transfer does not prove NEW canonical games. To pass supply require >=30 retained resolved NEW games and >=2 competitions, not 30 sections or attempts.
METHOD sealed:
1. Copy the immutable receipt prefix through T1 into owned evidence; freeze hashes, first/last byte offsets, UTC fields and receiver rule/version. Hash read-only state/config and record any epoch changes; bind measurements to [T0,T1), with the endpoint receipt proving coverage only.
2. Census every attempt using receipts.jsonl fields utc,status,ytid,section,rule,pc_path,sha256_pc,sha256_pod,bytes,width,height,avg_frame_rate,fetched_fmt. Deduplicate separately by attempt, video id, source digest and canonical game; never count repeated GONE polls as new failed games.
3. Verify retained objects from the WHOLE census by PC hash/readback and ffprobe; preserve missing/pruned/hash-failed objects as named failures. Capture full decoded-frame count/rational PTS, not packet totals. Stop at the time budget with explicit NOT MEASURED rows, never omit them.
4. Resolve game identity from retained discovery/title/source receipts and the existing canonical resolver, recording exact evidence for competition/date/participants and alternate uploads. Rebuild the union of ALL G404 old DEV/held-out/context/control identities; an unresolved OLD identity must be reconciled or conservatively prevent a disjointness pass.
5. Publish every unresolved, duplicate and exclusion reason before drawing. UNKNOWN titles/aliases are not negative matches; ytid/source-digest inequality is insufficient. Never infer distinct games solely from section ids or competition tags.
6. Report cumulative unique retained/resolved/new games at fixed six-hour boundaries and whole-day rates, loss counts/bytes and identity-resolution fraction /all distinct retained video ids; first-pass backlog and post-start arrivals remain separate. Preserve all epochs and no source replacement.
7. If >=30 resolved NEW games exist, sort by canonical competition/game/source hash and draw 30 exact-even games using floor(j*(N-1)/29+0.5); inspect one midpoint native frame per game and record chosen section, hash, dimensions and identity evidence. Fewer -> PARTIAL; never recycle games to fill n.
8. Give G404 a supply-only GO table: 30 resolved NEW games/>=2 competitions, source durability and per-game decoded duration; independent columns for fresh home, accepted controls/A1, section-selection amendment, blind audit and prospective ten-frame supply remain PENDING until evidenced. No controls or ball rating in this row.
9. Report measured rendition classes using G407's inherited mapping, raw height/rational rates and fetched-receipt coverage descriptively; request/fetched/measured fields stay separate, no class inferred solely from itag. Do not pool across changing producer/selector epochs or claim G407's 30/class prerequisite from 30 games.
ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Day census/reconciliation | Snapshot 19 retained ids; G404 old pool 14 exact-ID candidates | Entire fixed 24 h prefix, every attempt/object accounted and rate reproducible; no game/section/poll conflation | Whole pool; >=30 unique retained games for sampled pass | 30 even native frames if available; actual shortfall named | Receiver rule/state, T0/T1, source bytes | Complete census DONE diagnostic; missing coverage PARTIAL |
| G404 supply prerequisite | 30 required, canonical disjointness unresolved | >=30 retained resolved NEW canonical games, >=2 competitions, all retained-byte checks pass and old-set disjointness proved | >=30 independent games; full pool denominator | Same 30 plus every identity conflict | Old reference sets, game resolver semantics, all frozen gate bars | Pass SUPPLY_GO only; missing identities/supply PARTIAL, no overall G404 GO |
| Durability/reproduction | Rolling pod loss and repeated attempt rows | Two exact saved-input reproductions; receipt fields/parent shape retained; all failures and complete path manifest delivered | Whole census and all 30 selected games | Same full draw | Receiver, feeder, production and historical G404/G407 results | Complete DONE diagnostic; hidden losses REJECT |
EVIDENCE: docs/evidence/tracking/g414_retained_pool_day_census_2026-09-12.md plus directory: prereg.md, input_hashes.csv, receipt_prefix.jsonl, epoch_manifest.json, source_receipts.csv, object_checks.csv, identity_resolution.csv, old_identity_union.csv, exclusions.csv, accumulation.csv, measured_classes.csv, draw.csv, go_conditions.csv, eye_index.csv, renders/, summary.json, repeats.json, runtime_receipts/, q6_scan.json, SHA256SUMS. NOT VERIFIED: future supply, frozen play-gate validity, rater qualification and ball yield.
TEST: python -m pytest tests/platformkit/test_g414_retained_pool_census.py -q; repeated GONE attempts, duplicate uploads, unresolved old aliases, fixed time bounds, source loss, insufficient games, even draw and code-built scanner fixtures.
VERSION 2026-09-12 v1 PROPOSED; finite receiver measurement, separate from G404 continuation; no retroactive repair of G404's audit or G407's epoch.

COMMON BINDING CONTRACT (astra round 11, inherited by this block):
COMMON CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5 and Q1/Q3/Q6 bind every block. Orchestrator verifies the WHOLE-set premise, accepted prerequisites and free ownership before dispatch; supplied lane status is not a reservation.
Execution: codex terra PREPARE seals prereg alone, then Claude finisher completes the bounded measurement, test and memo within 180 min. Seal SHA-256 of every LF byte above the seal line, no intervening blank line; changed input hashes need a prospective handoff, never silent substitution.
Ownership: only each block's helper prefix, one test, evidence memo/directory and named PROPOSED diff; finishers also append their required RESULTS_LEDGER.md line in the same targeted commit. No register edit. They are not alone in the repo and must preserve peer edits; this reviewer writes none of those files.
Retention: before ANY pod stage, independently retain/read back/hash every selected original OFF-POD; record full paths, bytes, dimensions, rational PTS, all failures and existing receiver mappings. These three rows run on PC from retained bytes; no pod stage is needed or licensed by these specs.
Identity: hash every exercised model, route and configuration BEFORE launch; use an explicit empty model set when inference is absent. No new model invocation here. Source/source-frame/window joins are mandatory; branch ancestry never transfers across nondeterministic replays.
Bound all derived tables to sealed frame/PTS or census-time windows; list excluded history and archive raw overruns separately. Keep full parent columns and field meanings, ABSENT/UNKNOWN/silence and failures. Repeat receipts retain identical/runs, each run's stdout/returncode/command and per-table digests, with new fields additive.
No src/, kernel/, api/, intel/, scripts/team_system/, scripts/run_clip.py or shared-module edits; gated changes exist only as named PROPOSED diffs, applied nowhere. No training, held-out threshold sweeps, daemon restart, flag changes, registry writes or source pruning. Helpers <=300 LOC; full-package test imports.
Field-aware Q6 scans cover EVERY delivered code/text/log/table/claim, ledger addition, proposal and scan helper with a complete path manifest. Build ALL patterns and positive fixtures from character codes; emit pattern indices/counts only. Exempt exact opaque identifiers and typed numerical data only, not whole files; preserve numeric claim-field detection parity.
Write ASCII/LF; deliver declared byte-domain SHA256SUMS, all reproduction inputs, receipts and NOT VERIFIED. Run only each named test. Commit COMPLETE explicit paths including helpers/import dependencies, fixtures, raw observations, renders, scans and receipts; no bulk adds or push.
Two fresh processes reproduce delivered tables/cards from saved bytes; that does not establish inference repeatability. Never stop to wait: use bounded owned work, finish or close PARTIAL/NOT VALIDATED at deadline; false completed premise -> FALSIFIED; no repeated discovery loop or automatic extra attempt.

ORCHESTRATOR FOOTER (binding, 2026-09-12): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon, never interrupt a peer lane; the retired `~/bin/pod_run` path does not exist on this pod -- pod work runs over ssh in /workspace/wt/<lane> with bulk scratch under /workspace/g<N>_scratch (per-worktree quota truncates writes silently; verify sha256 after every copy; retain every drawn source OFF-POD and decode it BEFORE any pod stage; hash EVERY exercised model, route file and config BEFORE any launch); the PC pool receiver (C:/Users/neelj/pool_receiver/: objects/, receipts.jsonl, state.json; one section per new video id = the earliest-tracked section) is the accumulated off-pod source pool; finishers poll their OWN background jobs with bounded loops and never stop to wait; bound EVERY derived table to the sealed window and retain overruns separately; a failed planned unit stays an UNKNOWN row; inherited bars byte-identical; never change an existing field's semantics -- add a new field with an alias; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with a field-aware automated scan over EVERY touched text path incl. the scanner's own source and .log.txt receipts (patterns and restricted constants built from character codes; receipts emit pattern indices, never token text, with a complete path manifest); repeat receipts keep the parent shape (identical / runs with stdout + returncode / per-table digests) beside any new fields; write every text file with LF; commit the COMPLETE file list; n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round11_2026-09-12.md (local-only). COORDINATE CONTRACT (from G410, lane a13 tip 92f5894fc, verify pending; astra's sign correction applies: for a stored xyxy box B the native padded box is (Bx1, By1+60, Bx2, By2+60) and the native unpadded detector-form box is (Bx1+15, By1+75, Bx2-15, By2+45), clipped using the cropped dimensions BEFORE restoring the origin; fresh box-based court writes project ((x1c+x2c)//2, y2c, 1) through M then M1; ankle / flow / prediction / retained-point branches are exceptions). G402, G406 and G407 are landed on master; G409 (lane a12) and G411 (lane a19) are in verification.
