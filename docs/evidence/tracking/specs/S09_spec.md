GAP S09 | sport all (harness) | worktree a13 | log cx_s09_manifest_measurement_time
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): gate-manifest staleness does not BLOCK a claim: evidence rows fall back to mtime, so staleness is WRITE time, not MEASUREMENT time.
READ (exact symbols, re-grepped on disk 2026-09-03): scripts/platformkit/eval_gate/gate_manifest.py (213 LOC) -- `_ASOF_KEYS` = ("as_of","frozen_at","at","extracted_at","ts","generated_at"), `_VERDICT_KEYS`, `_as_utc`, `_parse_dt`, `_extract`, `_load`, `_walk`, `_scan`, `_dir_error_row`, `_row_for` (the `effective = _parse_dt(as_of_field) or mtime` line is the whole gap), `build_manifest`, `render_table`, `main` (exit 1 iff unreadable > 0); the row keys name, source_path, category, status, verdict, as_of_field, mtime, staleness_days, error; status values OK / EMPTY / UNREADABLE. Its test scripts/platformkit/eval_gate/test_gate_manifest.py (13 tests incl. test_readable_ledger_row_and_staleness, test_naive_as_of_is_read_as_utc_not_a_typeerror, test_offset_aware_as_of_field_is_not_double_counted, test_vanished_file_becomes_a_row_not_a_crash, test_untraversable_dir_is_recorded_not_silently_dropped, test_cli_rejects_unparseable_as_of_instead_of_defaulting_to_now, test_manifest_does_not_audit_its_own_output). READERS (A5): scripts/platformkit/mcp/gate_manifest_tool.py (filters rows by `status`, passes name/mtime/staleness_days/status/verdict through verbatim) and its test scripts/platformkit/mcp/test_gate_manifest_tool.py -- both must stay green.
PREMISE (step 0): the register says the row "falls back to mtime". On disk `_ASOF_KEYS` ALREADY reads the artifact's own generated_at/as_of, and mtime is only the fallback -- so measure the SCOPED premise, which is three facts: (i) no row says WHICH source its timestamp came from (grep for measured_at / measured_at_source: 0 hits), (ii) no status value STALE exists and no max-age threshold exists anywhere in the module (grep: 0 hits), (iii) run `build_manifest(repo_root)` on this repo and print how many rows got their effective timestamp from mtime rather than a field, out of the total. Premise holds iff (i) and (ii) are zero -> before = 0/3 cases block. If a STALE status or a max-age already exists, STOP, write the memo, commit, report FALSIFIED (a valid result).
LIMIT (step 1): n/a -- CONSTRUCT row (three cases enumerated). The mtime-fallback count from step 0 (iii) is the honest ceiling on how much of the manifest can be judged on MEASUREMENT time today; print it as a fraction with its denominator.
CHANGE (step 2): additive, inside scripts/platformkit/eval_gate/gate_manifest.py only (it is a small module and the fix belongs at the one line that computes `effective`; total additions <= 60 LOC):
  Two NEW row keys, nothing renamed or removed: `measured_at` (ISO string, the effective timestamp) and `measured_at_source` ("field:<key name>" when it came from `_ASOF_KEYS`, "mtime" when it fell back, None when neither exists). `as_of_field`, `mtime` and `staleness_days` keep their exact present meaning and values.
  NEW module-level `class StaleEvidence(Exception)` and NEW `assert_fresh(manifest: dict, max_age_days: float, categories: tuple[str, ...] | None = None) -> None` -- the CLAIM PATH. It RAISES StaleEvidence listing every offending row when a row's `staleness_days` exceeds `max_age_days`. FAIL-CLOSED: a row whose `measured_at` is None, or whose status is UNREADABLE or EMPTY, is STALE, never fresh -- missing evidence must never read as good evidence (B3 does not apply here: this gate's whole job is to refuse an unmeasurable claim, and it quarantines nothing, it raises).
  NEW opt-in CLI flag `--max-age-days FLOAT` (default None = today's behaviour byte-identical). When set, offending rows get `status = "STALE"` in the written manifest and `main` returns 1. With the flag absent, every row, every status and the exit code are exactly what master produces.
TEST: NEW scripts/platformkit/eval_gate/test_gate_manifest_measured_at.py (beside the module). Three enumerated cases against a tmp_path repo: (a) FRESH -- artifact with generated_at 1 day before as_of -> measured_at_source "field:generated_at", assert_fresh(30) does not raise; (b) STALE -- artifact with generated_at 400 days before as_of but an mtime of NOW -> measured_at comes from the FIELD not the mtime (this is the gap), assert_fresh(30) RAISES and names the row, and `--max-age-days 30` sets status STALE and exits 1; (c) ABSENT -- an artifact with no as_of-style field at all (measured_at_source "mtime") and an unreadable/corrupt one -> both treated as STALE by assert_fresh, which raises. Plus an invariance case: build_manifest with no max-age produces the same statuses and staleness_days as master for the same tmp repo. Run ONLY `python -m pytest scripts/platformkit/eval_gate/test_gate_manifest_measured_at.py -q`, then rerun `python -m pytest scripts/platformkit/eval_gate/test_gate_manifest.py -q` and `python -m pytest scripts/platformkit/mcp/test_gate_manifest_tool.py -q` (both must stay green) -- three files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = the block fires; denominator = 3 cases (fresh / stale / absent)
  before        = 0/3 (no measured_at, no measured_at_source, no STALE status, no max-age anywhere in the module -- measured in step 0)
  bar           = 3/3 with absent treated as stale
  n             = 3 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns the three test files in master and re-prints the mtime-fallback fraction from build_manifest on this repo
  must not move = the six existing fail-closed fixes and all 13 existing tests in test_gate_manifest.py, every existing row key and status value (OK / EMPTY / UNREADABLE), the default exit-code semantics with no flag, scripts/platformkit/mcp/gate_manifest_tool.py, every gate value and threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows; No `_charge_ledger` call -- the manifest is READ-ONLY toward it)
NON-TAUTOLOGY: all three cases are scored and case (c) is the one that would be excluded by a convenient rule -- an artifact with no measurement stamp is judged STALE, not skipped. Excluding unreadable/absent rows from the block is exactly the circularity B1 forbids; say so.
EVIDENCE: docs/evidence/harness/S09_manifest_measurement_time_2026-09-03.md -- the step-0 greps, the mtime-fallback fraction with its denominator, the three-case table, exact commands, all three test outputs, and a NOT VERIFIED list (no caller sets --max-age-days yet; S27 is the row that arms it). Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local only.
COMMIT: explicit pathspec (the module, the new test, the memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
