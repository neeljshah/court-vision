GAP G397 | sport basketball | worktree a10 | log cx_g397_mixed_source_producer_census

**RANK 1 - MEASURE THE NEW ADMISSION MIX BEFORE USING IT. Landed G380 at 7fdafd0e: docs/evidence/tracking/g380_producer_provenance_2026-09-10/receipts_trace_a3.csv SHA-256 81f5c530a0ff43e4f57fb8c83f62f9872bcbf09bddee8719ea32c17032888aee gives 30/30 attempt-transport agreement; identity remains 0/30. Landed docs/evidence/tracking/g386_pin_copy_one_pass_2026-09-10/summary.json SHA-256 e0e6119766899786a4004cd34e7587bacc615853cc87674c85ce4d06d297803f supplies retention. Neither measures the new format mix.**

WHERE THIS ROW RUNS: PC step 0/PREPARE 25 min; pod read-only census/probe/snapshot and G386 capture <=90 min; PC receiver, arithmetic, eye review/test/receipt 65 min. No producer rerun or live overlay.
OWNS: scripts/platformkit/tracking/g397_*.py; tests/platformkit/test_g397_mixed_source_producer_census.py; docs/evidence/tracking/g397_mixed_source_producer_census_2026-09-11.md and matching directory; optional docs/research/organization-sprint/PROPOSED_g397_source_fields.diff.
PREMISE step 0 (BINDING before-condition): author/orchestrator first census the whole current source/attempt set and verify both actual format groups and ORIGINAL route identity; user-reported admission time alone does not pass S2.
Resolve actual daemon executable/cwd and data/tracking/track_daemon_ledger.jsonl beneath that cwd; archive absolute path, complete snapshot digest and fields game_id, status, rows, decoded_frames, evaluated_frames, stride, source_height, source_fps, source_duration.
Existing completed format-effect census -> FALSIFIED. Fewer than 30 unique sections of either kind or unbound source identities -> PARTIAL with full distribution, no scoring a convenient smaller sample.

METHOD (sealed; an observational source-policy measurement, no claim of causal resolution or fps effects):
1. Freeze census UTC and all available attempt/source identities across the policy boundary; archive feeder DROP/probe records, selector/probe file hashes and policy timestamps. Do not infer height from an itag.
2. Probe all candidate objects: raw width/height, avg_frame_rate and r_frame_rate rationals, time base, duration and actual decoded PTS. Group 1920x1080 with 29-31 fps versus 1280x720 with 59-61 fps; keep OTHER/UNKNOWN separately.
3. Sort unique sections within each kind by competition/video/source-start/section/digest; select 30 evenly at floor(j*(N-1)/29+0.5), j=0..29. Carry every attempt for each selected section, including failures and repeats; no post-draw replacements.
4. Use the frozen earliest terminal ORIGINAL attempt per section for the primary output; include absent/nonterminal attempts as missing. Preserve outputs and source bytes immediately via G386 one-pass receiver readback/decode before rotation; never duplicate the whole cache.
5. Archive tracking_data.csv, ball_tracking.csv, evaluated_frame_count.json, harness_verdict.json and launch argv with the complete ledger records. Reconstruct evaluated ticks from ball frame/live==1 using G376; never substitute player-emitting frames or a declared stride grid.
6. Verify decoded-frame counts against retained source decoding under the recorded cap, naming source-decode versus producer-decode counts separately; never trust grader row cardinality. Validate schedule completeness against cap, decoded sequence and suspend records; missing/cleared tables or inconsistent bounds make that section's evaluated denominator UNKNOWN. G380's note_attempt wrapper proves transport only, not an independent boundary census.
7. Per section record raw rows, unique player/tick rows, decoded frames, declared/evaluated/emitting ticks, suspended ticks and evaluated ticks with zero player rows; rows per decoded frame and per evaluated tick retain their distinct denominators.
8. Held share = byte-equal x/y pairs for the same player on consecutive EVALUATED ticks / all shared-player pairs on those ticks. Also report no-shared-player tick pairs/all adjacent evaluated pairs and no-output ticks/all evaluated ticks; never bridge a missing evaluated tick.
9. Record actual --frames cap, stride, elapsed source PTS, cap/fps implied duration, decoder version and fps handling read from source. Keep native frame rate; no resampling. The same 3000-frame cap spans about twice as long at 30 fps as at 60.
10. Report per-kind distributions and 720p60-minus-1080p30 descriptive differences, including missingness and competition/video composition. Separate equal-PTS-duration summaries from equal-frame-cap summaries; no pooled producer-quality claim or attribution to resolution alone.
11. Audit source_height/source_fps against probes in EVERY selected ledger record, including failure rows; absent fields stay UNKNOWN. Export an additive enriched ledger copy with exact joins/digests; any runtime repair stays PROPOSED, never edits the active ledger/daemon.
12. Recompute canonical tables twice from snapshots and inspect one native interior frame plus output/timeline overlay per selected section, evenly ordered. No new live policy acceptance, no G395 dependency and no restart.

ACCEPTANCE RULE:

| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Format/attempt accounting | New mix unmeasured; source fields exist in code | 30 unique sections EACH kind; all attempts/source identities accounted; measured fps/height for every classified source | >=60 sections; full candidate census | 30 even native/metadata cards per kind | Actual admission policy and source bytes | Complete census DONE; supply short PARTIAL |
| Producer denominators and held share | No measured per-kind baseline; G380 historical class split 0.969 CLAMP | All 60 records retained; >=30 complete evaluated schedules per kind for scored comparison; zero silent omission; rates reproduce exactly | All planned decoded/evaluated ticks and keyed pairs | Same 60 cards with missing-output states | Original producer, caps, stride, coordinates, worker count | Complete diagnostic DONE regardless of direction; incomplete denominator NOT VALIDATED |
| Filterable ledger provenance | Runtime field completeness UNKNOWN | Report exact missing/mismatch counts; enriched copy joins every selected attempt once, preserves original fields; no invented values | Every selected attempt, >=60 section identities | Same cards cross-check source_height/source_fps | Active ledger/schema, flags, deployed files | Copy complete DONE; active wiring explicitly NOT VERIFIED |

EVIDENCE: docs/evidence/tracking/g397_mixed_source_producer_census_2026-09-11.md plus directory: prereg, policy_receipt.json, census.csv, draw.csv, source_probes.csv, source_receipts.csv, ledger_snapshot.jsonl, enriched_ledger.jsonl, per_section.csv, evaluated_ticks.csv, held_pairs.csv, snapshots/, eye_index.csv, summary.json, repeats.json and common receipts.
TEST: python -m pytest tests/platformkit/test_g397_mixed_source_producer_census.py -q; 30/60-fps cap duration, non-emitting evaluated tick, missing schedule, no bridging, failure-row fields, duplicate attempts and immutable enriched export.
VERSION 2026-09-11 v1 PROPOSED; run first. User-supplied policy change is the premise to verify; measured contrast is a mixture comparison under the ORIGINAL producer.

COMMON BINDING CONTRACT (astra round 6, inherited by this block):
Common binding contract: docs/evidence/tracking/VERIFIER_CONTRACT.md A/B, A7/A9/A11/B11/H1/S1/S2/S4/S5, Q1/Q3/Q6; each block inherits this paragraph and the following execution rules.
Each row: codex terra PREPARE, Claude finisher MEASURE, independent verifier; <=180 minutes including receipts. PREPARE seals protocol alone before metrics, then exact input/code/config identities before dependent work.
OWNS only its named new helper prefix, one test, memo and matching evidence directory. Helpers <=300 LOC, full-package imports; per-file tests only. Shared modules and production trees remain unchanged; gated changes only as named PROPOSED diffs.
Preserve full inherited schemas, all planned missing/failed/ABSENT/UNKNOWN states, exact paths/bytes/dimensions/PTS, hashes, raw judgments, even render index, exclusions and NOT VERIFIED. A missing denominator is UNKNOWN, never zero.
Pod steps use ~/bin/pod_run <lane> with explicit --ship/--fetch under /workspace/wt/<lane>/; deployed trees read-only. Gate actual VRAM/CPU needs, threads 1, <=6 CPU cores, scratch <2 GB and successful pod write probe before bulky writes; no restart or peer interruption.
PC free RAM >=2.8 GB and <=3 active lanes including raters; block/poll own work to completion within the fixed budget. Deterministic arithmetic is not repeatable inference or rating; single-run outputs remain descriptive.
Durable per-unit tables, summary.json, seals, time/resource receipts and readback belong under the row's evidence directory. Landable text/logs must pass ASCII and character-code-built Q6 scans; do not print matched text.
DONE means research completion; landing uses contract C. A failed evidentiary bar stays failed. No flags, registry writes, production deployment, threshold sweeps or renewed spent allowances.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only in every LANDING tree (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register, never restart the daemon; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round6_2026-09-11.md (local-only). G394's and G396's evidence lands on master after their verifies; a premise step that needs it reads the lane tips named in the spec until then (G394 a7 87a497e94; G396 a3 fead21999).
