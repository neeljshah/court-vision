GAP G390 | sport basketball | worktree a11 | log cx_g390_ball_a8_sealed_pass
**RANK 3 - ONE CONDITIONAL A8 PASS ON THE REUSED BENCHMARK. Landed docs/evidence/tracking/g384_ball_phase2_receipt_2026-09-10/arm_accounting.csv SHA-256 1d64edae0c8779b1fc195d3207dc8f230166a4225f652dc35da1306487ef3b62 and docs/evidence/tracking/g373_ball_detector_v2_2026-09-10/reference_v2_summary.json SHA-256 d3bbf294e9f5f7c4fec9fa60e3ff9b232bf9aad39ecf992a644e727d0faeb250 are memo-cited and byte-checked. G389's completed reference/boxes/seal hashes MUST be inserted at launch; they do not exist yet. The historical A8 LIMIT is execution accounting, not proved supply impossibility.**
WHERE THIS ROW RUNS: PC PREPARdocs/evidence/tracking/premise 30 min; pod /workspace/wt/a11/ train <=90 min then inference <=40; PC receipt/reproduction 20. Sequential handoff after G389; no GPU reservation while its reference is incomplete.
PREMISE step 0 (BINDING before-condition): require G389's independently accepted complete 1,620-key reference, >=500 deduplicated native dev boxes/>=5 games, all 549 held-out keys and >=150 VISIBLdocs/evidence/tracking/>=150 ABSENT, inherited usability pass, candidate executions exactly zero and cumulative training minutes explicitly reconciled.
Also verify full native inference/context cache and deployed A0 route/weight hashes against G373's receipt; today's pod cache is NOT VERIFIED by this review. Missing identity/reference/quota -> scoped NOT VALIDATED/PARTIAL, zero scoring; already-used candidate allowance -> FALSIFIED, no second run.
METHOD (sealed before training; G373/G384 accounting carried forward):
1. PREPARE freezes a single A8 configuration: existing A0 ball checkpoint as initialization, 960 px input, batch 2, 10 epochs, SGD lr0=0.001, seed 373, workers 0; explicitly serialize every remaining training/augmentation/NMS option and dependency version before launch. No defaults left implicit or autotuning.
2. Only audited development frames/boxes train the detector; reference ABSENT may supply labelled negatives, UNKNOWN cannot become negative training labels. Fixed game/context separation and all inherited causal-neighbour exclusions bind; no extra footage or class-dependent replenishment.
3. Arm list is A8 alone; A9 remains unavailable without its original sealed receipt and A10 is not scheduled. This allocation resumes the unexecuted successor allowance; it does not reopen G363 or reset the cumulative <=120 GPU-minute cap.
4. Train once in pod scratch with existing pinned dependencies/weights; no downloads or deployment. Use the final completed epoch checkpoint, never a held-out-selected epoch; require all 10 epochs within the 90-minute row cap and inherited remaining GPU budget, otherwise archive NOT VALIDATED without a held-out run.
5. Freeze the final weight hash and configuration before the ONE candidate held-out inference: confidence 0.05, highest-confidence OBSERVED rank-0 box per frame, no temporal or held-position output; pin remaining decode/NMS details to literal values in the launch seal. Keep no-detection frames.
6. Recompute A0 on the same frozen reference/cache as A8; keep v1 0 Tdocs/evidence/tracking/8 FP over 549 as historical context only. Charge a unique candidate-run token before inference; retain any failed/partial run and its outputs, never silently replace it with another held-out execution.
7. Convert both predictions and native reference consistently to 720p scoring coordinates. Primary match remains one-to-one centre distance <=max(3 px, diameter_720p/2); IoU never substitutes. UNKNOWN-frame predictions count FP, and ALL FP uses every scored frame.
8. Score all 549 held-out keys: integer Tdocs/evidence/tracking/Fdocs/evidence/tracking/FN, C=Tdocs/evidence/tracking/549, Wilson95 precision lower, ALL Fdocs/evidence/tracking/N_absent, recall=Tdocs/evidence/tracking/N_visible, with every per-frame pair archived. Subgroups below 30 are descriptive; report benchmark reuse, never fresh-corpus confirmation.
9. Recompute scoring from archived predictions in a second fresh process without rerunning candidate inference. Export weights and all reader-required pixels/artifacts off-pod; hash and decode at the PC receiver before reporting durability. Source rotation or receipt failure cannot become a quality pass.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Launch readiness and execution | No candidate score; 0 GPU minutes in G384 receipt | Complete usable reference; >=500 boxes/>=5 games; quotas >=150 each; exactly one candidate allowance | 1,620 reference keys, 549 held-out; all development keys checked | 30 even native reference/control frames | Historical accounting, source/split identities, no new sample | Missing gate NOT VALIDATED; used allowance FALSIFIED |
| Candidate detection quality | Historical v1 A0 0 Tdocs/evidence/tracking/8 FP; paired v3 A0 measured here | C>=0.25 AND Wilson95 precision lower>=0.90 AND ALL Fdocs/evidence/tracking/N_absent<=0.01 | All 549 unique held-out frames, ABSENT/UNKNOWN included | 30 evenly spaced held-out renders, including failures/no detection | Centre-only rule, all three bars, rank-0 OBSERVED semantics | All bars DONE; otherwise PARTIAL/NOT VALIDATED, no follow-up tuning |
| Reproducibility and cost | No new arm receipt | Per-frame score bytes reproduce; full off-pod hash/decode receipt; training <=90 row minutes and <=120 cumulative | Same 549 pairs; pixels never counted twice | Same 30 render keys, both outputs shown | Production code/weights, G344/G357, daemon, flags, orientation | Missing receipt NOT VALIDATED; no operational adoption |
EVIDENCE: docs/evidence/tracking/g390_ball_a8_sealed_pass_2026-09-11.md and matching directory: launch seals/config, parent execution accounting, cache/reference/box manifests, training log, final weights, run token, baseline/candidate predictions, paired_frame_scores.csv, full repeat/readback receipts, summary.json, renders/ and common receipts.
TEST: python -m pytest tests/platformkit/test_g390_ball_a8_sealed_pass.py -q; test incomplete-reference/quota blockers, cross-game/context exclusion, native-to-720p matching, UNKNOWN FP, missing-frame denominator, immutable candidate token and no second inference on resume.
VERSION 2026-09-11 v1 proposed; conditional successor execution only, dependent on G389 receipt and fresh resource/identity checks; no supply-based launch from a confidence interval.

Review identity correction: docs/evidence/tracking/g384_ball_phase2_receipt_2026-09-10/arm_readiness.json is raw SHA-256 efcb6fe27971d89172e4731f0ec275a35770a7ef113817720eb5aaeeb1661159, not its memo's dd9ecd1f7fd43208c01012a12124f4d0751e0e4c5d382a1017985020996cde6b. Use the matched queue/decision/accounting artifacts above and reseal a current derived readiness receipt prospectively; never silently inherit that memo digest.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` over every byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/` READ only (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round3_2026-09-11.md (local-only).
