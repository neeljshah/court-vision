GAP S24 | sport all (ops) | worktree a11 | log cx_s24_artifact_refresh_scheduler
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check every line of section B AND section Q (Q1-Q8) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim from the register): artifact refresh has no scheduler (`fleet_on` false).
READ (exact symbols, re-grepped on disk 2026-09-03): docs/research/organization-sprint/MCP_ARTIFACT_FRESHNESS_2026-09-01.md in full -- it is the inventory, the before/after table and the "Scheduler / cron" finding. scripts/platformkit/mcp_server/artifact_tools.py (185 LOC): `_ROOT`, `_ATLAS` = scripts/platformkit/analytics_showcase/out/market_strength_atlas.json, `_MECHANISM` (:16, 2 paths), `_HEALTH` (:20, 3 paths), `_EXECUTION` (:25, 3 paths), `_as_of` (:32), `_no_data` (:40), `_load` (:45), `strength_atlas` (:66), `mechanism_exposure` (:99), `tracking_program_status` (:118, globs docs/evidence/tracking/*.json + data/tracking_reports/**/*.json), `harness_health` (:147), `execution_status` (:161), `tool_specs` (:177). scripts/platformkit/mcp_server/tools.py `fleet_on` (:104, :108 -- derived from `.bot_state/live_status.json` `stop_requested`, false is the by-design resident-server default) and scripts/platformkit/analytics_verify/answers.py `STALENESS_HOURS` = 48.0. The generators named in the sweep: `python -m scripts.platformkit.analytics_verify.cycle`, `python -m scripts.platformkit.analytics_verify.system_map`, `python scripts/platformkit/analytics_showcase/paper_execution_audit.py --check`.
PREMISE (step 0): grep scripts/platformkit for a scheduler/cron entry that refreshes these artifacts (`cron`, `schedule`, `refresh`, `fleet_on`) and print the hits; the sweep found only `ops_doctor.py` (a diagnostic string for a different subsystem) and `pod_ops_watch.py` (pod liveness). Also print `fleet_on` as computed by tools.py:104 today and the age in hours of every artifact in the artifact_tools inventory. Premise holds iff no scheduler entry exists AND fleet_on is false -> before = 0 scheduled runs, 0 heartbeats. If a refresher entry already exists, STOP, write the memo, commit, report FALSIFIED (a valid result).
LIMIT (step 1): n/a -- CONSTRUCT row (3 enumerated runs). Print which inventory artifacts HAVE a producer and which do not: `harness_health.json` has NO producer anywhere under scripts/platformkit, intel/ or api/ (the sweep grepped it) -- the refresher must report it NO_PRODUCER honestly and must NOT invent one, and it does not count against the bar.
CHANGE (step 2): NEW scripts/platformkit/mcp_server/artifact_refresh.py (<=300 LOC, stdlib + subprocess only, supervisor-independent -- it does NOT read `fleet_on` and does NOT depend on the fleet being up):
  A module-level TARGETS table, one entry per refreshable artifact taken FROM artifact_tools.py's own constants (never a second hardcoded copy of a path): {name, artifact paths, generator argv, expected as_of field}. Generators are exactly the three the sweep ran; anything with no generator is recorded status NO_PRODUCER.
  `refresh_once(root, targets=TARGETS, timeout_s=...) -> dict` -- for each target: read the artifact's as_of/generated_at BEFORE, run the generator as a subprocess, read it AFTER, and record {name, as_of_before, as_of_after, advanced: bool, rc, status}. A generator that fails or times out is a FAILED row carrying its rc and stderr tail; it never kills the pass and never leaves a half-written heartbeat.
  Heartbeat: append one JSON line per pass to data/cache/mcp_server/artifact_refresh_heartbeat.jsonl {started_at, finished_at, n_advanced, n_failed, n_no_producer, per-target rows} and rewrite data/cache/mcp_server/artifact_refresh_status.json with the last pass. Both paths are parameters so the test writes under tmp_path only.
  CLI: `--once` (run one pass and EXIT), `--root`, `--out-dir`, `--targets` (comma-separated names). There is NO loop mode and this job STARTS NO DAEMON: the cadence is the OS scheduler's, named in the memo as the exact Windows Task Scheduler command line for the orchestrator to arm -- `schtasks /Create /SC HOURLY /TN CourtVision-ArtifactRefresh /TR "<python> -m scripts.platformkit.mcp_server.artifact_refresh --once"` -- and the job does NOT create it. `ponytail:` comment names the upgrade path (a per-target cadence) if hourly is wrong for a target.
TEST: NEW tests/platformkit/mcp_server/test_artifact_refresh.py (beside the existing tests/platformkit/mcp_server/test_artifact_tools.py). All writes under tmp_path, generators stubbed with a fake argv that bumps a stamp: (a) `--once` over a tmp root with 3 targets whose stamps advance -> 3/3 advanced, one heartbeat line appended, status.json written; (b) three consecutive `--once` passes -> the heartbeat has exactly 3 lines and each pass's as_of_after is strictly monotone increasing against the previous pass's (the bar); (c) a generator that exits non-zero -> that row is FAILED with its rc, the other rows still advance, and the pass still writes exactly one heartbeat line; (d) a target with no generator -> NO_PRODUCER, never counted as advanced. Assert no process is left running and nothing is written outside tmp_path. Run ONLY `python -m pytest tests/platformkit/mcp_server/test_artifact_refresh.py -q`, then rerun `python -m pytest tests/platformkit/mcp_server/test_artifact_tools.py -q` (must stay green) -- two files, nothing broader.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = the freshness stamp advances; denominator = 3 consecutive scheduled runs
  before        = no scheduler entry exists and fleet_on is false -> 0 scheduled runs, 0 heartbeat lines (measured in step 0)
  bar           = 3/3 with a monotone `as_of`
  n             = 3 (CONSTRUCT)
  eye check     = n/a (S-row); reproduction = verifier reruns the test in master and re-reads the three heartbeat lines, checking as_of strictly increases across them
  must not move = scripts/platformkit/mcp_server/artifact_tools.py (its constants are IMPORTED, not copied, and not edited), `STALENESS_HOURS` = 48.0 in analytics_verify/answers.py and every other staleness bound, `.bot_state/live_status.json`, the supervisor and every ProcSpec, every threshold under scripts/platformkit/eval_gate/, data/registry/** (never written), data/cache/eval_gate/backtest_fwer.jsonl (13 rows; No `_charge_ledger` call)
NON-TAUTOLOGY: the metric covers every target in the TARGETS table; NO_PRODUCER and FAILED rows are COUNTED and named, never dropped. Counting only the targets that happened to refresh is the circular metric B1 forbids. The three passes are the same three targets each time, so the monotone check cannot be satisfied by swapping the target set.
EVIDENCE: docs/evidence/harness/S24_artifact_refresh_scheduler_2026-09-03.md -- the step-0 grep and per-artifact ages, the producer/no-producer split, the three-pass heartbeat table with as_of before/after, the exact schtasks command line NOT run, test outputs, and a NOT VERIFIED list (say plainly: no OS task was created, no daemon was started, and the three passes are stubbed generators, not a live refresh of the real artifacts). Calibration language only (Q6): no dollar, ROI, profit or edge word.
POD: none. Local only. Do not start any long-running process.
COMMIT: explicit pathspec (module, test, memo), in this worktree, no push. Last line of your report: `SHA: <sha>`.
NEVER PARK: run everything to completion this turn; never end waiting.
