GAP S74 | sport all | worktree a10 | log cx_s74_family_tier_filter
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check against every line of section B AND section Q (Q1-Q9) before you report. Template: docs/evidence/tracking/CODEX_SPEC_TEMPLATE.md.
GAP (verbatim): results_db.family_p_values has no tier filter, so a SCREEN's DM p is archived only (archive.screen_p) and not indexed as raw_p -- the family bar cannot see screens and T2's family n counts only charged rows; add the tier filter + index screen_p as a separate column.
READ: scripts/platformkit/foundry/results_db.py (record():120-135, family_p_values():155-164), scripts/platformkit/foundry/results_db_sql.py (_SCHEMA, _RESULT_FIELDS, TierResult), scripts/platformkit/foundry/tiers.py:231-252 (_run_screen writes archive["screen_p"], never raw_p -- its own docstring at :232-233 names this exact gap). Only read tiers.py; do not edit it (out of OWNS below).
PREMISE (step 0): grep `family_p_values` in scripts+tests; confirm signature is still `family_p_values(self, family: str)` (1 arg, no tier), confirm `PRAGMA table_info(result)` on a fresh DB has no `screen_p` column, confirm `_EMPTY["raw_p"]` is None so every T1 SCREEN row (tiers.py:248-252) is invisible to today's query. If any of the three has already changed, STOP, write the memo, commit, report FALSIFIED.
LIMIT (step 1): n/a -- CONSTRUCT row; a 4-row synthetic DB is the whole limit.
CHANGE (step 2), additive only, results_db.py + results_db_sql.py ONLY (a third sibling module under scripts/platformkit/foundry/ is allowed if the diff would push results_db.py past 300 LOC -- do not touch tiers.py or any other caller):
  1. results_db_sql.py: `_SCHEMA`'s `CREATE TABLE result(...)` gains `, screen_p REAL` before the UNIQUE clause (new DBs only).
  2. results_db.py `__init__`: extend the existing additive-migration loop (the one already migrating queue.lease_until/claimer) to also `ALTER TABLE result ADD COLUMN screen_p REAL` when absent from `PRAGMA table_info(result)`, for pre-existing DBs.
  3. results_db.py `record()`: if the caller's row dict contains a `"screen_p"` key, append it to the insert's column/value list; `_RESULT_FIELDS` and its missing-field check stay untouched, so every existing caller that omits `screen_p` inserts exactly as it does today.
  4. results_db.py `family_p_values(self, family, tier=None)`: new optional `tier` kwarg. `tier is None` -> byte-identical SQL text and result to today (reads `r.raw_p`, no tier filter). `tier == "T1"` -> reads `r.screen_p` filtered to `r.tier = 'T1'`. Any other `tier` value -> reads `r.raw_p` filtered to `r.tier = ?`.
TEST: exactly one new test in tests/platformkit/foundry/test_results_db.py, e.g. `test_family_p_values_tier_filter_and_screen_p_column`. Run only: `python -m pytest tests/platformkit/foundry/test_results_db.py -q`.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = family_p_values() return-list contents across 3 calls on one constructed DB; denominator = the 4 recorded result rows
  before        = 0/4: family_p_values(family) sees only the 2 T2 rows (raw_p), 0/1 T1 SCREEN row's DM p is queryable in SQL (no screen_p column, TypeError on a tier kwarg)
  bar           = family_p_values("fam74") [no tier] returns exactly the same 2 T2 raw_p values as before (unchanged); family_p_values("fam74", tier="T1") returns exactly the 1 SCREEN row's screen_p; family_p_values("fam74", tier="T2") returns exactly the 2 T2 raw_p values; the 1 row of a different family is excluded from all three calls
  n             = 4 (CONSTRUCT): 1 T1 SCREEN row (screen_p=0.03, raw_p=None) + 2 T2 charged rows (raw_p=0.10, 0.20) in family "fam74", + 1 T2 row (raw_p=0.40) in family "other" as the negative control
  eye check     = n/a (S-row); reproduction = verifier re-runs the test file in master and re-reads the 4 rows via sqlite3 directly to confirm screen_p is a real column, not a JSON field
  must not move = family_bars.py q=0.05 BH/BY threshold; the untiered family_p_values() output (S59 charged-only default); data/registry/** (never written); data/cache/eval_gate/backtest_fwer.jsonl untouched
NON-TAUTOLOGY: the metric covers all 4 constructed rows across all 3 calls; the "other"-family row is the named exclusion, not a hidden one.
EVIDENCE: docs/evidence/harness/S74_family_tier_filter_2026-09-04.md -- before/after table (the 3 calls x their returned lists), the 4 constructed rows, test output, exact commands, a NOT VERIFIED list (tiers.py's SCREEN path is not wired to pass screen_p into record() by this spec -- that wiring is a separate, F1-owned follow-on). Calibration language only: no dollar, ROI, profit or edge word; none of +18.38/0.119/+54/78.11/8.94/54.57 outside retraction context.
POD: none. Local sqlite only.
COMMIT: explicit pathspec (results_db.py, results_db_sql.py, the test, the memo; + the sibling module only if created), in this worktree, no push. Report the sha as the LAST line: `SHA: <sha>`.
NEVER PARK: run the test to completion this turn; never end waiting.
