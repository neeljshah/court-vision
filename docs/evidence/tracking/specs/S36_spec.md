GAP S36 | sport mlb | worktree a13 | log cx_s36_midnight_fit_windows
CONTRACT: docs/evidence/tracking/VERIFIER_CONTRACT.md -- read it; self-check against every line of section B AND section Q (Q1-Q8) before you report.
INSTRUMENT FIX ONLY -- no charge. data/cache/eval_gate/backtest_fwer.jsonl must be byte-identical before/after (do not import or call `_charge_ledger`).
GAP (verbatim from the register): e4_blend and e2_regime fit windows self-leak across UTC midnight: gap_blend_arm.py:84 asserts DATE order only, and 124/178 corpus games span midnight, so 52.86 pct of e4's scored ticks (25,000/47,292) and 43.49 pct of e2's (2,867/6,593) had their own game's outcome in the fit window. e4 leak-free 0.206786 vs shipped 0.207033 (AHEAD direction stands, conservative); e2 leak-free 0.254351 vs shipped 0.252261 (leak FLATTERED e2 by +0.002090). The S06 stacker already consumes only the leak-free game-first-date variants (docs/evidence/harness/S06_OOF_PREFLIGHT_2026-09-03.md).
PREMISE (step 0): re-read gap_blend_arm.py:24-28 (`_date` keys per-TICK game_date/date/timestamp) and :84 (`train["date"].max() < test["date"].min()` -- date order only); gap_regime_arm.py:20-24 (`_date` same, per-tick) and :118 (`train_date_max < test_date` -- date order only). Confirm by grep that NEITHER module contains a train/test game-disjointness assert today. Confirm the leak-free path is produced ONLY externally today (S06 preflight's scratchpad tick-preparation feeding game_date = each game's first date into the unchanged module) -- no in-module `fit_window` option exists. If either module already asserts game-disjointness, STOP and report FALSIFIED.
CHANGE (step 1): additive only, both modules. Add an opt-in `fit_window` parameter: `"tick_date"` = default, current behavior, alias unchanged; `"game_first_date"` = new. In `"game_first_date"` mode key fold dates by each game's MINIMUM tick date (not the individual tick's own date) before grouping. In BOTH modes add a per-fold assert `assert not (set(train["game"]) & set(test["game"]))` (blend) / `assert not (train_games & test_games)` (regime), raising on any violation -- passes trivially in `game_first_date` mode and is EXPECTED to raise in `tick_date` mode on this corpus (do not catch it; that raise is the proof). No existing call site's behavior changes.
TEST: exactly one new per-file test (extend scripts/platformkit/ingame/test_gap_blend_arm.py or test_gap_regime_arm.py): a synthetic midnight-spanning game fixture where `fit_window="game_first_date"` scores with 0 pct self-leak and `fit_window="tick_date"` (default) raises AssertionError on the same fixture. Run only that file.
ACCEPTANCE RULE (the verifier applies exactly this and nothing else):
  metric        = pct of scored ticks whose OWN game's outcome sits inside the fit window, denominators 47,292 (e4) and 6,593 (e2)
  before        = e4 52.86 pct (25,000/47,292); e2 43.49 pct (2,867/6,593)
  bar           = 0.00 pct in `fit_window="game_first_date"` mode, enforced by the per-fold game-disjoint assert raising on any violation
  n             = 47,292 (e4) / 6,593 (e2) ticks (SAMPLED corpus counts)
  eye check     = n/a (S-row); reproduction = the verifier re-runs both arms on the S06 partition in `game_first_date` mode and recomputes the leak pct (expect 0.00) plus the Briers (e4 0.206786, e2 0.254351 to 1e-6)
  must not move = every existing threshold/bar in both modules, the default `tick_date` behavior and its call sites, backtest_fwer.jsonl
NON-TAUTOLOGY: metric covers all scored ticks in both fit_window modes on the same S06 corpus partition; no row is excluded to make the pct pass.
EVIDENCE: docs/evidence/harness/S36_midnight_fit_windows_2026-09-04.md -- before/after leak pct with denominators, both Briers reproduced, the game-disjoint assert raising in tick_date mode (paste the traceback), test output, and a NOT VERIFIED list. Calibration language only (Q6).
COMMIT: explicit pathspec, in this worktree, no push. Report the sha.
NEVER PARK: run to completion this turn; never end waiting.
