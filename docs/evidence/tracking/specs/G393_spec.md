GAP G393 | sport basketball | worktree a14 | log cx_g393_leading_hyphen_game_id
**RANK 3 - PRESERVE A LEADING '-' GAME ID THROUGH ARGUMENT PARSING. Landed scripts/platformkit/track_daemon.py SHA-256 9747d9a085405c492f04c4e3b162ed8b1d897309da6f0f9c014cb7092056f5ac passes split option/value at lines 95-97; scripts/run_clip.py SHA-256 1e490eabc142408144fef8911ee6732743e9b6f710dd727fbf87997c524ba369 declares --game-id at line 424. L380/aa_control.csv SHA-256 9046cb6b10e9f03a8e6c0e45a81a17d3d4d913ae9f4cc017453b2084bf727426 keeps producer variation out of this parser-only claim.**
WHERE THIS ROW RUNS: PC only, step 0/PREPARE 15 min, isolated syntax-level replay 25, Claude review/receipts 20; no imports of producer main, video decode, GPU, pod job or restart.
OWNS: scripts/platformkit/tracking/g393_*.py; tests/platformkit/test_g393_leading_hyphen_game_id.py; docs/evidence/tracking/g393_leading_hyphen_game_id_2026-09-11.md and matching directory; docs/research/organization-sprint/PROPOSED_g393_game_id.diff only.
PREMISE step 0 (BINDING before-condition): inspect current build_command plus actual argparse declarations. This review's read-only 30-case leading-id probe fails 30/30 with split tokens, passes 30/30 with equals tokens; 30 ordinary ids parse identically.
Reproduce against the exact complete parser setup, without executing main. Already repaired caller -> FALSIFIED. Current master run_clip differs from G380's deployed digest ccd08d32c8c4cb8625ec5ef67535206f74f4df307a8e191ec01f5700fb40d8c2; do not assume the files are interchangeable.
METHOD (sealed, finite CONSTRUCT universe):
1. Freeze 60 cases: '-Oa_BpdVT64_s'+str(1000+i) and 'Oa_BpdVT64_s'+str(1000+i), i=0..29; each receives fixed video/output paths containing spaces, frame cap 3000 and --no-show. These are parser controls, not 60 tracked games.
2. Extract build_command and the parser-construction statements from their actual AST into an isolated in-memory harness with explicit globals; include ALL argument declarations, stop before parse-triggered downstream work. Reject unrecognized extraction structure instead of silently approximating it.
3. Produce an anchored PROPOSED diff changing only the caller pair '--game-id', game_id to the single token '--game-id='+game_id on the run_clip route. Keep argv as a list; never invoke a shell or remove '-' from an identifier.
4. Apply that proposed change only to an in-memory source copy; run each original/proposed argv through the actual parser setup. Archive argv arrays, parsed namespaces, original stderr/return status and source/proposal digests.
5. Require exact game_id recovery on every case; all unrelated parsed fields and argv tokens must stay equivalent. Require ordinary-id namespaces byte-equivalent; record known leading-id old failures explicitly, not as excluded cases.
6. Include an exhaustive supplemental six-case set: ids '--help', '--frames', '-1', '-', '--', '-x=y'; value preservation must hold and no value may enable help or change frame count. Preserve resulting counts separately from the 60-case universe.
7. Scan all callers of run_clip/build_command read-only, including shell files; list remaining split-value callers as NEW GAP. Touch no shared-module file and do not acquire a deploy token for this proposed-only row.
8. Ask a19's finisher to compose the accepted proposal with G380 only at authorized deployment, against the actual deployed digest and current accepted diff; any anchor conflict requires fresh review. This row makes no live fix claim.
ACCEPTANCE RULE:
| metric | before | bar | n | eye check | must not move | verdict |
|---|---|---|---|---|---|---|
| Leading-id parse recovery | 30/30 split-token failures in review probe | 30/30 proposed argv recover exact value; reproduce all original failures | 30 unique leading-id CONSTRUCT cases | All 30 ordered original/proposed argv and parse cards | Identifier bytes, --frames 3000, --no-show, paths | DONE for reviewable proposal; mismatch REJECT |
| Ordinary/control invariance | 30/30 ordinary ids unchanged in review probe | 30/30 equal namespaces; all 6 option-shaped values preserved | 30 ordinary plus 6 exhaustive supplemental cases | 30 even ordinary cards plus all six supplemental cards | Every unrelated argument/default and parser declaration | Any drift REJECT; controls are not runtime evidence |
| Patch isolation and identity | Master/deployed run_clip digests differ | One caller hunk only; source hashes verified; no production writes | All 66 case receipts; full caller census | Same command cards; pixels not applicable to argument transport | G380 proposal/history, producer, shared modules, flags | Missing deployed confirmation limits scope to master proposal, no adoption |
EVIDENCE: docs/evidence/tracking/g393_leading_hyphen_game_id_2026-09-11.md plus directory: prereg, source_hashes.json, cases.csv, argv.jsonl, parsed_before_after.jsonl, failure_stderr.txt, caller_census.csv, summary.json, eye_cards/; named PROPOSED diff and NOT VERIFIED live routing.
TEST: python -m pytest tests/platformkit/test_g393_leading_hyphen_game_id.py -q; actual extracted caller/parser, exact leading-id recovery, option-shaped values, ordinary-id equivalence, space-containing paths and unchanged other argv semantics.
VERSION 2026-09-11 v1 PROPOSED; no shared-tree changes or state-changing git during this review, no source patch applied by the row, no tracking-quality claim.

ORCHESTRATOR FOOTER (binding, 2026-09-11): codex terra PREPARES (prereg sealed alone as its OWN commit, `SEAL sha256 <hex>` = sha256 of every LF byte above the seal line, no blank line before it); a Claude finisher MEASURES; codex-sol VERIFIES; `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, `scripts/platformkit/track_daemon.py` READ only (PROPOSED diffs only); never write `data/registry/`, never flip a flag, never touch the register; **ADD ONE RESULTS_LEDGER.md ROW IN THE SAME COMMIT** as the memo; every new file <= 300 lines; vocabulary follows contract Q6 with an automated scan over EVERY text artifact incl. rater logs (patterns built from character codes); n >= 30 with even sampling; no threshold sweep on any held-out set; ASCII stdout; **NEVER PARK.** Astra source: docs/research/astra_round4_2026-09-11.md (local-only). NOTE (astra A.2): G390's 169/188 = ALL false calls / N_absent; the ABSENT-only false-call rate is 50/188 = 0.266 (FP partition VISIBLE 100 / ABSENT 50 / UNKNOWN 19).
