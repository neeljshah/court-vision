VERDICT: PREMISE HOLDS -- 73 of 107 completed pod games carry a `> 0.5 * frame_h` player-box share at or above the sealed 0.10 bar (pooled 80060/412124 (~1.9426e-1); corpus max 914/1188 (~7.6936e-1)), the ENTRY POINT is pinned as the ABSENCE of any box-size gate between `src/tracking/advanced_tracker.py:1225-1228` (person-class detection, no size filter in the route) and the CSV writer, with `PAD` adding 30 px at `:1336` and `:1411` turning the box bottom into the footpoint -- and the sealed 20-panel sheet does NOT support a filter: 11 of 20 panels fall in the sealed NON_SINGLE_PLAYER set, below the 15/20 bar, so NO PROPOSED diff is written and nothing is changed.
# G322 attempt 2 -- oversized boxes: census, producer trace, sealed sheet (2026-09-07)
WHY ATTEMPT 2: attempt 1 (`a5056025e`) was REJECTED on Q6 alone (`G322_VERIFY_2026-09-07.md`) -- prohibited vocabulary inside the SEALED attempt-1 prereg, and a bare decimal share that spelled a retracted historical figure. Q1 forbids resealing after measurement, so the prereg was rewritten and re-sealed clean and the whole census / trace / sheet sequence was rerun from a fresh pod snapshot. EVERY share in every attempt-2 artifact is written as `numerator/denominator (~M.MMMMe-E)`; no bare decimal share is printed anywhere.
PREREG sealed and committed ALONE before the first metric, the first frame and the first label: `g322_prereg_attempt2_2026-09-07.md`, seal SHA-256 `60dea2d35090f443c550064742863e797b229abb30f51f6290275985a55bd92c` (LF-normalized bytes above its `## SEAL` line), commit `b917f4b21`. Spec `specs/G322_spec.md` VERSION 2026-09-07b (master `2beaa82ee`). Rules unchanged from attempt 1: same census rule, same 0.50/0.33/0.25 cuts, same 0.10 and 15/20 bars, same sheet selection rule, same categories. What DID change and is declared: the snapshot (the pod ledger grew from 94 to 132 lines while attempt 1 was being verified), the spec citation, and the two reworded prereg phrases named by the verifier.
INPUTS (A9/A11), all READ-ONLY, nothing on the pod written, signalled or killed: ledger snapshot `/workspace/nba-ai-system/data/tracking/track_daemon_ledger.jsonl` -> `track_daemon_ledger_snapshot.jsonl`, 132 lines, SHA-256 `443bc83d0640bb646c0cd58615b5f3a3de02238235e7b4a869bff2aaeef02004` (107 `tracked` with rows, 24 `thin`, 1 `timeout`); 107 per-game `tracking_data.csv` (150 MB) fetched by one `tar czf - -T -` stream, each file's SHA-256 and byte size in the census CSV and in prereg section 10. A 16-worker `track_daemon` was running throughout and the pod tree was mutating under the read.
## PREMISE (step 0, binding): HOLDS, and it reproduces G315 and attempt 1 exactly
G315 measured, over STEPS, 0.2187 `ncaa_basketball_mRkuGgeECak` / 0.2395 `wnba_05` / 0.0000 `ncaa_basketball_sRtHQbywiTE`. Over OBSERVATIONS this row measures **1112/5580 (~1.9928e-1) / 1086/4736 (~2.2931e-1) / 0/1653 (~0.0000e0)** -- same sign, same zero, denominators differ because a step is a pair of rows. Bit-identical to attempt 1 on all three games. Both G315 games clear the 0.10 bar.
## CENSUS -- every completed game, no sampling (n = 107 games, 412,124 observations, 0 unusable rows, 0 games excluded)
Denominator of every share is that game's OWN row count in its OWN `tracking_data.csv`; `box_h = bbox_y2 - bbox_y1` as written; `frame_h` = the table's own `source_height` column, which agrees with the ledger `source_height` in **107 of 107 games** (S4: field named, both compared).
| source_resolution | games | observations | share > 0.5 | share > 0.33 | share > 0.25 | games >= 0.10 |
|---|---|---|---|---|---|---|
| 1920x1080 | 7 | 34,428 | 4127/34428 (~1.1987e-1) | 6769/34428 (~1.9661e-1) | 14269/34428 (~4.1446e-1) | 3 |
| 1280x720 | 45 | 178,303 | 32094/178303 (~1.8000e-1) | 61091/178303 (~3.4262e-1) | 116913/178303 (~6.5570e-1) | 32 |
| 640x360 | 55 | 199,393 | 43839/199393 (~2.1986e-1) | 98550/199393 (~4.9425e-1) | 164377/199393 (~8.2439e-1) | 38 |
| ALL | 107 | 412,124 | 80060/412124 (~1.9426e-1) | 166410/412124 (~4.0379e-1) | 295559/412124 (~7.1716e-1) | 73 |
Diagnostics sealed in prereg section 3, pooled: against the post-TOPCUT height (`source_height - 60`) the `> 0.5` count is **98806/412124 (~2.3975e-1)**; with the writer's `PAD` removed (`box_h - 30`) it is **66195/412124 (~1.6062e-1)**. `max(bbox_y2)` exceeds `source_height` in **102 of 107 games** (max 3,620.49 on a 1,080-high frame, `g220c_jh3fnwMi7dM`) -- the off-frame tail of Kalman-predicted rows, sized below.
Coincidence flagged and independently re-computed rather than hidden, and it survives the corpus growth: `> 0.25` is EXACTLY one half in the same two games as attempt 1 (`ncaa_basketball_mRkuGgeECak` 2790/5580 and `wnba_06` 1803/3606). A second, separately written pass reproduces both counts; it is a coincidence, not a degenerate rule.
## PRODUCER TRACE -- full file:line route in `g322_producer_trace_attempt2_2026-09-07.md`, re-cited against the current blobs
Determination: **primarily (a); (b) contributes via PAD; (c) is confined to the off-frame tail.** POST-HOC and additive, moving no threshold: the `> 0.5` count is **44609/230102 (~1.9387e-1) on MATCHED rows and 35451/182022 (~1.9476e-1) on COASTING rows** (`confidence == 1.0` means `lost_age == 0`, i.e. the row carries a detection box; otherwise it carries the Kalman prediction written at `advanced_tracker.py:1160-1165`). The two are level, and `_make_kf:104-127` holds `w` and `h` CONSTANT in the transition matrix, so a coasting box cannot grow taller at all -- it can only carry forward the size the detector last emitted, which is exactly what a level split looks like. Coasting is a carrier, not a producer. Honest change from attempt 1: on 71 games the matched share was the higher of the two and the memo leant on that sign; on 107 games they are level, and (a) now rests on the constant-`w`/`h` structure, not on the sign.
ENTRY POINT, pinned with numbers: `src/tracking/advanced_tracker.py:1225-1228` runs YOLO with `classes=[0]` (no class merge) and ultralytics' default NMS `iou`, `:1332-1339` admits every returned box with only an empty-crop `continue`, `:1336` adds `PAD = 15` per vertical border (30 px total, worth 80060 -> 66195 of 412,124 pooled), and `:1411` sets `foot_y = y2c`, the box bottom, unless an ankle keypoint clears conf 0.5 -- which the code's own comment at `:263-264` records as about 0.005 at `imgsz = 640` on 640x360 broadcast, i.e. the fallback is the normal path on the 55 640x360 games here. **No height, area, aspect or frame-fraction gate exists anywhere in that route**; that absence is the entry point.
## SEALED CONTACT-SHEET CHECK -- 20 panels, rule fixed before the first frame was cut, re-rated fresh
PANEL SET CHANGED, as the sealed rule requires on a larger corpus: `wnba_06` (1080p median) and `0022500594_s2700` (corpus max) are the same games as attempt 1 and their five sealed rows are identical; `0022400909_s3307` (720p median) and `0022500575_s5400` (360p median) replace attempt 1's `bos_mia_playoffs` and `0022500594_s7200`, because the 720p and 360p eligible sets grew. So 10 of the 20 panels are the same images as attempt 1 and 10 are new -- the two ratings are NOT independent evidence and no agreement rate between them is computed or quotable.
Five panels each at the sealed odd-decile ranks over each game's own above-cut rows (n = 311 / 1,190 / 645 / 914), sweeping ratio 0.52-0.94. Sources (A9), pod `data/footage_corpus/`: `wnba__wnba_06.mp4` 430,058,965 B 1920x1080 30/1 28,674 frames; `nba__0022400909_s3307.mp4` 34,752,463 B 1280x720 30000/1001 4,002; `nba__0022500575_s5400.mp4` 13,258,934 B 640x360 30000/1001 **4,575, against 4,496 in the ledger's `decoded_frames` -- a 79-frame disagreement**; `nba__0022500594_s2700.mp4` 12,548,321 B 640x360 30/1 **4,541, against 4,501 -- a 40-frame disagreement**. Both are reported, not resolved. Frames cut on the pod with `ffmpeg`, `select=eq(n,N) -frames:v`, into `/tmp/g322b/`, then `scp`-ed out; panels drawn locally and headless with PIL, never `cv2.imshow`. Each panel carries BOTH readings: the sealed post-TOPCUT box (`y + 60`, green) and the no-offset box (red).
COUNTS PER CLASS from ONE declared MODEL rater (Claude Opus 5, `claude-opus-5[1m]`), rated fresh from the images with the attempt-1 label file unopened until after the last call was written. **These are counts, never a rate**: G315 measured two model raters agreeing on 14 of 30 panels, so no share is computed here and human labels are the limit on every content statement below.
COUNTS, one label per panel, 20 panels: bench_or_courtside 6, crowd 5, one_player_close 5, other 3, unreadable 1, broadcast_graphic 0, two_or_more_players 0.
`NON_SINGLE_PLAYER` (the sealed set: crowd + bench_or_courtside + broadcast_graphic + two_or_more_players) = **11 of 20**, below the sealed 15/20 bar, so **NO PROPOSED diff is written and no filter is proposed.** Reported honestly rather than rescued: 15 of 20 panels contain no correctly-boxed single on-court player once the 3 `other` and 1 `unreadable` panels are counted, but those are outside the set I sealed before viewing and the bar is not moved to reach them.
What the panels show that the census cannot: **5 of 20 oversized boxes are CORRECT** -- a genuine camera close-up of one player, tightly boxed. A naive `box_h > 0.5 * frame_h` reject would delete those. The rest are non-game content the tracker had no business boxing: an in-arena host with a microphone and his promotion contestant and credentialed staff (6 panels), spectators in the stands (5), the padded courtside barrier and the media table (2), a wide-shot box run about 1.7x its player's height (1), and one near-black transition frame the rater could not read. Per-panel labels and one-line reasons: `g322_panel_labels_attempt2_2026-09-07.csv`.
## ATTEMPT 1 vs ATTEMPT 2
| quantity | attempt 1 (`a5056025e`, REJECTED) | attempt 2 (this row) |
|---|---|---|
| games censused / excluded / observations | 71 / 0 / 250,369 | 107 / 0 / 412,124 |
| pooled `> 0.5` | 47206/250369 (~1.8855e-1) | 80060/412124 (~1.9426e-1) |
| 1920x1080 `> 0.5` | 4127/34428 (~1.1987e-1), 7 games | 4127/34428 (~1.1987e-1), 7 games (identical) |
| 1280x720 `> 0.5` | 4659/36112 (~1.2902e-1), 13 games | 32094/178303 (~1.8000e-1), 45 games |
| 640x360 `> 0.5` | 38420/179829 (~2.1365e-1), 51 games | 43839/199393 (~2.1986e-1), 55 games |
| games >= 0.10 bar | 44 of 71 | 73 of 107 |
| panel games | wnba_06, bos_mia_playoffs, 0022500594_s7200, 0022500594_s2700 | wnba_06, 0022400909_s3307, 0022500575_s5400, 0022500594_s2700 |
| sheet counts (NON_SINGLE_PLAYER) | one_player_close 8, bench_or_courtside 5, other 3, crowd 2, two_or_more_players 2 (9 of 20) | bench_or_courtside 6, crowd 5, one_player_close 5, other 3, unreadable 1 (11 of 20) |
The 1080p cell is bit-identical across the two runs (no 1080p game was added), which is the cheapest available check that the rerun reproduces attempt 1 where the input did not move.
## NOT VERIFIED
No ground truth of any kind, and no human label. One model rater on 20 panels is a SCREEN for whether a labelling effort is worth mounting, not a measurement of content; G322 establishes no content rate for any game and none may be quoted. 10 of the 20 panels are the same images attempt 1 rated, so the two label sets are not independent and no inter-run agreement may be read off them. The census counted numbers in a CSV -- only these 20 rows were ever compared to an image, out of 412,124. An oversized box is not by itself an error: this row cannot separate "wrong box" from "correct box on a close-up shot" without labels, and 5 of 20 panels are the second kind. The frame-index mapping (`frame` = 0-based index into the decoded sequence) is an INFERENCE; G198 (prefetch frame misalignment) is open against it, and two of the four panel games disagree with `ffprobe` on frame count by 79 and 40 frames. `imgsz` and `conf` are read from the code defaults, not from a pod run record, so a `_cfg` override on the daemon is not excluded. Nothing was re-decoded through the tracker and no panel render was repeated (B11: one run). The pod tree mutated throughout; ALL 107 censused games are `passed = false` in the daemon ledger and the 107 are not a sample of any population. Image space only (`coordinate_space = image_px`): no court, foot, metre or registration claim is made or may be read in. Whether any gate would improve any downstream number is NOT measured.
DEVIATION DECLARED (unchanged from attempt 1): prereg section 6 makes a game's panels re-render without the offset if `max(bbox_y2)` exceeds `frame_h - 60`; it does, in all four panel games, because of the off-frame coasting tail, and I did NOT re-render, because the same panels already carry BOTH rectangles and every panel whose box bounds an identifiable person shows the `y + 60` box on that person while the no-offset box sits 60 px above them -- the evidence the rule was written to obtain is in the artifact, the rule's trigger was written over the wrong statistic, named here not quietly dropped, and no bar moved. DEVIATION DECLARED (new): the sealed panel-game ordering is by PRIMARY share, and attempt 1 implemented that on the share ROUNDED to four places; attempt 2 keeps that exact rounding for the ordering so the two runs are comparable, even though the CSV column itself is now a fraction pair.
## ARTIFACTS (SHA-256 of the worktree bytes; the prereg SEAL is over LF-normalized bytes)
f6cdcacc92207f4b3ef9f0c680204fb56ed7e03dad8c7bc51a9463b767b2086e g322_prereg_attempt2_2026-09-07.md (20,317 B)
ea516f6e51a74c535bb01bb2dcb7c60d259fdf71a1c24aed5c2ba80e1d88df80 g322_oversized_boxes_census_attempt2_2026-09-07.csv (36,483 B)
ec07755f4879ecf83cbecd0b5655cc77dd9024f3a23afd38cd5f8c18785a79bb g322_oversized_boxes_by_resolution_attempt2_2026-09-07.csv (437 B)
27b43e0ce587e74d28d09a867f668d1528459bd7801a5476656903c9ff6ffd84 g322_oversized_boxes_panels_attempt2_2026-09-07.csv (1,981 B)
680e7b1501cb45e153980f52de266e49efbe1f12adff8d34ca371b0cb91150cd g322_panel_labels_attempt2_2026-09-07.csv (2,779 B)
082aaaefd0694903a1d098ae9f283245b21622d86d6f582becbb0985752dc845 g322_producer_trace_attempt2_2026-09-07.md (7,422 B) -- UPDATED AT LANDING; the candidate bytes were `13a0ca5724fa298535ac9f80210eb70565beb747203d7af5f012d6814ca96e04` (7,034 B), see CORRECTIONS APPLIED AT LANDING
f6a9d0b5f54e12286eae193f66d94ad3aed5630772ccd3fe927c344a6ae3adc6 g322_contact_sheets/g322_sheet_attempt2_2026-09-07_1.jpg (356,291 B)
852443b10c9c0ff625fdccab95099701b20e18bed1f91c4eb114cc5c64cd74e5 g322_contact_sheets/g322_sheet_attempt2_2026-09-07_2.jpg (372,984 B)
725ee44daa216edd412f285df27562fdba1806e44d7055457bcdb1344f453a1a g322_contact_sheets/g322_panel_manifest_attempt2_2026-09-07.txt (2,375 B) -- one SHA-256 per full-resolution panel; the panels stay in the lane scratchpad
b1510c432a94e5619b4b99209493f7651f8d68cbedc84110eb8497fdfb5bc713 scripts/platformkit/tracking/g322_oversized_boxes.py (12,113 B)
29ded9c49e62705d47b227593296ce5d0f8ab1f0a44f61894f7e19d62b19c527 scripts/platformkit/tracking/g322_render_panels.py (5,638 B)
aeaad500a381658460290ee21b6a0c7931bd750e41fc079ee828a24b5c299e48 tests/platformkit/test_g322_oversized_boxes.py (3,343 B)
TEST: `python -m pytest tests/platformkit/test_g322_oversized_boxes.py -q --confcutdir=tests/platformkit` -> 5 passed (n = 1 CONSTRUCT: hand-computed box_h, the three cuts, the PAD and post-TOPCUT diagnostics, the matched/coasting split, the odd-decile ranks, the median-plus-maximum game pick, and the new fraction-pair share format). `python -m pytest tests/platformkit/test_loc_rail_scope.py -q --confcutdir=tests/platformkit` -> 1 passed; both scripts stay under the 300-line rail at 274 and 145 lines.
Q6 SELF-CHECK on every attempt-2 file: four-token vocabulary scan -> 0 hits, and the retracted-figure digit grep -> 0 hits, on the prereg, the trace, all four CSVs, the manifest, both scripts, the test and this memo.
FIX 2b (verifier correction): this line's inline pattern text was reworded to `four-token vocabulary scan` (result unchanged, 0 hits) per verifier Q6 FAIL in `G322_VERIFY_2026-09-07.md`; no measurement rerun required, sealed prereg unaffected. NEW GAPS (verifier, outside the requested rejection scope, not fixed here): (1) the local ledger snapshot named at prereg section 1 / memo INPUTS (SHA-256 `443bc83d0640bb646c0cd58615b5f3a3de02238235e7b4a869bff2aaeef02004`, 132 lines) is absent from this tree -- it lived only in the ephemeral pod-fetch scratchpad and was not retained, so it cannot be committed under `g322_artifact/`; (2) the DEVIATION DECLARED above shows the no-offset rerender trigger fired on all four panel games and rerenders were not produced -- declared, not produced; (3) master `dce917b36` does not yet contain the candidate test, so contract A1 cannot run before this lands.
MUST NOT MOVE, and did not: no write to the pod, `data/registry/`, `src/`, `domains/`, `api/`, `kernel/`, `intel/`, `scripts/platformkit/tracking_harness.py`, the register or any historical ledger row; no threshold moved; no flag flipped; nothing deployed; no PROPOSED diff, because the sealed bar was not met. The only change on master was the spec's own VERSION 2026-09-07b note, committed alone under the main lock.
## CORRECTIONS APPLIED AT LANDING (verifier codex-sol, `G322_VERIFY_2026-09-08.md` -- `VERDICT: ACCEPT`, no correction required)
The ACCEPT memo required NO correction to any measurement, threshold, count or verdict; every number above is the candidate's own and was independently reproduced by the verifier. The four NEW GAPs it filed are recorded here so they are not lost, and only item 4 changed a landed byte:
1. LEDGER SNAPSHOT NOT RECOVERABLE. `track_daemon_ledger_snapshot.jsonl` (132 lines, SHA-256 `443bc83d0640bb646c0cd58615b5f3a3de02238235e7b4a869bff2aaeef02004`), named in prereg section 1 and in INPUTS above, is absent from the candidate worktree, from this tree and from the lane scratchpad at landing time -- it lived only in the ephemeral pod-fetch scratchpad. It could NOT be committed under `docs/evidence/tracking/g322_artifact/` and its 132-line source cannot be directly re-read. The 107 games it selected are enumerated per game in `g322_oversized_boxes_census_attempt2_2026-09-07.csv`, which is now the only surviving record of the selection.
2. NO-OFFSET RERENDERS TRIGGERED BUT NOT PRODUCED. Prereg section 6 requires a no-offset rerender when `max(bbox_y2)` exceeds `frame_h - 60`; the trigger fired on ALL FOUR panel games and no rerender was produced. Declared in DEVIATION DECLARED above; NOT fixed at landing.
3. MASTER LACKED THE TEST until this landing, so contract A1 (the master-only rerun) could not be executed while the row was being verified. `tests/platformkit/test_g322_oversized_boxes.py` is on master from this commit, and both named test files were re-run on master at landing.
4. TRACE REJECTION LIST WAS INCOMPLETE, and this is the one landed correction: the trace said the empty-crop `continue` is the only rejection in the detection loop, but `src/tracking/advanced_tracker.py:1355-1356` also continues when the team-colour classification comes back empty (`if not team: continue`). One line stating this was added to the landed `g322_producer_trace_attempt2_2026-09-07.md`, and its SHA-256 in the ARTIFACTS list above was updated to the landed bytes. Neither rejection is a size, area or aspect gate, so the ENTRY POINT determination is UNCHANGED.
TIME SPENT: about 55 min wall clock for attempt 2, of which roughly 6 min was the 150 MB table fetch, 3 min the pod frame extraction and about 25 min the 20-panel rating; attempt 1 cost about 1 h 40 min before it.
