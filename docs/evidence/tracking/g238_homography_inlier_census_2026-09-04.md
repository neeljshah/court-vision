# G238: homography inlier census could not start

**Verdict: NOT VALIDATED.** Neither permitted observer launch reached
`run_clip.py`, `_load_pano`, or `_get_homography`. Therefore this row has zero
homography observations, no inlier or match distribution, no acceptance-tier
counts, and no observed panorama code path. The requested result is not
available from these attempts.

This memo follows `docs/evidence/tracking/VERIFIER_CONTRACT.md`. It makes no
production source, threshold, gate, coordinate-contract, corpus, legacy-table,
daemon, keeper, or pod-deployment change.

## Machine, source, and load hold

The measurement machine was the pod at `/workspace/nba-ai-system`, because its
resident WNBA clip and daemon-equivalent route are the requested execution
environment. The external observer was launched from this worktree at
`C:\Users\neelj\nba-track-a6`; it dynamically compiled method copies in its
own Python process and restored the class methods in `finally`. It did not
write a byte to `src/` or the pod checkout.

The intended source, independently recorded by both launches, was:

| Input | Observation |
|---|---|
| Full path | `/workspace/nba-ai-system/data/footage_corpus/wnba__wnba_01.mp4` |
| Bytes | 2,931,985,407 |
| Resolution | 1920x1080 |
| Frames / rate | 174,430 / 30/1 |
| `scripts/run_clip.py` SHA-256 | `7aec5d57e0357ff4585deabeb7a18bbc5bcb2c197cefe5fad591c16ca3bc761b` |
| `src/pipeline/unified_pipeline.py` SHA-256 | `047dd04e9b12b588c560f68dbab32aa1855f791c2e1a46f19f4e082f50c4f331` |

Before each write probe, a read-only `/proc` census selected Python processes
only when an argument's exact basename was one of `run_clip.py`,
`g235_build_court_crash_confirmation.py`,
`g236_label_reindex_existence.py`, or
`g238_homography_inlier_census.py`. Both censuses returned an empty active
transient-route set, so the two-lane hold was clear at 2026-09-04T07:25:08Z
and 2026-09-04T07:26:10Z. Permanent residents were neither counted as this
hold nor waited on, stopped, or restarted. This named-argument check cannot
detect a lane launched exclusively through stdin without a named script
argument.

## Disk guard and cleanup

`df` was not used. Each attempt recorded `du -sm /workspace/nba-ai-system/data`
and passed `dd if=/dev/zero ... bs=1M count=4 conv=fsync` before it created its
new tracking directory.

| Attempt | Pod data before probe | Probe | Pod temporary root bytes | Tracking-dir bytes | Pod bytes freed | Cleanup verification |
|---|---:|---|---:|---:|---:|---|
| 1 | 32,268 MB | passed | 1,064 | 0 | 1,064 | both exact paths absent |
| 2 | 32,272 MB | passed | 1,028 | 0 | 1,028 | both exact paths absent |

The two local returned bundles totalled 2,298 bytes and were deleted after
their facts were transcribed here. The focused test's two generated bytecode
files totalled 16,252 bytes and were also deleted. These are temporary
observations, not retained evidence. Total temporary bytes freed were 20,642
bytes: 2,092 pod bytes plus 18,550 local bytes. No corpus source or legacy
table was deleted.

## Attempts and observed runtime state

The observer was designed to record each computed match's inlier count, mask
size as its total-match count, acceptance tier, installation state, matcher
path, bootstrap state, and the `_load_pano` source branch, shape, and MD5. It
also counts `_get_homography` calls that reuse a prior matrix separately, so a
distribution would have named its computation denominator rather than treating
reuse calls as matches.

Both attempts imported the actual pod module before failing, so the runtime
constants were observed as `_H_MIN_INLIERS = 5` and `_H_RESET_INLIERS = 40`.
Neither produced a panorama record or a homography call.

| Attempt | External instrumentation result | `run_clip` entered | Homography calls | Rows | Panorama | Inlier / match distribution |
|---|---|---|---:|---:|---|---|
| 1 | Dynamic `_get_homography` source assembly stopped with `G238 homography final-return insertion point not found`. | no | 0 | 0 | not observed | unavailable |
| 2 | A different, final-return position strategy passed that stage, then dynamic `_load_pano` assembly stopped with `G238 panorama insertion point not found`. | no | 0 | 0 | not observed | unavailable |

These are instrumentation setup failures before pipeline construction, not an
`_build_court` crash, a zero-row route completion, or an observed failure of
the basketball tracking route. Per the two-attempt limit, no third attempt was
made.

## G237-INLIER-FLOOR conclusion

**Not determined.** G237-INLIER-FLOOR neither survives nor is refuted by this
row: no runtime inlier count was observed. In particular, this result does not
support the claim that counts sit at 5--7, and it does not support the opposite
claim that they are healthy. It also does not establish which panorama the run
would have used.

## Focused construction check

The isolated observer and its test are
`scripts/platformkit/tracking/g238_homography_inlier_census.py` and
`scripts/platformkit/tracking/test_g238_homography_inlier_census.py`. The test
was run after each observer revision:

```text
python -m pytest scripts/platformkit/tracking/test_g238_homography_inlier_census.py -q
2 passed
```

The test checks the process-only restoration and required observer/guard
construction. It does not validate source insertion against the pod's live
method body, as the two setup failures demonstrate.

## NOT VERIFIED

- Any actual `_get_homography` inlier count, match count, matcher choice,
  acceptance tier, or installed matrix on this route.
- Which panorama branch, path, shape, or MD5 this route would use.
- Whether the bounded 1,200-frame route would reach the frame loop, emit rows,
  or hit `_build_court` on this draw.
- Repeatability: G235 already showed this shared route can vary across runs;
  this row has no route draw at all.
- Coordinate correctness. An inlier count alone would not establish it even if
  the requested census had run.
