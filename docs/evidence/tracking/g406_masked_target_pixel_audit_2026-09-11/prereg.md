# G406 sealed preregistration

## Scope

This is a diagnostic-only native-pixel person-target audit of the G402 accepted export. It neither establishes an independent teacher nor qualifies targets for training.

## Frozen inputs and selection

The finisher must first reproduce the binding G402 accounting of 3,075 candidate rows over 19,087 evaluated ticks, 59 of 60 completed windows, zero forbidden target admissions, and the exact 30-per-kind card draw. It must join the blank target-mask bounds from authoritative G402 draw and evaluated-tick manifests. Missing bindings remain retained as UNKNOWN or PARTIAL; no tick replacement is allowed.

All 60 exact-even native ticks, including silence frames and raw out-of-frame boxes, are frozen before any inference or adjudication. Full inputs, source manifests, receiver paths, bytes, native dimensions, PTS, producer rows, target_weight, and mask_reason are retained. Source identities use this worktree root when a quoted path has another nba-track-aNN prefix.

## Frozen comparator and review sequence

The comparator is G402 YOLOv8n person with expected raw weights SHA-256 f59b3d833e2ff32e194b5bb8e08d211dc7c5bdf144b90d2c8412c47ccfc83b36. The finisher records actual paths, byte hashes, route hashes, inherited preprocessing, class selection, image size, confidence, and suppression arguments before one pod-scratch invocation. An absent exercised argument is PARTIAL; no default is substituted.

Before any overlay is opened, a reviewer marks every visible person and uncertainty on every full native frame, including sideline and crowd persons. The blind timestamp must precede the overlay timestamp for every tick. The finisher then associates native-pixel boxes using maximum-IoU one-to-one assignment, IoU at least 0.50, with lexicographic tie resolution. It retains unmatched boxes, ambiguity, candidates, disagreements, and silence frames for adjudication.

## Outputs and limits

Per-kind tables retain ALL candidate rows, incorrect and uncertain candidate rows, missed blind-visible persons, silence over all 30 ticks, conditional matched geometry, excluded masks, decoded/evaluated window denominators, and sampled-tick denominators. Two fresh processes regenerate tables and cards from delivered outputs and blind/adjudication records. The export has zero training callers and no accepted-reference promotion.

No rating, inference, scoring, source decoding, pod job, deployment, restart, flag change, registry write, register write, or ledger write occurs in this preparation commit. The finisher owns those measurement steps and must append its RESULTS_LEDGER row with the completed evidence commit.
SEAL sha256 88449bac72ab8bbe972faf01944e3d53c60c5a95342205dc32fddf1099d7aba5
