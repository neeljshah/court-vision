# G408 preparation inventory (SUPERSEDED 2026-09-11)

Every deferred item below was delivered by the finisher in this same directory. The file is retained unchanged beneath this note so the preparation handoff stays auditable.

---

# G408 preparation inventory

This directory is a preparation handoff, not a completed measurement. The Claude finisher must create the remaining required receipts from independently sealed G401 native schedules and retain every required path before assigning a final verdict.

Prepared now:

- `prereg.md` sealed on disk before comparison.
- `base_hashes.json` records the gated-file bases for the un-applied proposal.
- `PROPOSED_g408_pts_duration.diff` is a text-only three-interface proposal.
- `scripts/platformkit/tracking/g408_stop.py` supplies pure PTS mechanics and 30 construct controls.
- `scripts/platformkit/tracking/g408_proposal.py` validates proposal paths, anchors, and base hashes.

Deferred to the finisher: source receipts/hashes, native PTS decode, real-source tables, cards/renders, reader survey, two saved-input reproductions, Q6 scan, SHA256SUMS, summary, and final results-ledger entry.
