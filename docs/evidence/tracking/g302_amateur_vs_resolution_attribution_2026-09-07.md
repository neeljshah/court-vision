# VERDICT: NULL -- G302 attempt 2, the 0.597 -> 0.347 PLAYER gap does NOT decompose (resolution +0.014, amateur-at-matched-resolution 0.000, 0.236 of 0.250 UNRESOLVED)

## Verdict

**NULL: THE GAP DOES NOT DECOMPOSE. On the ORIGINAL G273 and G280 sources, at matched 720p, the resolution term is +0.014 (nominal p 0.867) and the amateur-at-matched-resolution term is EXACTLY 0.000 (nominal p 1.000); together they span 0.014 of the prior 0.250 PLAYER gap, leaving 0.236 - 94.4 percent of it - UNRESOLVED and attributed to nothing.** ARM 1 broadcast 1920x1080 native **34/72 = 0.472 PLAYER**; ARM 2 the SAME decoded frames downscaled to 1280x720 **33/72 = 0.458**; ARM 3 the amateur clip native 1280x720 **33/72 = 0.458**. Denominators: 72 crops per arm, 216 pooled, 1152 processed frames per arm, 3 arms, 2 source clips plus 1 in-memory downscale of one of them, **1 rater which is a MODEL (Codex, GPT-5), not a human**, 1 draw per arm. The spec says a null is a full success and this is reported as one. **No percent-of-gap survival is claimed** and no production change, filter, threshold, gate or retrain is proposed. **This row does NOT correct, replace or re-judge any G273 or G280b verdict.**
**The single largest unmeasured candidate for the 0.236 is RATER DIFFERENCE.** On G273's own population this rater read 34/72 PLAYER where G273 read 43/72, and on G280's own clip it read 33/72 where G280b read 25/72, with 0/72 NOT A PERSON against G280b's 37/72. G291 measured kappa 0.283 between two raters on exactly this task, so that spread is expected and is NOT evidence that either prior row was wrong. This row cannot separate rater from content and does not try.

## Sources: the ORIGINAL G273 and G280 populations, recovered and proved (A9)

**Attempt 1 was REJECTED because both named sources were lost in the 2026-09-07 pod rebuild and SUBSTITUTES were measured. Attempt 2 recovered both and proves it**, before the sealed prereg and before any detector; reports in `g302_amateur_resolution_attribution_artifact/identity/`.

| | BROADCAST = the G273 population | AMATEUR = the G280 population |
|---|---|---|
| file the detector opened | `/workspace/g302_attempt2/g302_broadcast_1080.mp4` | `/workspace/g302_attempt2/g302_amateur_720p.mp4` |
| recovered from | the retained bridge partial `data/videos/bridge/wnba_01.f137.mp4` of `youtube.com/watch?v=1zPhldjbJnU`, the recorded `source_url` for `wnba_01` | re-fetched with G245's committed command, `-f '232+233' --download-sections '*00:20:00-00:22:00'` on `youtube.com/watch?v=jh3fnwMi7dM` |
| bytes / master recorded | 2,841,750,689 / 2,931,985,407 | 24,523,745 / 24,523,745 |
| resolution, fps | 1920x1080, 30 -- EQUAL to record | 1280x720, 30 -- EQUAL to record |
| frames, duration | 174,430 / 5814.333 s (master records neither) | 3,729 / 124.3 s -- BOTH EQUAL to the G280 run manifest |
| SHA-256 | `f2421bc24e5cbb28f41fa79f9ea755b2eeff4daebd48dc5496cc97e5617cf9d3` | `773e77669a8876c0c8807baa8f733530ed00413f989cdec49ca078229b9e1bea` |
| byte hash vs master | **DIFFERENT**, and reported as different | **IDENTICAL** to the hash G245 committed |
| reproduction of the prior row's 72 committed blind crops, POD file | **72 / 72** under 3 MAD; 1.2219 mean, 1.8835 max | **72 / 72** under 3 MAD; 1.1703 mean, 1.8215 max |
| same check on the LOCAL file before shipping | **72 / 72**; 1.5446 mean, 2.5103 max | **72 / 72**; 1.4117 mean, 2.1334 max |
| verdict | **SAME POPULATION** | **SAME POPULATION** |

The broadcast byte hash CANNOT match: the recovered file is the 1080p H.264 VIDEO stream and the lost file was that stream muxed with its audio, so a hash match is impossible and is reported as DIFFERENT rather than as a failure. The decisive check is CONTENT REPRODUCTION - re-rendering all 72 of the prior row's committed blind crops at that row's recorded source frames and footpoints with the same 512x640 geometry, central 48x48 marker patch excluded. All 288 re-renders, 144 local and 144 on the pod copies, land at JPEG-requantization distance (worst case 2.51 of 255). **Both sources reproduce all 72 committed checks frame for frame; only G280 is byte-identical, while G273's container hash differs.** Both were shipped whole to pod scratch as `.part` then renamed, never into the daemon's `data/footage_bridge` stage; md5 `62ff0c41c20576fb5f5c399b0ceca94a` and `b1613abde31994781e9e18dd1effc991` matched on both sides.

## Design, premise (Q8), seal, blinding, determinism

DESIGN. ARM 1 vs ARM 2 isolates RESOLUTION on identical content: one decode pass feeds both, ARM 2 taking `cv2.resize(..., (1280,720), INTER_AREA)` of the very same frame before the detector sees it. ARM 2 vs ARM 3 isolates AMATEUR-vs-BROADCAST at matched 720p. Every arm uses the identical grid SHAPE - stride 3, 1152 processed frames, 72 equal-width bins of 16 processed frames - over its own row's span: ARMS 1 and 2 over source frames 19599..23052, inside G273's own inherited span [19599, 23399]; ARM 3 over source frames 0..3453 of the G280 clip, on the stride-3 lattice the G280 production tracker emitted. One crop geometry (512x640 in that arm's own source pixels) and one detector call imported unedited from the human-gated `src/tracking/player_detection.py` (`yolov8n`, `imgsz=640`, `classes=[0]`, `conf=0.3`, `half`, device 0) serve all three arms. No fourth difference is introduced.
PREMISE RE-MEASURED, TRUE, from committed artifacts rather than quoted. Joining G280b's `blind_verdicts.csv` against its `unblind_map.json`: **G280b n=72, PLAYER 25/72 = 0.347, NOT A PERSON 37/72 = 0.514**. Recounting G273's `unblinded_rows`: **G273 n=72, PLAYER 43/72 = 0.597, NOT A PERSON 15/72 = 0.208**. The prior PLAYER gap is **(43 - 25)/72 = 0.250000** exactly. `RESULTS_LEDGER.md` held ONE `| G283` row and **ZERO `| G302` rows**, so no row had ever compared the two sources at matched resolution and this row's ledger line is an APPEND.
SEAL. The prereg seals `f7028693dfdae4c428d2599a4723ff93673fb1c82faef51e04596f6b6e1c1aa2` over LF-normalized bytes before the hash assignment, with terminal newlines stripped, re-verified from `git show HEAD:<path>` after being committed BY ITSELF at `f6d7609c2`, before any detector ran. `f6ecff768` then committed the harness, both identity reports, the 216 crops, the presentation order, the BLANK verdict sheet, the per-frame counts, both ARM 1 determinism draws, and `blind_order_commitment.json` declaring pool size 216, 72 per arm and unblind-map SHA-256 `37cb6f521bb821ccf1d33a5fea85e019272a89239414a7427ae15db2961c1dc5`. The completed sheet was committed BY ITSELF at `4ff920485`; **`private/` did not exist in this repository at that moment - the map's only copy was pod scratch until after that commit** - and on fetch it reproduced that exact sealed hash. **THERE IS NO RERATE: all 216 were rated once, in the spec's four fixed categories, and nothing was re-scored after unblinding.**
INTERLEAVING CONFIRMED: one shuffled pool, every render named `blind_NNN.jpg`, and all 216 decode to exactly 640x512x3, so no arm label, filename, path or image dimension distinguishes an arm. **ARM 1 DETERMINISM: BYTE-IDENTICAL.** ARM 1 ran twice in one pod session over the same span with the same imported call, and BOTH draws are committed as files - `arm1_detection_rows_first.csv` and `arm1_detection_rows_repeat.csv`, **9,303 rows and 194,701 bytes each, both file SHA-256 `dd1b5579785c4114fb0d9125ed1a5ce29b3ae1e85d9d68585278daf9c4e1817b`**, canonical row hash `bc4e2ac2c0527ee6acb2ac7c2a3dcbc383ef8a5cb33578d5359891059de4c94c` - so a verifier recomputes it without the pod. Cross-SESSION determinism is not established and is not claimed.

## Counts (72 crops per arm; detection volume over 1152 processed frames per arm)

| Row | ARM 1 broadcast 1080 native | ARM 2 broadcast 720 downscaled | ARM 3 amateur 720 native |
|---|---:|---:|---:|
| (a) PLAYER on the court of play | 34 / 72 = 0.472 | 33 / 72 = 0.458 | 33 / 72 = 0.458 |
| (b) PERSON NOT PLAYER IN PLAY | 32 / 72 = 0.444 | 32 / 72 = 0.444 | 39 / 72 = 0.542 |
| (c) NOT A PERSON | 2 / 72 = 0.028 | 3 / 72 = 0.042 | 0 / 72 = 0.000 |
| (d) CANNOT JUDGE | 4 / 72 = 0.056 | 4 / 72 = 0.056 | 0 / 72 = 0.000 |
| (b)+(c) reported grouping | 34 / 72 = 0.472 | 35 / 72 = 0.486 | 39 / 72 = 0.542 |
| total detections | 9,303 | 9,357 | 20,083 |
| detections per processed frame | 8.076 | 8.122 | 17.433 |
| processed frames with zero boxes | 3 | 4 | 0 |

**ARM 3 emitted 2.16x more boxes per processed frame than either broadcast arm (17.433 vs 8.076), so its category SHARES are not a like-for-like yield on a like-for-like box count; both rows must be read together.** All 8 CANNOT JUDGE crops fall in the two broadcast arms, which cut to close-ups the amateur fixed camera never takes; that is a content difference between the arms, not a resolution effect, and it is not redistributed into any other category.

## The two declared tests (nominal two-sided p, NO multiplicity correction), and the decomposition

Sign convention: `z = ((left - right)/72)/se`, so a POSITIVE `player_drop_*` means the right arm lost PLAYER share.
- **RESOLUTION, ARM 1 vs ARM 2, PLAYER:** 34 vs 33; pooled p 0.465278, SE 0.083132, **z 0.167070, nominal p 0.867315**. NOT A PERSON: 2 vs 3; pooled p 0.034722, SE 0.030513, z -0.455186, nominal p 0.648975. `player_drop_resolution = +0.013889`.
- **AMATEUR AT MATCHED RESOLUTION, ARM 2 vs ARM 3, PLAYER:** 33 vs 33; pooled p 0.458333, SE 0.083043, **z 0.000000, nominal p 1.000000**. NOT A PERSON: 3 vs 0; pooled p 0.020833, SE 0.023804, z 1.750380, nominal p 0.080053. `player_drop_amateur_matched = 0.000000`.

**ONE-SENTENCE ANSWER: of the 0.597 -> 0.347 PLAYER gap, this row attributes +0.014 to resolution and 0.000 to amateur-ness at matched resolution - the two terms sum EXACTLY to the measured ARM 1 -> ARM 3 gap of 0.013889, and the remaining 0.236111, 94.4 percent of the prior 0.250000, is UNRESOLVED and attributed to nothing.** `+0.013889 + 0.000000 = 0.013889`; `0.250000 - 0.013889 = 0.236111`. **Neither downscaling identical broadcast frames nor swapping to the amateur clip at matched resolution reproduces the prior gap on these same populations. The gap does not decompose along the two axes this row can measure.** The direction of both terms is the finding, and rater difference, span subsampling and one-draw sampling all sit inside the unresolved remainder undistinguished.

## ACCEPTANCE RULE applied line by line

- Per-arm counts in all four categories over 72 plus the (b)+(c) grouping: **PASS** (table above). Per-arm detection counts and detections per frame: **PASS** (9,303 / 9,357 / 20,083; 8.076 / 8.122 / 17.433).
- ARM 1 determinism check: **PASS within the session, byte-identical, both draws retained as committed CSVs**; cross-session not established and not claimed. Sealed-pool record (pool size 216, unblind-map SHA-256 `37cb6f52...`, blank sheet committed at `f6ecff768`, completed sheet committed ALONE at `4ff920485` before the map existed in the repo): **PASS**. Interleaving confirmation: **PASS**.
- Both two-proportion tests with nominal p and the explicit no-correction statement: **PASS**. One-sentence decomposition with the unresolved remainder named: **PASS**. Bar: none exists; a large resolution effect, a large residual amateur effect and a null are all full successes, and the measured answer, a null, is reported as found: **PASS**.
- n = 216 = 3 x 72, all 216 rated once, no arm rated more thinly than another: **PASS**, so NOT CLOSED AT LIMIT. Eye check, 6 EVENLY SPACED crops per arm at bins 6/18/30/42/54/66, 18 rows with post-unblind verdicts, no head slice: **PASS** (`eye_check.csv`).
- Must not move: **PASS** - `src/` and `domains/` read and imported only, `player_detection.py` unedited, no production default touched, `/workspace/nba-ai-system` never written, `yolov8n.pt` read only through a link, G273's and G280b's committed crops, sheets, counts and verdicts untouched, G298's counts untouched, no corpus source and neither bridge partial download deleted, the register untouched, `TRACKING_GAPS_2026-09-01.md` untouched, the ledger APPENDED with one line and never rewritten.

## Machine, probes verbatim, bytes, tests

Detection ran on the pod (RTX 3090) in compute-only scratch `/workspace/wt/a14/jobs/g302_attempt2_prepare` via `~/bin/pod_run`, sources in `/workspace/g302_attempt2/`; the deployed `/workspace/nba-ai-system` tree was never written. Crop rating and every arithmetic step ran locally in `C:/Users/neelj/nba-track-a14`. GPU gate before launch, verbatim: `1932439, 484 MiB` / `1946992, 822 MiB` / `1962254, 486 MiB` / `1965040, 484 MiB` / `2000837, 482 MiB` / `2013217, 462 MiB` / `2027185, 482 MiB` / `2033292, 480 MiB` / `2033178, 462 MiB` / `2040762, 482 MiB` / `2040791, 484 MiB` / `2040919, 484 MiB` / `2047452, 482 MiB` / `2029103, 522 MiB` / `2073067, 426 MiB` / `2075710, 442 MiB` / `2099744, 426 MiB` / `2136238, 426 MiB`, with `8924 MiB, 24576 MiB` used of total, about 15.3 GB free. Nothing on the pod was killed, interrupted or restarted. Disk guard verbatim: `v=$(timeout 60 du -sm /workspace | cut -f1); [ -z "$v" ] && v=UNKNOWN` printed **`workspace_MB=UNKNOWN`** (empty means UNKNOWN, never 0, and is not a stopping condition); `pod_run` printed `POD /workspace 37668 MB used of 50000; /workspace/wt 17573 MB` with `WARNING /workspace/wt above 3 GB -- ping the tracking session`. Environment: torch 2.8.0+cu128, ultralytics 8.4.143, cv2 4.14.0 (NOT version 5), `resources/` present. A11: `src/tracking/player_detection.py` SHA-256 `532389342b93e259e01f2d177ea34ccb1f52dc1c4b9b7f62aed2bf07c7df785a`; `g302_amateur_resolution_attribution.py` `2180ee77b8a2137be477272b94107e2f1910315d0fdc03406198b02e427267fe`; weights read through a link to the pod's pinned `/workspace/nba-ai-system/yolov8n.pt`, SHA-256 `f59b3d833e2ff32e194b5bb8e08d211dc7c5bdf144b90d2c8412c47ccfc83b36`, so ultralytics downloaded nothing. ARTIFACT SHA-256s: prereg `6acf0b5972afcb88a3157cc22f215fba11869e0895337c3072022e19b9bfc8dc`; order commitment `a115ca14f7bda7ea4df9e41252c8dc7c6682db18b5e9ae4da2063255e97014a1`; presentation order `6e2d66983f43f43439359571de3ec20909c65a123e86fbd6d0e9fc5bdda2db8a`; arm detection summary `eed140ce92d5e452da40c31fdaa70722d33f6b01471c018542f991f16a48578b`; per-frame counts `01c84642f39c093eadc9014f37579c3652e42b29cecda21f0961a7b56c799262`; identity reports `643f1e05bdb4eb3e3636d5aee56943107573c2aaa5cbabd86d4f712ac753361d` (G273) and `2bbacef5eb81162184e10ffed03261307824a02f5b4f74105abd0a66a4727943` (G280b); completed verdict sheet, LF-normalized, `46215a473dd27510d87dcb22be8962372ea9a0fd1c7bdd8ca12057e0f8ddb077`. BYTES: about 12 MB of crops plus small CSV/JSON added under `docs/evidence/`; 2,866,274,434 bytes of source shipped to pod scratch and md5-verified both sides; **nothing freed and nothing deleted.** TIME: about 3 hours wall clock for the lane, of which the rater reports about 30 minutes on the 216 crops. TESTS: `python -m pytest scripts/platformkit/tracking/test_g302_amateur_resolution_attribution.py -q` -> **11 passed**; `python -m pytest tests/platformkit/test_loc_rail_scope.py -q` -> **1 passed**. A12: the lane's five Python files are 294, 147, 84, 34 and 161 lines, all at or under the 300 rail; **no allowlist entry was added or raised.**

## NOT VERIFIED

- **No human has rated these crops.** The rater is a MODEL (Codex, GPT-5), single and unreplicated. **G291 measured kappa 0.283 between two raters on this exact task**, so ARM 1's 34/72 is a SECOND RATER'S reading of G273's population, not a correction of G273's 43/72, and ARM 3's 33/72 is likewise not a correction of G280b's 25/72. **The 0.236 unresolved remainder is consistent with rater difference alone, and this row cannot and does not separate rater from content.**
- **A null is not proof of absence.** With n=72 per arm the SE on a PLAYER difference is about 0.083, so this row could not have detected a true effect much below roughly 0.17 at conventional confidence. It reports what it measured; it does not establish that resolution has no effect.
- **Crop field-of-view asymmetry:** a 512x640 crop in source pixels covers 1.5x more of the scene at 720p than at 1080p. That is inherent to matching resolution and the crops were NOT rescaled to hide it.
- **Downscale is not capture:** ARM 2 BOUNDS the resolution effect; a camera that actually shot 720p differs in encoder, optics, framing and motion blur, so ARM 2 does not reproduce amateur capture.
- **The spans are subsamples.** ARMS 1 and 2 cover 1152 stride-3 frames of G273's 3801-frame span and ARM 3 covers 1152 stride-3 frames of the G280 clip's 3729 frames; each is a subsample of its row's own population, not that population entire.
- **One draw per arm, two clips, one rater.** Nothing here is a claim about amateur video as a class, about either clip's whole detection population, or about any other arena, sport, camera or labeller. ARM 3's 2.16x box rate means its shares and its volume must be read together.
- Content still separates amateur from broadcast to a rater even though labels, filenames and dimensions do not; the blinding protected the crop-to-arm mapping, not visual indistinguishability.
- Detector recall, identity correctness, association, box extents, court space, and any causal mechanism for a (b) or (c) location are all outside this row.

## Corrections applied at landing

Applied literally at landing from the verify memo `G302_VERIFY_2026-09-07.md` (`VERDICT: ACCEPT WITH CORRECTIONS` on `8509d0343`; verifier codex-sol, contract A/B/Q):

- CORRECTION 1, this memo line 25. The sentence claiming the two recovered sources are the prior populations "frame for frame and byte for byte" is replaced by the verifier's sentence: **Both sources reproduce all 72 committed checks frame for frame; only G280 is byte-identical, while G273's container hash differs.**
- CORRECTION 2, this memo line 31. The seal is described as computed **over LF-normalized bytes before the hash assignment, with terminal newlines stripped**, replacing "over its LF-normalized bytes above the seal line".

Open notes the verifier raised at landing. Neither is an acceptance failure and neither is addressed by this row:

- NEW GAP: the stored source-identity MAD arrays reproduce their means and maxima, but the 2,841,750,689-byte G273 source was not reread under the verifier's 300 MB rail; future identity artifacts should store batch-recomputable pixel-difference inputs.
- NEW GAP: `g302_rating_manifest.py` has no importing test; it is not candidate-touched and is not an acceptance failure.
