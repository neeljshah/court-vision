# G372 preregistration

Spec: `docs/evidence/tracking/specs/G372_spec.md`.
Contract: `docs/evidence/tracking/VERIFIER_CONTRACT.md` B1-B10 and Q1/Q6.
Machine: pod read-only for live premise census and any measurement because the tracking stores may exceed laptop memory; local only for deterministic unit tests.

Premise binding, before any result: run exactly the two commands named in G372 under `/workspace/deploy/nba-ai-system`; preserve their complete stdout in the evidence memo. The deployed route digests must be recorded before an attribution statement. The local before-window route digests are `track_daemon.py` ee425ee56b0f92500d1d460f6da5610ddd1124b8c236f9f33169c2418d3fd420 and `track_daemon_done.py` f6f4cbd25c4dc4101c79fe61c852bd8942d7296a05c7dab3c62cbecfee397e5b.

Fixed live window: the first contiguous 60 minutes beginning with the first eligible claim after code ACCEPT; it is never chosen after results. All claim attempts, including missing, terminal-error, and in-flight states, remain in the claim manifest. The independent G369 pin is invoked once per claim within 60 seconds; the even sample is every floor(n/30)-th manifest row when n exceeds 30, otherwise all rows. No selected row may be omitted. A second 60-minute window is not substituted for a failed or insufficient first window.

Stopping rules: coverage, independent agreement, byte-flip, deterministic-read/write, producer-schema, and paired-throughput clauses are each NOT VALIDATED unless their exact stated denominator and bar are met. Zero eligible aged rows cannot establish the survival false-stop; fewer than 30 sampled sections cannot establish a sampled clause. There is no model, prediction, OOS loss comparison, or evaluator tick in this lane; if one is added, it must use `scripts/platformkit/eval_gate/walkforward.py` or `cpcv_engine.py` with purge, symmetric nonzero embargo, and one stable state per tick before any number is emitted.

Schema contract: the immutable sidecar has source path, source_sha256, source_bytes, ffprobe fields, crop_rule, route_digest, weight_digest, deploy_manifest_sha256, claim_utc, daemon_pid, and terminal diagnostic; a completion row gains only `source_identity_path`; claim-journal records cover all reservations. ABSENT is terminally journaled and passes tracking through without quarantine, retry loop, or sidecar rewrite.

The evidence package uses fixed CSV headers, an empty row set when the pod is unavailable, and a `NOT VALIDATED` result rather than inferred values. The proposed integration patch is a review artifact only and is never deployed by this lane.
SEAL sha256 6e6469a0dac4829a65e0c05ab2e705b08411452bdcd726ea835355a80901a75e
