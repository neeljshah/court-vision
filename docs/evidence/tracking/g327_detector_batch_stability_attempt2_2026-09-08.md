VERDICT: **NOT PINNED** -- no APPLIED isolation arm reaches the sealed >= 110/120 bar (`pad` 48/120 and MEASURED NOT APPLIED, `fp32` 0/120, `nmsord` 48/120, against a `batch8` baseline of 48/120) -- and the daemon route is **DETERMINISTIC 120/120**: the repeated single-image pass is bit-identical on every one of 3 games x 40 decoded frames, with 587/587 matched box pairs coordinate-exact and 0 thousandths of a pixel of movement. Denominators: 120 snapshotted frames, all 120 re-hashed and MATCHED before scoring (40/40, 40/40, 40/40); 7 GPU arms x 120 = 840 detector passes; 587 reference boxes; 0 zero-box frames on every arm. Confidence is the route's OWN `_fill_conf_threshold` **0.22**, which CONFLICTS with the registered 0.3 (G303's finding, restated here, not resolved).

**SCREENING ONLY. THERE IS NO GROUND TRUTH HERE.** This row says whether two passes AGREE, never which one is CORRECT. No recall, precision, registration, accuracy or pass claim; neither pass is better. Nothing adopted, no production default moved, no input size, confidence or batch size changed anywhere in `src/`. **No render, no eye check** -- this row is arithmetic over box coordinates and tensor shapes.

**PREREG `g327_prereg_2026-09-08d.md`, seal `fb668830470aff9abdff6b580771db97fe58c7ce4ab04f3cb61fc551fed2cb13`, committed ALONE as `541f2f331` BEFORE the scored run**, carrying only the prereg and the two raw files it references by sha256. It seals the 120 frame hashes verbatim, the selection rule, the 7 arms, the CSV-only reconstruction rule, and the 110/120 and 120/120 bars **byte-identical to attempt 1. No bar was moved (contract Q3).**

**PREMISE, AND IT IS STRONGER THAN ATTEMPT 1'S: G324's evidence is now IN THIS TREE.** It landed on master as `1a129aa90`, so `docs/evidence/tracking/G324_VERIFY_2026-09-08.md` and `docs/evidence/tracking/g324_artifact/` are addressable here and no absent path is cited. Read from those committed bytes: `g324_summary.json` carries production `yolov8n` `batch_vs_single` = identical_box_counts true / max_abs_coord_delta_px **310.5**, true / **861.0**, false / **null**, and RTMDet true / **0.0** on all three; and every committed G324 box CSV has the header `frame_index,x1,y1,x2,y2,score,class` with **no pass or arm column**. Row counts, qualified by suffix: the `_Y` (yolo) CSVs carry **343 / 320 / 326** rows and the `_R` (RTMDet) CSVs **741 / 541 / 729** rows, in the order wnba_01 / ncaa / wnba_06. That CONFIRMS from bytes -- not from a claim -- that only ONE pass was written and **the spec's per-index coordinate recompute is impossible from any committed G324 artifact.** Carried as an INHERITED SUMMARY, never recomputed. RTMDet was not re-run.

**THE ONE DEVIATION FROM THE ATTEMPT-2 AMENDMENT, MEASURED AND DECLARED IN THE PREREG BEFORE ANY DETECTOR CALL.** The amendment directs attempt 2 to read ONLY attempt 1's snapshotted frames. **Those frames DO NOT EXIST AS BYTES.** Attempt 1 held its decoded arrays in memory and recorded only their hashes; a `find` over `/workspace/wt/a21` returns no frame array or image at all, and all three of its clips have been deleted by the rotating corpus. Two consequences, both stated before the run: **"the realised clips (same as attempt 1)" is UNREACHABLE**, and **the cross-check that attempt 1's reference single pass reproduces its coordinate-exact 120/120 is NOT POSSIBLE** -- it needs those exact frames, and no arithmetic on different frames substitutes for it. What IS reported instead: the same sealed selection rule re-executed, and the analogous determinism result reproduced on an independent corpus (`single_repeat` 120/120 here, 120/120 there). **The fix that stops this recurring: the frames are now PERSISTED to disk before the seal and re-hashed at load, so this construct stays checkable after the clips rotate out.**

**SOURCES AS REALISED (recorded, never chosen; full path under `/workspace/nba-ai-system/data/footage_corpus/`).** `nba__m7K0J4wzDn4_s2070.mp4` 41,073,189 B, 1920x1080, 60000/1001, anchor **8043**; `nba__4PH6yOSvcs4_s2932.mp4` 48,368,543 B, 1920x1080, 60000/1001, anchor **7825**; `wnba__6FlZaoWSfFw_s90.mp4` 61,722,800 B, 1920x1080, 30000/1001, anchor **3908**. Fed array `[1020, 1920, 3]` on all three, the SAME shape G303 recorded on production. 40 evenly spaced indices spanning 0..anchor, 40 of 40 decoded per game, one decode reused by every arm.

**THE PER-GAME / PER-ARM TABLE does not fit these 60 lines and is committed in full as `docs/evidence/tracking/g327_attempt2/g327a2_table.md`, sha256 `b7da6cb75d3bdeee513d61dd72a3a03d0c07aa7e28175d633e6dc274a28504ce`** -- 18 per-game/per-arm rows plus a pooled table and a per-arm zero-box table, every cell recomputed from the seven per-arm CSVs. **The verbatim GPU, process and disk probe output is `g327a2_probes.txt`, sha256 `0268348e5010ccf170c6a12c13c738f1d74ae51060f14b517b6a2b98f44be990`.** **The mandated `du -sm /workspace` is committed verbatim as `g327a2_du_probe.txt`, 115 B, sha256 `b34eefb12d21c5bb23e4c3430d30a33cdf4b9c85aa0ba7a7464a117800e034be`: `21608\t/workspace`, run read-only between `2026-09-08 09:34:08` and `09:34:44 UTC` -- 36 minutes AFTER this run wrote its summary at `08:58:11 UTC`. Between the run and this probe the orchestrator pruned about 17.9 GB of landed job roots under `/workspace/wt` (REPORTED by the orchestrator, NOT measured by this row), so 21,608 MB is NOT the run-time figure; the run-time disk reading remains the `df -m /workspace` in `g327a2_probes.txt`.** **The verbatim pod log tail of the scored run (last 40 lines of `/workspace/wt/a21/g327a2/detect.log`, ending `DETECT_DONE rc=0`) is committed as `g327a2_pod_log_tail.txt`, 4,808 B, sha256 `3cd943bbfcf29acbd746dabd7b68f1bb623dd37f19eaf7abe0e11ef5698ba7ad`; it is kept byte-verbatim and therefore carries the non-ASCII warning glyph ultralytics emits (contract Q6 NOTE: raw machine output, unedited).** Pooled isolation result, denominator 120 frames in every cell:

| arm | batch | network tensor | dtype | factor applied? | bit-identical vs single |
|---|---|---|---|---|---|
| `single_repeat` | 1 | [1,3,352,640] | float16 | reference repeat | **120/120** |
| `batch2` | 2 | [2,3,352,640] | float16 | baseline | 46/120 |
| `batch8` | 8 | [8,3,352,640] | float16 | baseline | 48/120 |
| `pad` | 8 | [8,3,352,640] | float16 | **NO -- measured no-op** | 48/120 |
| `fp32` | 8 | [8,3,352,640] | **float32** | yes | 0/120 |
| `nmsord` | 8 | [8,3,352,640] | float16 | yes, its OWN pass | 48/120 |

**WHAT IS FALSIFIED BY MEASUREMENT, reproducing attempt 1's three falsifications on a different corpus.**
- **DYNAMIC PER-BATCH LETTERBOX: FALSIFIED.** The network tensor is `[B, 3, 352, 640]` at every batch size; H and W never vary. ARM PAD's measured shapes equal `batch8`'s, so it is a **measured no-op**, excluded from the pinning decision rather than counted.
- **NMS / EMISSION ORDERING: FALSIFIED, and now by an arm that ran its own pass.** `nmsord` is no longer derived from `batch8` (the attempt-1 defect): it runs its own batch-8 detector pass and sorts INSIDE the arm before writing its rows. It lands on 48/120, identical to the unsorted `batch8` baseline, with the same 465/587 coordinate-exact pairs. **The boxes are genuinely different values, not the same boxes reordered.**
- **HALF PRECISION: NOT THE CAUSE.** `fp32` was really applied (measured dtype float32) and made agreement **worse** (0/120), so single/batch disagreement is not simply fp16 rounding.

**HOW BIG THE DISAGREEMENT IS -- small, and the frame-level bar is strict.** Over IoU >= 0.5 one-to-one matched pairs pooled across the three games, coordinates only: `batch8` **465 of 587 pairs coordinate-exact**, and 311 of 587 exact including score and class (**coordinate-exact**, never bit-exact, is the correct word for the first figure); `batch2` 466/587 and 314/587; `nmsord` 465/587 and 311/587; `fp32` 0 of 586. Median matched-pair coordinate delta is **0 thousandths of a pixel** on every fp16 arm; the maxima are `batch2` 3,000, `batch8` 3,750, `nmsord` 3,750 and `fp32` 11,115 thousandths of a pixel. Box COUNTS are equal game-by-game on every arm, and both-way IoU >= 0.5 agreement is **587/587 in both directions** on all four fp16 arms and 586/587 both ways on `fp32`. A frame fails bit-identity if ANY of its boxes moves, which is why frame-level identity fails on most frames of the one busy clip while box-level agreement is total.

**CONSTRUCT LIMITATION, STATED BECAUSE IT BOUNDS EVERY NUMBER ABOVE.** The sealed rule selected two clips that return **exactly ONE box on each of their 40 frames**: 80 of the 120 frames carry a single box, and slot 3 carries 507 of the 587 reference boxes. So the pooled frame-level counts are dominated by easy frames, and this run's 48/120 is NOT comparable with attempt 1's 3/120 on busier clips. The rule was sealed before the run and was not re-rolled after seeing this.

**NEW GAPS FROM THE VERIFIER, BOTH CLOSED.** (1) *The live-queue claim lacked a raw census.* Raw `ls` listings are now committed as `g327a2_corpus_rotation.txt`, sha256 `71b478255c1475e7629eea0330ccce9076c50690e4f5973d44271e9e75974838`: **93 corpus files at 08:43 UTC** (section [6] of `pod_source_absence.txt`) and **20 about twenty minutes later**, two independent raw captures, with all three of this row's clips still present at both. Attempt 1's own 119 -> 85 transition was not re-observable and is NOT re-asserted here as this row's measurement. (2) *A cited path was absent from the tree.* G324's verify memo and artifact are now IN this tree at `docs/evidence/tracking/G324_VERIFY_2026-09-08.md` and `docs/evidence/tracking/g324_artifact/`, landed by commit `1a129aa90`; every G324 citation above is to those in-tree paths and to that commit.

**NOT VERIFIED.**
- **Which pass is CORRECT. There is no ground truth in this row**, so nothing here ranks single against batched, and no detection-quality claim is made.
- **The cause remains unpinned.** Only three factors were preregistered; cuDNN kernel selection by batch shape, reduced-precision reduction order and non-deterministic CUDA reductions are UNEXAMINED. **NOT PINNED means "not among these three", never "no cause exists".**
- **Attempt 1's coordinate-exact 120/120 was NOT reproduced and CANNOT be**: its frames were never written to disk and its clips are deleted. Only the determinism *result* is reproduced, on different frames.
- **G324's inherited figures were NOT recomputed.** The committed CSVs are shown above to hold one pass only, so no artifact can carry that recompute; the figures are quoted, not re-measured, and no claim is made about G324's code.
- The 11,115-thousandths `fp32` outlier was not investigated, and no box was looked at.
- **The realised corpus cannot be re-selected.** A rerun of the sealed rule on a rotated corpus picks different clips; only the committed per-frame hashes make THIS construct reproducible.
- **NEW GAP (verifier, fix 2b): the persisted frame arrays are NOT in this tree.** The 120 arrays the prereg names exist only in the pod job root `/workspace/wt/a21/g327a2` and total about 1 GB, which is not committable here. The committed per-frame sha256 list is therefore the durable record: a future reader can check a frame it already holds against a hash, but CANNOT re-hash the source bytes from this tree. The arrays are also subject to pod pruning and should not be assumed to persist.
- Determinism was measured **in one process on a shared card**; it says nothing about across-process or across-driver repeatability. `ms/frame` was recorded in integer microseconds and is contended-card, warm-up-included, and **not a benchmark**; no cost claim rests on it.
- The explicit zero-box CSV row is pinned by the per-file test but **had no realised instance**: this snapshot produced 0 zero-box frames on all 7 arms.
- ONE corpus, three clips, 120 frames, one GPU, one driver, one ultralytics version, one seed.

**INTEGRITY / MUST-NOT-MOVE.** Deployed `src/tracking/player_detection.py` `c3bc2f7d4c4fda36...`, `src/tracking/advanced_tracker.py` `df2ae698ae03e804...` and `yolov8n.pt` `f59b3d833e2ff32e...` are **byte-identical before and after** (`gated_unchanged: true`). GPU lease honoured: `1730 MiB, 24576 MiB` at pre-flight (22,846 MiB free, above the 8,192 MiB gate), `2325 MiB, 24576 MiB` after, with 4 other compute apps listed verbatim. **`ps` was run before and after and OBSERVED that pid 1039858 `vol_guard.py` was alive (elapsed 01:58:35, then 02:00:05) and that the named `track_daemon` pid 929149 was NOT present at run time; neither process was stopped, signalled or restarted by this row, and nothing was done about the missing pid.** `df -m /workspace` 62 pct used; `dd conv=fsync` rc=0 at 86.6 MB/s. All compute under `/workspace/wt/a21/g327a2`; the deployed tree was never written and no corpus file was written, moved or deleted. Pod route SHA-256 as deployed: `g327_arms.py` `02368e4fd3722376...`, `g327_sources.py` `459507ecfe5b8efd...`, `g327_report.py` `686e4fbf3bea036f...`, `g327_detector_batch_stability.py` `a0e02a4c0ad155ca...`. Env: Python 3.12.3, torch 2.8.0+cu128, ultralytics 8.4.143, cv2 4.14.0, RTX 3090.

**CORRECTIONS CARRIED from the verify memo.** "bit-exact" is used ONLY where score and class were compared too, and "coordinate-exact" everywhere else. Attempt 1's module line counts are **181 / 139 / 282**, not the 267 its memo printed. No path absent from this tree is cited. Attempt-1's memo and its three preregs are FROZEN. **Attempt 1's reject is cited by its verdict text, not by a path this tree does not hold: it opens `VERDICT: REJECT` / `Candidate: 668b9929b; full 8-file diff reviewed; evidence memo is 56 lines.`, and the prior reject is cited by its quoted verdict only; its full transcript is not in the candidate tree and is NOT VERIFIED here.**

**HARNESS FIX, the three acceptance failures removed.** Every arm now writes its OWN raw CSV at run time (`g327a2_boxes_<arm>.csv`, 587 rows each, 7 files); every evaluated frame is an explicit row, a zero-box frame being written with `n_boxes` `000000` and empty box fields; `nmsord` emits its own pass; and scoring reads the seven CSVs plus the run's own tensor record, **never `g327a2_summary.json`, for any reported value**. Coordinates and scores are stored as EXACT dyadic fractions `num/den`, zero-padded, which round-trip with no tolerance and are pure digits. Modules **293** / 155 / 200 / 275 lines, all inside the 300-line rail; the allowlist was not edited. **TESTS.** `tests/platformkit/test_g327_batch_stability.py` -- **16 passed** (the 10 attempt-1 properties, the explicit zero-box row round trip, the NMSORD own-emission rule, the attempt-1 nine-column legacy read, and three malformed-CSV rejections); `tests/platformkit/test_loc_rail_scope.py` -- **1 passed**.

**TIME.** About 4 h 20 min of lane wall (2026-09-08 03:20 - 07:40 CDT, the merge and harness fix included). The snapshot job finished at `20260908_085430` UTC and the scored 7-arm run wrote its summary at `20260908_085811` UTC (the compact stamp is the same collision-free form the raw listings use), so 840 detector passes took under four minutes on the shared card.

**ARTIFACT SHA-256 (all under `docs/evidence/tracking/g327_attempt2/`).** `g327a2_boxes_single.csv` 65,807 B `b76ecc457a0ff57ce7fab139a7648fb9a5177c2972ea69d9dea71da78f67c884`; `g327a2_boxes_single_repeat.csv` 69,916 B `e0e600910f30c71993d37b9a62b320b8f48c160ea768f70786e6100274d9d867`; `g327a2_boxes_batch2.csv` 65,807 B `a1ddafbf7d065a54b09dc99be2eee579e3bf40d1f45e1bee787ae27caff8d0dd`; `g327a2_boxes_batch8.csv` 65,807 B `169ccb3a3283388f9ea37976fc9e1b150e3a628e7073944cfc13b32f0ea63be4`; `g327a2_boxes_pad.csv` 64,046 B `b242cfcff7c8b08f7ee3fde790048aa8b01aca17d4be9932c9a6a05fa82ab7d7`; `g327a2_boxes_fp32.csv` 69,070 B `688519ef0010dab6b23b90f897db0310f2993ef19fa6dc726be22fb8f977c18a`; `g327a2_boxes_nmsord.csv` 65,807 B `36b73d8bf9a9a688092579e05adc0ada3c54b42a2b2680a82064a3ce1eff9f83`; `g327a2_arms.csv` 1,848 B `c693fc1f05b577f0c945c59160674903f21efbd0fb59b837ec170e649c97d18e`; `g327a2_frames.csv` 9,504 B `d564e094078f4c5e6ee39d2ac58595c40556645113ab666a1ab9c39ea0ad8e4f`; `g327a2_report.json` 11,440 B `c05cb90f80123d4de1df1d4a23167648fc462906bbe0aaad466fb20adbb81b6e`; `g327a2_summary.json` 14,623 B `995d597f4e12795d0c96259fdccc272a8836c7f906458c43f35cf095bc023b07`; `pod_source_absence.txt` 1,848 B `3d7ffb8054b51bbe838c6823f98a031758ff4b002a9fb4a1158a9eec0db20ae0`; `g327a2_du_probe.txt` 115 B `b34eefb12d21c5bb23e4c3430d30a33cdf4b9c85aa0ba7a7464a117800e034be`; `g327a2_pod_log_tail.txt` 4,808 B `3cd943bbfcf29acbd746dabd7b68f1bb623dd37f19eaf7abe0e11ef5698ba7ad`. Every box behind every cell above is in those seven CSVs, arm-labelled, in emission order, with an explicit row per evaluated frame, so a verifier can recompute bit-identity, the count deltas and the greedy IoU matching from the committed cells alone.

**Corrections (fix 2b, 2026-09-08).** The verifier's three acceptance failures and its B2 finding are addressed WITHOUT re-running any detector arm and WITHOUT touching the sealed prereg: the mandated `du -sm /workspace` and the scored run's pod log tail are now committed verbatim and cited above; attempt 1's reject is cited by verdict text plus the local run log's sha256; G324's row counts are qualified as `_Y` vs `_R`; and the persisted-frame-array gap is recorded under NOT VERIFIED. **B2 additive:** `read_boxes_csv`, `write_boxes_csv` and the three-argument `rows_for(table, arm, idxs)` are restored with their attempt-1 signatures and behaviour, and `read_arm_csv` gained a nine-column legacy branch, so attempt-1 CSVs and callers still work; `read_arm_csv` additionally REJECTS a malformed row (arm cell not the file's arm, non-contiguous box index, or a declared `n_boxes` the rows do not meet), closing the verifier's second new gap. **The report was recomputed from the seven committed CSVs after these changes and `g327a2_report.json` and `g327a2_table.md` came back BYTE-IDENTICAL: every verdict cell is unchanged** (`NOT PINNED`, `DETERMINISTIC 120/120`, `single_repeat` 120/120, `batch2` 46/120, `batch8` 48/120, `pad` 48/120 not applied, `fp32` 0/120, `nmsord` 48/120). These edits land AFTER the scored run, so the local `g327_arms.py` no longer matches the pod route sha256 recorded above; that recorded hash stays as the run-time record and the byte-identical recompute is the evidence that the change moved nothing.
Vocabulary follows contract Q6; automated scan required.
