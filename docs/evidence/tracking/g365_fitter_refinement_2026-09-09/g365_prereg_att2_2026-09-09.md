# G365 AMENDMENT preregistration -- attempt 2, candidate B
Date 2026-09-09. Builder claude-opus on the pod, worktree /workspace/wt/a15, branch track-a15, tip eb9c94b7.
Spec docs/evidence/tracking/specs/G365_spec.md (VERSION 2026-09-09); contract VERIFIER_CONTRACT.md (A/B; Q1,
Q3, Q6, Q7, Q8; A9, A11, B3, B8, B10, B11). Amends `g365_prereg_2026-09-09.md`, SEAL sha256
e25f1770895d6426d43083b9196edb2018991ef5f8f926266328dadadde2d6c4 -- that file is NEVER edited by this row;
this amendment is ADDITIVE and every clause it does not restate is carried unchanged. Committed ALONE, before
any attempt-2 number exists: every number below was computed by a READ-ONLY diagnostic over attempt 1's
COMMITTED artifacts or is quoted verbatim from them, and no candidate-B draw has been scored.
Attempt 1's candidate A (scipy trf, Huber f_scale 2.0 px, correspondence fixed once from the 4-corner matrix)
was REJECTED by the sealed ladder: G1_SYNTH_QUAD 1.6516 -> 1.1000 px (bar 1 missed), G4_OFF_AXIS 1.8003 ->
0.5992 px (inside the bar), G3_TIGHT 2.0160 -> 2.1991 px (worsens), G2_WIDE 469.2083 -> 469.3300 px (worsens);
sweep medians improved at every sigma; the exact control held at 0.000 px. A is NOT withdrawn and is
re-reported beside B.

## 1. DIAGNOSTICS (read-only over attempt 1's committed artifacts; NOT bar results)
G2_WIDE is a SYMMETRY-BASIN INITIALISATION -- not a degenerate quad and not an ill-conditioned solve. The nine
straight markings of `g334_court_template.template_polylines` are EXACTLY invariant, AS A SET, under the court
mirror x -> 50 - x (measured: the mirrored set is identical; the whole 398-point template is mirror-invariant
to 0.509294 ft, half its own 1 ft sampling step). The initialisation recovers the court UNDER that mirror: gap
469.2083 px against the truth, 1.9159 px against the truth composed with the mirror -- the fit quality the
other three geometries reach. Ill-conditioning is REFUTED: the two W-family lines are parallel in image space
to 0.0000 deg on G1, G2 and G3 alike (0.1942 deg on G4); G2's two L-family lines meet at 38.3580 deg, the
WIDEST of the four; the condition number of the 4-point system on G2's own initialisation quad is 52196.7401,
the LOWEST of the four (G1 87238.3468, G3 82554.6344, G4 86529.4580); 398 of 398 template points land in frame
under both truth and initialisation on all four. Corroboration: 44.45 pct of G2's assigned supports go to a
marking other than their true nearest one -- the mirror partner, whose image coincides. An objective invariant
under that mirror cannot see the difference, so no local refinement can cross it.
G3_TIGHT is dragged by FOREIGN supports. 56 of its 678 assigned supports (8.26 pct, the highest of the four)
lie more than 3 px from EVERY true straight marking -- arc and off-marking content the fixed correspondence
admitted under ASSIGN_MAX_PX = 12.0. Across candidate A the median forward residual ON THOSE supports FELL
8.4502 -> 6.0527 px while the median on the clean supports ROSE 0.0000 -> 1.1466 px, and p90 rose 2.7718 ->
3.1237 px, the only geometry whose p90 worsens (foreign medians elsewhere unmoved: G1 7.4195 -> 7.5156, G2
7.8076 -> 7.8895, G4 7.2933 -> 7.6252 px). 44.40 pct of G3's assigned residuals exceeded the sealed 2.0 px
f_scale before the fit, so the clean data sat deep in the quadratic region while the foreign block, about four
f_scales out, kept a linear pull a correspondence computed ONCE from the initialisation can never expel.

## 2. Candidate B (sealed; additive, candidate A untouched)
Start: the SAME 4-corner matrix H0, normalised by H0[2, 2]. Same eight parameters, same symmetric
forward-plus-inverse point-to-line residual of base prereg section 3, same `px_per_ft` weight taken ONCE from
H0 and held fixed, same BIG_PX = 1.0e4 charge, same `scipy.optimize.least_squares` with method `trf` and loss
`huber`, same TOL = 1e-10 and MAX_ITER = 200 per round. Three changes:
(a) TEMPLATE-GUIDED RE-ASSOCIATION, ROUNDS_B = 3. Round r starts from H(r-1), H(0) = H0. Its supports are
    RE-GATHERED: every FIT support whose perpendicular distance to the nearest of the nine markings' images
    UNDER H(r-1) is <= BAND_B_PX = 6.0 is kept and assigned to that marking; every other support is dropped
    FOR THAT ROUND and may return in a later one. Nothing is added -- the FIT support array is the one
    candidate A received, and the VALIDATION strokes are never read (contract B8). BAND_B_PX comes from the
    measured bimodality of section 1, not per-geometry tuning: clean supports sit at or below 2.9 px (p90 over
    all four geometries) and the foreign block at 7.3-8.5 px (median per geometry), so 6.0 px at 720p
    separates them with margin.
(b) LENGTH WEIGHTING. Each kept support carries w = L / median(L) over that round's kept set, L being the
    pixel length of the parent stroke on the GEOMETRY path and of the parent noisy line on the NOISE path,
    CLIPPED to [0.5, 2.0]; both of its residuals are multiplied by sqrt(w). The clip is part of the seal: it
    holds the effective robust scale within a factor of 1.42 of the sealed f_scale, so the loss does not
    quietly become a different loss on the longest markings.
(c) HUBER_DELTA_B_PX = 1.0 (scipy `f_scale`), half of candidate A's.
(d) CONVERGENCE: the rounds stop early when the largest displacement of the four HALF_COURT_QUAD image corners
    between successive rounds is <= CONV_B_PX = 0.01 px. Rounds run are recorded.
(e) The refusals of base prereg section 3 are unchanged: fewer than MIN_ASSIGNED = 12 kept supports in a round
    leaves that round's matrix unchanged with status `too_few_assigned` (missing support is not bad support,
    contract B3); a non-finite or singular result is REFUSED with status `degenerate` and the previous round's
    matrix returned.

## 3. The INIT_FAIL carve-out (new, sealed, deliberately narrow)
A geometry is INIT_FAIL when BOTH hold AT INITIALISATION: (i) the G362 validator's state on the 4-corner
winner is REFUSED, and (ii) that winner's own recovery gap exceeds INIT_FAIL_PX = 50.0 px. It is EXCLUDED from
bar 1 and bar 4 of this row and from NO other bar; it KEEPS its row in `recovery_b.csv` with state INIT_FAIL
and both gaps, gets its own memo line, and the memo carries a SUCCESSOR note: breaking the template's mirror
symmetry at initialisation, or a whole-template search independent of corner labelling, is a DIFFERENT row.
An explicit carve-out, never a silent drop, never a lowered bar -- the bar stays 1.0 px. Clause (ii) is
REQUIRED and keeps it narrow: clause (i) ALONE would swallow three of four geometries, since under attempt 1's
committed `premise.json` G3_TIGHT and G4_OFF_AXIS are ALSO REFUSED, at 2.0160 px and 1.8003 px, and both stay
fully inside bars 1 and 4 here. Under those same committed numbers exactly ONE geometry meets both clauses --
G2_WIDE at 469.2083 px, mechanism named in section 1. 50.0 px is two orders of magnitude above the bar and
above the largest movement any refinement produced in attempt 1 (1.2011 px).

## 4. Fixtures, draws, seeds, and the bars (bars byte-identical to the base prereg, never moved: Q3, B10)
The four sealed geometries of base prereg section 6, byte-identical. The same sigma set {0.00, 0.25, 0.50,
1.00} px and DRAWS_B = 40 per (geometry, sigma), above the n >= 30 rail. NEW seeds: base prereg section 12's
formula with the prefix `G365B`, so no attempt-2 draw can be an attempt-1 draw. On each new draw the archived
corner solve, candidate A and candidate B all run on the SAME noisy lines, so the A-vs-B sweep comparison is
PAIRED; attempt 1's committed sweep medians are re-reported beside them, unchanged and not re-run. FIT /
VALIDATION separation is base prereg section 4, unchanged, on both paths.
    bar 1: refined gap <= 1.0 px on every NON-INIT_FAIL geometry (`recovery_b.csv`)
    bar 2: refined median gap <= 1.0 px at sigma = 0.50 px over 40 draws, EVERY geometry -- the carve-out
           does NOT apply to this bar (`sweep_b.csv`)
    bar 3: the exact control stays < 1e-6 px before AND after candidate B
    bar 4: no NON-INIT_FAIL geometry worsens (improvement >= 0)
    also:  0 bars moved, 0 src edits, nothing written under `data/`
    DONE all four bars hold | PARTIAL any bar unmet, naming the geometry, sigma or clause | REJECT B worsens a
    non-INIT_FAIL geometry (bar 4) -- a REJECT of candidate B, not of the row
ROW LEVEL: attempt 2 is the last attempt; all four bars hold -> DONE, otherwise CLOSED AT LIMIT naming the
failing clause. An honest PARTIAL, REJECT or CLOSED AT LIMIT is a success; calibration language only (Q6).

## 5. Outputs and finisher commands (`python3 -m` from /workspace/wt/a15, CPU only, in this order)
All under docs/evidence/tracking/g365_fitter_refinement_2026-09-09/: `premise_b.json` (premise re-run, compared
cell by cell with attempt 1's committed `premise.json`), `recovery_b.csv` (per geometry: state, INIT_FAIL flag,
baseline / A / B gaps, improvements, rounds, kept counts, status, G362 held-out medians), `sweep_b.csv` (per
geometry, sigma, draw: seed and all three gaps), `refined_h_b.json`, `summary_b.json` (sealed constants, both
tables, each bar clause separately), `renders_b/` (baseline and B per geometry, <= 200 KB each).
    python3 -m pytest tests/platformkit/test_g365_candidate_b.py -q -p no:cacheprovider
    python3 -m pytest tests/platformkit/test_g365_fitter_refinement.py -q -p no:cacheprovider
    python3 -m pytest tests/platformkit/test_loc_rail_scope.py -q -p no:cacheprovider
    python3 -m scripts.platformkit.tracking.g365_sweep_b premise --out <EVID>
    python3 -m scripts.platformkit.tracking.g365_sweep_b sweep   --out <EVID>
    python3 -m scripts.platformkit.tracking.g365_sweep_b render  --out <EVID>
    python3 -m scripts.platformkit.tracking.g334_seal verify <EVID>/g365_prereg_att2_2026-09-09.md
    python3 -m scripts.platformkit.tracking.g334_seal scan <EVID>/... (the attempt-2 artifact list)
`<EVID>` = `docs/evidence/tracking/g365_fitter_refinement_2026-09-09`; `premise` runs FIRST. Every run is
CPU-only in this worktree, `nice -n 19`, `OMP_NUM_THREADS=2`, `CUDA_VISIBLE_DEVICES=-1`; the deployed tree
/workspace/nba-ai-system stays read-only, no pod process is signalled, and these commands write nothing under
`data/`, `data/registry/`, the tracking register or RESULTS_LEDGER.md.

## 6. What may not move
The base preregistration and its seal; G362's bars and validator; `g362_synth.py`, `g362_strokes.py`,
`g362_fit_validate.py`; `g365_refine.py`, `g365_sweep.py`, `test_g365_fitter_refinement.py` (imported, never
edited); `domains/basketball/tracking/line_calibration.py`; `src/`, `kernel/`, `api/`, `intel/`; every feature
flag; `data/`, `data/registry/`, the register, and RESULTS_LEDGER.md apart from the single row the finisher
appends. The four quads, section 2's constants, section 3's carve-out, the sigmas, the draw count, the seed
prefix and section 4's bars are fixed by the seal below; any change voids this amendment, not amends it.
SEAL sha256 cc6e3d30e813600377797807f66d8b02255ab5d1bbeba7e9573e14d1e5af4f5b
