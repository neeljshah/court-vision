# G399 preregistration: qualified paint larger sample

## Scope

This prepare-only protocol cites `docs/evidence/tracking/specs/G399_spec.md`
and `docs/evidence/tracking/VERIFIER_CONTRACT.md`, sections A, B, Q, A9,
A11, B11, H1, and S1-S5. It creates no rating, inference, scoring, pod job,
deployment, daemon action, register write, ledger write, or feature-flag
change. The Claude finisher alone may decode, open native tiles, dispatch the
two raters, collect answers, pair fragments, perform pixel audits, render, or
compute the final descriptive accounting.

## Binding before-condition

Before any decode or dispatch, rerun the G396 qualification from its raw
qualification score table: astra 30/30, sol 27/30, and same-control joint
27/30. Reproduce the three G396 real audit passes from the immutable raw
accounting. Verify that both frozen instructions and the recorded high runtime
configuration hashes are present and that native images can be opened. Then
rehash every one of the 30 G386 retained sources one at a time and verify its
1920x1080 stream identity. A missing, changed, unreadable, duplicated, or
wrong-resolution source is retained as a named planned failure. It is never
replaced. If this before-condition fails, write its exact output, declare the
premise FALSIFIED, and do not dispatch.

## Sealed method

Use the 30 G386 retained sources in their recorded competition/video/section/
source-digest order. A decoded PTS census, not a header or one frame, fixes
each usable span. Plan two targets at one-third and two-thirds of that span;
choose the nearest decoded PTS, breaking ties earlier. Archive all 60 planned
states before labels. Preserve the original 60/49/11 parent accounting
separately from this new 60-state census.

Export unchanged 640x540 native tiles with integer offsets and context views.
Only astra and sol at their G396 high settings use the frozen instructions.
They independently return VISIBLE, ABSENT, or UNKNOWN and no more than two
three-point fragments. Pair only same-family fragments by stable smallest
symmetric line distance. Every point must satisfy the frozen 3 px, 6 px,
60 px, and strict-interior constraints.

Before seeing traces, Claude records native visibility for every planned state.
Claude audits every candidate pair independently: both fragments must be on the
same physical paint band and at least eight of nine evenly spaced native points
per fragment must be within 3 px of visible paint. Each pair has one unique
audit token. Recovery counts a state once when at least one pair passes both
fragment audits. Arithmetic repeats twice from immutable answers. No fit,
orientation inference, registration acceptance, correction tournament, or
replacement occurs here.

## Accounting and handoff

The finisher preserves every planned state, including decode failures and
ABSENT/UNKNOWN answers. It names planned, decoded, and visible denominators.
It reports recovery over 60, recovery over decoded states, and visible-
conditioned recovery without treating a missing denominator as zero. At least
30 decoded unique contexts are required for a scored statement. Fewer than 30
recovered states is CLOSED AT LIMIT; at least 30 establishes necessary supply
only, not the G383 FIT and VALIDATION prerequisites.

All final inputs, code identities, source paths, byte sizes, resolutions, PTS,
raw judgments, audit points, repeat receipts, render indices, and exclusions
belong under this evidence directory. The final memo must state that
improvement is baseline loss minus candidate loss and positive means candidate
better, even though this row has no loss comparison. It must end with NOT
VERIFIED. Text artifacts receive the G399 character-code vocabulary scan.

PREPARE-ONLY VERDICT: UNMEASURED. No final result is claimed by this file.
SEAL sha256 1ce0d72bb49ee61b95bbb92e5742e804d24af576a9c11adbdb65b49aa347f9df
