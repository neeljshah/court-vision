# G396 preregistration: third-rater paint qualification

## Scope and authority

This prepare-only preregistration cites
`docs/evidence/tracking/specs/G396_spec.md` and
`docs/evidence/tracking/VERIFIER_CONTRACT.md`, sections A, B, and Q. The
Claude finisher alone may create controls, open rater packets, collect
responses, score qualification, rate real contexts, audit paint, or render
measurement output. This preparation creates neither a rating nor a metric.

All paths are repository-relative and resolve in this worktree. If a named
input is unavailable, the finisher prints `ABSENT-IN-WORKTREE <path>` and
stops. Each opened input is recorded one at a time with its path, byte size,
and image resolution where applicable.

## Binding premise before dispatch

Before any packet dispatch, recompute all 60 rows in
`docs/evidence/tracking/g392_per_rater_paint_qualification_2026-09-11/qualification/scores.csv`:
sol must be 30/30, terra must be 20/30, joint must be 20/30, and
`real_scored` must be false. Rehash the 49 retained native images, 30 context
identities, and 180 tile identities, preserving the 11 parent decode failures.
Write the complete result to `input/premise_receipt.json`. If it does not
hold, report the complete output as FALSIFIED and make no packet.

## Frozen protocol

The raters are exactly sol and ASTRA. ASTRA is a fresh isolated process using
the pre-existing ASTRA home; its model, runtime, and configuration digests are
recorded without modifying that home. It receives neither truth, prior
answers, nor review history. An unscored image-open or output fixture failure
ends preparation as PARTIAL.

Use seed 396 to create 30 practice and 30 qualification controls over the
same retained contexts, six tile positions, and the 0/30/60 degree cycle.
Archive all controls before dispatch. Pixel hashes and truth tuples must be
distinct within, between, and from G392. One documented ASTRA
instruction/display correction is permitted after practice; freeze it before
qualification. Sol uses its frozen G392 instruction.

Both raters independently rate the same 30 fresh qualification controls once,
blind and without feedback. Every three-point answer requires at most 3 native
pixels perpendicular error, finite-band projection, first/second span of at
least 60 native pixels, and a strictly interior third point. Missing output is
a failure. Each rater and their same-control joint total must be at least
27/30. A failure blocks real-paint dispatch without replacement, retry, fitted
offset, or blanket displacement.

Only a passed gate permits the original even 30 retained G392 contexts. Real
fragments retain ABSENT, UNKNOWN, occlusions, and unmatched observations. Pair
only by tile/family, stable smallest symmetric line distance, 6-pixel pairing,
and the independent 3-pixel third-point rule. The finisher independently
audits every candidate pair against visible paint using 9 evenly spaced points
per fragment, with at least 8 within 3 pixels. Orientation remains UNKNOWN.
Two fresh processes repeat scoring and rendering from raw responses.

## Limits and reporting

No source under `src/`, `kernel/`, `api/`, `intel/`, `scripts/run_clip.py`, or
`scripts/platformkit/track_daemon.py` is edited. No evaluator, pod, register,
feature flag, or path under `data/` is used. Text artifacts, including rater
logs, receive the character-code vocabulary scan before reporting. The memo
states that improvement means baseline loss minus candidate loss; positive
means candidate better. This file uses LF line endings.
SEAL sha256 18124321ca2b0daf315283e16ec79f06c5498cd2bb6948bd65f2893c2976dd12
