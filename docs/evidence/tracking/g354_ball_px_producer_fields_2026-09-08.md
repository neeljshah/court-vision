VERDICT: PARTIAL -- fields deployed and verified on 28 fresh readable post-deploy clips (of 30 fresh; 2 excluded for an absent store, not a failure; all 28 pass ratio <= 1.5 both axes); sealed 130 s smoke completed on pod CPU; deploy preceded verification (B5 deviation, user-authorized)

# G354 Ball Pixel Producer Fields
Prereg `g354_prereg_2026-09-08.md`, seal RECOMPUTED and MATCHING `b0595a9cb4d5cbc61be8ef4790fa5dc8b20a79cc1e63c41b2612d3091379d382`; sealed and committed alone at 5f8127809 before a byte under `src/` moved. EYE CHECK: NONE -- no labels here, only digests, parses and row counts.

## Premise (spec step 0) -- HOLDS
`git apply --check ball_px.patch` -> PASS. BOTH writers covered: the dedicated route sets `self.last_2d_pos_px = (cx, cy)` beside the existing `ball_center` at `src/tracking/ball_detect_track.py:869` and clears it per frame at :589; the second writer -- the alternate YOLO ball bbox projected through `M1 @ (M @ ...)` -- sets `self._last_ball_px = (cx, cy)` at `src/pipeline/unified_pipeline.py:3101`. Header before the patch, `data/tracking/0022400625/ball_tracking.csv`: `frame,timestamp,ball_x2d,ball_y2d,detected,live,ball_inferred` -- no pixel ball field existed.

## The diff -- 15 changed src lines, additive
`git diff --numstat -- src/` = 11 added / 1 deleted in `unified_pipeline.py`, 3 added / 0 in `ball_detect_track.py`. The one deletion is the `fields = [...]` line at `unified_pipeline.py:4051`, re-emitted over two lines with the original SEVEN columns in their original order and the two new names APPENDED -- no existing column renamed, removed or reordered. Empty-value rule: the suspended-frame row (:1843-1844) and the live row (:1996-1997) both write the empty string when there is no current bbox, and `_last_ball_px` is reset to None at the top of every frame (:1900) and inside `ball_tracker` (:589), so a value can never carry over from the previous frame. Explicit-zero rule: the live row tests `ball_px_pos is not None`, so a component of 0 is written as `0`, never blank.

## Tests -- each file alone; the full suite was never invoked
Interpreter `C:/Users/neelj/anaconda3/envs/basketball_ai/python.exe`. `test_g354_ball_px_fields.py` -> `4 passed`, run against the APPLIED `src/`. `test_g354_postdeploy_freshness.py` (NEW, fix 1b) -> `3 passed`. `test_g333_producer_diffs.py` -> `19 passed`. `test_g351_ball_scale_census.py` -> `2 passed`.

## Smoke -- the SEALED 130 s section, COMPLETED (pod CPU); the capped arm kept for comparison
Source, named and extant: `/workspace/nba-ai-system/data/footage_corpus/nba__Hm7V4jOxlUI_s3633.mp4`, 41776987 bytes, sha256 `659f331e758237d17c93a2ad9e40198529e34c94ccdb209f6d1fd28936c96094`, 1920x1080, 60000/1001 fps, 8006 container frames, 130.043 s. The pod's two deployed producer files were sha256-confirmed EQUAL to `deploy.csv`'s after-digests before the run, so this smoked the DEPLOYED code, not a copy. Headless (`--no-show --skip-features`), no frame cap (`--start-frame 0`, `--frames` unset; the route's own sidecar records `max_frames: null`), `nice -n 19`, `OMP_NUM_THREADS=2`, `CUDA_VISIBLE_DEVICES=` (CPU), output to `/workspace/wt/a15/smoke/out`. `COURTV_NO_OCR=1` as in the capped arm: it skips jersey OCR and the preflight, and touches no ball or bbox writer.

    scope               frames  ball_rows  n_px    ratio_x   ratio_y   px_within_60   old_within_60  wall_s
    full_section_130s   007788  000060     000045  1.132348  0.777702  000025 (556pm) 000000 (000pm) 000389
    capped_1500_frames  001500  000015     000015  0.005420  0.002977  000014 (933pm) 000000 (000pm) 000072

Both scopes pass the 1.5 bar on both axes, but only the full section carries power: its ball px span is 1458 by 783 px against a player range of 1288 by 1006, where the capped arm's span was about 7 by 3 px and so passed trivially. The route decoded all 7788 frames and emitted 60 ball rows, all `live=1`: it writes rows only for live-play frames, and this section is largely non-gameplay (its preflight median person count was 2, under the threshold of 3). 15 of those 60 rows carry the empty px value and 45 carry a detection, so the never-stale empty rule is exercised on real footage.

## Deploy -- G333 recipe, epoch 1788904286 = 2026-09-08T21:51:26Z (UNCHANGED)
Pre-image check FIRST: each file's LF digest at HEAD equalled the pod's live file exactly (`unified_pipeline.py` `627961ab...`, the G333 after-digest; `ball_detect_track.py` `873cdead...`). LF-normalised, `scp` to `/workspace/wt/a15/deploy/<name>.new`, digest re-checked after transfer, then `mv -f` into `/workspace/nba-ai-system/<path>`; same mount, so the mv is atomic. sha256-after equals the local patched LF digest on both files, and still did at this memo's close. `git -C /workspace/nba-ai-system status --short` is NOT AVAILABLE -- that tree is not a git repository on the pod (git-archive deploy) -- and is recorded as such in `deploy.csv` rather than faked. Daemon pid 1596016 and guard pid 1519254 read 08:18:53 / 09:18:56 elapsed at close: continuous, never signalled.

## Post-deploy -- freshness now judged by START time
The ledger has no start field. `scripts/platformkit/track_daemon.py:384` stamps `job["started"] = time.time()` when a clip is claimed; :302-303 write `"seconds": int(finished - job["started"])` and `"finished_at": int(finished)`. START is therefore `finished_at - seconds`, within 1 s of the true claim time (both values truncated). A clip that STARTED before the deploy had already imported the old module even if it FINISHED after it -- the finish-time rule swept those in, which is why the sealed checker read 0. `postdeploy.csv` now carries `started_at_utc`, `finished_at_utc`, `duration_s`, `start_epoch` on every row.

    38 ledger entries finished after the deploy epoch; 30 STARTED after it (fresh), 8 started before it (excluded)
    the 8 excluded started 21:37:02Z to 21:49:34Z and ALL 8 are MISSING_PX_FIELDS
    the 30 fresh started 21:55:07Z to 23:30:24Z; 28 are OK and ALL 28 pass, ratio_x 0.005376-1.223706, ratio_y 0.019753-1.389447
    the 2 remaining fresh are MISSING_TABLE: acb-8Oh7-GrvKvE_s90 and fiba-UG_eQNRDHiI_s6565, both ledger status "thin" with rows 0, no store on disk to read
    checker output (fix 1c): FRESH_ENTRIES 000030 | EXCLUDED_STARTED_BEFORE_EPOCH 000008 | EXCLUDED_MISSING_STORE 000002 | POSTDEPLOY_PASS 1

The split is exact and is the natural experiment: every clip that started after the deploy and produced a store carries both fields; every clip that started before it carries neither. Fix 1c judges only the 28 fresh clips that produced a store, excluding the 2 absent-store clips from judgment instead of failing them; all 28 pass ratio<=1.5 both axes, so POSTDEPLOY_PASS is now 1 (was 0 under fix 1b's stricter all-fresh-must-be-OK rule).

## Re-join, 60 px radius, before against after (per mille of that arm's own denominator) -- the four original clips UNCHANGED
    fiba-jFUMvm_epUE_s5410     old 000211 of 000764 (276pm) -> px 000345 of 000975 (354pm)   started 21:55:07Z
    eurocup-Y6POC6V1Y5E_s1221  old 000002 of 000483 (004pm) -> px 000153 of 000796 (192pm)   started 22:00:29Z
    fiba-jFUMvm_epUE_s6740     old 000178 of 000306 (582pm) -> px 000221 of 000373 (592pm)   started 21:56:17Z
    eurocup-Y6POC6V1Y5E_s4614  old 000000 of 000631 (000pm) -> px 000008 of 000937 (009pm)   started 22:27:11Z
Across all 28 fresh OK clips the px arm joins higher on 22, lower on 5, and one clip has no old denominator; pooled 6231 of 22545 px frames against 3950 of 18209 old frames. Diagnostic only -- not a calibration or accuracy claim.

## 2026-09-08 fix 1b: what the REJECT asked for and what changed
The 2026-09-08 18:39 verify memo returned REJECT on three findings. (1) SMOKE / Q3: memo line 1 claimed every acceptance clause held while the sealed 130 s scope had been replaced by a 1500-frame cap. FIXED: line 1 no longer claims all clauses hold, and the sealed section ran to completion on pod CPU (7788 frames, no cap); the capped arm is kept and relabelled `capped_1500_frames`. (2) POSTDEPLOY: the four clips' post-deploy START times were not reproducible from the CSV and the sealed checker filtered on `finished_at` only. FIXED: `g354_postdeploy_check.py` gains `--freshness-key start|finish` defaulting to START, computes START from the cited ledger fields, writes the four timing columns on every row, and prints the fresh and excluded counts; a new test pins the two-clip construct (one started before the epoch and finished after -> not fresh; one started after -> fresh). (3) B5: the producer was deployed after an incomplete smoke and before ACCEPT. RECORDED AS A DEVIATION, NOT UNDONE: the deploy stands as deployed under the user's 2026-09-08 authorization for producer deploys; nothing was redeployed in this row and nothing was rolled back. The verifier's smoke-source NEW GAP is also closed: the source is now recorded as a named extant path with byte size and sha256, not a basename and scratch copy.

## HONEST LIMITATIONS
The two touched producer files exceed the 300-line guidance -- `src/pipeline/unified_pipeline.py` is 4801 lines and `src/tracking/ball_detect_track.py` is 1053 -- and are EXEMPT as pre-existing landed code: this row appended 15 lines to them and created no new oversized file (the checker is 171 lines, the new test 82). The sealed 130 s smoke ran on POD CPU, not locally; the local box could not hold the full section, and a pod CPU run is a different machine, interpreter (Python 3.12.3) and device from the local arm. A pixel field fixes the UNIT, not ball detection coverage (G335) nor ownership (G344). The ledger timestamp remains the ONE epoch marker separating old from new semantics; every clip tracked before it keeps the old semantics. No NBA game id is among the fresh clips -- all 28 are non-NBA basketball -- so nothing here speaks to the 214-of-356 NBA census G351 measured. No consumer was rewired; G344 and G349 re-runs are their own rows.

## NOT VERIFIED
- B5 DEVIATION (unchanged from fix 1b): the producer was deployed 2026-09-08T21:51:26Z, before this row's post-deploy verification ran; user-authorized 2026-09-08, not undone or redeployed here.
- The sealed 130 s smoke ran on POD CPU, not the local box (RAM-bound locally); it rests on 45 px-detection frames from one largely non-gameplay section, so the 556-per-mille re-join is a screening, not a census.
- `peak_rss_mib` 001627 for the pod run is the maximum of four `ps` samples, not a true peak; the route reported no stride for this invocation and that cell is left empty rather than assumed.
- The pod corpus is a live rotating queue, so the recorded source path may not persist; its sha256 and byte size are recorded so the bytes can be re-identified.
- DEVIATION, pod repo: the smoke's rectification step rewrote `/workspace/nba-ai-system/resources/Rectify1.npy` via `src/tracking/rectify_court.py:197`, which saves to a module-relative `resources/` directory that `--data-dir` does not redirect. Its mtime moved; its BYTES did not -- sha256 `19caf48dcfbb2e2818e056e8a2b2f18484d7b7ee5819b76418437d52669192ad`, 200 bytes, identical to the main repo's canonical copy. Nothing else under `/workspace/nba-ai-system` was written and nothing else on the pod was measured other than read-only ledger and clip-store reads; no footage was deleted, no flag flipped, no registry write, no daemon or guard restart.
WALL TIME about 35 min local for this fix-1b pass; the pod CPU smoke is 389 s measured by the route (2026-09-08T23:47:39Z start).
SHA-256, LF-normalised bytes: `ball_px.patch` `817e0abbfa1122d0cd7596b3dff12cc7fe7dc792088f985dc261bcdd222a0ec0` | `unified_pipeline.py` `c4eae385bfeebd4e44c8161c791ef7ed4589697020f424d3dde7aec6816d9226` | `ball_detect_track.py` `cbc7cd9dfb7e18691f310b7594be046ac0d3184a279c43d8eb427210355bc9ea` | `g354_postdeploy_check.py` `545b6ce868a624e19faabf69f8b67bff55d178576f07acc04e8abf5f9afa808e` | `test_g354_ball_px_fields.py` `957c60e7d625987952c98d6b266264355bfb3f80c44955d166c25f02465991af` | `test_g354_postdeploy_freshness.py` `3709120eb5f434a8c0b28a02353329cf3e5e406b1398371399ac9259ee880faa` | `smoke.csv` `1176bf08f4154bed9bdf8fda7fbbd478454244a40a1b178c5e55dfd4f0ad5037` | `deploy.csv` `0a077e7f554c2fca89a6b1383afbdefde98b4307050317ad2a8de463f42b4543` | `postdeploy.csv` and `rejoin.csv` both `5605c6cc29e6df0bad249d6889d7a77bdec741325eb709e9dd03a2ba406c4cf2` (the checker writes the same rows to both outputs). Hexadecimal digests are single tokens; a digit run inside one is not a standalone figure.

2026-09-08 fix 1c: corrected `g354_postdeploy_check.py` to exclude MISSING_TABLE rows from the judged set (the >=3 fresh-clip and ratio<=1.5 gate) instead of failing the row on them, per the REJECT's B3 finding; two new tests in `test_g354_postdeploy_freshness.py` pin a 3-pass/2-missing PASS construct and a readable-clip-over-ratio FAIL construct, both green. Re-ran the corrected checker READ-ONLY against a frozen copy of the SAME ledger window fix 1b used (finished_at <= 1788911113, the original 38-row cutoff; none of the live daemon's later-appended rows were included, per this row's scope) -- identical 30 fresh / 8 pre-epoch / 2 absent-store split reproduced row-for-row, only POSTDEPLOY_PASS changed, 0 to 1. This pass's own rerun of `test_g333_producer_diffs.py` (plain pytest, no ACL wrapper) shows 18 passed, 1 skipped (no local panorama cache directory in this worktree) rather than the memo's earlier 19-passed count from a different harness -- recorded, not reconciled. NOT CORRECTED (deviations, unchanged from fix 1b): the sealed 130 s smoke ran on pod CPU, not locally; the producer deploy preceded verification (B5, user-authorized). Vocabulary follows contract Q6; automated scan required. Scan output: EMPTY (0 hits) -- pattern set assembled from character codes at run time, never spelled in this file, the CSVs or the test.
