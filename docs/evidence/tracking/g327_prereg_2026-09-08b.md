# G327 PREREG, ATTEMPT 2 -- sealed BEFORE any decode, any detector call and any number

Row: GAP G327 | sport all | worktree a21 | log cx_g327_detector_batch_stability
Spec: `docs/evidence/tracking/specs/G327_spec.md` (master 708e2e14e)
Attempt 1 prereg: `docs/evidence/tracking/g327_prereg_2026-09-08.md`, seal
`3f19f817c4d836b5d120fc003cb3aafb3141cefb17100128ddb4a01601fce6dc`, commit `dfb190cc6`.
Contract: `docs/evidence/tracking/VERIFIER_CONTRACT.md`

This file is committed ALONE, before any decode, any detector call and any number.
**No metric of any kind has been computed at the time of this seal: attempt 1 aborted
inside `ffprobe`, before a single frame was decoded or a single detector call was made.**

## Why there is an attempt 2 -- ATTEMPT 1 IS CLOSED AT LIMIT ON SOURCE AVAILABILITY

Attempt 1 sealed the three ffprobe-confirmed native 1920x1080 games it inherited from
G324. **All three had been deleted from the pod corpus before this row launched.**
Measured, not inferred (`g327_artifact/g327_attempt1_source_absence.txt`, and the pod job
log `20260908004617_611055_7433`, which ends in `KeyError: 'streams'` from `ffprobe` on
the first sealed path):

- `wnba__wnba_01_1080p.mp4`, `ncaa_basketball__ncaa_basketball_IB-_u4gW3ds_1080p.mp4` and
  `wnba__wnba_06.mp4` are all **`No such file or directory`** under
  `/workspace/nba-ai-system/data/footage_corpus/`.
- A resolution census of the WHOLE corpus (119 files) is **56 at 1280x720 and 63 at
  640x360**. **There is no 1920x1080 source left in the corpus.**
- A `find` over `/workspace` to depth 5 finds no 1920x1080 game source outside the corpus
  either; only two unrelated `g302_*` clips and a mosaicing sample.

**Attempt 1's construct is therefore UNRUNNABLE, and its bar is not lowered: it is
reported CLOSED AT LIMIT ON SOURCE AVAILABILITY (contract Q3), which is a valid result.**
This is the D2 landmine again -- footage a row names was deleted before the row ran.

## What attempt 2 changes, and what it does NOT

**CHANGED: the three source clips, and only those.** Every arm, every kwarg, the
bit-identity rule, the IoU rule, the 110/120 and 120/120 bars, the 40-frames-per-game
count, the readable-anchor rule and the scope statement are carried from attempt 1
BYTE-IDENTICAL. **No bar and no threshold moves (contract Q3).**

**WHAT IS LOST BY THE SUBSTITUTION, stated here and not discovered later:**
- **Comparability with G324's inherited summary is GONE.** G324's 310.5 px / 861.0 px /
  changed-count figures were measured on three 1920x1080 clips that no longer exist.
  **Attempt 2 cannot reproduce those numbers and does not try to.** It answers the same
  MECHANISM question on the corpus that exists.
- **The array fed to the detector is a different shape.** With `TOPCUT = 60` a 1280x720
  source yields `[660, 1280, 3]` where the 1080p route yielded `[1020, 1920, 3]`. The
  letterboxed network tensor is **MEASURED and reported, never assumed**.
- These are broadcast NBA segment clips, not the WNBA/NCAA clips of the earlier rows, so
  scene content differs as well as resolution.

## PREMISE, re-measured before this seal (contract Q8/S2) -- PARTLY FALSIFIED

1. **G324's evidence is NOT ON MASTER.** Candidate `6dc55b929` was REJECTED
   (`docs/evidence/tracking/G324_VERIFY_2026-09-08.md`, commit `76772791f`, line 1
   `VERDICT: REJECT`). `docs/evidence/tracking/g324_artifact/` does not exist on master;
   it exists only on branch `track-a2`. **This row lands no G324 file.**
2. **The committed G324 box CSVs carry the SINGLE-IMAGE pass ONLY** (`g324_arms.py:measure`
   writes `run_single`'s rows; the batch rows were compared and DISCARDED), so **the
   per-frame count deltas and the single-vs-batch IoU-matched agreement CANNOT be
   recomputed from any committed artifact.** The premise is CARRIED AS AN INHERITED
   SUMMARY, never recomputed, read from
   `git show 6dc55b929:docs/evidence/tracking/g324_artifact/g324_summary.json`:

   | game | arm | identical_box_counts | max_abs_coord_delta_px |
   |---|---|---|---|
   | wnba__wnba_01_1080p | Y (production yolov8n) | true | 310.5 |
   | ncaa_basketball__ncaa_basketball_IB-_u4gW3ds_1080p | Y | true | 861.0 |
   | wnba__wnba_06 | Y | false | null |
   | all three | R (RTMDet) | true | 0.0 |

   That comparison is PER-INDEX and UNMATCHED, so a pure reorder and a genuinely different
   detection are indistinguishable in it -- which is what this row replaces.
3. **RTMDet is NOT re-run.** Its stack is not installed in this worktree; its role in the
   premise is only to show the effect is not universal.
4. **The production call path is INHERITED from G303** (landed `b27d89028`,
   `g303_artifact/g303_report.json` key `step0`): `classes=[0]`, `verbose=False`,
   `imgsz=640`, `half=True`, `device=0`, `TOPCUT = 60`, route
   `_fill_conf_threshold = 0.22` against a registered 0.3 (`conf_conflict = true`), a
   `[1, 3, 352, 640]` `torch.float16` network tensor on a 1080p source, and
   `arm_P_byte_identical_repeat = true`. **Attempt 1 CONFIRMED the kwarg half of this on
   the live pod before it aborted**, printing
   `ROUTE {"classes": "[0]", "conf": "0.22", "verbose": "False", "imgsz": "640",
   "half": "True", "device": "0"}`. The determinism claim is RE-MEASURED, never cited
   (contract B11).

**A falsified premise is a VALID result (Q8). This one is partly falsified and the row
continues, because the construct below generates the missing data itself.**

## The sealed construct -- THREE DISTINCT GAMES, ffprobe-confirmed 1280x720

Anchors are MEASURED by the readable-anchor rule (binary search on the real cv2 seek
path, never ffprobe's `nb_frames`) in a read-only pod probe run BEFORE this seal; on all
three the last index was directly decodable, so the search terminated at the first probe.
40 evenly spaced indices per clip spanning 0 .. anchor. `list sha256` is the SHA-256 of
the comma-joined decimal indices with no spaces.

| game | pod path | bytes | w x h | nb_frames | anchor | list sha256 |
|---|---|---|---|---|---|---|
| nba__0022400909_s1015 | /workspace/nba-ai-system/data/footage_corpus/nba__0022400909_s1015.mp4 | 35117690 | 1280x720 | 3995 | 3994 | 392e19c8e04410bb1426a82f39d1dc6088c548647edecf5cf5cd4fa92722791c |
| nba__0022401156_s2491 | /workspace/nba-ai-system/data/footage_corpus/nba__0022401156_s2491.mp4 | 33115938 | 1280x720 | 3931 | 3930 | 55cf3ffb2baa77346890f750e3ef91ec5fd415208a1971ae2daf98de0a244020 |
| nba__0022500081_s1248 | /workspace/nba-ai-system/data/footage_corpus/nba__0022500081_s1248.mp4 | 34569555 | 1280x720 | 3901 | 3900 | 73364b69a11bfaef6508c1c626ec44f823b6802e8bcab6aa96e1fe76fe56d300 |

Three DISTINCT game ids, one clip each. A run-time source whose path, byte size, width or
height differs from this table is FATAL, never adjusted. A failed frame read is FATAL,
never dropped. **One decode per game; every arm sees byte-identical decoded frames,
asserted by hashing the decoded stack once and reusing it.** The decode is never widened
beyond 3 x 40 (the pod CPU quota is 27 cores and the daemon saturates it).

## The sealed arms -- carried from attempt 1 unchanged

| arm | batch | what differs from the single-image reference |
|---|---|---|
| `single` | 1 | nothing -- this is the REFERENCE and the daemon's own route |
| `single_repeat` | 1 | nothing; a second pass in the same process (determinism) |
| `batch2` | 2 | batch size only |
| `batch8` | 8 | batch size only |
| `pad` | 8 | batch letterbox pinned to the single pass's network shape |
| `fp32` | 8 | `half=False` |
| `nmsord` | 8 | COMPARISON-SIDE ONLY: both sides sorted before comparing |

`nmsord` changes NO model setting and runs NO extra detector pass -- it is computed from
the `batch8` boxes. **If `nmsord` alone restores identity, the cause is ORDERING and the
boxes were never different.** So 6 GPU arms x 120 frames = 720 detector passes, and
7 arms x 120 = 840 arm-frame comparisons.

## The sealed rules -- carried from attempt 1 unchanged

- **Confidence: the route's OWN `_fill_conf_threshold`, 0.22**, because the question is
  what the daemon route does. The conflict with the registered 0.3 is stated in the
  verdict line. `imgsz` and `half` are the route's own.
- **BIT-IDENTITY** between two per-frame box sets means: equal box count AND, after the
  arm's own ordering rule, every `x1,y1,x2,y2,score` and class equal as float64 with NO
  tolerance. **Two zero-box frames ARE bit-identical.** Committed coordinates are written
  in shortest round-tripping form, so the CSV and the run agree exactly.
- **AGREEMENT**: IoU >= 0.5, one-to-one, greedy by descending IoU, reported in BOTH
  directions with its denominator, SEPARATELY from bit-identity. The verdict turns on
  bit-identity, never on agreement.
- **Every decoded frame is in every denominator, zero-box frames included.**

## The sealed bars -- NEVER MOVED (contract Q3), byte-identical to attempt 1

- **CAUSE PINNED** iff EXACTLY ONE of `pad` / `fp32` / `nmsord` is bit-identical to
  `single` on **>= 110 of 120** frames while `batch8` is not, and that arm is NAMED.
  **Two arms clearing it is NOT PINNED (CONFOUNDED).**
- **NOT PINNED** otherwise, including when no arm clears the bar. **NOT PINNED is a VALID
  RESULT and a full success of this row.**
- **DETERMINISTIC** iff `single_repeat` is bit-identical to `single` on **120/120** frames.
  Otherwise NOT DETERMINISTIC, reported as `<k>/120`.
- `n = 120` frames (3 games x 40). There is NO pass bar on any agreement share or count
  delta.

## Scope and operational rails

**SCREENING ONLY. There is no ground truth here.** This row cannot say which pass is
CORRECT, only whether they AGREE. No recall, precision, registration, accuracy or pass
claim; no claim that either pass is better. Nothing is adopted, no production default
moves, no input size, confidence or batch size changes anywhere in `src/`.

`src/`, `domains/`, `api/`, `kernel/` and `intel/` are READ and IMPORT only;
`player_detection.py` and `advanced_tracker.py` are SHA-256-pinned before and after, as is
the pod `yolov8n.pt`. `data/registry/`, `data/tracking/`, the deployed
`/workspace/nba-ai-system` tree and the daemon's site-packages are not written; all compute
is under `/workspace/wt/a21`. GPU lease: proceed only when free VRAM is >= 8,192 MiB, read
and reported verbatim; this row stays under 4 GB; `track_daemon` is never stopped or
signalled. Wall budget 1,800 s per game for all arms together; a game that exceeds it is a
BUDGET LIMIT for that game and the row continues. `g324_arms.py` is NOT importable (not on
master); G303's `Capture` proxy IS imported from
`scripts/platformkit/tracking/g303_recall_vs_resolution.py`.

Vocabulary follows contract Q6; automated scan required.

--- SEAL 2026-09-08b ---
SHA-256 of the LF-normalized bytes above this seal line:
458026976ed93e5e8c295845c4d664d20dd58637a520ea1f551352cceef914fd
