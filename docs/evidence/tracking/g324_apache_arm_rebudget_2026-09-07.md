MEASURED -- the sealed 3,600 s install premise CLEARED at 267.8 s (7.4 pct of the limit) by reusing the SHA-256-pinned mmcv 2.1.0 wheel, and both arms ran on all three ffprobe-confirmed native 1920x1080 games. DENOMINATORS: 3 games x 40 decoded frames x 2 arms = 240 arm-frames; every decoded frame is in the denominator, zero-box frames included and counted. PER-PROXY SIGN RULE (sealed `g324_prereg_2026-09-07.md:116-121`, restated here as the spec requires): higher `boxes_per_frame` is CONSISTENT WITH more detection; higher agreement in EITHER direction is CONSISTENT WITH the two stacks seeing the same scene; longer `median_track_length` and FEWER `distinct_ids` at equal box count are CONSISTENT WITH steadier detections; LOWER single-image `ms_per_frame`, LOWER `ms_per_frame_batch8` and LOWER `peak_vram_mb` are CONSISTENT WITH a cheaper arm and say NOTHING about detection quality. CONSISTENCY IS NOT EVIDENCE.
**SCREENING ONLY: no labels, no renders, no eye check; no recall, precision, registration, accuracy or pass claim; NEITHER DETECTOR IS BETTER -- more boxes can mean more players found OR more false boxes and this row cannot tell them apart.** Low agreement says the two stacks disagree and says nothing about which is right; G303 and G296 are the rows that score recall against labelled frames. The arms differ in model AND input size, so this is a DETECTOR-STACK comparison, never a single-variable ablation. **This row ADOPTS NOTHING, moves no production default, proposes no cutover and changes no file under `src/`.**
**PREREG.** `g324_prereg_2026-09-07.md`, sealed `19154bcf48b4d291d87cc363e2b727f3671d284d1d00b7954aca7de481af27c7` and committed ALONE (`fac1ba8f0`) before any build, install, decode or number. It fixes the 3,600 s limit, the wheel hash, the three games with byte sizes, the readable-anchor rule, all six proxies, batch size 8 and the sign rule. **No bar was moved (contract Q3); 3,600 s is never lengthened again and there is no third attempt at this premise.**
**INSTALL PREMISE -- MET at 267.8 s of 3,600 s.** Wheel `/workspace/mmlab_wheels/mmcv-2.1.0-cp312-cp312-linux_x86_64.whl`, 35,202,838 bytes, SHA-256 `608637e5600ec15708acf451aa2e067d3e7b1e8c6dc40856e6f42d2e8ffaa9a1` -- **re-hashed on the pod inside the run and byte-identical to the value sealed in the prereg, so REUSE is valid.** Three timed steps, all rc=0: `pip install --target /workspace/mmlab_env --no-deps --force-reinstall <wheel>` 15.1 s; `pip install --target ... -c g324_constraints.txt mmdet==3.3.0 mmengine==0.10.7` 181.7 s; import gate `import mmcv, mmdet; from mmcv.ops import batched_nms` 69.8 s, stdout verbatim `2.1.0 3.3.0`. **A SECOND, INDEPENDENT PREMISE MEASUREMENT** ran 34 minutes earlier in job `20260907222928_461664_22971` (the run that then aborted on a wrong checkpoint path, before any model loaded or any number existed): **416.8 s, also MET, same three steps rc=0, same gate output** -- so the premise clears twice on two separate jobs, not once (contract B11). **Fix 1b (closes G324-SECOND-PREMISE-DURABILITY):** the run's `pod_run.log` was still on the pod at `/workspace/wt/a2/jobs/20260907222928_461664_22971/pod_run.log` and is now committed verbatim as `g324_artifact/g324_premise_run2_transcript.txt` -- traceback, `PREMISE {...}` JSON and `POD_RUN_DONE ... rc=1` all reproduce the 416.8 s figure exactly.
**Resolved runtime versions** (Fix 1b, re-run of the import gate ONLY, `docs/evidence/tracking/G324_VERIFY_2026-09-08.md`; verbatim transcript `g324_artifact/g324_import_gate_labeled.txt`, 38.605 s wall, 2026-09-08T05:28:31Z-05:29:11Z): `python=3.12.3`, `numpy=2.1.2`, `cv2=4.14.0`, `torch=2.8.0+cu128`, `mmcv=2.1.0`, `mmdet=3.3.0`, `mmengine=0.10.7`, and `from mmcv.ops import batched_nms` OK. Same values recorded under `environment.resolved` in `g324_summary.json`. The 267.8 s premise measurement itself is unchanged; only the gate's printed/labelled output was incomplete.
**REBUILD FROM SOURCE, REPORTED SEPARATELY AND NOT RE-MEASURED THIS ROW.** **INHERITED from G311: 2,921 s** (wheel 1,898 s + install 1,023 s, `TORCH_CUDA_ARCH_LIST=8.6`, sm_86 only), citation `g311_premise_artifact/route_i5_mmcv210_build.txt`. It does NOT count toward this row's 3,600 s because the reuse succeeded; had the hash differed, the row would have rebuilt inside the same sealed clock. **A reused wheel measures INSTALLABILITY, not buildability** -- 267.8 s is an install cost, and the 2,921 s figure remains the only measured build cost.
**LICENCES, pinned by URL.** Checkpoint `rtmdet_m_8xb32-300e_coco_20220719_112220-229f527c.pth` (pod `/workspace/wt/a2/g311/rtmdet_m_coco.pth`), 224,299,609 bytes, SHA-256 `229f527ca88498e8894a778a62a878a322b4a3ea2cae09ea537d34b7e907792b` re-verified inside this run, from `https://download.openmmlab.com/mmdetection/v3.0/rtmdet/rtmdet_m_8xb32-300e_coco/rtmdet_m_8xb32-300e_coco_20220719_112220-229f527c.pth`; config `mmdet/.mim/configs/rtmdet/rtmdet_m_8xb32-300e_coco.py`. Apache-2.0: `https://github.com/open-mmlab/mmdetection/blob/main/LICENSE`, `https://github.com/open-mmlab/mmcv/blob/main/LICENSE`, `https://github.com/open-mmlab/mmengine/blob/main/LICENSE`. **No mmyolo (GPL-3.0), no TVCalib/Sportlight/KaliCalib, no new AGPL dependency.** `torch.load` kept `weights_only=True` with exactly the 7 safe-globals G311 named.
**SOURCES (contract A9: full pod path + BYTE SIZE + resolution, all three ffprobe-confirmed native 1920x1080 at run time).**
`/workspace/nba-ai-system/data/footage_corpus/wnba__wnba_01_1080p.mp4` 308,078,882 B, 30/1, 600.067 s, nb_frames 18060, measured last decodable 18000.
`/workspace/nba-ai-system/data/footage_corpus/ncaa_basketball__ncaa_basketball_IB-_u4gW3ds_1080p.mp4` 328,260,234 B, 30000/1001, 600.099 s, nb_frames 18115, measured last decodable 17982.
`/workspace/nba-ai-system/data/footage_corpus/wnba__wnba_06.mp4` 430,058,965 B, 30/1, 960.033 s, nb_frames 28674, measured last decodable 28673.
40 evenly spaced indices per clip spanning 0 .. the MEASURED anchor (never ffprobe's `nb_frames`), **40 of 40 decoded on every game**, both arms on the same decoded frames from the same decode. Arm wall 437.2 / 275.8 / 224.3 s, all far inside the 3,600 s per-game budget; no BUDGET LIMIT.
**PER-GAME PER-ARM PROXY TABLE (denominator = 40 decoded frames in EVERY cell; ARM Y = production `yolov8n` AGPL-3.0, ARM R = RTMDet Apache-2.0; batch size 8).**
| game | arm | boxes (total / per frame) | zero-box frames | distinct ids | median track len | ms/frame single | ms/frame batch-8 | peak VRAM MB | raw rows |
|---|---|---|---|---|---|---|---|---|---|
| wnba_01_1080p | Y | 343 / 8.575 | 0 / 40 | 321 | 1 | 152.48 | 235.08 | 223.7 | 343 |
| wnba_01_1080p | R | 741 / 18.525 | 0 / 40 | 653 | 1 | 2947.78 | 2092.36 | 186.2 | 741 |
| ncaa_IB-_u4gW3ds_1080p | Y | 320 / 8.000 | 1 / 40 | 265 | 1 | 177.27 | 80.19 | 61.8 | 320 |
| ncaa_IB-_u4gW3ds_1080p | R | 541 / 13.525 | 1 / 40 | 446 | 1 | 2649.94 | 2489.87 | 185.8 | 541 |
| wnba_06 | Y | 326 / 8.150 | 1 / 40 | 283 | 1 | 124.67 | 42.66 | 61.8 | 326 |
| wnba_06 | R | 729 / 18.225 | 0 / 40 | 608 | 1 | 2127.75 | 1950.11 | 190.7 | 729 |
**BOTH-WAY AGREEMENT, greedy one-to-one, IoU >= 0.5, per frame (both shares reported because the arms emit different counts).** `wnba_01_1080p` 323 matched -> **agree_Y_in_R 323/343 = 0.9417**, **agree_R_in_Y 323/741 = 0.4359**. `ncaa_IB-_u4gW3ds_1080p` 298 matched -> **298/320 = 0.9313** and **298/541 = 0.5508**. `wnba_06` 291 matched -> **291/326 = 0.8926** and **291/729 = 0.3992**. Read only as the sign rule declares: 89-94 pct of ARM Y's boxes have an IoU >= 0.5 partner in ARM R's set while 40-55 pct of ARM R's do in ARM Y's -- CONSISTENT WITH ARM R emitting a superset on this corpus, and this row cannot say whether the extra boxes are players or false positives.
**BATCHED VS SINGLE-IMAGE, the figure G311 could not report.** ARM R batch-8 is CHEAPER on all three games (2947.78 -> 2092.36, 2649.94 -> 2489.87, 2127.75 -> 1950.11 ms/frame) and its boxes are **bit-identical to the single-image pass on all three games** (`identical_box_counts` true, `max_abs_coord_delta_px` 0.0). ARM Y batch-8 is cheaper on two games (177.27 -> 80.19, 124.67 -> 42.66) and DEARER on one (152.48 -> 235.08), and its boxes are **NOT stable under batching**: `wnba_06` returned a DIFFERENT box count from the batched path (`identical_box_counts` false), and on the other two games the per-index coordinate delta is 310.5 px and 861.0 px. **That delta is order-sensitive and unmatched -- it is CONSISTENT WITH the batched path merely reordering the same boxes OR with genuinely different detections, and this row cannot tell them apart.** Both ms/frame columns are the same frames, warm-up (3 frames) excluded and said to be excluded.
**RAW BOX ROWS COMMITTED (closes G311-RAW-BOX-DURABILITY).** Six CSVs under `docs/evidence/tracking/g324_artifact/`, header `frame_index,x1,y1,x2,y2,score,class`, one line per box, **the FULL set behind every count above, not a subset**: 343 / 741 / 320 / 541 / 326 / 729 rows, every cell above the >= 200-row floor. The greedy IoU matching is recomputable from these coordinates alone.
**PROVENANCE (closes G311-PROVENANCE; contract A9/A11), computed ON THE POD INSIDE THE RUN, not reconstructed.** Route SHA-256 as deployed: `g324_apache_arm_rebudget.py` `2d6123214200b9458cfe5f4dd77927251fdac75043100ae1d1a2f04da3599a00`; `g324_arms.py` `7209a4e4fd583959bc94b13e803ab306b7b317dbbbb88a6803774d8f309fc0bc`; `g311_apache_detector_arm.py` `68ac2a6379f6dc22a5af693dc04c4d439b22571211e3838c25ccf9378e52bd4b`; `g311_proxies.py` `9646d7ee73430b06813926fb9155b3eb1bd12817dd5d4bba6cf97a5513c334f0`. Source byte sizes are listed above and also in `g324_summary.json`.
**INTEGRITY / MUST-NOT-MOVE.** Deployed pod `src/tracking/player_detection.py` `c3bc2f7d4c4fda366f83523dd0aac86e47a40fecaf26e36b490d5a8c73ca5cc7` and `yolov8n.pt` `f59b3d833e2ff32e194b5bb8e08d211dc7c5bdf144b90d2c8412c47ccfc83b36`, **byte-identical before (02:42:53Z) and after (05:02:44Z)**. The `yolov8n.pt` the run actually loaded from the job cwd hashes `f59b3d833e...` too, so ultralytics did not auto-download a substitute and ARM Y really ran the production weights. The harness's own `gated_src_sha256` before == after == `532389342b93e259...` (the SHIPPED CRLF copy of the same file). GPU lease honoured: `7689 MiB, 24576 MiB` used/total at pre-flight (16,887 MiB free, above the 8,192 MiB gate) with 17 daemon compute apps listed verbatim in `g324_preflight1.txt`, and `7846 MiB, 24576 MiB` after; `track_daemon` was never stopped or signalled and is alive after (3 matching processes). Disk: `du_workspace_mb=UNKNOWN` at both probes (UNKNOWN, never 0, never a stop) while `pod_run`'s own untimed walk reported `POD /workspace 40491 MB used of 50000`; `dd conv=fsync` passed at 47.6 MB/s before and 28.0 MB/s after. All compute ran in `/workspace/wt/a2/jobs/20260907230339_499150_9145` and the install only under `--target /workspace/mmlab_env`; `data/registry/`, `data/tracking/`, the deployed `/workspace/nba-ai-system` tree and the daemon's system site-packages were not written. Pod log tail verbatim in `g324_artifact/g324_pod_log_tail.txt`, ending `WROTE g324_out/g324_summary.json` then `POD_RUN_DONE job=20260907230339_499150_9145 rc=0`.
**TESTS.** `tests/platformkit/test_g324_rows.py` -- **5 passed** (batch-8 chunking covers every frame with a short last chunk; batched and single agree for a deterministic detector; `batch_vs_single` reports a real difference rather than assuming none; a zero-box frame keeps its slot on both paths; the raw-row writer emits index + xyxy + score + class for every box). `tests/platformkit/test_loc_rail_scope.py` -- **1 passed**. New modules 179 + 138 lines, both inside the 300-line rail; the LOC allowlist was NOT edited. G311's IoU/matching/agreement coverage is imported, not duplicated.
**THREE ABORTED LAUNCHES, all BEFORE the sealed clock and none producing a number** (recorded so the run history is complete): job `20260907214500` died at import on a `SyntaxError` in a string literal; job `20260907220753` died in `sha256()` because Git Bash rewrote the argv token `/workspace/mmlab_wheels/...` into `C:/Program Files/Git/workspace/...`; job `20260907222928` cleared the premise at 416.8 s and then died on a checkpoint path one directory too shallow. **No detector loaded and no proxy existed in any of the three.** The fix was pod-relative argv plus `Path.resolve()` inside the harness, so every recorded path is the absolute pod path.
**NOT VERIFIED.**
- **Which arm is correct. Nothing here is ground truth**, so the box-count gap and the asymmetric agreement are unexplained by this row. G303/G296 score recall against labelled frames.
- **ARM Y's boxes are not stable under batch-8** (`wnba_06` count mismatch; 310.5 px and 861.0 px per-index deltas on the other two). The comparison is ORDER-SENSITIVE and unmatched, so reordering and genuine difference are indistinguishable here. **The single-image pass is the box source of record for every count in the table above** -- no proxy in the table is computed from the batched pass.
- **This run's single-image counts differ from G311's by 0-2 boxes** (ncaa Y 320 vs 318, ncaa R 541 vs 540, wnba_06 Y 326 vs 325; wnba_01 identical at 343 / 741), so the route is repeatable to about 0.6 pct on box count and NOT bit-exact. Cause not investigated.
- **`median_track_length` is 1 and `distinct_ids` ~ box count for BOTH arms, so those two proxies are UNINFORMATIVE here** -- the sampled frames are 15-25 s apart, so the greedy IoU linker cannot link across them. Reported because they were pre-declared.
- **`peak_vram_mb` is INCREMENTAL (post-load, post-warm-up) and now spans the single-image AND batch-8 passes, so it is NOT comparable with G311's single-only figures** and is not a weights comparison. ARM Y's 223.7 MB on game 1 vs 61.8 MB on games 2-3 is unexplained.
- **ms/frame is this configuration only** -- full 1920x1080, warm-up excluded, on a card shared with a 16-worker daemon, so it is an UPPER bound, not a benchmark. No engineering effort was spent making either arm fast, so a poor ARM R showing is as likely to be an unconfigured default as a property of RTMDet.
- mmdet logs `unexpected key in source state_dict: data_preprocessor.mean, data_preprocessor.std` on load. Believed benign for RTMDet but **not confirmed**, and its effect on ARM R's boxes was not measured.
- ONE corpus, three clips, 40 frames each, one seed. `ncaa_basketball_WFl3V7ZY4ss` still reports two video streams to ffprobe and was still not investigated.
**ARTIFACT SHA-256 (all under `docs/evidence/tracking/g324_artifact/`; `g324_summary.json` and `g324_import_gate_labeled.txt` re-hashed after Fix 1b's additive `environment.resolved` block, all other hashes unchanged).** `g324_summary.json` `c8277d0283bade9dd4a26dacd8ac09806753d24fc7223d7d77ff01abda732d13`; `g324_import_gate_labeled.txt` `1aa821f2273e453496e5b59c91a7412c3b8658576a87b5bb042789f4c0c1e063`; `g324_premise_run2_transcript.txt` `2758e696b2ca011d01ea5918e515b636eead439e4c1f9f0602103eb6c9049263`; `g324_boxes_wnba__wnba_01_1080p_Y.csv` `f228f3722a01f25bdc6debf2266783e887f23ba0c139243cf9fa943cbc663627`; `..._01_1080p_R.csv` `197dffab9ddbbc96d7aa31e0e6e02f63650217cd1c8945cf841da5d55bae252d`; `..._IB-_u4gW3ds_1080p_Y.csv` `b1ef930640e0e25815d9a230c058e6829110443eb0fe64d296a00d224d90eca7`; `..._IB-_u4gW3ds_1080p_R.csv` `c1f6e232ae7ecb571e557d7fffaedc0f1db5147412527d293d4435c38093c82a`; `..._wnba_06_Y.csv` `178b0200fd81a87db5eccec816a711293990339fdc0235cd4f7b6a8134af5a68`; `..._wnba_06_R.csv` `4992e9440726f6303ec4702b5d167fd15c4ee61341779514f4ef49cd6c731079`; `g324_pod_log_tail.txt` `0f35a4587068184adbc2e6fdbc94189bf1effb74582859eb0e9971db55b938a2`; `g324_preflight1.txt` `e37e0153b9393aa0a86ce4138108d8c027591a0d445c43d62de230fa215fdbdd`; `g324_after_probes.txt` `4090e743de11bc541f0ebc3697e8a61783b8cd17de6a165dc38f216d60dda8b0`; `g324_before_hashes.txt` `9a85269187427a680da736cbf196ae318a80db71b703957695b05837a69c9571`.
**TIME SPENT.** About 3 h 10 min of lane wall (2026-09-07 21:10 CDT -> 2026-09-08 00:20 CDT), of which roughly 70 min was `pod_run`'s own untimed `du -sm /workspace` walk across four launches (15-22 min each on the MooseFS volume) and 16 min was the three aborted launches above. The sealed premise itself took 267.8 s and the three games' arms took 937.3 s together.
**Fix 1b (verifier corrections, `docs/evidence/tracking/G324_VERIFY_2026-09-08.md`, REJECT on `6dc55b929`).** No arm or proxy was re-run; the 267.8 s premise and all six proxy cells are unchanged.
1. **Labeled resolved-version import gate (closes the FAIL).** `g324_apache_arm_rebudget.py`'s `GATE` constant now imports and prints labeled `python=`, `numpy=`, `cv2=`, `torch=`, `mmcv=`, `mmdet=`, `mmengine=` lines plus the `mmcv.ops` import, in the gate function only. Re-ran ONLY the import-gate probe on the pod under `/workspace/mmlab_env` (no arm/proxy/install rerun): `python=3.12.3`, `numpy=2.1.2`, `cv2=4.14.0`, `torch=2.8.0+cu128`, `mmcv=2.1.0`, `mmdet=3.3.0`, `mmengine=0.10.7`, `mmcv.ops` import OK -- 38.605 s wall, 2026-09-08T05:28:31Z-05:29:11Z. Verbatim transcript `g324_artifact/g324_import_gate_labeled.txt`; same values under `environment.resolved` in `g324_summary.json` (additive, every prior key byte-identical) and in the "Resolved runtime versions" line above.
2. **G324-SECOND-PREMISE-DURABILITY (NEW GAP, closed).** The 416.8 s second premise run's `pod_run.log` was still on the pod (`/workspace/wt/a2/jobs/20260907222928_461664_22971/pod_run.log`) and is now committed verbatim as `g324_artifact/g324_premise_run2_transcript.txt`.
3. **G324-CKPT-HASH-GUARD (NEW GAP, closed).** `main()` now compares the loaded checkpoint's SHA-256 against the sealed `CKPT_SHA256` constant (`229f527ca88498e8894a778a62a878a322b4a3ea2cae09ea537d34b7e907792b`, `g324_prereg_2026-09-07.md:60`) immediately after hashing it and BEFORE `provenance()`/`run_game()` can load the model, raising `RuntimeError` on any mismatch (`g324_apache_arm_rebudget.py` `main()`, right after `ckpt_bytes`).
4. **G324-REUSE-FALLBACK (NEW GAP, closed).** `install_premise()` no longer returns `REUSE REFUSED` on a wheel-hash mismatch; it now calls the new `source_build_step()` (pip source build of `mmcv==2.1.0`/`mmdet==3.3.0`/`mmengine==0.10.7` with `TORCH_CUDA_ARCH_LIST=8.6`, matching G311's inherited build route) under the remaining sealed budget, then still runs the same import gate. `sh()` gained an optional `env` kwarg (default `None`, unchanged behaviour for every existing call) so the fallback can set `TORCH_CUDA_ARCH_LIST`. New test `test_wheel_hash_mismatch_invokes_source_build_fallback` in `tests/platformkit/test_g324_rows.py` mocks both `source_build_step` and `sh` and asserts the fallback is invoked on a hash mismatch, with no real pip/subprocess call. This code path itself was NOT exercised on the pod (no arm rerun) -- it is unit-tested only.
5. **Tests.** `tests/platformkit/test_g324_rows.py` -- 6 passed (5 prior + 1 new). `tests/platformkit/test_loc_rail_scope.py` -- 1 passed. `g324_apache_arm_rebudget.py` grew 179 -> 213 LOC, `g324_arms.py` unchanged at 138 LOC; both still inside the 300-line PlatformKit rail, no allowlist edit.
**Fix 1c (verifier corrections, `docs/evidence/tracking/G324_VERIFY_2026-09-08.md`, REJECT on `0fef385fb`).** No arm or proxy was re-run; the 267.8 s premise and all six proxy cells are unchanged.
1. **Memo cap (closes the FAIL).** Compressed by removing 20 blank paragraph-separator lines (no table row, list item, sign-rule sentence or corrections section dropped); line count is now 50 lines (`wc -l` after LF normalization), inside the 60-line cap (`G324_spec.md:143`).
2. **B2 status alias (closes the FAIL).** `install_premise()`'s wheel-hash-mismatch branch now sets `premise_status: "REUSE REFUSED -> SOURCE BUILD FALLBACK"` (additive; `verdict`/`gate_ok` semantics unchanged) before calling `source_build_step`; `test_wheel_hash_mismatch_invokes_source_build_fallback` asserts the literal string alongside the fallback call.
3. **G324-FALLBACK-INSTALL-IDENTITY (NEW GAP, closed).** The source-build fallback now installs into a FRESH prefix `<target>_fallback_<epoch>` (never the original, possibly-populated `target`), and the import gate targets that fresh prefix; `gate_ok` is now `build_ok and gate.rc==0 and elapsed<=limit`, so a failed build (`rc!=0`) can never be masked by a stale import. New test `test_fallback_build_failure_blocks_gate_ok_even_if_gate_would_pass` mocks a failing build behind a mocked-passing gate and asserts `gate_ok is False`; both new tests assert the fresh-prefix path was actually used, not `target` itself.
4. **Tests.** `tests/platformkit/test_g324_rows.py` -- 7 passed (6 prior + 1 new). `tests/platformkit/test_loc_rail_scope.py` -- 1 passed. `g324_apache_arm_rebudget.py` grew 213 -> 229 LOC, `test_g324_rows.py` grew 119 -> 157 LOC; both still well inside the 300-line PlatformKit rail, no allowlist edit. Neither fallback code path was exercised on the pod (no arm rerun) -- unit-tested only, per fix 1b's same disclosure. Vocabulary scan (banned words/digits) on both changed files: 0 hits.
