# G357 preregistration - pixel-space ball ownership attempt 2 (2026-09-08)

Scope: finisher measurement only. The preparation lane does not fetch, open, or score real tracking windows. This artifact cites `docs/evidence/tracking/VERIFIER_CONTRACT.md`, especially B1-B10 and Q1-Q4.

Binding premise before any selection: read `/workspace/nba-ai-system/data/tracking/track_daemon_ledger.jsonl` once, in ledger order. Eligible clips have `started_at > 1788904286`, a basketball competition label, and a `ball_tracking.csv` header containing both `ball_x2d_px` and `ball_y2d_px`. Print clips, distinct games, and NBA/WNBA/other counts. Do not call the premise false unless this exact census was run and its output is quoted. The finisher waits/polls as the G357 specification directs when fewer than 30 windows from at least 3 games and 2 basketball competitions exist.

Selection: before a state-machine run, commit `sealed_windows.csv` alone. It contains the first eligible 2-second G344 windows in ledger order until it has at least 30 windows from at least 3 games and 2 competitions, or all currently eligible windows on a partial result. Each row names the full tracking and ball paths, their SHA-256 values and byte sizes, source dimensions, competition, start/end frame, and a stable `window_id`. The list reports its total observed-ball 3-frame motion-pair count; the required bar is at least 100 pairs. Every score tick has its own stable `window_id,frame` key; no game-level evaluator state stands in for its ticks.

Pixel join: only a finite pair from `ball_x2d_px,ball_y2d_px` may populate the image-space join (`join_source=px`). A missing px header is excluded and counted as `missing_px_header`; a present header with an empty/non-finite pair is excluded and counted as `missing_px_coordinate`. Existing court-map columns are neither changed nor used by the px arm. Per clip, print the G351 span ratio `ball_span/player_span` for x and y and flag a ratio above 1.5. The old court-map arm uses the same sealed windows only and emits survival diagnostics only.

Unchanged state and survival rules: G344 retains radius `60 * source_height / 720`, 10-pixel ambiguity margin, 3-frame history, cosine >= 0.5, ratio [0.5, 2.0], MAX_AGE 30, and cut protection. G349 stages are `frames`, `any_ball_row`, `detected_ball`, `player_rows`, `player_within_radius`, `prerequisite_available`, `motion_agreement`, and `accepted_ownership`, cumulatively over every full frame F. Report full-frame F and eligible observed-ball motion-pair denominators together. Run observed-only and inferred-inclusive rows separately. Run the unchanged state machine at unshifted, -30-frame, and +30-frame ball shifts; the shuffle bar is a drop of at least 200 additive per-mille (20 pp) in motion agreement, reported met or not met without alteration.

Outputs: `survival.csv` and `states.csv` retain every existing G349 column, add only columns, zero-pad integer cells, and provide additive per-mille shares. Archive loss or comparison records only from the shared `scripts/platformkit/eval_gate/` evaluator records, with purge and a symmetric nonzero embargo, if any scored comparison is attempted. This row makes no calibration comparison in preparation.

Sign convention: improvement = baseline loss minus candidate loss; positive = candidate better.
SEAL sha256 b71d5241cd82b258beb8d5a1b047e16714f4381d5f5d381f0762dd21ffae9fe3
