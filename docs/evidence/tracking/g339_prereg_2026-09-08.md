# G339 preregistration - ball-table consumer census

Date: 2026-09-08. Machine: LOCAL, because this row is a read-only repository census and CSV join; no pod, GPU, video, daemon, or deployed tree is used.

Binding premise command: recursively search `src/`, `scripts/`, `kernel/`, `domains/`, `api/`, and `intel/` for `ball_tracking.csv`, `ball_x2d`, `ball_y2d`, `ball_inferred`, `cls`, and `"ball"`; inspect the possession-simulation and feature call sites.
Binding output before this file: `src/features/feature_engineering.py:79` contains feature names `ball_x2d`, `ball_y2d`; no `src/sim/` line reads `ball_tracking.csv` or an equivalent joined table. The premise therefore holds.

Planned inputs, read one at a time: `wnba_05/ball_tracking.csv`, `wnba_05/tracking_data.csv`, `wnba_02/ball_tracking.csv`, and `wnba_02/tracking_data.csv`, identified by the G314/G320 artifacts. G314 records ball-table sizes 24473 and 23410 bytes; G320 says both tables are committed nowhere. The harness must not score or substitute an input if these paths are absent in this worktree.

Fixed harness behavior: group player rows by frame; left-join a ball row by frame; use `detected`, `inferred`, `ball_x2d`, and `ball_y2d`; calculate nearest-player Euclidean distance only for finite ball coordinates; report all frames, frames with detected ball, frames without detected ball, and distance count.
Fixed bar: classify every semantic reader hit; name writer schema; test one synthetic CSV pair; run the join report only on the two named WNBA pairs; propose at most ten lines for the first simulation-path BALL_BLIND consumer. No thresholds or production paths move.

This is a construct and repository census, not an OOS comparison. No evaluator, loss delta, charged trial, ledger K, or AHEAD claim applies. If a report runs, counts are descriptive: no candidate-vs-baseline delta is asserted.
SEAL sha256 c7076a78cf1b7c8afe6810dc10d46f73c8d36993758f3853e50d046a5dbc2dee
