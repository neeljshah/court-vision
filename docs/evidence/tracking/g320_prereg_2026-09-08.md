# G320 PREREG -- ball rows flagged inferred with no coordinate (2026-09-08, LOCAL)

Written and committed ALONE, before any G320 measurement existed (contract Q1). Nothing below is a
result. Two read-only acts preceded this seal and are disclosed here so the reader can discount them:
(a) `src/tracking/ball_detect_track.py` and `src/pipeline/unified_pipeline.py` were READ (no edit) to
name the candidate branches in section 2; (b) a header scan of the committed CSV files fixed the
census file list in section 4. Neither produced a count of the rows this row is about.

## 1. PREMISE CONSTRUCT (step 0, binding before-condition)

The G314 artifact `docs/evidence/tracking/g314_ball_inferred_coords_2026-09-07.md` reports 1 row in
`wnba_05` and 6 rows in `wnba_02` -- 7 of 2,000 -- carrying `ball_inferred = 1` with `detected = 0`
and no coordinate. The two ball tables are NOT committed anywhere under `docs/evidence/tracking`, so
they are fetched read-only from the pod (`ssh ... cat .../data/tracking/<game>/ball_tracking.csv`),
never written, no daemon or guard touched.

PREMISE PASSES only if, over EVERY row of both tables (head to tail, no sampling, no head slice):
the count of rows with `ball_inferred` true AND `detected` false AND no finite `ball_x2d`/`ball_y2d`
pair is 7 in total, 1 in `wnba_05` and 6 in `wnba_02`. Each such row is PRINTED by game and frame id
(zero-padded). If the count is other than 7 the premise is FALSE or PARTIAL: the actual count is
reported with its n and the row continues only for the rows that exist. A byte size differing from
the size G314 asserted (24473 and 23410) STOPS the row and is reported as such.

## 2. TRACE PLAN AND CANDIDATE BRANCHES (read-only, `file:line`)

Plan: follow the flag from the dribble predictor to the CSV cell, then enumerate every branch that
can leave the coordinate absent on a frame where the flag is already true. Named before measurement:

- WRITER SPLIT (the structural candidate). `src/pipeline/unified_pipeline.py:1991` writes the flag
  from `ball_det.ball_inferred`; `:1947` reads the coordinate from `ball_det.last_2d_pos` and
  `:1985-1987` writes it and `detected` from that one value. Two independent sources, never
  reconciled. Evidence for: the two cells are written in one dict from two variables. Against: it
  explains the shape, not which frame.
- BRANCH A, negative-projection reject, `src/tracking/ball_detect_track.py:919`. For: unconditional,
  fires on stale homography. Against: leaves no distinguishing trace in the table.
- BRANCH B, possessor-drift reject, `src/tracking/ball_detect_track.py:951`. For: fires when the
  projected point is far from every tracked player. Against: same, no distinguishing trace.
- BRANCH C, projection block skipped, `src/tracking/ball_detect_track.py:908` false. For:
  `last_2d_pos` is assigned only at `:919`, `:951`, `:956` and `:156`, never reset per frame, so a
  skipped frame inherits the previous value, which may be None. Against: requires the prior frame to
  have been None as well.
- STALENESS (expected NOT to apply to these rows). `:1991` reads the flag by `getattr` even on
  frames where `ball_tracker` was not called, but such frames have a coordinate and so `detected` is
  true, which the 7 rows are not.

The trace names the cause in one sentence if the code alone fixes it. If the code alone cannot fix
WHICH branch fired on a given frame, the verdict says UNATTRIBUTED for the branch, lists the
candidates above with the evidence for and against each, and states how it would be confirmed: print
the frame id at each of A, B and C and re-run exactly the frame ids the premise printed. No re-run
happens in this row.

## 3. READER HARDENING DESIGN AND ITS TWO TESTS

`scripts/platformkit/tracking/g314_ball_inferred_coords.py`, additive only (contract B2): every
existing output field keeps its name and its meaning, and the two existing tests stay green.

- `count_ball_table` RAISES when the `ball_inferred` header is absent, instead of reporting the flag
  counts as zero.
- NEW field `inferred_no_coord`: rows with the flag true and no finite coordinate pair, kept as its
  own class instead of being folded into `ball_detected` or `ball_inferred`. It is equal by
  construction to the existing `inferred_without_coords`, which is retained unchanged under B2; the
  test asserts the equality so the two cannot drift.
- NEW field `inferred_no_coord_frames`: the frame ids of those rows, so the premise can print them.

Tests, in `tests/platformkit/test_g320_ball_inferred_no_coord.py`: (1) a table whose header omits
`ball_inferred` makes `count_ball_table` raise; (2) a table built to the 7-row pattern reports
`inferred_no_coord = 7` with its n stated. A third test pins the census selection rule of section 4.

## 4. CENSUS SCOPE

Exhaustive, no head slice (contract B7). Scope rule, fixed here: EVERY file tracked by git under
`docs/evidence/tracking/` whose name ends in `.csv` is header-scanned; a file is a BALL TABLE when
its header carries both `ball_x2d` and `ball_y2d`, the per-row coordinate columns the reader
classifies. The hardened reader is run over every ball table so found and the outcome of each is
reported, a raise included, with its n. Files carrying a `ball_inferred` column that are not per-row
ball tables are listed separately with the reason they are not in scope. The two pod tables are not
committed and are reported in their own section, never merged into the committed census. Integer
cells in `census.csv` are zero-padded to six digits; shares are written as fractions.

## 5. VERDICT RULE

- DONE if all of: the premise rows are reproduced and printed by game and frame id; the trace names
  the cause at the writer with `file:line`, or honestly says UNATTRIBUTED for the specific branch and
  lists the candidates with evidence for and against each; the reader raises on a missing header and
  reports `inferred_no_coord`; the per-file test passes; every census cell carries its n.
- PARTIAL, with the explicit list of what failed, otherwise.
- PREMISE FALSE stops the row at section 1 and reports the count found, as G314 did.

## 6. STATED IN ADVANCE, NOT DISCOVERED

A cause named from code is a hypothesis until a re-run with the proposed producer change reproduces
those frames with a coordinate; this row performs no re-run, decodes no frame and measures no ball
recall. EYE CHECK: NONE. Image space only. A census over committed tables is a census of what was
archived, not of the pod fleet. No bar, threshold, register or committed artifact is moved. The
producer change is a PROPOSED diff under `docs/research/organization-sprint/` only; `src/` is not
edited.

SEAL RULE: the hex on the last line is the SHA-256 of every byte of this file above that line, after
normalising any CRLF to LF, up to and including the newline that ends the line before it.
SEAL sha256 30b530367c7a019a8c8f3d803b1ba4b06bc708b596d6621e028426836fafc634
