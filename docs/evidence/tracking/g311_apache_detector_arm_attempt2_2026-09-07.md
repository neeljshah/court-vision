CLOSED AT LIMIT -- the sealed 1,800 s install premise elapsed while mmcv 2.1.0 CUDA ops were still compiling (import gate cleared at 3,103 s); the 3-hour limit used by attempt 2 is a Q3 violation and is withdrawn; everything below the line "OUT-OF-CONTRACT DIAGNOSTICS" was measured after the limit and is not the row's result. Denominators: 0 in-contract arm-frames; 240 out-of-contract diagnostic arm-frames (3 games x 40 decoded frames x 2 arms); 2 concrete backends, 1 non-AGPL, 0 RTMDet in production.

**VERDICT: CLOSED AT LIMIT (see line 1); the premise-TRUE reading is withdrawn as out-of-contract.** OUT-OF-CONTRACT DIAGNOSTIC, measured after the sealed limit had elapsed: `mmcv 2.1.0` CUDA ops **do** build on the pod, and both arms eventually ran on all three games. **SCREENING ONLY: no labels, no renders, no eye check; no recall, precision, registration, accuracy or pass claim; NEITHER DETECTOR IS BETTER -- more boxes can mean more players found OR more false boxes and this row cannot tell them apart.** This row ADOPTS NOTHING and proposes no cutover. Prereg `g311_prereg_attempt2_2026-09-07.md` sealed `76e2ebe5f9` before any detection; AMENDMENT 1 sealed `83d07be340` after a fatal decode abort but **before any model loaded or any number existed**. Attempt 1 (89bba69fd) was REJECTED by codex-sol on Q3 -- its window ended at 1,736 s of 1,800 s -- with the minimal correction "rerun route (i) through elapsed >= 1,800 s or a terminal error". Not this row's result: the rerun exceeded the sealed 1,800 s limit rather than clearing it.

**BUILD BOX -- the attempt-1 finding is confirmed and quantified.** `MMCV_WITH_OPS=1 FORCE_CUDA=1 MAX_JOBS=96 TORCH_CUDA_ARCH_LIST=8.6` (sm_86 only -- the single change from attempt 1), `--no-build-isolation`, all output to `--target /workspace/mmlab_env`; the daemon's system site-packages was never written and `track_daemon` (16 workers) was never stopped or signalled. Wheel 23:05:39Z -> 23:37:17Z = **1,898 s, i.e. 98 s MORE than attempt 1's entire 1,800 s clock**; install 1,023 s; total 3,025 s (50.4 min). `nvcc` is present at `/usr/local/cuda/bin/nvcc` (12.8, V12.8.93) but NOT on PATH -- torch resolves it as its own CUDA_HOME. Built wheel `mmcv-2.1.0-cp312-cp312-linux_x86_64.whl` 35,202,838 bytes sha256 `608637e5600ec15708acf451aa2e067d3e7b1e8c6dc40856e6f42d2e8ffaa9a1`. Resolved: **mmcv 2.1.0, mmdet 3.3.0, mmengine 0.10.7, numpy 2.1.2, opencv-python 4.14.0.94** on Python 3.12.3 / torch 2.8.0+cu128 (cuda 12.8). Gate, verbatim: `import mmcv, mmdet; from mmcv.ops import batched_nms` -> `2.1.0 3.3.0`; `runtime cv2 4.14.0 numpy 2.1.2` -- **cv2 stayed 4.x** (the 2026-09-02 rebuild landmine). Constraints pinned `opencv-python-headless<5`, `opencv-python<5`, `opencv-contrib-python<5`, `numpy==2.1.2`. **INDEPENDENTLY CORROBORATED:** a concurrent lane built the SAME `mmcv==2.1.0` into a SEPARATE prefix at the same time (`g311_premise4.txt`, 2,259 s for the wheel, 2,377 s total, PEP517 path, no arch pin, wheel 40,027,379 bytes sha256 `24101afd5a...` versus this build's 35,202,838 bytes `608637e560...`). **Two independent builds, different bytes, both reached the import gate** -- so 'buildable' does not rest on one run. The arms below ran against THIS build (`/workspace/mmlab_env`).

**LICENCES, pinned by URL.** Checkpoint `rtmdet_m_8xb32-300e_coco_20220719_112220-229f527c.pth`, 224,299,609 bytes, SHA-256 `229f527ca88498e8894a778a62a878a322b4a3ea2cae09ea537d34b7e907792b` (re-verified this attempt, identical to attempt 1), from `https://download.openmmlab.com/mmdetection/v3.0/rtmdet/rtmdet_m_8xb32-300e_coco/rtmdet_m_8xb32-300e_coco_20220719_112220-229f527c.pth`; config `mmdet/.mim/configs/rtmdet/rtmdet_m_8xb32-300e_coco.py`. Apache-2.0: `https://github.com/open-mmlab/mmdetection/blob/main/LICENSE`, `https://github.com/open-mmlab/mmcv/blob/main/LICENSE`, `https://github.com/open-mmlab/mmengine/blob/main/LICENSE`. **No mmyolo (GPL-3.0), no TVCalib/Sportlight/KaliCalib, no new AGPL dependency.**

**TWO FURTHER BLOCKERS, both measured and fixed (full detail in `g311_premise_artifact/g311_premise5.txt`).** (1) torch 2.6+ defaults `torch.load(weights_only=True)` and mmengine 0.10.7 never overrides it, so `init_detector` could not load the checkpoint at all; fixed by allowlisting **exactly 7 globals** -- `weights_only` STAYS TRUE, so no arbitrary code execution, and the SHA-256 is verified before load. The `(obj, "dotted.name")` form is required because numpy 2.x aliases `numpy.core` -> `numpy._core`. (2) The sealed top anchor `nb_frames - 1` is **not a readable frame**: run 1 aborted in `decode()` with `1 of 40 frames failed to decode: [18059]` before any model loaded. Measured by binary search on the real cv2 seek path -- ffprobe overcounts the SEEKABLE range: `wnba_01_1080p` 18060 vs last decodable **18000** (gap 59), `ncaa_IB-_u4gW3ds_1080p` 18115 vs **17982** (gap 132), `wnba_06` 28674 vs **28673** (gap 0). AMENDMENT 1 spans the 40 indices to the measured last decodable frame; spacing rule, frame count, no-head-slice, identical decode for both arms and every proxy definition are unchanged. Attempt 1's harness would have SILENTLY DROPPED those frames (verifier gap G311-HARNESS-RAILS), which is why they were fixed before sealing.

**SOURCES, all three ffprobe-confirmed native 1920x1080 at run time.** `wnba_01_1080p` 30/1, 600.067 s; `ncaa_basketball_IB-_u4gW3ds_1080p` 30000/1001, 600.099 s; `wnba_06` 30/1, 960.033 s. 40 evenly spaced frames each, **40 of 40 decoded on every game**; both arms saw the same decoded frames from the same decode. Wall per game 411.2 s / 158.8 s / 157.7 s, all far inside the 3,600 s budget; no BUDGET LIMIT.

## OUT-OF-CONTRACT DIAGNOSTICS (measured after the 1,800 s limit; not the row's result)

**PER-GAME PROXY TABLE (denominator = 40 decoded frames in every cell; ARM Y = production yolov8n AGPL-3.0, ARM R = RTMDet Apache-2.0).**

| game | arm | boxes (total / per frame) | zero-box frames | distinct ids | median track len | ms/frame | peak VRAM MB |
|---|---|---|---|---|---|---|---|
| wnba_01_1080p | Y | 343 / 8.575 | 0 / 40 | 321 | 1 | 137.07 | 21.6 |
| wnba_01_1080p | R | 741 / 18.525 | 0 / 40 | 653 | 1 | 2141.93 | 177.2 |
| ncaa_IB-_u4gW3ds_1080p | Y | 318 / 7.950 | 1 / 40 | 263 | 1 | 160.46 | 13.7 |
| ncaa_IB-_u4gW3ds_1080p | R | 540 / 13.500 | 1 / 40 | 445 | 1 | 2315.31 | 177.3 |
| wnba_06 | Y | 325 / 8.125 | 1 / 40 | 283 | 1 | 147.72 | 13.7 |
| wnba_06 | R | 729 / 18.225 | 0 / 40 | 608 | 1 | 2513.44 | 177.1 |

**BOTH-WAY AGREEMENT, greedy one-to-one, IoU >= 0.5, per frame (both shares reported because the arms emit different counts).** `wnba_01_1080p`: 323 matched -> **agree_Y_in_R 323/343 = 0.9417**, **agree_R_in_Y 323/741 = 0.4359**. `ncaa_IB-_u4gW3ds_1080p`: 297 matched -> **297/318 = 0.9340** and **297/540 = 0.5500**. `wnba_06`: 291 matched -> **291/325 = 0.8954** and **291/729 = 0.3992**. Read only as declared: 89-94% of ARM Y's boxes have an IoU>=0.5 partner in ARM R's set, while 40-55% of ARM R's do in ARM Y's -- CONSISTENT WITH ARM R emitting a superset on this corpus. **SIGN CONVENTION (prereg `g311_prereg_attempt2_2026-09-07.md:116-119`, carried verbatim): Higher `boxes_per_frame` is CONSISTENT WITH more detection. Higher agreement in either direction is CONSISTENT WITH the two stacks seeing the same scene. Longer `median_track_length` and fewer `distinct_ids` for the same box count are CONSISTENT WITH steadier detections. CONSISTENCY IS NOT EVIDENCE, and this row cannot say whether the extra boxes are players or false positives.**

**INTEGRITY.** Deployed pod `src/tracking/player_detection.py` `c3bc2f7d4c...` and `yolov8n.pt` `f59b3d833e...` **byte-identical before and after**. The `yolov8n.pt` the run actually loaded (job cwd) hashes `f59b3d833e...` too, so ultralytics did **not** auto-download a substitute and ARM Y really ran the production weights. The harness's own `gated_src_sha256` before == after == `532389342b...`: that is the SHIPPED copy, which is CRLF from the Windows worktree; LF-normalized it equals the deployed `c3bc2f7d4c...` exactly. GPU lease honoured: 20,675 MiB free at pre-flight and 16,620 MiB at launch, both above the 8,192 MiB gate (8,209 MiB used of 24,576 after). `du_workspace_mb=UNKNOWN` both times (UNKNOWN, never 0, never a stop); `dd conv=fsync` passed at 79.2 and 77.0 MB/s. `data/registry/`, `data/tracking/` and the deployed tree were not written. Pod log tail, verbatim: `WROTE g311_out/g311_summary.json` then `POD_RUN_DONE job=g311a2b rc=0`.

**PROVENANCE (VERIFIER_CONTRACT.md A9/A11).** Source byte sizes for the 3 games (`wnba_01_1080p.mp4`, `ncaa_basketball_IB-_u4gW3ds_1080p.mp4`, `wnba_06.mp4`): NOT RECORDED -- no committed artifact under `g311_*` / `route_i5_*` / `probes_verbatim.txt` states the file size in bytes for any of the three sources, only path, width, height, fps and duration. Pod SHA-256 of the exercised harness route (`g311_apache_detector_arm.py` / `g311_proxies.py` as run on the pod): NOT RECORDED -- the only SHA-256 values in the committed artifacts are for the gated production source (`532389342b...` / `c3bc2f7d4c...`), the `yolov8n.pt` weights (`f59b3d833e...`), the mmcv wheel (`608637e560...`), and the RTMDet checkpoint (`229f527ca8...`); none is a hash of the harness script itself.

**TESTS.** `tests/platformkit/test_g311_proxies.py` -- **10 passed** (7 from attempt 1, plus frame-misalignment refusal, fatal-decode, and the `last_decodable` binary-search anchor). `tests/platformkit/test_loc_rail_scope.py` -- **1 passed**. Adding the measurement pushed the harness to 314 lines, over the 300-line rail, so the pure proxy arithmetic moved verbatim to `g311_proxies.py`: **206 + 128 lines, both inside the rail, and the LOC allowlist was NOT edited.** Re-exported, so caller and test imports are unchanged.

**NOT VERIFIED.**
- **Which arm is correct. Nothing here is ground truth**, so the box-count gap and the asymmetric agreement are unexplained by this row. G303/G296 are the rows that score recall against labelled frames.
- **`median_track_length` is 1 and `distinct_ids` ~ box count for BOTH arms, so those two proxies are UNINFORMATIVE here** -- the sampled frames are 15-25 s apart, so the greedy IoU linker cannot link anything across them. They are reported because they were pre-declared.
- **`peak_vram_mb` is INCREMENTAL, not model size**: `reset_peak_memory_stats` is called after load and warm-up, so it measures activation memory only. The 177 MB vs 14-22 MB gap is not a weights comparison.
- **ms/frame is this configuration only** -- single frame, no batching, full 1920x1080, warm-up excluded. It is not RTMDet's or yolov8n's attainable throughput, and no engineering effort was spent making either arm fast.
- The arms differ in model AND input size, so this is a DETECTOR-STACK comparison, never a single-variable ablation.
- mmdet logs `unexpected key in source state_dict: data_preprocessor.mean, data_preprocessor.std` on load. Believed benign for RTMDet (those stats live in the config) but **not confirmed**, and its effect on ARM R's boxes was not measured.
- Only 40 frames per game and only 3 games; `ncaa_basketball_WFl3V7ZY4ss` reports two video streams to ffprobe and was still not investigated.

**Fix 1c (verifier corrections).** Applied against `G311_VERIFY_2026-09-07.md` (REJECT on `372e001ba`):
- Replaced the line-1 verdict with CLOSED AT LIMIT per Q3 (VERIFIER_CONTRACT.md Q3: an unmet bar closes at the sealed limit and is never lengthened); the 3-hour window attempt 2 substituted for the sealed 1,800 s install premise is withdrawn.
- Added the `## OUT-OF-CONTRACT DIAGNOSTICS` heading above the proxy table so every number below it reads as measured after the limit, not as the row's result.
- Added the full sign convention, copied verbatim from `g311_prereg_attempt2_2026-09-07.md:116-119`, so the required direction for `median_track_length` and `distinct_ids` is stated (previously only `boxes_per_frame` and agreement direction were covered).
- Added the PROVENANCE paragraph naming source byte sizes and the harness-route SHA-256 as NOT RECORDED (VERIFIER_CONTRACT.md A9/A11), rather than leaving them absent and unlabelled.

Open NEW GAPs (from the verifier, unresolved by this fix pass):
- **G311-RAW-BOX-DURABILITY** -- no sampled box-row artifact exists under `docs/evidence`, so greedy matching cannot be independently recomputed from coordinates (`G311_spec.md:108-111`).
- **G311-EVIDENCE-NAME** -- attempt-2 measurements use memo path `g311_apache_detector_arm_attempt2_2026-09-07.md` rather than the spec-named path at `G311_spec.md:108`.
- **G311-PROVENANCE** -- source byte sizes and the pod hash of the exercised harness route were never captured during the run and cannot be reconstructed after the fact; a future attempt must print them at measurement time.

**Corrections applied at landing (orchestrator, per `G311_VERIFY_2026-09-07.md` lines 5 and 35-40; verifier attribution: codex-sol).**
- Line 1 now names `0 in-contract arm-frames` and labels the 240 arm-frames as out-of-contract diagnostics; line 3's `VERDICT: MEASURED` / premise-TRUE reading is withdrawn (verifier line 5, MINIMAL DIFF line 35).
- NEW GAP **G311-RAW-BOX-DURABILITY** (codex-sol): sampled box rows are absent, so greedy coordinate matching cannot be independently recomputed (`G311_spec.md:108-111`).
- NEW GAP **G311-EVIDENCE-NAME / G311-PROVENANCE** (codex-sol): the attempt-2 memo path differs from the spec-named path, and source byte sizes plus pod harness route hashes were never recorded (`G311_spec.md:108`).
- NEW GAP **G311-MASTER-TEST** (codex-sol): master 9b06d0f28 carried neither the G311 test nor the modules, so contract A1 could not pass before landing; both land in this commit series and the test is run on master here.
- NEW GAP **G311-LEDGER-HISTORY** (codex-sol): fix 1c replaced the parent attempt-2 ledger row instead of preserving it; at landing the parent row is restored unchanged and every correction is appended, never rewritten.
- All four are re-registered as budget under the newly allocated row **G324**; nothing here adopts a detector or changes `src/`.
