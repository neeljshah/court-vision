VERDICT: PARTIAL. The sealed supply premise is FALSE -- the whole retained pool held 14 resolved exact-ID-disjoint candidates; older alternate-upload relation unresolved against the sealed 30 -- so no draw, no controls and no ball rating were made. The frozen play gate was still run over the retained sources and its sealed 60-card audit was drawn, but only one reviewer visited the cards, so the two-rater gate bar is NOT EVALUABLE and the gate is neither passed nor failed.

# G404 Play-state-gated ball growth stage 2 -- supply premise FALSE, gate audit drawn not adjudicated

PREREG `g404_play_gated_ball_growth_stage2_2026-09-11/prereg.md`, sealed ALONE at 5112012e9, SEAL `36ffa3620e776f6bb3a7e8e335fe1cb4dbd90791ac6d3994743e1cf48eeb4de4`, verified here by rehashing every LF byte above the seal line. Not edited. AMENDMENT A1 (astra round 10 transport clarification: one native card per transport request, per-card id and image-hash echo, BINDING_FAULT on a failed echo) was sealed ALONE in its own commit 039ac8811 BEFORE any rating, SEAL `93857d40a18f09d740e29618546e75398259d759447fb02beadab93a9eaee5d8`. No bar below differs from `specs/G404_spec.md`.

PREMISE (step 0, MEASURED). Recount of the landed reference: `g389_ball_reference_completion_2026-09-11/dev_boxes_v3.csv` holds 530 boxes over 27 game identities, unchanged. The old identity set is 101 distinct identities unioned from seven landed tables (G389 splits 60, the 530-box games 27, G387 context 12, G394 context 32, G398 dev shadow 28, G400's 30 rated ids and its 35 censused ids); each source path, field and count is in `premise.json`. G403's handoff reproduces its sealed instruction text exactly: `docs/evidence/tracking/g403_ball_rater_failure_controls_2026-09-11/instructions.md` internal SEAL `9693c36fb2e928107e0cf99dd20501c953286072d174ac205bf11e951892f825`, file SHA-256 `7b73ab53a26c9e4a105223f4d633481d92d507c3215381204209c1789e2ddd12`. That path is `ABSENT-IN-WORKTREE` on lane a18 (its base 9257388bb predates the G400 and G403 landings) and was read read-only from master; both rows are in `input_hashes.csv`. Gate assets verify against their sealed identities: reference embeddings `docs/evidence/tracking/g364_learned_court_presence_2026-09-09/dev_embeddings.npz` SHA-256 `f5d526d6cdcb6d3177e2b0240ae01e412be3dd82d165cd08c382168d10b5c22d` MATCH, frozen weights `C:/Users/neelj/g404_receiver/weights/resnet18-f37072fd.pth` SHA-256 `f37072fd47e89c5e827621c5baffa7500819f7896bbacec160b1a16c560e07ec` MATCH, and `g375_corpus_sport_purity_2026-09-10/diagnostic.json` matches its sealed `2c2a4338d616b12e59433de25208272ac3f65a8ecec15f23cb874efc0926fc36` after LF normalization (the lane checkout is CRLF). One cited digest does NOT match: the spec's G400 summary digest `604ab2c7...` predates G400's own fix 1c correction, whose current file is `e3e3434b43ae89390adc2845cc4478a303b900f660ac9355b4055aa0947d7969`; the row proceeds on the corrected file and names the difference.

SUPPLY (the binding failure). Whole-pool census at CENSUS_UTC 2026-09-11T20:39:09Z over `/workspace/data/footage_corpus` and the deploy bridge: 79 retained basketball-family sections over 14 video ids, every one ffprobed for actual width, height, frame rate and duration, every title resolved from the feeder discovery log. Zero overlap with the 101 old identities, two competitions. **14 resolved exact-ID-disjoint candidates across 2 competitions vs 30 required; relation to older alternate uploads remains unresolved.** The premise is FALSE and nothing was drawn. Supply then eroded further while the row ran: of the 14 median-offset sections only 6 survived to retention 20 to 40 minutes later (3 bridge and 5 corpus objects were pruned by the rotation), each of the 6 re-hashed on the PC to exact byte agreement with its pre-retention pod digest (`source_receipts.csv`, 6 MATCH / 8 ABSENT). The structural finding for the successor row: the retained pool is a rolling window of roughly a dozen games that prunes faster than one row can census and retain it, so a single-snapshot 30-new-game requirement is not satisfiable at the current feeder and quota settings.

FROZEN GATE (run, not fitted). Exactly the G375/G364 route: torchvision ResNet-18 ImageNet1K V1 at the weights digest above, cosine nearest class among the 240 frozen G364 reference embeddings, first reference index on ties, no new fit, cutoff, smoothing or alternate classifier, and no ball detector or visibility cue anywhere in candidate selection. Sheets follow the landed G375 rule (1280 wide, the same quality ladder and size cap). Over the 6 retained sections the decoded frame PTS schedule was probed per container (22,756 decoded frames in `decoded_pts.csv`, never a packet count) and the sealed two-second grid produced 386 candidates strictly inside the decoded spans, 386 decoded, 0 undecodable, 0 duplicate pixel digests. Gate outputs: 187 admitted, 199 excluded (`gate_outputs.csv`, `gate_summary.json`).

GATE AUDIT (drawn under seal, adjudicated by one reviewer only). 30 admitted and 30 excluded rows were drawn evenly within each FULL stratum at `floor(j*(N-1)/29+0.5)` over N=187 and N=199, rendered as native full-resolution cards at `sheet_scale` 1.0 with opaque ids in a dispatch order derived from the preregistration seal; draw digest `b9c84664cbc010d1c861fb292a54954351b29f7dca61b661bdd1e0b64e3d287f`, dispatch digest `0ac26c4389221ad3cec73b54d85b5c6c9f4504b61651489e0164aec6b3135ac9`, all 60 cards re-hashed on the PC with 0 mismatches. The finisher labelled all 60 blind from native pixels before any stratum was read (`gate_raw/gate_raw_claude.csv`, contact sheet `renders/gate_audit_cards.jpg`).

| bar | measured |
|---|---|
| >= 30 resolved new games, >= 2 competitions, before drawing | 14 games / 2 competitions NOT MET -> PARTIAL, no draw |
| >= 30 rows in each gate-audit stratum | 187 admitted / 199 excluded MET |
| >= 27 of 30 admitted frames JOINTLY PLAY | NOT EVALUABLE (one reviewer); single-reviewer upper bound 26 PLAY / 3 NONPLAY / 1 UNKNOWN, so the bar is unreachable on this draw even with a perfectly agreeing second rater |
| >= 24 of 30 excluded frames JOINTLY NONPLAY | NOT EVALUABLE (one reviewer); single-reviewer upper bound 25 NONPLAY / 4 PLAY / 1 UNKNOWN, which would clear the bar |
| both rater control sets 30/30 plus 20/20 centre matches | NOT REACHED -- 0 controls administered |
| ten paired rounds, kappa, usability, adjudication, yield over 300 keys | NOT REACHED -- 0 keys drawn, 0 ratings |
| 530 accepted boxes and G400's 116 unused additions unchanged | untouched, 0 writes to either MET |
| two fresh-process reproductions of every delivered table | `repeats.json` identical true MET |

WHY NO RATER RAN. Amendment A1 requires a fresh authenticated terra home built by copying only the authentication material of the existing home. Creating that directory was denied by this session's permission system, and a home change is in any case not the binding remedy -- the per-card echo is. Rather than dispatch rating cards from a home carrying prior context, which the amendment forbids, no smoke card, no control and no rating card was sent (`common_receipts/rater_transport.csv`). The transport driver that implements the amendment is committed unused at `scripts/platformkit/tracking/g404_rate.py`.

NOT VERIFIED.
- The gate audit is NOT adjudicated: one reviewer is not two independent raters, and the sealed joint bars are therefore neither passed nor failed. The 26-of-30 admitted count is this reviewer's judgment and an upper bound, not a gate verdict.
- The dominant labelling ambiguity is a single bcl broadcast shot from a fixed stand camera in which the court fills a small part of the frame; 8 of the 60 cards are that view and two cards were answered UNKNOWN for exactly this reason. A second rater could move the admitted count in either direction.
- Alternate-upload disjointness is exact on video id and source digest only; titles resolve for the 14 retained games but not for most of the 101 old identities, so an alternate upload of an old game remains UNRESOLVED.
- The pod ran OpenCV 4.14.0 and torch 2.8.0 while the G375 diagnostic was produced on an earlier pod build; query embeddings were recomputed here, reference embeddings were loaded from the sealed archive, and no cross-build equivalence was measured.
- Nothing here estimates ball yield, and no statement is made about whether this gate would help a later collection.

Q6 SCAN. `q6_scan.json` carries the complete repository-relative `path_manifest` for every scanned landable text artifact, including runtime `.txt` receipts, the memo, changed source and test; patterns are built from character codes, prose patterns use word boundaries and numeric patterns record their pattern indices and CSV fields. `claim_context_hits` is 0 exactly; the sole finding is three `cosine_distance` data-field hits in `gate_outputs.csv`, never a claim. No prose pattern appears anywhere.

DISCIPLINE. No `data/registry/` write, no flag change, no daemon, feeder or discovery interaction, no deploy-tree write, 0 GPU minutes (the gate ran CPU-only with one thread), pod scratch peaked at 467 MB inside the 2 GB clause and its staged source copies were released after the PC receipt; the 6 retained sources stay at `C:/Users/neelj/g404_receiver/sources` and the 60 native cards at `C:/Users/neelj/g404_receiver/cards`. Route digests are in `common_receipts/route_digests.txt`, environments in `common_receipts/environment.csv` and `common_receipts/pod_environment.txt`.

FIX 1b (2026-09-11). Scanner `582ccd3fc0a5ed936123f2790cd2ec8c83c218c315b70320dbe4dc15f7c681c3`; Q6 receipt `e3b73d63dd0dc849149e4022f1129b72876433439a0e8bfc84dce2aa246cbe63`.
Receiver mapping `74b638d013b9337694a9adbe5c23690053b4af3d81689da790b992a725827aa9`; summary `990ec151fb13a594f4d832cd8361900fd220ae325b9e79c9b33aa50e0011e74d`; repeats `55865f4ec2b45d5d292083fc42ac6ca708d09e02927306fbf21edea8222a05a1`; route digest `ba7ff7511102b980d69fd48cb39ecefc1a315a3ac43bd812cde5150cda89a5c9`.
`SHA256SUMS` records the byte-domain digests; measurements, labels and PARTIAL verdict are unchanged.
