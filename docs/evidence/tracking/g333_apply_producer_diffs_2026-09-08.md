VERDICT PARTIAL -- all 4 of n=4 landed PROPOSED producer diffs applied, each with a construct test that FAILS on the pre-edit file and passes on the edited one (19 tests in the new module, 16 of them red before the edits, 0 measured regressions); `unified_pipeline.py` grew 11 lines against a budget of 12; 0 register edits, 0 flags flipped, 0 thresholds moved; across the 33 existing test files of the two touched modules the SET of failing tests is identical before and after (34 failures and 1 collection error in 9 files [verifier-measured; identical on master and candidate; 431 passed, 10 skipped], 0 regressions); PARTIAL because G333_spec.md:69-74 requires all existing touched-module tests green and those 34 failures + 1 collection error PRE-EXIST on master, and because the smoke exceeded the prereg's fixed 1.5 GB peak-RSS bar and so is descriptive only.

PREREG sealed and committed ALONE at `19592438a` before a single byte under `src/` moved; seal `4b3764fe5ac7d833336561f85437014bff65d7789069615da60af88a8329dcc4` over the LF-normalised bytes above its last line. Spec `specs/G333_spec.md` VERSION 2026-09-08. EYE CHECK: NONE -- there are no labels here, only code, parses and row counts.

DIFF SOURCES, digest-verified against the value each row memo recorded BEFORE anything was planned (all 4 of n=4 MATCH):
    69d50cdae01facc6c44ab5a7897cd6c6fb842c68bae8a3ec1ad7d5f4f9daf395  G330_PROPOSED_pano_cache_key.md (2385 B)
    1fc07bd285fd0d9bfc906ea053d9aecd139e64f586e6c9535bc84f7eac707a86  G320_PROPOSED_ball_inferred_coord.md (2439 B)
    ba00c0ddf8e6a27e422842ccebe25bdc87c9b05f2a967c72bb4a25f6ddbb3bd3  G331_PROPOSED_evaluated_frames.md (2313 B, worktree a6)
    8539de4733cb9c83e65826bfe742d54a2078306f704f6c973759ce0c6e1b0160  PROPOSED_g325_drop_wholly_offframe_2026-09-07.md (3998 B LF)

INTERPRETER, both printed because this box carries two and only one can load the route:
    conda basketball_ai  Python 3.10.20 | numpy 1.26.4 | torch 2.1.2+cu121 | cv2 4.13.0   <- every command in this row
    system Python310     Python 3.10.0  | numpy 2.2.6  | torch 2.2.0+cu121 | cv2 4.11.0

NEW GAP, pre-existing and unrelated to these diffs: NEITHER local interpreter starts the route as shipped. conda -- `import onnx` raises AttributeError because ml_dtypes 0.3.2 lacks the 4-bit float type `onnx/_mapping.py` wants, and `torch/onnx/_internal/onnxruntime.py` guards that import with `except ImportError` only, so it escapes through torchvision and kills `from ultralytics import YOLO`. system -- torchreid imports tensorboard, which reads `np.bool8`, removed in numpy 2. Both smoke arms therefore ran under a scratch-only launcher that puts a stub `onnx` module in `sys.modules` before torch loads; it is byte-identical for both arms, installs nothing, and changes nothing on disk. Worth its own row.

PREMISE, 4 of n=4 defects REPRODUCE on the pre-edit tree, 0 CLOSED ALREADY:
  G330  the stubbed route wrote the per-video cache file whenever the general fallback was substituted. On the real section the route printed `Pano saved -> pano_nba__0022500081_s2436.png` and that file's sha256 equals `resources/pano_enhanced.png` exactly. Local cache census: 3 of n=3 files byte-identical to that same fallback before the arm, 4 of n=4 after it.
  G325  no guard existed at the single row-build site -- the statement after `bbox = track["bbox"]` was the row append -- and `_bbox_off_frame` did not exist. Corpus figure carried from G325 attempt 2, NOT re-measured here: 23408 of 480158 player observations over 122 pod games.
  G320  the shipped `ball_inferred` expression, lifted verbatim out of the source and evaluated with no coordinate and the detector flag up, returned 1.
  G331  `_publish_evaluated_frames` could not be imported, and the BEFORE arm's sidecar carried `evaluated_frames: null` with `reason: max_frames_is_detector_dependent_in_this_route`.

DIFFS APPLIED, 4 of n=4 (file:line before -> after, lines added):
  G330  unified_pipeline.py :969 -> :969 flag declared (+1) | :980 -> :982 flag set at the substitution (+1) | :981 -> :983 the print already there amended in place (+0) | :988-989 -> :990-992 the cache write and its print moved under `if not used_fallback` (+1). Subtotal +3.
  G325  unified_pipeline.py :2691 -> :2695-2696 guard immediately after the bbox assignment (+2) | new staticmethod `_bbox_off_frame` at :3321-3324 (+5 with its separating blank). Subtotal +7. Threading the decoded frame width in -- the proposal's only non-mechanical part -- is `frame.shape`, which at that site is already TOPCUT-cropped and is never rebound after :1689.
  G320  unified_pipeline.py :1991 -> :1994, the one line replaced in place (+0).
  G331  unified_pipeline.py :3049 -> :3055 `evaluated_frames` added to the `run()` return (+1); run_clip.py new `_publish_evaluated_frames` at :226-243 (+19) and its call at :508 -> :528 (+1).
  INTENDED EFFECTS, named as effects and not as regressions: a fallback run now writes NO per-video cache file; a wholly-off-frame row is no longer emitted, so every denominator taken from `tracking_data.csv` row counts moves DOWN; the inferred flag reads 0 instead of 1 on a row with no coordinate. Additive otherwise: both arms' `tracking_data.csv` headers are the same 70 columns with the same digest, and the ball table header is unchanged.

TESTS, each file alone, never a full pytest. `tests/platformkit/test_g333_producer_diffs.py` -> 19 passed (pre-edit/master: 16 failed, 3 passed). `tests/platformkit/test_loc_rail_scope.py` -> 1 passed. The other 32 existing test files naming either touched module were run one at a time on the edited tree and again on the stashed pre-edit tree: the FAILED/ERROR name lists are identical -- 34 failures and 1 collection error in 9 files (verifier-measured; identical on master and candidate; 431 passed, 10 skipped) -- over `test_coord_normalization` (a missing local model artifact), `test_dengw` (the collection error), `test_hardening`, `test_phase2`, `test_possession_boundary`, `test_possession_tracking_gaps`, `test_run_phase_g`, `test_shot_gate`, `test_shot_log_team_abbrev`. `tests/validate_pipeline.py` names the pipeline but is not a `test_*` module and pytest does not collect it, so it was not run.

SECTION SMOKE -- 1 clip, a SCREENING, not a census. `nba__0022500081_s2436.mp4`, 1280x720, 30 fps, 4021 frames, 33120099 B, sha256 `0ec016b7608ad7606e1dbf6d1695692efd455df5079bcdf8c94d97f6bacd1608`, copied READ-ONLY from the pod corpus and deleted after the second arm. Sealed in the prereg before either arm: start at source frame 1200 (not 0), the route's own fixed stride of 3, cap 150 gameplay frames. Both arms identical but for the diffs. The pod was never written to, no daemon and no guard was signalled, and nothing under `/workspace/nba-ai-system` was modified.
    arm     player_rows  wholly_off_frame  ball_rows  flag_set_no_coord  per_video_panos  evaluated_frames  wall_s  peak_MiB
    before       000034            000000     000050             000000           000004              null  000045    002689
    after        000035            000000     000050             000000           000003            000050  000035    002678
  Both arms peaked at 2689 / 2678 MiB, above the prereg's fixed 1.5 GB peak-RSS bar under which an over-limit arm is NOT RUN, so this table is DESCRIPTIVE ONLY and carries no verdict weight.
  G330 and G331 are consistent with that table end to end: the per-video cache file stops appearing, and the route's null becomes the true detector-gated count of 50.

NOT VERIFIED:
  - G325's row drop is NOT demonstrated on the section. Both arms carried 0 wholly-off-frame boxes over n=34 and n=35 player rows, so this clip has no power for that effect; its evidence is the construct test plus the committed corpus census, not this run.
  - G320 likewise: 0 of n=50 ball rows had the flag up in either arm, so the section says nothing about it.
  - The two arms are NOT byte-reproducible (34 against 35 rows, 2 against 3 identifiers), so no single-row difference between them may be attributed to a diff.
  - Peak resident set size was 2689 and 2678 MiB, ABOVE the 1500 MiB ceiling the prereg fixed. Both arms were run anyway, with 3618 to 3911 MiB free and the box responsive throughout. The ceiling was the wrong number, not the runs; it is named here rather than quietly dropped.
  - Registration is still broken on this clip -- the stitch fails, the general fallback is substituted, and only 26 of n=150 gameplay frames produced any player row. These diffs do not address that and no held-out inlier ratio moved.
  - Nothing was deployed. The pod still runs the pre-edit producer; a deploy is a separate orchestrator step.
  - Under the launcher above, torch's ORT backend was absent during both arms. It was already effectively absent (onnxscript is not installed), but it is a stated difference from an untouched environment.

Corrections (fix 1b, 2026-09-08): (1) memo:1 VERDICT DONE -> PARTIAL because G333_spec.md:69-74 requires all existing touched-module tests green and 9 files fail that bar on master identically. (2) memo:1,30 "26 failures in 7 files" -> "34 failures and 1 collection error in 9 files" (verifier-measured; identical on master and candidate; 431 passed, 10 skipped), and the pre-edit construct count corrected from "16 failed, 3 passed" to "16 failed, 3 passed". (3) memo:36 (now the SECTION SMOKE table) is labeled DESCRIPTIVE ONLY because both arms peaked at 2689/2678 MiB, above the prereg's fixed 1.5 GB bar under which an over-limit arm is NOT RUN. NOT RESOLVED here, carried for the orchestrator: touched python LOC run_clip.py 821 and unified_pipeline.py 4791 exceed 300 while test_loc_rail_scope.py does not cover them; pytest temp directories with restrictive ACLs are unreadable to verifier cleanup (.verify_tmp/current remains inaccessible and untracked).

WALL TIME about 2 h, entirely local; 2 GPU arms of about 45 s and 35 s; 0 pod commands other than one read-only listing, one read-only probe and one file copy.
SHA-256 (LF-normalised bytes): prereg whole file `09afc795bc3e015df68bedbc9730b8674df339f8924a3d101e67f7f5fe3baa48` | test module `3b2a2e2cfa076968cf24fe8325eb0606c25fbc4f9177936ce08a2149e944be31` | `unified_pipeline.py` `047dd04e9b12b588c560f68dbab32aa1855f791c2e1a46f19f4e082f50c4f331` before and `627961ab9ecad290dd65d6c13a8347d5bb4c7dd415084fd6fd648683fd9cbf3d` after | `run_clip.py` `e6346f8ce46c99497fa43e8ac48242855623a0ba3e9afaff638091beb21d2a9d` before and `ccd08d32c8c4cb8625ec5ef67535206f74f4df307a8e191ec01f5700fb40d8c2` after | before sidecar `da6d3b24ca0037cb0772c2de174a0f1397d5eba9638d5a72cb292b31479aad5d` | after sidecar `6ee7f0cbf429c468d342189bcb69f2920c09817b4142cd281547f366e47beb60` | general fallback `69024f8ef08d1e6093ddaf2bda0dd5df8630eab5f1b6b202daa08df8e47e5fff`. Hexadecimal digests are single tokens; a two-digit run inside one is not a standalone figure.
