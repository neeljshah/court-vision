# G313 preregistration -- sealed BEFORE any G313 number or trace exists (2026-09-07)

Sealed in worktree a12 at base commit cf9612bdf (the G312 landing), BEFORE the three distinct-id
counts were printed, before the id assignment path was traced and before any construct was written.
Everything below the seal line is a hash of everything above it.

## What is fixed before any G313 number exists

1. PREMISE, binding. THREE named censused games -- `den_phx_2025`, `wnba_02` and
   `ncaa_basketball_sRtHQbywiTE` -- read from the pod READ-ONLY into a scratchpad. Print the count
   of distinct `player_id` values in each `tracking_data.csv`. **If any of the three is not exactly
   10, the premise is FALSE: STOP, write the memo, commit, report PREMISE FALSE.** The three games
   are fixed now and were chosen before counting, for spread: two strides (6 and 3), two source
   heights (720 and 1080), three sports (nba, wnba, ncaa_basketball), and row counts 1,653 / 2,972 /
   6,287 spanning the census range.
2. THE COUNTING RULE, fixed now: distinct non-empty `player_id` values over ALL data rows of
   `tracking_data.csv`, no filtering by class, frame, confidence or track length. This is the G309
   `distinct_track_ids` convention and is not re-defined here.
3. THE TRACE IS READ-ONLY. `src/`, `domains/`, `api/`, `kernel/` and `intel/` are read and imported,
   never edited. The path followed is `scripts/run_clip.py` -> `src/pipeline/unified_pipeline.py`
   -> `src/tracking/`. **The deliverable is a `file:line`.** If more than one mechanism could
   produce a ceiling of 10, every candidate is named and the memo says which one the construct
   actually fires and which were eliminated, with the reading that eliminated them.
4. THE CONSTRUCT IS THE PROOF, fixed now. A synthetic test in `scripts/platformkit/` (exercised by
   `tests/platformkit/test_g313_ten_id_cap.py`) feeds the traced component **more than ten
   simultaneous, well-separated, unambiguous detections** and asserts the distinct id count
   saturates at **exactly 10**. **A trace with no reproduction is a hypothesis and will be reported
   as one.** If the construct does NOT saturate, that is reported as a FAILED reproduction and the
   trace is withdrawn -- it is not re-tuned until it passes.
5. NO EDIT TO A HUMAN-GATED PATH. If the fix lies in `src/`, the deliverable is
   `docs/research/organization-sprint/PROPOSED-<name>-2026-09-07.md` carrying the exact change and
   the exact reason, and **the row STOPS there for a human to apply.** Only a fix demonstrably
   confined to `scripts/platformkit/` may be made directly.
6. SCOPE, fixed now. **Naming the cause is NOT fixing it, and lifting the ceiling is NOT improving
   tracking**: raising the id count can lower identity quality. No recall, precision,
   identity-accuracy or registration claim is made or may be read in. Image space only
   (`coordinate_space = image_px`). All 13 adjudicated census games are `passed = false`.
7. GENERALISATION LIMIT, fixed now. A construct that saturates at 10 proves the mechanism EXISTS;
   it does NOT prove that mechanism produced the 10 in every pod game. The memo states which games
   were traced end to end and which were only counted.
8. THE POD IS READ-ONLY. Nothing under `data/tracking/` is written, deleted or renamed; the running
   `track_daemon` is never stopped or signalled; fetched CSVs go to a scratchpad, never into
   `data/` in the repo. No GPU, no GPU lease. No video is decoded and no model weight is run on the
   pod for this row.
9. NO PRODUCTION CHANGE. No threshold, no production default, no register edit, no adoption, no
   `--force`.
10. TEST: `tests/platformkit/test_g313_ten_id_cap.py`, the construct of point 4. n = 1 (CONSTRUCT).
    That ONE file is run. Never a full pytest.

## SEAL
SHA-256 of the LF-normalized bytes above this line: 5cb2a5da7a37186b967dec37807cc931a92e456b2cc5e5e141aac51cc6f121da
