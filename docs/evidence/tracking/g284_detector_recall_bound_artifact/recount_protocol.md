# G284 blind recount protocol

After Pass 1 was committed in `c5c526691`, a fresh order of 24 of the 54 `COUNTED` frames was drawn without replacement with .NET `System.Random` seed `2840904`. The recount uses the same raw JPEGs only and the same visible-on-court rule as Pass 1. No detection record, overlay, marker, or Pass 1 count is displayed during the visual recount. Agreement is calculated after the recount values are recorded.

