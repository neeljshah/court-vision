VERDICT: CLOSED AT LIMIT -- the sealed rule qualifies no arm on >= 4 of 6 sections (640/960/1280/1920 = 0/0/0/0 of 6) and 640 is not displaced: two sealed sections are frozen video with 0 person rows at every arm, so at most 4 sections can ever qualify, and on the 4 live sections 640 leads the players band share everywhere but never reaches 0.95x the best fallback-homography height share. This is a limit, not an open question: the sealed n >= 60 per section and two-distinct-game 720p requirement were unmeetable on the rotating corpus (n 59/58/60/60/58/59; source mix 5x1080p + 1x720p; the two frozen sections are filed as G346), so the preregistered decision cannot be reached -- this row reports, it does not decide.

# G338 detector input decision (measured on the pod, 2026-09-08)

Spec `docs/evidence/tracking/specs/G338_spec.md` VERSION 2026-09-08 and 2026-09-08b; contract `docs/evidence/tracking/VERIFIER_CONTRACT.md` A and B; prereg `docs/evidence/tracking/g338_prereg_2026-09-08.md`, seal c1e93fb53c7957f9f74bce9867f55685080d3f568f05f67ddbca6408c9c87e6c re-verified by the runner at the head of the run and left byte-frozen. Players/frame uses the G325 rule only (the unpadded rectangle intersects the decoded frame); referees and bench remain, so the 8-13 band share is a screening number.

PREMISE (binding before-condition, rerun on sealed section 000001 over its own sealed frames): the 1920 arm emits 2536 in-frame person rows against the 640 arm's 635 over n=59 frames, ratio 3.99 -- at or above 1.5, so the premise HOLDS and nothing is falsified. Ratios on the other three live sections: 3.03 (000003, n=60), 3.08 (000004, n=60), 1.99 (000006, n=59).

SECTIONS (6, sealed in `snapshot.json`; salt G338-2026-09-08b; stride F//60, offset from the salted basename hash; target n=60; per-source path, bytes, dims and sha256 in that manifest). "live" = mean absolute inter-frame delta over 6 sealed indices. (a) = in-frame person boxes/frame, median / share of frames in the 8-13 band, per arm.
  id      source                               WxH        n   live   (a) 640    960        1280       1920
  000001  nba__hOtf1-DI88w_s4044               1920x1080  59  45.87  12/0.339   28/0.051   44/0.051   52/0.068
  000002  basketball__eurocup-qrWmO434a0k_s90  1920x1080  58   0.18   0/0.000    0/0.000    0/0.000    0/0.000
  000003  nba__0022500081_s2436                1280x720   60  48.18   9/0.367   13/0.200   15/0.100   17/0.100
  000004  ncaa_basketball__N-eE7ZwTuno_s3990   1920x1080  60  68.45  14/0.233   23/0.083   32/0.083   49/0.050
  000005  nba__XCaGht2GmPg_s6720               1920x1080  58   2.65   0/0.000    0/0.000    0/0.000    0/0.000
  000006  basketball__ncaaw-2u7_bVcenTg_s5074  1920x1080  59  66.40  10/0.339   12/0.305   13/0.220   16/0.203
000002 and 000005 are FROZEN video (1208 and 1150 bytes/frame against 8237-18057 on the live sections): 0 in-frame person rows at every arm, so no plausibility share is defined on them. n is below 60 wherever a sealed index would not decode (6 such indices in all, listed in `premise.json`).

(b)-(e) over the 4 live sections; every arm x section cell with its own n is in `arms.csv` (24 rows).
  arm   boxes  (b) height 5.5-7.5 ft  (c) wholly in frame / in court  (d) persist   (e) ball/frame  ms/frame  MiB
  640   2543   0.0062-0.0713          1.000 / 0.0046-0.0065           0.031-0.396   0.00-0.12       56.9      068
  960   4227   0.0059-0.0842          1.000 / 0.0024-0.0086           0.020-0.318   0.00-0.31       40.4      097
  1280  5651   0.0070-0.0734          1.000 / 0.0020-0.0075           0.030-0.281   0.00-0.64       48.1      187
  1920  7681   0.0078-0.0699          1.000 / 0.0016-0.0108           0.032-0.223   0.00-0.15       52.2      358
ms/frame is the mean over all 354 frames of that arm (medians 20.5 / 19.0 / 21.9 / 16.9; excluding the warmup section 000001, means 37.6 / 32.9 / 39.7 / 33.9). MiB is peak torch-allocator memory per arm.

DECISION RULE APPLIED AS PREREGISTERED -- the smallest imgsz within 5 pct of the best arm on all five plausibility measures on >= 4 of 6 sections, and at least 0.8x of the 640 arm's clips/hour. Qualified sections: 640 = 0, 960 = 0, 1280 = 0, 1920 = 0, so no arm is eligible and `decision.json` carries recommended_imgsz null.
The two frozen sections define no share for any measure, capping every arm at 4 of 6 before the comparison begins. On the 4 live sections 640 holds the best band share on every one (0.339 / 0.367 / 0.233 / 0.339) but sits below 0.95x the best height share on every one (0.052 vs 0.069, 0.053 vs 0.073, 0.071 vs 0.084, 0.0062 vs 0.0078), and every larger arm is far below the best band share.
`decision.json` also carries the failure_mode for the incomplete resolution matrix: the corpus holds 4 sections at 1280x720 but all from one game, so the sealed two-distinct-game 720p requirement cannot be built, and the runner recorded that rather than substituting a resolution. The throughput leg was not the binding constraint and is not relied on: on a card shared with the 8-worker daemon the 640-to-1920 ms ratio is 1.07 on 000001 and 0.63 on 000003.

SCREENING DIRECTION (reported, not decided): nothing measured here supports moving the default off 640. The extra rows a larger input emits are not plausible players -- median in-frame person boxes per frame rises from 9-14 at 640 to 16-52 at 1920 against 10 players on court. Per spec step 4 a hook is added only when the decision differs from 640, so NO hook was added and no flag, default or register moved.

DEVIATIONS (the prereg stays frozen and unedited). Transport: the prereg names `pod_run a20 --ship/--fetch`; the finisher instead shipped each module by scp into `/workspace/wt/a20/g338/` beside read-only symlinks to the deployed `src`, `resources` and `data`, and fetched by scp, because one blocking pod_run call exceeds this session's per-call limit. No sampling, arm or metric changed.
Harness fixes, all in `g338_detector_input_arms.py` and none in `src/`: a sealed index that will not decode is dropped for every arm alike and listed in `premise.json` (container metadata over-reports the frame count) instead of aborting the run; the detector call is bracketed by CUDA synchronize so ms/frame is not launch time; `environment.json` also carries the G62 `env_sidecar` capture. The corpus rotated during the row (61 to 56 mp4 files), so an earlier aborted pass drew a different section set; the run reported here is the one sealed in `snapshot.json`.

ENVIRONMENT: pod, Linux-6.8.0-110-generic-x86_64-with-glibc2.39, Python 3.12.3, torch 2.8.0+cu128 (CUDA 12.8), ultralytics 8.4.143, cv2 4.14.0, numpy 2.1.2, scipy 1.18.1; detector = the deployed route's yolov8n weight through `AdvancedFeetDetector`, classes [0, 32], conf 0.22 (the route's fill threshold), half precision, device 0; route file hashes in `environment.json`.

## NOT VERIFIED
- The deployed pod `src` is the pre-G333 producer (G333's src changes are not deployed). The detector call this row exercises does not read them, but that was not tested.
- Plausibility is not accuracy. No ground truth was used and no box was checked by eye. Six sections are a screening and the throughput figure is an estimate.
- (b) height and (c) court inherit the fallback homography (G330). Their levels -- at most 0.084 of boxes in the 5.5-7.5 ft band, at most 0.011 inside the court polygon -- say the proxy itself is broken, so the 5 pct test on those two measures is not a player-quality judgement even though it is what excluded 640.
- ms/frame was taken on a card shared with the 8-worker daemon; it is contention-dominated and not monotonic in image size, so it cannot rank the arms.
- No arm x section cell covers a second distinct 1280x720 game, because the corpus has none; the two frozen sections were not traced to an ingest cause and no other corpus section was checked for the same defect.
- The 640 2x2 tile arm was not requested and was not run. No eye-check frame was exported: there is no recommended size to compare 640 against.
- No loss, calibration or evaluator claim is made or implied; the spec defines none for this row.

WALL TIME: reported run 16:58:28Z to 17:16:20Z (17 min 52 s: 27 s snapshot, 17 min 18 s for the 1416-record matrix); whole finisher including the aborted first pass and the post-pass, 16:34:46Z to 17:18:25Z.

SHA-256 of the fetched artifacts, LF-normalized so each equals its committed git blob (the two CSV files leave the pod with CRLF line endings and are stored LF; every other artifact arrived LF and is byte-identical to its pod copy): arms.csv c3fed1048927ab0f6807289ac5f5687575251371b5207bcc1e6e9383e7575e90; frames.csv d610986c1186ac9eed08a8fb1e4005df902002d4d20628a395c16fe45f3d252f; snapshot.json f10876e68afd4b58fdc5bb6f01a9d8981dfe7de617c0da5090cf659ec3efb239; premise.json 5b3f23f6064f6d789e25027c4c6bca88f3f6b8abbd0de7be713a129ce2516591; decision.json a0055429dcf4f78c123dfd87ed981ec45744f28ecbe7d18fcc8e9f5c596e321b; environment.json ff203255fe713cc3728c311914d0ea451a78d4c54d18d8ec6d8b443cec72a104; pod-only raw.jsonl (1.4 MB, not committed) 1eac443b8c7a3b38abb119ef5999d8adece546af1f732e9c8463e02acf5aa16b.
SHA-256 LF-normalized: prereg c6bc9bb05f027858a869a1245f2d9ebc35be52e4f2a0425080512f0b9c7b8d1d; arm runner e13241eb6fc38afaa75518ce8df5fed709a349f986cd622a1bab6c9b9aa0f392; scorer 99a4b412a7ab70ed4d021f706495a71bf4aafce57a22ede82409a7527c9c39a1; test 7f8d5c9f0308a51ff4a859d7ece20a70014dc4c0b5b345af07988ae01b4b9725.

## Corrections (fix 1b, 2026-09-08)
Verifier codex-sol REJECTed the 2026-09-08 row (contract A/B/Q); this pass makes the demanded in-place corrections only, not a rerun. (1) VERDICT relabeled UNDECIDED -> CLOSED AT LIMIT above and in `RESULTS_LEDGER.md`'s G338 row: the sealed n >= 60 and two-distinct-game 720p bars were unmeetable on the rotating corpus, so this is a reporting limit, not an open decision. (2) B2: `_frame` in `g338_detector_input_arms.py` again raises on an undecodable sealed index, as it did before this row's candidate changed it to return `None`; the caller (`run_section`) now wraps the call in try/except and records the index into `premise.json`'s `undecodable_sealed_indices`, so identical frame sets across arms are preserved without changing the helper's contract. (3) B9, stated as a limit and not fixed here: wholly_in_frame_share is exactly 1.0 in all 16 nonempty arm-section cells because `AdvancedFeetDetector` boxes are already clipped to the decoded frame, so measure (c-frame) carries no information in this construct; any attempt 2 must seal a replacement in-frame measure before rerunning -- that reseal and rerun are out of scope for this pass. (4) Two verifier-named gaps, left uncorrected here: `frames.csv` rounds timing to 0.001 ms, so 24 exact ms_per_frame aggregates differ from `arms.csv` by up to 0.000095 ms; and the committed bundle omits raw boxes and homographies, so the (b)-(e) table's height, court, and persistence numerators are not recomputable from it alone.

Orchestrator adjudication (2026-09-08, Fable): the fix-1b verifier REJECTED on B9 (the constant in-frame measure stays a sealed decision input; removing it needs a new seal and rerun) and on the unmeetable sealed n / 720p requirements; the row is landed as CLOSED AT LIMIT with that REJECT recorded verbatim; attempt 2 is deferred until G346 lands a frozen-video gate and the corpus holds >= 2 distinct 720p games. The 640 direction is a screening, not a decision.

Vocabulary follows contract Q6; automated scan required.
