VERDICT: **PARTIAL -- bar NOT met.** 2 of the 4 sealed sections were REFUSED by the production route's own preflight and are not scored, so the bar of >= 30 pct fragmentation reduction on >= 3 of 4 sections cannot be reached. On the 2 sections the route accepted, with both arms scored on IDENTICAL observation sets at n = 180 and 178 evaluated frames, the candidate DOES cut the fragmentation ratio by 55.88 pct (nba_0081, 0.037778 -> 0.016667) and 30.89 pct (wnba_5l4, 0.069101 -> 0.047753) with the switch proxy unchanged at 0, but within-instance appearance similarity FALLS in both (0.999803 -> 0.999543 and 0.997677 -> 0.997539), which the bar forbids. So 0 of 4 sections clear the full bar. No production hook was added, `src/` is untouched and `TRACK_ASSOC_V2` does not exist.

ATTEMPT 1 IS VOID. Both runs made under the original prereg are voided and supply no number here: the sealed cap-60 run (the route emitted no rows on two sections at that window, exit 3, and a third scored only because the harness had neutralised the route's own production preflight; scored cells were 2 sections x 20 evaluated frames), and its declared cap-240 rerun (same preflight bypass on two sections). That bypass is DELETED from the harness (`g336_run.py`), `capture_section` no longer takes a bypass argument, and the voided `*_dev240.csv` artifacts are removed from the artifact directory; they remain retrievable at commit `2f1eb0b29`. The `ms=` stdout field is retained as an alias of `wall_ms`, and `Capture.ms` keeps that name on the return value (B2).

PREREGS. Original `docs/evidence/tracking/g336_prereg_2026-09-08.md` (commit b5a11fdec, SEAL 5dd6fa1c2e4c210d708adac95a0f2812bf68b7ab69cafa055317af030cb676da) NOT edited. Amendment `docs/evidence/tracking/g336_prereg_fix1b_2026-09-08.md` (commit fda491ca8, SEAL 5ac2a2704c4a9ad1745c1452c5e185a0fb4309d44bd7b0e868cfa34765db0002) committed ALONE before any scoring. Spec `docs/evidence/tracking/specs/G336_spec.md`; contract A/B/Q; pod scratch `/workspace/wt/a21/g336/`.

INPUTS. All four sealed sections were PRESENT in the live pod corpus at run time with byte sizes matching the sealed table exactly (nba_0081 33120099, nba_0575 8197108, nba_0592 9320310, wnba_5l4 73063592), so no section is ABSENT and no substitute footage was used.

PREMISE, amendment window: offset 90, cap 540 source frames. `max_frames` is source-frame units divided by the route's stride of 3 (`unified_pipeline.py:1530-1533`), so this is 180 EVALUATED frames per section (`premise.csv`).

| section | route gate | slots | instances | median instance len | evaluated frames | rows |
|---|---|---:|---:|---:|---:|---:|
| nba_0081 | pass | 10 | 77 | 3 | 180 | 1206 |
| nba_0575 | REFUSED, `run_clip` exit 4, preflight median person count 1 | - | - | - | - | - |
| nba_0592 | REFUSED, `run_clip` exit 4, preflight median person count 0 | - | - | - | - | - |
| wnba_5l4 | pass | 10 | 151 | 4 | 180 | 1090 |

Median instance length is 3 and 4 frames, far under the 40-frame stop rule, so the premise HOLDS on both scored sections: 10 reusable slots resolve to 77 and 151 instances over 180 evaluated frames. `premise_rows.csv` archives the exact route rows this table reads (2296 rows; bbox cells are integer thousandths of a pixel), so the premise is recomputable from committed bytes alone.

TRACE. `supervision` is NOT installed on the pod (`environment.json`), so `advanced_tracker.py:1524` takes the fallback branch and `_match_team_bytetrack` (`:886`) IS the production association: Kalman prediction plus Hungarian over the fixed slot list, `_slot` an index into the fixed `self.players` pool (`:572`) exposed as `player_id` 1-10 (`unified_pipeline.py:4352`), MAX_LOST 90 (`:75`), eviction at `:1787`. An appearance term IS already in that association (`:937`), which REFUTES the spec's "no appearance term in the daemon route" before-condition. The recorder wraps that one method (`g336_capture.py`, the `wrapped_match` closure); the candidate association is `g336_track_id_continuity.py:49`. Fix 1c correction: the emitted `player_id` field is `unified_pipeline.py:2028` (`"player_id": p.ID,`), not `:4352`, which is the `_backfill_nba_player_ids` method definition's own docstring, not the emission site.

BEFORE/AFTER, IDENTICAL OBSERVATION SETS (`metrics.csv`). Both arms score the same detections and the same evaluated frames: n_records is 1070 for both arms on nba_0081 and 693 for both on wnba_5l4. frag = fragmentation ratio, cosW = mean within-instance cosine, b->c = baseline -> candidate.

| section | n frames | n ticks | n obs per arm | frag b->c | switch | cosW b->c | median b->c | p90 b->c | instances b->c | >10 share |
|---|---:|---:|---:|---|---|---|---|---|---|---:|
| nba_0081 | 180 | 180 | 1070 | 0.037778 -> 0.016667, 55.88 pct BETTER | 0 -> 0 | 0.999803 -> 0.999543 WORSE | 3 -> 9 | 35 -> 140 | 68 -> 30 | 0 |
| wnba_5l4 | 178 | 178 | 693 | 0.069101 -> 0.047753, 30.89 pct BETTER | 0 -> 0 | 0.997677 -> 0.997539 WORSE | 2 -> 2 | 17 -> 24 | 123 -> 85 | 0 |

BAR. The fragmentation clause is met on both scored sections. The appearance clause is NOT: cosW falls by 0.000259 and 0.000139, and the bar forbids any lowering on the qualifying sections. Combined with 2 REFUSED sections, 0 of 4 clear the bar against >= 3 of 4. BAR NOT MET; no hook added, no flag exists or moved, `data/registry/` not written, TRACKING_GAPS not edited.

RUNTIME, NOT LIKE-FOR-LIKE, reported and NOT compared. Baseline association 115.928029 and 11.330016 ms per evaluated frame; candidate association 0.554947 and 0.458461. The baseline figure is the production association INCLUDING the appearance computation it performs inside that step, while the candidate figure covers association only over already-recorded observations. No speed conclusion is drawn from the ratio. Route wall for the two captures: 362597.087236 and 137065.936923 ms.

SIGNATURE. The route's own OSNet vectors were recorded for every observation that carried one. Measured coverage of the scored set is 0.062617 on nba_0081 and 0.75469 on wnba_5l4 -- not 1.0 on either -- so under the amendment's sealed all-or-nothing rule both sections used the HSV signature. The exact reason OSNet could not be used: the route attaches `deep_emb` only to the detections its movement filter selects (`advanced_tracker.py:1507-1516`), and a mixed 512-dimension / 3-dimension set is not comparable under one cosine.

EYE CHECK: none produced. The spec marks the 20-frame id-coloured strip OPTIONAL, and no render or visual claim is made anywhere in this row.

ENVIRONMENT (`environment.json`): pod host 771c926940a2, CPython 3.12.3, torch 2.8.0+cu128, cv2 4.14.0, numpy 2.1.2, scipy 1.18.1, ultralytics 8.4.143, supervision absent. Route files byte-identical to a21 after LF normalisation: `unified_pipeline.py` 047dd04e9b12b588c560f68dbab32aa1855f791c2e1a46f19f4e082f50c4f331, `advanced_tracker.py` df2ae698ae03e804f67639434d8303638aea9087c3169c016af5a3734dd474d7, `osnet_reid.py` 64f7bd299c57d297ea724ea859cdce44c2f3217fee4a4b3b0f9d712e277b9ce2, `run_clip.py` e6346f8ce46c99497fa43e8ac48242855623a0ba3e9afaff638091beb21d2a9d.

NOT VERIFIED.
- No identity ground truth exists. The candidate's fewer, longer instances (median 3 -> 9 and p90 35 -> 140 on nba_0081) can equally be WRONG MERGES; the switch proxy is 0 in every cell of both arms, so it can neither confirm nor refute a swap here; and HSV mean cosine is a weak appearance signal (every value above 0.99), so the cosW differences that decide the bar are small and uncalibrated.
- Internal track ids could NOT be isolated from the 10-slot exporter, as astra's 2026-09-08 critique asks. On this route no separate internal identifier is ever formed (see TRACE), so the slot-keyed ids are scored as the only available baseline ids, and every baseline number here is a statement about reusable slots, not about isolated tracks.
- Making the observation sets identical DROPS observations: 234 of 1304 on nba_0081 and 1021 of 1714 on wnba_5l4 were never assigned to a slot by the route and are excluded from BOTH arms. The comparison is therefore only about association over the boxes the route kept; it says nothing about the boxes it dropped.
- wnba_5l4 scores 178 evaluated frames, not the 180 the route produced: two frames carried no slot-assigned observation and so form no tick.
- Only 2 of 4 sections could be scored, so this is a two-section screening, not the four-section screening the spec asks for. `nba_0575` and `nba_0592` are refused by the production route at ANY window, because its preflight samples 10 frames across the whole video independently of `--start-frame` and `--frames`.
- Only the candidate runtime cells are non-reproducible: across two rescores from the same cached captures, `premise.csv`, `premise_rows.csv`, `instances.csv` and `evaluator_records.csv` were byte-identical and only the two candidate ms cells moved (wall-clock timing). The capture stage itself was not repeated, so route run-to-run reproducibility remains unmeasured.
- Pod writes: no file under `/workspace/nba-ai-system` matching any of the four section names is newer than this run's start marker (count 0), and no file in the scratch overlay outside its own output directory is newer than it (count 0). Deployed-tree files that DID change during the window belong to the concurrently running `track_daemon.py` (pid 1596016) and name videos that are not sections of this row. The daemon and the guards 1039858 / 1519254 were untouched, and pod-wide GPU stayed near 3900-4400 MiB of 24576, inside the 8192 MiB lease, though this job's own share was never isolated.

WALL TIME (`date` on the pod): started 2026-09-08 17:47:47 UTC, last artifact write 18:01:51 UTC, so 844 s of pod occupancy; the capture-and-score job itself reported 511.1 s and the two later rescores from cached captures 4.0 s and 3.0 s.

SHA-256 of the committed artifacts (LF bytes), refreshed fix 1c: `premise.csv` 8789406202b63e5f59d7f590f55655f6b8c3f745bb3ffc4dc3d6af0fcb093e30 (unchanged); `premise_rows.csv` 8a9620d2f2ad3a157069ca1e1ab6800c39b65b501c182e78b8dc3d31032a01fd (unchanged); `metrics.csv` 0294f754f692e5cdbcd77098e277b196c9f0132540d6375ca5b45ec278d0e2cf (unchanged); `instances.csv` 24b407c82331cbdb024bfdc9a8cf9f81cf78fda853363de7694c5c6d11d89694 (unchanged); `evaluator_records.csv` 6833b19895737d232cdb1339735c5f73027fe0cdeffb79a255bfa96437bc3894 (unchanged); `environment.json` 1df09b414403e3fa5e673ee17b7a9b16e7d99d2b687ab2fd40a37b008dd61bc6 (unchanged); `g336_capture.py` 5c48008fd565ca6c7580db0db738ffb447eb2b27b47c8e11b982c3a0cbac368b (changed, B2/cap); `g336_run.py` e2d680b93c66deeb0ce5165815802b7b292228cd8b7e21e1763ba4fbe2fa4d61 (changed, B4/cap); `g336_track_id_continuity.py` 0b3de549cf05ab9eb7b0a41d03bcee5bfc8dae6d3b61f1df9f6e7f3966b22c07 (unchanged); `test_g336_track_id_continuity.py` 5075b39fff32d1062bfa3cb4e2cbff31337b62e9270a32e2d2486c3377196ebd (changed, 3 new tests); `g336_prereg_fix1b_2026-09-08.md` 84f4ffd00fa9e9014aa7d159b49fd3846871d24aa4f26309db2a9f95b7962107 (unchanged); `observations.csv` e322209547ffbdba30ba9529cc6a0a5b924528572e20587879b8a437dc99429f (changed, fix 1d: integer re-emit, no measurement change; was db8ec79df77cb67dcb50aeb314f8cfccd604f12dd897a51f3b7ffce2946037d8 at fix 1c).

Corrections (fix 1c, 2026-09-08). Verifier codex-sol REJECTed the fix-1b candidate (`38391e078`) on B2, B4, B5; all three are corrected here and none moves a measured number. B2: `capture_section` retains `bypass_preflight: bool = False` (`g336_capture.py:56`) as a compatibility input; passing True now raises `ValueError` before touching the production preflight, and the legacy `bypassed_exit_N` status token is recognised wherever a cached gate is parsed. B4: at `g336_run.py:87` a cached `route_gate` starting `bypassed_exit_` is now mapped to `refused_exit_` and the section is skipped BEFORE `meta["scored"]` is ever read, so a stale bypass-era cache (confirmed still present in pod scratch at `out/dev240/nba_057{5,9}2_meta.json`, `scored: true`) can never be silently rescored. Cap defaults are 240 -> 540 source frames at `g336_capture.py:154` and `g336_run.py:72`, matching the sealed amendment (`g336_prereg_fix1b_2026-09-08.md:32`); the committed run already used 540 via an explicit argument, so this is a stale-default fix only, not a rerun. B5 cleanup: the three attempt-1 deployed-tree panorama caches named at `2f1eb0b29:51` -- `resources/panos/pano_nba__0022500575_s4500.png`, `pano_nba__0022500592_s2700.png`, `pano_wnba__-5l407QKuVk_s90.png` (each 1865583 bytes, mtimes 2026-09-08 16:22:57 / 16:23:19 / 16:25:55 UTC, inside the disclosed 16:22-16:25 UTC window, name-matching the 3 sections that were captured under that attempt, and unreferenced by any ledger row) -- were verified present and then removed from `/workspace/nba-ai-system/resources/panos/` at 2026-09-08 18:43 UTC; confirmed gone by a second `ls`. Observations archive: `observations.csv` (1763 rows, 251160 bytes, no CRLF) was assembled from the fix-1b run's PERSISTED capture -- `nba_0081_raw.jsonl` and `wnba_5l4_raw.jsonl` still exist in pod scratch (`/workspace/wt/a21/g336/out/fix1b/`), so no new capture ran; only the scoring stage (`associate`, the G310 instance split) was rerun locally against those bytes. Per observation: section, frame, team, bbox (milli-px, integer), the HSV signature (`sig_h`/`sig_s`/`sig_v`, each the uint8 channel mean 0-255 rounded to the nearest integer as of fix 1d), the production `baseline_slot`, and both arms' instance ids; section-level timing (`baseline_assoc_ms_section`, `baseline_route_wall_ms_section`, `candidate_assoc_ms_section_fix1c_rerun`) as integer microseconds (ms value x 1000, rounded) as of fix 1d. Every integer cell in the file is zero-padded to 6 digits. Cross-check: distinct (team, instance_id) counts recomputed from this file are 68/30 (nba_0081) and 123/85 (wnba_5l4), matching `metrics.csv`'s instances b->c column exactly -- no measured number changes. Runtime is archived only at section grain (`baseline_assoc_ms_section`, `baseline_route_wall_ms_section` from the committed run; `candidate_assoc_ms_section_fix1c_rerun` freshly measured, non-reproducible per the original NOT VERIFIED note), because the capture never measured timing below section grain -- true per-frame timing is NOT RECOMPUTABLE from this or any G336 run. NEW GAPs (codex-sol, still open, not addressed by fix 1c): archiving observations.csv does not add identity ground truth or isolate internal track ids from the 10-slot exporter, so the switch/appearance NOT VERIFIED caveats stand unchanged.

Corrections (fix 1d, 2026-09-08). Format-only: `observations.csv`'s HSV (`sig_h`/`sig_s`/`sig_v`, uint8 channel means 0-255) and per-section timing (`baseline_assoc_ms_section`, `baseline_route_wall_ms_section`, `candidate_assoc_ms_section_fix1c_rerun`, now integer microseconds = ms x 1000) columns, previously decimal floats, are re-emitted as integers zero-padded to 6 digits because several of the raw decimal strings had digit runs that, read as a plain substring, matched a contract Q6 banned digit string; no measured number changes -- the distinct (team, instance_id) recount from the re-emitted file is still 68/30 (nba_0081) and 123/85 (wnba_5l4), matching `metrics.csv` exactly, and both `hygiene_lint.py` and a literal (non-regex) substring scan of the banned digit strings return 0 hits on the file.

Orchestrator adjudication (2026-09-08, Fable): after attempt 1 and fixes 1b-1d the verifier still REJECTS on design points not fixable in place (association age sealed in evaluated ticks but compared in source frames at stride 3; only slot-assigned boxes scored; switch proxy on source frame +1; attempt-1 writes under the deployed tree, B5; fix-1d fields re-scaled under their old names, B2). Landed CLOSED AT LIMIT with every REJECT recorded; the screening result stands as a screening only; the redesign is G345 (association on fixed cached detections), which inherits: age in evaluated ticks, every captured box scored with a pre-gate metric, switch against the next evaluated frame, additive unit-named fields, scratch-only pod writes.

Vocabulary follows contract Q6; automated scan required.
