# G344 preregistration - ball shadow possession (2026-09-08)

Scope: preparation only in `C:/Users/neelj/nba-track-a16`, LOCAL conda `basketball_ai`; no pod, video, GPU, `src/`, `data/`, registry, consumer, or flag change.

Binding inputs: `data/tracking/<game_id>/tracking_data.csv` and `ball_tracking.csv`. G343 results are not present here, so the frozen rule is: sort eligible local game ids lexically, select every 15th eligible id until 24 ids are selected, and use frames 600-659 inclusive or the final 60 frames if shorter. The finisher writes the resolved list and input byte size, SHA-256, and source resolution to `states.csv` and the memo.

State: OBSERVED_VALID, INFERRED, ABSENT, or AMBIGUOUS. F is every integer frame in the sealed window, including missing tracking or ball rows. A detected ball uses the nearest player image-pixel bottom-centre within radius `60 * source_height / 720` pixels. It receives ownership only when the last-three-frame ball/player displacement cosine is at least 0.5 and the displacement-magnitude ratio is in [0.5, 2.0]. Two player candidates within 10 pixels yield AMBIGUOUS. Age greater than 30 frames never yields OBSERVED_VALID. A G341 shot-table cut prohibits cross-cut ownership; without its table, cuts are UNKNOWN.

Synthetic construct: exactly 200 enumerated seeded cases, seed 3440908. It includes timestamp offsets, one cut, missing and duplicate ball rows, and a radius pass-through without motion agreement. Exact state and ownership are scored for every case. Prefix check compares first N output rows against an N-frame construct and three real windows. Real control shifts ball rows by -30 and +30 frames; accepted-ownership motion agreement must fall by at least 0.20 (20 percentage points) against unshifted.

Output fields: frame, state, source, age_frames, confidence, owner_id, abstention_reason, nearest_distance_px, motion_cosine, motion_ratio. Ball pixels and player box points are image coordinates, never floor positions. The nearest player is a proxy-supported ownership hypothesis, not true possession.

Sign convention: improvement = baseline loss minus candidate loss; positive = candidate better. This row does not score a loss or make a calibration comparison.

SEAL sha256 98ce3227549060791ef6e792d60a65ce4cea54ac670ef9f58d3b834a9fabf75d
