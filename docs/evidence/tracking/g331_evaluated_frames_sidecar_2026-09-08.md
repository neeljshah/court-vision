DONE -- 17 of 17 committed G310 sidecar records carry `evaluated_frames` null with the route's own reason, so every rows-per-frame number in the tracking evidence divides by EMITTED frames; on the 7 archived runs that emitted more than one row that denominator is 13 to 42 frames short of the 500 frames the run actually processed, inflating rows-per-frame by factors 1.026694 to 1.091703 (n = 10 committed attempt-2 records, 7 committed attempt-1 pilot records, 10 archived runs of which 9 carry rows).

# G331 -- evaluated-frame denominator sidecar (TOOLING ROW)
Spec `docs/evidence/tracking/specs/G331_spec.md` VERSION 2026-09-08. Prereg `docs/evidence/tracking/g331_prereg_2026-09-08.md`, seal `97d3b5d0564259eb402598a8174f1cb3bf092bac4388baae5c0da1c8f5156d42` over the LF-normalised bytes above its seal line, committed ALONE at `4c3c8eff2` BEFORE any measurement below was written into evidence. NO GPU, NO route arm, NO video decode: every number is recomputed from bytes already committed to this repository. The pod was not contacted at all, so `track_daemon` and `vol_guard.py` were never stopped, signalled or restarted. Eye check: NONE -- there are no labels here, only arithmetic and code reading.

PREMISE (step 0), exhaustive, no head slice. `g310_attempt2/g310_attempt2_proxies.json` key `runs`: 10 records, 10 null, 0 non-null, ONE distinct reason string, `max_frames_is_detector_dependent_in_this_route`. Attempt-1 archive `g310_pilot_pre_prereg/pilot_summary.json` key `runs`, sub-key `proxies`: 7 records, 7 null, 0 non-null. Its sibling `pilot_proxies.csv` column `denominator_evaluated_frames`: 7 records, 7 empty, 0 non-empty. TOTAL 17 of 17 NULL, so the premise is FULLY TRUE and nothing was excluded to keep it true. The attempt-1 records carry no reason field of their own; that archive's README states the same reason string verbatim.

PRODUCER TRACE (every line below was read from the file, not guessed). `scripts/run_clip.py:473-474` writes `evaluated_frame_count.json` BEFORE tracking starts, fed only by `args.video`, `args.frames` and `args.start_frame`. `scripts/run_clip.py:191-193` is the null branch: when `max_frames` is not None the builder returns immediately with `evaluated_frames` still None and `reason` set to `max_frames_is_detector_dependent_in_this_route`, so the whole-file computation at `:207-214`, and the ffprobe plus cv2 cross-validation it rests on at `:107-156`, are never reached. WHY the cap makes the count unknowable: `src/pipeline/unified_pipeline.py:1532-1533` rescales the cap into decoded units, `:1676` breaks the loop on `gameplay_frames >= self.max_frames`, and `:2069` increments `gameplay_frames` only AFTER the detector-gated `_is_gameplay` test at `:1696` (defined at `:992`, a YOLO person-count gate with two skip caches). The number of frames handed to the detector before the cap trips is therefore A FUNCTION OF THE DETECTOR'S OWN OUTPUT and cannot be known before the run. Stride is decided at `:1528-1529` and mirrored read-only by `run_clip.py:159-163`; the source-read cap is `:1555`. `run()` returns `total_frames = frame_idx` at `:3049`, which is the last ABSOLUTE source index, not a count of anything.

DENOMINATOR CONTRACT.

| name | meaning | where it is knowable | who uses it |
|---|---|---|---|
| SOURCE | frames the container declares for the whole file | ffprobe `nb_frames`, else round(duration x rate) | context only; never a rows-per-frame denominator |
| DECODED | frames the prefetcher read for THIS run | only inside `_FramePrefetcher` (no counter exists); the route computes a whole-file value only when the cap is null | route key `decoded_frames`, null here |
| EVALUATED | frames handed to the detector after stride and cap | only inside `unified_pipeline.py`, and only AFTER the run (`gameplay_frames`, `:2069`) | route key `evaluated_frames`, null here |
| EMITTED | frames carrying at least one person row | the tracking table | EVERY rows-per-frame number in the G310 evidence today |
| PROCESSED_RECON | distinct ball-table frames; a RECONSTRUCTION of EVALUATED past the gameplay gate | the ball table, after the fact | this row only |
| DELIVERED_IN_SPAN_RECON | strided positions between the first and last frame written; a RECONSTRUCTION of DECODED over the covered span | both tables plus the stride | this row only |

RECOMPUTED ROWS-PER-FRAME, all 10 archived runs, no head slice. Companion CSV `g331_evaluated_frames_sidecar_2026-09-08/rows_per_frame.csv` (integer cells zero-padded to six digits). `den` is the denominator count, `rpf` the person rows divided by it. NK = NOT KNOWABLE.

| run | rows | EMITTED den / rpf | PROCESSED_RECON den / rpf | DELIVERED_IN_SPAN_RECON den / rpf | EVALUATED route | DECODED route | SOURCE | inflation |
|---|---|---|---|---|---|---|---|---|
| pilot ncaa IB-_u4gW3ds_1080p armN | 2349 | 487 / 4.823409 | 500 / 4.698000 | 530 / 4.432075 | NK | NK | 18115 | 1.026694 |
| pilot ncaa IB-_u4gW3ds_1080p armP | 1953 | 470 / 4.155319 | 500 / 3.906000 | 500 / 3.906000 | NK | NK | 18115 | 1.063830 |
| pilot ncaa mRkuGgeECak armN | 1 | 1 / 1.000000 | 1 / 1.000000 | NK | NK | NK | 28905 | 1.000000 |
| pilot ncaa mRkuGgeECak armP | 1 | 1 / 1.000000 | 1 / 1.000000 | NK | NK | NK | 28905 | 1.000000 |
| pilot wnba_01_1080p armN | 2994 | 470 / 6.370213 | 500 / 5.988000 | 530 / 5.649057 | NK | NK | 18060 | 1.063830 |
| pilot wnba_01_1080p armP | 2116 | 470 / 4.502128 | 500 / 4.232000 | 530 / 3.992453 | NK | NK | 18060 | 1.063830 |
| pilot wnba_01_1080p armP repeat | 2185 | 470 / 4.648936 | 500 / 4.370000 | 530 / 4.122642 | NK | NK | 18060 | 1.063830 |
| postprereg ncaa IB-_u4gW3ds_1080p armP | 0 | NK | NK | NK | NK | NK | 18115 | NK |
| postprereg wnba_01_1080p armN | 2940 | 469 / 6.268657 | 500 / 5.880000 | 530 / 5.547170 | NK | NK | 18060 | 1.066098 |
| postprereg wnba_01_1080p armP | 1433 | 458 / 3.128821 | 500 / 2.866000 | 530 / 2.703774 | NK | NK | 18060 | 1.091703 |

EVALUATED and DECODED as the ROUTE defines them are NOT KNOWABLE for 10 of 10 runs, exactly as the preregistration expected, and no run contradicted that. INFLATION of the EMITTED denominator against the best available true count (PROCESSED_RECON): the frames the run processed but that emitted no person row are 13/500, 30/500, 30/500, 30/500, 30/500, 31/500 and 42/500 on the seven multi-row runs and 0/1 on the two one-row runs, so rows-per-frame as published is 1.026694 to 1.091703 times the value the processed-frame denominator gives. PROCESSED_RECON equals the committed `ball_rows_total` in 9 of 9 runs with rows and equals the route's cap of 500 in 7 of 7 complete runs, which is the `gameplay_frames` cap of `:1676` after `1500 // 3`. Stride recovered as 3 in 8 of 10 runs and NOT KNOWABLE in the two with a single frame.

READER FIX 1 -- an explicit zero read as absent. `g310_instance_key.py:80` selected the denominator with a truthiness test; it is now `is not None`. THE SAME DEFECT sat at a second call site in the shared reader, `g310_native_input_arm.py:41`, and is fixed with it; the spec named only the first. After the fix an explicit 0 reports source `evaluated_frames` with denominator 0 and a null ratio, instead of silently reporting source `frames_with_rows`. No committed number changes, because no committed record carries a zero -- all 17 are null.

READER FIX 2 -- the percentile label. `g310_native_input_arm.py:33-37` computed `sorted[round(0.95 * (n - 1))]`, a rounded linear index, under a docstring that said nearest-rank. DECIDED IN THE PREREG AND APPLIED: the estimator is KEPT and the LABEL is corrected, because the committed `p95_norm_footpoint_step` and `p95_norm_step_instance` columns were produced by it and changing the formula would change their meaning (B2). The stale test name `test_p95_is_nearest_rank...` was renamed to match; its assertions are unchanged. BEFORE and AFTER on the archived columns, over the same instance-keyed step samples (kept estimator -> true nearest-rank, n = step pairs): 0.1330372226345319 -> 0.13425925925925927 (n = 2014); 0.1235686221885425 -> 0.1240878930944515 (n = 2458); 0.12422599874998831 -> 0.12577006656674866 (n = 1714); 0.10967044410420575 -> 0.10988870810771058 (n = 1299). The two estimators AGREE exactly on the other 3 of 7 samples with steps, and nearest-rank is never lower. The kept estimator reproduces every committed value to at most 2.8e-07 (the thousandth-pixel archive rounding), so the archive is confirmed as produced by it.

HARNESS SIDECAR. `scripts/platformkit/tracking/g331_evaluated_frames_sidecar.py` writes a NEW file, `g331_frame_denominators.json`, beside a route output directory. `evaluated_frames` and `evaluated_frames_reason` are the route's own values VERBATIM and are never a reconstruction; `decoded_frames` and `frame_cap` come from the route sidecar; `stride` comes from the route sidecar when present and otherwise from the modal frame-id gap in the ball table, with `field_sources.stride` saying which; `emitted_frames` and `processed_frames_reconstructed` are distinct frame counts in the tracking and ball tables; `delivered_frames_in_span_reconstructed` is the strided span and is null unless the stride divides every observed gap; `source_frames` is ffprobe `nb_frames`, else duration times rate, with `source_frames_method` naming which. B2: the route's own sidecar is opened read-only and a test asserts its bytes are unchanged; no archived CSV column name or meaning is touched.
 THE ROUTE'S NULL STAYS NULL: the true EVALUATED count exists only inside the human-gated pipeline, so the 4-line change that would publish it is a PROPOSED diff, NOT APPLIED -- `docs/research/organization-sprint/G331_PROPOSED_evaluated_frames.md` (gitignored, local-only), SHA-256 `ba00c0ddf8e6a27e422842ccebe25bdc87c9b05f2a967c72bb4a25f6ddbb3bd3`.

G149. `scripts/platformkit/test_g149_persist_decoded_denominator.py` is RED on master, reproduced here: `TypeError: ...<lambda>() got an unexpected keyword argument 'publish'` at `scripts/platformkit/track_daemon.py:278`, which is a STALE TEST DOUBLE whose fake `verdict` signature drifted from the caller, not a defect in the denominator path. This row's sidecar FEEDS G149 rather than superseding it: G149's consumer `attempted_frame_count_source.py:157-175` reads the ROUTE's `evaluated_frame_count.json` and gates on its `formula` string, which this row does not touch, so the daemon ledger columns keep their current behaviour until the PROPOSED diff is applied.

NOT VERIFIED:
- A denominator recorded by the harness after the fact is a RECONSTRUCTION, not the producer's count. PROCESSED_RECON and DELIVERED_IN_SPAN_RECON are named that way for that reason.
- The route's `evaluated_frames` is still null for every run and stays null until a human applies the PROPOSED diff. Nothing was adopted, no default moved, no flag flipped.
- DELIVERED_IN_SPAN_RECON assumes a constant stride and covers only the span between the first and last frame written; frames the prefetcher read before the first or after the last written frame are NOT counted, so it is a LOWER bound on DECODED.
- PROCESSED_RECON assumes the ball table holds one row per processed frame. It matches `ball_rows_total` and the cap in every run where both exist, but no route counter confirms it from the producer side.
- SOURCE frames come from the frozen attempt-1 ffprobe corpus probe; the three source videos are deleted from the pod and were not re-probed. The 10th archived run emitted zero rows, so 9 of 10 runs carry a recomputed ratio and 7 carry a multi-row one.
- This row measures NO recall, NO precision and NO registration and compares no arm against another; nothing here says whether more rows per frame is better. The archived runs are one draw each on a card shared with a live daemon, so no timing figure here may be quoted as throughput.

WALL TIME: about 1 h from lane pickup to memo, entirely local; 0 GPU seconds; 0 pod commands. ARTIFACT SHA-256 (over LF-normalised bytes): prereg `b9d60851cb436cad548f1f88bf49b64c9ab695c07d648173a712348e219d5a85` (as committed, seal line included) | module `33a0641a7638cfaa444fb65b788d20524aa85a7000a4217bc0343702a1cd7c59` | test `44a4d6ae3ea50c708f49c6662e762a5cb1722da0ead9d816eb8df9a72f4a4eae` | rows_per_frame.csv `722a2d9905ed69828754de91b786e21b41ab87e0eaee1a043792cb4fd1c1e0c5` | `g310_instance_key.py` `4ed89500cff9dd66d05122dd459f013befafac049cb68a5d07cb95b6d03c0f3e` | `g310_native_input_arm.py` `075047427c70afb24bfda4fef60210b2f17ef01fcd35009b03995f1f27b769d4`. Hexadecimal digests are single tokens; a two-digit run inside one is not a standalone figure.

TEST: `python -m pytest tests/platformkit/test_g331_evaluated_frames_sidecar.py -q -p no:cacheprovider` -> 6 passed. `tests/platformkit/test_g310_instance_key.py` -> 10 passed; `scripts/platformkit/tracking/test_g310_native_input_arm.py` -> 5 passed; `tests/platformkit/test_loc_rail_scope.py` -> 1 passed. Never a full pytest. The CSV reproduces byte-identical on a second run of the module.

Vocabulary follows contract Q6; automated scan required.
