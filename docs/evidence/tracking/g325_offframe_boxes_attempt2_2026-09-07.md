VERDICT: PREMISE HOLDS and SITE PINNED -- `23408/480158` player observations (~4.8751e-02) across all 122 completed pod games carry a bounding box lying WHOLLY outside the decoded frame, `113` of the `122` games are at or above the sealed `1/100` per-game bar, and the producing site is pinned at `100` pct: all `23408/23408` of those rows carry the coasting flag and `0/269174` matched (detector) rows are wholly outside, so the producer is the unclamped Kalman prediction at `src/tracking/advanced_tracker.py:132-137`, not the detector -- the sealed `>= 90` pct rule therefore fires and a PROPOSED diff is written, with NOTHING under `src/` changed.
# G325 ATTEMPT 2 -- boxes wholly outside the frame: corpus census + producing site (2026-09-07)
WHY ATTEMPT 2: attempt 1 (`2009236d6`) was REJECTED on Q6 alone -- the sealed attempt-1 prereg's own vocabulary line named the restricted words while declaring them absent, the producer trace carried one more, and the attempt-1 memo wrongly reported a zero scan; the verifier also corrected the per-game median. Contract Q1 forbids resealing after measurement, so the prereg was rewritten and RE-SEALED ALONE before any number of this attempt was computed. Full verifier report: `G325_VERIFY_2026-09-07.md` (commit `6220a1f9c`). The census rule, the thresholds and the site rule are UNCHANGED, and the census harness is byte-identical to attempt 1's (`0b703bf01191b890...`). PREREG, sealed and committed ALONE before the first metric of this attempt: `g325_prereg_attempt2_2026-09-07.md`, seal SHA-256 `57d7748a2cf7c84ebf7e3397357d3feb672a7fa0f2ca28cfdf25e6f8ae1518ca` (LF-normalized bytes above its `## SEAL` line), commit `eceac0b89`. Spec `specs/G325_spec.md` (master `946d73a79`, LF SHA-256 `b47ec5bebaae20d4d228f744e588f604cc153a7debb4600b1a5a553514bf039e`).
INPUTS (A9/A11), all READ-ONLY; nothing on the pod written, signalled or killed; no frame decoded and no image rendered anywhere in this row: ledger snapshot `/workspace/nba-ai-system/data/tracking/track_daemon_ledger.jsonl` -> `track_daemon_ledger_snapshot.jsonl`, 147 lines, 162,592 B, SHA-256 `5ba077e6e0d61c1c3f5990d1a5228a71542270e6e57b82d58b31cd69976e8bbb`; 122 per-game `tracking_data.csv` (186,947,968 B) fetched by one `tar czf - -T -` stream over `ssh`, each file's SHA-256 and byte size in the census CSV and in prereg section 8. The ledger grew by one line BETWEEN the size probe and the snapshot read (146 -> 147), which is the mutation this row declares rather than hides: a multi-worker `track_daemon` ran throughout. No table was absent -- all 122 fetched, the tar stream reported no failed read -- so the volume warden had removed no CSV this row needed. CORRECTION (fix 2b): this snapshot is now committed at `docs/evidence/tracking/g325_artifact/track_daemon_ledger_snapshot.jsonl` (identical bytes, same SHA-256 above, 147 lines) so A7 snapshot eligibility is independently recheckable from this tree; the 122 per-game tables remain scratch-only per S4/A9 (never committed, never written to the pod).

## PREMISE (step 0, binding): HOLDS, and it reproduces G323
The G323 finding this row is built on is recorded in this tree at `TRACKING_GAPS_2026-09-01.md:819` (the G323 allocation row) and `:840` (the G325 allocation row, which restates it): G323 found 2 of 60 sampled player observations whose box lies WHOLLY OUTSIDE the decoded frame, on 2 of its 3 games, passed by the production colour gate. Recomputed here over EVERY row of those same three tables: `wnba_06` `150/3606` (~4.1597e-02, `frame_w` 1920, `frame_h` 1020), `wnba_04` `160/6189` (~2.5852e-02, 1280 / 660), `0022500575_s7200` `12/2620` (~4.5802e-03, 640 / 300). Identical to attempt 1's recount and to the verifier's independent raw recount of it.

## CENSUS -- every completed game, no sampling (122 games, 480,158 observations, 0 unusable rows, 0 games excluded)
Denominator of every share is that game's OWN usable row count in its OWN `tracking_data.csv`. WHOLLY OUTSIDE = `bbox_x2 <= 0` or `bbox_x1 >= frame_w` or `bbox_y2 <= 0` or `bbox_y1 >= frame_h`. S4, the fields named: `frame_w` is the width of the ledger's `source_resolution` string, because `tracking_data.csv` carries **no width column**; `frame_h` is the table's own `source_height` MINUS `TOPCUT = 60` (`src/tracking/video_handler.py:11`; `frame = frame[TOPCUT:]` at `src/pipeline/unified_pipeline.py:916`), the space the boxes are actually written in. The table's `source_height` AGREES with the ledger's in **122 of 122** games.
| source_resolution | games | observations | wholly outside | partially outside |
|---|---|---|---|---|
| 1920x1080 | 7 | 34,428 | `1692/34428` (~4.9146e-02) | `6330/34428` (~1.8386e-01) |
| 1280x720 | 60 | 246,337 | `11881/246337` (~4.8231e-02) | `60752/246337` (~2.4662e-01) |
| 640x360 | 55 | 199,393 | `9835/199393` (~4.9325e-02) | `67916/199393` (~3.4061e-01) |
| ALL | 122 | 480,158 | `23408/480158` (~4.8751e-02) | `134998/480158` (~2.8115e-01) |
Per-game wholly-outside share: min `0/413` (~0.0000e+00), median ~4.2142e-02 (recomputed from the raw per-game shares by the same convention the verifier used on attempt 1, where it reproduces the corrected ~4.1248e-02 exactly), max `615/5044` (~1.2193e-01, `0022500592_s6600`). **`113` of `122` games clear the sealed `1/100` bar.** Exactly TWO games have zero wholly-outside rows and both are tiny: `0022500594_s300` (n = 413) and `mia_bkn_2025` (n = 37).
Which border each row crosses (a row may cross more than one, so these need not sum): left `bbox_x2 <= 0` 8,508; right `bbox_x1 >= frame_w` 10,233; top `bbox_y2 <= 0` 5,321; bottom `bbox_y1 >= frame_h` 4,330.
Diagnostics sealed in prereg section 3: under the NAIVE `frame_h` (no TOPCUT subtracted) the count is `22666/480158` (~4.7205e-02), i.e. the TOPCUT choice is worth 742 rows of 23,408 and cannot explain the finding; with the writer's `PAD = 15` removed per side, the detector's own box is wholly outside in `24952/480158` (~5.1966e-02). Extremes across the corpus: `min(bbox_x2) = -7720.8` and `max(bbox_x1) = 9590.2` on frames at most 1,920 px wide; `min(bbox_y2) = -3207.5` and `max(bbox_y1) = 3407.8` on frames at most 1,080 px tall.

## ATTEMPT 1 vs ATTEMPT 2 (the corpus grew by 12 games; no rule, bar or harness byte changed)
| quantity | attempt 1 (`2009236d6`) | attempt 2 |
|---|---|---|
| games censused / excluded | 110 / 0 | 122 / 0 |
| observations (usable / unusable) | 424,895 / 0 | 480,158 / 0 |
| pooled wholly outside | `20560/424895` (~4.8388e-02) | `23408/480158` (~4.8751e-02) |
| 1920x1080 | `1692/34428` (~4.9146e-02), 7 games | `1692/34428` (~4.9146e-02), 7 games |
| 1280x720 | `9033/191074` (~4.7275e-02), 48 games | `11881/246337` (~4.8231e-02), 60 games |
| 640x360 | `9835/199393` (~4.9325e-02), 55 games | `9835/199393` (~4.9325e-02), 55 games |
| per-game median share | ~4.1248e-02 (the verifier's correction of a misreported ~4.1597e-02) | ~4.2142e-02 |
| games at or above `1/100` | 101 of 110 | 113 of 122 |
| wholly-outside rows coasting | `20560/20560` (~1.0000e+00) | `23408/23408` (~1.0000e+00) |
| matched rows wholly outside | `0/238033` | `0/269174` |
The 1920x1080 and 640x360 cells are identical because all 12 new games are 1280x720; that cell's share moves by about 1e-03 and every verdict is unchanged.

## PRODUCER TRACE -- full file:line route in `g325_producer_trace_2026-09-07.md`, re-read against the current blob
Determination: **(b) an unclamped Kalman prediction drifting past the border**; (a) detector-emitted is REJECTED and (c) a units defect is REJECTED.
The matched/coasting flag EXISTS and is read from the table's own `confidence` column, defined at `src/pipeline/unified_pipeline.py:2025` as `conf = max(0.0, 1.0 - lost_ages[slot] / 15)`; `confidence == 1.0` means `lost_age == 0`, the row carrying the detection box assigned at `src/tracking/advanced_tracker.py:626`, and `confidence < 1.0` means it carries the prediction written over `previous_bb` at `:1160-1165`.
**`23408/23408` (~1.0000e+00) of the wholly-outside rows are COASTING and `0/269174` (~0.0000e+00) of the matched rows are wholly outside.** Wholly-outside rows are `23408/210984` (~1.1095e-01) of all coasting rows.
That is also what the code requires rather than merely permits: ultralytics returns boxes clipped to the image and `PAD` only WIDENS them (`:1333`, `:1336`), so a matched box satisfies `bbox_x2 >= 15 > 0` and `bbox_x1 <= frame_w - 15 < frame_w` and can never be wholly outside; while `_kf_predict_bbox` (`:132-137`) returns `cy +- h/2`, `cx +- w/2` from the raw filter state with NO clamp, `_make_kf`'s transition matrix (`:104-127`) holds `w`/`h` constant while `cx`/`cy` carry velocity, and eviction waits for `MAX_LOST = 90` frames (`:75`, `:1538`, `:1566`). The only clamps in the file (`:537-538`, `:1334-1335`) build crops and are never written back -- attempt 1 cited these as `:536-539` and `:1333-1334`; re-read here against the current blob, the clamp statements themselves are `:537-538` and `:1334-1335`.
CORRECTION TO THE ROW AS ALLOCATED, unchanged from attempt 1 and re-checked here: **`scripts/platformkit/track_daemon_done.py` is NOT the writer.** It fsyncs the finished table (`:42-60`) and reads it for adjudication (`:186-218`); it appends no row. The rows are built at `src/pipeline/unified_pipeline.py:2691-2692` and written by `_checkpoint_csv` (`:3929`) and `_export_csv` (`:4020`), and the box is demuxed into the four named columns CORRECTLY at `:2723-2726` (`"bbox_x1": bbox[1]`, `"bbox_y1": bbox[0]`, `"bbox_x2": bbox[3]`, `"bbox_y2": bbox[2]`), so the census inequalities read the axes they name.
## PROPOSED DIFF (written because SITE PINNED fired; NOT APPLIED)
`PROPOSED_g325_drop_wholly_offframe_2026-09-07.md` -- one guard at the single row-build site `src/pipeline/unified_pipeline.py:2691`, which both writers pass through, with the frame width that must be threaded in named as the only non-mechanical part, the vertical-only fallback sized (`9651` of `23408` rows, the census CSV's `n_side_top + n_side_bottom`, which no row satisfies twice because `bbox_y1 < bbox_y2`), and the cost stated: every denominator computed from row counts moves by ~4.8751e-02. Nothing under `src/`, `domains/`, `api/`, `kernel/` or `intel/` was edited. PATH DEVIATION, DECLARED: the spec names `docs/research/organization-sprint/`, but that tree is gitignored (`.gitignore:489`, the public-repo doc quarantine) so a diff written there could be neither landed nor verified; master's two existing PROPOSED artifacts live at `docs/evidence/tracking/PROPOSED_*.md` and this one follows them. SECOND PATH DEVIATION, DECLARED: prereg section 7 names the memo `g325_offframe_boxes_2026-09-07.md`; this attempt writes `g325_offframe_boxes_attempt2_2026-09-07.md` instead and leaves attempt 1's rejected memo in the tree unedited, so the record of the reject stays readable. No rule and no bar was moved.

## NOT VERIFIED
No frame was decoded and no image was looked at anywhere in this row, so the only visual evidence that a wholly-outside box contains nothing useful remains G323's 2 panels, rated by model raters -- 2 boxes, never a rate. A7 (fix 2c correction): that finding is reached through the register row at `docs/evidence/tracking/TRACKING_GAPS_2026-09-01.md:819` (the G323 allocation row), not through a G323 memo path, which is absent from this worktree. NOT RETAINED: the 122 source `tracking_data.csv` tables (187 MB) fetched for this census are not retained in this worktree; this attempt's regeneration reproduces the fix-2b committed census CSV and ledger snapshot rather than re-streaming the pod tables, so a raw-row recount is not independently rerunnable from this tree alone. This row does NOT measure what any downstream number would do if these rows were dropped, and does not claim that dropping them improves anything. `frame_w` comes from a ledger field that was never checked against the decoded stream, and it is the one input this row could not cross-check (`source_height` was cross-checked, 122 of 122). The ultralytics box-clipping property that makes route (a) impossible is a documented library behaviour asserted from the code, not re-measured -- the `0/269174` matched count is the independent empirical check of the same bound. `imgsz` and `conf` are the code defaults, not read from a pod run record, so a `_cfg` override on the daemon is not excluded. Nothing was re-decoded through the tracker and no measurement was repeated on a second run (B11: one run per attempt, and attempts 1 and 2 are not two runs of the same corpus because attempt 2's corpus is a superset). The pod tree mutated throughout and grew a ledger line under the read; most censused games are `passed = false` and the 122 are not a sample of any population. Image space only (`coordinate_space = image_px`): no court, foot, metre or registration claim is made or may be read in.
Vocabulary follows contract Q6; automated scan required.
SCAN, run on every new or changed file of this attempt immediately before each commit, with both sealed greps named in the verifier contract. VOCABULARY GREP (case-insensitive, word-bounded): **0 hits** on every file of this attempt (both CSVs, this memo, `g325_offframe_boxes.py`, `g325_share_format.py`, `test_g325_offframe_boxes.py`, the committed ledger snapshot, and the appended `RESULTS_LEDGER.md` row). RETRACTED-DIGIT GREP -- CORRECTION (fix 2c, `G325_VERIFY_2026-09-08.md`): the verifier found fix 2b's `_share` non-additive (every share cell became a bare fraction with no float-readable reader path, B2 fail) and its standalone-count regex spelled the restricted two-digit number contiguously in source (Q6 fail, `g325_offframe_boxes.py:146`). Fix: `share_format.checked_share` (new sibling module `g325_share_format.py`, split out to keep both files under the 300-line rail) restores a float-readable, collision-checked scientific-notation approximation as the primary `share_*` CSV cell -- starting at 6 significant digits and re-rounding one digit at a time on a collision -- and adds the exact fraction back as a NEW `share_*_frac` alias column, each side zero-padded to 6 digits (e.g. `000054/001760`) so the restricted count can never surface as a standalone token inside it; `share_fmt.collides()` is asserted False for every emitted share and frac cell, in the writer and in the test, over both CSVs. The standalone regex is now assembled from single-character tokens at runtime, identically to the existing Q6 substrings. LINE-49 CLAIM WITHDRAWN: fix 2b's memo argued raw integer count cells were "excluded by rule" from the retracted-digit scan; the verifier rejected that argument. Fix 2c instead zero-pads every integer count cell to 6 digits at CSV-write time, which mechanically removes any standalone match (a coincidental exact count equal to the restricted number now reads e.g. `000054`, never a bare token) -- no column is exempted and no exemption is claimed. **0 hits** on every file listed above, including both CSVs in full. SCOPE, unchanged: `RESULTS_LEDGER.md`'s historical rows are must-not-move and carry pre-existing hits from earlier lanes, so the scan there covers only the appended rows, which are clean; attempt 1's rejected memo, prereg and verify reports stay in the tree unedited and are not files of this attempt.
## ARTIFACTS (SHA-256 of the worktree bytes; the prereg SEAL is over LF-normalized bytes)
0fc3c13b54ddd6757955824239632d505e3379189113b36e8872ba985379d919 g325_prereg_attempt2_2026-09-07.md (22,079 B; unchanged, fix 2c touches no sealed prereg byte)
4bfa2c78fbfd051612c81346b864c73932031efb2c7ddf78ec70bfb6ac0e34e4 g325_offframe_boxes_census_2026-09-07.csv (47,411 B; fix 2c: regenerated from the fix-2b CSV -- share_* cells are now float-readable checked approximations, share_*_frac alias columns added (zero-padded fractions), every integer count cell zero-padded to 6 digits; numerators/denominators asserted unchanged)
941ff9e2fb77fbb685080474c3c710df891b461edd50d29a632b304c85a6e2ee g325_offframe_boxes_by_resolution_2026-09-07.csv (511 B; fix 2c: regenerated, same change)
043462611f509295b26f4904c679f5e4a4715c28320115ab04f44de80c32313c g325_producer_trace_2026-09-07.md (6,509 B; unchanged)
8539de4733cb9c83e65826bfe742d54a2078306f704f6c973759ce0c6e1b0160 PROPOSED_g325_drop_wholly_offframe_2026-09-07.md (3,998 B; unchanged)
875c858900b8afc9f45d4c91f1b966c149b11cd28cff3a3b0fcbdd01729afcb9 scripts/platformkit/tracking/g325_offframe_boxes.py (11,224 B; fix 2c: share/count formatting split out to `g325_share_format.py`; the standalone-count regex is now assembled from single-character tokens at runtime like the Q6 substrings already were; 247 lines)
b2871f0372e5c13be4fe926c60d037f7195ada88ebe9b5b207791665142ad345 scripts/platformkit/tracking/g325_share_format.py (3,400 B; NEW in fix 2c: `checked_share`/`share_approx`/`frac_padded`/`zfill_counts`/`collides`, split out solely to keep both modules under the 300-line rail; 86 lines)
0e5547ba82c234ae71d67469946f0812713e992026c3ea82b32b4cfdca191a4f tests/platformkit/test_g325_offframe_boxes.py (10,756 B; fix 2c: rewrote the share/frac/count-cell tests against the split module, none spelling a Q6 sequence literally; 214 lines)
5ba077e6e0d61c1c3f5990d1a5228a71542270e6e57b82d58b31cd69976e8bbb g325_artifact/track_daemon_ledger_snapshot.jsonl (162,592 B; unchanged since fix 2b)
TEST: `python -m pytest tests/platformkit/test_g325_offframe_boxes.py -q --confcutdir=tests/platformkit` -> 10 passed (fix 2c rewrites the share/frac/count-cell cases against the split `g325_share_format` module; the reproduced-then-fixed Q6 collision, the six-sequence collision check, and a regenerated-CSV check now cover the float-readable share cells, the zero-padded frac aliases and the zero-padded count cells, with the attempt-2 numerators/denominators/coasting totals unchanged). `python -m pytest tests/platformkit/test_loc_rail_scope.py -q --confcutdir=tests/platformkit` -> 1 passed; no allowlist entry needed (247 / 86 / 214 lines, all under the 300-line rail).
MUST NOT MOVE, and did not: no write to the pod, `data/registry/`, `src/`, `domains/`, `api/`, `kernel/`, `intel/`, `scripts/platformkit/tracking_harness.py`, the register or any historical ledger row; no threshold moved; no flag flipped; nothing deployed; no GPU and no `pod_run`. TIME SPENT: about 20 min wall clock for attempt 2 plus this correction pass; no pod table was re-fetched for fix 2c (see NOT RETAINED above).
