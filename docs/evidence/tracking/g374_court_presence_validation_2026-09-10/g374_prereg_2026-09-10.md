# G374 court-presence validation preregistration

Contract: `docs/evidence/tracking/VERIFIER_CONTRACT.md`, sections B and Q1-Q9.

Scope is prepare-only. This commit does not open a pod store, build a sheet, read a
blind label, run a scorer, or compute a validation metric. The Claude finisher runs
the binding and the single sealed draw on the pod after this seal is committed.

Machine (S1): the scorer binding is for `/workspace/wt/a14` on the pod because the
frozen G364 artifacts are read-only there and expected scoring memory may exceed the
local RAM guard. This preparation run does not dispatch `pod_run`.

Frozen inputs, each read one file at a time by the finisher, are under
`/workspace/wt/a13/docs/evidence/tracking/g364_learned_court_presence_2026-09-09/`:

- `validation_pool.csv`; SHA-256: TO_BE_FILLED_BY_FINISHER; byte size: TO_BE_FILLED_BY_FINISHER.
- `validation_predictions.csv`; SHA-256: TO_BE_FILLED_BY_FINISHER; byte size: TO_BE_FILLED_BY_FINISHER.
- `model.json`; SHA-256: TO_BE_FILLED_BY_FINISHER; byte size: TO_BE_FILLED_BY_FINISHER.
- `model_identity.json`; SHA-256: TO_BE_FILLED_BY_FINISHER; byte size: TO_BE_FILLED_BY_FINISHER.
- `dev.csv`; SHA-256: TO_BE_FILLED_BY_FINISHER; byte size: TO_BE_FILLED_BY_FINISHER.

Binding before-condition, to be run exactly once before any draw:

`python -m scripts.platformkit.tracking.g374_premise --model <model.json> --identity <model_identity.json> --pool <validation_pool.csv> --predictions <validation_predictions.csv> --development <dev.csv>`

The binding prints the model SHA-256 and threshold, the whole-pool prediction census,
and the development/validation game overlap. It passes only when the identity and
model SHA-256 agree, threshold is exactly 0.30, the census is 360 total (224 COURT,
122 NON_COURT, 14 ABSTAIN), and overlap is empty. If it fails, the finisher quotes
the output, writes the premise-false memo, and does not draw, rate, or score.

Sealed draw (seed 374): select all 14 ABSTAIN frame keys. From the remaining 286
places, select COURT and NON_COURT counts proportional to their whole-stratum
availability after enforcing the 60-per-stratum floor: 185 COURT and 101 NON_COURT.
Within each predicted stratum, sort by stable `frame_key`, use a seeded fractional
start and fixed spacing across the strict interior, and select no label-dependent
row. The final manifest is exactly 300 unique keys. Every nonselected key is emitted
to `excluded.csv` with `quota_not_selected`; no selected key appears there.

Blind procedure: build sheets from only the sealed manifest before any reference
label is read. Terra and sol rate all four G364 classes with no prediction shown;
the finisher adjudicates disagreements blind. Sheets are at most 200 KB. The eye
check is 30 evenly spaced sheets, including every ABSTAIN frame, with both ratings
and the prediction shown only after rating.

Scoring: once and only after the preceding steps, emit `reference.csv`,
`predictions.csv`, `ratings.csv`, `confusion.csv`, and `summary.json`. The prediction
manifest fields are exactly `frame_key,prediction,court_probability,model_sha256`;
confusion fields are exactly `scope,prediction,reference_label,count`. Report
USABLE_COURT precision and recall with Wilson 95 percent bounds, abstention share,
kappa, and per-competition cells. The acceptance bar is precision >= 0.90, recall >=
0.80, at least 60 rated rows in each predicted stratum, no threshold move, and no
refit. Development values are printed only as NOT evidence (B8).

No paired model comparison is specified for this row. If the finisher adds one, it
must use `walk_forward` or `cpcv_evaluate` with purge and a symmetric nonzero embargo,
one unique evaluator state per frame key, and archive losses only from evaluator
records. The delta convention is improvement = baseline loss minus candidate loss;
positive means candidate better.

No `src/`, `kernel/`, `api/`, `intel/`, flag, register, ledger, or `data/` path is
changed. The prepare-only memo verdict is the placeholder `PREPARED -- NOT SCORED`.

SEAL sha256 6556c4a6749921378bf9d9957fe0ae17d4f7975e3a07dbe224671fc8b5b17d73
