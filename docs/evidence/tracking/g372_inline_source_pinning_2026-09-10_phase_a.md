VERDICT: PARTIAL -- phase A (code + attribution + scratch controls); deploy-only measurement (phase B) pending authorization
# G372 inline source pinning -- phase A
Spec `docs/evidence/tracking/specs/G372_spec.md`; contract `docs/evidence/tracking/VERIFIER_CONTRACT.md` A/B plus Q1/Q6, self-checked. Astra's binding order is honoured: code ACCEPT first, deploy-only measurement later.
FIX 1b, after the codex-sol REJECT of attempt 1 recorded at `docs/evidence/tracking/G372_VERIFY_att1_REJECT_2026-09-10.md` (PC path `C:/Users/neelj/nba-track-a9/docs/evidence/tracking/G372_VERIFY_att1_REJECT_2026-09-10.md`). Six corrections, each named: (1) the collection window, the attempt set, the even sample and the failure-retaining denominators are sealed as AMENDMENT A2 in its own commit `07ece33f2` BEFORE any new number -- A1 clause A3 (stop at the first 40 collected sections) is replaced, never edited; (2) the attempt set is now EVERY unique `game_id` in the sealed window, not only the sections whose bytes survived, so the coverage denominator is no longer conditioned on the outcome (B1); (3) the sample is every k-th index over the whole window from a digest-derived start instead of a sorted first-40 head slice (B7); (4) the exercise reads the archived `collect_manifest.csv` and never a directory listing, and every failed attempt is LEFT-JOINED into `join.csv` and `pins.csv` with its status; (5) the named snapshot, `collect_manifest.csv`, `sampled_indices.csv` and the STANDALONE unified diff are all committed in this landing (A7); (6) the PROPOSED overlay now opens the source handle AT THE HOOK, on the tick thread, before any worker dispatch, and a new test file imports `g372_phase_a` -- the two NEW GAPs the verifier filed. Attempt 1's attribution and premise sections reproduced and are carried forward unchanged.
Machine (S1): pod `d69708b8b854` at 213.192.2.120:40117, CPU only, thread caps 1. The pod is required because the ledger, the footage stores and the tracking tables live only there. The deploy tree, `/workspace/data`, the daemon (pid 12154 during attempt 1; the daemon restarted and is pid 475000 at fix 1b) and the feeder were opened READ-ONLY; every write went to `/workspace/wt/a9`.
Q1 SEAL ORDER. The sealed prereg (`g372_prereg_2026-09-10.md`, SEAL `6e6469a0dac4829a65e0c05ab2e705b08411452bdcd726ea835355a80901a75e`) fixes only the post-ACCEPT live window. `g372_prereg_amendment_A1_2026-09-10.md`, SEAL `21c12872970c84598b39d41aab24bce95e06925f21d96665e54bb51358d15513`, commit `9ce396284`, sealed the phase-A census. `g372_prereg_amendment_A2_2026-09-10.md`, SEAL `4dd969f54ba526f71348da37ffeb7cb462f3dd977984c07ed36543b114bc5131`, commit `07ece33f2`, seals the window and the sampling rule and predates every number in the scratch section below. Neither the prereg nor A1 was edited.
## Before-conditions (full stdout archived in `before_conditions.txt`, SHA-256 `a46bda1f96f77ed7ec3e10c2db26ff55be8105d94e364d49b335ef8241d8e927`)
CODE IDENTITY (A11): deployed `scripts/platformkit/track_daemon.py` = `9747d9a085405c492f04c4e3b162ed8b1d897309da6f0f9c014cb7092056f5ac`, byte-identical to the hash the spec cites and to the lane file after LF normalization, and re-checked unchanged after this attempt's dry-run. Deployed `track_daemon_done.py` = `75cbff79cd115952ad14f76c94ab5fdb62239f30841b06e73b967473a2d69f70`. The prereg's `ee425ee5...` / `f6f4cbd2...` are the SAME two files hashed with CRLF line endings in the Windows lane checkout; that is a checkout artifact, not drift. NO DRIFT; attribution is permitted.
HOOK LINES: every line number the lane cites is correct in the deployed file -- duplicate handling 367-372, `log_path = path.with_suffix(".log")` at 373, `subprocess.Popen` at 376, completion entry 285-323, `retain()` (which MOVES, never deletes) at `track_daemon_done.py`:262-276.
SURVIVAL (spec command 2, verbatim, 18:17:59Z): `fresh_n 133 available 23 aged_n 58 aged_available 0 aged_share 0.0 median_deletion_age UNKNOWN`. The deploy `data/footage_bridge`, `data/footage_corpus` and `data/tracking` are symlinks into `/workspace/data`, so those zeros are real reads, not a missing path.
## Attribution of source loss (`attribution.csv`, `deleters.csv`, `attribution_summary.json`, `attribution_ages.json`) -- unchanged from attempt 1, independently reproduced by the verifier
Population: ledger rows 856 to end of the immutable snapshot, archived here as `ledger_snapshot.jsonl` (the 2026-09-10T18:15:46Z snapshot; SHA-256 `1547d32d82d5909bb43a8f5f6d82d5f4b76c5e6d145121cc5563e0a3a8945efe`; 1089 rows total, 234 fresh rows, 149 unique fresh sections -- 85 rows are duplicates, so rows are never the section denominator).
THE DELETER IS NAMED. 122/149 fresh sections have no bytes anywhere; 122/122 of them are matched to an explicit DEL event in `/workspace/vol_guard.log` written by `/workspace/vol_guard.py` (pid 863166), which prunes `footage_corpus` files that already have a ledger row. `gone_unattributed = 0`. G369's "36 pct of sources gone" is therefore not the daemon (`retain()` moves bytes into the corpus) and not the feeder: it is the pod volume guard, with reasons `corpus_over_2500MB` and `probe2000_failed` recorded per file.
| stratum | sections | retained | GONE | retained share | re-tracked | median age at snapshot (min) | median finish-to-delete (min) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BEFORE dup gate (first claim < 17:49:00Z) | 125 | 8 | 117 | 0.064000 | 85 | 60.667 | 10.567 |
| AFTER dup gate | 24 | 19 | 5 | 0.791667 | 0 | 9.900 | 3.083 |
| all fresh | 149 | 27 | 122 | 0.181208 | 85 | -- | -- |
Locations: BRIDGE 0, CORPUS 27, QUARANTINE 0, GONE 122.
THE SURVIVAL DIFFERENCE IS AGE-CONFOUNDED AND IS NOT A DUP-GATE RESULT. At the snapshot the AFTER cohort is 9.9 min old against 60.7 min for BEFORE, and the age-matched stratum (>= 30 min old) contains 107 BEFORE sections and ZERO AFTER sections, so no age-matched comparison exists. The survival column is reported descriptively; the dup gate's effect on survival is NOT VALIDATED.
WHAT THE DUP GATE DID CHANGE, measurably: re-tracking went from 85/125 sections BEFORE to 0/24 AFTER, and 77 of the 117 BEFORE losses were sections that had also been re-tracked. Three BEFORE sections carry a DEL event yet are present now, i.e. they were deleted and re-supplied -- churn, not retention.
CONSEQUENCE FOR THE ROW: median time from tracking finish to byte deletion is 10.567 min (BEFORE) and 3.083 min (AFTER). Post-hoc pinning cannot work on this pod at any practical cadence; claim-time binding is the only mechanism that can hold, which is what G372 proposes.
## Sidecar module exercised on the sealed even sample (`sampled_indices.csv`, `collect_manifest.csv`, `sidecars.csv`, `pins.csv`, `join.csv`, `controls.csv`)
SAMPLE (A2.2/A2.3): window n = 149 unique `game_id`, ordered by `(first_claim_utc, game_id)`; k = floor(149/40) = 3; start = int(`1547d32d`, 16) mod 3 = 0; sampled n = 50 at indices 0, 3, ... , 147, i.e. spread across the whole window -- first sampled claim 15:14:14Z, last 18:11:45Z. Every sampled index and `game_id` is archived.
COLLECTION (A2.4, A9): 5 attempts copied OK (43-80 MB each, `basketball__fiba-Y1cgaIWW8gk_s90.mp4`, `basketball__fiba-fyp4VsE-8Fc_s90.mp4`, `basketball__fiba-tZeqmc_-_sA_s90.mp4`, `basketball__fiba-tZeqmc_-_sA_s6365.mp4`, `ncaa_basketball__9F5q5jCbrMI_s2515.mp4`); 45 attempts BYTES_GONE (no file under `footage_bridge`, `footage_corpus` or `footage_quarantine`, `.failed` renames and `source_variants` probed). 0 SKIP_OVER_CAP, 0 copy errors. All 50 rows are in `collect_manifest.csv`; nothing was written under `/workspace/data`.
COVERAGE = 5 committed sidecars / 50 sampled attempts = 0.100000, over 4 distinct videos among the committed rows (25 distinct videos across all 50 sampled). This is BELOW the 1.000 / >= 30 sections / >= 10 videos bar and is reported as a NAMED FAILING CLAUSE of the PARTIAL verdict, per A2.6. It is a measurement of the volume guard's pruning, not of the overlay: 45 of the 50 sampled sections had already lost their bytes by collection time, which is the condition claim-time binding exists to remove. It licenses no statement about phase-B claim-time coverage in either direction.
INDEPENDENT AGREEMENT = 5 agreeing rows / 50 sampled attempts = 0.100000. Over the 5 joinable rows it is 5/5 = 1.000000, which is a CONDITIONAL figure printed beside the scored one, never instead of it; with 5 joinable rows against a >= 30 bar the agreement clause is NOT VALIDATED for phase A. The independent side imports only `g369_prospective_identity._file_record` / `._probe` / `._manifest`; `cmd_pin` and `cmd_exact_align` were never called. Join key is `game_id` plus the scratch source SHA-256; 0 DISAGREE, 0 rows omitted, and all 45 failures appear in `join.csv` and `pins.csv` with status `BYTES_GONE`.
CONTROLS (CONSTRUCT, enumerated separately, never counted toward n): identical_read PASS (two reads agree AND the two independent implementations agree on the same 58,883,653 bytes); byte_flip PASS (one flipped byte changes BOTH digests); deterministic_write PASS (two writes of one fixed payload are byte-identical).
CAVEAT ON THE AGREEMENT (B8): both sides stream SHA-256 over the same file, so agreement proves the two code paths bind the same bytes; it does not prove the digest is correct against a third implementation.
PRODUCER SCHEMA (`schema_diff.txt`): EMPTY, because phase A deployed nothing. That is a statement that this lane changed nothing, NOT evidence that the overlay preserves the schema. A static reader survey of all 12 ledger readers is archived: every non-test reader projects into its own fixed `DictWriter` fieldnames and none asserts an exact key set, so one added key is additive (A5/B2).
## PROPOSED overlay -- not applied anywhere
The STANDALONE unified diff is committed as `PROPOSED_g372_track_daemon_sidecar.diff`, SHA-256 `e0d8e9247d288df0f3a1c033a725b27331d7c3f0818d963bdb1603062d543b6f`; `PROPOSED_g372_track_daemon_sidecar.md` (SHA-256 `0fc196b83a567d55dd971c5454dab00c4116302c9448a877dda86f5fe16686e8`) is the wrapper and embeds the same bytes. Hook at deployed line 373. `patch --dry-run -p1` run with `/workspace/deploy/nba-ai-system` as cwd applies all 7 hunks, return code 0; the edited scratch copy parses; the deployed file's SHA-256 is unchanged after the dry-run. It was generated on a scratch copy and applied to nothing.
The claim now OPENS the staged source and takes its `fstat` size on the tick thread at the hook, before any worker is dispatched; only the streamed digest and the five-second probe run off the loop, and the probe is handed `/proc/<daemon pid>/fd/<fd>` so it reads the pinned inode rather than a path the guard may already have unlinked. An open that fails leaves `thread` None and the section passes straight through (B3/B4).
## Files and tests
`scripts/platformkit/tracking/g372_source_sidecar.py` (140 lines; `sha256_file` now delegates to a new `sha256_stream`, which is what lets the hook digest an already-open handle); `scripts/platformkit/tracking/g372_window.py` (128 lines, NEW: sealed window, attempt set, even sampler, failure-retaining collection); `scripts/platformkit/tracking/g372_phase_a.py` (294 lines, the measurement route); `tests/platformkit/test_g372_source_sidecar.py` (73 lines); `tests/platformkit/test_g372_phase_a.py` (81 lines, NEW).
Per-file tests, pod, one file per command, `--basetemp` under `/tmp`: `test_g372_phase_a.py` 4 passed in 3.17 s (even sampler is not a head slice; small window takes every attempt; the coverage denominator counts failed attempts; a failed attempt is left-joined into `join.csv` with its status); `test_g372_source_sidecar.py` 6 passed in 1.55 s; `test_loc_rail_scope.py` 1 passed in 2.78 s (no allowlist entry needed; both new modules are under the 300 cap). No full suite was run.
Delta convention: for survival and agreement, higher is better; no loss delta and no throughput delta were computed in phase A.
Q6 automated scan over the candidate diff and every new artifact (memo, amendment A2, the archived ledger snapshot, every new CSV/JSON/TXT, the PROPOSED `.md` and `.diff`):
```
```
Wall time: fix 1b -- amendment 6 min; sealed collection 3 s; exercise 20 s; diff regeneration and dry-run 2 min; memo and landing 12 min; about 25 min, on top of attempt 1's 25 min.
## NOT VERIFIED
- Live claim-time coverage, the live completion-row column identity, and the paired throughput clause: all three need the deploy and are PHASE B. None is inferred from the scratch exercise.
- The phase-A coverage clause FAILS its bar (0.100000 against 1.000) and the phase-A agreement clause is NOT VALIDATED (5 joinable rows against a >= 30 bar); neither is cured in phase A and neither is evidence about the overlay.
- That the PROPOSED overlay behaves as written under the running daemon; only a dry-run apply and a syntax parse were done. The descriptor-pinning behaviour of the new hook (digest and probe surviving an unlink) is argued from POSIX semantics and is NOT measured here.
- The correctness of the SHA-256 binding against a third independent implementation (both sides use a streamed SHA-256).
- Any claim about the dup gate changing byte survival: there is no age-matched AFTER stratum.
- Whether `vol_guard.py` will keep the same prune policy after its 09:15Z headroom deadline; the policy was read, not exercised.
