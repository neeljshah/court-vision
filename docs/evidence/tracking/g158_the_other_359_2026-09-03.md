# G158 - the other 359: producer and recoverability audit

Contract: [VERIFIER_CONTRACT.md](VERIFIER_CONTRACT.md), including A7, B1-B10,
and Q8. This is a local-only, read-only measurement. No pod or network
connection was made. No tracking table, provenance helper, adapter, census
definition, threshold, or verdict was changed.

## Verdict

**CLOSED AT LIMIT - source-population premise FALSIFIED.** The committed G154
census records 359 `other` tables, but none of those 359 source paths exists
in this worktree at measurement time. `data/tracking/` contains exactly one
`tracking_data.csv`: the non-target `timebase_tennis_600` table. Therefore
G158 cannot truthfully give the requested source-table date range, header
shapes, geometry/re-stampability count, or five headers.

The source absence does not prove the orchestrator's legacy-tables hypothesis.
It instead prevents testing it. The current code audit independently falsifies
the stronger implied premise that the G152 unconditional `court_feet` stamp
describes the 359: G152 traces the tennis adapter, while G154 labels 358 of
the 359 as basketball. The basketball `UnifiedPipeline` remains a separate,
direct, unstamped producer at HEAD.

The measured availability and systematic sample are committed in
[g158_other/source_availability.csv](g158_other/source_availability.csv).

## Frozen denominator and what is observable

The requested construct is the 359 rows in G154's committed
`table_census.csv` whose `rollup_bucket` is `other`. Re-reading that artifact
found 361 distinct table names, 359 distinct `other` names, and no duplicate
unit. The **eligible denominator for all shares in this memo is 359 named
`other` tables**, unless a smaller explicitly named denominator is stated.

| Observable from the committed G154 artifact | Tables | Share of the 359-table eligible denominator |
|---|---:|---:|
| basketball routing label | 358 | 358/359 = 99.7214% |
| UNKNOWN routing label | 1 | 1/359 = 0.2786% |
| Source CSV present in this worktree | 0 | 0/359 = 0.0000% |
| Source CSV absent in this worktree | 359 | 359/359 = 100.0000% |

The sport labels above are a recomputation of the committed G154 classification
artifact, not a fresh read of a missing CSV header. There is no source-table
date range and no source header-shape count to report: both would require
reading the absent files. The directory's one non-target current table has the
header shown in `source_availability.csv`; it is not included in any numerator
or denominator here.

## Stamping-code dates and producer distinction

The shared helper entered history in commit
`7914c648661cd10320b986f2688e35bbdbea1684` at
2026-09-01T06:48:26-05:00, subject
`refactor(tracking): one shared provenance helper; football and baseball keep their detections`.
The strict omitted-declaration contract change is
`de124527ed653b24df58f27f5bef9d9a45ad9728` at
2026-09-01T07:13:11-05:00, subject
`fix(contract): an omitted declaration no longer means "assume court coordinates"`.
The tennis/soccer caller adjustment is
`f16b3863ad2f1a6516c1b23d94ead33e4b5d5655` at
2026-09-01T10:32:50-05:00, subject
`fix(tennis,soccer): stop labelling stale and ordinal calibration as current`.

`scripts/run_clip.py` acquired its late image-space stamp in
`74db4a40147a0414ab0daee7211175df2b5467c6` at
2026-09-01T09:33:09-05:00, subject
`feat(tennis): FIRST GAME TO PASS THE HARNESS -- rank players by continuity, not box size`.
Its commit message explicitly says basketball had been writing to a shared
file, then changed callers to per-game `data/tracking/<game_id>` paths, and
that `run_clip` now stamps persisted rows `image_px`.

The source paths needed to compare these September 1 dates with the 359 table
dates are absent. The timeline therefore **cannot decide** whether any given
one of the 359 predates the stamp. It does establish that the court-feet
adapter trace is not evidence that a basketball table was stamped.

## Current writer inventory

This inventory is the source-producing paths that create a canonical
`tracking_data.csv`, not every later maintenance script that can rewrite an
already-existing table.

| Producer path | What it writes | Provenance status at HEAD | Consequence |
|---|---|---|---|
| `scripts/platformkit/adapter_run.py` with `domains/tennis/tracking/adapter.py` | `data/tracking/<game_id>/tracking_data.csv` through `adapter.write_csv()` | `stamp_court_space_rows(..., "tennis")` is unconditional on normal `process_video()` return; writes `court_feet` | This is G152's producer, not the basketball majority. |
| `scripts/platformkit/adapter_run.py` with baseball or football adapter | Same normalized destination | `adapter_run` passes `image_space=True`; each adapter emits complete `image_px, observed, none` provenance | Declared image coordinates, not unstamped court rows. |
| `scripts/platformkit/adapter_run.py` with soccer adapter | Same normalized destination | Its current runner also passes `image_space=True`, so it stamps `image_px`; a direct soccer-adapter caller with `image_space=False` and zero accepted homographies can return an unstamped DataFrame | The runner is stamped, but the adapter API is not an unconditional declaration path. |
| `src/pipeline/unified_pipeline.py:_checkpoint_csv` | `self._data_dir/tracking_data.csv`, first write overwrite then append | **Unstamped.** It imports no provenance helper and writes its legacy tracking field list directly with `csv.DictWriter`. | This is a live alternate basketball-family producer. Direct callers include `scripts/process_game.py`, `scripts/full_game_pipeline.py`, the module CLI, and other callers of `UnifiedPipeline`. |
| `scripts/run_clip.py` | Calls `UnifiedPipeline(..., data_dir=...)`, then reopens that CSV | Attempts a late `stamp_image_space_rows` only if all provenance columns are absent; the whole block is `try/except` and logs `stamp skipped` on an exception | Normally declares the completed wrapper output as `image_px`; it does not make the underlying direct pipeline writer unconditional. |

This is the requested attempt to falsify the legacy explanation: an unstamped
writer is still live at HEAD. Consequently, even if the unavailable 359 were
old, their missing declaration cannot be attributed to age alone. A future
re-track routed directly through `UnifiedPipeline` can still create another
undeclared table; a re-track through a successful `run_clip` completion instead
gets an `image_px` declaration and is a coordinate-contract rejection, not a
court-feet success.

## Recoverability and evenly spaced hand checks

Recoverability is intentionally **NOT VERIFIED**, not zero: determining it
requires each source header to establish whether geometry columns exist. With
0/359 source files available, there is no evidence for either
"geometry columns present; potentially re-stampable" or "no geometry; requires
re-tracking." Re-stampable would describe a file's columns only; it would not
authorize a re-stamp.

The five requested hand checks used systematic evenly spaced indices
`floor((i + 0.5) * 359 / 5)` for `i = 0..4`, yielding indices 35, 107, 179,
251, and 323. Their names, expected paths, and `false` existence observations
are in `source_availability.csv`. Every requested header is **unavailable**:

| Index | Table | Header | Classification |
|---:|---|---|---|
| 35 | `0022500041` | unavailable: source path absent | not hand-verifiable |
| 107 | `0022500330` | unavailable: source path absent | not hand-verifiable |
| 179 | `0022500575` | unavailable: source path absent | not hand-verifiable |
| 251 | `0022500892` | unavailable: source path absent | not hand-verifiable |
| 323 | `0022501147` | unavailable: source path absent | not hand-verifiable |

## Verifier-contract self-check

### A

- **A1:** No code changed and no test was added; no focused test applies.
- **A2:** Recomputed the available committed artifact: 361 unique G154 table
  rows, 359 unique `other` rows, 358 basketball labels, and one UNKNOWN label.
  Separately checked each of the 359 expected local paths: 0 exists. No absent
  source-table metric is quoted as if reproduced.
- **A3:** The construct itself is exhaustive. The five required source checks
  are systematic across all 359 positions, not a head slice; each transparently
  fails availability rather than being substituted with a leading record.
- **A4:** The unit is a distinct G154 table name; 359 are unique.
- **A5:** Evidence only. No field or reader changed.
- **A6:** This is an explicit-path worktree evidence commit only. No archive
  landing, results-ledger/register edit, deployment, pod, or SSH action occurred.
- **A7:** Before commit, the memo, availability CSV, G154 census artifact,
  provenance helper, all four adapter files, `adapter_run.py`, `run_clip.py`,
  `unified_pipeline.py`, and verifier contract exist. The 359 absent paths are
  measured negative inputs and are not claimed evidence artifacts.

### B

- **B1 CIRCULAR METRIC:** Clear. The complete 359-row committed construct is
  retained; unavailable inputs are named, never excluded to improve a metric.
- **B2 NON-ADDITIVE SCHEMA:** Clear. No schema, field, status, or reader changed.
- **B3 FALL-THROUGH LOSS:** Clear. Missing source files are reported as absent,
  not labelled bad data or silently converted into a recoverability result.
- **B4 RE-CLAIM LOOP:** Clear. No claim, queue, retry, or ownership path changed.
- **B5 PRE-VERIFICATION DEPLOY:** Clear. No pod, SSH, copy, deploy, restart,
  kill, re-track, repair, or table write occurred.
- **B6 ORPHANS:** Clear. No module, import, command, or test moved or retired.
- **B7 HEAD-SLICE EVIDENCE:** Clear. The availability census is exhaustive and
  the five unavailable sources are systematically distributed, not head-picked.
- **B8 SELF-FIT AS INDEPENDENT:** Clear. No fitted residual is claimed.
- **B9 DEGENERATE DENOMINATOR:** Clear. One distinct table name is one unit;
  rows, frames, and track IDs are not reused as units.
- **B10 MOVED BAR:** Clear. No threshold, eligibility definition, coordinate
  contract, or verdict was changed.

## NOT VERIFIED

- The 359 source-table modification-date range, header shapes, and per-table
  source sports, because every source path is absent in this worktree.
- The requested five raw headers and classifications beyond "not hand-verifiable."
- The counts with real geometry columns versus no geometry, and therefore the
  number potentially re-stampable from file contents.
- Whether any individual table predates September 1, 2026 or was written by a
  particular producer.
- Whether direct `UnifiedPipeline` caller paths are used in current operations;
  code reachability is not an execution trace.
