VERDICT: DONE (PROPOSED ONLY) -- coordinate accounting DONE, writer/geometry compatibility DONE, comparator re-derivation DONE (diagnostic); the receipt is applied nowhere and geometry quality stays NOT VERIFIED.

Prereg `docs/evidence/tracking/g412_box_frame_contract_2026-09-12/prereg.md`, SEAL sha256 f48ae105abc5d869864bc19d5e580870bcf8b98eafd33baf99a4e400882be916, sealed alone at e44f8d096, unedited and revalidated from its own LF bytes.

## PREMISE step 0 -- reproduced before anything else

60 distinct exact-even ticks over 53 sections, 206 stored rows, 5 producer-silent ticks, 592 comparator boxes and 51 frozen producer-comparator pairs over 28 frames: all identical to landed G406. 60/60 retained originals and 60/60 decoded native frames rehash equal off-pod (53 distinct receiver files, 0 failures). The two spec-quoted digests match on LF bytes: G402 `target_mask.csv` c2c4f57a56330afbc9a7772b61743864aa04d746d9715e0374743e724a79dc71 and G380 `summary.json` 2155b6f861fd2a611e61488f6ce7bb42af7d231c1860bfde324bee7b7c47dba9 (this worktree has core.autocrlf=true, so `input_hashes.csv` carries both the on-disk and the LF digest of every text input). Writer receipt absence reproduced: 0 hits for box_frame / box_crop_origin_y_px / box_padding_px across the 4 archived route files and all 59 landed raw `tracking_data.csv` headers. The one neighbouring field, `coordinate_space` = image_px, is a G380 PROPOSED provenance label about the court point and is NOT an equivalent receipt: it names no crop origin, no padding and no box. The premise is TRUE and no substitution was made.

## Identity before launch, no inference

Model set is EXPLICITLY EMPTY: this row runs no inference, no pod stage and no GPU. Every exercised file was hashed before use. The four archived route files in this worktree reproduce G402's `route_hashes.json` "original" on LF bytes: `src/pipeline/unified_pipeline.py` c4eae385bfeebd4e44c8161c791ef7ed4589697020f424d3dde7aec6816d9226 and `src/tracking/advanced_tracker.py` df2ae698ae03e804f67639434d8303638aea9087c3169c016af5a3734dd474d7, plus `src/tracking/video_handler.py` 5e78e7fa8a43a251 and `src/tracking/player_detection.py` c3bc2f7d4c4fda36 (full digests in `input_hashes.csv`). `src/` is READ ONLY here and is byte-unchanged.

## (1) Coordinate accounting -- DONE (PROPOSED ONLY)

`route_chain.json` (589fd3f7b475ff60d977bc57dbf4c53978b7522e0231835d9100e1d5d5b00997) traces 12 stages from archived bytes, each with file:line, the quoted source line and the file digest: decode at unified_pipeline.py:1677 -> crop at :1693 with TOPCUT = 60 from video_handler.py:11 -> integer conversion at advanced_tracker.py:1333 -> clipped corners at :1334 (used ONLY for the pixel crop and the court projection) -> PAD = 15 from player_detection.py:20 expanded at advanced_tracker.py:1336 as `(y1-PAD, x1-PAD, y2+PAD, x2+PAD)` -> fresh store at :626, Kalman overwrite at :1165 -> court projection at :1410 from `((x1c+x2c)//2, y2c)` -> drop-only guard at unified_pipeline.py:2703 -> writer tuple permutation at :2736. The stored tuple is NEVER clipped; only the crop array and the court point use the clipped corners. `transforms.json` publishes the equations and the clip order with an empty `fitted_constants` list. `reader_manifest.csv` inventories every reader of a stored bbox field: 178 hits over 53 files (39 in `src/`, 139 in `scripts/`; 124 read, 38 emit, 16 write), and 0 of them already carry a receipt field. All 60 planned cards are rendered in `renders/`: 55 measured native demonstrations over 55 distinct native frames (>= 30 required) and 5 explicit UNKNOWN silence cards. `eye_index.csv` binds each card to its crop-array dimensions (1920x1020 and 1280x660) and crop-array sha256. Zero unexplained origin or tuple errors.

## (2) Writer / geometry compatibility -- DONE (PROPOSED ONLY)

`construct_cases.csv` enumerates the sealed 2 sizes x 4 locations x 2 box states x 2 statuses = 32 controls; 32/32 agree with an independent corner-loop oracle, and the per-file test re-derives all 16 nonempty cases a THIRD time with its own inline clamp. `writer_compatibility.csv` applies the proposed receipt to all 206 real rows: 206/206 legacy fields byte-identical, 34 BOUND (position_source DETECTION conjoined with source_branch `_activate_slot` -- two independent columns, never one value passed twice) and 172 UNKNOWN carrying an explicit `unbound-<route>` reason. The bound-route values are box_frame `topcut60_pad15`, box_crop_origin_y_px 60, box_padding_px 15; METHOD 3's value string is used and supersedes the PREPARE helper's placeholder. The PROPOSED diff `docs/research/organization-sprint/PROPOSED_g412_box_frame_contract.diff` (byte-identical evidence copy 592e257097395ea7890e2c1577a5f75ab9839a3bbbcca058741ff02f6d33b304) adds 21 lines and removes 0; its receipt expression was extracted verbatim and exercised in an isolated fixture (`diff_fixture.json`) whose three route states agree exactly with the helper. It is APPLIED NOWHERE. 204/206 stored boxes sit within crop_height + PAD; the 2 that do not are named (720p60_22_r00 and _r01, both CLAMP / jump_clamp on a 660-high crop) and follow from the unclipped store at stage G, not from the contract.

## (3) Comparator re-derivation -- DONE (diagnostic only)

`paired_residuals.csv` re-derives G406's OLD associations unchanged: 51 producer-comparator pairs over 28 frames, signed centre dy median -58.203 and dx median 0.0, matching the landed G406 summary (-58.2, p10 -67.75, p90 -45.75). Adding the code-derived origin of exactly 60 before any clipping moves the pooled median to +1.797 at the original precision, and every one of the 51 per-pair deltas is exactly 60.0 to 1e-9. Clipping is logged separately and changes 18 of the 51 pairs (median after clipping +3.26). No constant was fitted: 60 and 15 are read from source. This is descriptive 51-pair / 28-frame arithmetic and never 51 independent frames; residual localisation and box quality remain G413's task.

## Peer lanes (METHOD 9)

G409 (lane a12) has COMPLETED and AGREES: its memo (587014737e29679b725773a4b85fc9ff509e781179f119142ea2542f42c1ddd7) reports the same TOPCUT = 60 site, the same serialization stage and the same -58.2 -> +1.8 pooled move over the same 51 pairs / 28 frames. G410 (lane a13, PARTIAL + NOT VALIDATED, 7dedc399259652ad4139eeaa08aa29401ee1cad2d1ff2f84569eb5b5be2f17e5) shares all four code citations. No contradicting executed stage was found. Every citation here was read from this worktree's archived `src/` bytes before either peer memo was opened; G410 remains PENDING corroboration and is not load-bearing.

## Reproduction, scans and rails

Two fresh processes rebuilt every delivered table, card and receipt from saved bytes; `repeats.json` keeps the parent shape (identical / runs with stdout + returncode + command / per-table digests) and reports identical = true with 0 nonzero returncodes. That does not establish inference repeatability, and none is claimed. Field-aware Q6 scan covers every delivered text path, this memo, the ledger line, the PROPOSED diff, all helpers, the test, the scanner's own source and every `.log.txt` receipt, with a complete path manifest (37 paths) and index-only counts: 0 hits in everything this row authored, including the appended ledger line; the single manifest hit is pre-existing `RESULTS_LEDGER.md` history from earlier rows in an image-gradient sense, recorded in `q6_scan.json` and left unmodified. `SHA256SUMS` declares its byte domain on line 1 (lf for text, raw for binary). Every new file is <= 300 lines. Test: `python -m pytest tests/platformkit/test_g412_box_frame_contract.py -q` -- 11 passed. The PROPOSED diff carries non-ASCII bytes only inside verbatim `src/` context lines, as the landed G380 PROPOSED diff does.

## NOT VERIFIED

- Live integration: the receipt is proposed, applied nowhere, and no export has ever carried it.
- Physical court registration and the x_position / y_position map columns.
- Branch freshness: the receipt states representation and nominal padding only; it never asserts that a stored box was observed by the detector at that tick.
- Geometry quality of any producer box; the cards demonstrate the transform, not correct localisation.
- Training suitability of any export, and whether any other consumer already applies a compensating offset.
