# G402 preregistration

GAP: G402
LANE: a13
DATE: 2026-09-11
MODE: PREPARE-ONLY. This preregistration seals the diagnostic protocol and the acceptance bars. It authorizes no producer traversal, rating, inference, scoring, pod job, deploy, restart, register write, ledger write, or training call in this landing.

PREMISE BEFORE-CONDITION: Reproduce the G380 CLAMP/(CLAMP+SUBPIXEL) fraction and its attempt-level receipt/transport 30/30 while unchanged-column identity remains 0/30. Then independently require G397 disposition plus at least 30 unique retained-source sections of each actual kind, with bounded bytes and at least 10 s usable spans. A supply shortfall is PARTIAL before any GPU launch; it is not zero and no replacement draw is permitted.

PLANNED POPULATIONS AND DRAW: Sort each actual-kind population by competition/game/section/digest. Select ordinal j=0..29 with floor(j*(N-1)/29+0.5). Preserve all 60 planned sections, failures, rejections, unknown identities, distinct-game counts, and competition mix. No resampling, substitution, or label-conditioned window selection is permitted.

WINDOW RULE: For ordinal j, use usable_start+(j+1)*(usable_span-10)/31, retain exact decoded PTS boundaries and original frame-index mapping, use native pixels and rate, and cap a 30 fps source at 300 source frames or a 60 fps source at 600 source frames. The future finisher may run one sequential instrumented traversal per planned window, threads=1, with an aggregate 85-minute stage deadline.

FROZEN MASK: Set weight=0 for CLAMP, HELD, PREDICTION, SUBPIXEL, UNKNOWN, any repeated-coordinate target, and absent or invalid matched_event_id. DETECTION with a valid matched box is a candidate only. The exported mask has zero training callers. A repeated DETECTION remains masked.

FROZEN ACCOUNTING: Preserve attempted, evaluated, suspended, emitting, and zero-output ticks without reconstructing evaluated ticks from player rows. Name all decoded-frame and evaluated-tick denominators. Held pairs require byte-equal x/y for the same track on consecutive evaluated ticks only; no missing tick is bridged. Missing schedules remain UNKNOWN.

ACCEPTANCE BARS (VERBATIM): 30 complete unique windows EACH kind; all 60 planned accounted; exact receipt/transport agreement and decoded/evaluated distinction. Zero masked-class/repeated/unbound rows admitted; exact full-denominator coverage table; no minimum surviving yield. Every opened source/output retained; tables and delivered renders reproduce exactly twice. Any submetric with n<30 unique units is descriptive only.

EVIDENCE DESTINATIONS: census.csv, draw.csv, source_receipts.csv, window_pts.csv, route_hashes.json, launch_receipts.csv, raw_tables/, trace/, evaluated_ticks.csv, held_pairs.csv, provenance_counts.csv, target_mask.csv, coverage_per_section.csv, pixel_audit.csv, eye_index.csv, summary.json, repeats.json, renders/, common_receipts/, SHA256SUMS.

SIGN CONVENTION: No delta is calculated in this preparation landing. For any later comparison, improvement means baseline loss minus candidate loss; positive means candidate better.
SEAL sha256 0bfd1363cc6df3796e11894c7973062c2078ee9aa2c0951961b114d238cd4955
