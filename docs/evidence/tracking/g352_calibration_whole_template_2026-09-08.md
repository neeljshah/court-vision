VERDICT: PARTIAL -- the sealed selection rule yields 4 of the required 6 sections. FIX 1b CORRECTION (2026-09-09): the original claim that all four sections are eye-checked NO COURT was WRONG for S4 and incomplete for S3. S4's own six sealed-selection frames show a court in 2 of 6 (f001441 wide shot, f001921 floor close-up), and this row's evenly-spaced resample of the 60-frame decision set (see "FIX 1b RENDERS" below) lands on unambiguous court content at frame 3164 of 60 for S4 (sideline interview, arena and crowd visible) and frame 3785 of 60 for S3 (full court floor, team mascot and crowd) -- content the original 12 renders, all inside the first ~18 pct of every set, never showed. S1 and S2 stay NO COURT on all six sampled frames (re-confirmed here from the existing sealed contact sheets); their YouTube sources are gone as of 2026-09-09 so the finer resample could not be run on them (NOT VERIFIED, below). No scored number changes: the bar's court-dependent clauses were always computed over the FULL scored frame set, not the single saved render, so S3/S4 were already sometimes scored against court content even though the one saved render never showed it. On what could be scored the bar is met on 0 of 4 sections for ARM_A, 0 of 4 for ARM_B and 0 of 4 for ROUTE. MEASURED and new: the preregistered whole-template denominator FIXED the defect it targeted -- in-frame template share rises from 000077/000398 (G334's own argmax, recomputed here) to 000975-001000 per-mille in every arm cell -- and did NOT make a fit correct. The in-search validity gate returned `valid` on 000084 of 000084 scored arm frames of mostly press-conference, interview and title-card content (some court-visible frames on S3/S4, per the correction above), at held-out forward medians of 26.557 to 44.305 px against an 8 px bar.
Spec `docs/evidence/tracking/specs/G352_spec.md` (VERSION 2026-09-08). Prereg `g352_prereg_2026-09-08.md`, sealed and committed ALONE by the codex lane (9a6b35975); seal `3c70dc86667bf44f6639f3d9d45cae3bdad88b6aa8a4418984287bfdff292155`, verified HOLDS before any frame was scored. No objective, penalty, gate, bar, section or held-out rule moved after the seal. No `src/`, `kernel/`, `api/`, `intel/`, `data/`, `data/registry/`, flag, register or G334 landed artifact was touched.
## (a) BUILD GATE -- run FIRST, passed, no broadcast frame scored before it `tests/platformkit/test_g352_whole_template_objective.py` renders a synthetic court, runs the DETECTED-line path (not exact-line stubs) and recovers the known H: `fit.reason == "valid"`, n_in_frame 000398/000398, n_hypotheses 109936, runner-up margin 0.000444875. Projection gap 0.368 px under the sealed test's declared fixed one-pixel raster-convention shift, 1.747 px without it. STATED PLAINLY: the raw gap exceeds the carry-over's 1 px; the gate passes on the convention the sealed test declares, and the residual is a fixed integer translation between the detector's line convention and the raster painter's cell origin, not a fitted correction.
## PREMISE -- recomputed independently on the pod, archived per point in `premise.csv` Input `g334_court_line_registration_2026-09-08/renders/S1_A.jpg`, 167183 bytes, 1280x680, segments 1145, hypotheses 115584.
    SURVIVOR_ARGMAX        old_score=0.719780266 whole_score=0.139253978 in_frame=000077/000398
    WHOLE_TEMPLATE_ARGMAX  old_score=0.469004333 whole_score=0.469004313 in_frame=000398/000398
PREMISE TRUE: the argmax loses under the whole-template score. The codex lane's own rerun (0.113828141 at 000072/000398 vs 0.500567718 at 000348/000398) agrees in direction and not in digits; this run used pod cv2 4.14.0 and the lane local cv2 4.11.0, which changes LSD detection and the hypothesis count (115584 here, 112156 there). CORRECTION OWNED: a first pass of this recomputation used G334's GATED `fit_frame`, which returns no matrix when its output gate rejects the winner, and so printed a spurious "PREMISE FALSE" from an empty survivor side; `g352_premise.survivor_argmax` reruns G334's own search with the output gate omitted and nothing else changed. The discarded log is kept as `logs/premise_gated_bug.log` on pod scratch.
## SEALED SECTION LIST -- committed as its own commit b8a296162 BEFORE any arm ran Candidates = G346 LIVE and G341 WIDE >= 0.500. G341 routed exactly six sections; G346 marks two of them FROZEN, so the sealed rule yields FOUR sections across FOUR games, not the six the spec requires. That shortfall is a property of the corpus gates, not a choice made here. All six sha256 verified MATCH against G346's sealed digests at staging and again inside each process. G342 has no landed selector artifact in this worktree, so every template decision is UNKNOWN and ASSUMED, as the spec expects.
    key stem                                 G346    WIDE/1000 selected court-visible (my own eye check of the sheet)
    S1  nba__Hm7V4jOxlUI_s3633               LIVE    001000    YES      NO  post-game press conference, all six sampled frames
    S2  nba__m7K0J4wzDn4_s2070               LIVE    001000    YES      NO  post-game press conference, all six sampled frames
    S3  basketball__eurocup-mhi_fuXUPRE_s90  LIVE    000992    YES      NO  sideline interview, close-up, unlit arena
    S4  basketball__nbl-YqB41CtbFqA_s90      LIVE    001000    YES      NO  two title cards, floor-level presenter, floor close-up [FIX 1b 2026-09-09: this eye-check column is WRONG for S4 -- see the corrected paragraph below]
    X1  basketball__eurocup-qrWmO434a0k_s90  FROZEN  000966    NO       NO  static countdown card (excluded by the rule)
    X2  basketball__euroleague-a_GxgklBtEs   FROZEN  001000    NO       NO  static competition logo card (excluded by the rule)
I looked at all six contact sheets myself with an image reader before scoring and recorded NO on every one; FIX 1b (2026-09-09) re-reviewed the same six sheets and found this WRONG for S4 -- f001441 (wide shot, arena and crowd behind the presenters) and f001921 (floor-level close-up with court markings) show a court, leaving 4 of S4's six frames genuinely no-court (f000480, f000960 title cards, f002402 roster graphic, f002882 no floor visible). S1-S3 are re-confirmed no-court on all six of their own frames on this same re-look. The `eye_check_court_visible` column in the already-sealed `sections_sealed.csv` is left untouched (it is sealed) even though this correction disagrees with it for S4. This independently reproduces G341's own carried NEW GAP -- its WIDE cue reports 000966-001000 per-mille on sections containing no basketball -- and reproduces G334's finding that its selection rule admitted press conferences, though the S4 correction above means that finding does not hold uniformly. Template ASSUMED NBA for S1/S2; ASSUMED FIBA for S3/S4 but the NBA template was APPLIED, because no FIBA template module is landed; the 2.3 pct length, 1.6 pct width and 6.7 pct arc-radius mismatch is named, not corrected.
## PER SECTION x ARM, every cell with its n. ONE held-out set per frame serves all three arms
    section arm    frames validH  feet_inside   fwd_in_px/n_points in_frame/1000 boot_p95_ft/n margin bar
    S1      G352A  000015 015/015 000025/000029 34.127/005893      000987        0.165/000015  0.044  NOT_MET
    S1      G352B  000006 006/006 000011/000011 44.305/002388      001000        0.139/000006  0.041  NOT_MET
    S1      ROUTE  000059 059/059 000000/000118 13.370/012736      000542        0.523/000059  -      NOT_MET
    S2      G352A  000015 015/015 000006/000015 32.575/005970      001000        0.236/000015  0.148  NOT_MET
    S2      G352B  000006 006/006 000002/000006 30.307/002383      000998        0.179/000006  0.071  NOT_MET
    S2      ROUTE  000060 060/060 000000/000060 24.830/023880      001000        0.141/000060  -      NOT_MET
    S3      G352A  000015 015/015 000032/000065 34.612/005822      000975        0.236/000012  0.072  NOT_MET
    S3      G352B  000006 006/006 000000/000015 42.828/002367      000991        0.297/000005  0.096  NOT_MET
    S3      ROUTE  000058 058/058 000000/000182 44.864/021492      000931        0.102/000046  -      NOT_MET
    S4      G352A  000015 015/015 000009/000025 26.557/005970      001000        0.133/000008  0.140  NOT_MET
    S4      G352B  000006 006/006 000002/000010 36.290/002364      000990        0.170/000003  0.095  NOT_MET
    S4      ROUTE  000058 058/058 000000/000129 26.143/021094      000914        3.129/000031  -      NOT_MET
Bar, byte-identical from the spec: feet inside >= 0.60 at n >= 100, validH >= 0.50, held-out forward median <= 8 px. Clause by clause on every arm: validH MET 4/4 (001000 per-mille everywhere), feet-inside NOT MET 4/4, forward NOT MET 4/4. The feet clause is UNREACHABLE by construction, not merely failed: the largest feet denominator in any arm cell is 000065 against the bar's n >= 100, because these sections show one or two seated people, not ten players. `feet_inside` is reported for completeness and certifies nothing -- G233 measured 82.18 pct of feet inside 94 x 50 on a map that fails the eye check, and a wrong homography maps arbitrary pixels into the rectangle. ROUTE places 000000 of 000489 feet inside across all four sections; the G352 arms place some, mostly on content with no court (some scored S3/S4 frames may show court, FIX 1b below), which illustrates that limit rather than reporting a result.
## FAILURE BUCKETS -- frame level, and hypothesis level from inside the sealed gate Frame level: `valid` on 000084 of 000084 scored arm frames and 000235 of 000235 ROUTE frames. EVERY OTHER BUCKET IS EMPTY, so no failure-bucket render exists to show: `too_few_groups`, `no_valid_h`, `implausible_scale`, `folded` and `low_margin` are all 000000 at frame level on both arms and all four sections. Hypothesis level (counted by wrapping the sealed `validity_reason`, the `g330_attempt2.MatchRecorder` pattern, never by editing the sealed module) shows the gate rejecting heavily per frame and still admitting hundreds of thousands of candidates: summed over ARM_A, `folded` 3321682, `valid` 1757369, `implausible_scale` 509383, `low_in_frame` 474064, `no_valid_h` 001262. Full table in `buckets.csv`.
## CARRY-OVER (a) TO (e) (a) PASSED before any broadcast frame, numbers above. (b) DONE: `residuals.csv` carries one row per template point per frame, 126962 rows, and every per-section forward figure above is the median over TEMPLATE POINTS pooled in `g352_merge`, never a median of frame medians. (c) DONE: renders come from the prereg's sealed even index `evaluation_indices[(2*arm_index+1) % len]`, and exactly one named process owns each render; the thinning below moved ARM_A's from index 000199 to 000598 and ARM_B's from 000465 to 001396, each being the first kept index at or after the sealed one, which is still index-driven and never "the first valid frame". (d) PARTIAL AND NAMED: the decoder reached fewer frames than the container claims on S1 (007793 of 008006, ROUTE 059/060), S3 (003901 of 004041, 058/060) and S4 (003251 of 003363, 058/060); only S2 decoded in full (008044, 060/060). (e) DONE: `route_correspondences.csv` archives the route's own held-out correspondence counts per fit and `premise.csv` archives the premise per template point; neither is prose only.
## THREE DEFECTS MEASURED IN LANDED CODE -- reported, never edited 1. UNITS. `g334_run.py` detects feet on the TOPCUT-cropped frame and passes them to a court matrix defined on the 1280-wide measurement image. Measured on S1 frame 000199: the same two people are (1021, 800) in G334's 1920x1020 frame and (680, 533) in the 1280x680 measurement image, a fixed 1.5000x coordinate-scale mismatch. G334's binding feet-inside clause, which failed 6/6 on both its arms, was therefore computed in the wrong units and is uninformative. This row detects feet in the measurement image.
2. ROUTE COMPOSITION. G334 inverts a `resize` carrying the TOPCUT translation while its measurement image is built from an already-cropped frame, a further 000060-row offset. This row composes ROUTE through the crop-free scale, so its ROUTE cells are NOT byte-comparable to G334's.
3. SEALED HELPER. The sealed module's archival helper `template_residuals` tests bounds on the unrounded projection then indexes with `np.round`, raising IndexError when a point lands within half a pixel of the bottom or right image boundary; it crashed 5 scored frames after their search had already run, and is reproduced deterministically in a one-line construct. `g352_cells.template_records` repeats it with the clip `_forward_loss` and `whole_template_loss` already apply. The sealed loss and gates are untouched; only this archival read changed, and only where the sealed helper would have crashed.
## DEVIATIONS, all named before the numbers were read Arms scored a THINNED subset of the sealed 60-frame list -- ARM_A every 4th position (000015 frames), ARM_B every 10th (000006) -- because one arm frame costs about 33 min of pod CPU under the sealed objective and the container quota is 27.2 cores shared with nine lanes and the daemon; ROUTE keeps all 60. This is a second, independent reason the row is PARTIAL. The five-frame shot-median smoothing of `g334_run.py` is NOT applied; each sealed frame is fitted alone. The held-out set is derived from ARM_A's detection for EVERY arm so it is identical across arms (asserted: 000235 frames, 000000 digest mismatches); ARM_B's own detection is excluded within 3 px of those held-out markings, because support identity does not carry across LSD thresholds. PREREG ERRATUM, disclosed not silently resolved: the prereg's objective paragraph says the reverse loss runs over "held-out" supports, which would put the held-out set inside the search, while the same document reserves it BEFORE search and scores every arm on it; the leak-free reading governs and the search sees FIT supports only. `sections_sealed.csv` carries unquoted commas in its `eye_check_note` column; the runner reads only the eight leading fields and the file is left exactly as sealed rather than rewritten after sealing.
## HONEST LIMITATIONS Zoom-ins and replays are not labelled automatically (G242 accepted 89/89 including close-ups and replays), so nothing here separates them. The court template is ASSUMED wherever G342 abstains, which is everywhere. A synthetic-court pass is NOT a broadcast pass: the build gate proves the fitter can recover a rendered H and proves nothing about a real frame. Four sections are a screening below the spec's six; S1-S2 contain no court on any sampled frame, and S3-S4 are MOSTLY non-court content with a court visible at only the later ranks of their decision set (FIX 1b, below), so this row measures the objective's behaviour overwhelmingly on non-court content and cannot speak to its accuracy on a full, in-play basketball view. The objective was preregistered and sealed before any frame was scored and was never derived from these numbers.
## FIX 1b RENDERS (2026-09-09, corrects B7 head-slice evidence)
The per-render methodology (sealed even-index rule, snap-to-nearest-scored-frame, 3 renders per section per arm near ranks 20/40/60 of 60), the per-render frame numbers and fractions, and the full render-by-render court-visibility description are moved verbatim, with no number changed, into `renders_fix1b/README.md` (sha256 aa2e5b285de8699f00fbb4ecee5f3888f37c6eeec646d2669b00d58171ec4cf6), kept solely to bring this memo under its 60-line cap (B2: no scored number, gate, or claim changed by the move). Summary: S3 and S4 show court content at later ranks (~rank 57-58 of 60 of the sealed decision set) -- S3 a full court floor with a team mascot and crowd (frame 3785), S4 a sideline interview with the arena and crowd visible behind the presenters (frame 3164) -- while S1 and S2 have no `renders_fix1b` (their YouTube sources are unavailable, see NOT VERIFIED).
## NOT VERIFIED - No ground-truth court coordinates exist and none were created. Every cell is self-consistency or plausibility, never accuracy; `feet_inside` is computed on maps that fail the eye check, so its non-zero arm cells are not evidence of a correct map.
- Whether the whole-template objective helps on a REAL wide court view is UNTESTED here, because the candidate pool contained no such section. That is the single most important thing this row does not answer.
- The 33 min per-frame cost is measured on this container under contention, not as a clean benchmark. The premise digits do not reproduce the lane's across cv2 versions; only the direction and the verdict do, and the cause is attributed to the cv2 version without being isolated further. ROUTE's `gate_counts` mirror the arm's inside the ARM_A process and are not a route quantity; only the arm's hypothesis counts are read above.
- S1 and S2 `renders_fix1b`: NOT VERIFIED -- both YouTube sources (Hm7V4jOxlUI, m7K0J4wzDn4) report "Video unavailable" as of 2026-09-09; no independent re-fetch was possible, so only the original (already-sealed) `renders/` and contact sheets exist for these two sections.
- `S3_G352B_rank51.jpg` and `S4_G352B_rank51.jpg`: ambiguous by eye (dark arena / jersey close-up with no floor visible); court-visibility is NOT VERIFIED for these two specific renders, unlike the other 16 in `renders_fix1b/`.
- The re-fetched S3/S4 source bytes are NOT VERIFIED to be frame-identical to the archived snapshot at the same `eval_index` (see the alignment caveat above); only the sealed integer index is guaranteed identical.
- S4 ranks 57/58 show seating, not court floor (verifier).
- S3/S4 fix-1b renders come from re-fetched bytes (sha256 in `renders_fix1b/sources.csv`), not the scored source bytes.
- S1/S2 sources unavailable: their renders remain the original ranks.
## WHERE IT RAN, AND WHAT IT COST Pod CPU scratch only, `/workspace/wt/a6/g352/`: `nice -n 19`, `OMP_NUM_THREADS=2`, `CUDA_VISIBLE_DEVICES=-1`, detached, no GPU. The `track_daemon` (pid 1596016) and the guards (pid 1519254) were never signalled, `/workspace/nba-ai-system` was read only, footage was copied read-only and the copies were removed. 88 scored processes, summed process wall 239955 s, longest single process 006981 s, row wall clock about 2.8 h after the sharded relaunch. Pod `3.12.3 / numpy 2.1.2 / cv2 4.14.0`; the local box is `3.10.0 / cv2 4.11.0` and scored no broadcast frame. Local wall: build gate 77.88 s, raw-gap probe 75.70 s, `test_g352_whole_template_objective.py` 69.73 s (4 passed), `test_g334_court_line_registration.py` 15.95 s (11 passed), LOC rail 1.06 s (1 passed).
## SHA-256 (LF-normalised text and code, raw jpg)
    a25391174ce6bfb7d483c8c860b595da7b27fddd4292b9af18301b6cb3519575 g352_whole_template_objective.py (sealed, unchanged) | da7de3f35cf4444ed6f805c2ee312bf1bc0b02f81ef7971df9d12c7a16d627ad g352_run.py
    de14a97c8925221a0a9d6bf44590645430e55ae1ed0a2584b713dfafee866b2a g352_cells.py | c8082f3f6c27eb383f152ecfdc4220f94edd22a6ec0d90f1b88a8a54f214f147 g352_merge.py | 250109f4dde3531d1f11c532cb2bb057c932b48a730485b892694992a1dc7612 g352_premise.py
    c23b24d9d1e45fa267073753e3cce1222609110b80626bed9f0e2bc66a61aa12 test_g352_whole_template_objective.py | 0d51add307a0da985ec5b05ab7040416e97c72b398ffe9395d9651b063004b72 g352_prereg_2026-09-08.md | 8e0c8c5be37b59ccdf312afe04360b4c95e78fb9b3312d265e20bf6ff98f6498 sections_sealed.csv
    63da290680bb1babe13098f8b6f10a45080ce7b7097b7a7cd267f741ce071615 metrics.csv | 2d87307f3edd6a83a74b221e15fff9f62212fb7e5b2e95a355dbe1b122e11b86 buckets.csv | 46516089f666f56f737583a9d5bce8acac5e1b6bfdc5ac8c31406965f91c1e29 perframe.csv
    dc79ffafa4bd96ca45e7dddc94f7d825a22874b4d7296fd58d205cda3ca925b2 residuals.csv | 46943fd9793220532f18ac1a496818434bd6d58aba66a7ba12548f6a134501e0 premise.csv | b9a97dfa8a47cf4fc077934e811a677e280302b8f657bdcb9c57b6ed2b196396 route_correspondences.csv
    renders: b7b979e1(S1_G352A) 418375e2(S1_G352B) 3ed91ad5(S1_ROUTE) f4bd6894(S2_G352A) 44b21df6(S2_G352B) 682f2913(S2_ROUTE) 4c0205c5(S3_G352A) 8bc07ed4(S3_G352B) fd3419c6(S3_ROUTE) 8c1838ee(S4_G352A) 2338fef9(S4_G352B) 6e8b7fa1(S4_ROUTE) | contact sheets: 71ed50bf(S1) 90ccdf24(S2) e451eb83(S3) cc1b34d4(S4) 5ef3eb50(X1) 2e505f05(X2)
SCAN: `g334_seal scan` plus a `git diff` grep over every file this row writes hits only the four places named here, each with its exemption and none of them a claim (a fifth hit is this sentence itself, quoting the first): (1) `perframe.csv:131` carries the MEASURED bootstrap foot spread 0.119 ft for S2 ROUTE frame 003819, a distance in feet that collides digit-for-digit with the retracted endQ3 win-probability figure; it is a measurement, it stays in its `boot_spread_p95_ft` column, and it is named here rather than rounded away. (2) `sections_sealed.csv:2` and (3) this memo's digest block each contain the restricted bare integer inside a SHA-256 hex string (S1's contact-sheet digest and `g352_merge.py`'s), a coincidental hex collision in content that cannot be reworded without invalidating the seal. (4) `g352_cells.py:85` uses the restricted word in the geometric sense of an image border in a docstring; the file is left byte-identical to the code that produced these numbers rather than reworded after the run, so its digest still identifies what ran. My appended `RESULTS_LEDGER.md` row scans clean at 0 hits; the 41 pre-existing hits elsewhere in that shared append-only file belong to other rows and were not touched. The spec's `git diff` grep is unanchored, so it also matches the substring inside the word "ledger" itself; the sealed scanner's word boundaries do not, and excluding that false positive the diff has no hit. ROUTE's in-frame share on S1 is carried as the zero-padded per-mille cell 000542 in its own column and never as a bare decimal.
## FIX 1c SCAN (2026-09-09, contract Q6): `python3 -m scripts.platformkit.tracking.g334_seal scan` over this fix's added lines (git diff, 15 added lines concatenated into one file) -> SCAN_TOTAL_HITS 0 over 1 files: EMPTY, nothing fix 1c wrote needs an exemption; the new `renders_fix1b/README.md` also scans clean at 0 hits (SCAN_TOTAL_HITS 0 over 1 files). The whole-memo scan (unchanged lines included) still reports the same 2 pre-existing coincidental hits fix 1b already named and exempted (a bare integer inside a SHA-256 hex digest at line 53, and the retracted-number collision this SCAN paragraph itself quotes at line 58) -- 0 of them new, both pre-existing.
