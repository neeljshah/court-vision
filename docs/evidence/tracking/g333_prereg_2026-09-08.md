# G333 PREREG -- apply the four landed PROPOSED producer diffs (sealed before any src byte moved)

GAP G333 | sport all | worktree a1 (branch track-a1) | spec `docs/evidence/tracking/specs/G333_spec.md`
VERSION 2026-09-08. Written and committed ALONE before a single byte under `src/` changed. Nothing in
this file is a new measurement: every figure is a target, a fixed design constant, or a number already
committed by the G320 / G325 / G330 / G331 rows.

SEAL RULE: the last line of this file is `SEAL sha256 <hex>`. The hex is the SHA-256 over every byte
ABOVE that line with CRLF normalised to LF, the trailing newline of the final body line included.
Hexadecimal digests are single tokens; a two-digit run inside one is not a standalone figure.

AUTHORISATION: the user authorised these four `src/` edits on 2026-09-08 for this row only. Nothing
else under `src/` is touched, and `kernel/`, `api/`, `intel/`, `scripts/team_system/` are not opened.

## 0. Diff sources, digest-verified before planning

    diff  source file                                                            sha256 prefix
    G330  docs/research/organization-sprint/G330_PROPOSED_pano_cache_key.md       69d50cda...
    G320  docs/research/organization-sprint/G320_PROPOSED_ball_inferred_coord.md  1fc07bd2...
    G331  a6:docs/research/organization-sprint/G331_PROPOSED_evaluated_frames.md  ba00c0dd...
    G325  docs/evidence/tracking/PROPOSED_g325_drop_wholly_offframe_2026-09-07.md 8539de47...

Each is compared against the digest its own row memo recorded: the G330 attempt-2 memo digest table,
the G320 memo PROPOSED-DIFF section, the G331 memo route-null paragraph, and the G325 attempt-2 memo
artifact digest table. The full-hex table, with the byte counts, goes in the memo.

## 1. The four defects, and the construct that reproduces each BEFORE the edit

D1 (G330). `_scan_and_build_pano` substitutes the general fallback image when the stitch is invalid and
then writes THAT image to the per-video cache path, so a later run reports a per-video cache hit for a
foreign panorama and never retries the stitch. CONSTRUCT: drive the real `_scan_and_build_pano` with a
stub video capture, a stub detector and a stub `collage` that returns an image too small to be a valid
panorama, against a temporary resources root holding a valid `pano_enhanced.png`. On the pre-edit code
a per-video cache file MUST appear. Second construct print: list `resources/panos/` in this worktree
and report how many of its files are byte-identical to `resources/pano_enhanced.png`, with n.

D2 (G325). The unclamped Kalman coasting prediction (`src/tracking/advanced_tracker.py:132-137`) is
written for up to MAX_LOST=90 frames even when the box lies wholly outside the decoded frame: 23408 of
480158 player observations across 122 pod games (G325 attempt 2). Such a box has no pixels, so its
footpoint and team label are derived from nothing. CONSTRUCT: on the pre-edit code the row-build site
has no frame-overlap guard at all, so the print is the absence itself -- assert by parse that the
statement following `bbox = track["bbox"]` is the row append and not a guard, and that the helper this
row adds does not yet exist. The empirical reproduction is the section smoke, arm BEFORE.

D3 (G320). `ball_inferred` is written from the detector attribute alone, so a row whose projected
coordinate was REJECTED (and is therefore blank) can still carry the flag set. CONSTRUCT: parse
`unified_pipeline.py`, take the `ball_inferred` value expression out of the ball-row dict literal
verbatim, and evaluate it with `ball_pos = None` and a stub whose `ball_inferred` is True. On the
pre-edit code it MUST evaluate to 1.

D4 (G331). `evaluated_frame_count.json` carries a null count with the reason
`max_frames_is_detector_dependent_in_this_route` whenever a frame cap is supplied, although the
pipeline holds the exact detector-gated count when it returns. CONSTRUCT: call the publisher with a
stub route result and a sidecar file holding the null; on the pre-edit code the publisher does not
exist (import fails), and the route sidecar from arm BEFORE of the smoke carries the null.

A defect that does not reproduce is reported CLOSED ALREADY and its diff is skipped.

## 2. Edit plan, exact and file:line (line numbers as of the prereg commit)

E1 (G330), `src/pipeline/unified_pipeline.py`, +3 lines net:
  - insert `used_fallback = False` immediately before :969 `if not UnifiedPipeline._pano_valid(pano):`
  - insert `used_fallback = True` immediately after :980 `pano = _fb_img`
  - amend the existing print at :981 IN PLACE to name the non-caching (no line added)
  - replace :988-989 (`cv2.imwrite(out, pano)` and the "Pano saved" print) with a three-line
    `if not used_fallback:` block that keeps both statements under the guard
  Intended effect, stated: a run that used the fallback writes NO per-video cache file. A run that
  legitimately stitched still caches, byte-identically to today.

E2 (G325), `src/pipeline/unified_pipeline.py`, +6 lines net:
  - add a staticmethod `_bbox_off_frame(bbox, frame_w, frame_h) -> bool` immediately before :3315
    `_dist_to_basket`, returning True when the box has no overlap with the frame rectangle
    (`bbox[3] <= 0 or bbox[1] >= frame_w or bbox[2] <= 0 or bbox[0] >= frame_h`), bbox being
    `(y1, x1, y2, x2)`; a falsy bbox returns False (4 lines including the separating blank)
  - insert a two-line guard after :2691 `bbox = track["bbox"]` calling that helper with
    `frame.shape[1]` and `frame.shape[0]` and `continue`-ing. `frame` at that site is already
    TOPCUT-cropped (:1689) and is never rebound between :1689 and the row build, so its shape is
    exactly the SOURCE width and the post-TOPCUT height the proposal names. This is the "thread the
    decoded frame width in" step the proposal declared as its only non-mechanical part.
  Intended effect, stated: rows disappear. This is the diff's purpose, not a regression. Every
  denominator computed from `tracking_data.csv` row counts moves down. The track keeps coasting
  internally, so the identifier survives re-entry.

E3 (G320), `src/pipeline/unified_pipeline.py`, +0 lines net:
  - replace :1991 in place with the proposed form gating the flag on `ball_pos is not None`.
  Intended effect, stated: the written flag value changes from 1 to 0 on rows with no coordinate.
  That is the diff, not a bug. No column is added, renamed or removed.

E4 (G331), `src/pipeline/unified_pipeline.py` +1 line and `scripts/run_clip.py` +10 lines:
  - insert `"evaluated_frames": gameplay_frames,` after :3049 `"total_frames":   frame_idx,` in the
    `run()` return dict; `gameplay_frames` is the detector-gated counter incremented at :2069
  - add `_publish_evaluated_frames(sidecar_path, results)` to `scripts/run_clip.py` and call it
    immediately after :508 `results = pipeline.run()`. It rewrites the sidecar with the post-run
    count and clears `reason` ONLY when the count is not None, so the reason is retained when the
    count is unavailable. It is wrapped so that a sidecar failure can never lose a finished run.
  Intended effect, stated: `evaluated_frames` stops being null on a capped run. Every existing
  sidecar key keeps its name and meaning; `formula` is not touched.

BUDGET: `src/pipeline/unified_pipeline.py` grows by 10 lines, under the 12 allowed.
`tests/platformkit/test_loc_rail_scope.py` walks only `scripts/platformkit/**` and is unaffected;
it is run anyway and must stay green.

## 3. Test cases -- `tests/platformkit/test_g333_producer_diffs.py`

T1 G330 fallback: stubbed route, stub `collage` returns an image too small to be valid -> the general
   fallback is substituted -> assert NO file at `_auto_pano_path(video)` and the returned image has
   the fallback's dimensions.
T2 G330 stitched: same stubs, `collage` returns a valid wide panorama -> assert the per-video cache
   file EXISTS and reads back valid.
T3 G330 local census: count the files in `resources/panos/` byte-identical to
   `resources/pano_enhanced.png`, reported with n. Informational, no assertion on the count.
T4 G325 helper: a box wholly left of, right of, above and below the frame each return True; a box
   straddling any frame boundary returns False; a fully interior box returns False; None returns False.
T5 G325 re-entry: a synthetic trajectory that leaves the frame and comes back, filtered by the helper,
   emits the same identifier before and after the gap and emits no wholly-off-frame sample.
T6 G325 shipped guard: parse `unified_pipeline.py` and assert the statement following the
   `bbox = track["bbox"]` assignment is an `if` whose body is a single `continue` and whose test calls
   `_bbox_off_frame`. This is what fails on the pre-edit file.
T7 G320: parse the ball-row dict literal, take the `ball_inferred` value expression verbatim from the
   shipped source, evaluate it with `ball_pos = None` and a stub flagged True -> 0; evaluate it with a
   coordinate and the same stub -> 1. This is what fails on the pre-edit file.
T8 G331: write a sidecar holding a null count and the capped-route reason, call
   `_publish_evaluated_frames` with a stub result carrying 7 gameplay frames -> the file reads back
   `evaluated_frames: 7` and `reason: null`, with every other key unchanged.
T9 G331 reason retained: same sidecar, stub result with no count -> `evaluated_frames` stays null and
   the reason string is unchanged.

ALSO RUN, each file alone, never a full pytest: every test file in `tests/` that names
`unified_pipeline` or `run_clip` (the union, enumerated in the memo with n), plus
`tests/platformkit/test_loc_rail_scope.py`.

## 4. Section smoke design -- one clip, a screening, not a census

SOURCE, sealed here: `nba__0022500081_s2436.mp4` from the pod corpus, 1280x720 h264, 30/1 fps,
4021 frames, 33120099 bytes, sha256 `0ec016b7608ad7606e1dbf6d1695692efd455df5079bcdf8c94d97f6bacd1608`.
Copied read-only to a local scratch path and deleted after the second arm. The pod is otherwise
untouched: no daemon, no guard, no write, no process signalled.

OFFSET AND STRIDE, sealed here, per B7: start at SOURCE FRAME 1200 (40 s in), NOT frame 0, and let the
route's own fixed stride of 3 apply (`_FRAME_STRIDE` is 3 and the clip's 4021 frames exceed the
`_FRAME_STRIDE_THRESH` of 3000, and 30 fps is not above 35, so the route selects 3 with no override).
Cap: 150 gameplay frames, which at stride 3 spans about 450 source frames. Both arms use the identical
video bytes, offset, cap and command; the ONLY difference between them is the four diffs.

ARMS: BEFORE runs at the prereg commit, with `src/` untouched. AFTER runs on the edited tree. Between
arms every per-video panorama cache file the BEFORE arm produced is hashed, recorded and removed, so
the AFTER arm cannot inherit a cache hit.

REPORTED, each with its n: (a) wholly-off-frame player rows and total player rows for both arms,
recomputed from `tracking_data.csv` with the same frame rectangle the guard uses; (b) the
`resources/panos/` listing before and after each arm, so the fallback-not-cached effect is visible;
(c) `evaluated_frame_count.json` `evaluated_frames` and `reason` for both arms; (d) wall time and peak
resident set size per arm.

RAM GATE: available memory is checked immediately before each arm and must be at least 2500 MiB; peak
resident set size must stay under 1500 MiB. If the gate does not hold, the arm is NOT RUN and says so
under NOT VERIFIED rather than being run degraded. The smoke is corroboration and is NOT a gate on the
verdict, because it is one clip.

INTERPRETER: the conda `basketball_ai` interpreter is used for every command and its version line is
printed in the memo. This box carries more than one interpreter and the other one has a broken numpy
binding, so the line is evidence, not decoration.

## 5. Verdict rule, fixed now

DONE if and only if all four diffs are applied AND each has at least one construct test that fails on
the pre-edit file and passes on the edited file AND every enumerated existing test file of a touched
module stays green AND `unified_pipeline.py` grew by no more than 12 lines AND zero register edits.
PARTIAL otherwise, naming each diff that could not be applied and why. A defect that does not
reproduce in step 0 is reported CLOSED ALREADY and does not by itself make the verdict PARTIAL, but is
named in the memo. The section smoke result, whichever way it comes out, does not move the verdict.

## 6. Must not move

`data/`, `data/registry/`, every feature flag, every threshold, every default outside the four diffs,
the pod (read-only) with its daemon and guards, every committed artifact and its hash,
`docs/evidence/tracking/TRACKING_GAPS_2026-09-01.md`. Exactly one LF row is appended to
`docs/evidence/tracking/RESULTS_LEDGER.md`, in the row commit, with a single append redirection.

## 7. Honest limitations, stated now rather than discovered later

These diffs remove known-bad rows and a foreign-panorama cache entry. They do NOT make registration
work: the G330 held-out inlier ratios stay near 1 part in 100 until a real court calibration exists,
which is a separate row. The smoke is one clip and one offset -- a screening, not a census. Nothing
here is a quality claim about any prediction, and no threshold moves. Eye check: NONE.
SEAL sha256 4b3764fe5ac7d833336561f85437014bff65d7789069615da60af88a8329dcc4
