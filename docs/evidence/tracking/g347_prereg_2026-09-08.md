# G347 Preregistration

Date: 2026-09-08. Scope: local environment only; no packages, route files, data, pod, or flags change.

Contract: `docs/evidence/tracking/VERIFIER_CONTRACT.md`, sections A and B. This is a four-cell environment construct, not a scored comparison.

Binding before-condition, run by the finisher after this preregistration is sealed: `C:/Users/neelj/AppData/Local/Programs/Python/Python310/python.exe -c "from ultralytics import YOLO"` and `C:/Users/neelj/AppData/Local/Programs/Python/Python310/python.exe -c "import scripts.platformkit.env_shim as s; s.prepare_route_imports(); from ultralytics import YOLO"`. Save and print both command tails. If the after command succeeds, the premise is FALSE: stop, write the memo, commit, and report PREMISE FALSE.

If the premise holds, the finisher runs the fixed 2 x 2 construct in both named interpreters: `from ultralytics import YOLO` and `import src.tracking.osnet_reid`, before and after `prepare_route_imports()`. Record `before_ok` and `after_ok` as 000000 or 000001, the sidecar action, and traceback references. The action is no-op only when an untouched route probe succeeds; otherwise the ONNX preflight can install its narrow placeholder after its documented failure.

Decision rule: DONE only if no cell regresses (`after_ok >= before_ok` in every cell), conda remains 2/2 after, both focused tests pass, and no packages were installed or removed. Otherwise report PARTIAL and name every failing cell and reason. EYE CHECK: NONE.

The finisher alone runs `C:/Users/neelj/AppData/Local/Programs/Python/Python310/python.exe -m pip show protobuf onnx` to name the exact installed versions in the unexecuted pin proposal. The shim is a workaround, not a repair; the pin is proposed, not applied; the system torchreid/numpy 2 failure is outside scope unless the pin resolves it.

Focused tests, each alone after the preregistration commit: `C:/Users/neelj/anaconda3/envs/basketball_ai/python.exe -m pytest tests/platformkit/test_g347_env_shim_no_regression.py -q -p no:cacheprovider` and `C:/Users/neelj/anaconda3/envs/basketball_ai/python.exe -m pytest tests/platformkit/test_g340_env_shim.py -q -p no:cacheprovider`. No full test run.
SEAL sha256 97f525f09511027f6d4592bdb68f40e2a2a4790183d4ce6731b42bdfecd5fcbf
