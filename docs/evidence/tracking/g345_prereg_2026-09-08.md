# G345 preregistration - fixed-detection tracklet continuity (2026-09-08)

Scope: preparation only in C:/Users/neelj/nba-track-a21, LOCAL conda basketball_ai; no video, GPU, pod, src, data, registry, flag, or detector run. The finisher alone regenerates absent G336 fixed-detection CSVs and measures them.

Binding input schema: exactly four CSVs, one per section and 240 frames each, with section, frame, score, bbox_x1, bbox_y1, bbox_x2, bbox_y2 and no track-id field. Before association the finisher prints per-section frame count, detection count, detections-per-frame median/p10/p90, and SHA-256 digest; all three arms must assert the same digest.

Arms use only fixed boxes and confidence. ARM_A is the production route's Hungarian plus Kalman geometry step because its full matcher cannot run without frames; ARM_B imports the G336 variant only to confirm availability, then uses its geometry-only IoU form because the fixed schema deliberately supplies no appearance input; ARM_C uses high-confidence first then low-confidence IoU matching. No appearance model or crop channel enters any associator. IDs are section:generation:k; generation is the sealed invocation value and a gated reconnect retains its ID.

ARM_C constants: high confidence >= 0.35; immediate-link IoU >= 0.30; low-confidence-link IoU >= 0.30; reconnect age 30 frames; reconnect IoU-of-prediction >= 0.30; one-to-one Hungarian assignment prevents another tracklet claiming a reconnect box.

Injection protocol: sealed seed 3450908. ARM_C clean-stream tracklets define the reference candidates. Generate exactly 100 gaps, deleting one reference tracklet box sequence for k uniformly selected in [3,20] frames, and exactly 100 crossings, swapping two reference boxes' positions for 5 frames. Each injection is scored independently; reconnect correctness means the arm's ID after its deleted interval equals its ID before; crossing score counts simultaneous merges.

Metrics: starts per 1,000 detection-supported adjacent pairs; coverage = linked observation-seconds / total detection observation-seconds; simultaneous merges = two detections in one frame assigned one ID; mean tracklet length. Every metric cell records its named n. Reserved ZNCC evidence is grayscale 32x64 crop correlation only, never an associator input; if frames are unavailable, it is NOT RUN.

Decision: propose ARM_C for a src change only when starts fall >=50 percent versus ARM_A on >=3 of 4 sections, coverage decreases by <=2 percentage points, reconnect correctness >=0.95, and injected simultaneous merges equal 0. Otherwise report the measured gap. Improvement = baseline loss minus candidate loss; positive = candidate better. This row has no loss comparison in preparation.

Contract: docs/evidence/tracking/VERIFIER_CONTRACT.md section B self-check applies; no scored comparison occurs before this seal.
SEAL sha256 6b1c9e94305f575f641f760c775e5fe68ca217ec6d28f10b901a2e7585112709
