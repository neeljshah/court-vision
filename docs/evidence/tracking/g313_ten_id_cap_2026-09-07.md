# G313 -- the ten distinct player_ids are a fixed slot pool, not a count (2026-09-07)

VERDICT: PREMISE HOLDS, CAUSE NAMED, CONSTRUCT REPRODUCES 10/10. Verified codex-sol: ACCEPT WITH CORRECTIONS
(`G313_VERIFY_2026-09-07.md`); all three are applied below. Prereg sealed BEFORE any count or trace:
`g313_prereg_2026-09-07.md`, SHA-256 `5cb2a5da7a37186b967dec37807cc931a92e456b2cc5e5e141aac51cc6f121da`.

PREMISE (binding, 3 games fetched pod read-only 2026-09-07 from
`/workspace/nba-ai-system/data/tracking/<game>/tracking_data.csv`), distinct `player_id` over all rows:
`den_phx_2025` (nba, stride 6, 720p; 1,178,187 B, mtime 21:48 UTC) 2,972 rows -> **10**;
`wnba_02` (wnba, 3, 720p; 2,398,304 B, 21:27) 6,287 rows -> **10**;
`ncaa_basketball_sRtHQbywiTE` (ncaa, 3, 720p; 640,087 B, 21:14) 1,653 rows -> **10**. HOLDS, and sharper than
the census: the ids are not arbitrary tracker keys but literally `1..10`, a dense slot numbering, and slot `0`
(referee) produced no row in any of the three.

CAUSE, with file:line:
- **`src/pipeline/unified_pipeline.py:821` `_build_players`** builds a FIXED pool of eleven `Player` objects --
  ids 1-5 team `green` (:826-827), 6-10 `white` (:828-829), 0 `referee` (:830) -- constructed once at `:669`,
  handed to the tracker at `:672`; the emitted column is that slot's id (**`:2028`**, `"player_id": p.ID`).
- Both matchers are bounded by the pool PER TEAM, so the real ceiling is 5-per-team, not 10 global:
  **`src/tracking/advanced_tracker.py:908`** (custom two-stage) and **`:1047`** (supervision). A detection with
  no free same-team slot is left UNMATCHED by the matcher (`:1061-1062` returns it).
- The TERMINAL loss is **`src/tracking/advanced_tracker.py:1604-1609`**: `truly_new` detections take the first
  free same-team slot and, when none is free, the inner `for` simply ends -- **no counter, no log, no column.**
- ELIMINATED by reading, not guessing: the OSNet gallery is a slot-keyed `Dict` with no size bound (`:297`);
  `_warmup_top_k = 10` (`:328`) is a per-slot crop budget, not an id budget; no top-N detection cut and no
  `max_players` constant exists in `src/tracking/`.

CONSTRUCT (`scripts/platformkit/tracking/g313_slot_cap_construct.py`, driving the REAL `_match_team_bytetrack`
on synthetic well-separated full-confidence detections, Kalman predictions seeded so the cost gate cannot be
the limiter). Offered per team -> distinct ids (matcher-unmatched): **3 -> 6 (0); 5 -> 10 (0); 8 -> 10 (6);
12 -> 10 (14)**, matched 5 green + 5 white in every saturated arm, ids exactly `1..10`. "Matcher-unmatched" is
the matcher's own `unmatched_dets` return; **this construct does not execute the terminal loss at
`:1604-1609`.** The count follows the detections to five per team then stops dead, so **10 is a ceiling, not a
constant** -- on the pod games' own ids.

PROPOSED DIFF (the fix is in `src/`, human-gated -- **APPLIED NOTHING**):
`docs/research/organization-sprint/PROPOSED-slot-pool-saturation-counter-2026-09-07.md` (local-only, gitignored)
proposes COUNTING the drops, **not widening the pool** -- five on-court players per team is the correct
basketball number and a wider pool would absorb coaches, benches and false positives into real ids. The count
is not the problem; the silence is.

TEST: `tests/platformkit/test_g313_ten_id_cap.py`, **3 passed** (conda env `basketball_ai`; the base interpreter
cannot import `src/` -- numpy 2.2.6 breaks the torch/tensorboard chain). n = 1 (CONSTRUCT), driven four ways;
that one file only, no full pytest.

**NAMING THE CAUSE IS NOT FIXING IT, AND LIFTING THE CEILING IS NOT TRACKING BETTER.** Raising the id count can
lower identity quality. Not recall, precision, identity accuracy or registration; image space only; all 13
census games stay `passed = false`; no threshold or default moved.

## NOT VERIFIED
- **Three games were counted; ZERO were traced end to end.** The construct proves the mechanism exists and
  saturates at 10; it does NOT prove this mechanism produced the 10 in any particular pod game, and the other
  10 completed census games were not re-read.
- **Which matcher the pod runs was not measured.** `_use_sv` (`advanced_tracker.py:1524`) picks between the two
  paths at runtime and the construct drives only `_match_team_bytetrack`; both are bounded by the same pool by
  reading, but only one was executed.
- The referee slot (id 0) emitted no row in the three games; whether it ever does was not checked.
- No video decoded, no frame rendered, no label read: **no eye check exists**, and no evidence the ten slots
  hold the right ten people -- or ten people at all.
- The drop RATE is unmeasured: nothing counts how often the pool saturates on real footage, which is what the
  proposed counter exists to answer, so the operational impact of this cap is UNKNOWN.
- `distinct_track_ids`, `id_churn_per_detection` and `median_track_len_rows` in G309 are now known to read a
  bounded quantity; **not retracted, but not readable as a count of tracked people.**
