# Intelligence brief -- soccer

Generated by `scripts/platformkit/intelligence_brief.py` from artifacts on disk.
As-of clock (newest input artifact mtime): 2026-09-01T00:13:47Z. No edge, ROI or profit claim is made in this brief.

## 1. Tracking status

Reports on disk: 1. Pass rate: 0/1 (the `passed` field of each file).

| report file | n_frames | passed | worst failing metric (verbatim) |
| --- | --- | --- | --- |
| `data/tracking_reports/soccer/soccer_wc22_bra_cro.json` | 263 | False | `jump_p95 51.7 > 8.0` |

Pending fixes are exactly the `failures` entries below; nothing else on disk.

- `ball_valid 0.00 < 0.2` -- `data/tracking_reports/soccer/soccer_wc22_bra_cro.json`
- `coverage 0.00 < 0.85` -- `data/tracking_reports/soccer/soccer_wc22_bra_cro.json`
- `jump_p95 51.7 > 8.0` -- `data/tracking_reports/soccer/soccer_wc22_bra_cro.json`

## 2. Model status

| source file | field | value (verbatim) | occurrences |
| --- | --- | --- | --- |
| `data/ab_reports/window_strategy_replay.json` | `calibration_oos.soccer_intl.folds[].status` | `OK` | 5 |
| `data/ab_reports/window_strategy_replay.json` | `calibration_oos.soccer_intl.note` | `ONLY OOS DELTAS COUNT; no isotonic model is scored on its fit ticks.` | 1 |

## 3. Signals

No row for this sport in the `data/ab_reports/` signal ledgers. Status: UNKNOWN.

## 4. Known limits

- `coverage 0.00 < 0.85` -- `failures` in `data/tracking_reports/soccer/soccer_wc22_bra_cro.json`
- `ball_valid 0.00 < 0.2` -- `failures` in `data/tracking_reports/soccer/soccer_wc22_bra_cro.json`
- `jump_p95 51.7 > 8.0` -- `failures` in `data/tracking_reports/soccer/soccer_wc22_bra_cro.json`

## 5. Open questions

Derived mechanically -- failing thresholds, the 10-game bar, and the 7-day freshness bar.

- Metric `ball_valid` misses its threshold in 1 of 1 tracking report(s) under `data/tracking_reports/`. What fix closes it?
- Metric `coverage` misses its threshold in 1 of 1 tracking report(s) under `data/tracking_reports/`. What fix closes it?
- Metric `jump_p95` misses its threshold in 1 of 1 tracking report(s) under `data/tracking_reports/`. What fix closes it?
- Only 1 tracking report(s) on disk for soccer; the 10-game bar is not met. What blocks collecting more?
- No soccer row in the foundry/decay ledgers under `data/ab_reports/`. Signal status is UNKNOWN, not good.
