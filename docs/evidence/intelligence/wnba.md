# Intelligence brief -- wnba

Generated by `scripts/platformkit/intelligence_brief.py` from artifacts on disk.
As-of clock (newest input artifact mtime): 2026-09-01T00:13:47Z. No edge, ROI or profit claim is made in this brief.

## 1. Tracking status

Reports on disk: 2. Pass rate: 1/2 (the `passed` field of each file).

| report file | n_frames | passed | worst failing metric (verbatim) |
| --- | --- | --- | --- |
| `data/tracking_reports/wnba/wnba_kangps_g1.json` | 5730 | True | (none) |
| `data/tracking_reports/wnba/wnba_kangps_g2.json` | 5987 | False | `coverage 0.41 < 0.6` |

Pending fixes are exactly the `failures` entries below; nothing else on disk.

- `coverage 0.41 < 0.6` -- `data/tracking_reports/wnba/wnba_kangps_g2.json`

## 2. Model status

No walk-forward or calibration file in `data/ab_reports/` names wnba. Status: UNKNOWN.

## 3. Signals

No row for this sport in the `data/ab_reports/` signal ledgers. Status: UNKNOWN.

## 4. Known limits

- Directory says `wnba` but the file's `sport` field says `basketball` -- `data/tracking_reports/wnba/wnba_kangps_g1.json`
- `coverage 0.41 < 0.6` -- `failures` in `data/tracking_reports/wnba/wnba_kangps_g2.json`
- Directory says `wnba` but the file's `sport` field says `basketball` -- `data/tracking_reports/wnba/wnba_kangps_g2.json`

## 5. Open questions

Derived mechanically -- failing thresholds, the 10-game bar, and the 7-day freshness bar.

- Metric `coverage` misses its threshold in 1 of 2 tracking report(s) under `data/tracking_reports/`. What fix closes it?
- Only 2 tracking report(s) on disk for wnba; the 10-game bar is not met. What blocks collecting more?
- No file in `data/ab_reports/` names wnba. Model status is UNKNOWN, not good.
- No wnba row in the foundry/decay ledgers under `data/ab_reports/`. Signal status is UNKNOWN, not good.
