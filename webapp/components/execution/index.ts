// execution barrel -- shared cross-venue decision-trail surface (WS5).
// UNITS / probability only -- no $ field anywhere.
// Real-money DENY note is always rendered in ExecutionTrail.
export { ExecutionTrail } from "./ExecutionTrail";
export type {
  ExecutionTrailProps,
  BookLineRow,
  DevigResult,
  DecisionStep,
  TierLabel,
} from "./ExecutionTrail";
