# Mission brief 2026-07-15 evening (user directive)

Read webapp/DESIGN.md first (binding design spec). This file adds tonight's
work items. HONESTY RAILS as always: units never $, no edge/ROI claims, never
fabricate data, stale-never-green, ASCII source.

## Facts discovered (do not re-derive)
- Paper trail rows CARRY full prop detail: prop_player, prop_stat, prop_side,
  line (e.g. "Ildemaro Vargas" / "Hits" / "under" / 0.5). UI just prints
  "prop away" -- render the real description.
- KX matchups are raw tickers in the ledger: KXMLBGAME-26JUL181510CINCOL =
  sport MLB, date 26 JUL 18, time 15:10, away CIN, home COL. Pattern:
  KX<LEAGUE>GAME-<YY><MON><DD><HHMM><AWAY3><HOME3>. Never show a raw KX
  ticker to the user anywhere; decode or fall back to "CIN @ COL (Kalshi)".
- CLV fields on rows: clv_pct, clv_status (true_close|no_close|...),
  clv_is_proxy, beat_close, clv_note. taken_decimal = entry price.
- Data services: same-origin /p5/* -> :8099, /api/board/slate -> :8098 slate
  (per-game markets incl. per-book prices + devigged_prob).

## Items
1. BET DESCRIPTIONS: every bet display (today digest, bets cards, paper
   tables, records) shows exactly WHAT the bet is: props ->
   "I. Vargas UNDER 0.5 Hits (underdog)"; moneyline -> "<TEAM> ML".
   Shared lib/betdesc.ts: describeBet(row), humanizeMatchup(str).
2. LIVE PAGE (centerpiece): every live/pregame game is a CLICKABLE card ->
   game detail with that game's best bets, model-vs-market bars, in-game
   number. Winner clarity: color-code the LEADING/favored side -- shared
   <SideProb> primitive: team abbrev + prob, green when leading, red when
   trailing, neutral tie; model amber vs market blue for source colors.
3. BOARD -> GAMES: /board page is REMOVED. /games becomes the hub: slate
   cards (best price/book vs our calibrated prob, from /api/board/slate) +
   best-bets summary per game + click-through to game detail. Mobile-first
   cards preserved.
4. CLV ACCURACY: audited end-to-end (separate backend agent).
5. READABILITY: no unreadable ink anywhere (light theme washed-out headline,
   low-contrast banners). Numbers must be high-contrast in both themes.
6. Model vs market naming/colors consistent everywhere: "model" amber
   (--s-model), "market" blue (--s-market) with those exact labels.
