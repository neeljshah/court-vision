{
 "id": "why_attribution",
 "title": "Why Attribution",
 "headline_insight": "The largest single calibrated win-prob swing between adjacent buckets is an MLB collapse: a team at .8-1 in innings 4-6 that falls to 0-.2 by innings 7+ loses 0.8985 of realized win probability (min support n=1462).",
 "what_it_means": "Any in-game probability move can be decomposed into the adjacent time-bucket transition it crossed, with impact measured as the difference in realized win rate between the two states rather than an observed frequency. Soccer's biggest drop is a 15-30 minute .4-.6 team falling to .2-.4 by 30-45, worth -0.775.",
 "how_to_read": "Each row is a transition between consecutive time buckets; the delta is mean_y(B) minus mean_y(A), the realized (calibrated) win rate of each state.",
 "why_it_matters": "It turns a raw probability move into a readable what-changed story anchored to real state transitions.",
 "caveat": "mean_y is treated as the calibrated win prob; transitions are restricted to consecutive buckets with no skipping; buckets thinner than n=30 are dropped. NBA is skipped because only a checkpoint-Brier file exists, with no per-state realized win rate to attribute. These are revision 2 numbers: the source state buckets were rebuilt on the segment-clean corpus, which is why the largest drop is now an MLB transition on n=1462 rather than a soccer one on n=120.",
 "cited": [
  {"path": "webapp/public/data/showcase/why_attribution.json", "field": "biggest_drops[0].winprob_delta", "value": -0.8985},
  {"path": "webapp/public/data/showcase/why_attribution.json", "field": "biggest_drops[0].min_support_n", "value": 1462},
  {"path": "webapp/public/data/showcase/why_attribution.json", "field": "biggest_drops[2].winprob_delta", "value": -0.775},
  {"path": "webapp/public/data/showcase/why_attribution.json", "field": "skipped[0].sport", "value": "nba"}
 ],
 "as_of": "2026-09-17"
}
