# =============================================================================
# CourtVision — Python 3.9 Dependencies
# conda activate basketball_ai && pip install -r requirements.txt
# =============================================================================

# ── Computer Vision ───────────────────────────────────────────────────────────
ultralytics>=8.0          # YOLOv8 detection + pose estimation
opencv-python>=4.8        # Frame processing, Hough circles, optical flow
easyocr>=1.7              # Jersey OCR (dual-pass)

# ── Deep Learning ─────────────────────────────────────────────────────────────
torch>=2.0                # PyTorch (CUDA 11.8 build recommended)
torchvision>=0.15         # Image transforms for re-ID
numpy>=1.24               # Numerical ops
scipy>=1.11               # Hungarian algorithm, spatial distance

# ── Re-Identification ─────────────────────────────────────────────────────────
deep-sort-realtime>=1.3   # DeepSORT tracker baseline
# torchreid                # OSNet — install from source: pip install torchreid

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.4         # LogisticRegression, Ridge, preprocessing, metrics
xgboost>=1.7              # XGBoost classifiers / regressors
joblib>=1.3               # Model serialization / parallel jobs
lightgbm>=4.0             # LightGBM (alt prop model)

# ── Data Processing ───────────────────────────────────────────────────────────
pandas>=2.1               # DataFrames, CSV/JSON I/O
pyarrow>=14.0             # Parquet support for large datasets
networkx>=3.2             # Passing network graph analysis

# ── NBA Data ──────────────────────────────────────────────────────────────────
nba_api>=1.4              # Official NBA stats API client

# ── Web Scraping ──────────────────────────────────────────────────────────────
requests>=2.31            # HTTP requests (BBRef, odds scrapers)
beautifulsoup4>=4.12      # HTML parsing (BBRef, HoopsHype)
lxml>=4.9                 # Fast XML/HTML parser backend
aiohttp>=3.9              # Async HTTP (injury monitor)

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9      # PostgreSQL client
sqlalchemy>=2.0           # ORM / query builder

# ── Task Queue ────────────────────────────────────────────────────────────────
celery>=5.3               # Async task queue (batch processing)
redis>=5.0                # Redis client (Celery broker + cache)

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.110.0          # REST API framework
uvicorn>=0.29.0           # ASGI server
pydantic>=2.0             # Request/response validation
httpx>=0.27               # Async HTTP client (API testing)

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.32           # Analytics dashboard
plotly>=5.18              # Interactive charts
matplotlib>=3.8           # Static charts, shot chart heatmaps
flask>=3.0                # Lightweight web server (legacy endpoints)

# ── NLP ───────────────────────────────────────────────────────────────────────
anthropic>=0.21           # Claude API (Phase 15 AI chat)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0        # .env loading
tqdm>=4.66                # Progress bars for batch jobs
loguru>=0.7               # Structured logging (optional, replaces logging)
tenacity>=8.2             # Retry logic for API calls
schedule>=1.2             # Simple job scheduler

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4               # Test framework
pytest-cov>=4.1           # Coverage reporting
pytest-asyncio>=0.23      # Async test support
