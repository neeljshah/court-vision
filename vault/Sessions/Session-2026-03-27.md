# Session 25 — 2026-03-27

## Goals
1. Update CourtVision for 2025-26 season predictions
2. Run pipeline loop iteration on tracking quality

---

## 2025-26 Season Update — Complete

### Code Changes

| File | Change |
|------|--------|
| `scripts/pull_missing_data.py` | Added phase A0 (per-player gamelog bulk fetch). `_SEASONS` → `["2025-26","2024-25","2023-24","2022-23"]`. A0 added to default run order and `--phase` choices. `pull_a3_synergy()` updated to fetch all missing seasons. |
| `scripts/retrain_props_v2.py` | Docstring updated. `_TRAIN_CUTOFF` → `datetime(2025,10,1)`. `_TEST_CUTOFF` → `datetime(2026,1,1)`. `_load_gamelogs()` now tags each row with `_season`. `_build_dataset()` resets rolling prior at season boundary (same-season prior only). `retrain_version` updated. |
| `scripts/retrain_all.py` | `wp_train(seasons=["2022-23","2023-24","2024-25"])` — removed 2025-26 (outcomes are live targets). |
| `scripts/daily_pipeline.py` | Docstring `--season 2024-25` → `--season 2025-26`. |
| `src/prediction/win_probability.py` | `predict()` default `season` → "2025-26". `train()` and `backtest()` defaults → `["2022-23","2023-24","2024-25"]`. `_SEASON_GAMES_VERSION` 4→5. `_fetch_season_games()` now writes C-1 through C-7 features (ELO, pnr_ppp, b2b_diff, etc.) to training rows — fixes "not in index" crash. CLI `--seasons` default updated. |
| `src/prediction/player_props.py` | `predict_props()` default `season` → "2025-26". CLI `--season` default → "2025-26". |
| `data/nba/games_2024-25.json` | Deleted — only had `game_id`/`season` fields, was stale. |
| `data/nba/season_games_*.json` | Deleted (v4 schema missing C-1 through C-7) — auto-regenerated on win_prob retrain with v5 schema. |

### Data Fetched
| Season | Dataset | Count |
|--------|---------|-------|
| 2025-26 | Tracking stats (A2) | 570 players |
| 2025-26 | Synergy offensive + defensive | cached |
| 2025-26 | Schedules (A4) | 30/30 teams |
| 2024-25 | Synergy (was missing) | now cached |
| 2022-23 | Gamelogs (A0) | 539 players ✅ |
| 2023-24 | Gamelogs (A0) | ~240/600 in progress |
| 2025-26 | Gamelogs (A0) | pending |

### Models Retrained
| Model | Result |
|-------|--------|
| `win_probability.pkl` | acc=68.8%, Brier=0.2037, n=3,685 games (2022-23 to 2024-25) |
| `possession_outcome.pkl` | Saved (empty — PBP uses player_name not player_id; Laplace priors only) |
| `props_*.json` | Pending gamelog download + `python scripts/retrain_props_v2.py` |

---

## Tracker — Pipeline Loop Iteration 2

**Clip:** 0022400430.mp4 (300 frames)

| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| Pipeline fps | 54 vid fps | ≥20 | ✅ |
| ball_valid (combined) | 62% | ≥60% | ✅ |
| Shots | 1/167s | ≥1/2min | ✅ |
| ID switches | 0 | 0 | ✅ |
| Possessions | 3 | ≥5 | 🟡 |

**Fix attempted:** `ball_detect_track.py:414` dribble predictor gap 8→12 frames — **reverted** (combined 62%→59.7%, 8–12 frame gaps rarely populated in broadcast).

**Background:** 0022400625 reprocessing (18000 frames, all ISSUE-039/040/061 fixes active).

---

## Open Issues Updated

- `ISSUE-062` (new): `possession_outcome_model` uses `player1_id` as key, but local PBP files have `player_name` only. Model saves empty and uses Laplace priors. Fix: add player name→ID resolution in `_parse_pbp_outcomes()` using NBA static teams/players lookup.
- `ISSUE-029`: ball detection at 62% combined (53% detected + 9% inferred). Target 60% combined ✅. Detected-only still 53% (target 60% if counting only YOLO/Hough).

---

## Next Steps

```bash
# 1. Wait for A0 gamelog download to complete
python scripts/pull_missing_data.py --check

# 2. Retrain props on 3 completed seasons
python scripts/retrain_props_v2.py

# 3. Continue Phase G — run untracked games
python scripts/run_phase_g.py --game-ids 0022401183 0022401185 0022401190 0022401194 0022401196 0022401198 --frames 18000

# 4. Fix possession count in short clips — audit _BALL_LOSS_THRESH fps detection
# in src/pipeline/unified_pipeline.py

# 5. Fix ISSUE-062 — player_name→ID in possession_outcome_model
```
