"""Reliability-weighted ensemble fusion (MASTER_SYSTEM_BUILD section 4D). weights.py builds the
correlation/redundancy guard (computable now from the measured engine decorrelation) and records a
default-OFF weight PROPOSAL. Equal-weight stays the SHIPPED default until a leak-free cross-season
per-engine reliability backtest exists AND a human approves the flip (section 7.6)."""
