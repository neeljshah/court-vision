"""The never-stop loop's autonomy + safety spine: state/ledger (state.py), the STOP kill-switch
(stop_run.py), and the disk/feed/PowerShell hygiene guards (guards.py). Resumable from disk alone
(MASTER_SYSTEM_BUILD section 7.6) -- never from chat memory."""
