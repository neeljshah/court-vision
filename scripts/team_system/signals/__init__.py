"""Signal foundry: the JUDGE (mechanized CAVEATs, judge.py) + GATE-A/FDR (gates.py).
The deliverable of the foundry is the REJECTION machinery, not the proposer (MASTER_SYSTEM_BUILD section 0/4A):
'most reject' is the product. Progress = signals that SURVIVE cross-season, never signals created."""
