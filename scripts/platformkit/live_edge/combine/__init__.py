"""scripts.platformkit.live_edge.combine -- LIVE-EDGE Track C composition (C1).

Per-observable regularized learned-weight combiners over ledgered tier-1/
tier-2 claim features. OWNS this package exclusively (LIVE-EDGE lane
C1-COMBINER); imports omni modules read-only, never writes the claims
journal directly (pending_ledger.jsonl handoff instead).
"""
