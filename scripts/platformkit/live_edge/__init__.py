"""scripts.platformkit.live_edge -- LIVE-EDGE program build namespace (A1+).

Own namespace per .planning/LIVE-EDGE-PROGRAM.md L-S1: build here, never in
scripts/platformkit/omni/** (OMNI-DEPTH owns that). Data outputs live under
data/omni/live_edge/** (own namespace, gitignored).
"""
