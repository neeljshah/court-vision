"""market_coverage -- the EVERY-MARKET predictor + OBSCURE-MARKET edge-finder.

NEW code only (this package). It CONSUMES the joint-sim output, the full-market
stack, predict_matchup and the REAL eval gate (walk_forward / scoring / dm_test /
shin) READ-ONLY -- it never edits src/sim, src/prediction, or
scripts/team_system/market_intelligence.py (all human-gated).

Thesis: liquid mainlines are razor-efficient (we MATCH them -- honest, recorded);
extreme/obscure/prop/deep-combo (SGP)/in-game-micro markets are thinly policed,
so per-market CALIBRATION is the product value and any in-sample beat on a soft
line is SUGGESTIVE only -- flagged needs-forward-CLV, never a $ claim.
"""
