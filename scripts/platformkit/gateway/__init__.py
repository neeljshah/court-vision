"""scripts.platformkit.gateway -- the WAVE 4 unified multi-API gateway.

ONE sports-intelligence model -> many contracted FACES (prediction, execution,
lines, intelligence), each wrapped with the standing honesty rails. See
gateway.py (FACES registry + wrap()) and catalog.py (catalog rows + all_honest).
"""
