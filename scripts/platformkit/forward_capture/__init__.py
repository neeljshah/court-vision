"""forward_capture -- the FORWARD-CAPTURE / CLV-CLOCK recorder.

Package marker so the dir imports both as `scripts.platformkit.forward_capture.*`
and via the per-file sys.path.insert fallback (mirrors edge_engine / eval_gate).

This package RECORDS the timestamped odds-movement corpus + logs predictions
BEFORE the line moves so forward CLV can be graded honestly over time. It claims
NO edge: it records the data that will one day PROVE or REJECT one.
"""
