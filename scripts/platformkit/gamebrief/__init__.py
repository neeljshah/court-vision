"""scripts.platformkit.gamebrief -- the GAME BRIEF assembler.

Read-only composition of existing intelligence stores into one brief (ASCII +
JSON) for a single NBA matchup. No new modeling: every number here is a
straight read (or a simple derived delta) from an already-computed store.
See cli.py for usage.
"""
