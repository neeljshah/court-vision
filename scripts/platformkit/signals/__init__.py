"""scripts.platformkit.signals -- LANE C signal-factory bridge (M2).

Person-free vault taxonomy loader + a CandidateSpec signal factory (playstyle_corr /
scheme_prior / archetype_matchup) + a per-family planted-null tripwire. Additive +
FLAG-OFF: with the PIPELINE_ENABLED sentinel absent the loop ships NOTHING.
"""
