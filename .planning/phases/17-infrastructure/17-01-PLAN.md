---
phase: 17-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_infrastructure.py
  - tests/conftest.py
autonomous: true
requirements:
  - REQ-14-1
  - REQ-14-2
  - REQ-14-3
  - REQ-14-4
  - REQ-14-5

must_haves:
  truths:
    - "All phase 17 test stubs exist and are importable without error"
    - "Each stub test raises AssertionError or pytest.skip (not ImportError)"
    - "Shared fixtures exist for mock model metrics and feature importance snapshots"
  artifacts:
    - path: "tests/test_infrastructure.py"
      provides: "Test stubs for Docker health, CI config, drift alert, auto-retrain, model gate"
      exports: ["test_docker_health_endpoint", "test_ci_config_lint", "test_drift_alert_fires", "test_auto_retrain_milestone", "test_model_validation_gate"]
    - path: "tests/conftest.py"
      provides: "Shared fixtures for infrastructure tests"
      contains: "mock_model_metrics, mock_feature_importance"
  key_links:
    - from: "tests/test_infrastructure.py"
      to: "src/pipeline/feature_drift_detector.py"
      via: "import FeatureDriftDetector"
      pattern: "from src.pipeline.feature_drift_detector import"
    - from: "tests/test_infrastructure.py"
      to: "src/pipeline/auto_retrain.py"
      via: "import check_and_retrain"
      pattern: "from src.pipeline.auto_retrain import"
---

<objective>
Create Wave 0 test stubs for all Phase 17 infrastructure requirements. These stubs define the test contracts that subsequent implementation plans must satisfy.

Purpose: Nyquist compliance — every verification command in plans 02 and 03 must reference a test that exists.
Output: tests/test_infrastructure.py with five stub test functions + conftest.py fixtures.
</objective>

<execution_context>
@C:/Users/neelj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/neelj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-infrastructure/17-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_infrastructure.py with all stubs</name>
  <files>tests/test_infrastructure.py</files>
  <behavior>
    - test_docker_health_endpoint: imports requests (or urllib), calls GET http://localhost:8000/health if DOCKER_TEST env var set, else pytest.skip("Docker not running"); asserts response JSON has keys status, database, redis
    - test_ci_config_lint: reads .github/workflows/ci.yml, parses YAML, asserts top-level keys on, jobs exist and jobs contains at least one job with steps key
    - test_drift_alert_fires: instantiates FeatureDriftDetector, calls log_importance with synthetic importances {feat_a: 0.5, feat_b: 0.3, feat_c: 0.2}, then calls log_importance again with drifted importances {feat_a: 0.1, feat_b: 0.3, feat_c: 0.6} (feat_a dropped 80% = above 30% threshold), calls check_drift, asserts result["drifted"] is True and "feat_a" in result["drifted_features"]
    - test_auto_retrain_milestone: mocks get_game_count to return 20, calls check_and_retrain("test_game_001", "2024-25"), asserts result["milestone_hit"] is not None (i.e., tier3_20 milestone fires at exactly game 20)
    - test_model_validation_gate: imports compare_models from scripts.compare_models (will not exist yet — test raises ImportError caught as pytest.skip with message "compare_models not yet implemented — REQ-14-3"); this stub ensures plan 03 wires the import correctly
  </behavior>
  <action>
    Create tests/test_infrastructure.py with the five stub tests above.

    Key implementation notes:
    - Use pytest.importorskip for Docker-dependent test: `requests = pytest.importorskip("requests")`
    - For test_docker_health_endpoint: wrap in `if not os.environ.get("DOCKER_TEST"): pytest.skip("Set DOCKER_TEST=1 to run")`
    - For test_ci_config_lint: use `import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))`; if yaml not installed use `pytest.importorskip("yaml")`
    - For test_drift_alert_fires: import FeatureDriftDetector from src.pipeline.feature_drift_detector; use tmp_path fixture to override _DRIFT_LOG path so tests don't pollute data/models/; monkeypatch the _DRIFT_LOG module-level constant
    - For test_auto_retrain_milestone: monkeypatch get_game_count to return 20; monkeypatch _run_props_retrain and _retrain_tier3 to noops (avoid actual model training)
    - For test_model_validation_gate: wrap ImportError in try/except and pytest.skip

    Do NOT use == game_count comparison assertions — just verify milestone_hit is truthy when count == 20. The failing milestone behavior is the bug Plan 02 fixes; the stub tests the interface, not the buggy behavior.

    All tests must be importable and runnable without GPU or Docker.
  </action>
  <verify>
    <automated>python -m pytest tests/test_infrastructure.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 tests collected; test_model_validation_gate shows SKIP (ImportError caught); remaining 4 show PASS or SKIP (if DOCKER_TEST not set); zero ERRORS on import</done>
</task>

<task type="auto">
  <name>Task 2: Add shared fixtures to conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
    Read the existing tests/conftest.py first. Append the following fixtures — do not overwrite existing content:

    1. `mock_model_metrics` fixture: returns dict {"pts_model": {"r2": 0.47, "mae": 3.2}, "reb_model": {"r2": 0.40, "mae": 1.1}} — used by test_model_validation_gate in plan 03
    2. `mock_feature_importance` fixture: returns a dict with two snapshots — first {"fg_pct": 0.4, "usage": 0.3, "rest_days": 0.2, "def_rtg": 0.1}, second (drifted) {"fg_pct": 0.05, "usage": 0.45, "rest_days": 0.35, "def_rtg": 0.15} — used by test_drift_alert_fires
    3. `drift_log_path` fixture (tmp_path-scoped): returns a tmp_path / "feature_drift_log.json" path string; also monkeypatches src.pipeline.feature_drift_detector._DRIFT_LOG to this path so FeatureDriftDetector writes to temp dir during tests

    Type hints required (Python 3.9 compat: use dict, list not Dict, List).
    Use @pytest.fixture decorator. Scope: function (default) for all three.
  </action>
  <verify>
    <automated>python -m pytest tests/test_infrastructure.py -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>conftest.py imports without error; fixtures are discoverable by pytest (no fixture not found errors in test run)</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_infrastructure.py -v 2>&1 | tail -15
All 5 tests collected, zero import errors. SKIP is acceptable; ERROR is not.
</verification>

<success_criteria>
- tests/test_infrastructure.py exists with 5 named test functions matching VALIDATION.md task IDs
- tests/conftest.py has 3 new fixtures without breaking existing fixtures
- Full suite still green: python -m pytest tests/ -q --ignore=tests/test_cv 2>&1 | tail -5
</success_criteria>

<output>
After completion, create `.planning/phases/17-infrastructure/17-01-SUMMARY.md`
</output>
