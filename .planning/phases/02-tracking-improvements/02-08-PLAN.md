---
phase: 02-tracking-improvements
plan: 08
type: execute
wave: 2
depends_on:
  - 02-06
  - 02-07
files_modified:
  - CLAUDE.md
autonomous: false
gap_closure: true
requirements:
  - REQ-02F

must_haves:
  truths:
    - "All 17 existing clips are re-processed on a capable machine (GPU or workstation) using the fixed tracker"
    - "Output tracking_data.csv contains both 'green' and 'white' values in the team column"
    - "Output shot_log.csv has at least 1 row per minute of footage across the 17 clips"
    - "CLAUDE.md dataset status is updated to reflect the corrected tracking row counts and shot counts"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated dataset status: non-zero shots, two distinct team labels, re-processed clip count"
      contains: "re-processed"
  key_links:
    - from: "run_clip.py"
      to: "data/tracking_data.csv"
      via: "python run_clip.py --video data/videos/<clip>.mp4 --no-show for each of the 17 clips"
      pattern: "tracking_data"
---

<objective>
Re-process all 17 existing clips through the fixed tracker (after 02-06 lands) and verify both teams appear and shot events fire. Update CLAUDE.md dataset status with the corrected counts.

Purpose: REQ-02F requires proof that the structural fixes in 02-06 actually produce correct output on real video. This is a human-action checkpoint because CLAUDE.md explicitly prohibits running video processing on the local dev machine — it requires a GPU machine or separate workstation.

Output: Re-processed CSVs for all 17 clips + updated CLAUDE.md dataset status section.
</objective>

<execution_context>
@C:/Users/neelj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/neelj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tracking-improvements/02-06-SUMMARY.md
@.planning/phases/02-tracking-improvements/02-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Re-process all 17 clips on GPU machine with fixed tracker</name>
  <what-built>Plans 02-06 and 02-07 fixed the all-green bug and added the clip duration validator. The tracker now uses 5 green + 5 white + 1 referee slots and does not collapse white detections to green.</what-built>
  <how-to-verify>
    On your GPU machine / workstation (NOT the local dev machine per CLAUDE.md rule):

    1. Pull latest code changes (02-06 and 02-07 must be committed).

    2. Activate env: conda activate basketball_ai

    3. Run each clip in data/videos/ — one at a time or in a loop:
       python run_clip.py --video data/videos/<clip>.mp4 --no-show

    4. After each clip, check the output:
       - data/tracking_data.csv: run `python -c "import pandas as pd; df=pd.read_csv('data/tracking_data.csv'); print(df['team'].value_counts())"`
         Expected: both 'green' and 'white' appear, neither is 0
       - data/shot_log.csv: check row count > 0

    5. Acceptable quick loop for all 17 clips:
       for f in data/videos/*.mp4; do python run_clip.py --video "$f" --no-show; done

    6. Record the summary counts:
       - Total tracking rows
       - Rows with team='green'
       - Rows with team='white'
       - Total shots detected across all clips
       - Clips with 0 shots (should be 0 or close to 0)

    Pass criteria (type "approved" when ALL met):
    - At least 3 clips show both 'green' AND 'white' rows in tracking_data.csv
    - At least 1 clip has > 0 rows in shot_log.csv
    - No clip output has ALL rows labeled 'green' (the all-green bug is gone)

    If the clips are still all-green: confirm 02-06 was actually executed (check git log for the advanced_tracker.py and unified_pipeline.py changes). Do not approve until the team separation is confirmed.
  </how-to-verify>
  <resume-signal>Type "approved" with a brief summary of shot counts and team label distribution seen across the clips. Example: "approved — 14/17 clips show both teams, shot_log has 47 total rows"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md dataset status with verified counts</name>
  <files>CLAUDE.md</files>
  <action>
    After the human checkpoint confirms re-processing results, update CLAUDE.md in two places:

    1. In the "Dataset Status > CV Tracking Data" table, update:
       - "Tracking rows": replace with the new count from re-processed clips
       - Remove the "⚠️ All players labeled 'green'" warning from that row
       - "Shots detected": replace 0 with the actual count from shot_log.csv across all clips
       - "Dribbles detected": update if non-zero
       - Add a note: "Re-processed 2026-03-XX with fixed dual-team tracker (02-06)"

    2. In the "Known Issues" table:
       - ISSUE-012: change status from "🔴 Active" to "✅ Fixed 2026-03-XX — dual-team slot fix (02-06)"
       - ISSUE-011: change status from "🔴 Active" to "✅ Fixed — EventDetector validated (02-07)"
       - ISSUE-013: update status to reflect new shot count (fixed or reduced)
       - ISSUE-014: keep as "🔴 Phase 6 — need full 48-min broadcast footage on GPU machine" (clips are still short even if re-processed)

    Use the actual counts provided in the human checkpoint resume signal. If the human said "47 total shots", use 47.
  </action>
  <verify>
    <automated>python -c "
import pathlib
text = pathlib.Path('CLAUDE.md').read_text()
assert 'Fixed' in text and '02-06' in text, 'CLAUDE.md not updated with 02-06 fix reference'
assert 'ISSUE-012' in text
print('CLAUDE.md updated correctly')
"</automated>
  </verify>
  <done>
    CLAUDE.md dataset status no longer shows "All players labeled green". Shot count is non-zero. ISSUE-012 marked as fixed. ISSUE-011 marked as fixed.
  </done>
</task>

</tasks>

<verification>
1. git log --oneline src/tracking/advanced_tracker.py | head -3 — confirm 02-06 commit exists
2. grep -c "white" data/tracking_data.csv — must be > 0 (after re-processing)
3. wc -l data/shot_log.csv — must be > 1 (header + at least 1 shot)
4. grep "ISSUE-012" CLAUDE.md — must show "Fixed"
</verification>

<success_criteria>
- 17 clips re-processed on GPU machine with corrected tracker
- tracking_data.csv from at least one clip shows both "green" and "white" team labels
- shot_log.csv has at least 1 detected shot event total
- CLAUDE.md dataset status updated with real counts (no longer shows 0 shots + all-green warning)
- Phase 2 ROADMAP status updated to "✅ Complete" after this plan finishes
</success_criteria>

<output>
After completion, create `.planning/phases/02-tracking-improvements/02-08-SUMMARY.md` following the standard summary template.
</output>
