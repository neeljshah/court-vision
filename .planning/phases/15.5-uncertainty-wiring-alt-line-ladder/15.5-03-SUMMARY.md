---
phase: 15.5-uncertainty-wiring-alt-line-ladder
plan: "03"
subsystem: prediction
tags: [conformal-prediction, kelly-sizing, alt-line, bet-selector, daily-slate]

# Dependency graph
requires:
  - phase: 15.5-01
    provides: ConformalPredictor.load_residuals() + predict_interval() with .npy residual files
  - phase: 15.5-02
    provides: build_alt_line_ladder() + ladder_to_bets() in alt_line_ladder.py
provides:
  - CI field injection (ci_lo_80, ci_hi_80) in every bet dict from bet_selector.select()
  - alt_line + alt_line_ev passthrough from input rows to bet output schema
  - --build-ladder CLI flag on run_daily_slate.py generating 2-3x bet count via ladder
affects:
  - record_slate_results.py (reads bets JSON — now includes ci and alt_line fields)
  - api/main.py bets endpoint (schema change: 4 new nullable fields)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Module-level conformal cache with graceful FileNotFoundError fallback (_conformal_cache + _has_conformal guard)
    - Ladder block as non-fatal try/except middleware between Steps 8 and 9 in slate pipeline
    - xfail-strict=False tests used as forward-compatibility probes; flip to XPASS when wired

key-files:
  created: []
  modified:
    - src/prediction/bet_selector.py
    - scripts/run_daily_slate.py

key-decisions:
  - "_get_ci defined as inner function inside select() (not module-level) to access _conformal_cache without globals"
  - "alt_line/alt_line_ev injected via row.get() passthrough — None for main-line bets, preserves schema uniformity"
  - "Ladder block placed before bet_selector call so CI-enriched rows flow into ladder before final selection"

patterns-established:
  - "Conformal CI injection pattern: module-level guard + stat-keyed cache + graceful per-call fallback"
  - "Non-fatal middleware pattern: every pipeline extension wrapped in try/except with log.warning"

requirements-completed:
  - 15.5-CONFORMAL
  - 15.5-SCHEMA
  - 15.5-INTEGRATION

# Metrics
duration: 12min
completed: 2026-04-23
---

# Phase 15.5 Plan 03: Bet Selector CI Wiring + Alt-Line Ladder Integration Summary

**Conformal 80% CI fields (ci_lo_80, ci_hi_80) injected into every bet dict with stat-keyed cache; --build-ladder flag wires alt_line_ladder into run_daily_slate.py Step 8**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-23T13:17:57Z
- **Completed:** 2026-04-23T13:30:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `bet_selector.select()` now injects ci_lo_80/ci_hi_80 into every bet dict using ConformalPredictor with a per-stat module-level cache and FileNotFoundError-safe fallback
- alt_line and alt_line_ev passed through from input row to bet dict, completing Phase 15.5 schema requirements
- `run_daily_slate.py` gains `--build-ladder` CLI flag: Step 8 loops edge rows, calls build_alt_line_ladder(), appends top-EV alt bets before bet_selector (Step 9)
- 2 TestBetSelectorCI xfail tests flip to XPASS; all 15 tests in test_bet_selector.py + test_alt_line_ladder.py pass

## Task Commits

1. **Task 1: Wire CI fields into bet_selector.select()** - `58757ff7` (feat)
2. **Task 2: Add --build-ladder flag to run_daily_slate.py** - `db3b5722` (feat)

## Files Created/Modified

- `src/prediction/bet_selector.py` — Added _conformal_cache + _has_conformal guard, _get_ci() inner helper, ci_lo_80/ci_hi_80/alt_line/alt_line_ev fields in bet dict (284 LOC, under 300 limit)
- `scripts/run_daily_slate.py` — Added build_ladder param to main(), Step 8 ladder block, --build-ladder CLI arg

## Decisions Made

- `_get_ci` defined as inner function inside `select()` rather than module-level to access `_conformal_cache` naturally without threading globals through signature
- `alt_line/alt_line_ev` use `row.get()` passthrough pattern — None for main-line bets. Schema uniformity: all bets have all 4 keys, null-safe for downstream consumers
- Ladder placed in Step 8 before bet_selector (Step 9) so that alt-line rows carry ci fields when they flow through select()

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The xfail tests with `strict=False` appeared as "xpassed" in pytest output rather than "passed" — this is correct pytest behavior, not a failure.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 15.5 fully complete: conformal CI (Plan 01) + alt_line_ladder (Plan 02) + bet_selector wiring (Plan 03) all done
- Daily pipeline can now generate 2-3x bet count via `--build-ladder` flag when Pinnacle lines are available
- bets_YYYYMMDD.json schema includes ci_lo_80, ci_hi_80, alt_line, alt_line_ev on every bet
- Next: Phase 16 (LSTM live win prob) or Phase 20 (RunPod CV ingest) per ROADMAP

---
*Phase: 15.5-uncertainty-wiring-alt-line-ladder*
*Completed: 2026-04-23*
