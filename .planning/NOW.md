---
updated: 2026-06-21
north_star: BEST predictions per sport (beat/match the devigged close on OOS calibration); honest, no fabricated edge.
active_project: sports-betting decision-support product (4 sports) + own keyless odds API
loop_queue_source: this file's NEXT list
---

# NOW -- the single source of truth for "what's done / what's next"

## >>> CEILING MODE (user directive 2026-06-26b): RAISE THE CEILING -- BEST PREDICTIONS
Shift from floor-raising (hardening/tests, wakes 1-15) to CEILING-raising: the highest-level
CALIBRATED predictions per sport (beat/MATCH devigged close on OOS calibration). Three levers in
priority: L1 MORE DATA independently (FIRST reclaim already-on-disk data discarded at ingest via NEW
leak-free extractors in domains/<sport>/**, zero-network; daemons keep capturing), L2 MORE SIGNALS
(many new leak-free candidates/cycle), L3 BETTER MODELS where room exists = IN-GAME conditioning +
same-day FRESHNESS (pregame is efficient -> MATCH not beat). GATE every candidate through the REAL
tools: use the `signal-audit` skill (REAL leak-free SHIP/REJECT gate per sport), `eval-gate`,
`cross-sport-benchmark` + `calibration-report` (OOS Brier/ECE vs baseline AND vs close). SHIP gated
survivors; REJECT-log the rest WITH the number (REJECT = success, never fabricate a win). Full spec:
BUILD_BACKLOG.md SECTION 0 CEILING PRIORITY block. Heavier than the hardening loop (1-2 Sonnets/wake
ok). Daemon flywheel + builder loop run independently; enders `bot stop` / program_complete only.
- DEEP RESEARCH DONE (2026-06-26b): docs/research/ceiling/ADVANCED_TECHNIQUES.md (top technique =
  Conformalized Quantile Regression for prop intervals) + ONDISK_RECLAIM_TARGETS.md (BIG finding:
  leak-free asof_*.parquet already BUILT for every sport but NO gate adapter merges them -> each
  reclaim = cheap wire+gate experiment). EXECUTION QUEUE in BUILD_BACKLOG S0.
- WAKE-29 (PAPER TRADING AUDIT -> FOUND+FIXED the in-game channel never SETTLED, 2026-07-01):
  user "make sure paper trading is working." Audited live: stack 29/29 up, bankroll live-updating
  (68.4u of 100 = the measured bleed), PREGAME channels healthy (today props=105, paper_pm=42,
  moneyline=27 all placing+settling+CLV). BIG WIN confirmed: in-game channel now PLACES (paper_ingame
  =55 today vs ~1 historically -- the WAKE-25 two-sided fix is live incl edge-flip re-entry on both
  legs). BUT found the break: **82 paper_ingame rows EVER, 0 EVER settled** -> the in-game channel
  placed but NOTHING graded (no outcome, no CLV, no bankroll impact, no learning). ROOT: inplay_daytrader
  places via paper_ingame.record_ingame_bet and paper_ingame.grade_live can settle one, but NOTHING
  called grade_live -- there was no in-game SETTLE arm (pregame has one, in-game never got one), and the
  same KALSHI-TICKER-not-ESPN-id gap meant an ESPN-id settler could never match anyway. FIX: added
  MlbOutcomeResolver.final_score (ticker->boxscore final score, not just win/loss) + NEW
  scripts/platformkit/ingame/ingame_paper_settle.py: loads OPEN paper_ingame rows, resolves each MLB
  bet's final score from the ticker, calls grade_live -> settles with real outcome+unit_result;
  idempotent (already-settled edge_keys skipped), a not-yet-final game stays OPEN, soccer stays open
  pending a soccer resolver; +5 tests. RAN LIVE: settled 68 stuck MLB bets (33W-35L, net +0.353u, all
  unique settle_keys = dedup-safe), 5 MLB still-open (not final) + 9 soccer open. Registered ProcSpec
  m27_ingame_paper_settle (--interval 900) so it settles continuously; test_manifest resynced. Also
  fixed a latent flake: ingame_segment_trust corpus split used builtin hash() (per-process randomized
  -> non-reproducible verdict) -> switched to md5 (deterministic). ANSWER TO USER: paper trading IS
  working -- pregame end-to-end, and in-game now settles too (was the one broken leg). 50+ touched tests
  green; <=300 LOC each, ASCII, units-only no $, executed=False, edge_claimed=False, no flag armed.
  m27 goes live on next boot.ps1 restart (I already ran the backfill by hand). NEXT: a soccer in-game
  outcome resolver (domains/soccer results) so the 9 open WC in-game bets settle too.
- WAKE-28 (SELF-IMPROVING EXECUTION via CROSS-CORPUS trust gate + CORRECTS WAKE-27, 2026-07-01):
  user "keep building ai so it independently gets better execution." Built the mechanism that lets
  in-game EXECUTION improve on its own -- and immediately caught that WAKE-27's headline was a
  single-fold ARTIFACT. Split the labeled MLB corpus into TWO INDEPENDENT corpora (even-dated vs
  odd-dated games) and re-ran the outcome verdict on each: corpus A (50 games) = ALL innings MATCH;
  corpus B (44 games) = I5-I8 BETTER_THAN_VENUE (big deltas). So the "I5-I9 beats venue" from WAKE-27
  was driven ENTIRELY by one half and does NOT replicate -> under the discipline (">=2 independent
  corpora; single-fold lifts are artifacts") it is NOT robust. Correct honest state: NO segment is
  proven better-than-venue across independent corpora. BUILT the gate that enforces this automatically:
  scripts/platformkit/ingame/ingame_segment_trust.py (splits corpus into >=2 disjoint date-parity
  corpora, runs ingame_outcome_verdict on each, marks a segment TRUSTED only if BETTER_THAN_VENUE in
  EVERY non-insufficient corpus / ADVERSE only if WORSE in every / else NEUTRAL; writes
  data/frontend/ops/ingame_segment_trust.json; +7 tests). WIRED into inplay_capture_loop._build_tick:
  floor_for_segment routes the in-game EV floor -> an ADVERSE segment reverts to the STRICT
  pre-registered floor (suppress its marginal relaxed bets), TRUSTED/NEUTRAL/unknown keep today's
  relaxed floor. So execution self-improves as games accrue but changes ONLY on cross-corpus PROOF;
  thin/unreplicated data changes nothing (do-no-harm), reversible via CV_INGAME_SEGMENT_TRUST=0.
  LIVE build: EVERY segment NEUTRAL (correct -- nothing replicates yet) -> ZERO execution change today,
  gate armed to act autonomously when a lift replicates (->TRUSTED) or a segment proves consistently
  worse (->ADVERSE->auto-suppress). Registered ProcSpec m26_ingame_segment_trust (--interval 1800),
  test_manifest resynced. 46 touched tests green; <=300 LOC, ASCII, no $ field, edge_claimed=False,
  flips no flag, places no bet. Live on next boot.ps1 restart. NEXT lever unchanged but now
  AUTOMATED: the gate itself promotes/demotes segments; watch ingame_segment_trust.json cross slates.
- WAKE-27 (IN-GAME MODEL better-than-venue vs OUTCOME in innings 5-9 -- FULL-CORPUS ONLY, NOT
  cross-corpus-replicated: see WAKE-28 correction, 2026-07-01):
  user "keep making paper trade better ... beat pinnacle, make money on kalshi/sportsbooks." Attacked
  the recurring in-game-CALIBRATION lever (WAKE-24/25/26 all ended pointing here). The in-game CLV
  verdict could only compare model vs the CONTEMPORANEOUS venue price (MATCH, hit_rate 0.42) and the
  per-segment CLV showed the model leaning BELOW the Kalshi price in late innings -- ambiguous: model
  lag, or the thin venue quote lagging? SETTLED IT by unlocking OUTCOME labels. ROOT of the blind spot:
  capture writes each game's grade series keyed by the KALSHI TICKER (KXMLBGAME-26JUN241845PHIWSH), not
  the ESPN id, so settle_stamp (keys by ESPN id) never landed a home_win label -> the whole in-game
  layer had ZERO outcome labels. FIX: the ticker ENCODES date+away+home abbrs -> NEW
  scripts/platformkit/ingame/ingame_outcome_label.py (parse ticker + Kalshi->ESPN abbr alias map
  AZ->ARI/CWS->CHW/... + unambiguous away|home split, join to data/domains/mlb/espn_boxscores.parquet
  -> home_win; offline, leak-free, None on unresolved/tie/non-final; +7 tests). Refreshed the stale box
  parquet (ended 06-25) through 07-01 via domains.mlb.ingest_espn_box (keyless ESPN, +73 finals) ->
  labeled 94/97 captured games. NEW scripts/platformkit/ingame/ingame_outcome_verdict.py: per inning
  segment, Brier(model vs OUTCOME) vs Brier(venue in-play price vs OUTCOME), per-GAME clustered
  bootstrap CI (game = cluster unit), verdict BETTER_THAN_VENUE only when CI upper < 0; +6 tests.
  LIVE VERDICT (data/frontend/ops/ingame_outcome_verdict.json): I1-I4 MATCH, I5-I9 BETTER_THAN_VENUE
  (delta -0.032/-0.035/-0.043/-0.052/-0.060, all CI upper<0), UNK MATCH. So from the 5th inning on our
  live model is BETTER CALIBRATED TO TRUTH than the Kalshi in-play price -> the two-sided daytrader's
  late-inning disagreements with Kalshi are usually RIGHT (validates placing there; be cautious I1-I4=
  MATCH). HONEST CAVEATS (binding): Kalshi in-play is THIN/LAGGY, so this beats a STALE VENUE QUOTE, NOT
  an efficient close, NOT Pinnacle, NOT a $ edge; better-Brier != profitable (round-trip cost ~3-5c);
  n=29-45 games/1 slate window = single-fold, artifact risk. So made it CONTINUOUS: ProcSpec
  m25_ingame_outcome_verdict (--interval 900), test_manifest resynced (32 green) -> accrues games across
  slates so the lift replicates or washes out honestly. NO placement behavior change this wake (honest:
  don't act on one window); NEXT lever = once BETTER_THAN_VENUE holds across >=2 slate windows, gate the
  in-game EV floor to relax ONLY in proven segments (I5+) and stay strict/suppress where MATCH. All
  under scripts/platformkit/ingame (safe area), <=300 LOC, ASCII, no $ field, edge_claimed=False, no
  flag armed, paper-only. Live on next boot.ps1 restart (running supervisor has pre-edit manifest).
- WAKE-26 (NEUTRAL-SITE: user "this game has no home or away" -- VERIFIED model already correct + made
  it VISIBLE, 2026-06-29): user flagged that a World Cup game has no home/away so the model shouldn't use
  it. INVESTIGATED + corrected my own prior over-claim (I'd blamed hfa_lambda): soccer_intl does NOT use
  the domestic hfa_lambda -- it uses domains/soccer_intl/predictor.IntlSoccerPredictor which DEFAULTS
  neutral=True (no home tilt) for both predict + predict_live, and the in-game path doesn't override it.
  PROVEN by pricing GER-PAR both ways: NEUTRAL (what we use) Germany pregame 0.689 / live33 0.589 vs
  HOME-TILT 0.888 / 0.793; snapshot home_ml=0.689 confirms neutral is applied. So NO home/away bug --
  the model already drops the tilt exactly as the user wants; the home/away labels are just pairing tags.
  The 0.589-vs-market-0.67 gap is small IN-GAME calibration (our favorite-decay slightly faster than the
  market), NOT home/away, and likely our model trailing an efficient market. REAL residual = the
  snapshot/state stamped neutral=None (why it LOOKED like home/away context). FIX: ingame_live_state now
  stamps a `neutral` flag (_neutral_site: honors ESPN competition.neutralSite when present, else True for
  soccer_intl) so the in-game state explicitly carries neutral=True; +helper, 10 tests green; LIVE-
  VERIFIED GER-PAR -> neutral=True. Additive, no model/number change (model was already neutral), no flag,
  no bet. NEXT real lever stays in-game CALIBRATION (favorite time-decay), not home/away.
- WAKE-25 (IN-GAME ROOT CAUSE FIXED: signal was HOME-ONLY -- now two-sided + places live, 2026-06-29):
  user "world cup going on, should be working." There WAS a live WC game (GER-PAR 33' 0-0). Ran the
  m24 funnel against it: 47 markets -> 1 live_state (the live game resolved fine; 46 upcoming = correct
  no_live_state) -> cleared model_prob/home_leg/priced -> died at below_floor. Dug in: GER-PAR our model
  Germany 58.5% vs market 69% = +EV on PARAGUAY (away). ROOT CAUSE: scripts/platformkit/ingame/
  inplay_edge_signal.evaluate was HOME-ONLY (SIDE='home'; ev = ev_vs_price(mp, home_dec) only) -> every
  AWAY-side edge (~half the opportunity space) silently discarded -> the dominant reason the in-game
  channel placed ~nothing (1 ever vs 136 pregame). FIX: made evaluate TWO-SIDED -- price both legs at
  no-vig fair, take the +EV side, return side/bet_model_prob/bet_devigged_price; the GRADE pair stays
  HOME-aligned (leak-free) -- only the placed bet's side/odds/prob reflect the chosen leg. Wired
  inplay_daytrader.on_tick to place ev['side'] (was _sig.SIDE) with that side's prob+decimal. Also
  added mlb to _INGAME_RELAXED_EV_FLOOR (was soccer-only -> MLB in-game now places too). Tests: updated
  1 relaxed test (model==fair is the true no-edge point under two-sided) + added 2 two-sided tests; 14
  edge-signal + 7 capture-loop green; the only 2 reds (test_inplay_daytrader G1/G2) are PRE-EXISTING
  (verified via git stash: fail on original code too -- short game_id write-guard, not mine). LIVE-
  VERIFIED: re-ran poll_once -> GER-PAR action=bet tier=A on the away (Paraguay) edge, n_bets=1 (was 0).
  HONEST CAVEAT: an 11-pt in-game disagreement is MORE LIKELY in-game MODEL MISCALIBRATION than a real
  edge; this is PAPER, CLV-graded -- the mechanism is now correct + places, the NEXT lever is in-game
  model CALIBRATION so the disagreements are trustworthy. No $ claim, no flag armed, paper units only.
  Goes fully live on next boot.ps1 restart (running supervisor has pre-edit code).
- WAKE-24 (IN-GAME is the move, pregame ML isn't -- DIAGNOSE the in-game starvation, 2026-06-29):
  user "pregame ML isn't the move, should make a lot of bets in-game, efficient paper trading, make
  in-game edges better." QUANTIFIED the inversion from the live ledger (data/frontend/clv_ledger.jsonl,
  137 rows): paper(pregame props)=80, paper_pm=30, moneyline(pregame ML)=26, paper_ingame=ONLY 1 (and
  malformed: market_prob/edge/units=None). So 136 pregame vs ~0 in-game -- exact inverse of the thesis.
  ROOT CAUSE (ran inplay_capture_loop.poll_once live): 48 Kalshi game markets, ALL 48 -> no_live_state,
  model_prob=None; n_live=0 + statsapi live_games=0 -> there are simply NO in-progress games right now
  (19:36Z = ~3:36pm ET; tickers are 26JUL01 = 2 days out; MLB slate starts ~23:00Z). So 0 in-game NOW
  is CORRECT. BUT historically 1-ever vs 136 => during live windows the funnel (markets->live_state->
  model_prob->home_leg->priced->tier_floor->bet) drops almost everything and we had ZERO visibility
  into which stage. The engine is SOUND + wired (inplay_daytrader uses the LIVE leak-free model via
  live_board.live_model_home_prob, edge gate inplay_edge_signal = calibration_justified+liquid+fresh+
  tier, quarter-Kelly, places to paper_ingame; capture loop m2 calls on_tick every tick). NOTE: the
  SEPARATE pm_game_placer(m12) prices LIVE exchange markets with the PREGAME model -> stale-edge bets;
  the real in-game channel is the daytrader, not m12. BUILT instrumentation:
  scripts/platformkit/ingame/ingame_placement_funnel.py (folds poll_once's per-game decisions into the
  stage funnel + reason histogram + biggest_dropoff; writes data/frontend/ops/ingame_placement_funnel
  .json; +8 tests) -> ProcSpec m24_ingame_placement_funnel (--interval 300), manifest resynced (21
  green). Live on next boot.ps1 restart. NEXT (do it DURING tonight's live slate ~23:00Z+): read the
  funnel -> if drop is at live_state, fix the Kalshi-ticker->live_state resolution for the daytrader
  (the chronic id gap, see WAKE-20d/21); if at tier_floor, the gate is too tight for in-game. Diagnose
  on REAL live data, then tune so in-game places a lot. Candidate/diagnostic only; no bet, no flag,
  no $ edge.
- WAKE-23 (FIND GAPS IN OUR OWN SCRAPED LINES, all sports, 2026-06-29): user corrected -- they have
  their OWN scraper, NEVER meant to use OddsAPI; want the NBA "every little detail" edge-hunt applied
  across sports on the lines WE are getting. FOUND the real feed: data/cache/line_history/<sport>/
  <date>.jsonl = our DK(via espn)/FanDuel/Pinnacle scrape, ML+spread+total, devigged+timestamped,
  fresh today (7610 MLB rows). OddsAPI was a red herring. BUILT sport-blind gap-finder
  scripts/platformkit/clv/scraped_line_gaps.py (reads OUR feed, groups by game/market/line, best book
  price vs Pinnacle-sharp-fair via vetted best_price; +8 tests). 1st live run found 2 MLB "gaps"
  (Yankees@RedSox FanDuel 3.50 away +31.6% CLV) -- I CHECKED them: STALE-DATA MIRAGE (DK 02:53 said
  50/50; FanDuel quote was 30min OLD at 02:23 saying 74/26; +CLV on BOTH sides = the tell). THE LESSON
  = finding apparent gaps is trivial; the real work is rejecting the ~99% stale/mismatched. ADDED a
  FRESHNESS GATE (max_stale_sec default 600: within each game/market/line, drop any quote staler than
  the freshest) -> mirages correctly vanish -> honest EMPTY (slate efficient across our 3 books when
  comparing only contemporaneous quotes). Then made it AUTONOMOUS: scraped_line_gaps_daemon.py (~4min,
  writes data/frontend/ops/scraped_line_gaps.json + scraped_line_catches.jsonl only on a real fresh
  gap; +6 tests) -> ProcSpec m23_scraped_line_gaps, manifest resynced (21 green). Goes live next
  boot.ps1 restart. CANDIDATE-ONLY: our files only, no OddsAPI, no bet, no flag, +CLV is prob space.
  REAL LEVER now: more CONTEMPORANEOUS books (only 31-34/143 MLB groups shoppable >=2 books; NBA/
  soccer_intl ~1 book) + tighter capture cadence to catch transient gaps in their live window.
- WAKE-22 (USE-MORE-BOOKS / FIND-GAPS made CONTINUOUS, 2026-06-29): user frustrated "ai should
  make money on kalshi/DK, use more books, find gaps". Ran the honest model-free lever LIVE:
  best_price_audit (best price across all wired books vs sharp fair) over the real slate = ZERO +CLV
  gaps at min-clv 0.0 (26 MLB games, 13 shoppable, MAX 3 books each; soccer pinnacle 401). Market is
  efficient at line-shop level RIGHT NOW. KEY: cross-book gaps are TRANSIENT (book lags ~60-120s) so a
  manual one-shot run misses them -> the lever only pays off polling continuously. BUILT
  scripts/platformkit/clv/best_price_scan_daemon.py (every ~4min runs the scan, writes
  data/frontend/ops/best_price_scan.json, appends data/frontend/ops/best_price_catches.jsonl ONLY when
  a real +CLV gap appears; injectable/offline tests; +6 green) -> registered as supervisor ProcSpec
  m22_best_price_scan (argv --interval 240), test_manifest resynced (21 green). CANDIDATE-ONLY: reads
  public odds, places NO bet, flips NO flag, +CLV is probability space NOT a $ edge. Goes live on next
  boot.ps1 restart. HONEST FRAMING for the user: the two real "more books" levers are (1) this
  continuous scan + (2) WIRING MORE bettable feeds (only 3 shoppable; Pinnacle soccer auth 401, add
  MGM/Caesars) to widen best-of-N -- NOT a smarter model. The bleed is still PROPS (139-217); pregame
  prop calib suppress-gate is the next unit-saver (lane 2, not yet built this wake).
- WAKE-16 (CEILING, 2026-06-26 ~10:00): SHIP b3b108da -- first ceiling experiment: reclaim MLB
  sp_ra_diff_asof through the REAL WF DM gate -> HONEST REJECT (Brier 0.240312->0.240226, DM p=0.54;
  planted-null collapses; truncation-invariant). SP form priced into the close. Reusable as-of
  reclaim gate template kept. NEXT: CQR prop intervals (domains/basketball_nba/prop_cqr.py) -- the
  prop-coverage gap has REAL room (efficient pregame reclaims will mostly reject; props + in-game won't).
- WAKE-20 (UNITS: reset + stopped the measured bleed + FE open, 2026-06-27): user asked to fix
  everything, reset units, make as many units as possible. (1) RESET paper bankroll 100u via NEW
  reversible scripts/platformkit/paper/bankroll_reset.py (archives the 5 canonical settled/display
  files to _ledger_archive/<ts>_reset, reinits, daemon reconciles -> live 100.0u, net 0; +3 tests,
  --restore reverses). Was -2.06u (net -102u, -24u that day). (2) DIAGNOSED the -31% in-game-prop
  CLV bleed on the archived ledger (n=402 true-close): mean CLV -31.0% (CI excl 0) AND CLV is
  ANTI-correlated with model edge (corr -0.083; high-edge half -34.7%) -> tightening the vig/edge
  gate selects the WORST bets. Channel is structurally adversely-selected; only CLV-positive action
  is STOP placing it. (3) NEW scripts/platformkit/improve/ingame_prop_clv_guard.py SUPPRESS-ONLY,
  DEFAULT ON (CV_INGAME_PROP_CLV_GUARD=0 restores broad-capture), composed into auto_loop
  _place_ingame_props gate (over the optional calib gate). +4 tests; 16 trader+guard green. Honest
  anti-edge move, NOT an edge claim, paper units only, no flag armed ON. (4) FE verified live +
  opened: :3000 UI + :8098 boards + :8099 API all 200; /api/paper/today reflects 100u; supervisor
  23/23 READY. CAVEAT: guard takes effect on NEXT stack restart (running auto_loop process has the
  old code cached) -> restart boot.ps1 to apply live. WAKE-19 below stands.
- WAKE-20b (RESTARTED LIVE, running without user, 2026-06-27 ~15:45): boot.ps1 -Stop (clean drain,
  21 PIDs) then boot.ps1 -> governance preflight PASSED, ONE supervisor (PID 17612, singleton OK),
  24/24 procs READY incl m19_asof_reclaim (ran its first autonomous sweep: tick=0 candidates=6
  reject=6, scoreboard row written, now daily). In-game prop CLV guard is_enabled()=True in the live
  auto_loop. The pre-restart guard-less code had queued 34 open paper_ingame_prop positions ->
  re-baselined bankroll to a clean guard-on 100u (2nd reversible reset; archived). FE live: :3000 UI
  + :8098 + :8099 all up; /api/paper/today + supervisor doc show 100u + 24/24. Stack is detached
  (Start-Process) so it persists unattended. m19 + bleed-fix are now LIVE.
- WAKE-20c (IN-GAME edge-hunt made CONTINUOUS, 2026-06-27): user thesis = in-game has the small
  gaps, hunt them continuously w/ live data+lines. FOUND: m11 already logs model+market tick series
  per live game (data/cache/ingame_grade/<sport>/) and ingame_clv_grade.grade_sport produces the
  honest in-play-close anticipation verdict -- but it was MANUAL-ONLY (segment_emit not on flywheel).
  Ran it live (8 live MLB games): MLB MATCH mean_clv=+0.043 over 24.7k ticks/70 mkts (BEAT 24 vs
  BEHIND 16); soccer MATCH +0.035. The in-game GAME engine is the one channel that leans POSITIVE
  (vs -31% props). BUILT m20_ingame_clv_verdict daemon (scripts/platformkit/ingame/
  ingame_clv_verdict_daemon.py, ~10min loop, writes data/frontend/ops/ingame_clv_verdict.json; +5
  tests) -> registered as ProcSpec m20 (25 procs), test_manifest resynced. RESTARTED: 25/25 READY,
  m20 wrote first verdict, guard still ON. Verdict is CLV/probability MATCH not BEAT -- NOT a $ edge;
  the honest path is to keep measuring so a real persistent gap crosses MATCH->BEAT on its own.
- WAKE-21 (MATCH->BEAT TRIGGER wired, 2026-06-27): user asked "when does in-play start beating".
  Refused a profit DATE (would fabricate a $ edge); instead made the crossing self-firing. Why MATCH
  not BEAT today: mean_clv +0.0255 = 2.5 prob pts < the ~3-5c in-play round-trip cost, hit_rate 0.43,
  BEAT 23 ~= BEHIND 20. BUILT scripts/platformkit/improve/ingame_baseout_gate.py (leak-free: target =
  in-play close = last market tick; residual = close - model_prob_t; does deep base-out/RE24/count/
  pitch state predict the residual OOS? two chronological corpora must BOTH improve RMSE w/ DM p<0.05
  AND planted null must collapse -> SHIP_REVIEW else REJECT; INSUFFICIENT below 4000 ticks/16 games) +
  ingame_baseout_gate_daemon.py (hourly) registered as ProcSpec m21 (26 procs); +12 tests, manifest
  resynced; 33 green. LIVE corpus today = 1792 ticks / 14 games -> honest INSUFFICIENT (still filling;
  deep state only flows post-id-fix). Candidate-only: flips NO flag, places NO bet, probability space.
  CAVEAT: m21 goes live on NEXT boot.ps1 restart (running supervisor has pre-edit code). The verdict
  now crosses MATCH->BEAT (or REJECTs the lever) on its OWN -- no date-guessing.
- WAKE-20d (DEEP in-game variables, 2026-06-27): user wants deep live data (base-out, every variable)
  collected constantly + validated. SHIPPED the deep MLB base-out extractor:
  scripts/platformkit/ingame/ingame_baseout_mlb.py (parse_baseout + baseout_summary_fields; outs,
  baserunners 3-bit base_state, base_out_state 0-23, standard RE24 run-expectancy table, count; leak-
  free, never raises; +7 tests). VALIDATED ON 8 REAL LIVE GAMES (e.g. ARI@TB 1out/runner-on-3rd
  RE24=0.950; SEA@CLE 0out/1st 0-2). Wired ADDITIVELY into ingame_live_state._extract + the
  live_grade._state_summary key tuple (outs/base/bos/re/count) -> when capture resolves an ESPN
  event, the series now carries the deep state (verified end-to-end: "...inning=7 half=bottom outs=1
  base=4 bos=13 re=0.95 count=1-1"). 22 tests green; restarted 25/25 READY.
  HONEST BOTTLENECK (not fixed): the ACTIVE capture keys games by KALSHI TICKER (KXMLBGAME-...), and
  ingame_live_state.live_state("mlb", <kalshi_ticker>) returns None -> empty state ("live") -> deep
  vars (and even score/inning) DON'T flow into the active Kalshi-keyed series. Pre-existing ID gap,
  not caused by this change. NEXT: build an ESPN<->Kalshi live-game id resolver (parse ticker ->
  date+away+home -> match ingame_box_mlb.live_games -> statsapi situation has outs+offense) so deep
  capture flows into the active series; THEN once accumulated, gate base-out -> win-prob (no base-out
  corpus exists yet, so collection must precede validation). Extractor is built+validated; the
  resolver is the remaining wire. No edge claim; calibration/descriptive state only.
- WAKE-21 (BLOCKER UNBLOCKED: deep state flows into the ACTIVE series, 2026-06-27): built the
  ESPN/Kalshi<->statsapi MLB id resolver WAKE-20d called for -> deep base-out now flows into the
  active Kalshi-keyed in-play series continuously, LIVE-VERIFIED. NEW
  scripts/platformkit/ingame/ingame_id_resolver_mlb.py: parse_kalshi_mlb_ticker (KXMLBGAME-26JUN271810
  AZTB -> date/time/away+home blob) + resolve_ticker (matches the blob's away+home Kalshi abbrevs vs
  ingame_box_mlb.live_games full team names by EXACT mapped-abbrev equality; KALSHI_ABBR = all 30
  clubs incl the divergent AZ/CWS/WSH/SD/SF/ATH; UNIQUE match only -> 0 or >1 (live doubleheader) =
  None, NEVER mis-binds) + linescore_deep_fields (adapts the statsapi linescore offense/outs/count ->
  the EXISTING ingame_baseout_mlb.parse_baseout, reuse not duplicate) + deep_state_for_ticker +
  make_tick_deep_fn (LAZY per-tick enricher: no candidate fetch until a real game needs it -> dead-feed
  test path stays offline); +13 tests. ROOT CAUSE found beyond the id gap: inplay_capture_loop._build_
  tick PROJECTED the state to 7 keys, DROPPING score/inning/outs/base/bos/re/count before they reached
  live_grade._state_summary -> every tick logged a bare "live" even when ESPN HAD the situation. FIX:
  widened _build_tick passthrough (label-only; model/price/sizing read top-level tick fields, cannot
  change a decision) + merged the statsapi deep state in _process_game (mlb-only, additive, guarded) +
  threaded deep_state_fn through poll_once/serve_forever (default OFF -> existing tests offline) +
  enabled mlb_deep=True in inplay_capture_runner.run (production). LIVE-VERIFIED on the real slate:
  all 7 in-progress games resolved to correct gamePks with accurate base-out; the abbrev map has ZERO
  gaps (every live ticker blob decomposes uniquely into known abbrevs); after restart the SAME active
  file KXMLBGAME-26JUN271915MIASTL flipped from "live" -> "home_score=0 away_score=4 inning=5 half=top
  outs=0 base=0 bos=0 re=0.481 count=0-0"; 10 live series now carry deep base-out. Restarted 25/25
  READY, guard is_enabled()=True. No edge claim; descriptive state + the gamePk join-key for items 2-4.
  PRE-EXISTING (not mine, noted): tests/platformkit/ingame/test_inplay_daytrader.py 2 fails -- the
  paper_ingame _is_malformed_input write-guard now rejects the tests' short "G1"/"G2" game_ids
  (added in a prior WAKE; my changes don't import that path).
  ALSO SHIPPED item 2 (deeper vars) same wake: NEW scripts/platformkit/ingame/ingame_pitcher_mlb.py
  (pure adapters tto_for + pitcher_batter_fields; current pitcher/batter from linescore
  defense.pitcher/offense.batter, pitch_count from boxscore numberOfPitches, TTO =
  battersFaced//9+1 = the order-turn the batter NOW up is in -> the 3rd-time-through zone; +7 tests).
  Wired ADDITIVELY into deep_state_for_ticker (one extra boxscore fetch/game/tick, guarded -> base-out
  still flows if it misses); pitch_count + tto added to _build_tick passthrough + live_grade._state_
  summary (numeric tokens only -- pitcher/batter NAMES flow in the structured state but stay OUT of the
  space-joined label to avoid breaking k=v parsing). LIVE-VERIFIED: e.g. COLMIN Paredes pc=73 tto=3,
  SEACLE Cecconi pc=81 tto=3; after 2nd restart the active MIASTL series carries
  "...outs=2 base=0 bos=2 re=0.098 count=0-0 pitch_count=13 tto=1". 25/25 READY, guard ON. COST NOTE:
  the capture tick now does ~1 schedule + 2N linescore + N boxscore fetches/tick (N live games);
  boxscore ~100KB each -- monitor tick cadence (heartbeat as_of should advance ~every 20s); throttle the
  boxscore if cadence slips. NEXT (queue): item 3 in-game true-close capture (m21, exact-gamePk/game_id
  join, mirror m16/m18) -> item 4 gate base-out+tto -> win-prob once the corpus accumulates.
- WAKE-19 (AUTONOMY: ceiling loop runs WITHOUT Claude, 2026-06-27): wired the asof-reclaim gate
  as a self-running daemon. NEW scripts/platformkit/ceiling/asof_reclaim_sweep.py (gates ALL on-disk
  *_diff_asof candidates: NBA ast/dreb/fg3m/stl/blk + MLB sp_ra_diff = 6, normalizes to one verdict
  row, logs each to reject_ledger + ceiling_reclaim_scoreboard.jsonl, control-failing SHIP ->
  SHIP_REVIEW, never auto-ships) + asof_reclaim_daemon.py (daily loop, survives any tick failure,
  injectable for tests) + test (7 green). Registered as supervisor ProcSpec m19_asof_reclaim in
  supervisor/stack_specs.py (readiness NONE, _FOREVER, no depends_on) -> launches on next supervisor
  boot, 24 procs now. ALSO resynced the STALE tests/supervisor/test_manifest.py frozen proc set (it
  stopped at m13; m14-m18 were added in prior sessions but never added to the test -> it had been RED
  for sessions; now includes m1_bankroll + m14-m19, 36 green). First sweep: ALL 6 REJECT (efficient
  pregame). predictor.py untouched, NO flag flipped, candidate-only. WAKE-18 below stands.
- WAKE-18 (CEILING / Rung-2 player intel, 2026-06-27): reclaimed leak-free `ast_rate_diff_asof`
  (asof_features.parquet) through the REAL single-corpus WF DM gate vs leak-free Elo ->
  HONEST REJECT. NEW domains/basketball_nba/asof_ast_rate_eval.py (+test, 3 green), reuses the
  MLB asof_ra_diff_eval template (inlined helpers, F5-isolated). Brier base 0.205026 -> cand
  0.205040 (delta -1.4e-5, WORSE); DM p=0.78; fitted feat_w shrinks to -0.006; planted-null
  collapses (p=0.997); truncation-invariant. base Elo BSS 0.174 (non-degenerate). VERDICT: assist
  rate carries NO incremental win-prob signal over Elo -- the memo "AST ~+4-5%" was a box/prop
  artifact, not a team edge. player_plusminus.json is scouting_only (season aggregate = leaky) so
  it is NOT gate-able; this is its leak-free cousin. Logged to reject_ledger. CANDIDATE-ONLY, no
  flag flipped, predictor.py untouched. Remaining pregame box reclaims (dreb/fg3m/stl/blk diffs)
  expected REJECT too (efficient); the real lever stays IN-GAME conditioning.
- WAKE-17 (READINESS, 2026-06-26 ~16:46): flywheel healthy (23/23 procs all READY incl m15-m18).
  V1 PROP SETTLEMENT = VERIFIED WORKING, NOT a bug: the 47 props tagged game_date=2026-06-25 are
  actually 06-26 games (statsapi: Nationals@Orioles, Cubs@Brewers etc. are 'Scheduled' today, NOT
  final) -- an ET-day mis-tag the settler's date+1 fallback already handles -> honest-pending, will
  settle tonight when the 06-26 slate goes final. prop_settler_mlb._team_hit/city-abbrev fix is
  sound (ran live: full-name matchups resolve correctly; the mismatch was date, not name).
  SHIP 7a3fc3ba (V3): clv_ledger.record_bet now labels market_type on market-omitted ML rows.
  ROOT: record_bet only persisted 'market' when a caller passed it, but the game-ML placers
  (run_paper_today/m1_paper + /api/clv/record) omit market= -> rows got a moneyline bet_id but NO
  market/market_type field -> rendered '?' (9 live mlb rows). Now persists bet_id()'s resolved
  value (default 'moneyline') as BOTH market+market_type at the writer (honest label, identity key
  already classified them ML). +2 fail-before/pass-after tests; 12/12 file + 100/100 dependent
  (sanity/dedup/betid/guard) green; ASCII; LOC 327. Note: the 9 EXISTING rows aren't retro-fixed
  (don't race the live ledger) -- forward-looking; a betid_backfill relabel could clean them later.
  Scoreboard: props settled 7 (all PM ML, all clv no_close); boards games-shown mlb 0 / soccer_intl
  0 (V2/V4 OPEN: API serves 200 but games:[] -- next wake: honest-empty vs fill-bug); CLV 7 settled
  / 0 clv; bankroll 97.0u (net -3.0, day 06-26); reject delta 0 (plumbing SHIP, no signal).
  NEXT: V2/V4 -- determine if mlb/soccer_intl empty boards are honest-empty (no card clears the
  tier gate / no game in-window) or a real snapshot-fill bug; fix if bug, document if honest.
- WAKE-18 (READINESS, 2026-06-26 ~17:15): flywheel healthy (23/23 all READY; m13 was mid-RESTART,
  transient). OPS MISHAP + RECOVERED: ran `stop_bot.py --status` to probe -- it IGNORES args and
  ACTUALLY STOPPED the bot (disabled CourtVisionBot task + set stop_requested=True). Reversed BOTH
  (schtasks ENABLE + cleared flag); flywheel never halted (separate from go.ps1). Memory written:
  PROBE the stop flag by READING .bot_state/live_status.json, NEVER run stop_bot.py.
  V2/V4 = FALSE ALARM (my WAKE-17 probe bug): /api/v1/bestbets/<sport> serves status=ok, mlb 16
  games/94 candidates/10 best_bets + soccer_intl 6 games/24 candidates/0 best_bets (honest no-bet,
  games shown); daemon board /api/bestbets/board serves 2461 mlb / 85 soccer cards (stale=by-design
  serve-stale-never-green). The WAKE-17 'games:[]' was reading d['cards'] when the envelope uses
  d['games']. NO board bug. Corrected here.
  SHIP 94cc287c (grade_summary under-count): scoreboard.py n_settled required clv_pct, so all 7
  settled paper_pm bets (clv_status='no_close') were dropped -> grade_summary.json showed n_settled=0
  + flat_unit 0/0 despite a real 1W/5L paper record. Split populations: n_settled + flat-unit count
  ALL settled; CLV stats stay over the clv-bearing subset; added n_no_close. Live now n_settled 0->7,
  flat-unit 1W/5L (mlb 6 / soccer 1), CLV INSUFFICIENT_DATA. SAFE: real-money gate recomputes from
  ledger rows (summary advisory-only, never trusted) -> cannot spoof. NEW test_scoreboard.py +5; 59
  dependent green; artifact regenerated. Scoreboard: props settled 0 (pend to tonight's finals);
  boards games-shown mlb 16 / soccer_intl 6 (WORKING); CLV 7 settled / 0 clv; bankroll 97.0u (net
  -3.0); reject delta 0 (SHIP). NEXT: K1 Kalshi team_total pricer (add SP-ERA + park, calibrate vs
  settled totals through the REAL gate, place ONLY if it beats/matches the Kalshi line OOS else
  REJECT-log) -- the next genuine ceiling/exec item now that V1-V4 are closed.
- WAKE-22 (READINESS, 2026-06-26 ~17:30): m13 props pred tick RESTART FLAP ELIMINATED (was restart
  every ~22 min, now zero restarts). TWO-LAYER FIX in scripts/platformkit/props/props_pred_tick_runner.py:
  (1) START beat at tick() entry (was end-only -> sleep+score=700s > 660s fresh threshold);
  (2) NEW _beat_during_scoring background thread every 300s during _score_props_bounded (scoring
  700+ props takes 700-985s > threshold, so start-beat alone still fails after ~660s). FIX VERIFIED:
  background beater fired exactly at t+300s (mtime 17:35:00 updated mid-scoring), no supervisor restart
  since PID 31112 launch at 17:29:59. 9/9 tests pass, 297 LOC.
  ALSO: soccer_intl old DraftKingsProvider (sportsbook-us-il 404 endpoint) REMOVED from prop_edge_config.py
  + test updated (34/34 pass). MLB calibration cache stamped settle_logic_version=v2-void-nan (was null;
  stale warning fired every tick). Full stack: 14/14 supervisor daemons GREEN; paper_today fresh (388
  placed/444 pending/3 settled/-4.0u, executed=False, edge_claimed=False); predict :8099 200. STALE
  by-design (untracked/long-cadence): m14/m17/m15/m18/m8. Props snapshot stale (m13 scoring in progress);
  will refresh on next tick completion. NEXT (CEILING): T2 Mondrian group-conditional conformal OR E1
  LLM in-game context (detail_layer_gate template backend).
- WAKE-21 (CEILING/T4 ACI, 2026-06-26 ~15:57): SHIP fb468a9c -- 3rd CEILING experiment answered
  through the REAL gate -> HONEST REJECT (pinball + planted_null). Q: does Adaptive Conformal
  Inference (Gibbs+Candes 2021) hold in-game interval coverage under distribution drift better
  than static split-conformal? A: PARTIAL -- coverage YES, sharpness NO -> do NOT default-on.
  New leak-free streaming wrapper (scripts/platformkit/ingame/aci_online.py, 249 LOC, +13
  tests, +36 regression green): pure numpy+stdlib, online a_{t+1}=a_t+gamma*(a-err_t), width-
  only multiplicative adjustment per tick around base band midpoint. At tick t, ACI sees ONLY
  series[:t] -- never the future. Synthetic non-stationary stream (2000 ticks, sigma DOUBLES
  at t=500, gamma=0.05, target 90%): ACI coverage 89.9% (gap -0.001 vs nominal) vs static
  77.7% (gap -0.123) -- ACI RECOVERS coverage cleanly under drift. BUT pays pinball cost (wider
  bands) -> fails pinball <= static+0.01 check; planted-null (iid resample) shows residual-var
  drift even after shuffling -> null_collapses=False -> REJECT:pinball+planted_null. Default-OFF
  wrapper; not wired into ingame_serve_recal_seam. Reusable for in-game wp interval coverage
  replay, prop-band streaming, future Mondrian+ACI composition. Scoreboard: REJECT delta +1
  (ACI online).
  NEXT (CEILING queue, ranked): T2 Mondrian / group-conditional conformal on prop coverage
  (scripts/platformkit/mondrian_conformal.py) -- attacks per-regime miscoverage (stat x minutes
  -tier), wraps the EXISTING conformal/coverage code, the lightest-touch experiment that may
  unstick CQR's over-cover gap on subsets; OR T3 Venn-Abers in calibrator_zoo (probability
  validity guarantee + [p0,p1] band on the probability). T5 NGBoost stays last (highest reject
  prior given the pregame data ceiling).
- WAKE-20 (CEILING/T1 CQR, 2026-06-26 ~15:50): SHIP a642eac5 -- 2nd CEILING experiment answered
  through the REAL gate -> HONEST REJECT 7/7. Q: does CQR (Romano+ 2019, adaptive-width
  conformalized quantile regression) beat the incumbent constant-width split-conformal for NBA
  prop intervals? A: NO on the strict gate -- the existing split-conformal ships unchanged.
  New leak-free WF gate (domains/basketball_nba/prop_cqr.py, 288 LOC, +10 tests, +38 regression
  green) reuses pregame_oof.parquet (356k rows, 7 stats): chrono 50/25/25 train/calib/test split;
  lightgbm quantile (alpha/2, 1-alpha/2) over leak-free per-row features (rolling_mean_15,
  rolling_std_15, rolling_median_15, oof_pred, n_prior, season_avg_to_date -- all built from
  STRICT prior rows via shift(1)); conformity E_i = max(q_lo-y, y-q_hi) on CALIB; finite-sample
  conformal correction; planted-null = joint-shuffle features. Real-OOF verdict (n_test=11896
  per stat, alpha=0.10): CQR beats split-conformal on PINBALL on ALL 7 stats (pts ast reb fg3m
  stl blk tov; cqr_pb < sc_pb every row) AND on WIDTH (sharper bands every row) BUT
  OVER-covers nominal 90% (cov 91-96% vs sc 88-91%) -> fails |cqr_gap| <= |sc_gap| proximity;
  planted-null inflation (>1.5x) sub-bar on real data too -> REJECT all 7. Default-OFF
  measurement script; NOT wired into pricing. Honest finding: adaptive Q-quantile is sharper
  AND more conservative -- the conformal correction over-corrects on this OOF; the room CQR was
  supposed to fill (under-coverage) doesn't exist in the way the doc predicted on these splits.
  Reusable CQR template kept; next ceiling levers: (a) per-stat or per-(stat x minutes-tier)
  conformalization (Mondrian T2 hybrid), (b) T5 NGBoost / quantile-GBDT distributional head,
  (c) ACI online conformal for in-game (T4). Scoreboard: REJECT delta +1 (CQR adaptive-width).
  NEXT: T4 ACI online conformal in-game (scripts/platformkit/ingame/aci_online.py) -- attacks
  the freshness-drift gap, thin one-param wrapper, respects minimal-feature constraint, low
  rejection prior.
- WAKE-19 (CEILING/K1, 2026-06-26 ~17:45): flywheel healthy (23/23 all READY); stop flag clear
  (read .bot_state, did NOT run stop_bot.py). SHIP 1447a72b = K1 answered through the REAL gate ->
  HONEST REJECT. Q: does starting-pitcher RA-as-of + park factor make the Kalshi team_total pricer
  bet-ready? A: NO -- do NOT enable the placer. New leak-free WF eval (domains/mlb/totals_sp_park_eval.py,
  300 LOC, +5 tests) reuses totals_sigma_wf._build_wf_lambdas (run-rate lambda baseline) + OLS-blends
  asof_park.park_factor + (home+away)_sp_ra_asof; chronological 50/50 split, TRAIN-only centering,
  12037 held-out games: rmse_base 4.5621 -> rmse_combo 4.5313 (delta -0.0308 runs, BELOW the -0.05
  SHIP bar); planted-null collapses to -0.0003 -> the -0.031 is a small real-but-sub-threshold effect.
  Park+SP on totals is subsumed by the market, consistent w/ the prior moneyline SP-form REJECT.
  Verdict -> data/frontend/funnel/mlb_totals_sp_park_gate.json (scouting_only, vs_close UNPROVEN, no $).
  Kalshi team_total placer STAYS default-OFF -- never bet a biased pricer (exactly K1's bar).
  Scoreboard: props settled 0 (pend to tonight's finals); boards mlb 16 / soccer_intl 6 (working);
  CLV 7 settled / 0 clv (no_close); bankroll 97.0u (net -3.0); REJECT delta +1 (totals park+SP).
  NEXT (CEILING queue): CQR prop intervals (domains/basketball_nba/prop_cqr.py, GPU quantile fit) --
  the documented prop-interval under-coverage is REAL room (unlike efficient pregame reclaims);
  then E1 LLM in-game context (detail_layer_gate offline-template), asof reclaims NBA/tennis/soccer.
- INTELLIGENCE/LLM design DONE (2026-06-26c): docs/research/ceiling/LLM_CONTEXT_PRIORS.md -- use LLM +
  person-free brain to contextualize games into LEAK-FREE as-of priors, run independently (default-off
  daemon), gated like any signal. HONEST: LLM context is a HIGH-reject lane (CV_LLM_SCHEME already
  rejected +0.005 p=0.87; pregame efficient) -> ships SCOUTING-ONLY unless IN-GAME context moves OOS
  calibration. Reuses knowledge/contextual.py + detail_layer_gate.py (planted-null built in) +
  scheme_prior.py. CHEAPEST DECISIVE FIRST (E1, ~zero API cost): gate ONE regime_structural channel
  (offline template backend) through detail_layer_gate.py on 2 NBA seasons + shuffled-context null ->
  real SHIP/REJECT number; only pay for Haiku/PBP-text backend if the free channel survives.
  NEAR-TERM QUEUE: (next) CQR prop intervals, then E1 LLM in-game context, then asof reclaims (NBA/
  tennis/soccer), then ACI in-game.
- GAP AUDIT + FIX (2026-06-26 ~15:14, user "no gaps"): paper trading IS live (305 bets: 298 open/7
  settled, mlb+soccer_intl, newest 15:08). OLD-lines backtest fuel EXISTS (historical_event_odds 9.8M
  + line_history 193M). PRIMARY GAP FOUND + FIXED: live supervisor (20h uptime) predated m15_prop_settle
  / m16_prop_close_capture / m17_kalshi_scan / m18_pm_close_capture -> all 7 settled were clv_status
  'no_close' (CLV unmeasurable). RESTARTED stack (stop.ps1+go.ps1): now 23 procs all READY, the 4
  daemons LIVE -> close-capture + prop-settle restored; CLV will populate as bets settle. REMAINING
  SMALL GAP (queued for builder loop): grade_summary reports n_settled=0 despite 7 settled in ledger
  (PM settled rows not counted by the summary reader) -- fix the reader to count all settled + verify
  CLV starts populating post-restart.
- EXEC/EDGE AUDIT (2026-06-26 ~15:20, user 'kalshi high level + best bets + edges'): FRONTEND WORKING
  (webapp:3000=200, /api/paper/trail=200 real rows incl settled paper_pm w/ unit_result); KALSHI EXEC
  WORKING for game-winner ML (30 pm bets, m12 live; m18 now CLV-grades them post-restart); BEST-BETS
  board generating (best_bets.json cards, m10 fresh); m17_kalshi_scan scanning liquid surface. HONEST
  GAP (calibration not plumbing): Kalshi props/totals are LIQUID (~1c spread) but we do NOT bet them --
  totals pricer biased +5.2% (ignores starting pitcher+park), prop pricer matched 0 live. Betting them
  now = fabricated edge. PATH TO HIGH-LEVEL KALSHI (queued AHEAD of CQR/LLM): (K1) make kalshi_pricers
  team_total pricer bet-ready -- add starting-pitcher-ERA + park, CALIBRATE vs settled totals through
  the REAL gate, enable placement ONLY once it beats/matches the Kalshi line OOS; (K2) populate the
  prop board with Kalshi-offered stats so the prop pricer matches live; (K3) more soft books for +CLV
  line-shop. Build in scripts/platformkit/pm_trading/**; gated; never bet a biased pricer.
- EFFICIENCY AUDIT (2026-06-26 ~15:30, user 'efficient + GPU'): HONEST = system is ALREADY efficient,
  NO bloat to fix. GPU RTX 4060 healthy 42% util / 7GB free (torch cu121, CUDA avail). Daemon stack
  lean: ~3GB total / 24 procs (~120MB avg, max 394MB) + webapp 164MB. The 91% system RAM is mostly
  NON-system dev-box procs (editor/browser/Claude), not the AI. Do NOT make-work on lean daemons.
  STANDING EFFICIENCY DISCIPLINE (L5, folded into the loop prompt): (a) any NEW model fit the loop
  builds (CQR/NGBoost/Kalshi-totals calib) defaults to GPU (device='cuda'/gpu_hist/lgbm device='gpu')
  with CPU fallback; (b) offload heavy compute (CV, full-season WF) to RunPod 3090, keep local box
  light; (c) LLM = Haiku Batches + content-hash cache, call only on live state; (d) profile-before-
  optimize -- only touch a hot path with a measured before/after, never fabricate an efficiency win;
  (e) per-file tests only (full suite freezes the 16GB box).
- MLB + WC (soccer_intl) VERTICAL AUDIT (2026-06-26 ~16:10): BOTH live + betting on today's slate
  (mlb 285 bets=240 props+37 Kalshi ML, newest 16:08; soccer_intl 57=52 props+5 Kalshi ML, newest
  15:40). Models hardened tonight (mlb MOV-Elo bug fix + tests; soccer rho_fit fix). GAPS before
  'highest level' (queued, verify ahead of K2/CQR): (V1) PROP SETTLEMENT -- 0/292 props settled;
  m15_prop_settle just came online (2min) -> VERIFY props actually settle once games finalize; if not,
  it's the MLB city-abbrev name-match settler bug resurfacing (prop_settler_mlb._team_hit). (V2)
  FRONTEND BOARDS show 0 games for mlb + soccer_intl despite 240+52 props placed today -> snapshot
  fill/staleness gap (known FRONT-END FILL issue), NOT honest-empty -> regen + verify cards fill.
  (V3) 8 mlb bets have market_type='?' -> trace + label. (V4) bestbets API /api/v1/bestbets/<sport>
  returned a parse error -> verify it serves mlb + soccer_intl. CLV still pending (no_close on the few
  settled; populates as new bets settle with captured closes via m16/m18).

## >>> ACTIVE MISSION (user directive 2026-06-26): FULL-FUNNEL, ALL SPORTS, SMARTER EVERY NIGHT
Run the full funnel end-to-end (DATA->SIGNALS->MODELS->ENGINES->PREDICTIONS->INTEL->LINES->
EXECUTION->IMPROVE) for every in-season sport (NBA, MLB, soccer, soccer_intl, tennis; NFL in
season), unattended, getting better every cycle. Win = calibration + measured CLV; NEVER a
fabricated $/edge; REJECT logged = success. Full directive + funnel + per-cycle output spec:
.planning/platform/BUILD_BACKLOG.md SECTION 0 (ACTIVE MISSION). The runtime FLYWHEEL (go.ps1
supervisor m1..m18 -- capture lines / paper-trade / settle / CLV / m4 recalibrate) runs detached
at ~zero token cost and keeps improving predictions overnight with or without this chat; the
builder loop self-schedules wakes to add gated code/signals/models on top. Enders: `bot stop`
or `program_complete` only.
- WAKE-1 (2026-06-26 ~00:47): flywheel healthy (19/19 procs, all_ready). SHIP d6631ae5 --
  count_open_unsettled/open_unsettled_rows helpers in clv_ledger_dedup.py (excludes settled-twins
  from the open-bet count; +5 tests, 20/20 green; not yet wired). Scoreboard: bankroll 98.0u
  (net -2.0 since reset); CLV INSUFFICIENT_DATA (n_settled=0, needs 8+); reject-ledger delta 0.
  NEXT: wire helper into clv_lifecycle_reconcile.py:198-201 (primary over-reporter).
- WAKE-2 (2026-06-26 ~01:52): flywheel healthy (19/19). SHIP baa4c457 -- activity_sources.py
  n_open now twin-aware via count_open_unsettled (honest activity-feed open count; +6 tests, 6/6).
  CORRECTION: clv_lifecycle_reconcile.py is NOT an over-reporter (already exposes twin-aware
  unsettled_open + uses raw open_rows for drift detection) -> did NOT wire there. Scoreboard:
  bankroll 98.0u (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject-ledger delta 0.
  NEXT: remaining over-reporters grade_paper.py:244 / prop_paper.py:205 (verify display-only first).
- WAKE-3 (2026-06-26 ~02:26): flywheel healthy (19/19). SHIP 1a123f38 -- per-sport status:open
  in activity_sources now twin-aware (consistent with wake-2 headline; +1 test, 7/7). FINDING:
  grade_paper.py:244 + prop_paper.py:205 are settlement WORK-LISTS (already dedup vs settled-key
  sets), NOT display -> correctly SKIPPED, no change. Open-count honesty thread DONE (activity
  feed fully twin-aware; work-lists self-dedup). Scoreboard: bankroll 98.0u (net -2.0); CLV
  INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: new thread -- pick a fresh funnel
  increment (LINES/devig robustness or DATA capture coverage), offline-testable first.
- WAKE-4 (2026-06-26 ~03:00): flywheel healthy (19/19). SHIP f0ec404b -- LINES robustness:
  best_price._both now rejects NaN/inf quotes (a single bad book price was poisoning the
  statistics.median fair in sharp_fair -> killed value detection for the whole game). CLV math
  untouched; +6 tests, 12/12. (best_price module was untracked -> now committed.) Scoreboard:
  bankroll 98.0u (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: more
  LINES/devig edge cases or an IMPROVE/recalibrator guard, offline-testable.
- WAKE-5 (2026-06-26 ~03:33): flywheel healthy (19/19). LOCAL-ONLY fix (NOT committed --
  gitignored): improve/_market_metrics.py readout_for_segment now filters void/NaN/out-of-range
  rows (a void settled twin y=None was CRASHING the recalibrator readout; NaN p_raw poisoned
  Brier/ECE) -- +_valid_scored/_valid_close, 22/22 tests; live on disk so the running recalibrator
  benefits on next import. LEARNING: scripts/**/_* is gitignored (underscore=local-only) -> not
  committable; future committable increments must target NON-underscore files. Also compacted
  MEMORY.md 224->140 lines (was dropping entries past 200 on load). Scoreboard: bankroll 98.0u
  (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0.
- WAKE-6 (2026-06-26 ~04:13): flywheel healthy (19/19). SHIP 26bda83b -- prop_close_store
  (CLV-fuel store) now rejects NaN/inf two-way quotes on write + skips legacy non-finite rows on
  read (a non-finite 'close' was poisoning downstream CLV). +5 tests, 9/9. Same bug-class as the
  wake-4 best_price guard, now closed in the close store too. Scoreboard: bankroll 98.0u (net
  -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: continue committable
  offline robustness (best_price_audit / model_progress / odds_provider) or a SIGNALS helper.
- WAKE-7 (2026-06-26 ~04:46): flywheel healthy (19/19). SHIP 4709c057 -- odds_provider/
  pinnacle_parse now type-safe vs malformed legs (non-dict elements / non-list prices degrade to
  None/partial instead of AttributeError crash); new test_pinnacle_parse.py 20/20. GATE CAUGHT a
  Cyrillic-homoglyph identifier in the test (non-ASCII) -> fixed before commit (ASCII invariant
  held). Scoreboard: bankroll 98.0u (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject
  delta 0. NEXT: another committable parse-robustness/SIGNALS increment.
- WAKE-8 (2026-06-26 ~05:21): flywheel healthy (19/19). SHIP 6c782d85 -- market_join (LINES
  aggregation bridge, was UNTESTED) hardened: _safe_float so present-but-None edge fields ->0.0
  and bad-odds quotes (None/<=1.0) are skipped instead of TypeError aborting the whole join; new
  test_market_join.py 12/12. (espn.parse_pickcenter already per-entry try/except -> left alone.)
  ASCII byte-checked clean (wake-7 lesson). Scoreboard: bankroll 98.0u (net -2.0); CLV
  INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: SIGNALS helper or line_store/devig robustness.
- WAKE-9 (2026-06-26 ~05:55): flywheel healthy (19/19). SHIP 5120e9d7 -- first test coverage for
  domains/tennis/elo_core (MODELS-stage rating engine, was UNTESTED): 30 tests on the REAL
  _expected/_k/_is_walkover/prob/replay (leak-free pre-match snapshot, walkover skip). AUDIT (no
  bug, no change): espn.parse_pickcenter already per-entry try/except; line_store._parse_ts already
  normalizes naive->UTC-aware -> both confirmed CORRECT, not touched. Scoreboard: bankroll 98.0u
  (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: another domain engine test
  (tennis elo_walkforward / a sport repricer) or a SIGNALS helper.
- WAKE-10 (2026-06-26 ~06:31): flywheel healthy (19/19). SHIP 0c4fc137 -- REAL BUG FIX (first
  non-guard of the night): domains/mlb/ratings_mov._mov_mult degenerate-damp guard set
  damp=mov_offset (an exposed fitting knob); mov_offset<=0 -> ZeroDivisionError (damp==0) or
  sign-flipped multiplier (damp<0). Fixed to return 1.0 (no MOV scaling) on degenerate damp;
  default mov_offset=2.2 byte-identical. +14 tests (fail-before/pass-after + baseline-exactness +
  leak-free + zero-sum). MLB MOV-Elo engine was untested. Scoreboard: bankroll 98.0u (net -2.0);
  CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: another untested domain engine
  (soccer rho_fit/hfa_lambda, nba ingame_blend, tennis elo_walkforward) or a SIGNALS helper.
- WAKE-11 (2026-06-26 ~07:05): flywheel healthy (19/19). SHIP 0ae411f4 -- 2nd REAL BUG FIX:
  domains/soccer/rho_fit (Dixon-Coles low-score fitter) crashed when any pre-match lambda was
  0/NaN/neg (_poisson_log_pmf does math.log(lam); walk_forward_rho filtered only goals not
  lambdas). Fixed: dc_neg_log_likelihood skips undefined-Poisson matches (non-finite/<=0 lambda or
  negative goals); walk_forward_rho filter now requires finite positive lambdas. Valid-input math
  unchanged. +20 tests (fail-before/pass-after + tau formulas + leak-free warmup + NaN-safety).
  Soccer DC engine was untested. Scoreboard: bankroll 98.0u (net -2.0); CLV INSUFFICIENT_DATA
  (n_settled=0); reject delta 0. NEXT: soccer hfa_lambda / nba ingame_blend / tennis elo_walkforward.
- WAKE-12 (2026-06-26 ~07:40): flywheel healthy (19/19). SHIP d5d30266 -- first test coverage
  (25 tests) for domains/basketball_nba/ingame_blend_plive (in-game P_live win-prob head = the
  PROVEN-EDGE stage): real sec_remaining (Q1/Q4/OT/buzzer/neg-clamp), build_state_features
  (foul_diff sign, bonus, time_pressure 30s clamp), fit/predict (_ConstModel fallback, 2-class
  ranking, [0,1] clamp). No bug -- head correct. AUDIT (no change): soccer hfa_lambda already
  guards h=ew_home/ew_away (ew_away>0) + clamps sqrt_h -> correct, not touched. Scoreboard:
  bankroll 98.0u (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT:
  ingame_blend_surface/prior, tennis elo_walkforward, or mlb re24_table.
- WAKE-13 (2026-06-26 ~08:15): flywheel healthy (19/19). SHIP eb0b7500 -- first test coverage
  (48 tests) for domains/mlb/re24_table (pinned RE24 run-expectancy + leverage rule): 24-entry
  structure, monotonicity, pinned published values (transposition guard), base_run_value masking/
  out-of-range, leverage branch precedence, and LEAK-FREE provenance (est seasons 1999-2002
  disjoint from gate seasons 2022/2023). No bug -- table correct. Scoreboard: bankroll 98.0u
  (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: tennis elo_walkforward
  [245], nba ingame_blend_surface/prior, or a SIGNALS helper.
- WAKE-14 (2026-06-26 ~08:49): flywheel healthy (19/19). SHIP 80a5c904 -- domains/tennis/
  elo_walkforward: (A) ASCII-fix 23 non-ASCII docstring bytes (em-dash/<=/->/=>/section) ->
  restores binding ASCII invariant, docstring-only no logic change; (B) +20 tests on REAL
  walk_forward_elo/elo_state_asof/replay_with_snapshots locking the leak-free contract
  elo_state_asof(df,D)==replay(rows<D). FOUND A REAL BUG (queued NEXT): elo_core._sort_key:111
  does df["round"].map(...) with NO "round" in df.columns fallback (tour/tourney_id/match_num all
  have one) -> KeyError when round absent. Scoreboard: bankroll 98.0u (net -2.0); CLV
  INSUFFICIENT_DATA (n_settled=0); reject delta 0. NEXT: fix elo_core round-fallback + extend
  test_elo_core (fail-before/pass-after).
- WAKE-15 (2026-06-26 ~09:26): flywheel healthy (19/19). SHIP 20f33872 -- 3rd REAL BUG FIX:
  domains/tennis/elo_core._sort_key read df["round"] with no "in df.columns" fallback (siblings
  tour/tourney_id/match_num all guarded) -> KeyError when round absent. Fixed (missing->sentinel
  ->99). +2 tests (fail-before/pass-after). Also ASCII-cleaned elo_core docstrings (34->0).
  Scoreboard: bankroll 98.0u (net -2.0); CLV INSUFFICIENT_DATA (n_settled=0); reject delta 0.
  NEXT: nba ingame_blend_surface/prior, mlb ratings.py, soccer asof_xg_proxy, or a SIGNALS helper.

## >>> NEXT FOCUS (user directive 2026-06-22): FRONT-END FILL
Make the front end fully work + MATCH the system: every game card FILLED with lines + best
bets; /paper-trading shows real paper activity (open+settled, props + in-game channel=paper_ingame);
Best Bets view shows per-sport best bets (units only). Honest empty states where data truly
doesn't exist (NBA offseason, no liquid lines) -- NEVER fabricate lines/bets/$ edge.
- UI: webapp/ Next.js on :3000 (PROD build via `npm run start`; REBUILD `cd webapp && npm run build`
  + restart after any FE change). APIs on :8099 (/api/paper/trail, /api/v1/bestbets/<sport>,
  /api/ingame/..., /health) + legacy :8098. Snapshots data/frontend/snapshots/<sport>.json
  (snapshot_writer.py; serve.py prefers snapshot).
- LIKELY ROOT CAUSES (seen this session): (a) STALE snapshots -- no refresh daemon (soccer_intl
  was 4 days stale); regen + add a guarded periodic refresh. (b) API<->frontend FIELD-NAME
  mismatches -- the trail dropped `channel`; market_type maps from `group`; check each empty
  card's TS type vs the API row. (c) APIs 500/empty or a filter hiding rows.
- METHOD: audit each webapp page/card -> map to its API/field -> fix the specific break (don't
  fake-fill). Rails: units only / real-money DENY / honest-empty>fake / local commits never push /
  per-file tests only / <=300 LOC / ASCII / cd-prefix bash / build webapp+restart after FE edits /
  do NOT edit src,kernel,api,scripts/team_system,intel. Live now: mlb + soccer_intl (WC).
- Read reference-sys-frontend.md first.

## >>> SESSION 2026-06-25e (PROP-SETTLEMENT gap: fixed the MLB settler + WIRED the missing settle arm)
USER asked "hows paper trading running" -> dug into the prop-settlement gap. Fleet healthy; live paper bankroll 100->111.4u (units only, executed=False, no $). LOCAL working tree.
- UNIT/ODDS MATH VERIFIED CORRECT: grade_paper._unit_result = win:+(dec-1)*stake, loss:-stake, push:0. -130 American -> 1.769231 decimal; 1u win -> +0.769231u, loss -> -1.0u; +150 -> +1.5u; decimal round-trips to -130. All exact.
- FRONT-END PAPER TRAIL: /api/paper/trail (frontend.paper_routes -> paper_trail.read_trail) is LIVE + accurate BUT dropped unit_result (the per-bet PnL was computed+stored in clv_ledger for all 611 settled but the API row omitted it). FIX: added "unit_result" to _build_trail_row (frontend/paper_trail.py, offset a comment to hold ==300 LOC); now every settled trail row carries its authoritative +/-unit result (sum +31.07u over 611 raw rows). 45 frontend tests green. Takes effect on next API restart.
- ACCURACY/RECONCILE: bankroll net +30.17u (reconciles:True) is AUTHORITATIVE = one-position-per-market dedup (544 positions); my naive 611-raw-row sum (+31.07) over-counts re-settled twins. Webapp headline uses bankroll/pnl-series endpoints (correct), per-bet list uses the trail. UI (PaperTradeRow) shows odds+stake+win/loss LABEL but NOT the +/-unit amount -> can add a result-units column (needs webapp rebuild) if wanted.
- KALSHI thesis tested + measurement built (user: "all Kalshi markets, in-game, most profit"). HONEST FINDING: Kalshi LISTS the market types (2260 sports series: player props KXNBAPLAYOFFPTS/KXMLBHR, totals, spreads) but they are LISTED-NOT-TRADED -- pulled the real Pete Crow-Armstrong HR market: yes_bid/ask/volume/open_interest/last_price ALL None. Live scan: 383 series, 1949 open, 0 LIQUID (props 332/0, totals 365/0, spreads 130/0). Kalshi's only liquid sports surface = GAME-WINNER moneyline during live games (what we already trade). Did NOT build a prop system for dead markets.
  - (a) BUILT scripts/platformkit/pm_trading/kalshi_market_scan.py (liquid-surface discovery, gate=real two-way+tight+volume; classifies winner/total/spread/prop/half/event; 4 tests) + kalshi_scan_runner.py wired as ProcSpec m17 (1800s, per-type daily high-water mark) -> samples the live slate, surfaces any prop/total liquidity the instant it appears.
  - CORRECTION (2026-06-25, important): my earlier "Kalshi props are dead / 0 liquid" was a FIELD-NAME BUG -- the API list uses yes_bid_dollars/yes_ask_dollars/volume_24h_fp/open_interest_fp/liquidity_dollars, NOT yes_bid/yes_ask/volume. User was RIGHT. With correct fields Kalshi is LIQUID across types at ~1c spread (FAR below DK's 13%): team_total 238 liquid, event_future 153, player_prop 43, spread 34 (e.g. PHI@NYM total bid/ask 0.48/0.49 OI 549; Crow-Armstrong HR 0.16/0.17). FIXED kalshi_market_scan._is_liquid to the real fields (gate on tight two-way; volume is bonus not gate). Re-scan: 468 liquid / 1805 open.
- BUILT BOTH Kalshi pricers (user "do both") -> scripts/platformkit/pm_trading/kalshi_pricers.py (234 LOC, 7 tests): player_prop pricer reuses the prop board (parse title 'Player: K+ stat' -> P(over K-0.5)); team_total pricer = v0 run-rate Poisson (statsapi team RS/RA per game -> proj total -> P(>=line)). Wired into kalshi_edge_finder registry; 27 Kalshi-suite tests green; all <=300 LOC. LIVE END-TO-END WORKS (scan->price->candidates). BUT THE MEASUREMENT CAUGHT THAT NEITHER IS BET-READY (did NOT wire a placer -> would bet a fabricated edge): (1) totals v0 is SYSTEMATICALLY BIASED -- 92 over vs 30 under, mean +5.2% ABOVE the Kalshi line = miscalibration NOT edge (ignores STARTING PITCHER + park, the dominant total factors). (2) player_prop matched 0 live (the board doesn't carry the HR props Kalshi lists -> honest skip, no fabrication). NEXT to make real: feed totals model pitcher-ERA+park+calibrate vs settled totals BEFORE trusting; populate prop board with Kalshi-offered stats; only then place + let CLV/m18 judge. The framework (scan+devig+registry+honest-skip) is the durable win; the models need calibration.
- BUILT scripts/platformkit/pm_trading/kalshi_edge_finder.py (136 LOC, 6 tests): generalized multi-type pricer -- devig the Kalshi YES two-way (mid) -> for each liquid market, a per-type PRICER registry returns our model P(YES) -> edge=model-fair -> YES/NO candidate; types with NO model (futures/MVP/etc.) are SKIPPED+counted, never priced from thin air. Proven live on 175 real liquid markets (winner10/future58/prop32/spread75) -> 0 candidates with pricers={} (honest no-fabrication gate works). NEXT (the actual "bet them" step): wire per-type pricers -- game_winner already covered by pm_game_placer; player_prop -> prop board; team_total/spread need a totals/margin model we may not have (honest skip until built) -> then a placer+runner to stake candidates in UNITS + route through settle/m18/ratchet. 20 Kalshi-suite tests green; all files <=300 LOC; non-gated.
- (b) MADE THE KALSHI CHANNEL CLV-MEASURABLE (it carried clv_pct=None / n_clv=0): ROOT = close_capture.capture_close existed but was NEVER called AND pm rows had event_id=None. FIX: pm_game_placer now stamps event_id=game_id (the Kalshi event_ticker inplay_kalshi already provides); NEW pm_close_capture.py sweep (capture_close -> settle_closing_line -> append; CONFIRMED settled closes only=true_close, open/inferred NEVER stamped; idempotent; 4 tests) + pm_close_capture_runner.py wired as ProcSpec m18 (900s). Once PM bets settle with the stamped id, m18 fills their real CLV -> the +13u channel finally gets a yardstick. 25 tests green; all files <=300 LOC; non-gated. Activates on next supervisor restart.
- BANKROLL RESET (user request, 2026-06-25): bankroll is DERIVED (daemon recomputes current=start_units+sum(settled in clv_ledger) each tick) so an overwrite alone won't stick -> reset the SOURCE. Archived the 5 files (clv_ledger 611 settled+871 open, paper_bankroll/pnl_series/today/grade_summary) to data/frontend/_ledger_archive/20260625T215321_reset/ (REVERSIBLE: copy back to restore), atomically cleared clv_ledger, init_bankroll(100), ran a real daemon tick. VERIFIED clean+accurate: start=current=100.0, net=0.0, 0 points, n_bets=0, reconciles=True. prop_close_lines.jsonl (m16 capture corpus) KEPT so the recalibrator fuel survives. Live daemons already resumed (8 fresh open bets, 0 settled -> current stays 100). Fresh track now measures the POST-vig-gate system from 100u.
- ROOT CAUSE (two parts). (B) prop_settler_mlb._team_hit substring-matched whole labels, so prop matchups written with CITY ABBREVIATIONS ('MIL Brewers @ CIN Reds') never matched the MLB statsapi full names ('Milwaukee Brewers') -> _find_final_gamepk None -> EVERY abbrev MLB prop pended forever (settler returned 0). FIX: new _name_words (drops a leading all-caps city code) + nickname word-SUBSET _team_hit (robust to abbrev, unambiguous for Red Sox vs White Sox). Drained backlog: clv_ledger settled 364->400 (+36); 361 open rows already had settled twins (kept BY DESIGN -- clv_ledger_dedup.sanitize_rows keeps open+settled twins; the inflated "open" count is a READER bug, not a ledger rewrite).
- (A) STRUCTURAL, NOW FIXED: NO supervisor module ran the settler (m13 PLACES props, nothing SETTLED them). NEW scripts/platformkit/bestbets/prop_settle_runner.py (m15_prop_settle, 900s, mirrors m13 runner: ops.liveness heartbeat + tick/run + injectable clock/sleep/settle_fn/max_ticks, 5 tests) wired as ProcSpec m15 in supervisor/stack_specs.py (independent, fresh_sec=1980). TAKES EFFECT ON NEXT SUPERVISOR RESTART (live PID loaded specs at boot). Also added date+1 fallback in mlb_realized_stat for ET placement-day mis-tag (FINAL-only guard -> live next-day game still pends).
- m7_ingame_refresh "stale" (~50min) = FALSE ALARM: beats HOURLY by spec (cadence 3600s, fresh_sec=7800).
- CLV CAPTURE + improve-loop fuel (the real edge lever): close-capture infra existed (clv/prop_close_store read-side wired into settler; clv/prop_close_capture writer, source=draftkings_live in-game two-way) but ran ONLY off auto_loop's 20-MIN tick -> coarse, missed the last price before a market suspends. Keys MATCH format (no norm bug); the gap is COVERAGE (captured players != bet players) + pregame DFS/Underdog props have NO two-way = permanently un-measurable (DATA wall). FIX: NEW scripts/platformkit/clv/prop_close_capture_runner.py (m16_prop_close_capture, 60s, 6 tests) wired as ProcSpec m16 (fresh_sec=150, cheap-when-idle). One real tick captured 22 closes (store 437->496). WHY it ALSO "makes AI better": captured closes -> clv_corpus.build_close_corpus = the 2nd independent corpus the recalibrator REQUIRES (gate stays REPLICATION_PENDING without it) -> m4_selfimprove ships better calibration. So faster capture = more CLV coverage AND more improve fuel, one lever.
- IN-GAME PROP VIG-GATE (execution CLV-bleed fix): exec-by-channel showed game ML CLV ~neutral & slightly improving (-3%->+1.5%, n=17) but in-game props -16.4% mean CLV / 9% beat-close = the dominant bleed (structural live-prop vig ~-130/-130). ingame_prop_trader's ev>0 only cleared OUR HALF of the vig by a noisy in-game model -> marginal picks lose to close. FIX (scripts/platformkit/ingame/ingame_prop_trader.py _price_one): require edge over DEVIGGED fair >= _VIG_EDGE_MULT*round-trip-vig (default 1.0 = full hold) + skip vig>_MAX_VIG (0.20); env-tunable CV_INGAME_PROP_VIG_EDGE_MULT / CV_INGAME_PROP_MAX_VIG; STRICTER-only. 12 tests (2 new); trimmed docstring to hold ==300 LOC. Activates on next ingame-loop restart.
- MODELS honest verdict: NOT improving yet (game recal data-starved 41/60 settled -> INSUFFICIENT_DATA; prop recal ran -> HOLD dbrier -0.0003, already calibrated). "getting better" = measurement improved (CLV n 41->72 via m16); models holding; exec gate now stops juice-eaters. NO $ edge claimed.
- 47 tests green this session (m15:5 + m16:6 + vig:12-incl-existing + mlb/settler/clv); all files <=300 LOC; non-gated (scripts/platformkit + supervisor). NEXT: RESTART SUPERVISOR/ingame-loop to activate m15+m16+vig-gate (live PIDs loaded code at boot); fix the open-count READER to exclude settled-twins; watch in-game-prop CLV after gate lands.

## >>> SESSION 2026-06-25d (SOFT-BOOK FEED added (FanDuel, keyless) + caught/fixed a FABRICATED-EDGE merge bug)
USER chose "free per-book scraping" to fix the book-coverage constraint (25c). Added a real soft book and -- critically -- caught the honesty trap it exposed. LOCAL working tree.
- BUILT scripts/platformkit/odds_provider/fanduel.py: keyless FanDuel game-moneyline provider (public _ak site key, browser UA+Referer or it 403s; parses attachments.markets MONEY_LINE -> home/away from "Away (P) @ Home (P)" event name + runner decimals; UNAVAILABLE on block/empty). 5 tests. Wired into aggregate.default_providers (now ESPN+FanDuel+Kalshi+Polymarket+Pinnacle). LIVE: FanDuel returns 15 MLB games -> 7 games now have 3 books (DraftKings+FanDuel+Pinnacle), was 2.
- THE HONESTY CATCH (most important): first audit with FanDuel showed a +8.23% "edge" on Yankees ML -- FABRICATED. ROOT: FanDuel lists tomorrow's game too (same teams), and aggregate._event_match matched on TEAMS ONLY -> tomorrow's pick'em line (TBD pitchers) overwrote today's real line -> fake CLV. FIX: _event_match now ALSO requires commence_time within 6h (falls back to team-only when a time is missing, so no existing merge drops). Verified: today's Yankees game now merges all 3 books sharp-aligned (DK 1.676 / FD 1.675 / Pinn 1.735), tomorrow's splits off FanDuel-only, and the audit honestly reports 0 +CLV (FanDuel agrees with the sharps today). Did NOT ship the fake edge.
- HONEST STATE: infra is now genuinely better (3 books, time-safe merge that PREVENTS cross-game fabricated edges, +CLV engine ready). 0 +CLV right now = FanDuel aligned today (efficient), NOT a bug -- the engine fires when a soft book actually diverges. 40 tests green (5 fanduel + 30 clv + flip/robustness merge tests); 1 pre-existing UNRELATED failure (test_inert_when_sentinel_absent: PIPELINE_ENABLED sentinel present on this armed box -> corpus builds; fails with my change REVERTED too, confirmed). NEXT: restart to activate 3-book line-shopping in the live boards/placement (better CLV on placed bets); add more soft books (Caesars/BetMGM need header work, MGM 403s datacenter-style); the more soft books, the more real +CLV the engine surfaces.

## >>> SESSION 2026-06-25c (MONEY LEVER = price-edge/line-shop engine + the DIAGNOSIS: book coverage, not the model)
USER: "keep trying to make an efficient system that will make money." Built the model-free money lever (best-price line-shopping = take a better-than-fair number = +CLV) and it DIAGNOSED the real binding constraint. LOCAL working tree.
- BUILT scripts/platformkit/clv/best_price.py (sharp_fair = Pinnacle-anchored / cross-book-median no-vig; best_price = max decimal per side, sportsbooks only; price_edge = expected CLV% of best price vs sharp fair; value_bets = the +CLV opportunities) + best_price_audit.py (runs it on the LIVE slate). 7+30 clv tests green; robust to sub-1.0-booksum arb quotes (Shin asserts -> _safe_devig).
- THE FINDING (live, honest): MLB scanned 10 games, only 2 books quote each (espn:DraftKings + pinnacle), BOTH SHARP -> 0 +CLV bets even at min 0.0%. soccer 1 book (pinnacle 401) -> 0 shoppable. ROOT: ESPN now republishes only 1 book/game (DraftKings; parse_pickcenter already multi-book-capable, feed isn't), keyless coverage caps at ~2 sharp books. +CLV requires a SOFT book mispricing vs the sharp -- we have NONE. THE -3.18% moneyline CLV is exactly this: take DraftKings (sharp-ish, vigged) as the only price -> pay the vig to the close.
- HONEST ANSWER to "make money": it's a DATA-ACQUISITION problem, not a model one (the quant truth: edge is in data access + execution). The #1 money lever = add SOFT-book game-ML feeds (FanDuel/BetMGM/Caesars/ESPN BET) via a paid odds API (the-odds-api = 1 key, 10+ books) or per-book adapters; the detection engine is built + tested and auto-surfaces +CLV the moment soft books land. No book adapter beyond espn/pinnacle/kalshi exists for game ML (prop_betmgm/prop_draftkings are props-only). No $ edge claimed; 0 fabricated. Stack untouched this turn (analysis CLIs, not daemon-wired). NEXT: wire a multi-book feed (the real money unlock) OR wire best_price as a CLV-gate into the placer once >=3 books exist.

## >>> SESSION 2026-06-25b (EDGE MEASUREMENT: CLV scoreboard + model-progress + PROP CLOSING-LINE CAPTURE)
USER: "how do we start getting edges / think like big quants / models getting better." Reframe given (accuracy != edge; CLV is the only judge), then built the measurement layer. LOCAL working tree (not committed/pushed).
- BUILT scripts/platformkit/clv/clv_scoreboard.py (per-channel CLV truth table; measurable=true_close vs no_close split + 95% CI/significance; dedup). FINDING on real ledger: coverage 5.8% (16/275 bets have a captured closing line); the ONLY measurable channel (game moneyline n=16) = mean CLV -3.18% SIGNIFICANT, beat-close 0/16 = ANTI-edge (we take worse-than-close prices). Props (in-game -19.6u, pregame +1.0u) + Kalshi PM (+7.5u) = ALL un-measurable noise w/o closing lines.
- BUILT scripts/platformkit/clv/model_progress.py (two-axis "are models getting better": calibration trend from improve_ledger + edge from scoreboard). FINDING: self-improve loop is honest (134 SHIP / 1366 INSUFFICIENT_DATA, refuses to fabricate; needs >=60 settled/sport; NBA frozen n=6 offseason, MLB n=30 Brier 0.22 improving). KEY INSIGHT: on the SAME 16 MLB games the model PREDICTS better than close (BSS +0.10) but TRANSACTS worse (CLV -3.18%) -> bottleneck is EXECUTION/price+data, NOT the model.
- BUILT (user "build that next") PROP CLOSING-LINE CAPTURE so in-game props become CLV-measurable: NEW scripts/platformkit/clv/prop_close_store.py (two-way close history, latest-wins, never half-prices) + prop_close_capture.py (snapshots live DK two-way for OPEN in-game props each cycle; injectable quote_fn; honest no-op when nothing live) + prop_settler.settle_prop_row/settle_open_props now take close_fn (default reads the store) -> real CLV (Over->home/Under->away via compute_clv) when a close was captured, else honest no_close. Wired as auto_loop step "capture_prop_closes" (mlb+soccer_intl). 13 new tests + 6 prop_settler + 16 auto_loop GREEN. m1_paper surgically reloaded (restarts=1, new code live; supervisor stayed 1/19/19 - single-instance fix held). LIVE NOW = honest no-op (no MLB game live: 257 open props, 0 captured, 0 fabricated); starts accruing prop CLV the moment a game goes live, then scoreboard coverage climbs off 5.8% and we learn if the in-game edge is REAL. NEXT: pregame-prop CLV needs a DK/FD pregame two-way snapshot (different source); fix moneyline -3.18% via best-price line-shop. No $ edge; units only; safe areas only.

## >>> SESSION 2026-06-25 (ROOT-CAUSED + KILLED the chronic DUPLICATE-SUPERVISOR disease + fixed bare-boot UI)
USER: "make sure everything in paper trading is working, keep making it better." Found the stack in the documented multi-supervisor deadlock (3 supervisors, m1_api_boards+m1_ui crash-looping all_ready=False, served by orphans) and fixed the ROOT CAUSE so it cannot recur. LOCAL only (NOT pushed, NOT committed -- working tree).
- THE REAL ROOT CAUSE (why every session kept re-fighting "multi-supervisor"): (1) `run_forever` calls `boot()` (up to ~30s) BEFORE the first `_beat_self()`, so during the whole boot window the supervisor's self-heartbeat is stale; the autostart watchdog (watchdog_autostart.ps1, poll 15s, wedge threshold 90s) sees alive-but-stale -> declares the BOOTING supervisor "WEDGED" -> launches a DUPLICATE. (2) Two supervisors then each run reconcile_survivors() which kills the OTHER's freshly-spawned CHILDREN by cmdline match -> mutual child-kill -> EADDRINUSE flap on :8099/:8098/:3000. Manual stop/boot races the watchdog the same way.
- THE FIX (3 minimal, non-gated changes + tests): (a) NEW supervisor/_singleton.py -- OS-level exclusive single-instance lock (msvcrt/fcntl, kernel auto-releases on death, FAIL-OPEN so a lock hiccup never blocks boot); __main__ acquires it and a 2nd concurrent supervisor exits 0 (4 tests, incl. a real spawn'd 2nd process proving refusal). (b) __main__ stamps `sv._beat_self()` IMMEDIATELY (before boot()) so a booting supervisor is never mistaken for wedged. (c) watchdog wedge branch now kills the wedged supervisor before relaunch (safe given (b)) so genuine-wedge recovery still works with the lock. PROVEN LIVE: deliberately raced a manual boot against the running watchdog (watchdog fired its own boot at 12:44:53 same moment) -> exactly 1 supervisor survived, held at 1 through the full 90s boot window (pre-fix this made 2-3). Lock file is held (cross-proc read -> PermissionError = proof).
- ALSO FIXED (bare-boot UI footgun): `boot.ps1` alone didn't set NBA_AI_UI_DIR/CMD (only go.ps1 + the watchdog did) -> m1_ui fell back to stack_specs' legacy court-visions `npm run dev`, collided with a stale court-visions dev server on :3001, left :3000 empty. Added the webapp prod-UI env defaults to boot.ps1 (guarded, override-respecting) so ANY boot serves webapp `npm run start` on :3000. Killed the stale court-visions orphans.
- VERIFIED LIVE NOW: 1 supervisor, 19/19 READY, all_ready True, ZERO duplicate daemons. webapp on :3000 (/ /bets /games /records all 200). paper trail API 200 (channels paper/paper_ingame_prop/paper_pm). bestbets mlb ok 10g / soccer ok 6g (0 bets = honest no-edge-clears-floor; warm 0.25s after the one cold 61s stale-while-revalidate build). Placement RESUMED: 368 bets today. Bankroll 87.5u / net -12.5u, units-only, edge_claimed=False, executed=False (honest slightly-negative on an efficient market; CLV is the judge). Tests: 4 singleton + 22 supervisor + dry-runs GREEN. No src/kernel/api/team_system/intel edit; ASCII; <=300 LOC python.
- KNOWN MINOR: a phantom :3001 listener (taskkill says PID gone, netstat still shows it) -- harmless, off-stack, webapp owns :3000. Changes uncommitted in working tree (supervisor/_singleton.py, tests/supervisor/test_singleton.py NEW; __main__.py, boot.ps1, watchdog_autostart.ps1 MODIFIED) -- commit when you want.

## >>> SESSION 2026-06-24h (FRESH SLATE on the disciplined config: best-bets-only, edge-weighted)
USER: "reset bankroll, run ONLY best bets to make most units; edges are in-game + props; keep running." Reset + verified the disciplined book. LOCAL only.
- RESET: archived 3794 ledger rows + bankroll/pnl/today/grade -> data/frontend/_ledger_archive/<ts>_reset (REVERSIBLE), truncated clv_ledger to empty, bankroll.init_bankroll(100) -> 100u/net0, ran one bankroll tick (fresh empty curve). Stack stays up (19/19 from 24g restart).
- FRESH SLATE seeded by one placement cycle (the daemon's own steps) = 72 bets, ALL tier A/B (67 A / 5 B), ALL open: IN-GAME props 55 (MLB live -- the believed freshness edge firing broadly, cap20/game) + Kalshi PM 9 (game ML, 13 matched) + PREGAME props 8 (MLB tier-A, calibration-gated -> only Hits/Total Bases/Pitcher Ks, the replicated-SHIP families; soccer 0, MLB 549 edges -> 8 staked, 142 capped). 89% of stake is the in-game+PM edges; the pregame flood is gone.
- HONEST FRAME (told the user): "most units" on an efficient market = MORE of the +EV/calibrated best bets, NOT bigger stakes -- did NOT crank stake size (Kelly on an unproven edge just amplifies variance/bleed). Flat-1u discipline kept. The reset gives a CLEAN CLV track record of the disciplined config; whether in-game/props is a real edge is now a measurement (prior all-time realized was -10.4u; props bled -21.8u). edge_claimed=False, real-money DENY, UNITS only. The running daemons (m12 place, settler, m1_bankroll) keep this going idempotently.
- WORLD CUP RE-INCLUDED (user: "should be doing world cup too"): the calibration gate had zeroed WC (soccer is INSUFFICIENT_DATA -- unmeasured, not rejected). FIX: auto_loop._place_props now SPLITS the call -- MLB calibration-gated (replicated-SHIP families only) + soccer_intl settleable + tier-A but NOT calibration-gated (new _WC_PROP_MAX_PER_SPORT=8). WC props = leader-gradeable Shots/Saves (the only PRICED + settleable WC stats; goals/cards/assists have no two-way price). WC also rides Kalshi PM game moneyline. KALSHI WC TEAM-BRIDGE FIXED: pm_game_placer._name_matches now canonicalizes national-team names via _COUNTRY_ALIASES (e.g. model "South Korea" == Kalshi "Korea Republic"; Congo != DR Congo; MLB city bridge intact) -- match rate 1->2 of our 6 model WC games (2 new tests, 11 pm tests green; m12 reloaded restarts=3). HONEST: the BINDING limit is COVERAGE not the bridge -- our model predicts only 6 WC games vs Kalshi's 20 (the other 14 have no model -> correctly un-bet); the 4 unmatched model games aren't even in Kalshi's current feed. Full WC PM coverage needs the producer to predict all WC matches (predict_service-side, separate). Of the 2 matched, 1 cleared the edge floor -> placed (the other = no edge, honest skip). SURGICALLY reloaded m12_pm_paper_tick (kill -> supervisor respawn, restarts=2, 19/19 READY) so the daemon keeps placing WC. Live slate now: ~133 mlb + 9 WC (8 props Shots/Saves + 1 PM), bankroll fresh 100u. HONEST: WC props are settleable + tier-A but NOT calibration-proven (and most Shots/Saves settle only for the match leader) -> "best available WC bets," CLV will measure them. NOTE: pregame cap is per-TICK (8/sport) so the daily count accumulates as new tier-A edges clear across ticks (still all tier-A/settleable, not the old B/C flood).

## >>> SESSION 2026-06-24g (ACCURACY: found+fixed the bankroll OVERSTATEMENT bug -- now reconciles to the real book)
USER: "get it to a place where it's accurately making money." Honest answer given: I can make the dashboard ACCURATE + the betting sharper, but cannot fabricate a profit -- realized is NEGATIVE on an efficient market. Fixed the accuracy bug. LOCAL only.
- THE BUG (root cause of +22.4u shown vs -8.5u real): bankroll_daemon computes the curve over select_clv_positions(settled), which keys each "market" by _clv_market_key = (sport, matchup, line, day) -- NO prop identity. So 50+ players' o/u-0.5 props in ONE game (e.g. TEX@MIA 53 rows) collapsed to a SINGLE position, keeping only the highest-model_prob side -> silently DROPPED ~90% of the prop book (1150 settled prop rows -> 115 keys) and biased the kept subset positive. The prop bleed vanished from the headline bankroll.
- THE FIX: scripts/platformkit/paper/pnl_normalize._clv_market_key now carries prop_player + prop_stat, so DISTINCT props are distinct positions; only a prop's own over/under (same player+stat+line) collapse, and moneyline two-way still collapses (no prop fields -> key unchanged). 4 regression tests + 28 pnl/daemon tests GREEN.
- THE HONEST NUMBER (post-fix): net flat-1u = -10.28u (current ~89.7u, NOT 122.4u). By channel: pregame props -21.78u (BLEED, now counted), Kalshi PM +7.33u, in-game props +1.13u, moneyline +3.04u. This is the TRUTH: paper book is slightly NEGATIVE on an efficient market; in-game + PM are the only non-negative channels (small n). No $ edge; CLV is the judge.
- ACTIVATION: DONE (user OK'd restart). Hit the documented multi-supervisor straggler deadlock -- stop.ps1 left a supervisor (28888) alive that respawned children + a 2nd supervisor (15168); did a FULL python-stack sweep (kill all supervisor/platformkit/predict_service/... procs -> 0 remaining) then ONE go.ps1 -> 19/19 READY, all_ready=True, single supervisor. VERIFIED LIVE: paper_bankroll.json now reads CURRENT 89.6u / NET -10.4u (was 122.4u/+22.4u) -- the honest number. The 24f refocus (auto_loop) loaded in the same fresh m12 process. UNITS only, no src/kernel/api edit, ASCII, local commit NOT pushed.

## >>> SESSION 2026-06-24f (REFOCUS: in-game-first + calibration-gated "smarter" selection; starve the pregame bleeder)
USER DIRECTIVE: "in-game is my edge -- focus paper trading on in-game player props, FEWER better bets using SMARTER systems on Kalshi + sportsbooks, grind the edges and push." Data-backed: realized flat units by channel = pregame props -26.5u (BLEED), Kalshi PM +10.6u, in-game props +4.4u (both small-n positive). So concentrate AWAY from the prop flood toward in-game + PM. LOCAL only (NOT pushed).
- THE "SMARTER SYSTEM" (new): scripts/platformkit/bestbets/calibration_gate.py -- stake a PREGAME prop only on (sport,stat) families we have PROVEN we calibrate (replicated_verdict==SHIP_REPLICATED in prop_history_meta; explicit stat map, no substring false-match; fails OPEN on a meta read error). Wired into props_paper_placer.run(calibration_only=) -> _board_edges drops un-proven families. 4 gate tests + 1 placer test.
- THE REFOCUS (auto_loop config): PREGAME props = least-favored channel now: min_tier A (was B) + cap 8/sport (was 50) + calibration_only=True. IN-GAME props = the FOCUS: cap 20/live game (was 12), own floor _INGAME_MIN_TIER=B (decoupled from the tightened prop tier). Kalshi PM = kept "pushing": own _PM_MIN_TIER=B (decoupled), cap 25.
- DRY-RUN PROOF (today's live board): PROVEN families = mlb {Hits,Runs,Pitcher Strikeouts,Total Bases} (soccer not proven yet -> 0). Pregame placement 75/cycle (mlb50+soccer25) -> 8/cycle (mlb8 tierA-proven + soccer0). ~89% fewer pregame props, concentrated on the 4 leak-free replicated-SHIP MLB stats. In-game + PM unchanged (the favored channels).
- HONEST FRAME: this is DISCIPLINE + calibration concentration, NOT a proven $ edge. In-game is the validated FRESHNESS lever (calibration) and the realized-positive channel (small n); pregame props were the measured bleeder. CLV over larger n stays the judge; edge_claimed=False; real-money DENY. Takes effect on next daemon/stack RESTART (auto_loop imported at process start). 66 tests GREEN this session + 16 auto_loop + 17 placer/gate. UNITS only, no src/kernel/api edit, ASCII. Local commit, NOT pushed.
- NOTE: prop board's internal oos-calibration cache went stale ('v2-void-nan') after the SOT void -> it conservatively treats edges as 'unmeasured' (honest, not a bug); refreshes on the next board rebuild.

## >>> SESSION 2026-06-24e (WC props: SOCCER SETTLER shipped (fires now) + DK-live soccer price-side READY)
USER: "do both" (1 wire a live two-way WC prop source; 2 add a soccer prop settler). Did both honestly; the real binding wall is ESPN keyless exposes NO per-player shots/SOT. LOCAL only (NOT pushed).
- TASK 2 DONE + FIRING: NEW scripts/platformkit/bestbets/prop_settler_soccer.py -- keyless ESPN post-match resolver: goals/assists/goal+assist/cards from summary.keyEvents (FULL coverage; reuses ingame_box_soccer), shots/saves from summary.leaders (LEADER-ONLY -- the feed exposes just the top player/category), Shots On Target UNRESOLVABLE (no per-player SOT feed). FINAL-only, scoreboard-by-date, disk-cached. Wired into prop_settler._default_realized_fn (soccer_intl/soccer) so the EXISTING settle daemon now grades soccer props. 10 tests. RAN IT LIVE: settled 5 real WC props (Ronaldo Shots o4.5 -> 7 = WIN +1.06u; Diogo Costa Saves u0.5 -> 2 = loss; etc.), net -1.11u.
- THE HONEST COVERAGE FINDING (verified on real fifa.world finals): of 231 OPEN WC props only 5 are keyless-resolvable -- because the placed props are Shots(83)/Shots On Target(128)/Saves(20), and ESPN keyless gives per-player data ONLY for the single category LEADER (boxscore.players EMPTY, core-API roster EMPTY on the marquee ARG-AUT final too; keyEvents rich but that's goals/cards/assists). So 226 SOT/non-leader props stay PENDING forever on keyless data. ROOT ISSUE surfaced: the placer places SOT/Shots props it can NEVER grade keyless -- the real lever is to bias WC prop placement toward gradeable goals/cards/assists (or drop SOT). NOT fixed this session (placer change; flagged for the user).
- TASK 1 (live price source) READY, awaits a live-confirm: prop_draftkings_live made soccer-capable -- sport-aware stat phrases (Goals/Assists/Cards/Shots/SOT) + env-gated WC league id (DK_SOCCER_INTL_LEAGUE_ID / DK_SOCCER_LEAGUE_ID; NOT hard-coded because DK's WC id is not in the repo and is unconfirmable until a match is live). Absent env -> honest unavailable -> trader no-op. 4 new parse/env tests. Combined with the realized-feed wall, in-game soccer props can only EVER fire for goals/cards/assists (shots have no live per-player realized feed). To light it up: set DK_SOCCER_INTL_LEAGUE_ID after probing DK's live event endpoint DURING a live WC match (the 24c lesson).
- LANDED: 62 per-file tests GREEN across touched files. UNITS only, edge_claimed=False, real-money DENY, no src/kernel/api edit, ASCII. Local commit, NOT pushed. NET: WC props now SETTLE (the gradeable subset) + the in-game price-side is wired; the dominant blocker is the keyless feed having no per-player shots data -- a real DATA limit, not a code gap.
- FOLLOW-UP DONE (user: "yes" to the placer fix): props_paper_placer now has a per-sport SETTLEABLE-stat allowlist (_SETTLEABLE_STATS_BY_SPORT). soccer_intl/soccer place ONLY keyless-gradeable stats {Goals,Assists,Goal+Assist,Cards,Shots,Saves}; Shots On Target / Fouls (NO per-player keyless feed) are DROPPED so a WC prop is never staked that would pend forever. mlb unfiltered (full statsapi). KEY REALITY this exposed: the only PRICED WC props are Shots/SOT/Saves (goals/cards/assists have NO two-way price from Underdog -> 0 placeable), so post-fix WC places ~25/slate = Shots(19 leader-gradeable)+Saves(6); SOT(33) dropped. 4 new tests (12 placer total). CLEANUP DONE (user OK'd void): backed up the ledger (data/frontend/_ledger_archive/<ts>_pre_sot_void.jsonl) then appended a settled-VOID row (outcome=void, unit_result=None -> 0 bankroll impact, append-only via write_settlement, race-safe) for the 128 already-placed open Shots On Target props (never keyless-gradeable). VERIFIED: WC props terminal=133 (128 void + 5 settled 2W/3L); effectively-open now 98 = Shots(81)+Saves(17) only -- every open WC prop is now at least leader-gradeable. Shots/Saves KEPT (leader-gradeable); goals/cards/assists were never placed (no two-way price). 66 tests GREEN total.

## >>> SESSION 2026-06-24d (WC IN-GAME PROPS: engine made SOCCER-READY; honest 2-wall no-go on firing today)
USER ASK: "make sure World Cup props + in-game are working -- best bets player props, in-game is edge." Chose "attempt WC in-game wiring." Verified the live system, then attempted the wiring with full due diligence. LOCAL only (NOT pushed).
- VERIFIED WORKING NOW: WC pregame PROPS fine -- 228 placed today (190 A / 38 B), 140 on the best-bets board, from Underdog (DK/FD/MGM/PrizePicks all empty for the WC league). MLB IN-GAME player props = the live-firing edge (a real tick placed 6 on 2 live MLB games). WC in-game game-MONEYLINE fires when a WC match is live (none live at build time; 6 scheduled later today).
- THE WIRING (all SAFE areas, 20 per-file tests GREEN): made the in-game prop trader sport-blind. (1) NEW scripts/platformkit/ingame/ingame_box_soccer.py: keyless ESPN LIVE realized reader -- live_games (in-progress WC matches + frac from match minute/90) + boxscore_stat_map (per-player Goals/Assists/Cards from summary.keyEvents, rostered players 0-seeded) + realized_for + is_known_stat. 5 tests. (2) ingame_prop_trader.run now dispatches via _BOX_BY_SPORT (mlb->ingame_box_mlb, soccer_intl->ingame_box_soccer); _price_one uses box.is_known_stat + box.realized_for; soccer lam from dist_lookup_from_snapshot. MLB path byte-identical (7 trader + box5 + repricer green). 300 LOC. 3 new soccer tests. (3) auto_loop._place_ingame_props now ticks mlb + soccer_intl. Live tick: mlb placed 6, soccer_intl honest no-op ("no live games").
- THE HONEST 2 WALLS (why WC in-game props place 0, did NOT fake): (a) REALIZED FEED: ESPN keyless soccer has EMPTY boxscore.players + empty team stats; only keyEvents (goals/cards/assists, every one emits an event so absence=genuine 0) + a single shot-LEADER. So Shots / Shots On Target / Saves / Fouls (38% of WC props) are NOT per-player readable keyless -> is_known_stat False -> skipped (no fab 0). Only Goals/Assists/Goal+Assist/Cards (62%) are honestly readable. (b) LIVE MARKET: no liquid live two-way WC prop source -- Underdog is DFS pick'em (suspends at kickoff, not devig-able); DK/FD/MGM empty for WC pregame; DK WC live league id is NOT in the repo (v2 comment: "soccer can be added once their live league_ids land") and is unconfirmable until a match is live; Kalshi WC props zero-liquidity (see 22h). So the trader's default soccer market source returns unavailable -> clean no_prices no-op.
- NET (honest): the engine is now SOCCER-READY + tested; it auto-fires the moment a WC match is live AND a live two-way market exists for a readable (goals/cards/assists) stat. Until a live two-way WC prop source is wired, WC in-game props correctly place 0 (honest-empty > fake). NO stack restart needed (no functional change for WC until a market lands). The DAEMON picks up the soccer tick on next stack restart. UNITS only, edge_claimed=False, real-money DENY, no src/kernel/api edit. Local commit, NOT pushed. NEXT OPTIONS for the user: (1) wire a confirmed live two-way WC prop source (probe DK's live event endpoint DURING a live WC match -- the 24c lesson -- or a keyed feed); (2) add a SOCCER prop SETTLER using ingame_box_soccer.realized_for so the 228 placed WC props (goals/cards/assists) actually settle (today they sit pending -- the settler is MLB-only); (3) accept WC = pregame props + in-game moneyline.

## >>> SESSION 2026-06-24c (IN-GAME PLAYER PROPS NOW PAPER-TRADING LIVE -- the edge is firing)
USER PUSHED BACK ("props update live, I should be able to paper-trade those, figure it out") and was RIGHT. The 24b "no live market" conclusion was WRONG -- DK DOES serve live full-game props, just via the EVENT endpoint, not the pregame catalog. Found it, wired it, it's placing. LOCAL only.
- THE FIX (3 things that unblocked it): (1) LIVE PRICE SOURCE: DK serves in-play full-game props at /events/{eventId}/categories (NOT the pregame /leagues/{lid}/subcategories catalog, which suspends a game at first pitch). Built scripts/platformkit/odds_provider/prop_draftkings_live.py: live_event_ids (league root, status==STARTED) -> per-event two-way O/U player props (29-42 live MLB markets mid-game: Hits+Runs+RBIs, Pitcher Strikeouts, Runs...). 5 tests.
  (2) LAM SOURCE: the pregame board/snapshot ALSO drops a live game's players (same suspension) -> 0 lam coverage for live players. Fixed by pulling pregame lam from the player-rate ENGINE directly: scripts/platformkit/ingame/ingame_prop_dist_mlb.py (cfg.resolve_player -> cfg.engine_distribution; COMPOSITE Hits+Runs+RBIs = sum of component lams). Suspension-proof. 5 tests.
  (3) REPRICER additive set was missing the live stat names -> reprice returned None for everything. Added "Hits+Runs+RBIs" + "Pitcher Strikeouts" to ingame_prop_repricer._ADDITIVE_STATS (both genuinely additive).
- ENGINE (built 24b, now firing): ingame_box_mlb (live realized stat + frac from statsapi) + ingame_prop_trader (reprice on realized state -> devig DK live two-way -> edge -> policy tier+units -> idempotent row channel=paper_ingame_prop, book=draftkings_live). Wired as auto_loop step _place_ingame_props; settles via the EXISTING prop_settler (market_type=prop) at game end.
- LIVE PROOF: a real tick placed 24 tier-A in-game prop bets on 2 live MLB games (e.g. deGrom Pitcher Ks o5.5 model .95 vs mkt .64 -- he already had 5 Ks at the half; Owen Caissie H+R+RBI o2.5 model .51 vs .28 already at 2). Rows in clv_ledger channel=paper_ingame_prop, staked 1u, surfacing in /api/paper/trail. 19/19 READY. 40 per-file tests GREEN (box5 + trader7 + live-adapter5 + dist5 + repricer18).
- HONEST FRAME: the freshness reprice on realized state IS the validated lever (calibration). The big model-vs-live-line gaps (often +0.2-0.3) are MODEL_VIEW -- likely partly model overconfidence on a liquid line, SAME artifact family as pregame props; CLV is the judge, edge_claimed=False, units only, real-money DENY. Fires whenever an MLB game is live (and auto-extends to NBA in-play next season). Local commit, NOT pushed.

## >>> SESSION 2026-06-24b (IN-GAME PLAYER PROPS: edge ENGINE built+wired; live MLB prop MARKET does not exist on books -- SUPERSEDED by 24c: the market DOES exist via the DK event endpoint)
USER DIRECTIVE: "in-game props are my edge -- find it, use it for kalshi and sportsbooks." Built the full engine; ran the real due diligence on the live market. LOCAL only.
- BUILT (all SAFE areas, 12 new per-file tests GREEN): the in-game player-prop edge engine, end to end --
  (1) scripts/platformkit/ingame/ingame_box_mlb.py: LIVE realized-stat + freshness reader (keyless statsapi; REUSES prop_settler_mlb parsing so live reprice == post-game settle, no drift). live_games / boxscore_stat_map / realized_for / is_known_stat. 5 tests.
  (2) scripts/platformkit/ingame/ingame_prop_trader.py: the trader. per live game x prop -> reprice on realized state via the EXISTING ingame_prop_repricer (final = realized + remaining, lam shrunk by frac_elapsed; the user's exact thesis) -> devig the book's LIVE two-way -> edge = model_p - market_p -> pm_trading.policy tier+units -> idempotent paper row channel=paper_ingame_prop. Reads pregame lam/model from the refreshed snapshot props.edges (no 136s rebuild). 7 tests. 286 LOC.
  (3) WIRED as guarded auto_loop step _place_ingame_props (min_tier B, cap 12/live game); rows are market_type=prop so the EXISTING prop_settler grades them at game end (units realize) -- no new settler. m1_paper reloaded; 19/19 READY.
- THE HONEST MARKET FINDING (the blocker -- did NOT fake bets): there is currently NO liquid LIVE full-game player-prop MARKET to price against. PROOF: the one live MLB game (TEX@MIA) was probed mid-flight -- DK's pregame prop catalog DROPS a game at first pitch (all 14 listed events NOT_STARTED; the live game's DK payload had only 3 markets, ALL game-level moneyline/total, ZERO player props). FanDuel adapter = "unsupported sport mlb"; BetMGM = empty; Kalshi in-play = game moneyline only (no liquid player props, consistent w/ [[project-wc-prop-vertical-2026-06-17]] zero-liquidity finding). So MLB books offer full-game batter props PREGAME only; in-play they pivot to at-bat MICRO-markets (next-PA hit/out) which are a DIFFERENT model, not a full-game stat reprice.
- NET: the EDGE ENGINE is built, tested, wired, auto-settling, and sport-blind; it fires the moment a liquid live full-game prop two-way price exists for a live player. That market is NBA in-play player props (DK lists live pts/reb/ast) -- but NBA is OFFSEASON now. So today it correctly places 0 (honest no-op), not a fake. NEXT OPTIONS for the user: (a) it auto-lights-up for NBA season; (b) build a DK in-play MICRO-market (per-PA) model+adapter for MLB live action (real, different build); (c) re-probe DK mid-game later in case it re-lists some live props. UNITS only, edge_claimed=False, real-money DENY, no $ edge. Local commit, NOT pushed.

## >>> SESSION 2026-06-24 (PAPER AT HIGHEST LEVEL: stack un-deadlocked + best-bets boards un-stale, FAST)
USER DIRECTIVE: "make sure paper trading is at highest level today -- multiple books, best bets, in-game props, working efficiently, most units, front end showing it." Diagnosed + fixed the real breakages; honest about the in-game wall. Committed LOCAL only (NOT pushed).
- ROOT CAUSE #1 (stack looked broken): THREE supervisors were running (go.ps1 launched 3x), deadlocked over ports 8098/8099 -- each held one port, blocking the other's child -> m1_api_paper/m1_api_boards crash-looped (5+ restarts). stop.ps1 ALSO left stragglers (brain_rebuild_runner was MISSING from boot.ps1 STOP_PATTERN; scheduler/supervisor survived). FIX: full clean stop + manual straggler kill + ONE go -> 19/19 READY, 1 supervisor. Added platformkit.brain_rebuild_runner to STOP_PATTERN.
- ROOT CAUSE #2 (best-bets boards empty/stale): the FE Best-Bets page calls :8099 /api/v1/bestbets/<sport> (predict_service store) -- but (a) the producer only made NBA (BOOT_SPORT=nba, offseason-empty) so mlb/soccer store snapshots only got produced on-demand and a transient 'unavailable' stuck; (b) the route's build_edge_view line-shops+decides EVERY market live = 32-90s/req uncached -> the UI timed out. The :8098 boards (serve.py) read data/frontend/snapshots/<sport>.json which NOTHING refreshed (mlb missing, soccer 2 days stale). FIXES (all SAFE areas + per-file tests GREEN): (1) producer _boot_producer_runner -> BOOT_SPORTS csv default nba,mlb,soccer_intl (keeps store fresh; boot.ps1 heredoc+env updated). (2) frontend/bestbets_routes -> SERVE-STALE-WHILE-REVALIDATE cache (TTL 300s): once an OK envelope exists it serves instantly + rebuilds in a daemon thread; cold build blocks only the first req/sport (warmed at deploy). Live: mlb/soccer warm reqs now 15-57ms (were 53-90s timeouts). (3) auto_loop new guarded _write_frontend_snapshots step (staleness-gated 900s, mlb+soccer) keeps the :8098 snapshots fresh. Regenerated both snapshots now.
- RESULT (verified live): 19/19 READY, 1 supervisor; UI :3000 200; core paper endpoints 9-49ms; /api/v1/bestbets/mlb status=ok 15 games / 9 best bets (tier B moneyline line-shopped to best book e.g. pinnacle, model 0.526 vs mkt 0.481) / 37ms cached; soccer ok 6 games / 0 bets (honest-empty, no edge clears floor). Paper bankroll 113.6u (start 100). Today: 279 paper (props+ML) + 10 Kalshi PM + 2 in-game, tier A/B only. CLV +2.78% / 45% beat-close (n=20).
- HONEST WALL (told the user, did NOT fake): (a) IN-GAME is game MONEYLINE, deliberately MEASUREMENT-ONLY (stake=0, edge_claimed=False) because in-game vs-close CLV is UNPROVEN -- staking it would chase an unproven edge (banned). It fires + calibrates, does NOT make units yet. (b) IN-GAME PLAYER PROPS do NOT exist: no liquid in-play prop prices (Kalshi props = zero liquidity) -> building them would fabricate a line. Units come from pregame DFS props + multi-book moneyline + Kalshi PM. Tests: 15 auto_loop + 4 bestbets-cache GREEN. UNITS only, real-money DENY, no $ edge. Local commit, NOT pushed.
- KNOWN GOTCHA for next session: surgical "kill child -> supervisor respawns" RELOADS code but can ORPHAN the dependency cascade (killing api_paper orphaned the node UI on :3000 -> EADDRINUSE flap; fixed by killing the orphan node). The supervisor does not reliably reap detached Windows children before rebind. Prefer a full clean stop (hardened STOP_PATTERN) + manual straggler sweep + one go.

## >>> SESSION 2026-06-23 (WIRE MLB IN-GAME BETTING -- the daily slate now trades live, not just pregame)
USER DIRECTIVE: "in-game is the edge -- when does it start? wire MLB in-game betting." Did it. Committed LOCAL only (NOT pushed).
- THE GAP: in-game placement (inplay_capture_loop -> model_fn -> devig Kalshi live price -> gate -> bet, channel=paper_ingame) was LIVE for soccer/WC only -- live_board.live_model_home_prob returned None for every non-soccer sport, so MLB in-game ticks died at "model_prob None" even though the capture daemon ALREADY scans mlb (DEFAULT_SPORTS=[mlb,soccer_intl]) and ingame_live_state.live_state('mlb',gid) ALREADY supplies the leak-free pregame prior p0 (p0_source=PRIOR), the realized run-diff (state_diff), and the freshness lever (frac_elapsed from innings). The number was the only missing piece.
- BUILT scripts/platformkit/ingame/mlb_live_model.py (+8 tests, 93 LOC): mlb_home_prob(state) blends p0 toward the realized run-diff using the SAME VERIFIED eval-gate in-game blend core (blend_apply.apply_surface -- NOT reimplemented) via a SEED MLB surface (freshness-ramped weights + MLB run-margin sigma ~3.2 vs NBA's 13.5). Honest: only the validated FRESHNESS lever (early innings -> ~pregame prior, late -> ~live score; variance shrinks); carries the proven prior; NEVER fabricates (missing p0/diff/frac or a FINAL game -> None clean skip). Sanity: p0=.45 tied top-1st->.456; +2 in 9th->.934; -3 in 9th->.037; final->None. calibration, NOT a $ edge.
- WIRED via a 3-line seam in live_board.live_model_home_prob (s=='mlb' -> mlb_home_prob); soccer path unchanged. E2E proof: a live MLB state (home +2, 7th, prior .46) now returns model_prob 0.7492 (was None). The day-trader still gates every placement (calibration_justified=PRIOR + is_liquid + is_fresh + tier floor) -- this only supplies the number.
- WHEN IT FIRES: the moment an MLB game is mid-play (frac<1) with a liquid Kalshi price and the blended model diverges from the devigged market beyond the floor. The 3 MLB games live at build time had just gone Final (no bet, correct); fires on the next live MLB slate. MLB plays daily w/ many live games -> far more in-game action than the WC handful. 15 per-file tests GREEN (mlb_live_model 8 + capture_bridge 7). live_board 341 LOC (pre-existing >300; +7 seam). UNITS only, real-money DENY, edge_claimed=False, no src/kernel edit. Local commit, NOT pushed.

## >>> SESSION 2026-06-22h (coverage audit: Kalshi props NO-LIQUIDITY (honest skip) + raise caps for "more good bets")
USER DIRECTIVE: "bet Kalshi player props too, find best bets, do as many good bets as possible and record it -- make sure it's doing all that." Audited full coverage; raised caps; honest no-go on Kalshi props. Committed LOCAL only (NOT pushed).
- KALSHI PLAYER PROPS -- CHECKED, honest NO: Kalshi DOES list MLB per-game player props (series KXMLBRBI, e.g. "Elly De La Cruz: 2+ RBIs?") + WC player props (KXWCGOALCOMBO/MESSIMBAPPE), but scanned 96 open KXMLBRBI markets -> EVERY ONE has yes_bid=None, yes_ask=None, volume=None, last_price=None (zero liquidity, no tradeable price). WC props are illiquid combos/tournament-level too. So there is NO market price to bet against -> trading them would FABRICATE a line (banned). The system correctly does NOT bet them; that is the honest, correct behavior, not a missing feature. (Kalshi's liquid props are NBA Reb+Ast / NFL -- both off-season now.)
- COVERAGE AUDIT (what's placed vs available, all RECORDED in clv_ledger): DFS props Underdog mlb 491 + WC 38 A/B-placeable; Kalshi game moneyline mlb 10-matched/7-placeable + WC 1; game moneyline (sportsbooks); in-game when live. HONESTLY UN-BETTABLE + correctly skipped: Kalshi player props (no liquidity), Polymarket (outrights, no model), spread/total (we model moneyline only -> model_only line display, no price-able model), tennis/club-soccer props (no DFS prices right now). => the system already bets everything it can HONESTLY price.
- THE ONE REAL "more good bets" LEVER: the prop cap was 25/sport but ~491 mlb A/B-quality props clear the bar -> only a fraction placed. RAISED _PROP_MAX_PER_SPORT 25->50 and _PM_GAME_MAX_PER_SPORT 12->25 (auto_loop) so it records AS MANY A/B-quality bets as possible (min_tier='B' floor kept -> still NO tier-C garbage). Live post-restart: mlb props grew (249), all tier A/B (326A/6B, zero C); 19/19 READY 0 restarts; bankroll +7.57u; Kalshi PM 19. 11 auto_loop tests GREEN.
- HONEST FRAME (told the user): "good" = clears our positive-MODEL-EV + reliable + A/B bar (calibration view), NOT a proven $ edge; more volume = more VARIANCE + more exposure to the model-overconfidence artifact, not more guaranteed units. CLV stays the judge; real-money DENY. Local commit, NOT pushed.

## >>> SESSION 2026-06-22g (REAL Kalshi/Polymarket paper trading -- PM game-market placer SHIPPED + live)
USER DIRECTIVE: "start doing this for Kalshi and Polymarket -- have a paper trading system for them." Built + activated a genuine PM game-market paper trader. Committed LOCAL only (NOT pushed).
- DIAGNOSIS: the M12 PM tick produced 0 PM bets EVER because (a) the PREGAME Kalshi odds provider (odds_provider.kalshi.KalshiProvider) returns 0 game markets for every sport (dead/empty keyless endpoint), and (b) Polymarket only surfaces un-modelable tournament OUTRIGHTS ("Will Spain win the WC", home/away null). Meanwhile the keyless Kalshi EXCHANGE feed (inplay_kalshi.fetch_inplay) exposes ABUNDANT liquid game moneyline -- 87 WC 3-way + 42 MLB 2-way right now, each with a real tradeable YES price. The PM paper system was wired to the wrong source.
- BUILT scripts/platformkit/pm_trading/pm_game_placer.py (+10 tests, 300 LOC): fetch_inplay -> group_by_game -> match each Kalshi game to OUR predict-store slate by normalized-substring team bridge (Kalshi city "Toronto" -> model "Toronto Blue Jays"; unique-or-skip) -> devig the exchange price (2-way normalize; 3-way FOLD the Tie into the field) -> edge=model_prob-market_prob -> tier via the SAME pm_trading.policy -> record is_pm=True, venue=kalshi, channel=paper_pm, market_type=moneyline, bet_id=pm|venue|game_id|side (idempotent). HONEST guards: no model match / OUTRIGHTS -> NO bet (never guessed); PLAUSIBILITY band [0.05,0.95] + EV<=100% drops thin/stale quotes (caught a live Angels-home 0.0198 quote that fabricated a +2367% fake edge -> skipped). UNITS only, executed=False, edge_claimed=False, real-money DENY.
- WIRED as a guarded auto_loop step _place_pm_games (min_tier='B', cap 12/sport) right after place_props. LIVE post-restart: 19/19 READY 0 restarts; 18 Kalshi game bets placed (17 mlb + 1 WC), ALL market_prob plausible, venue=kalshi, is_pm=True. Removed the 1 pre-guard stale row from the ledger.
- HONEST CAVEAT (told the user): this is a real, working PM paper pipeline, but the bets it flags (tier A, big model-vs-Kalshi divergence e.g. SD 0.557 model vs 0.343 market) are MODEL_VIEW divergence, NOT a proven edge -- on a liquid exchange a 20-pt disagreement is usually our model being overconfident, same artifact family as props. CLV is the judge. Polymarket H2H + a PM settle/CLV path are the next pieces (Polymarket today = outrights only, un-modelable). 27 per-file tests GREEN across the session (10 pm_game_placer + 11 auto_loop + 10 props_paper_placer-incl-min_tier... ). Real-money DENY. Local commit, NOT pushed.

## >>> SESSION 2026-06-22f (betting DISCIPLINE: drop the tier-C prop flood + FRESH SLATE restart)
USER DIRECTIVE: "make the betting better, restart it as a new slate starting now, try to make as many units as possible, whole system at its best incl best in-game bets." Committed LOCAL only (NOT pushed).
- HONEST DIAGNOSIS first (told the user, did not spin): paper bankroll was 100->96.5u (-3.5u); moneyline CLV mean -0.56%, beat-close 17% (3/18); settled 135W-147L (~48%). The prop "EV" is MODEL_VIEW (tier A shows +17% median EV = the model disagreeing with a sharp DFS line = the SAME inflated-edge artifact family as the retracted +18.38%), NOT a proven edge. So "make more units" can NOT mean "bet more" -- on an efficient market that bleeds. The defensible lever = DISCIPLINE (fewer, sharper bets) + rely on the one calibrated lever (in-game), and let CLV judge. No $ edge claimed.
- CHANGE 1 (discipline floor): props_paper_placer.placement_from_edge/run gain a min_tier param (_TIER_RANK A>B>C); auto_loop._place_props now passes min_tier="B" -> DROP the marginal tier-C props whose proxy-adjusted EV is swamped by vig+no-true-close penalty. Live dry-run: mlb 351->303 placeable, soccer 50->44 (tier C gone). Cap stays 25/sport highest-EV-first. EV stays labelled MODEL_VIEW. 10 placer tests + 11 auto_loop tests GREEN (incl. 2 new min_tier tests).
- CHANGE 2 (FRESH SLATE): archived the 1574-row clv_ledger + grade/bankroll/pnl/today to data/frontend/_ledger_archive/<ts> (REVERSIBLE), truncated clv_ledger to empty, reset bankroll=100u/net0, grade n_settled=0, pnl empty. One clean restart -> 19/19 READY 0 restarts. Post-restart the loop placed a FRESH disciplined slate: 32 rows = 25 tier-A props (NO tier C) + 7 mlb moneyline; bankroll fresh 100u/net0.
- IN-GAME verified at its best: all in-game daemons (m2_inplay, m2_inplay_capture, m6_ingame_loop, m11, m12, m13) READY 0 restarts; captured 3 WC matches today (NOR-SEN/FRA-IRQ/ARG-AUT, live ticks) -> fires automatically on the next live WC kickoff (between matches this instant = honest no live bet). MLB in-game still model_fn=None (only soccer wired) = honest no-bet, not a flood. Real-money gate authorizes_bet=False (DENY).
- RAILS: units only / no $ key / real-money DENY / honest (no fabricated edge -- told the user props are unproven) / local commit NOT pushed / per-file tests only / build in scripts/platformkit. data/ archive is gitignored (not committed; reversible local backup). NEXT: watch the fresh CLV as the disciplined slate settles -- if A/B still bleeds, the honest read is "props have no edge, lean on in-game"; consider per-sport prop on/off based on settled CLV.

## >>> SESSION 2026-06-22e (brain: LIVE-ACTIVITY hubs so the graph shows WHAT THE AI IS DOING)
USER DIRECTIVE: deepen the Obsidian brain so opening graph view shows the LIVE layer (models+calibration, ship/hold/reject, paper-trading pregame+in-game, per-sport coverage, running daemons) -- WITHOUT widening the backbone scope filter or flooding the graph. Committed a41d58d4 (local only, NOT pushed).
- BUILT (generators only, safe area scripts/platformkit/**): (1) NEW activity_sources.collect_activity -- READ-ONLY snapshot from data/frontend/* (supervisor_status, autonomy_status, ingame _heartbeat, grade_summary, improve_ledger, prop_improve_ledger, prop_history_meta, clv_ledger) + read-only data/registry signal_*.parquet. PERSON-FREE (counts/sport-keys/verdicts/service-names only -- NEVER a player/team/matchup; clv_ledger player names + matchups are aggregated away), UNITS-ONLY (no $), idempotent (stamps from the stores), defensive honest-empty; writes NOTHING. (2) NEW brain_activity.build_activity -- renders _Index/_Whats_Happening.md (ONE master live map: services up, what's predicting per sport, CLV track record, SHIP/HOLD/REJECT self-improve ledgers incl prop-history replication meta, live paper trading incl paper_ingame, signal registry; links DOWN to detail) + per-sport <SPORT>/_Activity.md (calibration + ship/reject + live-trading, honest-empty per sport). Both live in the GRAPH BACKBONE scope (_Index/ and <SPORT>/_*) so they SHOW in the small default graph and link to detail. Wired as a guarded with_models stage in brain_pipeline (never on the hermetic fixture path).
- HARD CONSTRAINT HELD -- graph stays SMALL: did NOT touch _GRAPH_BACKBONE_SEARCH in brain_vault.py. Verified via a FULL --with-models pipeline rebuild into a THROWAWAY tree (live vault untouched -- Obsidian was open, 5 procs; deep-dive forbids rebuilding the live vault while Obsidian holds it): backbone graph.json filter UNCHANGED; small graph stays small = 57 backbone nodes (was ~52; +5 = 1 master + 4 per-sport _Activity) vs 5086 total; 100% reachable (5125/5125, 1 component) with the real master _Index.md as BFS root; the _Brain hub footer (enrich_hubs clique) bridges master -> _Whats_Happening -> per-sport _Activity.. GATES PASS: person_free / graph_clean / edge_clean all True; edge-claim audit 0 flagged; honest banner on all activity notes.
- REAL on-disk readout the hub now surfaces (genuine, honest): 19/19 services ready; per-sport last_decision NO_CANDIDATE; CLV n=16 settled mean -0.204% beat-close 18.75% (UNPROVEN, edge_claimed=false); game ratchet mlb=SHIP nba/soccer/tennis=INSUFFICIENT; prop-history replication mlb all/hits/runs/strikeouts=SHIP_REPLICATED, mlb rbi + tennis=REJECT (planted-null fired -- honest), soccer=INSUFFICIENT; paper 1237 placements (1163 open / 74 settled / 2 in-game paper_ingame); signals 86 catalogued (72 folded), lab 5 VALIDATED / 16 REJECTED. An honest REJECT/empty is a real node, not hidden.
- LANDED: 13 per-file tests GREEN (test_brain_activity 6 incl person-free/graph-clean/no-$/honest-empty/links-down + test_brain_pipeline 2 + test_brain_audit 5). UNITS only, no $ key, no flag/registry flip (data/registry read-only), real-money untouched, vault local-only (NOT committed), local commit a41d58d4 NOT pushed.
- ACTIVATION (honest): the m14_brain_rebuild daemon is a long-lived subprocess that imported run_pipeline at start -> it picks up the new stage on the NEXT stack restart (.\stop.ps1; .\go.ps1) or m14 restart. A live rebuild also needs Obsidian CLOSED (it reverts graph.json + locks files). Until then the live brain lacks the hubs; the integrated pipeline is proven correct via the throwaway full rebuild above. NEXT: close Obsidian + restart stack (or `python -m scripts.platformkit.brain_pipeline --with-models`) to materialize the hubs in the live vault; the snapshot then refreshes every m14 tick (~6h).

## >>> SESSION 2026-06-22d (FRONT-END FILL: real per-venue execution view + prop identity on /paper-trading)
USER DIRECTIVE: lean live-updating FE; paper-trading must show what it's DOING across venues (Kalshi + Polymarket + sportsbooks) with best execution; every card filled, honest empties, units only. Committed LOCAL only (NOT pushed).
- AUDIT (live, real data): supervisor 19/19 READY 0 restarts all_ready=True; :8099 health 200; pages /paper-trading,/bets,/games,/models 200. Mapped each page->API: /games SlateCards uses /api/predict/<s> (matchups+pregame_probs+markets) + /api/v1/bestbets/<s> (chip); /bets BestBetsBoard uses /api/bestbets/board?sport=<s> (sport-filtered cards); /paper-trading uses /api/paper/trail (+pm/trail, clv, pnl, bankroll).
- ROOT CAUSES FOUND + FIXED (the specific breaks, not papered over):
  (1) /paper-trading "Per-venue breakdown" was EMPTY because it was fed `pmRows` (the Kalshi/Polymarket PM trail, genuinely ~0) instead of the REAL trail. The trail already encodes venues via taken_book (underdog/DraftKings/pinnacle/paper_ingame). FIX: new helper mergeVenueRows(viewRows,pmRows) (dedup by stable key) -> VenueSummary now aggregates the real trail UNION PM. LIVE it renders DraftKings(52) + pinnacle(22) + underdog(125) + paper_ingame(1) with open/settled/W-L/units/CLV per venue. venueOf already mapped these correctly; only the data source was wrong.
  (2) Prop rows rendered opaquely ("prop / home / 0.5") -- the ledger carries prop_player/prop_stat/prop_side but frontend/paper_trail._build_trail_row DROPPED them. FIX: carry the 3 fields through read_trail (+type, +OpenPositions/PaperTrailSettled render). Props now show e.g. "Maikel Garcia . Hits+Runs+RBIs . over 1.5 @ underdog 2.06 tier C". Non-prop rows = None (never fabricated).
- WC moneyline HONEST FINDING (not a bug): the WC pregame moneyline canNOT be priced as a best bet -- only home+away decimal odds are captured (devigged_prob=null, NO draw row), so a clean 3-way devig is impossible; a 2-way normalization would fold the draw mass into home = fabrication. So the honest outcome is "no bet"; the WC /games cards ARE filled (calibrated 1X2 prediction home_ml/away_ml/draw + spread/total market rows + 124 prop cards on /bets). Capturing the draw side (3-way) is the real lever to make a WC moneyline best bet -- deferred (odds-provider+producer change, needs live verification; refused to fabricate).
- LANDED: backend frontend/paper_trail.py (+prop fields, kept 300 LOC) ; webapp types.ts + paper-trading/page.tsx + paperTradingHelpers.tsx (mergeVenueRows) + VenueSummary/OpenPositions/PaperTrailSettled. Rebuilt webapp (npm run build OK) + ONE clean restart -> 19/19, 0 restarts, status fresh; API serves new prop fields live; in-game WC ARG-AUT paper bet still present (channel=paper_ingame). TESTS: backend 57 GREEN (test_paper_trail incl new prop-fields test + test_paper_routes + test_paper_panel_render); webapp paper_pm 106 GREEN + venue/page venue-tests GREEN (the 10 venue failures my change caused are all fixed: WS3 inject PM with empty main trail still pass; WS2-4 updated to assert real-trail venue tiles). HONEST CAVEAT: 3 PRE-EXISTING page-test failures remain (WS2-2 "open count=8", WS2-6 + paper_trading_page "no P&L/PnL text") -- PROVEN pre-existing by stashing my webapp files and re-running on the clean baseline (they fail identically without my edits). The "P&L" hits are legit UNITS-context copy in PaperEquityPanel ("Daily P&L in units"); I did NOT delete assertions to game them.
- RAILS HELD: units only / NO new $ key (honesty grep clean) / real-money DENY / honest-empty>fake / local commit NOT pushed / per-file tests only / ASCII / did NOT edit src,kernel,api,scripts/team_system,intel. .claude/launch.json got a webapp preview config (serves the prod build on :3100; preview is CORS-blocked from :8099 so can't screenshot the live page -- verified data flow via the API + component tests instead).
- NEXT: true "best execution vs alternatives" = record the shopped book set into the ledger so the trail shows the price beaten (placer already books the best line; the alternatives just aren't persisted); WC pregame moneyline needs draw-odds capture (3-way devig); periodic snapshot-refresh daemon still wanted (soccer_intl snapshot ~7h stale but the webapp reads the fresh :8099 API, not the snapshot, so non-blocking).

## >>> SESSION 2026-06-22c (WC ready-to-trade: pregame props volume + in-game game-market trading WIRED)
USER ASK: "World Cup games starting soon -- be ready to trade in-game player props, most should have algorithms, make most units today." Committed 1d4ccfa8 (local only, NOT pushed).
- HONEST REALITY surfaced (and confirmed with the user): true IN-GAME *player* props are NOT tradeable -- no live prop PRICE exists anywhere (DFS books Underdog/PrizePicks lock player props at kickoff; the live exchange feed = Kalshi GAME-level markets only, 974k WC moneyline ticks today). I will not fabricate a price. User chose: do all three of (1) max pregame WC player-prop volume, (2) turn on in-game GAME-level trading, (3) refresh+surface the board.
- (1) PREGAME WC PLAYER PROPS -- the real player-prop lever, ALREADY LIVE: prop_edge.build_prop_board('soccer_intl') works (775 edges; guards flag 21 implausible + 282 thin; ~80-94 floor-clearing reliable). Raised auto_loop _PROP_MAX_PER_SPORT 8->100 (placer still gates reliable+ev_flag ok+proxy tier floor; cap is only a flood-bound) so a full slate places same-day. RESULT: 94 WC props + 456 MLB placed today (clv_ledger, units-only, open). HONEST: tiers are MODEL_VIEW, median reliable EV ~ -0.04 -> "most units" = place all floor-clearers, NOT flood; calibration != edge.
- (2) IN-GAME GAME-MARKET TRADING (moneyline) -- FIXED 3 real bugs that had it dead in production: (a) capture loop drove sport 'soccer_intl' but ingame_live_state only knew 'soccer' -> no_live_state; (b) default model_fn called live_board.live_model_home_prob which DID NOT EXIST -> model_prob None for EVERY sport; (c) loop keyed games by Kalshi ticker != ESPN event id -> live_state always missed (MLB in-game capture stale since 06-19). Fixes (paper/units, real-money DENY, leak-free, gated): ingame_live_state +soccer_intl (ESPN soccer/fifa.world) +absolute scores/display +live_states() scanner; live_board +live_model_home_prob (reuses the CALIBRATED soccer_intl predict_live 1X2 p_home_win; other sports None=clean skip); inplay_capture_loop +_scan_live_by_legs (bridge Kalshi game->ESPN live state by TEAM display-name; no match -> no bet, never misaligned). VERIFIED e2e offline: live USA-MEX tick -> bridge -> calibrated model -> devig -> gates -> tier-A paper bet (n_bets=1, reason=ok). LIVE NOW: no WC match in progress this instant -> honest no_live_state; fires automatically when the next game kicks off. NOT player props (team moneyline) -- the calibrated in-game model is 1X2/totals, and that is the only in-game market with a live price.
- (3) SNAPSHOT refreshed: snapshot_writer soccer_intl -> data/frontend/snapshots/soccer_intl.json (was 06-18 stale; now 786 prop edges + live section). serve.py prefers snapshot.
- LANDED: per-file tests GREEN (ingame_live_state 8, capture_bridge 4, live_board 18, capture_loop 5, auto_loop_steps 11, props_paper_placer 8). Two clean restarts -> 19/19, 0 restarts. real-money authorizes_bet=False everywhere; clv_ledger 0 `hist|` pollution. NOT pushed.
- LIVE-CONFIRMED on the ARG-AUT World Cup match (commit a0911441): the supervised m2_inplay_capture daemon is capturing the live game -- heartbeat n_live=1 n_pairs=1, data/cache/ingame_grade/soccer_intl/KXWCGAME-26JUN22ARGAUT.jsonl growing (market_prob~0.65 devigged, model p_home_win~0.48, side=home -> honest no_bet/below_floor: model sees no ARG edge vs the market, captured for CLV). Found + FIXED two bugs LIVE: (a) the team bridge OVER-MATCHED on a single shared team -> bound the live ARG-AUT game to a FUTURE JOR-ARG market (both have Argentina), cross-pairing one game's model with another's price; now requires BOTH teams to align (_team_in_legs x2). Removed the stale false-match capture file. (b) WC moneyline is a 3-WAY market (home/draw/away) but the daytrader devig is 2-way -> bad_price; _fold_draw_into_field folds the draw into the not-home complement -> correct fair P(home win)=home/(home+draw+away). It will paper-BET automatically the moment the calibrated model's edge clears the tier floor (e.g. ARG scores and the model jumps above the market price).
- PAPER-TRADING VISIBILITY: /paper-trading reads /api/paper/trail (frontend/paper_trail.read_trail, collapsed open->settled, cap 200). In-game bets DO land here (paper_ingame writes clv_ledger channel=paper_ingame), but read_trail returned open rows OLDEST-first -> with ~908 open paper bets (the raised prop cap), a freshly-fired bet was buried past the 200 cap and never shown. FIXED: sort each bucket NEWEST-first (settled-before-open contract kept) so a live in-game bet (ts=now) surfaces at the TOP of the open section. Committed. NOTE: the page is now a firehose (~900 open bets from cap=100); WC props are mixed in. Options if wanted: add a sport/in-game FILTER to the webapp page, or trim the prop cap. The live in-game WC bet WILL appear at top when it fires.
- LIVE IN-GAME BET FIRED + VISIBLE (commit bd46c610): on ARG-AUT a tier-C paper bet (ARG win_home) fired (model p_home_win~0.86 vs lagging market) -> landed in clv_ledger channel=paper_ingame -> now shows on /paper-trading under BOTH the new "World Cup" and "In-game (live)" scope filters. Three user-requested visibility changes shipped: (1) webapp paper-trading scope filter All/World Cup/In-game (client-side; WC=sport soccer_intl, In-game=channel paper_ingame); webapp recompiled OK. (2) relaxed in-game EV floor (opt-in min_ev in inplay_edge_signal.evaluate; soccer/WC=+1%) -> surfaces smaller model-vs-market divergences as LOWER-CONFIDENCE honestly-CLV-graded bets (reason=ok_relaxed), still gated on calibration_justified+liquid+fresh+positive-EV; default None=strict unchanged. (3) auto_loop prop cap 100->25 (de-flood) + frontend/paper_trail preserves channel so the UI isolates in-game rows. Per-file tests green (edge_signal_relaxed 5, paper_trail 24, capture_bridge 8). NOTE: 2 PRE-EXISTING test_inplay_daytrader failures (added_new) are from prior uncommitted clv_ledger.py WIP (game_id len guard / _strip_banned), NOT my edits -- proven by stash-revert. Real-money DENY, units-only, NOT pushed.
- NEXT: in-game bets are LOWER-confidence now (relaxed floor) -> watch CLV honestly (likely ~0/negative; that is the truth, not a failure); periodic snapshot-refresh daemon still wanted; MLB/NBA in-game model_fn still returns None (only soccer wired).

## >>> SESSION 2026-06-22b (prop history ratchet EXTENDED to MLB/tennis/soccer + replication meta-aggregator)
OVERNIGHT DIRECTIVE: keep the paper system running well + make it honestly better via the props-from-history calibration loop. Units-only, calibration NOT edge, real-money default-DENY, honest REJECT/INSUFFICIENT = SUCCESS. No fabricated $ edge. Committed 939b44f7 (local only, NOT pushed).
- HEALTH VERIFIED LIVE (real data): supervisor 19/19 READY, 0 restarts, all_ready=True. /paper-trading :3000 HTTP 200; :8099/health 200. clv_ledger 0 `hist|` rows (units ledger NOT polluted by backtest corpora). real-money gate authorizes_bet=False everywhere (DENY). honesty_mw._money_key_violations clean on units payload, fires on a $ key. Prior recent work intact: prop_calibration_ratchet + prop_history_backtest tests GREEN (16).
- MAKE-IT-BETTER step 1 (EXPAND coverage): NEW scripts/platformkit/improve/prop_history_multisport.py (+8 tests) -- reuses the NBA leak-free recency-Gaussian as-of pattern over on-disk per-player gamelogs for MLB (batting: hits/totalBases/rbi/runs/strikeOuts from player_gamelogs.parquet), tennis (serve: ace/df melted from match_stats+matches), soccer (totalShots/shotsOnTarget/foulsCommitted from espn_player_stats). Each sport writes its OWN corpus file (prop_history_corpus_<sport>.jsonl, never the units ledger) and runs the SAME prop_calibration_ratchet (planted-null do-no-harm; market_prob=None -> no historical close). REAL on-disk verdicts: MLB=SHIP (delta_brier +0.0109, dm_p=0.0, null rejected), tennis=REJECT (planted-null FIRES on low-count discrete serve stats -- honest do-no-harm), soccer=INSUFFICIENT_DATA (World-Cup-only table is genuinely thin). Wired as a guarded m1_paper step that rotates sport per cycle.
- MAKE-IT-BETTER step 2 (REPLICATION meta): NEW scripts/platformkit/improve/prop_history_meta.py (+8 tests) -- READ-ONLY aggregator over accumulated source=history folds. Groups by (sport,stat); replicated verdict = SHIP_REPLICATED only when SHIP holds across >=2 independent folds (single-fold lift = SHIP_SINGLE_FOLD artifact-suspect, NOT shippable); any REJECT or planted-null leak blocks the group. Writes read surface prop_history_meta.json; never recalibrates/serves/flips a flag. Wired as a guarded read-only m1_paper step. SEEDED real folds -> first meta report: mlb=SHIP_REPLICATED (4 folds, med dBrier +0.0099), nba=HOLD (7), tennis=REJECT (10), soccer=INSUFFICIENT_DATA (2). A GENUINE replicated leak-free calibration win on MLB batting props -- calibration, NOT a $ edge.
- LANDED: per-file tests GREEN (multisport 8, meta 8, auto_loop_steps 11). One clean restart -> 19/19 verified; live loop already wrote fresh multisport corpora + meta surface post-restart. UNITS only, no $ field, no flag/registry flip, real-money DENY, local commit 939b44f7 NOT pushed. NBA history folds keep accruing; MLB/tennis/soccer folds now accrue overnight too -> replication sharpens by morning.
- NEXT (overnight loop continues): more folds accumulate; meta report sharpens; watch MLB SHIP_REPLICATED holds across more independent folds; consider per-stat folds (only_stat) so the meta breaks MLB down by hits/rbi/etc. instead of ALL.

## >>> SESSION 2026-06-22 (props GET-BETTER ratchet SHIPPED + stack ACTIVATED live + honesty fix)
USER DIRECTIVE: make the paper system a GREAT, self-improving, INDEPENDENT live system (in-game + props, units-only, CLV the yardstick), then START IT RUNNING LIVE and verify on real data. ALL FIVE directive steps done; the previously-deferred ACTIVATION is now LIVE.
- VERIFY THE BUILD (step 1): all 7 directive test files GREEN (props_paper_placer 8, prop_settler 6, prop_settler_mlb 7, auto_loop_steps 10, self_improve 11, grade_paper 13, et_day 3). Reviewed the flagged UNCOMMITTED WIP that a restart loads: boot.ps1 (+STOP_PATTERN daemons -- safe), predict_service/app.py (+/ready deep-readiness probe + paper_series_routes mount, both guarded -- safe), produce.py (stale_drop integration + injectable `now` + a 2x grace so a long LIVE game keeps its prior for the in-game carry -- safe). Their dep tests GREEN after I FIXED a stale test: test_stale_drop asserted a LIVE-past-grace game is dropped, contradicting the deliberate design (a live game is NEVER stale so its prior survives the carry) -> fixed + added the long-running-live-kept invariant test. Boot-critical import smoke test clean (app builds 74 routes incl. the new ones). Committed b54309de.
- HONESTY FIX (caught by the REAL linter): paper_today.json emitted a nested `bankroll` key; the honesty linter bans the `bankroll` token even with a `_units` suffix (only the bare `stake` token is exempt). FastAPI-served paper_bankroll/pnl were already clean (flat *_units); only paper_today (served by the Next route, not the FastAPI middleware) tripped. FLATTENED to start_units/current_units/net_units (matches paper_bankroll.json) + fixed webapp paperToday.ts (was reading non-existent top-level keys -> always null). Verified linter-clean on the LIVE regenerated file post-restart. Committed b54309de.
- THE ONE REAL REMAINING BUILD -- prop CALIBRATION ratchet (step 2, "gets better on props"): NEW scripts/platformkit/improve/prop_calibration_ratchet.py (+10 tests) -- the prop twin of self_improve. Loads settled props (model_prob=P(picked side), outcome=win/loss, market_prob), runs the SAME eval-gate machinery (leak-free walk-forward isotonic recal on OUTCOMES + proper scoring + cluster-robust DM vs frozen baseline) and SHIPs only when recal beats held-out Brier (DM-significant, no regression) AND the PLANTED NULL (isotonic on SHUFFLED outcomes) rejects; else HOLD/REJECT; cold start = INSUFFICIENT_DATA. MARKET-FOLLOW TRAP STRUCTURALLY IMPOSSIBLE: the shipped recal NEVER reads market_prob -> SHIP verdict + recal Brier are provably INVARIANT to the market (test runs truth/oracle/noise markets -> byte-identical recal). shrink-toward-market is RECORDED as a transparency control, NEVER the shipped candidate. PROPOSE/RECORD only (appends prop_improve_ledger.jsonl; never arms serving / flips a flag / writes data/registry/). Wired as a guarded m1_paper step after settle_props -> props get better independently each cycle. On the REAL ledger today = honest INSUFFICIENT_DATA (0 settled props, offseason). Tests prove a planted-positive SHIPs, a base-rate-only "gain" trips the null -> REJECT, calibrated -> HOLD, cold -> INSUFFICIENT, market-invariance. Committed 0d1efa7e.
- MULTI-VENUE + IN-GAME (step 3, verified not faked): clv_ledger = 156 moneyline + (now) 112 prop + 1 Kalshi PM + 0 Polymarket + 0 in-game placements; m12_pm_paper_tick / m2_inplay / m6_ingame / inplay_daytrader (4-gate calibration_justified AND is_liquid AND is_fresh AND tier-floor) all WIRED + READY but offseason-near-empty (honest, not forced).
- ACTIVATED + VERIFIED LIVE (step 4-5, the deferred part): clean restart `.\stop.ps1; .\go.ps1` -> 19/19 READY, 0 restarts, all_ready=True, autonomy overall=ok. /paper-trading HTTP 200; paper_today.json (correct ET day 2026-06-21) shows 116 placed incl. 112 PLACED PROPS (player/stat/line/side/tier A/units 1.0), linter-clean (no `bankroll` key). clv_ledger 112 prop rows OPEN, 0 mis-settled (grade prop-skip guard holds). /api/paper/{bankroll,pnl/series} 200 units-only edge_claimed=false (real linter: NONE). /ready deep self-check status=ready selfcheck.ok=True. prop_improve_ledger got FRESH post-restart rows (INSUFFICIENT_DATA all sports) = the ratchet runs live each cycle. Real-money gate evaluate() = authorizes_bet=False / eligible=False (16<500 settled, CLV lb95<0, pct-beat-close 0.19<0.55) = DENY. 86 per-file tests GREEN across the session. UNITS only, no $ field, no flag/registry/sentinel, NOT pushed.
- HONEST STATUS: settled props = 0 (offseason -- no MLB finals for the keyless resolver; soccer_intl has no settler yet). The settler + prop ratchet are RUNNING in the loop, READY to realize units + recalibrate as games go final. NEXT (data-gated, not code-gated): props settle + the ratchet crosses MIN_RECAL (60) -> first real SHIP/HOLD/REJECT verdict; soccer_intl prop resolver; in-game placements once liquid in-play prices return in-season.
- HUMAN GATES UNCHANGED (Claude must NOT flip): real-money stays default-DENY; pre-existing git-conflict files (api/src markers) untouched/unpushed; autostart UI-env wrapper; self-improve serving-arm. Stack is RUNNING LIVE now.

## >>> SESSION 2026-06-21 (props REALIZE: stat-outcome settler + independent place/settle loop + do-no-harm)
USER DIRECTIVE: "do all the steps needed to get to a great paper system getting better on it independently." Built the rest of the props vertical so the paper system trades, SETTLES, and realizes props on its own each cycle -- and protected the existing self-improve ratchet from prop contamination.
- PROP SETTLER (the keystone -- without it props sit pending forever): NEW scripts/platformkit/bestbets/prop_settler.py (+test) -- grade an OPEN prop (market_type=prop) on its REAL stat outcome (over/under a line) -> UNITS at the taken price; CLV genuinely unavailable for props (no close feed) = honest clv_status="no_close" (never a fabricated proxy); idempotent (settle_key/bet_id); injectable resolver; NON-prop rows untouched. NEW prop_settler_mlb.py (+test) = keyless statsapi schedule->gamePk(FINAL)->boxscore->player->canonical-stat resolver (Hits/TB/RBIs/Runs/HR/Walks/K/SB/HRR/PitcherK/Outs); ANY gap (not final / DNP / network / unknown stat) -> None -> PENDING, NEVER fabricated (DNP returns None, not 0).
- INDEPENDENT OPERATION (no supervisor-registry change): folded place_props + settle_props into pm_trading.auto_loop (the supervised m1_paper loop) as guarded steps -> EVERY cycle the loop now: paper(book) -> place_props(<=8/sport, highest-EV, capped) -> grade(guarded) -> settle_props -> improve -> line -> scoreboard. props_paper_placer bet_id now carries game_date -> idempotent within a day, fresh position per slate day (continuous daily operation). The daemon that already runs hosts it -> props place+settle independently with zero registry edit.
- DO-NO-HARM (critical): self_improve.load_settled had NO market_type filter -> a settled prop (model_prob=P(over), outcome=over/under) would be ingested as a fake GAME home/away outcome, CONTAMINATING the game-win-prob calibration corpus that drives the self-improve ratchet. Added a guard EXCLUDING prop rows from the game corpus (+test). The game ratchet keeps improving on game volume, unpolluted; props recalibrate on their own future track (next step).
- VERIFIED end-to-end on a tmp ledger: place priced edge -> settle (2 hits > 1.5 line -> WIN +0.77u) -> realized in the units P&L series. 54 per-file tests GREEN (prop_settler 9, prop_settler_mlb 7, props_paper_placer 8, auto_loop_steps 9, self_improve 11, grade_paper 13 incl. prop-skip). Committed 8264d313 (safe paths only). UNITS only, no $ field, real-money DENY, not pushed.
- DEFERRED (honest, documented): (a) exec-driven best-bets card "placed" annotation -- a card<->ledger matcher is fragile and a wrong "placed" flag is an honesty regression; placed bets ALREADY render in the /paper execution view (last turn's _PLACED_FIELDS + et_day fixes), so this is polish not core. (b) prop-CALIBRATION ratchet (settled prop model_prob vs over/under -> Brier -> gated recalibration) so props also "get better" -- a separate gate-validated build. (c) multi-venue Kalshi/Polymarket + in-game placement stay VERIFIED-WIRED but offseason data-blocked.
- ACTIVATION NOT DONE AUTONOMOUSLY (by design): the new code activates on the next clean stack restart (loads the grade_paper guard + auto_loop place/settle + self_improve do-no-harm together, so live prop placement is safe). Did NOT run `.\go.ps1` because predict_service/app.py (+62) and produce.py (+123/-64) carry SUBSTANTIAL UNCOMMITTED non-Claude WIP -> a restart would load that unverified code into the live API/producer (risk of breaking the stack / clobbering in-progress human work). Restart is a human call. TO ACTIVATE: `.\stop.ps1; .\go.ps1` (or just restart m1_paper) -> then VERIFY props place+settle each cycle, /paper shows placed props, no $-keys, real-money DENY. Current live ledger has 0 prop rows (safe; last turn's premature placement was reverted) so there is NO hazard while unactivated.

## >>> SESSION 2026-06-21 (paper-trade PROPS into the unified ledger; multi-venue/in-game verified data-blocked)
USER DIRECTIVE: paper trading should cover books + Kalshi + Polymarket, focus on in-game + props, and execution should drive best-bets. DIAGNOSIS (live clv_ledger): 147 pregame sportsbook ML bets, 1 Kalshi, 0 Polymarket, 0 in-game, 0 PROPS -- the bankroll was the OPPOSITE of the "mostly in-game + props" intent. Found the real split: multi-venue PM (pm_paper_tick_runner M12 records Kalshi+Polymarket into the SAME ledger) and in-game (inplay_daytrader/paper_ingame) ALREADY EXIST in code but produce ~nothing offseason (no liquid PM pairs / no liquid in-play prices = DATA-blocked, not code-blocked). The one genuine CODE gap = props: computed as cards (PropLine over/under prices) but NEVER paper-placed.
- BUILT scripts/platformkit/bestbets/props_paper_placer.py (+8 tests, +CLI): reads PRICED prop board edges (prop_edge.build_prop_board, edge_basis==ev_vs_priced, reliable+ev_flag ok), devigs, tiers via the SAME pm_trading.policy (clv_is_proxy=True -> raised floors), sizes capped quarter-Kelly, records into clv_ledger as market_type=prop (over->home / under->away binary leg; raw VIG-INCLUSIVE taken price reconstructed (ev+1)/p = conservative), dedup by bet_id, max_per_sport cap (highest-EV first; overflow counted not dropped). UNITS only, executed=False, edge_claimed=False, linter-clean (_money_key_violations). DRY-RUN proved 128 real priced+tiered placements available NOW (65 soccer_intl + 63 mlb).
- SAFETY GUARD (was a real hazard): grade_paper.grade_open_bets settles EVERY open row whose matchup matches a final game as a MONEYLINE by side -- with NO market_type filter -> a prop row (side=home=over) would be MIS-SETTLED as a game ML, fabricating P&L. Added grade_paper._is_game_ml_bet so the ML settler SKIPS prop/PM rows (they settle on their own stat outcome). +1 test; verified on real data (grade pass n_open 76 game-ML-only, props invisible to it).
- 2 surfacing fixes so PLACED props show in the execution view: (a) et_day.et_day_of -- a bare YYYY-MM-DD is ALREADY an ET day; was being parsed as UTC-midnight then shifted BACK a day (00:00 UTC == 20:00 prev-day ET), hiding today's placed props (+test_et_day, 3 tests). (b) paper_today._PLACED_FIELDS now carry prop identity (player/stat/line/side) so a placed prop renders as a prop, not a blank home/away row. After both, build_today placed went 1->11 with all 10 props rendered correctly.
- LIVE PLACEMENT done THEN REVERTED (honest correction): seeded 10 priced props live to prove the path end-to-end (verified: rows tagged market_type=prop, no $ keys, P&L unaffected = open rows score 0, grade guard held). BUT the RUNNING grader daemon still holds the OLD code (no guard) -> would mis-settle them once today's games go final. Did NOT auto-restart the user's live 18-daemon stack (disruptive + unauthorized). So ATOMICALLY removed the 10 prop rows (ledger 160->150, back to safe pre-placement state). The CODE is committed + tested; props trade live on the NEXT stack restart (loads the guard) via `python -m scripts.platformkit.bestbets.props_paper_placer --max-per-sport N`.
- 32 per-file tests GREEN (props 8, grade_paper 13, et_day 3, paper_today 8). Committed a896dfb3 (7 files, all safe paths). No $ field, no flag/registry/sentinel, real-money DENY, NOT pushed.
- HONEST STATUS of the 4 directive items: (1) PROPS placement = BUILT+tested+ready (activate on restart). (2) EXECUTION-DRIVEN best-bets = PARTIAL: placed props now render in the paper_today execution view + correct day; annotating board cards as "placed" is the next step. (3) MULTI-VENUE Kalshi+Polymarket = VERIFIED WIRED (M12) but offseason DATA-blocked (no liquid PM pairs). (4) IN-GAME = VERIFIED WIRED (inplay_daytrader) but DATA-blocked on liquid in-play prices. NEXT: (a) restart stack to activate the guard + live prop placement; (b) build the prop STAT-OUTCOME settler (resolve player box-score stat vs line -> outcome/unit_result/CLV) so props REALIZE P&L, not just sit pending; (c) wire props_paper_placer into a supervised tick (PROPOSED -- frozen registry); (d) exec-driven best-bets card annotation.

## >>> SESSION 2026-06-21 (in-game sharpener: finer time-grid blend RUN-to-verdict = honest REJECT)
Pushed the IN-GAME + EXECUTION engines per the "validated peak + measurable CLV" directive. The one genuinely-remaining buildable sharpener was item 1 (finer frac_elapsed time-grid blend): the gate runner scripts/platformkit/ingame/ingame_finegrid_gate_nba.py (6x6 fine grid: 6 time x 6 margin, min_cell=30) + its 10 tests were ALREADY BUILT but had NEVER been RUN to a recorded verdict. RAN it on the two real NBA corpora (linescores_2024_25 + linescores = 2025_26, ~3940 quarter-end leak-free states each, cross-corpus both directions). RESULT = honest REJECT: the finer 6x6 grid does NOT beat the proven coarse 4x5 grid -- A->B fine WORSE by -0.00067 (fine_wins=False), B->A fine better by only +0.00034 (fine_wins=True) => single-direction lift = textbook artifact; AND the planted-null fired (null_rejected=False: the finer grid has enough free cells to BEAT coarse on shuffled noise = overfits). Two independent reasons to REJECT. => CONFIRMS THE PLATEAU at the grid-resolution axis: the coarse 4x5 surface is at the calibration ceiling; finer time/margin resolution overfits. The coarse surface stays live (no serving swap). Verdict recorded data/frontend/ingame/finegrid_gate_nba.json (units/Brier only, NO $, vs_close UNPROVEN). EVENT-DRIVEN REPRICE (item 1 P3): NOT built -- it samples the SAME validated surface at more time points (a capture-density change, no new surface to gate); since finer time-resolution REJECTED, event-driven sampling carries no calibration gain -> honored the "don't bolt on unvalidated state" rail and did NOT add it. MEASUREMENT-COMPLETENESS: folded the new REJECT into the closed-index -- added ("ingame","finegrid_gate_nba.json")->"nba_finegrid_blend" to funnel_verdict_reconcile._EXTRA_SOURCES (+1 test) so the improvement_finder recognizes it as CLOSED (now 15->16 recorded gate verdicts; --fold added 1 ledger row source funnel_gate). VERIFIED items 2-4 already-built + correct (no rebuild, no running-stack change): (2) inplay_aggregate_grade enforces INSUFFICIENT_DATA until >=5 games + >=40 ticks + cross-corpus DM, edge_claimed=false, units/prob only; (3) ingame_refresh_runner.refresh_cycle = fetch newly-settled finals past per-sport high-water cursor -> ingest -> re-gate + re-fit -> HONESTY-GATED atomic swap (SWAPPED/DOWNGRADED/HELD); offseason real-feed = no new games -> HELD/no-op (honest idle); shrink-yields-no-corpus trap structurally impossible; (4) edge=model_prob-devig(price) via odds_shop.devig_twoway (Shin), 4-gate (calibration_justified AND is_liquid AND is_fresh AND tier-floor A.08/B.04/C.02 +proxy), capped quarter-Kelly (0.25, corr-haircut, hard-clamp) + kill-switch/exposure caps. 6 per-file tests green (finegrid 10 already-green re-run + reconcile 5). UNITS only, NO $ field, no flag flip, no data/registry, no sentinel, real-money DENY, not pushed. HONEST HEADLINE: the in-game surface is sharpened-OR-rejected -> RECORDED REJECT (the validated coarse lever is at its ceiling); a recorded REJECT is the correct, honest deliverable.

## >>> SESSION 2026-06-21 (in-game + execution engines + "make the most" Kelly curve)
VERIFIED both engines live + working per sport: in-game repricer covers 5 sports (mlb/soccer/soccer_intl/nba/tennis), 9-10 LIVE games repriced this minute carrying the proven prior (p0); execution/paper engine running (100->97.87u flat, units, executed=False). HONEST RAIL HELD: "more models/sims = more edge" is the PLATEAU (deep in-game state beyond margin+time+prior REJECTS across 4 sports) -> did NOT bolt on unvalidated models; the validated engines are already lean. "MAKE THE MOST MONEY" = NEW scripts/platformkit/paper/kelly_curve.py (+test): an edge-proportional CAPPED QUARTER-KELLY overlay sizing each placed bet by conviction via the SAME pm_trading.policy the live day-trader uses, merged into the bankroll series (one line in bankroll_daemon). The flat-1u curve stays the conservative CLV-reconciling record; the Kelly overlay nets -0.15u vs flat -2.13u over 52 bets (RECONCILES to the placed bets at their Kelly sizes). UI: /paper page now shows BOTH curves (flat vs edge-proportional Kelly) with honest labels; HTTP 200, tsc clean. CAUGHT+FIXED a real HONESTY-LINTER bug mid-build: the served 'kelly_stake' key tripped the units-rail banned-money-key linter (token 'stake') -> fail-closed-blocked the whole /api/paper/pnl/series endpoint -> renamed to kelly_stake_units (units-exempt) + kelly_n_sized; verified against the REAL _money_key_violations linter (clean) before re-landing. UNITS only, NO $ field, NOT proven profit, CLV is the yardstick, edge_claimed=False, real money default-DENY. Committed ecb39857 (4 files); 19/19 live; 3 tests green. Honest: a Kelly curve on an UNPROVEN edge can fall as fast as it rises -- it is a paper track record, nothing more.

## >>> SESSION 2026-06-21 (gate build pass) -- 2 remaining untested layers honestly resolved (no fabrication)
Ran the "do the remaining gates" pass HONESTLY. RESULT: (1) MLB ESPN-box defensive/efficiency as-of layer -> NEW scripts/platformkit/gate_run_mlb_espn_box.py (+test) reusing the PROVEN gate core (gate_one/_gate_corpus from gate_run_soccer_espn28) verbatim; on current disk = INSUFFICIENT_DATA (only 317 box rows / ~3 weeks; a leak-free cross-corpus walk-forward needs >=2 seasons -> materialize via domains.mlb.ingest_espn_box then re-run UNCHANGED). machinery_ready=True; a synthetic 2-season corpus proves the gate runs + the planted-null tripwire fires. CAUGHT+FIXED a real HONESTY BUG mid-pass: the synthetic test's run(box=...) wrote its REJECT to the REAL mlb_espn_box_gate.json (fabricating a REJECT on the real layer + falsely closing it) -> added out_path so tests write to tmp; restored the honest INSUFFICIENT_DATA; verified the reconciler does NOT mark it closed (data-blocked, not gated). (2) tennis "ESPN set-by-set" = covered-in-substance by the already-recorded tennis_setdetail REJECT (abundant 30k/11k-row setdetail corpus, set-level serve/return signal rejected); the finder honestly KEEPS it visible (no over-close) rather than force-closing without a fresh run. So the 2 "untested" items are honestly resolved: MLB box = data-blocked-expected-REJECT (network materialization to gate), tennis = re-expresses the rejected setdetail. EXPOSE/SURFACE remain DELIBERATELY un-done: they touch the SERVED registry/provenance and must be calibration-gated first -- refused to force an ungated number into serving (no-edge/honest-serving rails). 3 local commits (1326fc0f p0+reconcile, e4e61dd5 mlb-box-gate, 99a116cb honesty-fix); never pushed. 22 per-file tests green across the session. No $/flag/registry/sentinel; nothing force-fed.

## >>> SESSION 2026-06-21 (funnel-completeness reconcile) -- backlog now honestly reflects near-complete data coverage
FINDING: the improvement_finder re-listed already-TESTED data layers as UNTESTED because its closed-detection read ONLY reject_ledger.jsonl, never the recorded gate verdicts in data/frontend/funnel/*.json (and 2 scattered ones: defender_matchup_gate.json, ingame/pitch_layer_gate_mlb.json). So a near-complete funnel looked unfinished. FIX (measurement-only, safe paths): NEW scripts/platformkit/meta/funnel_verdict_reconcile.py -- folds the 13 recorded funnel GATE verdicts into reject_ledger (source 'funnel_gate', clean FILENAME-derived slugs, idempotent) AND exposes a PRECISE per-sport closed index (>=5-char distinctive-slug substring, sport-scoped, curated aliases e.g. redcard->reddiff; explicit _EXTRA_SOURCES map for sport-less filenames). verdict_index reads BOTH the ledger AND the live funnel files (read-only) so the finder is correct without depending on a prior write. Wired into improvement_finder scan_untested_and_priors (TYPE A) + scan_tier3 additively (old _is_closed/load_verdicted_signals API + tests UNCHANGED); added a "resolved gap" skip to the EXPOSE scanner. Added reject_ledger source 'funnel_gate'. Marked the STALE tennis-markets EXPOSE row RESOLVED in COVERAGE.md (registry verified to expose all 9 markets). RESULT: backlog UNTESTED_DATA 10->2, TIER3 3->1, EXPOSE 3->2, total candidates 71->61. The 9 already-tested layers (nba travel/possession, mlb atbat/statcast/pitch_states, soccer redcard/espn28, nba defender_matchup, tennis setdetail) are now correctly recognized as CLOSED. GENUINELY-REMAINING data/market work (honest short list): UNTESTED MLB ESPN-box + tennis ESPN set-by-set (both REJECT-leaning); EXPOSE MLB/NBA orphan prop pricers (gate into registry); SURFACE MLB SP-form / NBA quarter-shape / soccer xG-proxy as scouting-provenance (NEVER features); +25 missing-test +25 LOC chores. 19 per-file tests GREEN (funnel_verdict_reconcile 4, improvement_finder 7, reject_ledger 8). No $/flag/registry/edge; nothing force-fed; honest under-close (an unsure row stays OPEN). => the funnel honestly reflects that data coverage is near-complete; "use all available data" = every data point has a recorded verdict, and now the tracking shows it.

## >>> SESSION 2026-06-21 (p0-resolution + autostart armed) -- served in-game number now carries the proven prior across provider-id mismatches
- AUTOSTART ARMED: register_autostart.ps1 -Register run by human in an elevated shell -> Scheduled Task NBA-AI-Stack-Autostart (AtLogOn -> watchdog -> boot.ps1) Status=Ready. Boot-on-login is LIVE; real-money stays default-DENY. Also patched register_autostart.ps1 to THROW (with an elevation hint) instead of printing a false "Registered" line after a failed Register-ScheduledTask (the old script printed success even on Access-denied).
- LEVER #1 HARDENED (p0_source=PRIOR coverage): the live ESPN feed and the producer snapshot key some games under DIFFERENT provider id namespaces (ESPN 401... vs a 10-digit fallback) -> live_p0's exact-id match silently degraded to BASE_FALLBACK, dropping the one PROVEN in-game beat (the pregame prior). Added scripts/platformkit/ingame/live_p0.live_p0_resolved (exact-id FIRST, else a UNIQUE leak-free team match -> source PRIOR_TEAMS; a doubleheader / >1-match / wrong-teams -> BASE, NEVER guesses). Wired ingame_live_state (production path passes live team displayNames; the injected-p0_provider test path is byte-identical) + inplay_capture_loop trade-gate now honors PRIOR_TEAMS as prior-justified. LEAK-FREE (only pregame team names + pregame_probs read; no score/close/outcome), unique-or-BASE safe, non-gated paths only (scripts/platformkit/ingame/**), no $/flag/registry. VERIFIED on the REAL on-disk snapshot: mlb Cincinnati Reds@New York Yankees with a mismatched id went BASE_FALLBACK -> PRIOR_TEAMS p0=0.7028; 14 + 5 per-file tests GREEN (test_live_p0, test_inplay_capture_loop). ACTIVATES LIVE on the next stack restart (autostart applies it on next login). Does NOT create an edge -- it makes the served in-game number carry the proven prior for MORE games, a necessary input to the in-game CLV frontier (still UNPROVEN until many in-season games settle).

## >>> SESSION 2026-06-21 (later) -- re-verify-clean + CLV crossed min-n floor + 1 honesty fix landed live
Re-verified the already-DONE system end-to-end (no regression since the FINAL REVIEW below) + ran a fresh 3-auditor adversarial review (CLV/bankroll honesty, served-product honesty, supervisor robustness) -> 2 CLEAN, 1 finding fixed.
- LIVE STATE re-confirmed: `.\go.ps1` 18/18 READY 0 restarts; 8 pages (/ /today /bets /paper-trading /records /live /models /system) all 200; autonomy overall=ok, loop liveness ok, honest idle ("no newly-settled games to fold; waiting on slate"), all 4 sports last_decision=NO_CANDIDATE; served board 1653 cards ALL edge_claimed=false, no $-value keys (only "UNITS not $" disclaimers); paper bankroll honest 97.87u net -2.13u over 5 days, today 1 MLB bet placed executed=false. m10 had 1 benign one-time restart (transient daemon exit 05:03, recovered, far under 5/300s breaker). Served-product + supervisor audits = CLEAN, no regression vs the FINAL REVIEW.
- CLV CROSSED THE MIN-N FLOOR (honest, NOT a proven edge): grade_summary.json now n_settled=13, n_clv=12 (>= MIN_CLV_N=8), mean_clv_pct=+1.023%, pct_beat_close=23.08% (3/13), all MLB, flat 4W-9L. This is a tiny single-sport single-corpus sample -> vs_close stays UNPROVEN, edge_claimed=false; NOT validated (no replication / cross-direction / clustered-DM; n=13 well within noise). It is real accrual on the money yardstick = the frontier doing its job; needs many more settled days + the autostart gate to mean anything.
- HONESTY FIX LANDED + LIVE (safe dirs only): the CLV-mean writers computed mean_clv_pct + n_clv over ALL settled rows INCLUDING the 1 proxy-close row (clv_pct=+4.41%), inflating the headline to +1.28% even though every docstring labels n_clv the "true-close sample". Fixed in 4 writers (scripts/platformkit/pm_trading/scoreboard.py, scripts/platformkit/paper/{paper_today,pnl_series,bankroll_daemon}.py): the CLV mean + n_clv are now over TRUE-CLOSE rows only (proxy excluded, still surfaced as n_proxy_close + counted in n_settled); n_clv == n_true_close. Honest served headline dropped +1.28% -> +1.023% (n_clv 13 -> 12). The real-money gate already excluded proxy (realmoney_gate.py) so gate safety unchanged. Added test_proxy_close_excluded_from_mean + assertions; per-file tests GREEN (scoreboard 47, paper_today, pnl_series, bankroll_daemon 11, realmoney_gate 10). Landed via ONE clean go.ps1 restart (cleared pyc, stop, start) -> 18/18 0 restarts, daemon regenerated grade_summary with the honest value.
- HUMAN GATES (Claude must NOT flip; status re-checked this session):
  (1) GIT CONFLICT -- OPEN. 6 TRACKED files carry baked-in `<<<<<<<`/`>>>>>>>` markers (NOT mid-merge, no MERGE_HEAD; branch fullsend-ingame-pregame-execution is 396 ahead of origin): api/_courtvision_odds.py, api/templates/_bet_card.html, api/templates/_bet_card_reasoning.html, scripts/backfill_cv_features.py, src/pipeline/tracking_feature_extractor.py, src/prediction/prop_pergame.py. 4/6 are human-gated (api/src). Claude must NOT auto-resolve (can't safely guess merge intent in production code) + NEVER push. Human resolves each, targeted `git add`, local commit only.
  (2) AUTOSTART -- OPEN (two human steps). The UI-env wrapper is NOT applied (watchdog_autostart.ps1 has no NBA_AI_UI_DIR) -> a bare `register_autostart.ps1 -Register` would autostart-serve the OLD court-visions dev UI, not prod webapp (chain logon->watchdog->boot.ps1 bypasses go.ps1 which sets NBA_AI_UI_DIR=webapp). Human: apply docs/research/organization-sprint/PROPOSED-autostart-ui-env-wrapper.md (edits human-gated repo-root launch scripts) THEN `.\register_autostart.ps1 -Register` (dry-run preview verified: AtLogOn task -> watchdog -> boot.ps1, M10 preflight first, real-money stays DENY). Reversible via -Unregister.
  (3) SELF-IMPROVE -- ALREADY ARMED 2026-06-19 (was stale-listed as OFF). data/cache/improve/PIPELINE_ENABLED PRESENT (content: "self-improve ARMED by human (go.ps1) on 2026-06-19. Scope: PAPER/UNITS only. No real money."). Loop runs do-no-harm: ratchet state IDLE, n_ships=0, shipped_version=None (NOTHING injected into served predictions) -- honest NO_CANDIDATE/no_new_games cold-start; improve_ledger "SHIP" rows are calibration READOUTS (bss_vs_close NEGATIVE -0.11, "calibration != edge" disclaimer), not served changes. The only remaining sliver of the FULL serving closed-loop = the PROPOSED-unapplied status_routes.router mount in human-gated frontend/serve.py (propose-not-edit).
  (4) REAL-MONEY -- stays default-DENY until CLV is replicated-positive (currently +1.02% on n=13 single-corpus = UNPROVEN).

## >>> FINAL REVIEW COMPLETE 2026-06-21 -- verified-working, honest, robust, INDEPENDENT; brought to a clean end
6-dimension adversarial audit (docs/research/organization-sprint/FINAL_REVIEW_*.md + FINAL_ASSESSMENT.md) + fix wave + final restart + chaos test. HONESTY VERIFIED CLEAN (no $-leak/edge/retracted; a buyer couldn't find a fabricated edge). SELF-IMPROVE+BRAIN+FUNNEL VERIFIED (gate trustworthy/planted-nulls reject, brain 872/873 reachable person-free, funnel coherent). LINES+PROPS SOLID. ROBUSTNESS VERIFIED (self-heal chaos-tested, rollover clean, disk 7-day retention). FIXES: (CRITICAL) equity curve double-counted two-way markets -> one-position-per-market in curve+placement; honest -2.13u/52 (was a flattering -0.80u/71), reconciles truly. (P0) board card game_id populated -> detail links resolve (90/681; rest honestly disabled). (P0) /models crash fixed (flat-ledger + per-panel boundaries). store.py history atomic; prop_cards_cache future-ts fail-closed; props date->ET; self-improve transient-cursor. FINAL VERIFY: 18/18 0 restarts; chaos kill m10 -> self-heal 27s + 18/18 isolation; 8 pages 200; board 681 props edge_claimed=false; bankroll honest CLV INSUFFICIENT(n_clv<8); autonomy green; no $-keys; a bet settled overnight w/ NO Claude. Overnight soak = 5 green ticks, all rails held. HUMAN-GATED (Claude must not): pre-existing git conflict (api/src markers -> no commit/push), autostart -Register + UI-env wrapper, real-money DENY, self-improve serving-arm.

## >>> CAPSTONE 2026-06-20 -- AUTONOMOUS DAILY EXECUTION PRODUCT: lines+props+CLV+bankroll+self-improve, morning-ready, zero-flaws-verified
~30 Opus/Sonnet agents over many disjoint waves; VERIFIED LIVE on clean go.ps1 restart (18/18 READY, 0 restarts, self-heal proven: killed m13 -> auto-restart 33s, other 17 stayed up). Rails held: units-only no-$, real-money DENY, stale-never-green, leak-free, no flag flips, no data/registry, human-gated (src/api/kernel/team_system/intel) untouched, NO origin push.
- LIVE MULTI-SPORT IN-GAME: live_loop now covers mlb/soccer/soccer_intl/nba/tennis (was nba-only); n_live=5 repriced live; MLB/soccer honest pregame_carry (reprice=False/is_carry flags), NBA full blend.
- LINES: Pinnacle parses ML+TOTAL+SPREAD (was ML-only); per-card books[] line-shopping; orientation-flip bug fixed. 2 keyless sharp books (ESPN/DK + Pinnacle); honest ceiling. line_history fills thousands/day autonomously.
- PROPS: perf-fixed (build_prop_cards hung >240s inline -> m13 caches, m10 reads 0.5s); market_type=prop unified into the one board; edge_claimed=false on ALL (was true on priced); MODEL-ONLY props now SYNTHESIZE from on-disk gamelogs when PrizePicks 429s (board never empty); every tick writes fresh-stamped cache (no stale-omit).
- EVERY MARKET = distinct card: totals/spreads emit as own cards (edge_api line-merge + exec_decision info_bets + bestbets_compute); model-only line-only cards honest.
- BANKROLL + UNITS P&L: NEW m1_bankroll SUPERVISED daemon (now 18 specs) writes paper_pnl_series.json (per_day+daily unified schema, single writer) + paper_bankroll.json + paper_today.json daily w/ UTC rollover. FIXED CRITICAL BUG: placed bets were staked in DOLLARS mislabeled as units (stake 382/500) -> now flat 1u, curve RECONCILES to actual placed bets = +0.38u over 59 settled (was a -29u dollar-contaminated artifact). would_pass_real_gate=False. "how much you'd have made" = honest paper units curve, CLV yardstick, edge_claimed=false.
- CLV NOW RESOLVES (was a MATCHING BUG not coverage): every ledger row carries only "Away@Home" matchup but grade_paper_close read empty home/away -> no_close on ALL 58 settled. FIXED (recover teams from matchup; date from explicit game_date not write-ts; sign verified better-than-close=positive). 61/76 open bets have a matchable close (both signs -7.6%..+38.3%, 31% beat); live grade resolved CLV. ACCRUES AUTONOMOUSLY. Old 58 stay null (append-only). Needs autostart + N settled-days for a meaningful sample; vs_close UNPROVEN until replicated.
- SELF-IMPROVE un-stuck (was INERT from wiring bugs, NOT efficiency): state reconstruction (settled_states.py p0+outcome leak-free) -> loop now BUILDS a candidate + RUNS the 5-gate ratchet daily; planted-null still REJECTS (gate honest); cursor advances; CLV 2nd corpus wired; serving closed-loop adapter BUILT do-no-harm but INACTIVE-pending-human. Honest expected daily outcome = REJECT (market efficiency) = success.
- STALE-PURGE / DAILY REFRESH: producer drops post/final/past-tipoff games (NBA finished-Finals row gone; nba envelope demotes ok->stale); day-rollover purges all surfaces (verified by injecting tomorrow: NBA/MLB/soccer_intl -> honest unavailable, future games kept). Snapshot TTL guard on legacy :8098.
- FRONT END: TODAY digest (HomeTodayCard + /today route) = today's best bets + PLACED (staked) + yesterday W-L + day P&L + bankroll equity sparkline + self-improve decision; card-click -> /bets/[sport]/[gameId] shows player props + ALL markets + execution trail + what-was-actually-staked; money headline ("how much you'd have made", units); honesty fixes ("avg model edge"->divergence neutral, stale-green pills gated on freshness, small-N win-rate guarded). npm build green, 8/8 pages 200, no $-leak.
- SUPERVISOR ROBUSTNESS (zero-flaws): supervisor self-heartbeat (m9_supervisor.txt) + watchdog detects WEDGED supervisor (was process-exists-only); boot stall 600s->15s; m5 health-monitor tick guarded; -Stop reaps ALL daemons+next-server (was missing 6); crash-rate breaker (>5/300s->DEGRADED); next-server restart pattern + tree-kill; PID-reuse cmdline check; wmic->CIM; atomic writes (io_atomic.py) on 4 torn-write paths.
- OBSIDIAN BRAIN AT PEAK: 872 notes, 100% reachable, 1 component, person-free, 3 self-policing gates green (brain_pipeline + brain_graph_finalize).
- LEGACY FOOT-GUN RETIRED: removed 3 hardcoded 2026-05-26 slate daemon entries from daemon_registry.json; auto_place_daemon default -> dynamic today.
- KNOWN/HUMAN-GATED REMAINING (Claude must NOT do): (1) repo is in a PRE-EXISTING unresolved git-conflict state (9 files incl human-gated api/src have <<<<<<< markers) -> a commit is BLOCKED/unsafe until a human resolves it; NEVER push to public origin (leaks data/vault/ledgers). (2) arm autostart (register_autostart -Register) for true boot-on-login + the PROPOSED watchdog UI-env wrapper (docs/research/organization-sprint/PROPOSED-autostart-ui-env-wrapper.md) so autostart serves webapp not court-visions. (3) flip real-money (stays default-DENY). (4) arm the self-improve serving closed-loop. (5) external prop-line 429 + offseason mean some surfaces are honestly-sparse, not broken.

## >>> CAPSTONE 2026-06-19 -- INDEPENDENT + RUNNING + SELF-HEALING + HONEST + FRONT-END-SHOWS-IT = DONE
DEFINITION OF DONE: MET. INDEPENDENT (one-command supervised boot owns the whole stack), RUNNING (12/12 READY in ~18s; producer + cadence wrote real artifacts), SELF-HEALING (kill-matrix -> honest degrade -> auto-restart -> isolation; clean stop leaves 0 procs), HONEST (all_honest=TRUE, 0 $, 0 retracted numbers, frontend truth 6/6, parity GREEN 5 adapters), FRONT END SHOWS IT (read-only Next route + LiveIndicator: green ONLY when fresh<=120s AND all_ready AND no DEGRADED; honest INERT/DENY/INSUFFICIENT_DATA badges).
- PROOFS (docs/research/organization-sprint/): BOOT_VERIFY_PROOF.md (bounded boot all_ready=TRUE 12/12, m8 cadence row, in-play honestly IDLE), CHAOS_VERIFY_PROOF.md (kill-matrix m8/m2_inplay/line/api -> never green-on-dead -> restarted -> isolation; -Stop alone = 0 procs), HONESTY_REAUDIT_PROOF.md (honesty_linter PASS, 0 $, frontend 6/6, parity GREEN).
- NEW supervised measurement-only services: m8_ci_cadence (HB m8_ci_cadence.txt fresh 7800) + m2_inplay_capture (HB m2_inplay_capture.txt fresh 300) via scripts/platformkit/progress/ci_cadence_runner.py + ingame/inplay_capture_runner.py; m4_selfimprove INERT (sentinel absent). boot.ps1 $STOP_PATTERN patched (autonomy/ci_cadence/inplay_capture) so -Stop fully cleans.
- CAPSTONE DOCS: docs/research/organization-sprint/FINAL_STATE.md (done-state + invariants + DOD-MET) + RUNBOOK.md (one-command VIEW/START/STOP + the 3 HUMAN-ONLY gate flips). VIEW: view_local.ps1 at repo root (read-only UI :3000 + boards :8098; no daemons/flags/real-money).
- KNOWN MINOR (benign): governance concurrency flags 3 dup append-only ledger keys from this session's repeated bounded boots -- measurement residue, NOT an honesty/integrity issue, no $.
- 3 HUMAN GATES remain OFF (the ONLY remaining human steps; system never flips them): (1) create data/cache/improve/PIPELINE_ENABLED; (2) register_autostart.ps1 -Register; (3) flip real-money from default-DENY.

## IN-GAME DAY-TRADER FRONTIER (2026-06-19, user directive: in-game is where the edge + trades are; push to peak + execute high-level + measure CLV)
THE FRONTIER. Pregame is efficient (every data tier rejected); the ONE proven measured edge is IN-GAME prior-conditioning CALIBRATION (NBA fine-res DM p=1.2e-12, 3/4 sports). The in-game MARKET/CLV beat is UNPROVEN -> now MEASURABLE on the unblocked liquid keyless Kalshi in-play path.
- IN-GAME PEAK PUSH DONE + VERIFIED INGAME_PUSHED_HONEST 2026-06-19 (workflow wf_050d6a5e-8ae). Plan .planning/product/ingame/INGAME_PEAK_PLAN.md. (1) DAY-TRADER EXECUTION LOOP BUILT (scripts/platformkit/ingame/inplay_edge_signal.py + inplay_daytrader.py + tests): devig liquid Kalshi YES-home/away via Shin -> edge=model_inplay_prob - devigged_price (HOME-aligned); 4-way entry gate (calibration-justified AND is_liquid AND is_fresh AND clears pm_trading.policy tier floor A.08/B.04/C.02 +.01 proxy) -> no_bet w/ reason; ENTER->capped quarter-Kelly UNITS->paper-place (paper_ingame, executed=False, idempotent); HOLD captures; passive EXIT; CLV via inplay_aggregate_grade (clustered two-arm, BEAT needs n>=5 games + >=40 ticks + both arms). 14 new + 20 existing tests PASS. LEAK-FREE (series[:i+1], close held out), PAPER-ONLY, NO $, real-money DENY. (2) LIVE MEASUREMENT on a REAL game (MLB DET@CHW, real liquid KXMLBGAME pair 1c spread vol>1.3M): model P(DET) 0.716->0.739 vs devigged market ~0.77->0.79 => edge ~-0.05 (model ~5pp BELOW market on the home favorite) => 0 paper bets (floor correctly never cleared -- system did NOT force a trade). CLV=INSUFFICIENT_DATA (n=1, 3 ticks). (3) EDGE-HUNT: ALL candidate in-game edges REJECT (prior_x_margin/time, run_x_prior, leverage, leverage_x_prior, pitch-fatigue, red-card, xG) -- each beat ONE direction with the other MATCHing over a skillful base, null rejects = textbook single-fold artifact; base+prior already price margin/time/score; NO survivor. Verdicts data/frontend/funnel/mlb_{priorinteract,leverage}_ingame_edge.json.
  - KEY PEAK BUG + 3 WIRING GAPS to run unattended + reach a real CLV verdict (all non-gated scripts/platformkit/ingame + odds_provider): (P1, #1) ingame_live_state.live_state returns p0=None -> the SERVED in-game number silently FALLS BACK TO BASE, LOSING the only proven beat -> wire a live-p0 provider so the served/paired model_prob carries the PROVEN pregame prior. (W2) inplay_kalshi.fetch_inplay filters event_ticker by _SERIES_HINT (KXMLB/KXEPL) under an unfiltered limit=200 page -> per-GAME markets (KXMLBGAME/KXWCGAME) never page in -> query series_ticker=KX<league>GAME. (W3) add a FINAL settled-outcome stamp (binary home_win on ESPN FINAL) so the aggregate OUTCOME arm can fire (else pool stays INSUFFICIENT forever). (W4) build inplay_capture_loop.py thin live daemon (reuse live_loop cadence/error-isolation) to pair predict_live w/ the liquid devigged price + capture_pair_once unattended. P3 event-driven reprice + P2 fine-grid blend = plan-only serving improvements next.
- IN-GAME WIRING COMPLETION DONE + VERIFIED INGAME_ACCRUAL_READY 2026-06-19 (workflow wf_7fb08047-c53; all checks TRUE). P1 scripts/platformkit/ingame/live_p0.py: serves the PROVEN pregame prior into the in-game number (reads pregame_probs.home_ml from data/frontend/predict_service/<sport>/latest.json, READ-only, leak-free; p0_source label PRIOR/BASE_FALLBACK/CALLER; ingame_live_state auto-supplies it) -> THE PEAK FIX (the served number no longer silently falls back to BASE losing the proven beat); 8 tests. W3 settle_stamp.py: stamps binary home_win at ESPN FINAL (idempotent, label-only, leak-free) -> UNBLOCKS the aggregate OUTCOME arm (was INSUFFICIENT forever); 6 tests. W2 odds_provider/inplay_kalshi.py: fetch_inplay now queries series_ticker=KX<league>GAME (KXMLBGAME/KXWCGAME/KXEPLGAME/KXNBAGAME) -> per-game liquid markets page in (is_liquid/stale-never-green preserved); 8 tests. W4 inplay_capture_loop.py: thin MEASUREMENT-ONLY live daemon (reuse live_loop cadence/error-isolation/heartbeat) per-game-per-tick pairs predict_live(+live-p0) with the liquid devigged price via capture_pair_once -> day-trader on_tick (paper units) -> settle_stamp on FINAL; 5 tests; supervisor wiring PROPOSED-only (frozen registry untouched). BOUNDED LIVE RE-MEASURE: W2 verified LIVE (63 MLB + 125 WC liquid KX*GAME legs paged in!); ran on live KXMLBGAME DET@CHW -> model_prob 0.8053 (Elo-anchored) vs devigged 0.565 edge +0.240 BUT p0_source=BASE_FALLBACK (no on-disk pregame snapshot for that gid) -> 0 paper bets (prior-justified gate correctly withheld); CLV INSUFFICIENT_DATA (4 games/1 settled). Daemon STOPPED 0 procs. PAPER-only, UNITS, NO $, real-money DENY, no flag/no autostart, leak-free. accrual_ready=TRUE. TWO operational gates to reach a real CLV verdict (both human/by-design): (a) ARM autostart (PROPOSED docs/research/organization-sprint/PROPOSED-inplay-capture-loop-supervisor-wiring.md); (b) produce pregame snapshots per live game_id so served ticks carry p0_source=PRIOR. Then >=5 settled games -> a truthful BEAT/MATCH/BEHIND; real-money stays DENY until positive. => THE IN-GAME DAY-TRADER IS COMPLETE + ACCRUAL-READY; the proven prior is now served; the edge (if any) shows up as CLV across many games.
- W5 FINAL CAPSTONE IN FLIGHT -- end-to-end integration smoke (bounded boot API+webapp -> every page serves real data + launcher + cadence + in-game capture, honest, then STOP) + a WHOLE-PRODUCT honesty/correctness RE-AUDIT (parallel read-only over deep-data/gates + execution/in-game/CLV + autonomy/cadence + product/UI/API surfaces: leaks / fabricated wins / stale-green / $-fields / gate-crossings / over-claims / regressions) -> the honest 'final product + handoff ready' verdict + consolidated memory.


## DEEP-DATA + LIVE-ACQUISITION THRUST IN FLIGHT (2026-06-19, lean Opus dispatcher + Workflow fan-outs)
Driving Phases A->E of .planning/product/DEEP_DATA_THRUST_PROMPT.md. Parity GREEN (4/4 + soccer_intl) re-confirmed 06-19. Rails held: calibration not edge, units-only no-$, no flag flips, no data/registry writes, no origin push, human-gated dirs untouched.
- PHASE A (deep-data inventory + live-feed map): DONE 2026-06-19 (workflow wf_67d81694-1b3, 4 read-only sport agents + Opus synth). Outputs .planning/product/deepdata/{basketball_nba,mlb,soccer,tennis}.md + PRIORITIZED.md. FINDINGS: genuine untapped on-disk depth EXISTS in 2 places; the other 2 sports are honestly near on-disk ceiling.
  - RANK 1 MLB per-PITCH tier: pitchType/pitchVelocity/pitchCoordinate + pre-pitch count/runners/outs ride the SAME keyless ESPN plays[] payload already fetched but DROPPED at ingest. Finest grid below the at-bat layer; structurally-new lever = within-start SP velocity-decline/fatigue (half-inning grid can't see it). -> build domains/mlb/ingest_pitch_states.py. Expected REJECT-leaning for win-prob; real value maybe in SP-fatigue + K/BB prop priors. Full Statcast (exit velo/spin/xwOBA) = Tier-3 network.
  - RANK 2 NBA defender-matchups: data/defender_matchups_*.parquet (37,395 rows, clean def_player_id) = cleanest ZERO-NETWORK on-disk depth. -> build domains/basketball_nba/ingest_defender_matchup_states.py, gate via combo_gate. (NBA CV frame tier (401 games) is IDENTITY-WALLED by the jersey-OCR noise wall ~2.3% -- a CV research problem, not a download; team/possession aggregates ARE the coarse micro-state that already rejected.)
  - SOCCER shot-location: per-shot fieldPositionX/Y on the already-fetched ESPN commentary wire (18/18 populated; goalPositionX/Y all-ZERO, unusable) = NETWORK tier (zero-incremental). -> ingest_shotxg_states.py (location-based xG-proxy). football-data match-summary substrate AT CEILING. vendor xG/pass-networks/PPDA/set-pieces = Tier-3.
  - TENNIS: honestly AT on-disk ceiling. Sackmann match CSV ~fully mined; per-set/tiebreak layer (ingest_setdetail_states.py) is the last zero-network step, expected to CONFIRM the ceiling (re-expresses serve/return). True point-by-point/shot/rally/pressure = Tier-3 (separate GitHub repos NOT on disk).
- PHASE B (build top layers + per-file test + gate + adversarial verify): DONE 2026-06-19 (workflow wf_79a1e9b8-8b8). RESULT = 0 SHIP / 3 REJECT (all verified honest, negative-controls solid) / 1 INSUFFICIENT_DATA. All modules built leak-free + per-file tested + proposal-only (no data/registry, no flag, no $).
  - NBA defender_matchup_states -> REJECT (verified): as-of def FG%-allowed is slightly WORSE than the constant base-rate BOTH dirs (A->B delta -0.0032 DM p=0; B->A -0.0042 p=7e-6; beats=False). Pearson(as-of,realized)=0.0325 -- almost no signal. Planted-null rejects harder (gate provably fails noise). Files domains/basketball_nba/ingest_defender_matchup_states.py + scripts/platformkit/defender_matchup_gate_run.py.
  - soccer shotxg_states (location xG-proxy) -> REJECT (verified): both-dir Brier delta exactly 0.0, TRAIN coef shrank to 0.0, replicated across 2 league-pairs (eng_ger 669g / esp_ita 753g); base skillful (BSS 0.25-0.27, non-degenerate); feature genuinely varies (91.6% nonzero, not 0-filled). Files domains/soccer/ingest_shotxg_states.py + scripts/platformkit/ingame/.
  - tennis setdetail_states -> REJECT (verified): no additive diff with DM p<0.05 in BOTH corpora (best sets_dropped_rate ATP p=0.049 but WTA wrong-sign p=0.143 = PARTIAL = artifact); planted-null rejects; base non-degenerate. CONFIRMS the tennis on-disk ceiling. domains/tennis/ingest_setdetail_states.py.
  - MLB ingest_pitch_states (within-start SP velo-decline/fatigue + pre-pitch count) -> INSUFFICIENT_DATA: module BUILT + leak-free + per-file tested + MACHINERY PROVEN (planted-null rejects DM p=0.40; a true injected signal SHIPS brier_delta +0.063 DM p=0 on the SAME gate -> can both fail noise and pass real signal). BUT no mlb_pitch_states__*.parquet on disk (only half-inning mlb_states__2021-24 + at-bat mlb_atbat_states__2022-23). To gate for real: materialize >=2 seasons via the keyless ESPN feed ('python -m domains.mlb.ingest_pitch_states --season 2022/2023') then re-run scripts/platformkit/ingame/ingame_pitch_layer_gate_mlb.py. (Minor: module docstring CLI uses bare-path form that ModuleNotFoundErrors; use -m form.)
  - META: reinforces the 4-sport meta-finding -- coarse/shallow additive layers REJECT; the ONLY genuinely-new resolution tier (MLB per-pitch within-start fatigue) is the one not yet truly tested. NEXT = materialize per-pitch + gate it for real.
- PHASE B+ (materialize MLB per-pitch + gate for real): DONE + VERIFIED 2026-06-19 (workflow wf_6e610570-749). RESULT = REAL, TRUSTWORTHY REJECT -- THE DECISIVE DEEP-DATA FINDING. Historical per-pitch IS available (not live-only): materialized data/cache/ingame/mlb_pitch_states__{2022,2023}.parquet = 199g/66,266 + 200g/69,823 pitch rows, REAL non-null fractions (count 1.0, loc ~0.98, velo/type ~0.81, velo_decline ~0.80 NaN only on each side's first pitches by leak-free construction). Gate on 2 INDEPENDENT corpora: full layer (fatigue+count) beats base = FALSE both dirs (A->B delta -0.00011 DM p=0.32; B->A ~0 p=0.11). ISOLATED within-start SP velo-decline lever (count beta forced 0) = NO held-out signal (A->B delta -0.00001 DM p=0.892; B->A p=0.111). Planted-null REJECTS through the identical gate -> trustworthy; base genuinely skillful (cross-corpus BSS +0.368/+0.461, not degenerate); 24 per-file tests pass; no $, vs_close UNPROVEN, proposal-only, no flag/registry. Adversarially re-verified independently (parquets real, outcome label never a feature, prior-N only, beta TRAIN-only).
  - => META-FINDING COMPLETE: even at the FINEST available resolution (per-pitch), in-game state carries NO held-out calibration signal beyond (run_diff, frac_elapsed) for MLB win-prob. Deeper data, tested honestly at the deepest grid, REJECTS. This is the honest PLATEAU. The materialized per-pitch corpus's only plausible remaining value is PROP priors (K/BB pitch-outcome sub-models), NOT game calibration -- untested, separate hypothesis.
  - MINOR must-fix (applied): clamp impossible pre-pitch counts (ESPN payload edge cases gave count_balls=4/strikes=3 on a small fraction) so the on-disk per-pitch corpus stays clean for any future prop use. CLI docstrings fixed to -m form. Optional follow-up (not done): persist the isolated-lever result into pitch_layer_gate_mlb.json.
- PHASE C/D foundation (live recon + real in-play series capture): DONE 2026-06-19 (workflow wf_d03855c9-95e). BREAKTHROUGH -- the in-play LIQUIDITY blocker is UNBLOCKED: found a genuinely LIQUID keyless Kalshi in-play market live this minute (MLB CHC@TOR, KXMLBGAME-26JUN191420TORCHC, 1-cent spread, volume_fp ~2.87M, real bid/ask/size depth, is_liquid=True). Captured a REAL intra-game series (10 ticks, 4 HOME-aligned model/market pairs) end-to-end through snapshot_daemon->live_grade->inplay_clv_replay; grade verdict = INSUFFICIENT_DATA (n_scored=3<8), edge_claimed=false, freshness sidecar advanced -- the unattended path is GREEN end-to-end with no code change. CORE IS CLEAN (adversarial verify): leak-free replay, no $ field, single-game=INSUFFICIENT_DATA, Kalshi is_liquid gate all correct.
  - BUT the same audit caught 2 RED honesty bugs in the CAPTURE FRONT-END (non-gated scripts/platformkit/odds_provider): (2a) STALE-GREEN -- daemon re-stamps a cached/frozen feed body as fresh now() (inplay_espn/inplay_feed use body-only disk_cache_get dropping fetched_at; _clean_tick stamps ts=now(); _write_freshness advances unconditionally) -> a frozen ESPN line (0.5455 for a 7-1 blowout) read green. (4b) LIQUIDITY-GATE BYPASS -- default capture is ESPN-first + ungated, captured/graded that depthless ESPN line as in-play instead of the liquid Kalshi book. Plus 1c (dup-ts collapse) + 2c (no upper-time-bound on explicit-live admission).
  - FIX LANDED + RE-VERIFIED CLEAN 2026-06-19 (workflow wf_bcb8bafd-0ad). 2a STALE-GREEN closed: frozen feed -> RED/DEGRADED (tick dropped, last_source_ts=None, sidecar ok=False); new scripts/platformkit/odds_provider/inplay_freshness.py (79 LOC) threads true fetched_at->source_ts via disk_cache_get_meta; freshness.py last_source_ts now authoritative. 4b LIQUIDITY-BYPASS closed: default_fetch is now Kalshi-PRIMARY (is_liquid-gated, tradeable=True); ESPN/PM tagged tradeable=False (DEGRADED corroborators); _clean_tick drops any tick not proven tradeable -- the depthless 0.5455 ESPN line is no longer persisted/graded as in-play. 2c frozen-live bounded on source_fresh; 1c live_grade model_by_pos (positional, same-second ticks keep distinct model_prob). 106 tests PASS (touched + regression). NO regression: leak-free replay + single-game=INSUFFICIENT_DATA + Kalshi is_liquid gate + no-$/edge_claimed=False all preserved. Re-verify overall=CLEAN. RESIDUALS (proposed follow-ups, not holes): a latent back-compat 'src is None->live' escape hatch (UNREACHABLE by real feeds since both stamp source_ts); hard-coded SOURCE_MAX_AGE_SEC=120s (moneyline cadence); and -- KEY -- the MULTI-GAME AGGREGATE grader is still unbuilt (single-game correctly stays INSUFFICIENT_DATA) = the Phase D capability gap.
- PHASE D (multi-game aggregate CLV grader + corpus feed): DONE + VERIFIED CLEAN 2026-06-19 (workflow wf_38cc7da7-43e). NEW scripts/platformkit/ingame/inplay_aggregate_grade.py (289 LOC): pools per-game CLV with a GAME-resampling bootstrap CI + clustered-DM-by-game on per-state OUTCOME-Brier; BEAT requires n_games>=5 AND ticks>=40 AND settled-outcome arm AND (pooled mean CLV CI-lower>0) AND (DM mean_diff>0, p<0.05); BEHIND if mean_clv<-eps; else MATCH; too-few -> INSUFFICIENT_DATA. A market-copy/shrink pool -> mean_clv~0 + d~0 -> structurally cannot clear arm B -> MATCH (the +18.38% market-follow trap is IMPOSSIBLE by construction). Reuses leak-free live_grade.grade_game->inplay_clv_replay + verified eval_gate.dm_test.diebold_mariano. NEW adapter aggregate_clv_to_corpus.py (184 LOC): reshapes per-game grades into clv_corpus's settled shape (final captured tick EXCLUDED from scored states, is_true_close=True) then calls EXISTING build_close_corpus/inject_corpus VERBATIM -> P1 PRESERVED (market-copy -> d==0 -> corpus None) + loop INERT sentinel-absent. /api/clv/over-time on frontend/status_routes.py (measurement-only, units=probability, edge_claimed=False, NO $). 9 tests PASS; REAL data = INSUFFICIENT_DATA (n_games=1), no fabricated beat. PROPOSED human diff (NOT applied): mount status_routes.router in frontend/serve.py. proposal/measurement-only, flag-OFF, no registry/sentinel/flag.
- PHASE E (capstone honesty/leak re-audit + plateau verdict): DONE 2026-06-19 (workflow wf_d777d24d-9b5, 3 read-only auditors + Opus synth). DEEP-DATA GATES audit = CLEAN (all 4 REJECTs honest + leak-free verified ON DISK: 0 debut-row leaks, planted-nulls reject through the IDENTICAL path in all 4, gates provably CAN pass a signal, degenerate-base guards genuine, cross-corpus both-dir clustered-DM, vs_close held UNPROVEN, no $/flag/registry; mlb pitch parquet verified REAL 66,266/199 + 69,823/200). STALE-GREEN FIX audit = CLEAN for the real frozen-feed path (+1 LOW latent escape hatch). PLATEAU VERDICT CONFIRMED: deepest reachable on-disk in-game state REJECTS for win-prob calibration across all 4 sports under trustworthy gates -> the on-disk deep-data game-win-prob frontier is EXHAUSTED; recording these rejects IS the deliverable. Real remaining frontier = in-game prior-conditioning (proven) + the now-unblocked live CLV loop (UNPROVEN until many games).
  - BUT the audit CAUGHT 2 HIGH correctness bugs in the Phase-D aggregate grader (all_honest_status=FALSE until fixed -- on the vs_close YARDSTICK, must fix BEFORE any live accrual): (H1) inplay_aggregate_grade.py:156-175 _outcome_losses is labeled 'beat the close' (d=loss_close-loss_model) but actually grades model vs the CONTEMPORANEOUS per-tick market price over ALL pairs (close tick grades itself), diverging from the leak-free P1 keystone (close_p=pairs[-1] fixed once + excluded). Shrink-trap still holds (market-copy->d==0->no fire, NOT a fabricated-beat hole) but it answers a different question than its label. (H2) inplay_aggregate_grade.py:322 sets vs_close='BEAT' off a SINGLE pooled window with NO cross-corpus/both-dir replication -- weaker than maybe_vs_close_upgrade (gate_ship + n_rep>=2 + clustered-DM p<0.05); over-claims vs the UNPROVEN-until-replicated rail (blast radius CONTAINED: offline, router unmounted, INERT). +2 LOW (odds_provider source_ts-optional tradeable tick; single-game grade_game 'BEAT' label).
- PHASE E-FIX (vs_close yardstick correctness): DONE + RE-VERIFIED CLEAN 2026-06-19 (workflow wf_83da2d90-a8e). all_honest_now=TRUE restored. H1 closed: _outcome_losses now fixes close ONCE=pairs[-1].market_prob, iterates pairs[:-1] (close tick EXCLUDED, never grades itself) -- mirrors the P1 keystone; label matches math. H2 closed: vs_close hard-set 'UNPROVEN' regardless of pool_verdict; separate pool_verdict_label ('POOL_BEAT_SINGLE_WINDOW'); real upgrade ONLY via clv_corpus_inject.maybe_vs_close_upgrade (gate_ship + n_rep>=2 + clustered-DM p<0.05). L1 closed: a TRADEABLE in-play tick with source_ts=None is DROPPED (frozen-feed guard unavoidable; source_ts optional only for non-persisted tradeable=False corroborators). L2 closed: single-game raw 'BEAT' downgraded to 'PER_GAME_CLV_POSITIVE' (raw kept as raw_per_game_verdict, per_game=True); aggregate reserves 'BEAT' for the gated pool. shrink-trap + leak-free replay + INERT loop + no-$ all PRESERVED. 50 per-file tests PASS. RESIDUAL (non-blocking, pre-existing): scripts/platformkit/ingame/inplay_aggregate_grade.py = 374 physical LOC (~289 executable; overage is honesty-rail docstrings) -- slightly over the 300 guideline; trim is an optional follow-up.

## DEEP-DATA THRUST -- E-DEEPEN + TIER-3 PIVOT (2026-06-19)
- PHASE E-DEEPEN (per-pitch SP-fatigue as STRIKEOUT-PROP priors): DONE 2026-06-19 (workflow wf_ac85ef74-fd5) = honest INSUFFICIENT_DATA (NOT a forced result). Built leak-free per-outing fatigue PROFILE (prior-N shift1: velo_decline_slope, mean_early_velo, pitch_mix_entropy, frac_breaking) over the materialized pitch parquets (338/340 outings carry a profile) + a full prop gate (walk-forward OLS, Brier+pinball, clustered-DM both dirs, degenerate-base guard, cross-corpus, planted-null). BLOCKED on the LABEL: (1) NO_SEASON_OVERLAP -- the only on-disk per-pitcher K source player_gamelogs.parquet is 2026-ONLY (zero overlap with the 2022/2023 pitch corpora); (2) NO_SP_IDENTITY -- the ESPN pitch parquet has NO pitcher-id and no SP/reliever boundary, so a (game,side) block conflates starter+relievers. Label coverage = 0/678 outings. The gate machinery is REAL + unweakened (planted-null REJECTS, oracle PASSES, degenerate-base guard active on synthetic) -> runs unchanged the day an SP-isolated K source with season overlap lands. Files domains/mlb/ingest_sp_fatigue_prop_states.py (8 tests) + scripts/platformkit/gate_run_mlb_sp_fatigue_kprop.py (6 tests; 308 LOC, minor over-300). No $/flag/registry. => the honest signal to PIVOT to Tier-3 acquisition (the on-disk ceiling is real).
- PHASE E-TIER3 (Statcast keyless acquisition): DONE + VERIFIED 2026-06-19 (workflow wf_7466877b-7ef). ACQUISITION SUCCEEDED keyless: pybaseball ABSENT but the baseballsavant CSV export (statcast_search/csv, player_type=pitcher, type=details) works keyless -> acquired REAL Statcast 2022 (158,702 pitch-rows -> 1,082 starter-starts) + 2023 (154,552 -> 1,048), real pitcher_id + SP-isolated K label (relievers EXCLUDED, hand-verified on game 661362), leak-free, 2 INDEPENDENT corpora. Bounded (~3.7MB, 1 day/req, 4s delays, per-day cache; 1 transient 502 skipped). New domains/mlb/acquire_statcast_sample.py + statcast_sp_fatigue_adapter.py + scripts/platformkit/gate_run_statcast_sp_fatigue_kprop.py (+ tests). Data data/cache/statcast/{statcast,starter_table}__{2022,2023}.parquet. (A) SP-fatigue->K-PROP gate = REJECT (honest, verified): base skillful (BSS 0.032/0.027), profile single-direction opposite signs (2022 worse DM p=0.827; 2023 NS p=0.313), planted-null rejects -> trustworthy -> artifact -> REJECT. => THE PLATEAU EXTENDS EVEN TO ACQUIRED TIER-3 DATA (richest real pitcher-identity + SP-isolated K + within-start fatigue still doesn't beat a recent-K-rate base for strikeout props). (B) richer per-pitch WIN-PROB (spin/xwOBA/velo) = TIER3_BLOCKED (honest): the 18-col bounded pull omitted score cols AND had ZERO calendar overlap with the ESPN base -> can't join; refused to 0-fill. Closable via a small overlap re-acquisition (-> wave below). No $/flag/registry/sentinel.

## DEEP-DATA THRUST -- HONEST CLOSE-OUT (2026-06-19)
DONE-WHEN met truthfully. Phases A->E complete; all_honest_status=TRUE; parity GREEN; everything proposal-only + flag-OFF + real-money DENY; no $ field, no flag flip, no data/registry/ or sentinel write, no autostart, no origin push, no human-gated dir edited. The audit pattern caught + fixed a real bug on TWO passes (stale-green in capture; vs_close mislabel in the grader).
- DELIVERED: >=1 new leak-free deep-data layer per sport ingested + gate-run with an HONEST verdict recorded (NBA defender-matchup REJECT, soccer shot-xgloc REJECT, tennis set-detail REJECT, MLB per-pitch REJECT after REAL materialization incl. the within-start SP-fatigue lever). The system can actively acquire CURRENT-game data + REAL liquid in-play prices (Kalshi KX, is_liquid-gated) and degrades honestly (stale-never-green now enforced for the real frozen-feed path). A real live in-play series was captured + graded end-to-end (single game = INSUFFICIENT_DATA, edge_claimed=False). A multi-game aggregate CLV grader + clv_corpus feed is built with the shrink-trap structurally closed + the loop INERT.
- HONEST HEADLINE / PLATEAU: deeper data tested at the FINEST available on-disk grids across all 4 sports REJECTS for held-out win-prob calibration, under gates verified trustworthy (planted-nulls reject, bases skillful, leak-free confirmed on disk). The on-disk deep-data game-win-prob frontier is EXHAUSTED -- coarse->shallow->deepest in-game state adds nothing beyond the (margin/run_diff, time) base + pregame prior. Recording these rejects IS the deliverable.
- REAL REMAINING FRONTIER (honest): (1) in-game prior-conditioning = the proven decisive measured calibration edge (already shipped); (2) the now-UNBLOCKED live CLV measurement loop -- real liquid Kalshi prices + honest capture path + aggregate grader -- which needs MANY live games over DAYS via the HUMAN-GATED snapshot daemon to reach a vs-close verdict; vs_close stays UNPROVEN until cross-corpus both-direction clustered-DM replication. (3) Genuinely-new signal beyond the on-disk ceiling = Tier-3 ACQUISITION (Sackmann PBP/tennis, vendor xG/tracking/soccer, full Statcast/MLB, identity-walled NBA CV frame tier) -- a network/research investment, not a download. (4) Untested separate hypothesis: the materialized per-pitch corpus as PROP priors (K/BB), NOT game win-prob -- a distinct future gate run, expected-REJECT-leaning, recorded honestly.
- HUMAN GATES still OFF (unchanged): create data/cache/improve/PIPELINE_ENABLED (self-improve), arm autostart (live accrual), flip real-money. PROPOSED-but-UNAPPLIED diffs remain unapplied: mount status_routes.router in frontend/serve.py (/api/clv/over-time); the recalibrator/clv-corpus wiring; the legacy api/ :8098 $-leak fixes.

## FUNNEL COMPLETENESS THRUST IN FLIGHT (2026-06-19, user directive: every data point -> as many signals -> models -> engines -> ONE validated prediction; get smart; keep working)
Orthogonal axis to deep-data depth: make the funnel COMPLETE + COHERENT + VALIDATED (every data point TESTED, every gate-PASSING signal wired into the one prediction, every market = one coherent fused number w/ provenance). Honest framing: NOT an edge goal; gate-REJECTED signals stay VALIDATED SCOUTING, NEVER force-fed (force-feeding = market-follow + overfit -> wrecks calibration). vs_close UNPROVEN everywhere; no $.
- FUNNEL COVERAGE MAP: DONE 2026-06-19 (workflow wf_d1af98d7-adb, 4 read-only sport agents + Opus synth). FINDING: the funnel is MOSTLY-COMPLETE + unusually disciplined -- each sport has a real gate-validated internally-coherent served engine spine (NBA Elo/possessions; MLB MOV-Elo/anchored-NegBinom; soccer EW-Poisson/Dixon-Coles; tennis surface-Elo/MC) with a calibration-PASSING in-game repricer, and most ingested data has ALREADY been gate-tested + correctly kept as scouting on REJECT. ONE-PREDICTION property HOLDS: every sport resolves served team/match markets to ONE coherent fused number off a single engine distribution w/ provenance (NBA produce.py asserts served==engine prob; MLB RL@0==ML + totals partition asserted; soccer all markets off ONE DC matrix; tennis off ONE MC matrix). Outputs .planning/product/funnel/{basketball_nba,mlb,soccer,tennis}.md + COVERAGE.md + a 10-item prioritized wiring plan. REMAINING GAPS (bounded, NOT a wasteland): a few UNTESTED ingested layers/sport lacking a recorded verdict (soccer cardstates/ESPN-28; MLB atbat/ESPN-box; tennis ESPN/setdetail-not-materialized; NBA possession/travel), a few VALIDATED-BUT-UNWIRED priors (MLB SP-form, NBA quarter-shape -- wire as priors/provenance NOT features), and coherence/exposure gaps (tennis exposes only 2 of ~9 coherent markets; NBA/MLB orphan ungated prop pricers). Honest expected outcome of nearly every wiring = REJECT-scouting or calibration-neutral; that IS the point.
- FUNNEL WIRING WAVE 1: DONE + VERIFIED 2026-06-19 (workflow wf_1723f21e-53a). (1) TENNIS market exposure = COHERENT_EXPOSED (real win): registry went from 2 -> 9 markets (total_sets/set_handicap/correct_set_score/set_score_dist/game_handicap/per_set_winner/first_set_winner) all read off the SAME markets_for_matchup() MC matrix predict() uses (spot-check served==matrix exactly; corrupted-cell negative control FAILS coherence as designed); 34 estimate rows; no $, vs_close=UNPROVEN; tie-break/within-set HONESTLY excluded per POINT_MODEL_GAPS; 8+16 tests PASS. predict_service/registry.py + tennis markets. (2) SOCCER red_diff in-game gate = REJECT (honest, identical gate machinery, planted-null rejects both dirs p=1.0, base non-degenerate, B->A trivial +0.000276 p=0.311 = artifact); kept as scouting, not wired; 5/5 tests. (3) NBA possession/pace/run in-game gate = REJECT (honest, all 4 cols both-dir cross-corpus 2024_25<->2025_26, best DM p=0.214, planted-null rejects, base non-degenerate BSS 0.32); not wired; 4 tests. Each verdict in its own data/frontend/funnel/<name>.json. Nothing rejected force-fed.
- MLB DEEP-DATA WAVE 2: DONE + VERIFIED 2026-06-19 (workflow wf_16d78ce7-d25). BOTH REJECT (honest, adversarially reproduced) -> MLB DEPTH LADDER NOW EXHAUSTIVELY TESTED. (A) RICHER Statcast win-prob (spin/xwOBA/velo) = REJECT: the TIER3_BLOCKED is CLOSED -- re-acquired 396 OVERLAPPING games (2022:196 + 2023:200, 16/16 dates/season) with REAL home/away/post_score + release_spin_rate(~2220rpm) + estimated_woba(~0.32) + release_speed(~88.6mph); ~45% rows carry a finite as-of feature (rest correctly NaN, not 0-filled). Richer fields do NOT beat (run_diff,frac_elapsed) either dir (A->B delta -0.000337 DM p=0.496; B->A -0.000148 p=0.681); base skillful; planted-null rejects (A->B null DM wrong-direction = correctly not a beat). 9 tests. => even the RICHEST Statcast fields carry NO in-game win-prob signal beyond run-diff+time. (B) AT-BAT RE24/leverage/base-out in-game gate = REJECT: real orphan parquet (2022 202g/14,734 states; 2023 202g/13,546), adds nothing both dirs (A->B +0.00011 p=0.534; B->A +0.00003 p=0.866); base skillful BSS 0.335/0.386; planted-null rejects AND a leaky positive-control (re24 leaks label) SHIPS through the IDENTICAL machinery -> gate NOT rigged, REJECT meaningful. 6 tests. Verdicts data/frontend/funnel/mlb_{statcast_winprob,atbat_ingame}_gate.json. No $/flag/registry, nothing force-fed.
  - => MLB AT HONEST HIGHEST LEVEL for the close: every available data tier (half-inning -> at-bat RE24/leverage/base-out -> per-pitch ESPN -> Statcast spin/xwOBA/velo + SP-isolated K-prop) TESTED + recorded REJECT, under gates proven to both FAIL noise and PASS real signal. Nothing fabricated. Remaining MLB upside = served-market coherence (prop pricer into registry) + the live-CLV loop, NOT deeper data for win-prob.
- FUNNEL WIRING WAVE 3 (deferred, low-collision-risk later): surface validated SP-form sp_first6_diff_ew as scouting/provenance (NOT a feature without a clean replicated gate pass); consolidate pending asof/box/defender verdicts into data/frontend/reject_ledger.jsonl (serial single writer); sweep remaining UNTESTED layers (soccer ESPN-28/corners/referee, materialize tennis setdetail, NBA travel) so EVERY data point has a recorded verdict; gate the orphan NBA/MLB prop pricers into the registry on a calibration pass.
- AUTONOMY + SELF-IMPROVE READINESS: DONE + VERIFIED READY 2026-06-19 (workflow wf_434803f1-063). KEYSTONE for complete-independent-AI. stack_ready=TRUE (11/11 component checks; all 10 services import + heartbeat-wired; DAG topo-valid; nothing left running; sentinel ABSENT). degrades_honestly=TRUE (stale-never-green, monotone-down status, resource-governor fail-safe, crash-safe work queue, real-money default-DENY). ratchet_improves_when_enabled=TRUE -- SANDBOX proof (sentinel monkeypatched to tmp_path, real path NEVER touched): miscalibrated window -> recalibrator -> Platt candidate -> all 5 gates unanimous SHIP, sealed-fold Brier 0.2025->0.0958 (+0.107, seed-stable 8 seeds x 200 bootstrap). HONEST real-n: live feed ~1 NBA game now (offseason) < MIN_OBS=8 -> on the REAL feed today returns NO_CANDIDATE (cold start); mechanism proven on fixture. inert_when_sentinel_absent=TRUE (byte-identical NO_CANDIDATE, never auto-ships). shrink_yields_no_corpus=TRUE. no_human_gate_crossed=TRUE, no_dollar=TRUE. FIXED 2 stale-green gaps: m1_paper + m1_line_daemon had readiness=NONE (hung loop -> READY forever) -> now emit heartbeats + kind=HEARTBEAT (absent/stale -> NOT-READY). 74 tests pass. ALSO fixed a brittle pre-existing red test (test_supervised_runners.py::test_default_recalibrate_returns_none asserted the live feed returns exactly [] -> drifts to 1 in offseason; now asserts the robust contract (returns a list) + the INERT invariant; 9/9 pass). REMAINING HUMAN GATES (5, all still OFF): create data/cache/improve/PIPELINE_ENABLED; full supervised boot.ps1 -NoUI + browser pass; arm autostart; flip real-money; push origin (local ahead 394). => the system is PROVEN ready to run live unattended + PROVEN to get better when enabled, staying honest + INERT until a human flips the gates.
- COVERAGE-COMPLETION + PARITY-GREEN: DONE + VERIFIED 2026-06-19 (workflow wf_0bf21749-1b4). (1) soccer_intl PARITY -> GREEN: added real manifest (1 source: results.parquet 49,477 rows) + feature_spec (4 cols soccer_intl-base-v1, only genuinely-emitted lambdas; rest_days omitted not 0-filled); anti-fabrication control passes (a planted fake source turns the cell RED). PARITY MATRIX NOW FULLY GREEN (0 red cells -- last n/a closed). 16 tests. (2) soccer ESPN-28 = SHIP-CANDIDATE (NOT yet a confirmed ship -- needs hardening): diff_corners_asof replicated BOTH directions cross-corpus (A{E0,E1} dm_p=0.0014 brier_delta +0.0021; B{SP1,I1} dm_p=0.0018 +0.0027), base non-degenerate (BSS 0.049/0.094), planted-null rejects w/ directional guard. BUT the agent honestly flagged: single 50/50 chronological fold per corpus + only 2 corpora + corners is 1-of-5 candidates tested (selection multiplicity) + the '5-split-shift stability' was NOT independently re-verified -> a PROPOSAL needing nested-CV + FWER + seed-ensemble before promotion (per single-fold-lifts-are-artifacts + seed-stability discipline). fouls/yellow PARTIAL, red/sot REJECT. 15 tests. (3) tennis setdetail = REJECT (no column wins both corpora; sets_dropped ATP-only PARTIAL not shipped) -> confirms per-set ceiling; 29 tests. (4) NBA travel = REJECT (all 3 cols, deltas ~1e-4 p>>0.05 both dirs; Elo+rest subsume); 5 tests. Verdicts in data/frontend/funnel/*.json. No $/flag/registry, nothing force-fed.
- SOCCER CORNERS HARDENING: DONE + VERIFIED 2026-06-19 (workflow wf_5b2a7dba-5a8) = CONFIRMED_SURVIVOR -- THE FIRST CONFIRMED CALIBRATION SURVIVOR OF THE DISCIPLINE THRUST. diff_corners_asof (leak-free as-of trailing corners diff) cleared ALL SIX controls through the proven combo_gate harness: nested-CV SelectSpy (no selection-on-test; selector independently PICKED corners on BOTH corpora; sealed-outer-fold delta A +0.00072 / B +0.00329); FWER-tightened K=20 bar (eps_eff=0.00250; cross-corpus DM p=7e-6 A->B / ~0 B->A, both prior_beats_base); seed-ensemble 10 seeds mean +0.00216 p10 +0.00207 (>0); 3/3 positive chronological splits [0.00234,0.00220,0.00197]; planted-null-FDR 20 nulls reject fdr_hat=0.0 not-frozen (weakened-gate correctly FREEZES = teeth); NOT collinear with base (|corr|=0.341 << tau 0.92). Independently reproduced on the real 25,834-match corpus; 7 tests pass. HONEST FRAMING: CALIBRATION ONLY (held-out home-win Brier), NEVER a market edge; vs_close UNPROVEN; PROPOSAL-only (no flag/registry/sentinel/push); small magnitude (~0.0007-0.0033 Brier); only 2 corpora -> re-check at wider cumulative K as more discipline candidates accrue before any promotion beyond PROPOSAL. Files scripts/platformkit/survivor_corners_{combo_gate,corpus}.py + test. => the discipline works: ~everything rejects, and the one survivor survived the hardest scrutiny.

## LOCAL UI THRUST IN FLIGHT (2026-06-19, user directive: front page shows HOW the AI works + WHAT it's doing + GAMES/CARDS/BETS, in-depth, has everything; LOCAL only, no deploy)
Make the local webapp the in-depth window into the system: (1) HOW IT WORKS -- the funnel data->signals->models->engines->ONE prediction + the validation discipline + the honest findings (plateau + the confirmed corners survivor); (2) WHAT IT'S DOING -- live status/services/autonomy/self-improve/gate-verdict ledger/improvement-backlog/parity; (3) GAMES/CARDS/BETS -- per-sport slate, per-game one-coherent-prediction cards with the full market surface + best-bets (UNITS/tier, NO $) + CLV; in-depth drill-downs. Reads the REAL local APIs (predict_service :8099 + frontend routes) + artifacts (.planning/product/{funnel,meta,deepdata} + data/frontend/funnel/*.json). Honest empty/offseason states, never fabricated. No deploy/push; real-money DENY; no $ field anywhere.
- COHESIVE PRODUCT UI: DONE + VERIFIED COHESIVE_AND_GREEN 2026-06-19 (workflow wf_f6c7973a-096; foundation + 3 parallel pages + Opus cohesion verify). All 10 cohesion checks TRUE (build_green, all_routes_reachable, one_cohesive_product, shows_how_it_works, shows_what_its_doing, games_cards_bets_work, no_dollar, honest_states, local_only). Front page app/page.tsx (hero + funnel strip + live status row + entry cards + honest findings + 'calibration not edge' disclaimer); /how-it-works (FunnelDetail + 8-control ValidationDiscipline + HonestFindings gate-verdict table [1 SHIP corners / 7 REJECT / 1 TIER3_BLOCKED] + SelfImproveExplainer); /games + /games/[sport]/[gameId] (slate->cards->full market surface + best-bets UNITS/tier + CLV, REAL data nba1/mlb9/soccer10/intl5, tennis honest-empty); /system (services + autonomy + self-improve INERT timeline + gate-ledger + improvement-backlog + parity GREEN + CLV-over-time). Mounted 6 unmounted routers in frontend/serve.py (+predict_service/core_mounts.py); cohesive Nav + ProductStatusBadges + lib/api.ts. 29 backend + 15 page tests PASS; npm run build GREEN (10 routes); tsc clean. Verifier CAUGHT+FIXED real cohesion bugs (orphan /games route now linked from Nav/Home/how-it-works; duplicate /p6/system -> redirect to canonical /system). NO $ (units/probability/CLV only); honest labels (INERT/DENY/UNPROVEN); LOCAL-only (no deploy/push/autostart). Files webapp/app/{page,how-it-works,system,games/**}.tsx + webapp/components/{home,explain,system}/* + Nav/ProductStatusBadges + lib/api.ts + frontend/{serve,funnel_routes}.py + predict_service/core_mounts.py. ONE GAP (the user wants to SEE it work): the cohesion verify could not exercise LIVE data because :8099 was not running (correct under local-only) -> next = a one-command local launcher + bounded end-to-end render check so the user runs one script and opens the browser to real games/cards/bets.
- LOCAL VIEWABILITY: DONE + VERIFIED READY_TO_VIEW 2026-06-19 (workflow wf_274411cc-ada). scripts/view_local.ps1 = ONE COMMAND -> starts predict_service :8099 (uvicorn predict_service.app:app) + webapp :3000 (npm build -> next start; -Dev = next dev), sets NEXT_PUBLIC_API_BASE/P5_BASE=http://127.0.0.1:8099, waits-until-200, prints 'OPEN http://localhost:3000', stops cleanly on Ctrl-C or -Stop (pidfile taskkill /T /F), idempotent. FIXED a real bug: webapp/lib/config.ts default API base was :8000 (wrong) -> :8099. VIEW_LOCALLY.md at repo root (run cmd + per-page guide + honest notes). LIVE SMOKE all passed then STOPPED clean (0 listeners on 8099/3000): /api/status all_honest=true n_faces=4; /api/predict/mlb=9 real games; /api/predict/soccer=10; /api/parity green=true edge_claimed=false; /api/improve/status ratchet IDLE/INERT; /api/predict/tennis=unavailable (honest empty); npm build GREEN (10 routes); next start served / + /how-it-works + /games + /system = 200 real markup. NO $ (the $N tokens were React RSC stream markers); LOCAL-only; only scripts/view_local.ps1 + VIEW_LOCALLY.md created. => USER CAN NOW SEE IT: run scripts/view_local.ps1 -> open http://localhost:3000.
- UI DEPTH-ENRICHMENT + COHESION PASS: DONE + VERIFIED IN_DEPTH_AND_COHESIVE 2026-06-19 (workflow wf_2fb56caa-e12; shared primitives + 3 parallel enrich lanes + Opus verify). All checks TRUE (build_green, still_cohesive, in_depth_has_everything, every_number_explained, no_dollar, honest_states, local_only). NEW webapp/components/depth/ shared primitives (InfoTip, Legend, ProvenanceBadge, SignalVerdictBadge [SHIP always 'calibration-only vs_close UNPROVEN'], UncertaintyBar, MarketSurfaceTable, 12-term glossary; 17 tests). GAMES: CoherentPrediction (per-sport engine provenance + uncertainty) + GameMarketSurface (full coherent surface, NO price) + ValidatedSignalsStrip + InfoTips; reuses GameReport/BestBets(units/tier/no_bet)/InGameNumber/ClvScoreboard (34 tests). HOW-IT-WORKS: WhatAmILookingAt + LiveExample (pulls a REAL soccer prediction; honest-empty when none) + per-stage InfoTips + ValidationDiscipline 'why it matters' + HonestFindings deciding-stats grid (8 tests). SYSTEM: app/system/funnel-artifacts/route.ts (same-origin local-disk read of data/frontend/funnel/*.json + improvement_backlog.json) -> rich sortable GATE-LEDGER (15 rows VERIFIED vs the JSONs: 1 SHIP corners / 11 REJECT / 2 PARTIAL / 1 TIER3_BLOCKED) + ranked 73-item BACKLOG + CLV-over-time + READY-but-INERT banner + 'how to read this page' Legend. tsc clean; vitest all green; npm build GREEN. NO $ (grep clean -- all hits are template-literals/percent/negations); NO retracted numbers (18.38/0.119/78.11/54.57/8.94/+54 grep-clean); honest states preserved; LOCAL-only; frozen shared files untouched. => the product is now IN-DEPTH + SELF-EXPLAINING + cohesive + viewable via scripts/view_local.ps1.
- META-LOOP IMPROVEMENT-FINDER: IN FLIGHT workflow wf_935c3413-083 (measurement-only scanner -> ranked durable .planning/product/meta/IMPROVEMENT_BACKLOG.md; enforces the no-force-feed discipline; the 'keep finding things to get better at' engine).

## FINAL PRODUCT PLAN (2026-06-19, user directive: a final product that gets better + visible progress + risk + robust)
- PLAN DONE 2026-06-19 (workflow wf_1027538c-ed9, 5 Opus designers + master + skeptic). Docs .planning/product/final/{progress_visibility,risk,robustness,continuous_improvement,product_polish}.md + FINAL_PRODUCT_PLAN.md. Skeptic SOUND_WITH_FIXES (honest_progress/risk_units_only/robustness_real/no_gate_crossed all TRUE; its 'only polish doc exists' blocker was a STALE READ-RACE -- all 5 + master are on disk). VALID must-fixes to fold in: (a) cross-dimension FILE-OWNERSHIP MAP (AppShell/Nav/tokens/predict_service routers touched by multiple dims -> assign single owners so waves stay disjoint); (b) chaos check must add PARTIAL/CORRUPT/MALFORMED cases not just empty; (c) progress-ledger schema must ASSERT NO live-accuracy field; (d) README -> webapp/PRODUCT_README.md (build area); (e) launcher -Health needs a real freshness field; (f) empty-store toggle in build-area only (default OFF).
- WAVE SEQUENCE: W1 Progress Visibility (highest value) -> W2/W3 Risk + Robustness -> W4 continuous-improvement nightly cadence (measurement-only, conducts finder->backlog->schedule->work_queue->5-gate daemon->ledger; INERT; model-hash unchanged across 30+ simulated nights) -> W5 polish (onboarding+README+a11y) + final integration + CAPSTONE audit.
- HONEST FRAMING (binding): 'gets better' = growing VALIDATION COVERAGE + COMPLETENESS + the improvement ENGINE proven-ready-but-INERT (sealed-fold Brier 0.2025->0.0958, labelled ready-but-pending) -- NEVER a fake live-accuracy/edge climb while the model is static. RISK = units-only descriptive (joint<naive, RoR, Kelly, floors, caps), real-money DENY, no $. ROBUST = honest degradation (stale-never-green 100% coverage) + self-heal + chaos check. No gate flipped.
- EXECUTION STATUS: W1+W2 (Progress + Risk) DONE + SHIPPED HONEST 2026-06-19 (workflow wf_021aa7ff-57c). NEW scripts/platformkit/progress/progress_ledger.py (measurement-only: build_snapshot -> per-sport coverage_pct + n_ship/n_reject/n_insufficient, parity_green, backlog_total 73/remaining 15, engine_ready=True engine_enabled=False model_static=True edge_claimed=False; reconstruct_history from funnel mtimes; append-only idempotent; 7 tests; NO $) + GET /api/progress in frontend/serve.py + lib/api.ts getProgress/getRisk. /risk page (per-bet units/tier/capped-quarter-Kelly + portfolio joint<naive-PRINCIPLE/RoR-descriptive + exposure caps mirroring exec_decision tier floors + 'units not dollars / real-money DENY' banner; honest-empty when no pick clears a floor; 11 tests). /progress page (REAL coverage bars nba33.3/soccer42.9/tennis50.0, cumulative-verdict SVG sparkline, backlog 73->15 burn-down, parity GREEN + engine READY/INERT, CLV INSUFFICIENT_DATA, what's-next from backlog; explicit 'model STATIC until self-improve gate enabled; sealed-fold Brier 0.2025->0.0958 WHEN enabled; NOT a live accuracy climb' banner). npm build GREEN; tsc clean; NO $ (recursive scan); no retracted numbers; real-money DENY; LOCAL-only. => the product now shows RISK + honest PROGRESS.
  - W3 ROBUSTNESS DONE + VERIFIED ROBUST 2026-06-19 (workflow wf_ed02fd64-2dd, 3 disjoint lanes + chaos + verify; all checks TRUE). FRONTEND: webapp/lib/fetchHonest.ts (timeout+capped-backoff retry, never-throws, degrade-to-Unavailable) wired through lib/api+p5api; HonestState (Unavailable/Empty/Stale/Degraded); per-route error.tsx+loading.tsx for ALL 12 segments; useStream honest 'disconnected' (no frozen-as-live); 32 tests. BACKEND: predict_service/honest_route.py (@honest_route -> any handler raise becomes honest {status:unavailable, reason=exc-TYPE-only} 503, NEVER a 500/stack; drops $/edge keys) applied across status/parity/clv/quant/catalog/intel/bestbets/funnel routers; freshness_sidecar.py (reachable-but-stale -> STALE/ok=False) + GET /api/freshness monotone-DOWN rollup; 12 new + 27 regression tests. OPS: scripts/platformkit/autonomy/supervisor_readiness.py (every service a readiness predicate; hung/dead -> NOT-READY) + scripts/platformkit/chaos_check.py (15-cell kill MATRIX empty/stale/partial/corrupt/timeout x 3 surfaces) + robustness_manifest.py (untested->UNPROVEN, writes data/cache/robustness/manifest.json) + view_local.ps1 -Health (consumes freshness -> STALE/DOWN never green-on-reachable); 35 tests. CHAOS RESULT: 15/15 real cells degrade HONESTLY (0 gaps); the green-on-missing NEGATIVE CONTROL correctly FLAGGED FAILED (teeth confirmed). NO $; no existing honest behavior weakened; LOCAL-only. => the product is now ROBUST (honest degradation everywhere, chaos-proven).
  - W4 CONTINUOUS-IMPROVEMENT CADENCE DONE + VERIFIED CADENCE_HONEST_AND_INERT 2026-06-19 (workflow wf_2fbe414f-f6b; all 9 checks TRUE). Modules scripts/platformkit/progress/{ci_state,ci_regate,ci_survivor_recheck,ci_cadence,ci_schedule}.py + predict_service/progress_router.py (/progress/{summary,coverage,verdicts,readiness,schedule}, @honest_route, stale-never-green). Conductor steps A-H each degrade-isolated; ALLOWED_KINDS pinned to work_queue {recalibrate,regate_ingame,refresh_corpora,backfill_settled} (test-asserted no-drift); ship path = selfimprove INERT (_inert_recalibrate -> NO_CANDIDATE, sentinel READ never CREATED) -> proposal-only; ci_regate NO_OP when nothing grew; ci_survivor_recheck re-checks survivors at WIDER K via the real combo_gate, DEMOTE-on-fail. CI-9 CAPSTONE test_ci_boundary_invariants.py: 8/8 invariants PASS across 32 SIMULATED NIGHTS -- (1) served MODEL-HASH UNCHANGED (cadence has no handle to the artifact), (2) NO registry/sentinel/MEMORY/flag/autostart write (fingerprint equal before==after), (3) every enqueued kind in ALLOWED_KINDS, (4) coverage MONOTONE non-decreasing (verdicts only accumulate), (5) every ship NO_CANDIDATE proposal-only, (6) planted failing step degrade-isolated (cadence completes, status DEGRADED, progress row still appended), (7) survivor-recheck DEMOTES a planted-now-failing survivor, (8) NO $ field. 32+8 tests. Supervise wiring PROPOSED (frozen manifest) docs/research/organization-sprint/PROPOSED_ci_cadence_supervisor_wiring.md. Minor noted gap: integer-second ts filename could overwrite a same-second sibling (harmless once-nightly). => the product gets better unattended (coverage climbs) WITHOUT crossing any gate; model provably static.
  - LIVE API END-TO-END VERIFICATION IN FLIGHT workflow wf_ccfa7b92-202 (bounded boot :8099 -> verify EVERY endpoint real-data/contract/no-$/honest-degrade/latency per-sport -> STOP; comprehensive contract tests so it stays verified). FRONTEND HIGHEST-CAPACITY DONE + VERIFIED FRONTEND_HANDOFF_READY 2026-06-19 (workflow wf_351e03f7-2cc; all 8 checks TRUE). 111 new per-route test cases (games 37 / core 38 / data 36) + 12 a11y tests, ALL PASS, ZERO shared-component bugs found; every state covered (real-data/empty/stale/degraded/disconnected/below-floor/missing-prob/honest-empty); coherence asserted (displayed prob == the model prob it came from). A11Y: skip-to-content + focusable main landmark + aria-sort on the gate-ledger + focus-visible rings + text-not-color-only badges + Nav landmarks. ONBOARDING: webapp/components/onboarding/OnboardingOverlay.tsx (first-run Radix dialog, focus-trap, Esc/overlay dismiss, localStorage cv-onboarded + persistent '?' Help button; 4 honest rails + 6-page purpose map; non-blocking). webapp/PRODUCT_README.md (run via view_local.ps1; per-page table; data/API map; honest framing + 3 human gates OFF; the 'front-end dev: where to work on details' handoff map = component map + tokens + how to add a market view + honest-states + a11y + testing). npm build GREEN (12 routes); tsc clean; NO $; honest framing preserved; LOCAL-only. 2 non-blocking notes (benign useMemo ESLint + act() test-log warnings). => THE USER CAN NOW WORK ON FRONT-END DETAILS confidently.
  - LIVE API END-TO-END VERIFICATION DONE + VERIFIED APIS_LIVE_PERFECT 2026-06-19 (workflow wf_ccfa7b92-202; all checks TRUE). Bounded boot :8099 -> 69 endpoint probes = 56 PASS / 11 honest-degrade / 2 absent (canonical /api/status.all_honest + /api/intel/{q}, not bugs) / 0 real FAIL; ZERO $ leaks (recursive scan); honest flags everywhere (edge_claimed/real_money_enabled False, vs_close UNPROVEN, all_honest ok:true, autonomy INERT); server STOPPED 0 procs remain. CAUGHT + FIXED 2 REAL BUGS (non-gated predict_service): (1) honesty_mw.py units-rail OVER-MATCH -- the bare 'stake' forbidden-money token over-matched the CANONICAL UNITS field stake_units (+ arb stake_a/stake_b), fail-closing 11 live endpoints (ALL bestbets + quant validate/arb for nba/mlb/soccer/soccer_intl) to 500; fixed to exempt *_units + stake_a/stake_b while stake_dollars/usd/roi/pnl/bare-stake still trip (4 regression tests; 11 went 500->PASS). (2) predict_service/app.py predict_sport+predict_game called store.read_latest() WITHOUT @honest_route -> a request-time store exception leaked a raw 500+traceback; decorated both -> honest 503 (3 regression tests). COMPREHENSIVE CONTRACT NET added: tests/predict_service/test_endpoint_contract_matrix.py (54 cases, every endpoint x 6 rails) + test_app_predict_honest_degrade.py (3) + tests/frontend/test_mounted_in_predict_service_contract.py (18) + honesty_mw_units_rail (4) = 103 PASS combined. => THE AI APIs WORK PERFECTLY LIVE (real data flows, honest-degrade, no-$, contract-locked). PER-SPORT DEEP WAVE wf_4f4e51bc-f49 + IN-GAME PEAK PUSH wf_050d6a5e-8ae still in flight. W5 final capstone (integration smoke + honesty/correctness audit -> 'final product + handoff ready: GREEN') LAST.

## ORCHESTRATION IN FLIGHT (2026-06-19) -- FRESH FULL-BUILD ("GO" acceptance test)
Lean Opus dispatcher driving Phases 0->7 toward the GO acceptance test (browser-verified: live lines + ALL paper trading + a paper-trade HISTORY view showing every trade with units/tier/model_prob/price/result/CLV, NO $ column; fast+bulletproof; self-improve unattended; real-money default-DENY). Parity GREEN (4/4 + soccer_intl) confirmed 06-19.
- PHASE 0 (discovery+plan): DONE 2026-06-19. 12 discovery audits -> .planning/product/FRESH_BUILD_PLAN.md = ~281 deduped problems (~31 P0). 7-wave sequence. KEY HONESTY RAIL BREAK FOUND: $ stake/pnl/roi in canonical clv_ledger + grade_summary (Phase 2, blocking); freshness badges read browser-fetch-time not data-age; self-improve loop inert+reads-green (recalibrator.py + settled_ingest.py MISSING).
- WAVE 1 IN FLIGHT: workflow `wf_346bc354-b6d` -- 4 disjoint lanes each build->Opus-review->fix: B (predict_service endpoints: produce/lines/gate/results + CORS + live honesty_linter), E (paper engine UNITS-ONLY + void-not-0-fill + idempotency -- HONESTY-BLOCKING), I (odds_provider stale-never-green + tenacity retry + inplay O(n^2) fix), F (webapp design-system: Next CVE upgrade + shadcn + tokens + app shell/nav). No commits; lands in working tree for review.
- AUTONOMY OPUS FLEET DONE 2026-06-19: 8 design docs + master plan + skeptic -> .planning/product/autonomy/. KEY: the 5-gate ratchet is fully built but PERMANENTLY INERT (selfimprove_runner imports an absent improve/recalibrator.py). Skeptic verdict SOUND_WITH_FIXES (MF1-MF5 mandatory before any flag flip: settle_audit ships in Wave0 / kill-switch in BOTH runner+recalibrator / degenerate-base guard for recal-variants / wrapper routes through build_candidate / holdout excluded from priority_view). Plan = waves 0-5, all SAFE-now flag-OFF.
- WAVE 0 (self-improve unlock = Phase 6 core) IN FLIGHT: workflow `wf_12c2b001-f16` builds FLAG-OFF (PIPELINE_ENABLED sentinel ABSENT -> byte-identical NO_CANDIDATE) with MF1-MF5 baked in: improve/{pipeline_flag,settle_audit,base_guard,recalibrator,settled_ingest,candidate_enum,candidate_families,prioritizer}.py + runner kill-switch + daemon seen_ids fix (SI-P0-03). Skeptic-verify+fix at the end. Human must create data/cache/improve/PIPELINE_ENABLED to ever enable (the loop never creates it).
- FLEET SATURATED 2026-06-19 -- 4 background workflows on disjoint trees:
  - `wf_346bc354-b6d` Wave 1: DONE + VERIFIED 2026-06-19. Lane I clean SHIP; B/E/F SHIP_WITH_FIXES (fixes applied; a few non-keystone items folded into Phase 4). VERIFIED ON DISK: E paper units-only 35 tests GREEN + clv_ledger.py has ZERO $-field assignments (rail holds at source); B contracts GREEN (new /api/lines, /api/gate/status, /api/produce/status, /api/results + live honesty middleware test_honesty_mw); webapp `npm run build` GREEN (shadcn+tokens+shell+error/loading/not-found). New trees: predict_service/, frontend/, tests/predict_service/, tests/frontend/, clv_ledger_io+migrate, grade_paper_close+summary, odds_provider/freshness.py.
  - `wf_290b29e1-f81` Phase 4 UI VIEWS: DONE + VERIFIED 2026-06-19. place-paper-bet flow + PAPER HISTORY view (/p6/history, 10.3kB, every trade no-$) + live-lines freshness-from-generated_at + by_sport CLV. Review dollar_leaks:0, build GREEN (7 pages), vitest 11/11, 6 fixes (incl 2 stale-green) applied. webapp `npm run build` GREEN confirmed on disk.
  - `wf_8fa5f4a6-f78` IN-PLAY FRONTIER: DONE + VERIFIED 2026-06-19. odds_provider/inplay_kalshi.py (liquid connector, *_dollars, liquidity gate) + ingame/inplay_grade_run.py (leak-free, single-game=INSUFFICIENT_DATA) + honesty_mw harden + PROPOSED-legacy-api-honesty.md. Review SHIP_WITH_FIXES (4 rails -> 8 fixes); 28 tests GREEN on disk (connector 6 / grade 13 / honesty_mw 9).
  - `wf_d18e94ec-260` MOAT design fleet: DONE 2026-06-19. 4 docs + MOAT_PLAN.md (5 waves, 24 signal hypotheses) + MOAT_SKEPTIC_REVIEW.md (SOUND_WITH_FIXES, 8 must-fix-before-build). THESIS: AI turns deepest sport understanding -> person-free archetype/scheme signals -> the few that survive the same leak-free gate; most REJECT (honest); calibration not edge. Guards for wide search: FWER budget tightening eps as K grows + planted-null tripwire (a null that ships FREEZES the family) + clustered-DM + no-season-final-leak. .planning/product/moat/.
  - `wf_09868b90-766` MOAT BUILD inc 1-3: DONE + VERIFIED 2026-06-19 (skeptic SHIP, noise_reject_confirmed=TRUE, 0 must-fix; 26 tests GREEN). Inc1 detail_layer_gate.py = NEGATIVE-CONTROL backbone PROVEN (pure-noise col REJECTs -> the gate can FAIL a signal). Inc2 domains/mlb/ingest_atbat_states.py = HONEST REJECT (RE24 +0.0019 p=0.063 not<0.05, reverse dir worse; base-out/RE24/leverage kept as SCOUTING not served; RE24 pinned constant disjoint-seasons; balls/strikes honestly 0-0 at at-bat start = deferred network tier, disclosed). Inc3 domains/soccer/ingest_cardstate_states.py = PARTIAL->honest REJECT for serving (red-card man-advantage beat base ONE dir p=0.016 not the other p=0.071 on a 400-game sample; one-direction lift = probable artifact; kept as scouting). MOAT WORKS HONESTLY: built deep layers, gated vs a noise-proven harness, rejected both for serving, fabricated nothing.
  - `wf_bd36cbab-362` MOAT WAVE 2: DONE + VERIFIED 2026-06-19 (skeptic SHIP, person_leak=false, inert=true, 0 must-fix; noise-reject STILL holds + INERT confirmed on disk). (A) NBA possession = HONEST REJECT (base non-degenerate, micro-state adds nothing both dirs DM p>>0.05; ran 2024_25<->2025_26 as 2023-24 not on disk); (B) tennis games_diff REPLICATED both-ways BUT redundant-with-base -> scouting; others REJECT; DATA-LIMIT: true point-by-point NOT on disk (per-set scores only; match-final BP totals would leak, unused); (C) signal-factory bridge INFRA + INERT (81 specs enumerable, build_candidates=[] while sentinel absent; vault person-free; planted-nulls all REJECT through the IDENTICAL gate). META-FINDING: in-game MICRO-STATE rejects across all 4 sports -> confirms thesis; remaining frontier = archetype/playstyle-conditioned signals.
  - `wf_5bf4cc2a-76e` MOAT WAVE 3: DONE 2026-06-19 -- HONEST GAP FINDING (no fabricated verdict). signal_factory specs are name-stable PROPOSALS with NO corpus binding; playstyle_corr/archetype_matchup are pregame correlation proposals, NOT in-game detail cols -> the in-game detail_layer_gate is the WRONG gate and the CORRECT validation harness (full stat-pair surface, archetype-pooled residual co-movement, split-half-stable, cross-corpus, DM-by-game) is NOT WIRED. Forcing them through a mismatched gate = fabricated verdict (refused). Result: planted-nulls REJECT 3/3 (gate works), 68/68 real specs NOT-TESTABLE-HERE, 0 fabricated survivors. Skeptic SOUND_WITH_FIXES gate_correct=true. -> the gap to close = build the correct correlation-validation gate.
  - `wf_18beb970-e6a` MOAT WAVE 4+5 (playstyle-corr validation gate): DONE 2026-06-19 (~96min). scripts/platformkit/signals/playstyle_corr_gate.py = validation-of-record for archetype-conditioned stat-pair correlations (full-surface + split-half + cross-corpus both-dirs + perm/DM-by-game + FWER). RESULT: playstyle_corr 48/48 REJECT, 0 survivors; scheme_prior(8)+archetype_matchup(12) NOT-YET-BINDABLE (don't map onto same-player residual surface; not forced). SELF-CAUGHT ARTIFACT: the initial Wave-4 "61 cells pass + family FROZEN by null" was itself a floor/1.0 mechanistic artifact; the fix (Wave 5) corrected the null so it CLEANLY REJECTS (planted_null_rejects=True, family_frozen=False, 0 survivors) -> the REJECT is now genuine on a trustworthy gate. 12 gate tests PASS. Skeptic SOUND_WITH_FIXES (full_surface/fwer/leak_free/framing true; 2 residual must-fix on gate power = signals/ follow-up, off critical path). HONEST: the archetype/playstyle "unpriced frontier" yields NO validated survivor under a trustworthy gate -> consistent w/ the meta-finding.
  - `wf_c53f903e-c6b` GO FINAL INTEGRATION + SMOKE: DONE + VERIFIED 2026-06-19. Wired GET /api/improve/timeline (honest INERT: enabled=false,n_promoted counts ONLY real shipped_version rows) + GET /api/ops/autonomy (fail-closed 503 when status absent) + POST /api/paper/settle verified; m5_autonomy_monitor measurement-only daemon (13 tests w/ autonomy_routes); UI SelfImprovePanel+OpsPanel. GO_SMOKE_REPORT.md written. SCORECARD (8 criteria): PASS = boot-DryRun(exit0,11 svcs,governance preflight,default-DENY), endpoints(live-probed :8099, zero $ keys, inert), honesty(56 tests/8 files + parity GREEN + webapp build GREEN), green-while-inert assert HOLDS. NEEDS-HUMAN/PARTIAL = full supervised boot-to-ready (criterion 2) + full-browser pixel walkthrough of /p6 + /p6/history + place->settle click-through (criteria 4-7 UI) -- data layers all test-backed, only pixel render + live boot remain (no headed browser in harness).
- GO STATUS: code/data/API/test-complete + HONEST. Remaining to literal browser-verified 100% = (a) a live supervised boot (bounded boot.ps1 -NoUI -> check supervisor_status overall_state=ready + autonomy_status.json populated by m5 + curl endpoints -> boot.ps1 -Stop), and (b) a human browser pass. The 3 deliberate human gates stay OFF: create data/cache/improve/PIPELINE_ENABLED (self-improve), arm autostart, flip real-money.
  - `wf_12c2b001-f16` Wave 0 self-improve unlock: DONE + VERIFIED 2026-06-19. 8 modules (improve/{pipeline_flag,settle_audit,base_guard,recalibrator,settled_ingest,candidate_families,candidate_enum,prioritizer}.py) + runner kill-switch + daemon seen_ids fix. Skeptic SHIP_WITH_FIXES (MF1-4 satisfied, 3 must-fix applied, 0 rail violations). 68 per-file tests GREEN incl test_runner_pipeline_disabled (byte-identical NO_CANDIDATE with sentinel ABSENT). The ratchet is now wired-but-INERT: it ships NOTHING until a human creates data/cache/improve/PIPELINE_ENABLED (the loop never creates it). HONEST-ENABLE PATH for later: create sentinel -> recalibrator folds settle_audit-clean settled games -> Platt recal-variant candidate -> base_guard rejects degenerate -> 5-gate ratchet (WF + >=2 corpora + clustered-DM + degenerate-base) -> proposals.jsonl (never auto-ships).
  - `wf_7662512f-553` autonomy substrate: DONE + VERIFIED 2026-06-19. 8 modules scripts/platformkit/autonomy/{status_composer,loop_observer,heartbeat_reaper,resource_governor,progress_tripwire,work_queue,schedule_policy}.py + supervisor self-heal (RB-P0-02 env-passthrough fail-CLOSED; RB-P0-03 hung-loop readiness). 76 tests GREEN (status composer only-degrades-severity = stale-never-green; resource governor DEFERRED_RESOURCE fail-safe; durable lease work queue crash-safe; governance fail-closed). SHIP_WITH_FIXES, 2 rails cleared. State under data/cache/autonomy/ (never data/registry/).
  - `wf_d03814dd-912` design+honesty+inplay: DONE 2026-06-19. (a) 4 screen specs -> .planning/product/design/ (feeding Phase 4). (b) Honesty contract tests GREEN (tests/api + tests/database; gated api/ leaks pinned xfail w/ promotion guard). RE-AUDIT: GO surface (predict_service :8099 + webapp) is CLEAN units-only by construction; 2 NEW P0 $-leaks are in the LEGACY api/ :8098 board only (human-gated, NOT on GO path) -> PROPOSED fix. (c) IN-PLAY UNBLOCK (corrects a memory premise): Kalshi KX*GAME is LIQUID keyless in-season -- the "illiquid" read was a field-name artifact (deprecated int fields None; live *_dollars/*_fp carry real depth: top MLB mkt vol 606k, 1c spread; settled game = 1364 1-min candles 0.45->0.01). In-game-beat-vs-REAL-prices testable keyless TODAY (MLB/WC; not NBA-offseason). Research: .planning/product/autonomy/INPLAY_LIQUIDITY_RESEARCH.md. Memory project-inplay-edge-vertical updated.
  - `wf_8fa5f4a6-f78` IN-PLAY FRONTIER wave IN FLIGHT: build odds_provider/inplay_kalshi.py (liquid in-play connector, *_dollars fields, liquidity gate so untraded!=in-play, VOID-not-0-fill, commence_time None) -> ingame/inplay_grade_run.py (leak-free grade via inplay_clv_replay, single game=INSUFFICIENT_DATA); + harden predict_service/honesty_mw.py forbidden-keys (stake/total_stake/roi_pct); + PROPOSED docs/research legacy-api honesty fix. Opus review+fix.
- `wf_ec6f4c6c-82a` AUTONOMOUS SIGNAL-COMBINATION design fleet: DONE 2026-06-19. COMBINATION_PLAN.md (4 waves, 41 modules, 3 ranked increments) + COMBINATION_SKEPTIC_REVIEW.md (SOUND_WITH_FIXES; must-fix: D1 corpus-seam re-selection leak, --replay re-runs FROZEN spec, SHIP=proposal-only NEVER writes data/registry/). 21 p-hacking guards R1-R21 (selection-on-test->nested CV w/ select_fn-spy; multiplicity->eps/K; bar-weakening->gate constants in reused-consume-only modules; planted-null>=20/family IDENTICAL gate; collinear-with-base tau=0.92; re-roll/cycle-split->cumulative K). HONEST LINE: success=more OOS-replicating survivors + controlled FDR, NOT fewer rejections.
- `wf_87fd7e22-4ae` COMBO WAVE 0 (gate-at-scale NEGATIVE CONTROL): DONE + VERIFIED 2026-06-19 (skeptic SHIP; leaky_fails=null_rejects=no_registry=no_seam all TRUE; 26 tests GREEN on disk; 0 registry writes). scripts/platformkit/combo/{nested_cv,fwer_budget,planted_null_fdr,combo_base_guard,combo_gate}.py. PROVEN: a planted in-fold-leak combo scores ~0.25 (worthless) on the sealed outer fold (SelectSpy raises if selector saw a holdout game); 20 planted nulls REJECT fdr_hat=0.0; weakened-gate -> family FROZEN; collinear-with-base REJECTS at L0. The harness PROVABLY controls false discovery -> generation is now safe. (2 minor skeptic must-fix noted, non-honesty-critical, sweep next.)
- `wf_31aa7ba3-4bc` COMBO WAVE 1 (generate + run): DONE + VERIFIED 2026-06-19. RESULT = SHIP 0 / REJECT 195/195 (100%): MLB 0/33, NBA 0/54, soccer 0/54, tennis 0/54, all decided at L4 (cross-corpus both-dir DM not replicated). CRITICAL: identical all-REJECT at BOTH the strict cumulative-K bar (eps_eff=2.6e-4) AND a looser per-sport-K bar -> the rejection is REAL (no replicable cross-corpus combined signal), NOT a too-strict-FWER artifact. Planted nulls reject; skeptic SOUND (k_capped/screen_disjoint/nulls/gate_not_weakened all true, fabricated_survivor=false, 0 must-fix). HONEST FINDING: combining the COARSE in-game detail layers (interaction/regime-conditional/residual-on-residual/weak-on-proven) unlocks NOTHING -> the base+proven-prior capture the available in-game signal; the path to non-rejecting signal is DEEPER DATA + the autonomous loop, not recombining shallow data.
- `wf_80f728da-f62` COMBO WAVE 2+3: DONE + VERIFIED 2026-06-19. FUSION (improve/fusion/: STACK_OOF/REGIME_MOE/RESID_BOOST/REFUSE_CAL, OOF-only, meta_flex K=108 alpha=4.6e-4, 4/4 null families reject fdr_hat=0.0; live run human-gated, honest prior = mostly REJECT vs near-optimal). AUTONOMOUS NO-CLAUDE LOOP (combo_recalibrator MF1-choke, combo_bandit Thompson+freeze-on-null, combo_runner/daemon idempotent+checkpoint+STALE->DEGRADED, combo_status) INERT (sentinel-absent -> NO_CANDIDATE); manifest wiring = PROPOSED diff. Skeptic SHIP_WITH_FIXES (fusion_honest+loop_inert+autonomous+no_flag_registry all true, fabricated_survivor=false, 3 must-fix FIXED); 24 tests GREEN. The autonomous signal-combination engine is COMPLETE + honest + flag-OFF. (memory project-go-product-and-moat updated.)
- NEXT THRUST 2026-06-19 (user directive): ONE GIANT SPORTS-INTELLIGENCE MODEL -> multiple APIs (Prediction + quant-grade EXECUTION) with EXECUTION-IN-THE-LOOP so models get continuously better. Validation-first (CLV yardstick) betting-capable; real-money default-DENY; NO fabricated $ edge; loop optimizes calibration/CLV not invented profit.
  - DESIGN FLEET DONE 2026-06-19: .planning/product/apis/SPORTS_INTELLIGENCE_APIS_PLAN.md (6 waves, 46 modules, 3 increments) + E1-E5 + APIS_SKEPTIC_REVIEW.md (SOUND_WITH_FIXES). CORE DESIGN: settled CLV = a leak-free gate-able SECOND CORPUS the existing 5-gate ratchet consumes (replaces the empty corpora:[] that wedges the loop); CLV is a DO-NO-HARM guard / 2nd corpus NEVER a SHIP objective (SHIP target stays held-out Brier/BSS) -> avoids the +18.38% market-follow trap. 10 honesty guards. Skeptic KEYSTONE must-fix P1: the close-corpus fold must be model-BEATS-close on OUTCOME-Brier (DM-by-game), NEVER shrink-toward-close; a pure market-copy candidate yields NO corpus. +P2 no-self-stamp-upgrade, P3 is_true_close-only, P5 per-game loss vectors.
  - `wf_14d1ba9a-058` APIS BUILD inc1-3: DONE + VERIFIED 2026-06-19 (skeptic SHIP, 0 must-fix; shrink_rejected/no_auto_flip/inert/no_dollar/no_real_money_post/clv_sign_audited ALL TRUE; 25 tests GREEN). Inc1 quant/clv_core.py (sign-audit RAISES on mismatch, vintage guard). Inc2 (CORE = execution-in-the-loop) improve/clv_corpus.py (P1 PROVEN: a shrink-toward-close candidate returns None -- the loop CANNOT improve by copying the market; corpus only on model-BEATS-close OUTCOME-Brier DM-by-game; is_true_close-only; per-game loss vectors) + clv_corpus_inject.py (sentinel-absent -> candidate UNCHANGED byte-identical; no self-stamp upgrade; maybe_vs_close_upgrade only if gate_ship AND n_rep>=2 AND dm_p<0.05). Recalibrator wire-in = PROPOSED diff docs/research/organization-sprint/PROPOSED-clv-corpus-wiring.md (NOT applied). Inc3 quant_exec/validate.py + frontend/quant_routes.py (5 GET-only endpoints /api/quant/{sport}/validate|devig|arb + /risk|/clv, gate_proven=false default, NO POST, units-only, reuses vetted helpers). EXECUTION-IN-THE-LOOP IS BUILT + HONEST + FLAG-OFF.
  - `wf_a525dc92-d74` APIS WAVE 3+4 (prediction contract + unified gateway): DONE + VERIFIED 2026-06-19 (skeptic SHIP, 0 must-fix; vs_close_unproven/phase_not_averaged/stale_never_green/no_dollar/status_flips ALL TRUE; 38 tests GREEN). Prediction contracts 1.1.0 additive (ProbEstimate+Provenance{phase}+vs_close=UNPROVEN, uncertainty from held-out residuals, phase-LABELLED-not-averaged) + /api/predict surface|contract|uncertainty + predict_service/{uncertainty,surface,phase_merge}.py. Unified gateway scripts/platformkit/gateway/{gateway,catalog}.py + frontend/{catalog_routes,intel_routes}.py: /api/catalog (faces+versions) + /api/status.all_honest (ONE boolean, =True now, FLIPS False on any $/fabricated-edge/stale-green/real-money) + number-free /api/intel. 'ONE MODEL -> MANY HONEST APIs' is REAL.
  - `wf_26cc212e-322` APIS WAVE 5 (make it visible): DONE + VERIFIED 2026-06-19 (skeptic SHIP, 0 must-fix; no_dollar/honest_status/loop_labelled/build_green/clv_candidate_inert ALL TRUE). webapp FacesPanel (/api/catalog + all-honest badge) + QuantPanel (/api/quant validate+risk, units-only) + ClvFeedbackPanel (execution-in-the-loop CLV-vs-close, 'measurement-only' label) on /p6 (+ /p6/system); webapp build GREEN. risk_units.py (unit-space portfolio risk: joint<naive sum, RoR descriptive, no $). clv_candidate.py (2nd loop family FAMILY_CLV_VS_CLOSE, routes through beats-close gate so shrink yields nothing, INERT behind sentinel; 7 tests). => THE FULL SPORTS-INTELLIGENCE MULTI-API SYSTEM (Prediction + quant Execution + execution-in-the-loop + gateway + visible UI) IS BUILT + HONEST + FLAG-OFF. memory updated.
- NEXT (proposed, awaiting human go): DEEP-DATA + LIVE-ACQUISITION + HONEST-PROFITABILITY thrust. A ready paste-able orchestrator prompt was authored (deeper data per sport -> same gate; active live-game acquisition + independence; live paper-trading -> CLV vs true close -> the clv_corpus loop; honest INSUFFICIENT_DATA until many games). Phases A-E. The honest frontier = DEEPER RAW DATA (shallow micro-state/combos/playstyle all rejected). NOT started unattended (human decision: launch here vs run the prompt in a fresh session).
- GO INTEGRATION CHECKLIST WRITTEN: .planning/product/GO_INTEGRATION_CHECKLIST.md = the remaining new wiring (GET /api/improve/timeline; m5_autonomy_monitor daemon emitting autonomy_status.json + /api/ops/autonomy; settle-route verify; UI panels) + the ordered GO smoke procedure + the #1 risk (self-improve reads GREEN while INERT -> assert sentinel-absent + pipeline_enabled()==False + n_promoted==0 + honest UI label).
- ON-LAND NEXT (after Phase4 `wf_290b29e1` + in-play `wf_8fa5f4a6` land): (1) verify both diffs + tests; (2) execute GO checklist remaining wiring (improve/timeline route + m5_autonomy_monitor + UI panels), disjoint from nothing-then-running; (3) full cross-session honesty/correctness RE-AUDIT over everything built (read-only, after build trees settle); (4) run the GO smoke (boot -> browser -> 8 criteria). Autonomy Waves 1-5 (scoreboard/integrity FSM/wider proposals/meta-learning) build after. Keep everything flag-OFF; human creates data/cache/improve/PIPELINE_ENABLED + arms autostart + flips real-money.
- SELF-IMPROVEMENT + AUTONOMY OPUS FLEET: background workflow `wf_b3b9364c-0ea` (selfimprove-autonomy-opus-fleet) -- 8 Opus thinkers (loop architecture, data-acquisition autonomy, honest self-eval/rollback, autonomous proposal generation, self-healing independence, self-monitoring, continuous-vs-close scoreboard, meta-learning/cross-sport) -> Opus master plan -> Opus adversarial skeptic. Outputs: .planning/product/autonomy/A*.md + SELF_IMPROVEMENT_AUTONOMY_PLAN.md + SKEPTIC_REVIEW.md. Goal: make the loop genuinely better + the AI maximally independent (days unattended) WITHOUT violating honesty rails.
- COLD-RESUME: if FRESH_BUILD_PLAN.md exists, start from its wave sequence. Else re-run that workflow. If SELF_IMPROVEMENT_AUTONOMY_PLAN.md exists, fold its SAFE-now waves into the build. Rails: calibration not edge; UNITS only no $; local commits only never push; per-file tests only; src/kernel/api human-gated (build in scripts/platformkit + domains + webapp); never auto-ship/flip-flag/fabricate.


> Read THIS first (30s) before any work. Update it before you finish: tick a NEXT item, add the follow-up, rotate overflow into DONE.md. If git HEAD is newer than `updated` above, this file is stale -- refresh it.
> Invariants/gotchas/paths: memory `reference-invariants-paths-atlases`. Claimable vs retracted numbers: `docs/JOB_EVIDENCE_PACKET.md`.

## SIDE-TASK DONE 2026-06-18 -- Obsidian brain = ONE navigable enriched graph
Vault `vault/` (local-only, brain). Made it 100% reachable from `_Index.md`: **4400/4400 notes reachable, 1 component, 0 exclusions** (was 4321/4385, 13 components). New SAFE-area tooling in `scripts/platformkit/`: `brain_reachability.py` (BFS resolver: exact path -> unique suffix -> unique basename -> Obsidian shortest-path tiebreak; ignores code spans/fences; `--assert`), `brain_concept_blurbs.py` (~60 person-free concept descriptions), `brain_enrich.py` (master-`_Index` "Atlas" managed block links every top-level area + journal -> durable bucket-D fix; cross-sport hub footers), `brain_graph_finalize.py` (THE last-step entrypoint: folder_indexes -> system_map -> crosslinks -> enrich -> fix_links -> reachability assert). REWROTE `brain_folder_indexes.py` -> complete+recursive+enriched managed `_<Folder>_Index.md` (links every note incl underscore/subfolder/foreign-index + per-note descriptions + up/prev/next + cross-sport bridges). Wired finalize as FINAL step of `brain_pipeline.run_pipeline` (live-vault-only guard; hermetic test untouched). Removed 4 empty stray top-level cruft folders. Idempotent (whole vault byte-identical across 2 finalize runs); `fix_relative_links --dry-run` = 0. Tests: `tests/platform/test_brain_reachability.py` (7) + existing graph/crosslinks/pipeline green. KNOWN: identical season notes duplicated `Reference/` vs `Reference/Seasons/` = upstream-organizer artifact (rebuild-managed; both now reachable). PENDING (gated on Obsidian CLOSED): `graph.json` lineSize~=1.2/textFade~=0/`path:_System` colorGroup + remove junk `vault\.obsidian` registered-vault entry. Memory: `project_vault_graph_navigable_2026_06_18`.

## Active project
A decision-support betting product: model predictions for NBA / MLB / soccer-club / World-Cup(soccer_intl) / tennis, with full per-sport market surfaces, our own keyless odds aggregator (ESPN + Kalshi + Polymarket), line-shopping / arb / +EV, and a CLV ledger. Manual placement only; real money gated on proven CLV. Front end runs at http://127.0.0.1:8098/ (`python -m scripts.platformkit.frontend.serve`); React/shadcn UI in `scripts/platformkit/frontend/web/` (`npm run dev`).

## ACTIVE MILESTONE -- "First WORLD CUP prop edge, surfaced + paper-bet"
Plan LOCKED 2026-06-17: docs/research/revamp/REVAMP_DECISIONS_AND_PHASES.md (read it for the
full task graph + done-when + grounded probes). First vertical PIVOTED NBA->World Cup (NBA
offseason; WC = 3,927 live PrizePicks props NOW). Model = per-90 rate x E[minutes] ->
Poisson/NB; lines from Underdog/PrizePicks/FanDuel (probed keyless-OK) + DK via Playwright;
per-player stats from ESPN fifa.world rosters block (keyless, ~85% of prop volume). Surface
ALL edges tier-labeled (model-view/calibration/CLV); paper-only; CLV is the yardstick.

## NEXT (max 5 -- action | where | done-when) [MASTER PLAN v2 2026-07-02: .planning/PLAN_SELF_IMPROVING_AI.md -- 8 phases ending in PRE-REGISTERED proof-of-edge criteria (8.1a-g) + human-gated real-money pilot runbook (8.2); economics phase 6 (cost model/maker sim/beat-the-line scoreboard) decides if money ever happens; NBA-season readiness phase 7 before Oct. This queue = the Phase-1 head]
1. [P3.4] Widen the m19 asof-reclaim enumerator with the next tranche of on-disk leak-free candidates (ONDISK_RECLAIM_TARGETS.md) | scripts/platformkit/ceiling/ | new scoreboard rows for the tranche; REJECT expected, any SHIP_REVIEW surfaces to the human
2. FOLLOW-UP (not urgent): wire output_freshness.merge_freshness_into_services into the m5 autonomy monitor compose path -- deferred because status_composer.py + autonomy_monitor_runner.py both already sit AT the 300-LOC cap; needs its own trim-and-wire pass (mirrors the already-built-but-unwired autonomy.reaper_status_bridge precedent)
3. FOLLOW-UP: watch prop_close_capture_status.json's `captured` count for MLB -- now unblocked at the FanDuel sport-key level (see 07-02 DONE below) but still 0 as of 2026-07-02 because FanDuel had not yet posted player props for the day's games (~8h+ out); should start climbing automatically once props post closer to first pitch (m16 polls every 60s, no further code needed unless it stays 0 through a full game day)
4. FOLLOW-UP: a genuine 4th continuous keyless book (beyond pinnacle/fanduel/espn:DraftKings) is still open -- BetMGM's CDS API 403s from this environment (WAF/datacenter-IP block, external constraint, not a code gap; see 07-02 DONE below); the only other lever is spending OddsAPI paid-budget units (aggregate._oddsapi_affordable(), currently correctly OFF -- ODDS_API_KEY unset) or discovering a brand-new keyless source, neither attempted today
5. Pick the next item off the master plan (.planning/PLAN_SELF_IMPROVING_AI.md) -- Phase-2 queue is now thin; consider Phase 3 (ceiling/signal work) or Phase 6 (economics) next

DONE (2026-07-02): [P2.2] Feed-health scoreboard SHIPPED -- NEW scripts/platformkit/odds_provider/feed_health.py + feed_health_runner.py (registered as supervisor ProcSpec m30_feed_health, HEARTBEAT readiness, 600s cadence): live-probes every (provider, sport) pair the REAL slate uses (reuses aggregate.default_providers(), not a synthetic ping) and classifies GREEN (real data OR an honest empty/unsupported-sport degrade -- not an outage) vs RED (auth/forbidden/timeout/parse/unexpected-shape/exception -- the scraper is actually broken). LIVE-VERIFIED it catches a REAL fault: a live run hit an actual Pinnacle 401 Unauthorized on soccer_intl (transient rate-limit from repeated calls this session) that `aggregate()` had been silently swallowing (that venue just vanishes from the merged slate with no visible signal) -- feed_health correctly surfaced it as one RED row while mlb/espn/fanduel/kalshi/polymarket stayed GREEN. 16 new tests green (10 feed_health + 6 runner), both files well under 300 LOC (185, 115). Also updated tests/supervisor/test_manifest.py's service-set assertion for m30 (21 green).
DONE (2026-07-02): [P2.1] First new keyless book feed -- FOUND ALREADY SATISFIED, no code needed: live-verified `best_price_audit.audit(('mlb',))` -> max_books=3 (pinnacle+fanduel+espn:DraftKings), 9/10 games shoppable, matching the live best_price_scan.json on disk. The plan's premise ("live max_books=2") was itself stale -- the 2026-06-29 memory this session started from already recorded "only 3 shoppable" that day, so FanDuel's team-moneyline provider (odds_provider/fanduel.py, already in aggregate.default_providers()) had already closed this gap before today. Investigated a genuine 4th book: DraftKings-direct (via the curl_cffi-TLS-impersonation pattern prop_draftkings_v2.py already uses) is technically reachable -- probed live, confirmed working, found the real Game-Lines category/subcategory ids (league 84240, category 493, subcategory 4519 for MLB) -- but DK is ALREADY represented via ESPN's republished "espn:DraftKings" line, so a direct DK feed would duplicate a book already counted, not add independent line-shopping diversity. BetMGM (the one book with existing scaffolding, odds_provider/prop_betmgm.py) 403s live from this environment (WAF/datacenter-IP block -- confirmed by direct probe, an external constraint documented in the module's own docstring, not a bug). No further action; queued as a FOLLOW-UP (NEXT item 4) rather than forced.
DONE (2026-07-02): [P1.4] CLV-result reconciler SHIPPED + RESOLVES the paper_pm contradiction honestly -- NEW scripts/platformkit/clv/clv_result_reconciler.py: for a channel's measurable (true_close, non-suspect) settled rows, computes the CLOSE-IMPLIED expectation (Poisson-binomial normal approx for win count + EV for units, both driven by each bet's OWN fair_close_prob at the close) and z-scores the realized record against it. Live result on paper_pm (n=36, current numbers had moved since the plan was written: realized 16-20-0 net -4.01u, mean CLV +13.51%* significant): close-implied expected wins=18.06 (z=-0.69), expected units=+4.87 (z=-1.24) -- both |z|<1.96 -> VERDICT=GENUINE_VARIANCE, not a stale/fabricated close. Also mechanism-audited the close-capture path by hand before trusting the number: line_store.get_close's lock window is a real [tip-30min, tip] check (odds_provider/line_store.py:245-254); close_capture.py's LIVE Kalshi path can in fact never confirm is_proxy=False (a real-fetch always returns OddsEvent objects with no settlement-status field, so `is_settled` is hardcoded False at kalshi.py's normalization boundary) -- so every paper_pm true_close row in production is actually sourced from the line_store lock-window snapshot, a genuine pregame quote, not a Kalshi-settlement-price artifact. Ran the same reconciler on `moneyline` (n=28): also GENUINE_VARIANCE (max |z|=0.20). 12 new tests green, 241 LOC (<=300). NOT committed yet.
DONE (2026-07-02): [P1.2] Pregame prop close capture UNBLOCKED for MLB -- the capture pipeline (prop_close_capture_pregame.py -> prop_close_store -> prop_settler's true_close path) was ALREADY fully built and wired (from an earlier session) and IS running live (m16 daemon, fresh heartbeat, polling every 60s), but was capturing 0/168 open MLB pregame props because its only pregame two-way source, FanDuel (odds_provider/prop_fanduel.py), had NO customPageId entry for "mlb" at all -- 100% blocked at the sport-key level regardless of timing. FIXED: added "mlb":"mlb" to _PAGE_ID (live-probed: real page, 9 real MLB events surfaced) + added MLB player-prop market-name fragments to _PLAYER_STATS (total bases/rbis/home runs/stolen bases/earned runs allowed/hits allowed/walks allowed/batter+pitcher strikeouts/hits+runs+rbis -- deliberately NOT bare "hits"/"runs"/"strikeouts"/"outs", which collide with the real team/inning market titles observed live e.g. "1st Inning Hits", "Total Runs", "Runs Odd/Even", and would otherwise fabricate fake player props off a team market). Live-probed today's FanDuel MLB page: 9 real events, but games are ~8h+ out and only team/inning markets are posted so far, same "posts closer to game time" pattern already documented for soccer_intl -- so captured stays 0 TODAY, but the sport-key block that made it structurally impossible is gone; it activates automatically once FanDuel posts player props (no further code needed). 4 new regression tests (page-id resolves past "unsupported sport"; a real team/inning-market event correctly parses to 0 props; a real player-prop event parses correctly; provider end-to-end). 12/12 test_prop_fanduel.py green, canon_stat already had full MLB stat vocabulary (no change needed there). File stays 236 LOC (<=300). NOT committed yet.

DONE (2026-07-02): [P3.1] MLB moneyline DATA_LIMITED seam FIXED end-to-end -- edge_finder's mlb_moneyline market went from a permanent DATA_LIMITED (empty corpus) to a real MATCH verdict (n=2326 states, BSS=+0.0025, DM p=0.08 -- efficient mainline, honest null, exactly the expected result). TWO things done: (1) wired market_coverage/corpora.py's mlb_ml_states(root) to DELEGATE to odds_provider.oddsapi_close_corpus.build_states('mlb') instead of the old event_id-join that always returned [] (root is now unused/ignored -- kept only for signature compatibility with the other corpora builders); (2) FOUND+FIXED a real regression bug the wiring surfaced: oddsapi_close_corpus._mlb_result_fn built its date join key via `box["date"].astype(str).str[:8]`, which on today's datetime64-typed date column (domains.mlb.ingest_espn_box normalises dates that way, likely since WAKE-27's 07-01 box refresh) produced "2026-06-" (month-only, dashes) instead of "YYYYMMDD" -- silently zeroing the WHOLE join (build_states('mlb') was returning 0 states, not the 211 the 2026-06-26 memory recorded). Fixed to pd.to_datetime(...).dt.strftime("%Y%m%d") (mirrors the already-correct _soccer_result_fn pattern next to it) -> now yields 2326 real states, close Brier 0.2423. Added regression tests locking in both the date-format fix (test_oddsapi_close_corpus.py, 2 new tests) and the delegation (NEW test_corpora.py, 2 tests) since neither seam had direct per-file coverage before (only synthetic-injected builder tests existed). 64/64 tests green across the whole market_coverage + oddsapi_close_corpus suites; both edited files stay well under 300 LOC (corpora.py actually SHRANK, 167->145). No $ claim; the MATCH verdict is the expected honest result per the no-edge-claims discipline. NOT committed (local diff only -- ask before committing).

DONE (2026-07-02): [P1.1] Soccer in-game outcome resolver SHIPPED -- NEW scripts/platformkit/ingame/soccer_outcome.py (SoccerOutcomeResolver, ESPN-sourced, order-independent ticker->team-pair resolution, never guesses) + NEW domains/soccer_intl/ingest_espn_finals.py (ESPN scoreboard finals ingest, completed-only, mirrors domains.mlb.ingest_espn_box) + wired into ingame_paper_settle.py's score-fn dispatch (routes on KXMLBGAME/KXWCGAME ticker prefix; soccer finals auto-refresh each tick, 3-day UTC window, network-isolated from tests). LIVE RUN: settled all 9 stuck WC bets correctly (1 push/GERPAR draw, 4 losses, 4 wins matching real ESPN final scores incl. the CIV/COD FIFA-code overrides + ESPN's "Congo DR" spelling); idempotent on re-run (0 re-settled, MLB's 13 not-yet-final stayed open). 29 new/updated tests green, all <=300 LOC (soccer_outcome 200, ingame_paper_settle 251, ingest_espn_finals 177), ASCII, no $ field, executed=False, edge_claimed unset (matches existing MLB row convention), no flag flipped.
DONE (2026-07-02): [P1.3] Output-freshness sentinel SHIPPED for the 9 readiness=NONE daemons (m19-m27) -- NEW scripts/platformkit/ops_sentinel/ (output_freshness.py: declarative TABLE daemon->output-artifact->max_age_sec, GREEN/RED check + atomic write/load + a degrade-only merge_freshness_into_services bridge mirroring the existing-but-unwired autonomy.reaper_status_bridge pattern; output_freshness_runner.py: m29 loop wrapper, 300s cadence). Registered ProcSpec m29_output_freshness in supervisor/stack_specs.py (HEARTBEAT readiness, own heartbeat -- it is NOT itself readiness=NONE). DRIFT GUARD test asserts TABLE's keys == the real readiness=NONE ProcSpec name set from supervisor.stack_specs, so a future readiness=NONE daemon added without a sentinel row FAILS CI instead of going silently unmonitored. LIVE-VERIFIED against the real running stack: all 9 daemons GREEN (ages 194s-2399s, all well under threshold); simulated a wedge (future timestamp) -> correctly RED/stale. 49 tests green (13 ops_sentinel + 36 supervisor, incl. the pre-existing test_manifest.py service-set assertion updated for m29), all <=300 LOC (output_freshness 182, runner 114). NOT wired into the m5 autonomy monitor's live compose path yet (both status_composer.py and autonomy_monitor_runner.py are already AT the 300-LOC cap) -- flagged as a follow-up, not blocking; the sentinel's own output_freshness.json is independently useful today. Neither this nor P1.1 committed yet (local diff only -- ask before committing).

## P1->P7 LEDGER -- in-play edge vertical (in-game super-engine, proven vs REAL prices)
Frontier: the decisive combinable edge is IN-GAME conditioning. Build it end-to-end and prove it vs REAL captured in-play prices. Paper-only; CLV/calibration is the yardstick; no $-claims.
- [x] P1 HISTORICAL IN-PLAY ODDS -> CLV REPLAY HARNESS. DONE 2026-06-18: connectors got fetch_price_history (Kalshi candlesticks via api.elections.kalshi.com /series/{S}/markets/{T}/candlesticks; Polymarket clob /prices-history) -> scripts/platformkit/odds_provider/inplay_history.py (+test 5 green). Offline harness scripts/platformkit/forward_capture/inplay_clv_replay.py (leak-free: model sees series[:i+1] only; CLV sign matches src/betting/clv.py; gate BEAT/MATCH/BEHIND/INSUFFICIENT_DATA; +test 9 green). Ran e2e on 2 REAL series (Polymarket 200-tick + Kalshi 72-tick WC) -> naive + trend models both honestly MATCH/no-beat (mean_clv~0). HONEST LIMIT: fixtures are coarse hourly/daily candlesticks (multi-day span), NOT intra-game ticks -> fine in-play resolution comes from P2 live daemon. Real in-game BEAT attempt deferred to P3 super-engine.
- [~] P2 IN-PLAY CAPTURE. BUILT 2026-06-18: scripts/platformkit/odds_provider/inplay_snapshot_daemon.py (poll_inplay_once/serve_inplay_forever; fast 5s while live / idle 120s; per-sport isolated; atomic tmp+os.replace; freshness sidecar _freshness.json advances only on success) + inplay_feed.py (VENUE-NATIVE liveness: in-play iff open+not-settled+commence_time recently passed; futures/pregame/settled correctly excluded -- dodges the ESPN-id<->venue-id crosswalk landmine). 17 tests green; store=data/cache/inplay_history/<sport>/<date>.jsonl. Smoke: no game live this minute -> 0 captured (honest, not fabricated); injected live tick -> 1 captured (gate not trivially-zero). FOLLOW-UPS: (a) capture a REAL live-game intra-game series when a game is actually live (or via replay feed); (b) wire into supervisor for unattended run.
- [~] P3 IN-GAME SUPER-ENGINE. CLAUSE-1 DONE 2026-06-18 (SHIP): NBA pregame-prior detail layer beats (margin,time) BASE on held-out Brier, leak-free expanding-window WF -> scripts/platformkit/ingame/ingame_layer_gate_nba.py (+io, +test 4 green). Pooled OOS Brier 0.1676->0.1584; DM clustered p=4e-05; 3/3 folds; per-quarter pattern = helps most end-Q1 (+0.0187) least end-Q3 (+0.0012) = REAL early-game team-strength signal not shrink-artifact; noise-p0 control REJECTS (guards the added-flexibility artifact). CALIBRATION only, no $. CLAUSE-2 (graded vs REAL in-play prices): BRIDGE BUILT 2026-06-18 -> scripts/platformkit/ingame/live_grade.py (capture_pair_once pairs predict_live model_prob w/ venue in-play market_prob, HOME-side aligned, skips misaligned/None rather than fake; grade_game feeds pairs to inplay_clv_replay; single partial game = INSUFFICIENT_DATA by design; +test 10 green). predict_live is NON-gated in domains/<sport>/predictor.py. LIVE SMOKE honest result: model side WORKS (real NYY 0.5688, CAN 1.000) but MARKET side BLOCKED -- keyless venues exposed NO tradeable in-play moneyline for tonight's live MLB games via the live fetch path, and available soccer markets are stale futures -> 0 real pairs, correctly skipped. ==> THE binding constraint on proving an in-game beat is VENUE IN-PLAY COVERAGE, not the model. PROBED + CONFIRMED 2026-06-18: Kalshi KXMLBGAME markets are LISTED but UNTRADED/ILLIQUID (live CWS@NYY: yes_bid/ask/last/vol all None; 360 1-min candles all price=None; all 20 open KXMLBGAME same) -> NO real tradeable in-play price exists to grade against on our keyless venues for the live slate. External liquidity reality, not a wiring gap. NEXT to close clause-2: (a) NBA in-season live capture (Oct+), OR (b) monitor any venue for a LIQUID in-play market + capture+grade when one appears, OR (c) add a liquid in-play exchange source (e.g. Betfair historical/live -- NOT keyless). Then aggregate MANY games (one game = variance not signal). Detail-layer ladder (pace/rest/quarter-shape) can also keep growing, each gated.
- [~] P4 SELF-IMPROVEMENT ALWAYS-ON. SUBSTANTIALLY DONE 2026-06-18: reused improve/ 5-gate ratchet + registry; added continuous glue scripts/platformkit/improve/{selfimprove_daemon,artifact_store,checkpoint}.py -> run_cycle/run_forever, versioned artifacts data/cache/improve/artifacts/<name>/v%04d.json + atomic current pointer (keep-prev-10), auto-rollback on regression, checkpoint.json resume (lossless), replication-gated (>=2 corpora else REPLICATION_PENDING), proposals.jsonl emission (never MEMORY.md/data/registry/, never flips flags). 7 tests green; smoke consumed real in-game verdict -> 3 versioned artifacts + proposals + clean restart. FOLLOW-UP: run unattended under supervisor (P7) + widen enumerator.
- [x] P5 PREDICTION + EXECUTION API DONE 2026-06-18: completed existing Auto-API :8099 (predict_service/app.py reads ONE store data/frontend/predict_service/<sport>/latest.json). Added frontend/exec_decision.py (Shin EV via odds_shop, tier floors A>=.08/B>=.04/C>=.02 +.01 proxy, below-floor=no_bet, flat-unit + capped quarter-Kelly UNITS only) + frontend/bestbets_routes.py (GET /api/v1/bestbets/{sport}[/{game_id}] = line-shop+devig + decision + in-game number via report._live + CLV scoreboard). Pregame GET /api/predict/{sport}; in-game GET /api/report/{sport}/{game_id}. CONFIRMED no $/roi/pnl field anywhere, below-floor=no_bet, CLV present, edge_claimed=False. Contract test 7/7 + existing 7/7 green (non-breaking).
- [~] P6 FRONT END (CourtVision) DONE 2026-06-18 (build+served-markup verified; no pixel render in-env): webapp/ (Next.js 14.2 courtvision-live) gained /p6 dashboard + /p6/<sport>/<game_id> reading P5 APIs (never recomputes); panels Slate/GameReport/BestBets(units,tier,decision)/ClvScoreboard/ParityGrid/RatchetPanel/PaperTrail; SSE (game report + paper) w/ poll fallback; NO $ column (stakes in units); degrades to "Unavailable" w/ honest reason. Added frontend/status_routes.py -> /api/improve/status + /api/parity. npm build PASS, tsc clean, served HTML on /p6 + game page HTTP 200 w/ real panels; +3 backend tests. Files in webapp/ (lib/p5api.ts,useStream.ts; app/p6/*; components/p6/*). FOLLOW-UP: headless browser screenshot when a driver is available.
- [~] P7 AUTONOMY DONE 2026-06-18 (in-env proven; real reboot pending): supervisor/ already mature (manifest->boot->supervise->drain, readiness, capped-backoff, isolation, atomic status.json) -> added P2 daemon (inplay_runner.py) + P4 daemon (selfimprove_runner.py, MEASUREMENT-ONLY defaults so nothing ships/no flag flip) w/ heartbeat probes, wired into manifest/ops/status.json (9 services). Governance preflight exits 0, real-money default-DENY. VERIFIED offline: kill one service -> auto-restart + others stay up (isolation); P4 resumes from checkpoint (no reprocess); P2 capture resumes; ops doctor PASS; 47 tests green. Autostart register_autostart.ps1 + watchdog_autostart.ps1 BUILT + -DryRun tested, NOT registered (human go-live = `.\register_autostart.ps1 -Register`). Needs real reboot to confirm OS AtLogOn trigger.

## RECENT DONE (max ~7; older -> DONE.md)
- FRONT-END BEST-BETS ROUTE UN-HUNG -- MLB paper trail live (2026-06-27): `GET /api/v1/bestbets/{sport}` (:8099, the UI's best-bets+execution panel) hung >30s for MLB (16 games) and timed out the dashboard, though /api/predict/mlb was fast. ROOT = TWO O(games) costs in the cold path: (1) `_collect_markets` called `line_store.get_latest(game_id)` with a BARE id (no sport hint) -> globbed EVERY sport's line_history (222M) + re-parsed per game (~4s x16 = ~64s); (2) per-game live ESPN read `_live_for` ran SEQUENTIALLY (~1.3s x16 = ~22s). FIX: added `line_store.get_latest_batch(sport, game_ids)` (one pass over just that sport's files, indexed by game_id; edge_api threads per-game quotes through, `{}` on no-history so it never re-scans) = 64s->1.8s; and `_decide_and_decorate` runs live reads CONCURRENTLY (ThreadPoolExecutor 12w + 8s deadline; slow games -> clean `unavailable` sentinel) = ~22s->capped. CRITICAL 3rd fix: the `with ThreadPoolExecutor()` __exit__ does shutdown(wait=True) which BLOCKED until every live read finished even after the deadline fired (a LIVE box-score read takes 20s+ -> cold MLB still 30s once games went live); switched to explicit pool + `shutdown(wait=False, cancel_futures=True)` so the response returns AT the deadline -- this made the deadline REAL and also fixed soccer_intl (32s->10.6s). All sports now ~10s cold / 0.05s cached. (Tried a startup warm = DROPPED: thundering-herds ESPN, and the m10 daemon board /api/bestbets/board already serves an always-warm best-bets view.) VERIFIED LIVE: bestbets/mlb 16 games / 11 best_bets (units only -- flat_unit+kelly_units+stake_units, edge+EV+tier, edge_claimed=False, NO $); /api/paper/trail 200 settled MLB rows + CLV scoreboard; front-end :3000 /p6 200; flywheel running (selfimprove_runner+inplay_runner+auto_loop+scheduler). Files frontend/{edge_api,bestbets_routes}.py + scripts/platformkit/odds_provider/line_store.py + test_line_store.py (+2 tests, 11 green); all <=300 LOC, ASCII, local-only, no push. NOTE: soccer_intl still ~32s cold (latent, secondary to MLB). Py3.10 gotcha: concurrent.futures.TimeoutError != builtin. Memory: gotcha-bestbets-route-hang-linehistory-livereads.
- ODDS-API HISTORICAL = ONE-SESSION ACQUISITION, REFOCUSED TO MLB+WC, WIRED INTO AI (2026-06-26): strategy = use the PAID odds API for ONE session to grab historical, then run INDEPENDENTLY on our own keyless live scrapers + this corpus. Made scripts/platformkit/odds_provider/oddsapi_team_backfill.py MULTI-SPORT (SPORT_KEYS nba/mlb/soccer_intl; the gated client is NBA-locked so I fetch via its sport-agnostic _gate_or_fetch internal) with a PREGAME GUARD (keep event iff commence_time>snapshot_ts -> in-play MLB lines, still 2-way, no longer masquerade as closes) and n-way Shin devig (soccer 3-way 1X2 -> probs sum to 1). Active sports first: KILLED the mis-prioritized NBA full-run (offseason; ~80 units lost), ran WC (complete: 16 dates, 2107 rows, 920u) + MLB (recency-first 2026->2025, cap 17.6k, running in bg). us,eu=Pinnacle anchor; holds ~2-3%, 17-30 books; 0 in-play rows kept. THEN built the AI BRIDGE scripts/platformkit/odds_provider/oddsapi_close_corpus.py (+6 tests): select_closes picks THE CLOSE per (event,market)=latest pregame snapshot w/ anchor devig; joins realized outcomes (mlb espn_boxscores via team_resolver.canonical abbr<->name; soccer_intl results.parquet full-name) -> corpora.py-compatible states (game_id,home,away,state_ts,outcome,devig_close_prob). THIS FILLS THE MLB SEAM that was DATA_LIMITED (market_coverage/corpora.mlb_ml_states returned [] -- no joinable local close). JOIN BUG fixed: key outcomes on commence date + ET-prior-day (late UTC games are prior ET day) -> MLB labeled 35->211 states, close Brier 0.250 @ 50.7% home base = the market benchmark the AI must beat OOS; WC 11 decisive (76 h2h closes, 19 played, ~8 draws excluded from 2-way frame -- honest). 17 new tests. CALIBRATION only, NO $/ROI. NEXT: (a) let bg MLB run finish -> rebuild corpus; (b) one-line wire build_states('mlb') into edge_finder.py:129 (replaces the [] DATA_LIMITED mlb path) so recalibrator/eval-gate score vs the close; (c) point select_closes at live-capture JSONL too = old+live blend for continuous self-improve; (d) NBA/MLB spreads+totals already captured for later. The 5 NBA seed dates from the prior turn remain valid in nba_team_strength.jsonl.
- 4-SPORT IN-GAME CALIBRATION = NBA-LEVEL + LIVE-SERVED (2026-06-18): 3/4 sports REPLICATED in-game prior-conditioning calibration beat (NBA fine-res DM p=1.2e-12; TENNIS after p1/winner sign-bug fix, corr 0.01->0.72; SOCCER after team-name->Elo fix, fallback 42.9%->0%, both dirs p<0.002); MLB honest characterized NULL (prior+late-inning+leverage+half+base-out[100% cov] all reject; run_diff+innings near-sufficient). Generic sport-blind gate w/ degenerate-base honesty guard (caught + killed a FALSE tennis 'replicated'). LIVE-SERVED for in-season sports: ingame_serve.py persists per-sport models (data/cache/ingame/models/<sport>_ingame.json, proven/base provenance) + ingame_live_state.py keyless ESPN extraction + GET /api/ingame/{sport}/{game_id}; vs_close UNPROVEN, no $. ~150 new tests across ingest+gate+serve. CALIBRATION not market. (2026-06-18)
- 4-SPORT IN-GAME GATE built + honestly graded (2026-06-18): generalized the proven NBA in-game gate into a sport-blind module (scripts/platformkit/ingame/ingame_gate_generic.py+_models, DEGENERATE-BASE honesty guard) + ingested in-game state trajectories for MLB/soccer/tennis (domains/<sport>/ingest_*states*; data/cache/ingame/<sport>_states__*.parquet). VERDICTS: NBA REPLICATED (real); MLB REJECT (strong base, prior adds nothing); SOCCER PARTIAL (real beat 1 dir p=0.045, underpowered 180g/corpus); TENNIS INVALID_BASE (set-level only, corr 0.009 -- suspected reconstruction bug, killed a FALSE 'replicated'). ~90 new tests across 4 ingest+gate modules. Follow-ups in flight: soccer power-up, tennis bug hunt, MLB detail ladder. CALIBRATION not market. (2026-06-18)
- P3 c1 CALIBRATION BEAT REPLICATED CROSS-CORPUS + CONFIRMED AT FINE RESOLUTION (2026-06-18): ingested NBA 2024-25 linescores (1321 games) + FULL 2-season PBP scoring trajectories (23 as-of states/game, zero drops) -> ingame_crossval_nba.py (Q-boundary) + ingame_finegrain_nba.py (~2-min). BOTH gates, BOTH directions: +PRIOR beats BASE on held-out Brier (Q-bnd A->B 0.1706->0.1613; fine A->B 0.1684->0.1583 clustered DM p=1.2e-12, B->A 0.1716->0.1647 p=1.2e-4), early-helps-most pattern holds, per-GAME clustering applied (shrinks DM 3.3x vs iid, still significant). VERDICT=REPLICATED at serving resolution. Now a CONFIDENT, proof-rail-complete in-game CALIBRATION beat over a margin/time base (leak-free+WF+OOS+2 seasons+DM+mechanism) -- NOT a market beat (that's liquidity-blocked clause-2). Also: P5 execution math independently reviewed CLEAN (Shin-devig edge, tier floors, capped quarter-Kelly, units-only no-$, edge_claimed=False). 4-sport parity GREEN. (2026-06-18)
- P3 LADDER EXHAUSTED + P4 DAEMON BUILT (2026-06-18): 3 new in-game detail layers (pace/total-variance, momentum/run, home-bias) all HONEST REJECT vs BASE+PRIOR (no DM-significant OOS Brier gain) -> ingame_ladder_nba.py(+layers,+7 tests); linescore-derived in-game signal is exhausted, more lift needs PBP-rich state not Q-snapshots. P4 self-improve daemon built on existing improve/ ratchet (selfimprove_daemon+artifact_store+checkpoint, 7 tests, real smoke staged 3 artifacts+proposals+resumed). Honest, no $. (2026-06-18)
- IN-GAME SUPER-ENGINE P3 (2026-06-18): CLAUSE-1 SHIP -- NBA pregame-prior detail layer beats (margin,time) base on held-out Brier (0.1676->0.1584, DM p=4e-05, 3/3 folds, per-quarter pattern = real early-game team-strength signal, noise-p0 control rejects) -> ingame_layer_gate_nba.py. CLAUSE-2 bridge built (live_grade.py, 10 tests): model side works live, but proving an in-game BEAT is blocked by VENUE in-play coverage (no tradeable in-play moneyline exposed for tonight's MLB; NBA offseason; soccer markets are futures). Honest: stack fully wired+ready, awaiting a live in-play price to grade against. CALIBRATION only, no $. (2026-06-18)
- IN-PLAY EDGE VERTICAL P1 done + P2 substantially done (2026-06-18): own connectors now fetch real in-play price history (Kalshi candlesticks + Polymarket prices-history -> inplay_history.py); offline leak-free in-play CLV replay harness (inplay_clv_replay.py) gives honest BEAT/MATCH/BEHIND/INSUFFICIENT_DATA -- ran e2e on 2 REAL series, baseline correctly MATCH/no-beat; venue-native in-play capture daemon (inplay_snapshot_daemon.py + inplay_feed.py). Adversarial review CLEAN (leak-free, CLV sign correct). ~5 new modules under scripts/platformkit, 31 new tests green. Paper-only, no $-claims. Parity GREEN (4/4). REAL in-game BEAT attempt deferred to P3 super-engine. (2026-06-18)
- OVERNIGHT IMPROVEMENT CYCLE (autonomous, 2026-06-17->18): added TRUE-CLV capture (prop_line_history.py logs lines each tick -> CLV-vs-close in prop_summary; loop restarted w/ it). Built + HONESTLY MEASURED 2 model levers, both correctly held back: opponent-adjustment (team_defense.py, leak-free, wired live) = measured NULL on the thin 1-round WC slice (opp has no strictly-earlier history yet; activates as rounds accrue); isotonic P(over) recal (prop_recal.py) = DEFER, proper temporal train/test shows it OVERFITS 24 matches (OOS Brier slightly worse) -- module ready, NOT applied live. Meta: WC is data-limited (24 matches); MLB (full season) is the higher-value next vertical. All per-file tests green; paper-only; no $-claims (2026-06-18)
- WORLD CUP PLAYER-PROP VERTICAL built end-to-end (Milestone 1, plan REVAMP_DECISIONS_AND_PHASES.md): snapshot backbone (compute-once -> snapshots/<sport>.json, serve reads it, refresh_daemon); deep prop scrapers (Underdog two-way priced + PrizePicks pick'em live; FanDuel parser ready/props-not-posted; DK=Playwright-deferred); ESPN per-player WC ingest (1241 rows/24 matches); Poisson/NB prop engine w/ dispersion calibration + EV honesty guard; resolver (98% name hit) + prop_edge board (/api/props, in snapshot); prop settlement + paper-CLV loop (records ONLY reliable+ok edges -> 0 today, honest); CLUB-SEASON PRIORS via ESPN athlete overview = the unlock (0 -> 321 reliable edges on a 36-player sample). ~15 new modules, all per-file tests green, all in scripts/platformkit + domains/soccer. PAPER ONLY, tier-labeled, no $-edge claims (2026-06-17)
- Clickable per-game BET BOARD: GET /api/game returns every market ranked (best_bets + groups) across all sports; React detail Dialog built (builds clean). Honest: EV only where priced, else MODEL_VIEW (2026-06-17)
- LIVE in-game tracking working end-to-end: ESPN keyless feed -> predict_live -> /api/live; verified on live MLB (4 games) + World Cup (England 1-1 Croatia 40' -> 45.1% as Croatia equalized) (2026-06-17)
- In-game bugs fixed: soccer_intl gained a predict_live (was pregame-only); MLB live anchored to Elo; NBA buzzer = exact 1.0 (2026-06-17)
- Paper/auto-bet loop verified SAFE: no real-money path (place_order stubs raise, ENABLED=False, gate needs BEATS_CLOSE); paper_autobet.py built; CLV loop end-to-end (2026-06-17)
- Claude-org reorg: memory 33.5KB->20.4KB; .planning/NOW.md SSOT; rules now load (@-imports); guard hooks wired+verified; skills consolidated (2026-06-17)
- Full per-sport market coverage built (NBA/MLB/soccer/tennis), 45 tests; soccer gained a real 1X2 moneyline (2026-06-17)
- Own keyless odds API (odds_provider/) + line-shop/arb/EV + CLV ledger; MLB odds attach via team_resolver (2026-06-17)
- Adversarial-review loop fixed 2 confirmed bugs (wrong-game odds match; MLB integer-line push) (2026-06-17)
- React+shadcn UI scaffolded + builds; World Cup wired into the board (2026-06-17)
- CRPS+pinball distributional metrics shipped to eval_gate/scoring.py (C7) (2026-06-17)

## Active blockers
- WC prop edges: reliability depends on CLUB priors (1 WC match/player so far). Sample of 36 -> 321 reliable; FULL ~1200-player prior build running in bg -> then snapshot has reliable edges slate-wide. CLV proof needs forward settlement to accrue (paper loop records reliable+ok only).
- WC prop CLV-vs-close not yet computable: DFS/book CLOSING prop lines aren't captured yet (need refresh_daemon to log line movement). Until then prove via P(over) calibration + realized ROI at taken price (Underdog two-way) -- honest, not CLV.
- Self-improve loop running but improve verdict = INSUFFICIENT_DATA until ~60 real games settle (cold start; accrues automatically).
- Early paper ROI is NEGATIVE (-47% on 7 bets, small-N): the naive permissive paper policy bets soft-book "+EV" artifacts on a model that matches the close. Expected; CLV/larger-N is the real verdict, not this.
- Live odds limited to ESPN's single republished line until a 2nd venue matches -> no real arb yet.
- Real-money execution: GATED on proven positive CLV (paper-first). Loop is the gate's data source.
- HUMAN-CONFIRM list from the 2026-06-16 org sprint still pending (public push, key rotation, MCP registration).

## Pointers (links, never inline)
- Betting product research: docs/research/betting-product/
- Claude-org reorg research: docs/research/claude-org/
- Memory frontier entries: memory `project-betting-product-research-2026-06-17`, `project-betting-frontend-intl-mlb-2026-06-17`
